# ferro-whatsapp

WhatsApp Business Cloud API integration for the Ferro framework. Provides outbound message sending (text and template messages) via the Meta Cloud API v23.0, inbound webhook processing with HMAC-SHA256 verification, and wamid-level message deduplication with a pluggable `DeduplicationStore` trait.
