pub(super) const SOURCE: &str = r#"
    // ── Date picker (Plan 03 fills this) ─────────────────────────────────────
    //
    // Calendar grid inside native <dialog> + showModal(). Italian locale
    // (Monday-first, Italian month/day names). WAI-ARIA arrow-key grid
    // navigation. Time = keyboard-navigable list via shared listbox engine
    // (D-05). 15-minute slot granularity.

    function setupDatePicker() {}
"#;
