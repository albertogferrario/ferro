pub(super) const SOURCE: &str = r#"
    // ── Combobox (Plan 03 fills this) ────────────────────────────────────────
    //
    // Progressive enhancement of native <select> elements with searchable
    // opt-in (SelectProps.searchable = true). Native <select> remains the
    // form value carrier (D-06). Client-side filtering via shared listbox
    // engine (D-05). Idempotent via data-combobox-init guard.

    function setupCombobox() {}
"#;
