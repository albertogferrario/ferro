//! `ferro make:api-key` command — generates API keys for authentication.
//!
//! Placeholder for parallel plan implementation.

/// Run the `make:api-key` command.
#[allow(dead_code)]
pub fn run(_name: String) {
    eprintln!("make:api-key is not yet implemented");
    std::process::exit(1);
}
