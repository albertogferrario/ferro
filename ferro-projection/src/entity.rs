//! SeaORM `Entity` / `Model` / `ActiveModel` for the
//! `projection_snapshots` table.
//!
//! This file is a STUB. Plan 155-03 lands the full
//! `#[derive(DeriveEntityModel)]` Model with composite primary key
//! `(projection_name, key)`, the JSON `state` column, the `i64` version
//! column, and the round-trip unit test.

#![allow(dead_code)]

/// Placeholder for the SeaORM Entity — full body lands in plan 03.
pub struct Entity;

/// Placeholder for the SeaORM Model — full body lands in plan 03.
pub struct Model;

/// Placeholder for the SeaORM ActiveModel — full body lands in plan 03.
pub struct ActiveModel;
