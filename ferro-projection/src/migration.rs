//! SeaORM migration for the `projection_snapshots` table.
//!
//! This file is a STUB. Plan 155-03 lands the full migration with
//! composite PK declaration via `Index::create().col(...).col(...)`
//! and the `sqlite_master` smoke test.

#![allow(dead_code)]

/// Placeholder for the SeaORM migration — full body lands in plan 03.
/// Re-exported from `lib.rs` as `CreateProjectionSnapshotsTable` (D-23).
pub struct Migration;
