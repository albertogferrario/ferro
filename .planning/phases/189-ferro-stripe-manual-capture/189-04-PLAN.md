---
phase: 189-ferro-stripe-manual-capture
plan: 04
type: execute
wave: 4
depends_on: [189-01, 189-02, 189-03]
files_modified:
  - docs/src/features/stripe.md
autonomous: true
requirements: [STRIPE-MC-05]
tags: stripe, docs, manual-capture

must_haves:
  truths:
    - "docs/src/features/stripe.md has a '## Manual Capture' section between '## Stripe Connect' and '## Webhook Configuration'"
    - "The section documents the full authorize-at-checkout to capture/cancel flow with code examples using the real shipped API"
    - "The section contains a correspondence table mapping ferro-reservation hold/commit/release to Stripe authorize/capture/cancel"
    - "The section documents operational realities: ~7-day window, auto-cancellation surfacing as the Canceled event, partial-capture remainder auto-released"
  artifacts:
    - path: "docs/src/features/stripe.md"
      provides: "Manual Capture documentation section"
      contains: "## Manual Capture"
  key_links:
    - from: "docs Manual Capture section"
      to: "payment_intent::capture / cancel API"
      via: "code examples + correspondence table"
      pattern: "payment_intent::capture"
---

<objective>
Document Stripe manual capture end-to-end in `docs/src/features/stripe.md`: authorize-at-checkout, full/partial capture, cancel, the webhook lifecycle, Connect composition, and the `ferro-reservation` hold/commit/release to authorize/capture/cancel correspondence.

This plan delivers STRIPE-MC-05. It implements D-09 (new "Manual capture" section), D-10 (correspondence table, semantic parallel with explicitly no compile dependency), and D-11 (operational realities: ~7-day window, auto-cancellation, partial-capture remainder release). It runs last so every code example matches the actually-shipped API from Plans 01-03.

Purpose: docs are a phase success criterion, not an afterthought. gestiscilo and any future consumer reads this section to wire booking-deposit holds.
Output: one new `## Manual Capture` section in stripe.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/189-ferro-stripe-manual-capture/189-CONTEXT.md
@.planning/phases/189-ferro-stripe-manual-capture/189-RESEARCH.md
@.planning/phases/189-ferro-stripe-manual-capture/189-PATTERNS.md

<interfaces>
Shipped API to document (from Plans 01-03). Examples MUST match these signatures.

Builder (Plan 01): CheckoutBuilder::new(Mode::Payment).manual_capture() — no args, authorizes without charging; composes with .destination(connect_id, Some(fee)); requires .idempotency_key() before .create(). manual_capture() with Mode::Subscription returns Error::ManualCaptureRequiresPaymentMode from create() before any network call.

payment_intent module (Plan 02), accessed as ferro_stripe::payment_intent::*:
- payment_intent::capture(id, None) — full capture
- payment_intent::capture(id, Some(2000)) — partial capture of 2000 cents
- payment_intent::cancel(id) — release hold
- payment_intent::retrieve(id) — poll auth state

Typed events (Plan 03):
- StripePaymentIntentAmountCapturableUpdated { event_id, payment_intent_id, amount_capturable_cents, currency, metadata }
- StripePaymentIntentCanceled { event_id, payment_intent_id, cancellation_reason, metadata }

Doc anchor: insert AFTER the `## Stripe Connect` section (ends ~line 229, after the Destination Charges example) and BEFORE `## Webhook Configuration` (line 231).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Manual Capture documentation section</name>
  <read_first>
    - docs/src/features/stripe.md (read the whole file; match its neutral scientific voice per CLAUDE.md; mirror the `## Stripe Connect` / `### Destination Charges` section lines 175-229 for code-example style and the webhook event dispatch table for table style; insertion point is after line 229, before line 231 `## Webhook Configuration`)
    - .planning/phases/189-ferro-stripe-manual-capture/189-CONTEXT.md (D-09, D-10, D-11 — exact content requirements)
    - .planning/phases/189-ferro-stripe-manual-capture/189-PATTERNS.md (docs section lines 462-489 — section structure + correspondence table format)
    - .planning/phases/189-ferro-stripe-manual-capture/189-RESEARCH.md (Secondary sources note — the ~7-day window is training-knowledge; hedge with "approximately")
  </read_first>
  <action>
    Insert a new `## Manual Capture` section into `docs/src/features/stripe.md` immediately after the `### Destination Charges` code block (after line 229, before `## Webhook Configuration` on line 231). Use the file's existing neutral documentation voice — no marketing language. The section MUST contain all seven elements below.

    ELEMENT 1 — Intro (1-2 sentences): Manual capture authorizes card funds at checkout without charging them, then captures (charges) some-or-all of the authorized amount later, or cancels (releases) the hold. Useful for booking deposits.

    ELEMENT 2 — "Authorize at checkout" subsection with a Rust code block using the real shipped API. The example builds a CheckoutBuilder::new(Mode::Payment) with .manual_capture(), a LineItem named "Booking deposit" at unit_amount_cents 5000 / quantity 1 / currency "usd", success_url and cancel_url, .idempotency_key("booking-deposit-42"), then .create().await?. Below the block, note that manual_capture() is only valid with Mode::Payment; calling it with Mode::Subscription returns Error::ManualCaptureRequiresPaymentMode from create() before any network call.

    ELEMENT 3 — "Capture and cancel" subsection with a Rust code block (use ferro_stripe::payment_intent;) showing all four calls with comments:
      - payment_intent::capture(&payment_intent_id, None).await? — full capture (charge entire authorized amount)
      - payment_intent::capture(&payment_intent_id, Some(2000)).await? — partial capture of 2000 cents; Stripe auto-releases the remainder
      - payment_intent::cancel(&payment_intent_id).await? — release the hold without charging
      - payment_intent::retrieve(&payment_intent_id).await? — poll current authorization state

    ELEMENT 4 — "Webhook lifecycle" subsection with a markdown table (3 columns: Stripe event | Typed event | Meaning):
      - `payment_intent.amount_capturable_updated` | `StripePaymentIntentAmountCapturableUpdated` | Funds authorized and capturable (hold is live)
      - `payment_intent.canceled` | `StripePaymentIntentCanceled` | Hold released (manually or by Stripe auto-expiry)
    Below the table, note these parse like any other typed event and dispatch through SyncDispatcher / the queue path; register handlers the same way as existing events.

    ELEMENT 5 — Operational realities (D-11), as prose or a bullet list, with hedged language per the RESEARCH Secondary-source note:
      - Stripe holds a card authorization for approximately 7 days; uncaptured PaymentIntents are auto-cancelled after the window expires, surfacing as a `payment_intent.canceled` event whose `cancellation_reason` indicates automatic expiry.
      - A partial capture charges the specified amount and Stripe automatically releases the remainder of the authorization.

    ELEMENT 6 — Connect composition (links STRIPE-MC-04), one or two sentences: manual_capture() composes with destination(). The authorization is created on the platform account; on capture, Stripe performs the transfer to the connected account per the destination-charge pattern. Capture and cancel are platform-scoped.

    ELEMENT 7 — Correspondence with ferro-reservation (D-10): a markdown table (2 columns: ferro-reservation | Stripe PaymentIntent):
      - `hold()` | Authorize at checkout (manual_capture() sets capture_method=manual)
      - `commit()` | `payment_intent::capture(id, amount)`
      - `release()` | `payment_intent::cancel(id)`
    Immediately followed by this exact framing sentence: "This is a documented semantic correspondence — a convention for pairing a reservation hold with a payment authorization. There is no compile-time dependency between the two crates."

    Keep the section consistent with the file's heading depth: `##` for the section, `###` for subsections.
  </action>
  <verify>
    <automated>grep -q "^## Manual Capture" docs/src/features/stripe.md && grep -q "payment_intent::capture" docs/src/features/stripe.md && grep -q "ferro-reservation" docs/src/features/stripe.md && grep -q "StripePaymentIntentAmountCapturableUpdated" docs/src/features/stripe.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^## Manual Capture" docs/src/features/stripe.md` (section heading present at `##` depth)
    - The section sits between Stripe Connect and Webhook Configuration: `grep -n "^## " docs/src/features/stripe.md` shows "## Manual Capture" with a line number greater than "## Stripe Connect" and less than "## Webhook Configuration"
    - `grep -q "manual_capture()" docs/src/features/stripe.md` (builder example present)
    - `grep -c "payment_intent::capture" docs/src/features/stripe.md` >= 1 AND `grep -q "payment_intent::cancel" docs/src/features/stripe.md` AND `grep -q "payment_intent::retrieve" docs/src/features/stripe.md`
    - `grep -q "ManualCaptureRequiresPaymentMode" docs/src/features/stripe.md` (mode-guard documented)
    - `grep -q "StripePaymentIntentAmountCapturableUpdated" docs/src/features/stripe.md` AND `grep -q "StripePaymentIntentCanceled" docs/src/features/stripe.md` (webhook table)
    - `grep -q "payment_intent.amount_capturable_updated" docs/src/features/stripe.md` AND `grep -q "payment_intent.canceled" docs/src/features/stripe.md` (exact event strings)
    - `grep -qi "approximately 7 days\|~7 days\|7 days" docs/src/features/stripe.md` (D-11 window documented)
    - `grep -q "hold()" docs/src/features/stripe.md` AND `grep -q "commit()" docs/src/features/stripe.md` AND `grep -q "release()" docs/src/features/stripe.md` (correspondence table)
    - `grep -q "no compile-time dependency\|no compile dependency" docs/src/features/stripe.md` (D-10 no-coupling framing)
  </acceptance_criteria>
  <done>A `## Manual Capture` section exists between Stripe Connect and Webhook Configuration, documenting authorize/capture/cancel with real-API examples, the mode guard, the 2 webhook events, operational realities (D-11), Connect composition (STRIPE-MC-04), and the ferro-reservation correspondence table (D-10) with the no-compile-dependency framing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → consumer developer | docs shape how consumers wire payments — incorrect docs propagate to consumer payment code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-189-11 | Information Disclosure | docs code examples | mitigate | Examples use placeholder ids ("acct_xxx", "booking-deposit-42") and example.com URLs only — no real keys, accounts, or tenant identifiers (CLAUDE.md project-agnostic rule). |
| T-189-12 | Tampering (indirect) | idempotency caveat omission | mitigate | The docs (and the payment_intent module doc) must state that application-layer dedup is required for capture — omitting it would lead consumers to assume Stripe-layer idempotency and risk double-captures. The capture caveat from Plan 02 is reinforced here. |
</threat_model>

<verification>
- `grep -q "^## Manual Capture" docs/src/features/stripe.md` and the section is positioned between Stripe Connect and Webhook Configuration
- All shipped-API identifiers (manual_capture, payment_intent::capture/cancel/retrieve, both event structs) appear in the section
- Correspondence table and no-compile-dependency framing present
- Optional: if mdbook is available, `mdbook build docs` succeeds (do NOT block on this if mdbook is not installed)
</verification>

<success_criteria>
- ROADMAP SC-5: docs document manual capture end-to-end and the hold/commit/release to authorize/capture/cancel correspondence with ferro-reservation — VERIFIED by grep acceptance criteria
</success_criteria>

<output>
After completion, create `.planning/phases/189-ferro-stripe-manual-capture/189-04-SUMMARY.md`

NOTE — Phase close gate (run by the executor of this final plan, serialized per thermal constraint, ONE cargo op at a time):
  1. `cargo fmt --all -- --check`
  2. `cargo clippy --all --all-targets -- -D warnings`
  3. `cargo test --all-features`
All three must pass before the phase is considered complete. Then bump the ferro-stripe / workspace version per the publish convention (publish-update token is sufficient — ferro-stripe is an existing crate, no manual bootstrap needed) when the user authorizes the push.
</output>
