---
status: complete
phase: 140-core-reshape
source: [140-01-SUMMARY.md, 140-02-SUMMARY.md, 140-03-SUMMARY.md, 140-04-SUMMARY.md, 140-05-SUMMARY.md]
started: 2026-04-20T00:00:00Z
updated: 2026-04-20T01:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Workspace builds clean
expected: Run `cargo build --all --all-features`. Exits 0 with no errors.
result: pass

### 2. ferro-stripe tests pass
expected: Run `cargo test -p ferro-stripe`. All 25+ tests pass (idempotency, events, verify, testing, client, checkout, config).
result: pass

### 3. Stripe::with creates scoped client
expected: `Stripe::with(api_key)` returns a `stripe::Client` without touching the global static. Running `cargo test -p ferro-stripe client::` passes including `with_does_not_populate_global_static`.
result: pass

### 4. CheckoutBuilder enforces idempotency key
expected: `CheckoutBuilder::new(Mode::Payment).create()` without calling `.idempotency_key()` returns `Err(Error::MissingIdempotencyKey)`. Test `checkout_create_missing_key_returns_err` in `cargo test -p ferro-stripe` passes.
result: pass

### 5. ProcessedEventLog deduplication
expected: `MemoryProcessedLog::try_mark_processed("evt_123")` returns `true` on first call and `false` on second call for the same event ID. Test `memory_log_true_then_false` passes.
result: pass

### 6. Product-axis directories deleted
expected: `ls ferro-stripe/src/` shows no `connect/` or `subscription/` directories. `webhook/handler.rs` is also gone. The capability-axis layout is: `checkout.rs`, `refund.rs`, `account.rs`, `idempotency.rs`, `webhook/{verify,sync,queue,events,mod}.rs`.
result: pass

### 7. SubscriptionInfo moved to framework
expected: `grep -r "SubscriptionInfo" framework/src/` finds it in `framework/src/tenant/subscription.rs`. `grep -r "SubscriptionInfo" ferro-stripe/src/` returns nothing (deleted from ferro-stripe).
result: pass

### 8. ferro-stripe version is 0.4.0
expected: `grep "^version" ferro-stripe/Cargo.toml` returns `version = "0.4.0"`.
result: pass

### 9. Framework re-exports capability-axis modules
expected: `grep -A20 'pub use ferro_stripe' framework/src/lib.rs` shows `account`, `checkout`, `refund` re-exported as modules, plus `CheckoutBuilder`, `CheckoutIntent`, `ProcessedEventLog`, `MemoryProcessedLog`. None of the old symbols (`create_connect_checkout`, `create_subscription_checkout`, `is_processed`, `SubscriptionInfo`) appear.
result: pass

### 10. Full workspace tests pass
expected: Run `cargo test --all-features`. All 2360+ tests pass with 0 failed.
result: skipped
reason: ferro-json-ui has pre-existing unstaged changes (component.rs, render.rs) with compile errors unrelated to phase 140. All other workspace crates pass clean. ferro-stripe (25 tests) and framework tests pass.

## Summary

total: 10
passed: 9
issues: 0
pending: 0
skipped: 1

## Gaps

[none]
