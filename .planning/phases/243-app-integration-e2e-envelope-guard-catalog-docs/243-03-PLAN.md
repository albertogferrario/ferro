---
phase: 243-app-integration-e2e-envelope-guard-catalog-docs
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - ferro-mcp/src/tools/code_templates.rs
  - ferro-mcp/src/tools/generation_context.rs
  - docs/src/features/projections.md
autonomous: true
requirements: [CRUD-01, CRUD-02, CRUD-03]
must_haves:
  truths:
    - "An authoring agent reading ferro-mcp code_templates learns the CRUD opt-in (.creatable/.updatable/.deletable + .mcp_write_ability) and the derived create_/update_/delete_/list_ tool set"
    - "generation_context documents the projection-CRUD opt-in as an agent-facing write surface alongside the traditional handler"
    - "docs/src has a 'MCP CRUD Opt-In' section matching the shipped order projection (prerequisites, derived tools, authorization, confirmation flow, separation from crud_operations.rs)"
    - "The json-ui builtin-component drift guards remain at 47 (CRUD tools are not components — D-10)"
    - "ferro-mcp/src/tools/crud_operations.rs is NOT modified (separate developer-MCP surface — D-09)"
  artifacts:
    - path: "ferro-mcp/src/tools/code_templates.rs"
      provides: "projection_crud template category + guard assertion"
      contains: "projection_crud"
    - path: "ferro-mcp/src/tools/generation_context.rs"
      provides: "crud_handler extended with the projection-CRUD opt-in"
      contains: "creatable"
    - path: "docs/src/features/projections.md"
      provides: "MCP CRUD Opt-In documentation section"
      contains: "MCP CRUD Opt-In"
  key_links:
    - from: "ferro-mcp/src/tools/code_templates.rs"
      to: "app/src/projections/order.rs (shipped flip)"
      via: "the documented opt-in pattern matches the four builder calls actually shipped in Plan 01"
      pattern: "mcp_write_ability"
    - from: "docs/src/features/projections.md"
      to: "ferro-mcp-server confirmation flow"
      via: "documents request_confirm_delete_<svc> / confirm_delete_<svc>"
      pattern: "request_confirm_delete"
---

<objective>
Bring the `ferro-mcp` authoring surface and `docs/src/` to the same quality bar as the Rust
API: document the projection-CRUD opt-in so an authoring agent learns the
`.creatable/.updatable/.deletable/.mcp_write_ability` pattern and the derived
`create_/update_/delete_/list_` tool set. Then run the full BLOCKING workspace gate.

Purpose: SC#4 (D-08/D-09/D-10) — extend `code_templates` with a `projection_crud` category,
extend `generation_context`'s `crud_handler` with the agent-facing opt-in, add a docs/src
"MCP CRUD Opt-In" section. Explicitly: do NOT touch `crud_operations.rs` (D-09), keep the
json-ui component drift guards at 47 (D-10). The documented pattern MUST match the projection
actually shipped in Plan 01.

Output: edits to three files + a passing full workspace gate (fmt + clippy --all
--all-targets -D warnings + test --all-features).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/243-app-integration-e2e-envelope-guard-catalog-docs/243-CONTEXT.md
@.planning/phases/243-app-integration-e2e-envelope-guard-catalog-docs/243-PATTERNS.md
@app/src/projections/order.rs

<interfaces>
<!-- code_templates.rs: build_templates() (~line 48) ends with templates.extend(api_templates()); -->
<!-- (~line 76). Add a new private fn projection_crud_templates() -> Vec<CodeTemplate> and a -->
<!-- templates.extend(projection_crud_templates()); call. CodeTemplate fields: name, category, -->
<!-- description, code, imports, placeholders (struct at ~line 13; Placeholder at ~line 24). -->
<!-- test_all_categories_present (~line 1637) uses assert!(categories.contains("X")); adding an -->
<!-- extra category does NOT break it — add a new contains("projection_crud") assertion to it. -->

<!-- generation_context.rs: CommonPatterns struct (~line 41) has `pub crud_handler: String` (line 42). -->
<!-- The crud_handler value is set in execute() (~line 80). test_generation_context_has_all_sections -->
<!-- (~line 168) asserts `!context.common_patterns.crud_handler.is_empty()` — stays satisfied; -->
<!-- only the STRING CONTENT changes, no struct field added. -->

<!-- docs/src/features/projections.md: currently ends ~line 640 with `### projection_coverage` -->
<!-- inside `## MCP Tools`. Add a NEW top-level `## MCP CRUD Opt-In` section after that. -->

<!-- json-ui drift guards (MUST STAY 47 — D-10): -->
<!--   ferro-json-ui/src/catalog.rs:1101  assert_eq!(crate::render::BUILTIN_TYPES.len(), 47); -->
<!--   ferro-mcp/src/tools/json_ui_catalog.rs:294  assert_eq!(catalog.components.len(), 47, ...); -->
<!-- CRUD tools are MCP tools, not json-ui components — these counts MUST NOT change. -->

<!-- D-09: ferro-mcp/src/tools/crud_operations.rs is a SEPARATE developer-MCP SQL-introspection -->
<!-- tool. Read it only to confirm the boundary; do NOT modify it. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: code_templates projection_crud category + generation_context crud_handler extension</name>
  <files>ferro-mcp/src/tools/code_templates.rs, ferro-mcp/src/tools/generation_context.rs</files>
  <read_first>
    - ferro-mcp/src/tools/code_templates.rs lines 7-46 (CodeTemplate/Placeholder structs),
      48-79 (build_templates), 1438-1630 (api_templates as a struct-literal analog),
      1636-1676 (test_all_categories_present)
    - ferro-mcp/src/tools/generation_context.rs lines 41-47 (CommonPatterns), 80-88
      (crud_handler value), 168-201 (test_generation_context_has_all_sections)
    - app/src/projections/order.rs (the SHIPPED flip — the documented pattern must match it)
    - 243-PATTERNS.md "### ferro-mcp/src/tools/code_templates.rs" and
      "### ferro-mcp/src/tools/generation_context.rs" (the exact template + extension text)
  </read_first>
  <action>
**(A) code_templates.rs.** Add a private `projection_crud_templates() -> Vec<CodeTemplate>`
function returning one `CodeTemplate` with `category: "projection_crud"`, following the exact
struct-literal pattern of `api_templates()`. The template `code` must show the four builder
calls EXACTLY as shipped in Plan 01 (`.mcp_write_ability(...)`, `.creatable(true)`,
`.updatable(true)`, `.deletable(true)`), the read/write ability distinction, and a comment
that `status` is excluded from write inputs when a StateMachine exists, that `deleted_at` is
required for soft-delete, and that the derived tools are `create_<svc>`, `update_<svc>`,
`delete_<svc>`, `list_<svc>`. Use the template body from 243-PATTERNS.md "Pattern 8 /
### code_templates.rs" verbatim (with `{{service}}` / `{{Service}}` placeholders).

Register it in `build_templates()` after `templates.extend(api_templates());`:
```rust
    templates.extend(projection_crud_templates());
```

Add a guard assertion inside `test_all_categories_present` (alongside the existing
`assert!(categories.contains(...))` lines):
```rust
        assert!(categories.contains("projection_crud"),
            "Should have projection_crud templates");
```

**(B) generation_context.rs.** Replace the `crud_handler` string value (set in `execute()`,
~line 80) with the extended content from 243-PATTERNS.md "### generation_context.rs" — keep
the existing traditional handler as "Option A" and append "Option B: Projection-derived MCP
CRUD tools" describing the `.mcp_write_ability/.creatable/.updatable/.deletable` opt-in, the
`deleted_at`/`tenant_column`/`mcp_ability` prerequisites, and the derived
`create_/update_/delete_/list_` tool set. Do NOT add a new struct field — only change the
string content of `crud_handler` (the existing
`!context.common_patterns.crud_handler.is_empty()` assertion stays satisfied).

Do NOT modify `ferro-mcp/src/tools/crud_operations.rs` (D-09) — it is a separate
developer-MCP surface. Read it only to confirm the boundary if needed.
  </action>
  <verify>
    <automated>cargo test -p ferro-mcp code_templates && cargo test -p ferro-mcp generation_context</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "projection_crud" ferro-mcp/src/tools/code_templates.rs` returns >= 2
      (the category string + the test assertion)
    - `grep -c "fn projection_crud_templates" ferro-mcp/src/tools/code_templates.rs` returns 1
    - `grep -c "templates.extend(projection_crud_templates())" ferro-mcp/src/tools/code_templates.rs` returns 1
    - `grep -c "mcp_write_ability" ferro-mcp/src/tools/code_templates.rs` returns >= 1
    - `grep -c "creatable" ferro-mcp/src/tools/generation_context.rs` returns >= 1
    - `git diff --quiet ferro-mcp/src/tools/crud_operations.rs` exits 0 (UNCHANGED — D-09)
    - `cargo test -p ferro-mcp code_templates` exits 0
    - `cargo test -p ferro-mcp generation_context` exits 0
  </acceptance_criteria>
  <done>code_templates exposes a projection_crud category (guarded by a test), generation_context documents the agent-facing CRUD opt-in, crud_operations.rs is untouched, and ferro-mcp tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: docs/src MCP CRUD Opt-In section + verify json-ui drift guards unchanged at 47</name>
  <files>docs/src/features/projections.md</files>
  <read_first>
    - docs/src/features/projections.md lines 612-640 (the existing `## MCP Tools` section
      ending in `### projection_coverage` — the insertion point and the section style)
    - app/src/projections/order.rs (the shipped flip — the documented builder calls must match)
    - ferro-json-ui/src/catalog.rs lines 1093-1101 (the 47 component drift guard)
    - ferro-mcp/src/tools/json_ui_catalog.rs lines 285-297 (the cross-crate 47 mirror)
    - 243-PATTERNS.md "### docs/src/features/projections.md" (the exact section markdown)
  </read_first>
  <action>
**(A) docs.** Add a new top-level `## MCP CRUD Opt-In` section to
`docs/src/features/projections.md`, inserted AFTER the `## MCP Tools` section (after the
`### projection_coverage` content, ~line 640). Use the markdown from 243-PATTERNS.md
"### docs/src/features/projections.md" verbatim: it covers (1) enabling CRUD tools (the four
builder calls matching the shipped order projection), (2) the prerequisites table
(`tenant_column`, `deleted_at` column, `mcp_write_ability` present → validate() passes —
CRUD-07), (3) the derived tool-set table (create/update/delete/list with status/id/tenant
exclusions), (4) the authorization note (`read_write` scope + `write_authorized: Some(true)`;
list has no write-auth requirement), (5) the confirmation flow for delete
(`request_confirm_delete_<svc>` / `confirm_delete_<svc>`, single-use tokens), and (6) the
explicit separation from `crud_operations.rs` (D-09 — do not conflate the two surfaces).

The documented builder chain MUST match `app/src/projections/order.rs` exactly (same four
calls, same `view-orders`/`manage-orders` ability names) — the docs describe the real
shipped projection.

**(B) Verify drift guards unchanged (D-10).** CRUD tools are MCP tools, not json-ui
components. Confirm the component drift guards are STILL 47 and were not touched:
- `ferro-json-ui/src/catalog.rs` line ~1101: `assert_eq!(crate::render::BUILTIN_TYPES.len(), 47)`
- `ferro-mcp/src/tools/json_ui_catalog.rs` line ~294: `assert_eq!(catalog.components.len(), 47, ...)`
Do NOT change either count. If `cargo test -p ferro-json-ui` or the json_ui_catalog test
reports a count mismatch after this phase, that is a real regression to investigate — NOT a
number to bump. (No component was added; the count must stay 47.)
  </action>
  <verify>
    <automated>grep -c "MCP CRUD Opt-In" docs/src/features/projections.md && grep -c '47' ferro-json-ui/src/catalog.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MCP CRUD Opt-In" docs/src/features/projections.md` returns >= 1
    - `grep -c "mcp_write_ability(\"manage-orders\")" docs/src/features/projections.md` returns >= 1
      (matches the shipped order projection)
    - `grep -c "request_confirm_delete" docs/src/features/projections.md` returns >= 1
    - `grep -c "crud_operations.rs" docs/src/features/projections.md` returns >= 1 (the separation note)
    - `grep -c "BUILTIN_TYPES.len(), 47" ferro-json-ui/src/catalog.rs` returns 1 (UNCHANGED — D-10)
    - `grep -c "components.len()" ferro-mcp/src/tools/json_ui_catalog.rs` shows the 47 assertion present
    - `cargo test -p ferro-json-ui catalog` exits 0 (drift guard still green at 47)
  </acceptance_criteria>
  <done>docs/src documents the MCP CRUD opt-in matching the shipped projection, keeps the two surfaces separate (D-09), and the json-ui component drift guards remain at 47 (D-10).</done>
</task>

<task type="auto">
  <name>Task 3: Full BLOCKING workspace gate (fmt + clippy + test --all-features)</name>
  <files>(no source files — runs the workspace gate)</files>
  <read_first>
    - CLAUDE.md "Testing & Linting (MUST run before every commit)"
    - .planning memory note: ENOSPC — `cargo test --all-features` recurrently disk-full-fails
      on this workspace; check `df` and clean `target/` if it does, and fall back to per-crate
      test runs.
  </read_first>
  <action>
The full Track A CRUD surface is exercised end-to-end (Plans 01-02) and documented (this
plan). Run the project's BLOCKING pre-commit gate across the whole workspace to confirm no
fmt/clippy/test regression was introduced by the projection flip, the new e2e module, or the
catalog/docs edits.

Run the full gate (the exact CLAUDE.md command), which MUST exit 0:
```bash
cargo fmt --all -- --check && \
cargo clippy --all --all-targets -- -D warnings && \
cargo test --all-features
```
Also run the confirmation-feature path for the e2e specifically — it is not covered by the
default run if `confirmation` is not in the default feature set. Confirm whether
`--all-features` enables it; if it does, the single run covers both, otherwise ALSO run:
```bash
cargo test -p app --features confirmation crud_e2e
```

ENOSPC fallback (per project memory `project_ferro_disk_full_test_gate`): if
`cargo test --all-features` fails with `ENOSPC` / link / fingerprint errors (a disk issue,
not a real defect), run `df -h`, clean `target/` (`cargo clean` or remove stale incremental
artifacts), and re-run; if still tight, fall back to per-crate test runs covering at least
`-p app`, `-p ferro-mcp`, `-p ferro-json-ui`, and the `framework` / `ferro-mcp-server` write
path, then record which crates were run per-crate vs in the full run.

Note (per project memory `project_schema_export_test_dirties_tree`): a full `cargo test` may
regenerate `docs/protocol/schemas/*.json`. Those changes are unrelated to this phase —
`git checkout` them; do NOT fold them into the phase commit.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0 (no formatting drift)
    - `cargo clippy --all --all-targets -- -D warnings` exits 0 (no warnings)
    - `cargo test --all-features` exits 0 (all tests green, including crud_e2e, the ferro-mcp
      catalog/generation_context tests, and the json-ui 47 drift guard)
    - `cargo test -p app --features confirmation crud_e2e` exits 0 (confirmation flow), if not
      already covered by `--all-features`
    - The json-ui component count assertion is still 47 (not bumped — D-10)
    - `git diff --quiet ferro-mcp/src/tools/crud_operations.rs` exits 0 (D-09 unchanged)
    - If ENOSPC forced a per-crate fallback, the SUMMARY records which crates were run
      per-crate vs in the full run, and confirms each was green
    - `docs/protocol/schemas/*.json` regenerated-by-test churn is reverted (`git checkout`),
      not committed with the phase
  </acceptance_criteria>
  <done>The full BLOCKING workspace gate (fmt + clippy --all --all-targets -D warnings + test --all-features) is green, the confirmation-feature e2e is green, the json-ui drift guard stays at 47, and crud_operations.rs is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authoring agent → ferro-mcp code_templates / generation_context / docs | Documentation accuracy is part of the framework surface; inaccurate docs would mislead an authoring agent into an insecure or non-compiling configuration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-243-01 | I (Information disclosure / misconfiguration) | docs + code_templates accuracy | mitigate (verify) | The documented opt-in MUST match the shipped order projection (same four builder calls, same ability names), and MUST state the prerequisites that keep the surface safe: `tenant_column` set (tenant injected, never agent-supplied), `mcp_write_ability` required (else validate() fails at boot — CRUD-07), `read_write` scope + `write_authorized` for writes, single-use confirmation tokens for delete. Acceptance criteria grep-pin these strings against the real projection. An agent following the docs lands on the gated configuration, not a widened one. |
| T-243-02 | T (Tampering) | json-ui drift guard / crud_operations boundary | mitigate (verify) | D-10: the component drift guards stay at 47 — a CRUD tool is not a component, so a changed count would indicate accidental scope creep; the gate (Task 3) re-runs the 47 assertion. D-09: `crud_operations.rs` is left byte-for-byte unchanged (acceptance criteria `git diff --quiet`), preventing accidental conflation of the developer-MCP SQL surface with the projection-derived consumer surface. |
</threat_model>

<verification>
- `cargo test -p ferro-mcp code_templates` and `... generation_context` pass.
- `cargo test -p ferro-json-ui catalog` passes with the 47 drift guard unchanged.
- `crud_operations.rs` unchanged (`git diff --quiet`).
- Full BLOCKING gate green (fmt + clippy --all --all-targets -D warnings + test --all-features),
  with documented ENOSPC per-crate fallback if disk-constrained.
</verification>

<success_criteria>
- code_templates exposes a `projection_crud` category; generation_context documents the
  opt-in; docs/src has the MCP CRUD Opt-In section matching the shipped projection (SC#4).
- Two surfaces kept separate (D-09); component drift guards stay at 47 (D-10).
- Full workspace gate green.
</success_criteria>

<output>
After completion, create
`.planning/phases/243-app-integration-e2e-envelope-guard-catalog-docs/243-03-SUMMARY.md`.
</output>
