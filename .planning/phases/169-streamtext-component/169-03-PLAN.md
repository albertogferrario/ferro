---
phase: 169-streamtext-component
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - docs/src/json-ui/components.md
autonomous: true
requirements: [AISSE-02]

must_haves:
  truths:
    - "docs/src/json-ui/components.md has a `### StreamText` section documenting sse_url, placeholder, loading_text."
    - "The docs state the StreamText↔server contract: the SSE endpoint MUST emit `event: done` to terminate cleanly (otherwise EventSource auto-reconnects)."
    - "The docs note tokens are appended as plain text nodes (no innerHTML) — the security property."
    - "No `v2` / `legacy` / `migration` framing appears in the new section (JSON-UI naming convention)."
  artifacts:
    - path: "docs/src/json-ui/components.md"
      provides: "### StreamText documentation section"
      contains: "### StreamText"
  key_links:
    - from: "docs ### StreamText"
      to: "ferro-json-ui StreamTextProps"
      via: "prop table documenting sse_url / placeholder / loading_text"
      pattern: "sse_url"
---

<objective>
Add a `### StreamText` section to `docs/src/json-ui/components.md` documenting the three props,
the `data-ferro-stream-url` rendering, the server-side `event: done` contract, and the
plain-text-node security property.

Purpose: Satisfies AISSE-02 SC#4. The `event: done` server contract is the single non-obvious
correctness requirement of the whole component — without it the browser's EventSource
auto-reconnects in a loop — so the docs must state it explicitly, with a Phase-168 `SseEvent`
example showing how a server emits it.

Output: a new `### StreamText` section placed after `### RawHtml`, framed as the only/current
version (no "v2"/"legacy"/"migration" language, per the JSON-UI naming convention).

Depends on Plan 01: documents the final `StreamTextProps` field names and the
`data-ferro-stream-url` / `data-ferro-stream-placeholder` / `data-ferro-stream-loading` contract
established there. No file overlap with Plan 02 (different file) → runs in parallel within Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/169-streamtext-component/169-CONTEXT.md
@.planning/phases/169-streamtext-component/169-UI-SPEC.md
@.planning/phases/169-streamtext-component/169-RESEARCH.md
@.planning/phases/169-streamtext-component/169-01-SUMMARY.md

<interfaces>
<!-- The analog docs section the new section mirrors. -->
### RawHtml section (docs/src/json-ui/components.md:1426-1447) — structure to mirror:
  description prose → props table → JSON usage example → contract/trust-boundary note block,
  then a `---` separator.

Copy values from UI-SPEC.md Copywriting Contract:
  - docs example placeholder value: "Response will appear here…"
  - docs example loading_text value: "Generating…"
  - example sse_url value: "/ai/generate"
Phase-168 server emit example (framework/src/http/sse.rs:78 — `.event("done")` builder exists):
  `tx.send(SseEvent::new().event("done").data("")).await.ok();`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ### StreamText docs section (SC#4)</name>
  <files>docs/src/json-ui/components.md</files>
  <read_first>
    - docs/src/json-ui/components.md (read the `### RawHtml` section at lines 1426-1447 to mirror its exact structure, table format, and the `---` separator placement)
    - .planning/phases/169-streamtext-component/169-UI-SPEC.md (Copywriting Contract — exact example copy values)
    - .planning/phases/169-streamtext-component/169-CONTEXT.md (D-07 — what the section must document)
    - .planning/phases/169-streamtext-component/169-RESEARCH.md (the "Docs section shape" code example near line 499)
  </read_first>
  <action>
Add the following section to `docs/src/json-ui/components.md` immediately after the `### RawHtml`
section (after line ~1447), before its trailing `---` separator boundary — match the surrounding
section's heading depth and the `---`-between-sections convention used in the file:

```markdown
### StreamText

Connects to a server-sent-events endpoint and renders token-by-token output as
plain text. Tokens are appended as text nodes — no HTML interpretation.

| Prop | Type | Description |
|------|------|-------------|
| `sse_url` | `string` | URL of the SSE endpoint that streams tokens |
| `placeholder` | `string?` | Text shown inside the content area before the first token arrives |
| `loading_text` | `string?` | Status indicator shown while the stream is open |

\`\`\`json
"response_area": {
  "type": "StreamText",
  "props": {
    "sse_url": "/ai/generate",
    "placeholder": "Response will appear here…",
    "loading_text": "Generating…"
  }
}
\`\`\`

**Server contract.** The SSE endpoint must emit `event: done` when the stream
is complete:

\`\`\`rust
tx.send(SseEvent::new().event("done").data("")).await.ok();
\`\`\`

Without `event: done`, the browser's `EventSource` auto-reconnects after the
connection closes, causing the component to re-fetch the endpoint in a loop.

**Security.** Tokens are appended as plain text nodes — `innerHTML` is never
called. Streamed content cannot inject HTML or execute scripts regardless of
its content.
```

(Replace the escaped fence markers `\`\`\`` with literal triple-backtick fences when writing the
file.)

Framing constraints (JSON-UI naming convention): do NOT use "v2", "legacy", "v1", "migration",
or any version-comparison language. Describe StreamText as the current/only version of the
component. Do not add a migration subsection.
  </action>
  <verify>
    <automated>grep -n "### StreamText" docs/src/json-ui/components.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "### StreamText" docs/src/json-ui/components.md` returns exactly one line
    - The section's prop table documents `sse_url`, `placeholder`, and `loading_text`
    - The section contains the literal string `event: done` AND the words "auto-reconnect" (server contract documented — SC#4 / D-03)
    - The section contains a Security note stating `innerHTML` is never called
    - Within the new `### StreamText` section, none of these substrings appear: `v2`, `legacy`, `migration` (case-insensitive) — verify with `grep -in -A40 "### StreamText" docs/src/json-ui/components.md | grep -iE "v2|legacy|migration"` returning no matches
    - The new section is placed after the `### RawHtml` section
  </acceptance_criteria>
  <done>### StreamText section present after ### RawHtml: props table (three props), JSON example, `event: done` server contract with auto-reconnect rationale + SseEvent example, plain-text-node security note. No v2/legacy/migration framing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan edits a documentation Markdown file only. No code, no input handling, no trust boundary crossed at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-DOC | Information (incorrect security guidance) | docs/src/json-ui/components.md | mitigate | The docs accurately state the security property (text-node append, no innerHTML) and the `event: done` reconnect contract, so consumers do not build insecure or looping integrations. Acceptance: section contains the Security note and the `event: done` contract (Task 1 acceptance criteria). |

No runtime threats — documentation-only change.
</threat_model>

<verification>
- `grep "### StreamText" docs/src/json-ui/components.md` matches.
- Section documents three props, the `event: done` contract, and the text-node security property.
- No v2/legacy/migration framing in the new section.
</verification>

<success_criteria>
- `### StreamText` section added to docs with three props, `data-ferro-stream-url` behavior, the `event: done` server contract (+ rationale), and the plain-text-node security note. (AISSE-02 SC#4)
- Framed as current/only version — no v2/legacy/migration language.
</success_criteria>

<output>
After completion, create `.planning/phases/169-streamtext-component/169-03-SUMMARY.md`
</output>
