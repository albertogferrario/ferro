---
phase: 249.1-offload-surface-convergence-remove-transitional-paths
reviewed: 2026-08-15T08:35:00Z
depth: standard
files_reviewed: 4
files_reviewed_list:
  - framework/src/app.rs
  - framework/src/offload.rs
  - framework/tests/offload_result_round_trip.rs
  - framework/src/lib.rs
findings:
  critical: 0
  warning: 0
  info: 0
  total: 0
status: clean
---

# Phase 249.1: Code Review Report

**Reviewed:** 2026-08-15T08:35:00Z
**Depth:** standard
**Files Reviewed:** 4
**Status:** clean

## Summary

Both source-touching commits (`4b9421d7`, `be7e1512`) were examined against the acceptance
criteria in the PLAN, the invariants in the threat model, and the scope boundary (D-03).

All five review focus areas are satisfied:

**1. `run_common_boot` linearization (`framework/src/app.rs:422–491`)**

The surviving function body implements the D-02 five-step sequence without early returns or
branching registration logic. `App::get::<Broadcaster>().is_none()` at line 439 guarantees
the singleton is `Some` by the time Step 4 executes. Both `#[cfg(feature = "redis-transport")]`
(lines 447–465) and `#[cfg(not(feature = "redis-transport"))]` (lines 467–477) re-fetch the
`Broadcaster` via `App::get` after Step 2, so no `Option` is threaded across the `#[cfg]`
boundary and the `.expect("Broadcaster ensured in Step 2")` calls at lines 450, 469, and 482
have no panic path. `spawn_in_process_worker` appears once as a call site (line 489) and once
as the function definition (line 498) — single worker spawn. The only `return;` in `app.rs`
is at line 384 in an unrelated seeder dispatch function.

**2. Atomic deletion (`framework/src/offload.rs` + test)**

`register_offload_hooks()` is absent from `offload.rs` — confirmed by grep returning exit 1
on `fn register_offload_hooks(`. Both former callers (the production site in `app.rs` and
the integration test at `offload_result_round_trip.rs:244`) now call
`register_offload_hooks_with_broadcaster`. The import at line 23 of the test file is correct;
the `Arc` and `Broadcaster` imports are present. No dangling reference exists.

**3. Security-relevant invariants (D-05 redaction + best-effort semantics)**

- `offload.rs:312`: the failed-delta payload is `json!({ "status": "failed" })` with no `error`
  field. The raw error travels only through `persist_error` into the server-side snapshot
  (`offload.rs:317`). D-05 is byte-identical to its pre-sweep state.
- `broadcast_delta` (`offload.rs:269–289`): send failure is `warn!`-logged and the function
  returns normally — no error propagation to the caller. A persist failure at lines 319–326
  issues `return` before the broadcast call at line 329, so a missing snapshot is never
  followed by a broadcast. Both invariants from `threat_model` T-249.1-01 and T-249.1-02
  are preserved verbatim.

**4. Doc correctness / no broken intra-doc links**

The former trailing sentence "use [`register_offload_hooks`] instead" was removed from the
`register_offload_hooks_with_broadcaster` doc (verified: grep on `[`register_offload_hooks`]`
returns no hits across all changed files). `lib.rs:228` references the surviving fn by its
correct name. No intra-doc link to the deleted function remains anywhere in the four reviewed
files.

**5. Scope**

Commit `4b9421d7` touches exactly `framework/src/app.rs`, `framework/src/offload.rs`, and
`framework/tests/offload_result_round_trip.rs`. Commit `be7e1512` touches `framework/src/lib.rs`
and `framework/src/offload.rs`. No file under `framework/src/inertia/`, `framework/src/routing/`,
`framework/src/http/`, `framework/src/session/`, `ferro-queue/`, or `ferro-broadcast/` appears
in either diff.

The SC#2 structural grep confirms zero offload-surface transitional markers survive. The only
remaining `backward.compat` hits are pre-244 items on the D-03 keep-list (InertiaContext
re-export at `lib.rs:171`, inertia module re-exports, http mod doc, routing macros, session
driver — none touched by this phase).

All reviewed files meet the behavioral-preservation, security-invariant, and scope criteria.
No issues found.

---

_Reviewed: 2026-08-15T08:35:00Z_
_Reviewer: Claude (gsd-code-reviewer)_
_Depth: standard_
