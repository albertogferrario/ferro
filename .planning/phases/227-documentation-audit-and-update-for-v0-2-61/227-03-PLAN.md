---
phase: 227-documentation-audit-and-update-for-v0-2-61
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every page under docs/src/ was audited, not only the 6 flagged pages (D-01)"
    - "No stale TLS/OpenSSL reference exists anywhere except the single correct install-page line (D-04)"
    - "No stale hard version pin (0.2.NN) remains anywhere in docs/src/ (D-06)"
    - "No phantom CLI command remains; every documented command maps to a real source module (D-03)"
    - "MCP tool-count phrasing is version-neutral and non-contradictory across pages (DISC-07)"
    - "All intra-doc links resolve (mdbook build succeeds)"
  artifacts:
    - path: ".planning/phases/227-documentation-audit-and-update-for-v0-2-61/227-03-SUMMARY.md"
      provides: "Audit evidence: grep outputs proving the whole-tree sweep is clean"
  key_links:
    - from: "docs/src/ (all pages)"
      to: "ferro-cli/src/commands/ + Cargo.toml.tpl"
      via: "grep sweep proving zero residual discrepancies against the oracle"
      pattern: "native-tls|0\\.2\\.[0-9]|make:model"
---

<objective>
The goal-backward proof that "every OTHER page was audited" (D-01), not just the 6 flagged pages. This wave-2 sweep runs after Plans 01 and 02 land, and confirms the whole `docs/src/` tree is free of the four staleness classes the phase targets: stale TLS/OpenSSL config, hard version pins, phantom commands, and contradictory tool counts. It also runs the only available automated doc check (`mdbook build`) to confirm no intra-doc link broke.

Purpose: A docs audit's deliverable is not only the 6 fixes — it is the evidence that the rest of the tree is clean. This plan produces that evidence.
Output: A SUMMARY capturing the grep/build evidence; any NEW discrepancy found here is fixed in-place (audit, don't work around) and noted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/227-documentation-audit-and-update-for-v0-2-61/227-RESEARCH.md
@.planning/phases/227-documentation-audit-and-update-for-v0-2-61/227-CONTEXT.md

This plan depends on 227-01 and 227-02 being complete — the sweeps below assume the 6 targeted fixes have already landed. The TLS sweep (D-04) is expected to find exactly ONE match (the correct install-page line); finding more is a real discrepancy to fix.
</context>

<tasks>

<task type="auto">
  <name>Task 1: TLS/OpenSSL + version-pin + phantom-command full-tree sweep (D-01, D-04, D-06, D-03)</name>
  <files>docs/src/ (read-only sweep; edit any newly found discrepancy in place)</files>
  <read_first>
    - .planning/phases/227-documentation-audit-and-update-for-v0-2-61/227-RESEARCH.md ("TLS/OpenSSL Sweep Result", "Verified Clean Pages", "Real CLI subcommand registry")
    - ferro-cli/src/templates/files/backend/Cargo.toml.tpl (scaffold dep oracle: `runtime-tokio-rustls`, no native-tls/OpenSSL)
    - ferro-cli/src/commands/mod.rs (authoritative module list — any documented `make:*`/`db:*`/`docker:*` command must map to a file here)
  </read_first>
  <action>
Run these four sweeps from the repo root. Capture each command's output verbatim into the SUMMARY as evidence.

(1) TLS/OpenSSL sweep (D-04) — expect EXACTLY ONE match (installation.md:8):
```
grep -rniE "native-tls|runtime-tokio-native-tls|openssl" docs/src/
```
EXPECTED: a single line, `docs/src/getting-started/installation.md:8: ... no OpenSSL needed; the scaffold uses rustls`. If ANY other line appears, it is a stale config reference — fix it in place by aligning to `runtime-tokio-rustls` per Cargo.toml.tpl, then re-run until only the one install-page line remains.

(2) Hard version-pin sweep (D-06) — expect ZERO matches after Plans 01/02:
```
grep -rnE "0\.2\.[0-9]+" docs/src/
```
EXPECTED: no hard `0.2.NN` pins in command examples or config snippets. The `<pinned>` placeholder and version-neutral phrasing are correct. If a match is a genuine illustrative pin (not the install MSRV `1.88`, which is a Rust version not a ferro pin), replace it with `<pinned>` or version-neutral phrasing. Note: `1.88` (Rust MSRV) is NOT a ferro version pin — do not flag it.

(3) Phantom-command sweep (D-03) — confirm the phantom `make:model` is fully gone:
```
grep -rn "make:model" docs/src/
```
EXPECTED: ZERO matches. If any remain, replace with `make:scaffold` per Plan 02's pattern.

(4) MCP tool-count contradiction sweep (DISC-07) — confirm no hard count remains:
```
grep -rnE "[0-9]+\+? (introspection )?tools" docs/src/
```
EXPECTED: no hard tool counts (`57`, `80+`) in MCP descriptions — only version-neutral phrasing ("a full suite of introspection tools"). If a hard count remains, make it version-neutral.

For any NEW discrepancy found, fix it in place (D-03: surface and fix, don't work around) and record the file:line + fix in the SUMMARY.
  </action>
  <verify>
    <automated>test "$(grep -rniE 'native-tls|runtime-tokio-native-tls|openssl' docs/src/ | wc -l | tr -d ' ')" = "1" && test "$(grep -rnE '0\.2\.[0-9]+' docs/src/ | wc -l | tr -d ' ')" = "0" && test "$(grep -rn 'make:model' docs/src/ | wc -l | tr -d ' ')" = "0" && test "$(grep -rnE '(57|80\+) (introspection )?tools' docs/src/ | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rniE "native-tls|runtime-tokio-native-tls|openssl" docs/src/` returns EXACTLY 1 line, and that line is `docs/src/getting-started/installation.md` (the correct "no OpenSSL / uses rustls" statement)
    - `grep -rnE "0\.2\.[0-9]+" docs/src/` returns 0 lines (no residual hard ferro version pins)
    - `grep -rn "make:model" docs/src/` returns 0 lines
    - No hard MCP tool count (`57` or `80+`) remains in any page
    - SUMMARY contains the verbatim output of all four sweep commands as audit evidence
  </acceptance_criteria>
  <done>All four whole-tree sweeps pass with the expected counts; any newly surfaced discrepancy is fixed in place and documented.</done>
</task>

<task type="auto">
  <name>Task 2: mdBook link-integrity build + audit-evidence summary</name>
  <files>docs/ (build only — no source edits unless a broken link is found)</files>
  <read_first>
    - docs/book.toml (confirms `create-missing = false` — broken intra-doc links fail the build)
    - docs/src/SUMMARY.md (canonical TOC; all linked pages must exist)
  </read_first>
  <action>
Run the only automated doc check available (no CI step runs it; run locally):
```
mdbook build docs/
```
`book.toml` sets `create-missing = false`, so a broken intra-doc link (including the `frontend-types.md` cross-link corrected in Plan 02) causes a non-zero exit. If the build fails on a broken link, fix the link target (point to the real page/anchor) and re-run until the build is clean.

If `mdbook` is not on PATH, it is installed at `~/.cargo/bin/mdbook` (per RESEARCH.md Environment Availability) — invoke it by absolute path.

Record in the SUMMARY: the `mdbook build` exit status, and a confirmation line per the goal-backward must_haves that the whole tree was audited (D-01), TLS is clean (D-04), no version pins remain (D-06), no phantom commands remain (D-03), and tool counts are version-neutral (DISC-07).
  </action>
  <verify>
    <automated>(command -v mdbook >/dev/null 2>&1 && mdbook build docs/ >/dev/null 2>&1) || ~/.cargo/bin/mdbook build docs/ >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `mdbook build docs/` exits 0 (no broken intra-doc links; the corrected frontend-types.md cross-link resolves)
    - SUMMARY records the `mdbook build` result and a per-D-XX clean confirmation (D-01, D-03, D-04, D-06, DISC-07)
  </acceptance_criteria>
  <done>mdBook builds with zero broken links; SUMMARY captures the full audit evidence proving every page (not just the 6 flagged) was swept clean.</done>
</task>

</tasks>

<threat_model>
Docs-only phase: read-only sweeps plus a local `mdbook build`; any edits are static Markdown under `docs/src/`. No executable surface, no code paths, no inputs, no auth, no data flow. No new attack surface. STRIDE: N/A.
</threat_model>

<verification>
Validation note (Nyquist Dim 8): no runtime behavior to validate. This plan IS the verification layer for the phase — grep sweeps against the CLI/scaffold oracles plus `mdbook build` for link integrity. Evidence is captured in the SUMMARY rather than asserted by a test suite.
</verification>

<success_criteria>
- Whole `docs/src/` tree swept: TLS clean (1 expected match), zero version pins, zero phantom commands, no contradictory tool counts.
- `mdbook build docs/` succeeds — all intra-doc links resolve.
- SUMMARY contains verbatim grep/build evidence proving the audit covered every page (D-01).
</success_criteria>

<output>
After completion, create `.planning/phases/227-documentation-audit-and-update-for-v0-2-61/227-03-SUMMARY.md`
</output>
