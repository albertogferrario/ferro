---
phase: 227-documentation-audit-and-update-for-v0-2-61
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/getting-started/working-with-agents.md
  - docs/src/cli/frontend-types.md
  - docs/src/upgrading/migration-guide.md
  - docs/src/introduction.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "working-with-agents.md references only real CLI commands (no phantom ferro make:model)"
    - "working-with-agents.md and introduction.md agree on MCP tool count (version-neutral, no contradiction)"
    - "frontend-types.md has no stale hard version pin and no broken cross-link"
    - "migration-guide.md MCP config uses the real `ferro mcp` invocation form"
    - "introduction.md milestone string is current (not stale v12.0)"
  artifacts:
    - path: "docs/src/getting-started/working-with-agents.md"
      provides: "Corrected agent-scaffolding example using a real command"
    - path: "docs/src/cli/frontend-types.md"
      provides: "Version-neutral docker:init example and a valid cross-link"
    - path: "docs/src/upgrading/migration-guide.md"
      provides: "Correct ferro mcp MCP-server config"
    - path: "docs/src/introduction.md"
      provides: "Current status string and version-neutral tool count"
  key_links:
    - from: "docs/src/getting-started/working-with-agents.md"
      to: "ferro-cli/src/commands/make_scaffold.rs"
      via: "replacement command ferro make:scaffold maps to a real source module"
      pattern: "make:scaffold"
    - from: "docs/src/upgrading/migration-guide.md"
      to: "ferro-cli/src/commands/mcp.rs"
      via: "MCP config invokes the real `ferro mcp` subcommand"
      pattern: '"mcp"'
---

<objective>
Fix the remaining four single-file factual discrepancies (DISC-03 through DISC-07 from RESEARCH.md), each in a distinct file so they run parallel-safe with Plan 01:

- `working-with-agents.md`: phantom `ferro make:model` (3 occurrences) + stale `57` tool count (DISC-04, DISC-07)
- `cli/frontend-types.md`: stale `0.2.33` version pin + broken `do-init.md` cross-link (DISC-03)
- `upgrading/migration-guide.md`: stale `ferro-mcp` binary name in MCP config (DISC-05)
- `introduction.md`: stale `v12.0` milestone string + contradictory `80+` tool count (DISC-06, DISC-07)

Purpose: Eliminate phantom commands, stale version pins, contradictory tool counts, and an outdated upgrade-path config snippet — all factual-accuracy fixes within the D-02 boundary.
Output: Four corrected doc pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/227-documentation-audit-and-update-for-v0-2-61/227-RESEARCH.md
@.planning/phases/227-documentation-audit-and-update-for-v0-2-61/227-CONTEXT.md

Docs-only, factual-accuracy audit (D-02): correct facts, do not rewrite prose. Tool counts must be version-neutral, not a new hard number that will rot (Pitfall 4). Verify every replaced command against `ferro-cli/src/commands/`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace phantom make:model + stale tool count in working-with-agents.md (DISC-04, DISC-07)</name>
  <files>docs/src/getting-started/working-with-agents.md</files>
  <read_first>
    - docs/src/getting-started/working-with-agents.md (lines 1-12 for tool count; lines 99-120 for the scaffolding example)
    - ferro-cli/src/commands/make_scaffold.rs (ground-truth: `ferro make:scaffold` is the real model+migration+controller scaffolder; `ferro make:model` does NOT exist — no make_model.rs)
    - RESEARCH.md "Real CLI subcommand registry" (confirms make:scaffold real, make:model phantom)
  </read_first>
  <action>
Two edits in `docs/src/getting-started/working-with-agents.md`:

(A) Tool count (line 7, DISC-07). The page says `exposes 57 introspection tools`. The contradicting number on introduction.md is `80+`; neither is precisely right and both rot. Make it version-neutral.
CURRENT (exact substring): `exposes 57 introspection tools`
REPLACE WITH: `exposes a full suite of introspection tools`

(B) Phantom `ferro make:model` — 3 occurrences (DISC-04). Replace with the real `ferro make:scaffold` and fix the generated path. Make these exact substring replacements:

1. Line ~105:
   CURRENT: `generation_hint: "Use `ferro make:model <ModelName>` to scaffold a new model with migration"`
   REPLACE WITH: `generation_hint: "Use `ferro make:scaffold <ModelName>` to scaffold a new model with migration"`

2. Line ~108:
   CURRENT: `2. The agent reads the hint, identifies the CLI command: `ferro make:model`.`
   REPLACE WITH: `2. The agent reads the hint, identifies the CLI command: `ferro make:scaffold`.`

3. Line ~112 (inside the bash block):
   CURRENT: `ferro make:model Post`
   REPLACE WITH: `ferro make:scaffold Post`

4. Line ~115 (generated path — real scaffold writes to `src/models/`, not `app/models/`):
   CURRENT: `4. Ferro generates `app/models/post.rs` and a timestamped migration file with the correct SeaORM structure.`
   REPLACE WITH: `4. Ferro generates `src/models/post.rs` and a timestamped migration file with the correct SeaORM structure.`

Do not alter surrounding prose beyond these substrings.
  </action>
  <verify>
    <automated>! grep -q "make:model" docs/src/getting-started/working-with-agents.md && ! grep -q "57 introspection tools" docs/src/getting-started/working-with-agents.md && grep -q "ferro make:scaffold Post" docs/src/getting-started/working-with-agents.md && ! grep -q "app/models/post.rs" docs/src/getting-started/working-with-agents.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/getting-started/working-with-agents.md` does NOT contain the string `make:model` (all 3 occurrences replaced)
    - `docs/src/getting-started/working-with-agents.md` CONTAINS `ferro make:scaffold Post`
    - `docs/src/getting-started/working-with-agents.md` does NOT contain `57 introspection tools`
    - `docs/src/getting-started/working-with-agents.md` does NOT contain `app/models/post.rs` (path corrected to `src/models/post.rs`)
  </acceptance_criteria>
  <done>No phantom `make:model` remains; example uses real `make:scaffold` with the correct `src/models/` path; tool count is version-neutral.</done>
</task>

<task type="auto">
  <name>Task 2: Version-neutral pin + broken cross-link in frontend-types.md (DISC-03)</name>
  <files>docs/src/cli/frontend-types.md</files>
  <read_first>
    - docs/src/cli/frontend-types.md (lines 76-117 — the docker:init example, the pin-override prose, and the "Related commands" cross-links)
    - ferro-cli/src/commands/docker_init.rs (ground-truth: `ferro docker:init` is real and has a `--ferro-version` flag)
    - RESEARCH.md DISC-03 (the `do-init.md` link target is the DigitalOcean page, NOT a docker:init page; no `docker-init.md` page exists)
  </read_first>
  <action>
Two edits in `docs/src/cli/frontend-types.md`:

(A) Stale version pin (line ~97, D-06). Note: the Dockerfile snippet at line 79 already uses the `<pinned>` placeholder and is CLEAN — do NOT touch it. Only the override example at line 97 has a hard pin.
CURRENT (exact): `ferro docker:init --ferro-version 0.2.33 --force`
REPLACE WITH: `ferro docker:init --ferro-version <pinned> --force`
(The preceding prose already explains `<pinned>` is read from `Cargo.lock`.)

(B) Broken cross-link (line ~115, DISC-03). The label says `ferro docker:init` but links to `do-init.md`, which is the DigitalOcean (`do:init`) page — wrong target. There is no `docker-init.md` page. `docker:init` is documented in `reference/cli.md`. Fix the link to point to the CLI reference anchor.
CURRENT (exact):
```
- [`ferro docker:init`](do-init.md) — regenerates the Dockerfile with the
  current `types-gen` stage
```
REPLACE WITH:
```
- [`ferro docker:init`](../reference/cli.md#ferro-dockerinit) — regenerates the Dockerfile with the
  current `types-gen` stage
```
Before finalizing, confirm the anchor exists: `grep -i "ferro docker:init" docs/src/reference/cli.md`. If the heading is `### `ferro docker:init``, the mdBook anchor is `#ferro-dockerinit`. If no such heading exists in reference/cli.md, instead drop the link and leave plain text ``ferro docker:init`` — but prefer the working anchor.
  </action>
  <verify>
    <automated>! grep -q "ferro-version 0.2.33" docs/src/cli/frontend-types.md && ! grep -q "(do-init.md)" docs/src/cli/frontend-types.md && grep -q "ferro-version <pinned>" docs/src/cli/frontend-types.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/cli/frontend-types.md` does NOT contain `0.2.33`
    - `docs/src/cli/frontend-types.md` CONTAINS `ferro docker:init --ferro-version <pinned> --force`
    - `docs/src/cli/frontend-types.md` does NOT contain the broken link target `(do-init.md)`
    - The `<pinned>` placeholder Dockerfile line (line ~79) is unchanged (still present)
  </acceptance_criteria>
  <done>No hard version pin remains; the docker:init cross-link resolves to a real target (CLI reference anchor or plain text), not the DigitalOcean page.</done>
</task>

<task type="auto">
  <name>Task 3: Fix stale MCP binary name in migration-guide.md (DISC-05)</name>
  <files>docs/src/upgrading/migration-guide.md</files>
  <read_first>
    - docs/src/upgrading/migration-guide.md (lines 80-100 — the "Before"/"After" MCP config blocks)
    - ferro-cli/src/commands/mcp.rs (ground-truth: the MCP server is the `ferro mcp` subcommand of the main binary; there is no separate `ferro-mcp` binary)
    - docs/src/getting-started/working-with-agents.md (canonical correct MCP config form: `"command": ".../ferro"`, `"args": ["mcp"]`)
  </read_first>
  <action>
In `docs/src/upgrading/migration-guide.md`, fix the "After" MCP config block ONLY (lines ~91-99). The "Before" block uses the sample name `cancer-mcp` as an illustrative old value — leave it alone; only the "After" block claims the new correct form and is wrong.

CURRENT "After" block (exact):
```
// After
{
  "mcpServers": {
    "ferro-mcp": {
      "command": "ferro-mcp",
      "args": ["serve"]
    }
  }
}
```
REPLACE WITH (matching working-with-agents.md canonical form — `ferro mcp` subcommand, not a separate binary):
```
// After
{
  "mcpServers": {
    "ferro": {
      "command": "/absolute/path/to/target/debug/ferro",
      "args": ["mcp"]
    }
  }
}
```
Do not change the "Before" block or surrounding prose.
  </action>
  <verify>
    <automated>! grep -q '"command": "ferro-mcp"' docs/src/upgrading/migration-guide.md && grep -q '"args": \["mcp"\]' docs/src/upgrading/migration-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/upgrading/migration-guide.md` does NOT contain `"command": "ferro-mcp"`
    - `docs/src/upgrading/migration-guide.md` does NOT contain the "After"-block `"args": ["serve"]` paired with a ferro-mcp command (the After block now uses `"args": ["mcp"]`)
    - `docs/src/upgrading/migration-guide.md` CONTAINS `"args": ["mcp"]`
    - The "Before" block (with `cancer-mcp`) is unchanged
  </acceptance_criteria>
  <done>The "After" MCP config invokes the real `ferro mcp` subcommand, matching the canonical working-with-agents.md form.</done>
</task>

<task type="auto">
  <name>Task 4: Update stale milestone string + tool count in introduction.md (DISC-06, DISC-07)</name>
  <files>docs/src/introduction.md</files>
  <read_first>
    - docs/src/introduction.md (lines 9-17 for the tool count; lines 55-61 for the Status milestone string)
    - .planning/STATE.md (current milestone context — project is past v12.0; v15.0 shipped)
    - RESEARCH.md DISC-06 and DISC-07 (D-02 minimal-fix guidance: drop the stale milestone-specific sentence; make tool count version-neutral, not a new hard number)
  </read_first>
  <action>
Two edits in `docs/src/introduction.md`:

(A) Stale milestone string (line ~59, DISC-06). Within the D-02 boundary the minimal fix is to drop the stale milestone-specific sentence and keep the durable pre-1.0 statement.
CURRENT (exact): `Ferro is pre-1.0. Breaking changes are allowed between minor versions until 1.0. Current milestone work targets v12.0 spec-driven rendering.`
REPLACE WITH: `Ferro is pre-1.0. Breaking changes are allowed between minor versions until 1.0.`

(B) Tool count (line ~13, DISC-07). The page says `80+ tools`, contradicting working-with-agents.md. Make version-neutral (no new hard count — Pitfall 4).
CURRENT (exact): `- **MCP introspection** — 80+ tools via `ferro-mcp` for agent-assisted development`
REPLACE WITH: `- **MCP introspection** — a full suite of tools via `ferro-mcp` for agent-assisted development`

Do not change other content.
  </action>
  <verify>
    <automated>! grep -q "v12.0 spec-driven rendering" docs/src/introduction.md && ! grep -q "80+ tools" docs/src/introduction.md && grep -q "pre-1.0" docs/src/introduction.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/introduction.md` does NOT contain `Current milestone work targets v12.0 spec-driven rendering`
    - `docs/src/introduction.md` does NOT contain `80+ tools`
    - `docs/src/introduction.md` still CONTAINS `Ferro is pre-1.0. Breaking changes are allowed between minor versions until 1.0.`
    - `docs/src/introduction.md` tool-count phrasing is version-neutral (no hard number) and does NOT reintroduce `57`
  </acceptance_criteria>
  <done>Status section no longer references the stale v12.0 milestone; tool count is version-neutral and consistent with working-with-agents.md.</done>
</task>

</tasks>

<threat_model>
Docs-only phase: edits static Markdown under `docs/src/` only. No executable surface, no code paths, no inputs, no auth, no data flow. No new attack surface. STRIDE: N/A.
</threat_model>

<verification>
Validation note (Nyquist Dim 8): a docs audit has no runtime behavior to validate. Verification is grep/cross-reference against the CLI source oracles (`make_scaffold.rs`, `docker_init.rs`, `mcp.rs`) and STATE.md, captured per task in acceptance_criteria. No runtime validation requirements apply.

Cross-page consistency: after this plan, the only MCP tool-count phrasings remaining in `working-with-agents.md` and `introduction.md` are version-neutral and non-contradictory. Plan 03 re-verifies the absence of any hard count.

Optional: run `mdbook build docs/` from repo root to confirm the corrected cross-link in frontend-types.md resolves (book.toml `create-missing = false` errors on broken links).
</verification>

<success_criteria>
- No phantom `make:model` anywhere in `working-with-agents.md`; replaced with real `make:scaffold`.
- No `0.2.33` pin and no broken `do-init.md` link in `frontend-types.md`.
- `migration-guide.md` "After" block uses `ferro mcp`.
- `introduction.md` milestone string current; tool counts version-neutral and consistent across pages.
</success_criteria>

<output>
After completion, create `.planning/phases/227-documentation-audit-and-update-for-v0-2-61/227-02-SUMMARY.md`
</output>
