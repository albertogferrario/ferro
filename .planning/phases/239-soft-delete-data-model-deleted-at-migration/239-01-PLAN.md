---
phase: 239-soft-delete-data-model-deleted-at-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/migrations/m20260623_add_deleted_at_to_orders.rs
  - app/src/migrations/mod.rs
  - app/src/models/entities/orders.rs
autonomous: true
requirements: [SC#1]
nyquist_compliant: true

must_haves:
  truths:
    - "A fresh db:migrate applies a nullable deleted_at column to the orders table on SQLite"
    - "The deleted_at column has no default and no NOT NULL constraint (existing rows get NULL = not deleted)"
    - "The orders sea-orm entity Model exposes deleted_at so ORM code compiles"
  artifacts:
    - path: "app/src/migrations/m20260623_add_deleted_at_to_orders.rs"
      provides: "Additive ALTER TABLE migration adding deleted_at to orders"
      contains: "add_column"
    - path: "app/src/migrations/mod.rs"
      provides: "Registration of the new migration in Migrator::migrations()"
      contains: "m20260623_add_deleted_at_to_orders"
    - path: "app/src/models/entities/orders.rs"
      provides: "Entity Model with deleted_at: Option<String>"
      contains: "deleted_at"
  key_links:
    - from: "app/src/migrations/mod.rs"
      to: "m20260623_add_deleted_at_to_orders::Migration"
      via: "Box::new(...) in the migrations() vec, after the m20260614_* entries"
      pattern: "Box::new\\(m20260623_add_deleted_at_to_orders::Migration\\)"
---

<objective>
Add the nullable `deleted_at` column to the `orders` table via a new append-only sea-orm
migration, register it, and bring the auto-generated orders entity in sync so the workspace
compiles.

Purpose: SC#1 — the soft-delete data substrate. Every CRUD read/update/delete path in v16.3
depends on this column existing on the soft-deletable table. The column is nullable with no
default (NULL = "not deleted"), making it backend-portable across SQLite and Postgres.
Output: One new migration file, one registration edit, one entity field addition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/239-soft-delete-data-model-deleted-at-migration/239-CONTEXT.md
@.planning/phases/239-soft-delete-data-model-deleted-at-migration/239-RESEARCH.md
@.planning/phases/239-soft-delete-data-model-deleted-at-migration/239-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author + register the additive deleted_at migration (D-01, D-02, D-03, D-04)</name>
  <read_first>
    - app/src/migrations/m20260611_add_tenant_id_to_users.rs (the exact additive-migration analog — copy its structure verbatim)
    - app/src/migrations/m20260611_create_orders_table.rs (the orders table being altered; confirm created_at already has DEFAULT current_timestamp for D-10)
    - app/src/migrations/mod.rs (the registration list to edit)
  </read_first>
  <files>app/src/migrations/m20260623_add_deleted_at_to_orders.rs, app/src/migrations/mod.rs</files>
  <action>
Create `app/src/migrations/m20260623_add_deleted_at_to_orders.rs` by adapting the
`m20260611_add_tenant_id_to_users.rs` analog: substitute `Orders` for `Users`, `DeletedAt`
for `TenantId`, and `.timestamp().null()` for `.big_integer().null()`. Exact file body:

```rust
use sea_orm_migration::prelude::*;

#[derive(DeriveMigrationName)]
pub struct Migration;

#[async_trait::async_trait]
impl MigrationTrait for Migration {
    async fn up(&self, manager: &SchemaManager) -> Result<(), DbErr> {
        manager
            .alter_table(
                Table::alter()
                    .table(Orders::Table)
                    .add_column(ColumnDef::new(Orders::DeletedAt).timestamp().null())
                    .to_owned(),
            )
            .await
    }

    async fn down(&self, manager: &SchemaManager) -> Result<(), DbErr> {
        manager
            .alter_table(
                Table::alter()
                    .table(Orders::Table)
                    .drop_column(Orders::DeletedAt)
                    .to_owned(),
            )
            .await
    }
}

#[derive(DeriveIden)]
enum Orders {
    Table,
    DeletedAt,
}
```

Constraints (D-04, anti-patterns from RESEARCH §297-302):
- MUST be `.timestamp().null()` — never `.not_null()`, never a default. SQLite only allows
  `ALTER TABLE ADD COLUMN` for nullable columns without a constant default.
- The `DeriveIden` enum names ONLY `Table` and `DeletedAt` — do not copy the full orders
  column list.
- Do NOT edit `m20260611_create_orders_table.rs` (migrations are append-only).

Then register in `app/src/migrations/mod.rs` (two edits, mirroring existing entries):
1. Add `mod m20260623_add_deleted_at_to_orders;` after the last `mod m20260614_*`
   declaration.
2. Append `Box::new(m20260623_add_deleted_at_to_orders::Migration),` as the LAST entry in
   the `migrations()` vec, after `Box::new(m20260614_create_audit_log_table::Migration)`.
The `20260623` date prefix sorts chronologically after all existing `20260614` entries.
  </action>
  <verify>
    <automated>cargo build -p app 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/migrations/m20260623_add_deleted_at_to_orders.rs` exists and contains all of `add_column`, `Orders::DeletedAt`, `.timestamp().null()`, and `drop_column`
    - `grep -c 'm20260623_add_deleted_at_to_orders' app/src/migrations/mod.rs` returns >= 2 (the `mod` line + the `Box::new(...)` line)
    - `grep 'Box::new(m20260623_add_deleted_at_to_orders::Migration)' app/src/migrations/mod.rs` matches
    - `grep 'not_null\|\.default(' app/src/migrations/m20260623_add_deleted_at_to_orders.rs` returns nothing (column is nullable, no default)
    - `m20260611_create_orders_table.rs` is unchanged (`git diff --quiet app/src/migrations/m20260611_create_orders_table.rs` exits 0)
    - `cargo build -p app` exits 0
  </acceptance_criteria>
  <done>New migration file authored exactly to the analog idiom, registered last in the vec; app crate compiles; the shipped create-orders migration is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Sync the orders entity Model with deleted_at + verify migration applies clean (D-04, SC#1)</name>
  <read_first>
    - app/src/models/entities/orders.rs (current Model struct — additive edit target)
    - .planning/phases/239-soft-delete-data-model-deleted-at-migration/239-PATTERNS.md (Entity Model Update section, lines ~522-564)
  </read_first>
  <files>app/src/models/entities/orders.rs</files>
  <action>
Bring the auto-generated `app/src/models/entities/orders.rs` `Model` struct into sync with
the new column. Add as the LAST field of `Model` (after `pub tenant_id: i64,`):

```rust
#[sea_orm(column_name = "deleted_at")]
pub deleted_at: Option<String>,
```

Type rationale: matches the nullable timestamp — `created_at` uses `String`, so the nullable
version is `Option<String>`. Field order matches table column order (ADD COLUMN appends, so
deleted_at is last).

Preferred path if the ferro CLI is available in the execution environment: run
`cargo run --bin app -- db:sync` (or the equivalent `ferro db migrate` + sync invocation per
the `ferro:db` skill) to regenerate the entity automatically — this produces the identical
field. If `db:sync` is not runnable here, the manual edit above is the documented-equivalent
result (PATTERNS §558-564). The file header's "DO NOT EDIT" warning is satisfied because
db:sync would generate the same field.

After the entity is in sync, verify the migration applies clean on SQLite by running the
app's migrate command against an in-memory/throwaway SQLite database:
`cargo run -p app -- db migrate` (per the `ferro:db` skill). Confirm it exits 0 and the
`deleted_at` column exists and is nullable. (Postgres application is a manual verification —
see 239-VALIDATION.md Manual-Only Verifications; if a CI Postgres matrix exists it covers
this, otherwise note it as a HUMAN-UAT item in the SUMMARY.)
  </action>
  <verify>
    <automated>cargo build -p app 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/models/entities/orders.rs` contains `deleted_at: Option<String>`
    - `cargo build -p app` exits 0 (entity matches schema; no ORM type mismatch)
    - SQLite `db migrate` runs clean (record the exact command + exit code in SUMMARY); Postgres recorded as manual/CI-matrix verification
  </acceptance_criteria>
  <done>orders entity Model exposes `deleted_at: Option<String>`; app compiles; SQLite migration applies clean; Postgres path documented for manual/CI verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → DB schema | Developer-controlled DDL; no agent/user input crosses here |
| existing rows → new column | Pre-existing data receives NULL by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-239-MIG-01 | Tampering (I) | nullable `deleted_at` with no default | accept | Nullable-no-default is the only SQLite-portable ADD COLUMN form; NULL correctly means "not deleted". No data exposure — column is metadata only, not read back to agents this phase. |
| T-239-MIG-02 | Denial of Service | append-only migration registration | mitigate | New file with unique `20260623` stem (no version-name collision per RESEARCH Pitfall 4); never edits a shipped migration, so re-running migrate is idempotent. |

No high-severity threats: this plan is a schema-only DDL change with no input boundary and
no read path. The enforcement that makes a soft-deleted row invisible (T-239-02) lives in
Plan 03; the server-injected classification (T-239-01) lives in Plan 02.
</threat_model>

<verification>
- `cargo build -p app` exits 0.
- New migration file present, registered last, and authored to the analog idiom with a
  nullable no-default column.
- `m20260611_create_orders_table.rs` byte-identical to HEAD.
- SQLite `db migrate` applies clean.
</verification>

<success_criteria>
SC#1 satisfied for SQLite: a fresh `db:migrate` adds a nullable `deleted_at` to `orders`
and the workspace compiles against the updated entity. Postgres application recorded as a
manual/CI-matrix verification item.
</success_criteria>

<output>
After completion, create `.planning/phases/239-soft-delete-data-model-deleted-at-migration/239-01-SUMMARY.md`
</output>
