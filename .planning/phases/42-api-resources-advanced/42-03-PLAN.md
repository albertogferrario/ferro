---
phase: 42-api-resources-advanced
plan: 03
type: execute
wave: 2
depends_on: ["42-01", "42-02"]
files_modified:
  - docs/src/features/api-resources.md
  - ferro-mcp/src/tools/code_templates.rs
autonomous: true
---

<objective>
Update documentation and MCP code templates to cover ResourceCollection, pagination, and relationship helpers.

Purpose: Ensure developers and agents can discover and use the new collection/pagination/relationship APIs through docs and MCP templates.
Output: Updated api-resources.md documentation with collections, pagination, and relationships sections. Updated MCP index_handler template to use ResourceCollection.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-api-resources-advanced/42-RESEARCH.md
@.planning/phases/42-api-resources-advanced/42-01-SUMMARY.md
@.planning/phases/42-api-resources-advanced/42-02-SUMMARY.md

@docs/src/features/api-resources.md
@ferro-mcp/src/tools/code_templates.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update API Resources documentation with collections, pagination, and relationships</name>
  <files>docs/src/features/api-resources.md</files>
  <action>
Extend the existing api-resources.md documentation with three new sections. Add them after the existing content (which covers Resource trait, ResourceMap, and ApiResource derive).

**Section: Resource Collections**
- `ResourceCollection::new(items)` for simple collections → `{"data": [...]}`
- `ResourceCollection::paginated(items, meta)` for paginated collections → `{"data": [...], "meta": {...}, "links": {...}}`
- `.additional(json!({...}))` for extra top-level metadata
- `Resource::collection(&items, &req)` convenience for mapping to JSON values
- Show a complete handler example returning a simple collection

**Section: Pagination**
- How to integrate with SeaORM's PaginatorTrait
- `PaginationMeta::new(page, per_page, total)` — accepts 1-indexed page
- Complete paginated handler example showing the full flow: paginate query → fetch page → get totals → build meta → ResourceCollection::paginated → to_response
- Show the JSON output format for meta and links
- Note: SeaORM's fetch_page() is 0-indexed, PaginationMeta handles the conversion

**Section: Relationship Inclusion**
- Explain Ferro's explicit batch-loading model vs Laravel's whenLoaded()
- `when_loaded()` for belongs_to/has_one from HashMap
- `when_loaded_many()` for has_many from HashMap
- Complete handler example: paginate parent → batch load relations → construct resources with when_loaded → return ResourceCollection
- Anti-patterns: N+1 inside to_resource(), paginating joined queries

Keep the same documentation style as the existing sections in the file. Use concise code examples. No marketing language.
  </action>
  <verify>wc -l docs/src/features/api-resources.md shows increased line count; file has sections for Collections, Pagination, and Relationships</verify>
  <done>Documentation covers ResourceCollection, pagination with PaginationMeta, and relationship inclusion with when_loaded/when_loaded_many. Complete handler examples for each pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Update MCP index_handler code template to use ResourceCollection</name>
  <files>ferro-mcp/src/tools/code_templates.rs</files>
  <action>
Update the `index_handler` template (around line 78) to use `ResourceCollection` and `PaginationMeta` instead of manual `json!({})` envelope construction.

Replace the current template code with a version that uses:
- `PaginationMeta::new(page, per_page, total)` instead of manual meta JSON
- `ResourceCollection::paginated(items, meta).to_response(&req)` instead of manual `json!({...})`

Keep the same placeholder pattern ({{Entity}}, {{entity}}) and structure. Update the imports to include ResourceCollection and PaginationMeta.

Also update the template description to mention ResourceCollection.

Do NOT change other templates (show, store, update, destroy) — they don't deal with collections.
  </action>
  <verify>cargo build --package ferro-mcp</verify>
  <done>MCP index_handler template uses ResourceCollection and PaginationMeta. Template compiles. Other templates unchanged.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo build --all-features` succeeds
- [ ] `cargo clippy --all-features` has no warnings
- [ ] `cargo fmt --check` passes
- [ ] Documentation file has Collections, Pagination, and Relationships sections
- [ ] MCP code template uses ResourceCollection pattern
</verification>

<success_criteria>

- Documentation covers all Phase 42 features with complete examples
- MCP template guides agents toward ResourceCollection pattern
- All compilation checks pass
  </success_criteria>

<output>
After completion, create `.planning/phases/42-api-resources-advanced/42-03-SUMMARY.md`
</output>
