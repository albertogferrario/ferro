---
phase: 249.5
slug: offload-dispatch-key-reconciliation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-15
---

# Phase 249.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Load-bearing validation: the non-crate-root enqueue→claim→dispatch integration
> test for an `#[offload]`-**derived** job. It must be RED before the macro fix and
> GREEN after — a hand-written `impl Job` would pass in both states and prove nothing.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in test + `tokio::test` (+ `trybuild` for macro fixtures) |
| **Config file** | none — workspace `Cargo.toml` |
| **Quick run command** | `cargo test -p ferro-queue --test dispatch_key_derived_job` |
| **Full suite command** | `cargo test -p ferro-queue --all-features && cargo test -p ferro-macros --all-features` |
| **Estimated runtime** | ~60 seconds (ferro-macros recompile dominates) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ferro-queue --test dispatch_key_derived_job` (and `cargo test -p ferro-macros --test offload_macro` for the macro-fix task)
- **After every plan wave:** Run `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test -p ferro-queue --all-features && cargo test -p ferro-macros --all-features`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| W0 — derived-job dispatch test | 01 | 0 | OFFLOAD-01 | — / — | Derived job enqueued on db path is claimed + dispatched exactly once; never release-looped | integration | `cargo test -p ferro-queue --test dispatch_key_derived_job` | ❌ W0 | ⬜ pending |
| W1 — macro `name()` reconciliation | 02 | 1 | OFFLOAD-01 | — / — | Derived `name()` returns `type_name::<Self>()`; enqueue key == worker registration key | integration | `cargo test -p ferro-queue --test dispatch_key_derived_job` | ✅ (after W0) | ⬜ pending |
| W1 — macro doc-comment accuracy | 02 | 1 | OFFLOAD-01 | — / — | Emitted `name()` matches the module doc claim (`type_name`) | compile | `cargo test -p ferro-macros --test offload_macro` | ✅ | ⬜ pending |
| W1 — trybuild fixtures still pass | 02 | 1 | OFFLOAD-01 | — / — | `#[offload]` fixtures compile unchanged | compile | `cargo test -p ferro-macros --test offload_macro` | ✅ | ⬜ pending |
| W1 — hand-written jobs unchanged | 02 | 1 | OFFLOAD-04/05 | — / — | Default `name()` = `type_name` path still dispatches | regression | `cargo test -p ferro-queue --all-features` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Note: exact numeric task IDs are assigned by the planner; rows above map behaviors to waves.*

---

## Wave 0 Requirements

- [ ] `ferro-queue/tests/dispatch_key_derived_job.rs` — new integration test binary covering OFFLOAD-01 db-path dispatch for an `#[offload]`-derived job in a non-crate-root module. Authored to FAIL against the current (unfixed) macro (RED), proving it exercises the dispatch-key seam.

*All other test infrastructure (SQLite temp-file harness, `CreateJobsTable` migrator, `WorkerLoop::drain_for_test`, `serial_test`) already exists.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| — | — | — | — |

*All phase behaviors have automated verification.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
