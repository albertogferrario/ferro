---
phase: 32-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [docs/src/json-ui/components.md]
autonomous: true
---

<objective>
Create the JSON-UI component reference page documenting all 20 components.

Purpose: Provide developers with a complete reference of every JSON-UI component including props, variants, and usage examples.
Output: json-ui/components.md with all 20 components documented.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-documentation/32-RESEARCH.md

@ferro-json-ui/src/component.rs
@ferro-mcp/src/tools/json_ui_catalog.rs
@framework/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document display and data components (Card, Table, Badge, Alert, Separator, Stats, DescriptionList, Text)</name>
  <files>docs/src/json-ui/components.md</files>
  <action>
Create docs/src/json-ui/components.md with the full component reference. This task writes the page header and the first group of components.

**Page structure:**

1. **H1: Components** — One sentence: JSON-UI includes 20 built-in components for building complete application interfaces.

2. **Component Overview** — Quick reference table listing all 20 components grouped by category:
   - **Display**: Card, Table, Badge, Alert, Separator, Stats, DescriptionList, Text
   - **Form**: Form, Input, Select, Textarea, Checkbox, Switch
   - **Navigation**: Tabs, Pagination, Breadcrumbs
   - **Layout**: Modal, Container, Stack

3. **Shared Types** — Document shared enums used across components:
   - `Size` (Xs, Sm, Md, Lg, Xl)
   - `ButtonVariant` (Default, Destructive, Outline, Secondary, Ghost, Link)
   - `AlertVariant` (Info, Success, Warning, Error)
   - `BadgeVariant` (Default, Secondary, Destructive, Outline)

4. **Display Components section** — For each component:
   - H3 with component name
   - One-sentence description
   - Props table: | Prop | Type | Required | Default | Description |
   - Minimal Rust code example (3-10 lines showing ComponentNode construction)
   - Notes on defaults or common patterns where relevant

Read `ferro-json-ui/src/component.rs` for exact prop types and required fields. Read `ferro-mcp/src/tools/json_ui_catalog.rs` for component descriptions.

**Component documentation pattern (from 32-RESEARCH.md):**
- Name + one-sentence description
- Props table (name, type, required, default, description)
- Variants list with meaning (if applicable)
- Minimal Rust code example
- Notes (default behavior, common patterns)

Keep each component section concise: ~15-30 lines. Complex components (Table, Card) can be longer.

For Table specifically, document:
- TableProps (columns, rows, actions, pagination, searchable, filterable)
- TableColumn (key, label, sortable, format)
- ColumnFormat enum (Text, Date, Currency, Badge, Boolean, Custom)
- TableRow and how data maps to columns
- Show a realistic example with 2-3 columns

For Card, document:
- CardProps (title, description, children, footer)
- Show nesting children components
  </action>
  <verify>docs/src/json-ui/components.md exists. Contains page header, overview table, shared types section, and all 8 display components documented with props tables.</verify>
  <done>Components page has complete header, overview, shared types, and all display component documentation.</done>
</task>

<task type="auto">
  <name>Task 2: Document form, navigation, and layout components</name>
  <files>docs/src/json-ui/components.md</files>
  <action>
Append to docs/src/json-ui/components.md the remaining component groups.

**Form Components section:**
- Form: FormProps (action, fields, method). Show form with Input children.
- Input: InputProps (field, label, input_type, placeholder, required, data_path, error, help_text, disabled). Document InputType enum (Text, Email, Password, Number, Tel, Url, Date, Time, Hidden, Search, File, Color). Show basic example.
- Select: SelectProps (field, label, options, required, data_path, error, placeholder). Document SelectOption struct. Show example with options list.
- Textarea: TextareaProps (field, label, placeholder, required, rows, data_path, error). Brief, similar to Input.
- Checkbox: CheckboxProps (field, label, data_path, error). Note: same props as Switch, visual difference handled by frontend.
- Switch: SwitchProps (field, label, data_path, error). Brief cross-reference to Checkbox.

**Navigation Components section:**
- Tabs: TabsProps (tabs, default_tab). Document TabItem struct (id, label, content vec). Show example with 2 tabs.
- Pagination: PaginationProps (current_page, total_pages, base_url). Show basic example.
- Breadcrumbs: BreadcrumbsProps (items). Document BreadcrumbItem struct (label, url). Show example.

**Layout Components section:**
- Modal: ModalProps (title, trigger, body, size). Document trigger as ComponentNode. Show example with Button trigger.
- Container: ContainerProps (children, padding, max_width). Brief.
- Stack: StackProps (children, direction, gap, align). Document Direction enum (Horizontal, Vertical). Show layout example.

Same pattern per component: description, props table, code example, notes.

After all components, add a brief **JSON Output** section showing that any component tree serializes to JSON via serde. Show one small JSON snippet of a Card to demonstrate the `{"type": "Card", "title": "...", ...}` format.
  </action>
  <verify>docs/src/json-ui/components.md now documents all 20 components across 4 groups. Each component has a props table and code example. JSON output section exists at the end.</verify>
  <done>Complete component reference with all 20 components documented, props tables, code examples, and JSON serialization note.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] docs/src/json-ui/components.md exists with full content
- [ ] All 20 components documented: Card, Table, Badge, Alert, Separator, Stats, DescriptionList, Text, Form, Input, Select, Textarea, Checkbox, Switch, Tabs, Pagination, Breadcrumbs, Modal, Container, Stack
- [ ] Each component has: description, props table, code example
- [ ] Shared types (Size, ButtonVariant, AlertVariant, BadgeVariant) documented
- [ ] Props match actual struct definitions in ferro-json-ui/src/component.rs
- [ ] Code examples use correct `use ferro::{...}` imports
</verification>

<success_criteria>

- All tasks completed
- All 20 components documented
- Props tables are accurate to source code
- Code examples are idiomatic Ferro Rust
  </success_criteria>

<output>
After completion, create `.planning/phases/32-documentation/32-02-SUMMARY.md`
</output>
