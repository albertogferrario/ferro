# Phase 206 — Plan 01: STORAGE_* rename + workspace 0.2.54

**Wave:** 0 (single wave — no plan dependencies)
**Goal:** Apply the Phase 204 provider-agnostic naming pattern to the six S3-style env vars. Bump workspace 0.2.53 → 0.2.54. Single cargo cycle.

---

## Success Criteria (mapped from CONTEXT.md)

- **SC-1** — `StorageConfig::from_env` reads `STORAGE_BUCKET` / `STORAGE_REGION` / `STORAGE_ENDPOINT` / `STORAGE_PUBLIC_URL` primary via `env_with_fallback`
- **SC-2** — `S3Driver::new` reads `STORAGE_ACCESS_KEY_ID` / `STORAGE_SECRET_KEY` primary via `env_with_fallback`
- **SC-3** — `Storage::create_driver` (facade.rs) reads `STORAGE_ENDPOINT` primary via `env_with_fallback`
- **SC-4** — `env_with_fallback` lives at crate-level (`src/env_helpers.rs`); `cdn::mod` no longer defines a private copy
- **SC-5** — Legacy `AWS_*` fallback path: existing `from_env_cdn_url` + `cdn_url_parity_aws_fallback` tests continue to pass byte-identical
- **SC-6** — New primary-path test `from_env_storage_primary` asserts `STORAGE_BUCKET` registers the s3 disk with the expected `bucket` / `region` / `url`
- **SC-7** — `ferro/app/.env.example` declares the `STORAGE_*` set as primary with a deprecation note for `AWS_*`
- **SC-8** — Workspace `Cargo.toml` `version = "0.2.54"`; `ferro-storage/CHANGELOG.md` has a `## [0.2.54]` entry with the deprecation table
- **SC-9** — `cargo check -p ferro-storage --features s3` exits 0
- **SC-10** — `cargo test -p ferro-storage --features s3 --lib config::tests` → 7/7 PASS

---

## Tasks (sequential — atomic commit each)

### Task 1: Hoist env_with_fallback to env_helpers
**Files:** `ferro-storage/src/env_helpers.rs` (new), `ferro-storage/src/lib.rs`, `ferro-storage/src/cdn/mod.rs`

### Task 2: Rename config.rs s3 block reads
**File:** `ferro-storage/src/config.rs`

`AWS_BUCKET` → `STORAGE_BUCKET`, `AWS_DEFAULT_REGION` → `STORAGE_REGION`, `AWS_URL` → `STORAGE_ENDPOINT`, `AWS_PUBLIC_URL` → `STORAGE_PUBLIC_URL`. All via `env_with_fallback("NEW", &["OLD"])`. Update doc comments at lines 48-54. Add `from_env_storage_primary` test.

### Task 3: Rename drivers/s3.rs + facade.rs reads
**Files:** `ferro-storage/src/drivers/s3.rs`, `ferro-storage/src/facade.rs`

`AWS_ACCESS_KEY_ID` → `STORAGE_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY` → `STORAGE_SECRET_KEY` in `S3Driver::new`. `AWS_URL` → `STORAGE_ENDPOINT` in `create_driver`. Update doc comment at s3.rs:27.

### Task 4: Tests + .env.example + CHANGELOG + version bump
**Files:** `ferro-storage/tests/s3_integration.rs`, `app/.env.example`, `ferro-storage/CHANGELOG.md`, `Cargo.toml`, `Cargo.lock`

Integration test: skip-gate now checks `STORAGE_BUCKET || AWS_BUCKET`; URL test reads STORAGE_* primary with AWS_* fallback. `.env.example`: replace `AWS_*` block with `STORAGE_*` quartet + deprecation note. CHANGELOG: `## [0.2.54]` entry. `Cargo.toml`: `[workspace.package] version = "0.2.54"`. Single `cargo check -p ferro-storage --features s3` cycle.

### Task 5 (closeout): ROADMAP + phase dir + STATE
**Files:** `.planning/ROADMAP.md`, `.planning/phases/206-.../`, `.planning/STATE.md`

Phase 206 entry under Phase 204 in ROADMAP; CONTEXT + PLAN + VERIFICATION + SUMMARY in phase dir; STATE.md marks Phase 206 complete.

---

## Out of scope

- Removing the legacy `AWS_*` fallback (one release cushion — slated for 0.2.55+).
- gestiscilo consumer phase (mirrors Phase 205 — separate consumer ticket).
- Provider-agnostic rename of any other ferro env vars (queue/cache/etc.).

---

## Risk register

| Risk | Mitigation |
|---|---|
| Existing AWS_* parity tests break | Tests are unchanged; rename uses `env_with_fallback`. Confirmed via `cargo test config::tests` → 7/7 PASS. |
| External consumers (gestiscilo, others) use AWS_* and skip the upgrade | Deprecation cushion: AWS_* still works for one release with `tracing::warn!`. Consumer migration is a follow-up. |
| Workspace version churn (0.2.53 → 0.2.54 in same push wave) | crates.io publish-update token handles this; both releases publish atomically. |

---

## Estimated effort

5 commits, ~30 minutes including single cargo check + scoped test cycle.
