---
phase: 108-p0-accuracy-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/reference/cli.md
  - docs/src/features/storage.md
  - README.md
autonomous: true
requirements:
  - ACC-02
  - ACC-03
  - ACC-04
  - ACC-05

must_haves:
  truths:
    - "Zero // TODO: Implement stubs in docs/src/reference/cli.md — every example shows runnable logic"
    - "README.md does not claim JSON-UI is 'Work in Progress' — it reflects shipped status"
    - "Storage docs do not say S3 is 'coming soon' — it reflects shipped status"
    - "No incorrect MCP tool count claims exist in docs"
  artifacts:
    - path: "docs/src/reference/cli.md"
      provides: "CLI reference with real example bodies instead of TODO stubs"
    - path: "docs/src/features/storage.md"
      provides: "Storage docs with correct S3 shipped status"
    - path: "README.md"
      provides: "README with accurate feature status claims"
  key_links: []
---

<objective>
Remove all factually wrong status claims from CLI reference, storage docs, and README: replace TODO stubs with real example logic, correct S3 "coming soon" to shipped, and update README roadmap section to reflect JSON-UI as shipped.

Purpose: Wrong information in docs contaminates every agent and user who reads them. Stale TODO stubs make CLI scaffold output look incomplete. False "coming soon" / "Work in Progress" labels erode trust.
Output: 3 corrected files with accurate status claims and real example code.
</objective>

<execution_context>
@/Users/alberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-p0-accuracy-fixes/108-CONTEXT.md
@.planning/phases/108-p0-accuracy-fixes/108-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace CLI TODO stubs with real example logic</name>
  <files>docs/src/reference/cli.md</files>
  <action>
Replace 8 in-scope `// TODO: Implement` stubs in docs/src/reference/cli.md with minimal but real logic. Use generic names (Item, Resource) not domain-specific. Each replacement should be 1-2 lines of real logic showing the idiomatic pattern.

The middleware.md stub at line 214 is IN cli.md but is OUT OF SCOPE per CONTEXT.md decisions — do NOT touch it.

Specific replacements (line numbers from research):

1. **Line 181** (`make:controller` index handler): Delete the `// TODO: Implement` comment line. The `json_response!({ "message": "index" })` on line 182 is already real logic.

2. **Line 187** (`make:controller` show handler): Delete the `// TODO: Implement` comment line. The `json_response!({ "message": "show" })` on line 188 is already real logic.

3. **Line 214** (`make:middleware`): SKIP — OUT OF SCOPE, deferred to Phase 113.

4. **Line 238** (`make:action` execute method): Replace `// TODO: Implement action` with `tracing::info!("executing resource action");`

5. **Lines 317-318** (`make:listener` handle method): Replace `// TODO: Implement listener` with `tracing::info!("handling event");`

6. **Lines 347-348** (`make:job` handle method): Replace `// TODO: Implement job` with `tracing::info!(id = self.image_id, "processing item");`

7. **Lines 398-399** (`make:migration` up method): Replace `// TODO: Implement migration` + `Ok(())` with a real SeaORM table creation example. Add the `Item` Iden enum before the migration struct, and replace the stub body with:
```rust
        manager
            .create_table(
                Table::create()
                    .table(Item::Table)
                    .if_not_exists()
                    .col(ColumnDef::new(Item::Id).integer().not_null().auto_increment().primary_key())
                    .col(ColumnDef::new(Item::Name).string().not_null())
                    .col(ColumnDef::new(Item::CreatedAt).timestamp().not_null())
                    .to_owned(),
            )
            .await
```

8. **Lines 403-404** (`make:migration` down method): Replace `// TODO: Implement rollback` + `Ok(())` with:
```rust
        manager
            .drop_table(Table::drop().table(Item::Table).to_owned())
            .await
```

For the migration, add the Iden enum before the Migration struct:
```rust
#[derive(DeriveIden)]
enum Item {
    Table,
    Id,
    Name,
    CreatedAt,
}
```

9. **Line 511** (`make:task` handle method): Replace `// TODO: Implement task` with `tracing::info!("running scheduled task");`

After all replacements, the file should contain exactly one remaining TODO stub: the middleware one at (formerly) line 214, which is out of scope.
  </action>
  <verify>
    <automated>grep -c "// TODO: Implement" docs/src/reference/cli.md</automated>
  </verify>
  <done>grep -c "// TODO: Implement" docs/src/reference/cli.md returns exactly 1 (the out-of-scope middleware stub). All other 8 stubs have been replaced with real example logic using generic names.</done>
</task>

<task type="auto">
  <name>Task 2: Fix storage S3 status + README accuracy audit</name>
  <files>docs/src/features/storage.md, README.md</files>
  <action>
Two file edits:

**1. Storage docs (ACC-04):**
In docs/src/features/storage.md, line 285, change:
```
Requires the `s3` feature (coming soon):
```
to:
```
Enable the `s3` feature:
```
This is the only change in this file.

**2. README accuracy audit (ACC-03, ACC-05):**
Read README.md and fix all factually wrong claims. Known issues:

a) **Crates.io badge** (line 5): Links to `crates.io/crates/ferro-rs` — the crate was renamed to `ferro`. Update badge URL to `crates.io/crates/ferro` and shield to reference `ferro` not `ferro-rs`.

b) **Website/docs links** (line 10): Verify `ferro-rs.dev` and `docs.ferro-rs.dev` are the correct current URLs. If they are still the active domains, leave them. If incorrect, fix.

c) **JSON-UI roadmap section** (lines 59-88): The entire "Roadmap" section heading + JSON-UI block presents JSON-UI as a work-in-progress future feature. JSON-UI has been shipped since v3.0 (Feb 2026). Options:
   - Replace the `### 🚧 JSON-UI (Work in Progress)` heading with `### JSON-UI` (remove the WIP marker)
   - Update the descriptive text to present JSON-UI as a shipped feature, not future work
   - Keep the code example and bullet points (they describe real, shipped functionality)
   - Remove the `## Roadmap` heading if there's nothing actually in-progress to show — or rename it to something accurate

d) **MCP tool count (ACC-05):** Research confirmed there are currently NO numeric tool count claims in the README or docs/src/. The "5 tools validated" in api-mcp.md refers to a user's API endpoints, not ferro-mcp tools — it is correct. If you do NOT add any new tool count claims, ACC-05 is satisfied by verifying no wrong count exists. Do not add a count claim unless there is a natural place for it.

**Constraints:**
- Accuracy fixes ONLY — no tone, positioning, or marketing changes
- Do not add new sections or content beyond what is needed to fix wrong claims
- Agent-first rewrite is Phase 112 — do not change the value proposition
  </action>
  <verify>
    <automated>grep -n "coming soon" docs/src/features/storage.md | wc -l && grep -n "Work in Progress" README.md | wc -l</automated>
  </verify>
  <done>"coming soon" does not appear in storage.md. "Work in Progress" does not appear in README.md. All factual claims in README are accurate — JSON-UI reflected as shipped, crate badges reference correct name, no incorrect MCP tool counts exist.</done>
</task>

</tasks>

<verification>
- `grep -n "// TODO: Implement" docs/src/reference/cli.md` returns exactly 1 line (the out-of-scope middleware stub)
- `grep -n "coming soon" docs/src/features/storage.md` returns zero results
- `grep -n "Work in Progress" README.md` returns zero results
- `grep -n "ferro-rs" README.md` — verify any remaining references are intentional (domain names if still active) vs stale (crate name references)
- No numeric MCP tool count claims exist anywhere in docs (or if added, they say 65)
</verification>

<success_criteria>
1. CLI reference examples contain zero TODO stubs (except the deferred middleware one)
2. README accurately reflects JSON-UI as a shipped feature
3. Storage docs present S3 as a shipped feature
4. No incorrect MCP tool count claims exist in any documentation
</success_criteria>

<output>
After completion, create `.planning/phases/108-p0-accuracy-fixes/108-02-SUMMARY.md`
</output>
