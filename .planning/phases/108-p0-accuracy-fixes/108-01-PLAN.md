---
phase: 108-p0-accuracy-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/features/multi-tenancy.md
  - docs/src/json-ui/actions.md
  - docs/src/json-ui/data-binding.md
autonomous: true
requirements:
  - ACC-01

must_haves:
  truths:
    - "Zero occurrences of ferro_rs:: in docs/src/ — grep returns no results"
    - "All code examples in multi-tenancy.md, actions.md, data-binding.md compile with ferro:: import path"
  artifacts:
    - path: "docs/src/features/multi-tenancy.md"
      provides: "Multi-tenancy documentation with correct ferro:: imports"
      contains: "use ferro::"
    - path: "docs/src/json-ui/actions.md"
      provides: "JSON-UI actions documentation with correct ferro:: imports"
      contains: "use ferro::"
    - path: "docs/src/json-ui/data-binding.md"
      provides: "JSON-UI data-binding documentation with correct ferro:: imports"
      contains: "use ferro::"
  key_links: []
---

<objective>
Replace all 24 stale `ferro_rs::` import paths with the correct `ferro::` path across 3 documentation files.

Purpose: `ferro_rs` was the pre-rebrand crate name. These stale imports in docs cause confusion for agents and users who copy-paste examples — the code will not compile.
Output: 3 corrected documentation files with zero `ferro_rs::` occurrences.
</objective>

<execution_context>
@/Users/alberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-p0-accuracy-fixes/108-CONTEXT.md
@.planning/phases/108-p0-accuracy-fixes/108-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ferro_rs:: with ferro:: across 3 doc files</name>
  <files>docs/src/features/multi-tenancy.md, docs/src/json-ui/actions.md, docs/src/json-ui/data-binding.md</files>
  <action>
In each of the 3 files, replace every occurrence of `ferro_rs::` with `ferro::`. There are 8 occurrences per file (24 total). All occurrences are inside fenced `rust` code blocks — they are `use ferro_rs::` import statements.

Steps:
1. Read docs/src/features/multi-tenancy.md — replace all `ferro_rs::` with `ferro::` (8 occurrences)
2. Read docs/src/json-ui/actions.md — replace all `ferro_rs::` with `ferro::` (8 occurrences)
3. Read docs/src/json-ui/data-binding.md — replace all `ferro_rs::` with `ferro::` (8 occurrences)
4. Write each file back with replacements applied

Do NOT change any prose text, headings, or formatting — only the import paths inside code blocks.
Do NOT touch any other files in docs/src/.
  </action>
  <verify>
    <automated>grep -rn "ferro_rs::" docs/src/ | wc -l</automated>
  </verify>
  <done>grep -rn "ferro_rs::" docs/src/ returns 0 lines. All 24 occurrences across multi-tenancy.md, actions.md, and data-binding.md have been replaced with ferro::.</done>
</task>

</tasks>

<verification>
- `grep -rn "ferro_rs::" docs/src/` returns zero results
- `grep -c "ferro::" docs/src/features/multi-tenancy.md` returns >= 8
- `grep -c "ferro::" docs/src/json-ui/actions.md` returns >= 8
- `grep -c "ferro::" docs/src/json-ui/data-binding.md` returns >= 8
</verification>

<success_criteria>
Zero occurrences of `ferro_rs::` remain anywhere in docs/src/. All import paths now reference the correct `ferro::` crate name.
</success_criteria>

<output>
After completion, create `.planning/phases/108-p0-accuracy-fixes/108-01-SUMMARY.md`
</output>
