---
phase: 220-confirmation-gating-for-destructive-actions
plan: 02
type: execute
wave: 3
depends_on: ["220-01"]
files_modified:
  - app/Cargo.toml
  - app/src/controllers/mcp.rs
autonomous: true
requirements: [AMCP-05]
must_haves:
  truths:
    - "The sample app registers an InMemoryConfirmationStore and threads it into the MCP write path"
    - "Feature-OFF: cargo build -p ferro-mcp-server pulls no ferro-ai and no reqwest; read tools + 219 tests unaffected"
    - "Feature-ON: ferro-ai present, reqwest absent"
    - "The full --all-features CI gate (fmt + clippy + test) is green"
  artifacts:
    - path: "app/src/controllers/mcp.rs"
      provides: "ConfirmationStore wiring alongside the 219 dispatcher (behind app confirmation feature)"
      contains: "InMemoryConfirmationStore"
  key_links:
    - from: "app/src/controllers/mcp.rs"
      to: "handle_tools_call (store + config params)"
      via: "InMemoryConfirmationStore::new() threaded under #[cfg(feature)]"
      pattern: "InMemoryConfirmationStore"
---

<objective>
Wave 3: wire the sample `app`'s MCP endpoint to register an `InMemoryConfirmationStore` and thread it (plus `McpServerConfig`) into `handle_tools_call`, then assert the feature-off build graph (no ferro-ai, no reqwest — SC#5) and run the full `--all-features` CI gate.

Purpose: SC#5 is a build-graph guarantee, not just a runtime one: a consumer that never enables `confirmation` must see zero new dependencies and identical read-tool behavior. This plan proves that with `cargo tree`, exercises the end-to-end flow through the real sample controller, and closes the phase on the full CI gate.

Output: A sample app that exercises the confirm flow; verified feature-off purity; green `cargo fmt + clippy --all-targets --all-features + test --all-features`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/220-confirmation-gating-for-destructive-actions/220-CONTEXT.md
@.planning/phases/220-confirmation-gating-for-destructive-actions/220-RESEARCH.md
@.planning/phases/220-confirmation-gating-for-destructive-actions/220-PATTERNS.md
@.planning/phases/220-confirmation-gating-for-destructive-actions/220-VALIDATION.md
@.planning/phases/220-confirmation-gating-for-destructive-actions/220-01-SUMMARY.md

<interfaces>
<!-- Consumed directly. -->

app/src/controllers/mcp.rs (existing) — thin adapter:
- `make_write_dispatcher() -> WriteDispatcher` (line ~52) builds the concrete dispatcher.
- calls `handle_tools_call(call_params, services, db, tenant_id, ctx, &dispatcher)` (line ~210 area).
After Plan 01, `handle_tools_call` takes `#[cfg(feature = "confirmation")] store: &dyn ferro_ai::ConfirmationStore` and `#[cfg(feature = "confirmation")] config: &McpServerConfig`.

Store construction (ferro-ai, reqwest-free under confirmation feature):
```rust
#[cfg(feature = "confirmation")]
use ferro_mcp_server::... ; // or ferro_ai::InMemoryConfirmationStore
let store = ferro_ai::InMemoryConfirmationStore::new(); // construct once; share across requests (Arc)
```
The app needs its OWN `confirmation` feature that forwards to `ferro-mcp-server/confirmation` (and pulls ferro-ai). Mirror the framework `ai` optional-feature shape.

Feature-off build assertions (NOT #[test]s — run in this plan's verify):
```
cargo build -p ferro-mcp-server
cargo tree -p ferro-mcp-server --edges normal | grep -c ferro-ai   # == 0
cargo tree -p ferro-mcp-server --edges normal | grep -c reqwest    # == 0
cargo build -p ferro-mcp-server --features confirmation
cargo tree -p ferro-mcp-server --features confirmation --edges normal | grep -c reqwest  # == 0
```

Final CI gate (CLAUDE.md exact command):
```
cargo fmt --all -- --check
cargo clippy --all --all-targets --all-features -- -D warnings
cargo test --all-features
```
Note (MEMORY): `--all-features` test can disk-full on a constrained machine; if ENOSPC link/fingerprint errors appear, `df` + clean `target/` and retry — these are not real defects. The Cargo.toml dev/test profile already sets debug=false/incremental=false/strip=true.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire InMemoryConfirmationStore into the sample app MCP endpoint</name>
  <files>app/Cargo.toml, app/src/controllers/mcp.rs</files>
  <read_first>
    - app/src/controllers/mcp.rs (the full file — make_write_dispatcher at ~52, the handle_tools_call call at ~210, the per-request handler structure)
    - app/Cargo.toml (ferro-mcp-server dependency line; how features are declared; the framework `ai` optional-feature shape for reference)
    - 220-RESEARCH.md §"Pattern: Confirmation Request/Execute Flow" + §Architecture "app/src/controllers/mcp.rs wires ConfirmationStore"
    - 220-PATTERNS.md §"Guard re-evaluation at confirm time" (the dispatcher is already wired; the store is the new addition)
  </read_first>
  <action>
    app/Cargo.toml: add an app-level `confirmation` feature forwarding to ferro-mcp-server: `confirmation = ["ferro-mcp-server/confirmation"]` (this transitively pulls ferro-ai with the confirmation feature, reqwest-free). Keep it OFF by default so the default app build stays HTTP-client-free for the read path.

    app/src/controllers/mcp.rs: construct an `InMemoryConfirmationStore` once (shared via `Arc`, or an `OnceLock`/state field consistent with how the dispatcher is held) under `#[cfg(feature = "confirmation")]`. Build the `McpServerConfig` (already used for handle_initialize) and pass both `&store` and `&config` into `handle_tools_call` at the call site — feature-gated so the non-confirmation build calls the original arity. Keep the change minimal and additive; the existing read + non-destructive write paths must be untouched when the feature is off.
  </action>
  <verify>
    <automated>cargo build -p app 2>&1 | tail -3; cargo build -p app --features confirmation 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'InMemoryConfirmationStore' app/src/controllers/mcp.rs` exits 0
    - `grep -q 'confirmation = \["ferro-mcp-server/confirmation"\]' app/Cargo.toml` exits 0
    - `cargo build -p app` exits 0 (default, feature off)
    - `cargo build -p app --features confirmation` exits 0
  </acceptance_criteria>
  <done>The sample app registers the confirmation store and threads it into the MCP write path behind its own `confirmation` feature; both default and confirmation builds compile.</done>
</task>

<task type="auto">
  <name>Task 2: Feature-off build-graph assertion + full --all-features CI gate</name>
  <files>(no file edits — build-graph + CI gate verification only)</files>
  <read_first>
    - 220-VALIDATION.md §"Per-Task Verification Map" rows 220-02-01 / 220-02-02 (the exact build/tree/CI commands)
    - 220-RESEARCH.md §"Feature-Off Correctness (SC#5)" (the cargo tree grep assertions)
    - CLAUDE.md §"Testing & Linting" (the exact CI gate command, including `--all-targets --all-features`)
    - MEMORY note on `--all-features` disk-full (project_ferro_disk_full_test_gate) — df + clean target on ENOSPC, not a defect
  </read_first>
  <action>
    Run the feature-off build-graph assertions (SC#5): `cargo build -p ferro-mcp-server` then `cargo tree -p ferro-mcp-server --edges normal | grep -c ferro-ai` (must be 0) and `| grep -c reqwest` (must be 0); confirm the existing 219 tests stay green with `cargo test -p ferro-mcp-server` (no feature). Then `cargo build -p ferro-mcp-server --features confirmation` and `cargo tree -p ferro-mcp-server --features confirmation --edges normal | grep -c reqwest` (must be 0 — ferro-ai present, reqwest absent).

    Then run the full CI gate verbatim (CLAUDE.md): `cargo fmt --all -- --check` → `cargo clippy --all --all-targets --all-features -- -D warnings` → `cargo test --all-features`. Serialize these (one CPU-intensive op at a time per MEMORY). If ENOSPC appears, `df -h`, clean `target/`, retry — not a real failure. Record the schema-export test tree churn note: `git checkout docs/protocol/schemas/` if the test regenerates them (per MEMORY), do not fold into the phase commit.
  </action>
  <verify>
    <automated>cargo build -p ferro-mcp-server >/dev/null 2>&1 && echo "ferro-ai:$(cargo tree -p ferro-mcp-server --edges normal | grep -c ferro-ai) reqwest:$(cargo tree -p ferro-mcp-server --edges normal | grep -c reqwest)"; cargo fmt --all -- --check && cargo clippy --all --all-targets --all-features -- -D warnings 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `cargo tree -p ferro-mcp-server --edges normal | grep -c ferro-ai` == 0
    - `cargo tree -p ferro-mcp-server --edges normal | grep -c reqwest` == 0
    - `cargo tree -p ferro-mcp-server --features confirmation --edges normal | grep -c reqwest` == 0
    - `cargo test -p ferro-mcp-server` (no feature) passes — all 219 tests green
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy --all --all-targets --all-features -- -D warnings` exits 0
    - `cargo test --all-features` passes
  </acceptance_criteria>
  <done>SC#5 proven at the build-graph level (no ferro-ai/reqwest in the default ferro-mcp-server tree; reqwest absent even with confirmation on); the full `--all-features` fmt+clippy+test CI gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| default (read-only) consumer build → dependency graph | A read-only consumer must not be forced to compile or ship an HTTP client / confirmation machinery (SC#5). |
| sample app request lifecycle → shared confirmation store | Per-request handlers share one store; tenant binding (Plan 01) prevents cross-tenant retrieval. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-220-D06 | Information Disclosure / supply-chain | ferro-mcp-server default build graph | mitigate | Task 2: `cargo tree ... grep -c ferro-ai == 0` and `grep -c reqwest == 0` assert the feature-off graph is HTTP-client-free; reqwest absent even with confirmation on. |
| T-220-01..03 | Tampering / EoP | end-to-end confirm flow through real controller | mitigate | Task 1 wires the store into the actual sample-app path; Task 2's `--all-features` test run exercises the Plan 01 SC#1-#4 + guard-at-confirm tests against the integrated build (regression catch). |
| T-220-02e | Elevation of Privilege | shared store across tenants in the app | accept (mitigated upstream) | The store is process-shared, but Plan 01's tenant binding + tenant verification at confirm prevents cross-tenant execution; no app-level mitigation needed. |
</threat_model>

<verification>
- Feature-off: `cargo tree -p ferro-mcp-server --edges normal` contains neither ferro-ai nor reqwest.
- Feature-on: reqwest still absent in the tree.
- Sample app builds both default and `--features confirmation`.
- Full CI gate green: `cargo fmt --all -- --check && cargo clippy --all --all-targets --all-features -- -D warnings && cargo test --all-features`.
</verification>

<success_criteria>
- SC#5: consumers without the `confirmation` feature compile with no ferro-ai/reqwest; read tools + 219 write tests unaffected.
- The sample app exercises the two-step confirm flow through the real MCP controller.
- The full `--all-features` CI gate (fmt + clippy + test) passes.
</success_criteria>

<output>
After completion, create `.planning/phases/220-confirmation-gating-for-destructive-actions/220-02-SUMMARY.md`
</output>
