---
phase: 200-per-tenant-scoping-policy-authorization-dogfood-acceptance
plan: 06
type: execute
wave: 4
depends_on: ["04", "05"]
files_modified:
  - app/src/tests/mcp_tenant_isolation.rs
  - app/src/tests/mod.rs
autonomous: true
requirements: [AMCP-10, AMCP-11]
must_haves:
  truths:
    - "An integration test seeds two tenants + a user per tenant + orders per tenant in a test DB"
    - "A request through the [BearerAuthMiddleware, TenantMiddleware] stack with tenant A's token returns ONLY tenant A's orders"
    - "The same with tenant B's token returns ONLY tenant B's orders (cross-tenant isolation — SC-1)"
    - "current_tenant() established by the /mcp stack equals what the JwtClaimResolver resolves (SC-3 parity)"
  artifacts:
    - path: "app/src/tests/mcp_tenant_isolation.rs"
      provides: "two-tenant isolation + middleware-chain parity integration tests"
      contains: "tenant_isolation"
  key_links:
    - from: "tenant A token → /mcp stack"
      to: "dispatch tenant predicate"
      via: "current_tenant() set by middleware, forwarded by handler"
      pattern: "tenant"
---

<objective>
Prove SC-1 (tenant A token returns only A's rows; B only B's) and SC-3 (the /mcp tenant context
is established by the same TenantMiddleware + JwtClaimResolver path as the web surface) with an
app integration test that exercises the real `[BearerAuthMiddleware, TenantMiddleware]` chain
against a two-tenant SQLite fixture. This is the automated counterpart to the manual dogfood —
a single-tenant happy path does NOT prove isolation; the test must seed two tenants and assert
exclusivity both ways.

Purpose: The automated SC-1/SC-3 evidence the verification gate requires.
Output: An integration test module that fails if scoping leaks across tenants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-per-tenant-scoping-policy-authorization-dogfood-acceptance/200-VALIDATION.md
@.planning/phases/200-per-tenant-scoping-policy-authorization-dogfood-acceptance/200-RESEARCH.md

<interfaces>
<!-- /mcp stack (Plan 04): BearerAuthMiddleware → TenantMiddleware(JwtClaimResolver("tenant_id", DbTenantLookup)) -->
<!-- BearerAuthMiddleware inserts serde_json::Value {"sub","tenant_id"}; resolver reads tenant_id.as_i64() -->
<!-- To mint test tokens: ferro_mcp_oauth jwt mint (build_claims/mint_token) with sub + tenant_id + aud -->
<!-- handle_tools_call(params, services, db, tenant_id) returns the rows envelope; rows carry tenant_id column -->
<!-- DbTenantLookup queries crate::models::Tenant::find_by_id -->
<!-- Existing app test harness: check app/src/controllers/mcp.rs #[cfg(test)] and any app/src/tests/ for the routed-handler/middleware test pattern; if none exists, build a minimal one (Wave 0 per 200-VALIDATION.md) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-tenant isolation + middleware-parity integration tests</name>
  <read_first>
    - 200-VALIDATION.md §"Wave 0 Requirements" (two-tenant fixture: 2 tenants, orders per tenant, a user per tenant) and §"Per-Task Verification Map" (T-200-01, T-200-05 rows)
    - app/src/controllers/mcp.rs #[cfg(test)] module (existing test harness shape for the handler)
    - framework/src/tenant/middleware.rs + resolver.rs (how to drive the chain in a test, or how current_tenant() is set)
    - ferro-mcp-oauth/src/jwt.rs (mint a token with sub + tenant_id for the test) and validate.rs (BearerCheck)
    - 200-RESEARCH.md §"Validation Architecture > Phase Requirements → Test Map"
  </read_first>
  <action>
    Create `app/src/tests/mcp_tenant_isolation.rs` (register the module in `app/src/tests/mod.rs`,
    or add to the existing test module location — mirror the app's existing test layout; if the
    app has no `src/tests/` dir, place the tests in `app/src/controllers/mcp.rs` `#[cfg(test)]`
    instead and drop the `tests/mod.rs` file from files_modified).
    1. Fixture: in-memory/temp SQLite, run the Migrator (tenants/users-alter/orders), seed two
       tenants (ids 1 and 2), a user per tenant (`tenant_id` = 1, = 2), and ≥2 orders per tenant.
    2. Test `tenant_a_isolation`: drive the `/mcp` tools/call path for the `order` projection with
       a principal/token for the tenant-1 user (insert the serde_json::Value principal as
       BearerAuthMiddleware would, and establish current_tenant() via the same TenantMiddleware +
       JwtClaimResolver + DbTenantLookup chain — do not hand-set the task-local; go through the
       resolver so SC-3 parity is what's tested). Assert every returned row has `tenant_id == 1`
       and NO row has `tenant_id == 2`.
    3. Test `tenant_b_isolation`: same for tenant-2 user → every row `tenant_id == 2`, none == 1.
    4. Test `tenant_context_parity` (SC-3): assert that the `current_tenant()` resolved through
       the JwtClaimResolver for the inserted principal equals the tenant whose id is in the
       principal's `tenant_id` claim (i.e. the resolver, not a parallel path, sets the context).
    If driving the full middleware chain in-process is impractical, drive the resolver
    (`JwtClaimResolver::resolve`) + `with_tenant_scope` exactly as TenantMiddleware does, and call
    the same dispatch the handler calls — the point is structural identity, not a duplicate path.
    Reuse the seed helper from bootstrap if exposable; otherwise replicate minimally in the test.
  </action>
  <verify>
    <automated>cargo test -p app tenant_isolation -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - test seeds exactly 2 tenants and ≥2 orders per tenant (read to confirm)
    - `tenant_a_isolation` asserts all returned rows have tenant_id == 1 AND asserts none == 2
    - `tenant_b_isolation` asserts all returned rows have tenant_id == 2 AND asserts none == 1
    - `tenant_context_parity` resolves current_tenant() via JwtClaimResolver (no hand-set task-local)
    - `cargo test -p app tenant_a_isolation` exits 0
    - `cargo test -p app tenant_b_isolation` exits 0
    - `cargo test -p app tenant_context_parity` exits 0
  </acceptance_criteria>
  <done>Two-tenant isolation is proven both directions through the real middleware/resolver path, and the /mcp tenant context is shown to be established by the same JwtClaimResolver the web surface uses (SC-1, SC-3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test token (tenant A) → /mcp chain | The test exercises the untrusted-token boundary end to end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200-01 | Information Disclosure | end-to-end scoping | mitigate | Bidirectional isolation test fails if A's token ever returns a B row — the regression guard for cross-tenant leakage. |
| T-200-05 | context confusion | middleware/resolver path | mitigate | Parity test asserts the context comes from JwtClaimResolver, catching any future drift toward a second tenant path. |
</threat_model>

<verification>
- `cargo test -p app tenant_isolation` green
- Per-wave gate: `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` (serialize CPU-heavy runs)
</verification>

<success_criteria>
Automated bidirectional two-tenant isolation passes and the /mcp tenant context is shown
structurally identical to the web-surface path — SC-1 and SC-3 have automated evidence before the
manual dogfood gate.
</success_criteria>

<output>
After completion, create `.planning/phases/200-per-tenant-scoping-policy-authorization-dogfood-acceptance/200-06-SUMMARY.md`
</output>
