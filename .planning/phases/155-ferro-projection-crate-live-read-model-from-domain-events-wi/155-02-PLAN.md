---
phase: 155
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Cargo.toml
  - .github/workflows/publish.yml
  - CLAUDE.md
  - README.md
autonomous: true
requirements: [D-04, D-05, D-54, D-55]
requirements_addressed: [D-04, D-05, D-54, D-55]
must_haves:
  truths:
    - "Workspace root Cargo.toml has 'ferro-projection' in [workspace.members] appended after 'ferro-reservation' (the most recent Wave 1b sibling)"
    - "Workspace root Cargo.toml [workspace.package] version bumped from 0.2.32 to 0.2.33 per D-54 (or one patch higher if CI auto-bumped between this phase's start and this task's execution — read at execution time, do not rely on D-54's static value)"
    - ".github/workflows/publish.yml WAVE1B_CRATES string contains 'ferro-projection' appended after 'ferro-reservation' (per D-04 / D-55 — ferro-projection has internal ferro-* deps so it MUST go in Wave 1b)"
    - "CLAUDE.md Workspace Structure table contains a row for 'ferro-projection' positioned after the 'ferro-reservation' row with purpose 'Live read-model runtime: subscribe to domain events, persist per-key snapshots, broadcast deltas. **Not the same as `ferro-projections` (plural)** — see crate docs for the distinction.'"
    - "Workspace root README.md 'What's included' list contains a 'Live read-models' bullet referencing (`ferro-projection`) with the disambiguation phrase, placed adjacent to the existing `ferro-reservation` bullet"
    - "cargo metadata --no-deps succeeds (manifest still well-formed after edits)"
    - "After this plan lands, cargo build -p ferro-projection succeeds (replaces plan 01's --manifest-path workaround) and cargo build --workspace succeeds"
    - "The literal disambiguation phrase 'Not the same as `ferro-projections` (plural)' appears in BOTH CLAUDE.md row AND root README.md bullet (grep-verifiable surface extension from plan 01)"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace manifest with ferro-projection registered + version bumped to 0.2.33"
      contains: "ferro-projection"
    - path: ".github/workflows/publish.yml"
      provides: "Auto-publish Wave 1b slot for ferro-projection"
      contains: "ferro-projection"
    - path: "CLAUDE.md"
      provides: "Workspace Structure row for ferro-projection with disambiguation phrase"
      contains: "ferro-projection"
    - path: "README.md"
      provides: "Workspace README crates list entry with disambiguation phrase"
      contains: "ferro-projection"
  key_links:
    - from: "Cargo.toml [workspace.members]"
      to: "ferro-projection/Cargo.toml (created by plan 155-01)"
      via: "workspace.members entry"
      pattern: '"ferro-projection"'
    - from: ".github/workflows/publish.yml WAVE1B_CRATES"
      to: "crates.io ferro-projection auto-publish (post-bootstrap from plan 155-07)"
      via: "Wave 1b publish loop"
      pattern: "ferro-projection"
---

<objective>
Register `ferro-projection` in the workspace and bump the workspace version from 0.2.32 to 0.2.33 (D-54). This plan modifies only workspace-root files — `Cargo.toml`, `.github/workflows/publish.yml`, `CLAUDE.md`, `README.md` — and has zero file overlap with plan 155-01 (which created only files under `ferro-projection/`).

Plan 02 runs **sequentially after plan 01** (Wave 2, not parallel) because the workspace gate command shifts from `cargo build --manifest-path ferro-projection/Cargo.toml` (plan 01) to `cargo build -p ferro-projection` (post-plan 02). This is the same sibling-phase guidance Phase 154's plan 02 followed; Phase 153's parallel plan 01+02 surfaced a STATE.md-recorded planner deviation, so subsequent phases default to sequential 1→2.

**Critical Wave-1b placement.** D-04 places ferro-projection in Wave 1b (NOT Wave 1a) because it has two internal ferro-* runtime deps (ferro-events, ferro-broadcast — both Wave 1a). Add it to `WAVE1B_CRATES`, NOT `WAVE1A_CRATES`. The publish-loop serialization ensures Wave 1a ships before Wave 1b attempts to resolve those deps.

**Critical disambiguation surface extension.** Per D-02, the literal phrase "Not the same as `ferro-projections` (plural)" must appear in CLAUDE.md's workspace structure row and root README.md's crates bullet. Plan 01 placed it in Cargo.toml description + README.md (crate-local) + lib.rs rustdoc; this plan extends it to the two workspace-level documents. Plan 07 will extend it to docs page + CHANGELOG. Every public-facing surface that names `ferro-projection` MUST also name the disambiguation.

Purpose: make `ferro-projection` a recognized workspace member so `cargo build --workspace` picks it up; reserve its auto-publish slot in CI for subsequent versions (the first publish is bootstrapped manually in plan 155-07 per D-55); surface the crate in the developer-facing project documents with the disambiguation phrase intact.

Output: workspace registration committed; `cargo metadata --no-deps` succeeds; `cargo build --workspace` succeeds; the two new disambiguation surfaces (CLAUDE.md + root README.md) carry the locked phrase.
</objective>

<context>
@.planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-CONTEXT.md
@.planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-RESEARCH.md
@.planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-PATTERNS.md
@.planning/phases/154-ferro-reservation-crate-generic-hold-commit-release-with-ttl/154-02-PLAN.md
@.planning/phases/154-ferro-reservation-crate-generic-hold-commit-release-with-ttl/154-02-SUMMARY.md
@Cargo.toml
@.github/workflows/publish.yml
@CLAUDE.md
@README.md

<interfaces>
<!-- Current state of files this plan modifies. Excerpts extracted from codebase. -->

From Cargo.toml [workspace.members] (current, after Phase 154):
```toml
members = [
    "framework",
    "app",
    "ferro-cli",
    "ferro-macros",
    "ferro-events",
    "ferro-queue",
    "ferro-notifications",
    "ferro-broadcast",
    "ferro-storage",
    "ferro-cache",
    "ferro-mcp",
    "ferro-inertia",
    "ferro-json-ui",
    "ferro-lang",
    "ferro-api-mcp",
    "ferro-projections",
    "ferro-stripe",
    "ferro-theme",
    "ferro-ai",
    "ferro-whatsapp",
    "ferro-wallet",
    "ferro-orm",
    "ferro-audit",
    "ferro-reservation",
]
```

From Cargo.toml [workspace.package] (current, after Phase 154):
```toml
[workspace.package]
version = "0.2.32"
edition = "2021"
rust-version = "1.88.0"
license = "MIT"
```

From .github/workflows/publish.yml line 236 (Wave 1b — THIS plan modifies):
```
WAVE1B_CRATES="ferro-ai ferro-projections ferro-stripe ferro-whatsapp ferro-notifications ferro-reservation"
```

From CLAUDE.md Workspace Structure table (relevant rows, near the end, with the new ferro-reservation row from Phase 154):
```markdown
| `ferro-orm` | Atomic conditional updates and ORM primitives (`GuardedUpdate`) | `src/lib.rs` |
| `ferro-audit` | Append-only structured before/after audit log with replay | `src/lib.rs` |
| `ferro-reservation` | Generic hold/commit/release reservation kernel | `src/lib.rs` |
| `app` | Sample application | Reference implementation |
```

From README.md "What's included" list (relevant bullet, post-Phase 154):
```markdown
- **Resource reservations** — race-free hold/commit/release with TTL, audit, and event broadcast (`ferro-reservation`)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register ferro-projection in workspace Cargo.toml and bump version 0.2.32 → 0.2.33</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml lines 1-35 (current `[workspace] members` list + `[workspace.package] version`)
    - .planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-CONTEXT.md D-54 (version target 0.2.33)
    - .planning/phases/154-ferro-reservation-crate-generic-hold-commit-release-with-ttl/154-02-PLAN.md task 1 (analog version-bump task)
    - .planning/phases/154-ferro-reservation-crate-generic-hold-commit-release-with-ttl/154-02-SUMMARY.md (records the actual version-bump applied in Phase 154)
  </read_first>
  <behavior>
    - Workspace `[workspace.members]` now contains `"ferro-projection"` appended at the end of the list (after `"ferro-reservation"`)
    - `[workspace.package] version` bumped from `"0.2.32"` to `"0.2.33"` per D-54 (or one patch higher if execution-time value differs)
    - `cargo metadata --no-deps` succeeds (manifest still well-formed)
    - No other workspace member entries are removed or reordered
  </behavior>
  <action>
    Edit `/Users/alberto/repositories/albertogferrario/ferro/Cargo.toml`.

    **Step 1 — Verify current version at execution time.** Run:
    ```bash
    grep -E '^version = ' Cargo.toml | head -1
    ```
    Expected current value: `version = "0.2.32"`. If the version is already higher than `0.2.32` (CI auto-bumped after Phase 154 publish), bump to the next patch (e.g. `0.2.32 → 0.2.33`, or `0.2.34 → 0.2.35`). The principle is: bump by one patch from whatever is currently recorded. D-54's static `0.2.33` is the target IF the current value is still `0.2.32`.

    **Step 2 — Add `"ferro-projection"` to `[workspace.members]`.** Insert at the END of the list (after the existing `"ferro-reservation"` line). The resulting block:

    ```toml
    members = [
        "framework",
        "app",
        "ferro-cli",
        "ferro-macros",
        "ferro-events",
        "ferro-queue",
        "ferro-notifications",
        "ferro-broadcast",
        "ferro-storage",
        "ferro-cache",
        "ferro-mcp",
        "ferro-inertia",
        "ferro-json-ui",
        "ferro-lang",
        "ferro-api-mcp",
        "ferro-projections",
        "ferro-stripe",
        "ferro-theme",
        "ferro-ai",
        "ferro-whatsapp",
        "ferro-wallet",
        "ferro-orm",
        "ferro-audit",
        "ferro-reservation",
        "ferro-projection",
    ]
    ```

    **Step 3 — Bump `[workspace.package] version`** from `"0.2.32"` to `"0.2.33"` (use Step 1's result if the current value differs from `0.2.32`).

    Final `[workspace.package]` block:
    ```toml
    [workspace.package]
    version = "0.2.33"
    edition = "2021"
    rust-version = "1.88.0"
    license = "MIT"
    ```
    Do not touch `edition`, `rust-version`, or `license`.

    **Step 4 — Verify the manifest still parses.** Run:
    ```bash
    cargo metadata --no-deps --format-version 1 > /dev/null
    ```
    This must exit 0. Plan 01 has already created `ferro-projection/Cargo.toml`, so the workspace member path resolves; if plan 01 was NOT completed (executor ordering violation), this would fail with `package not found at 'ferro-projection'` — fix by re-running plan 01 first.
  </action>
  <verify>
    <automated>grep -q '"ferro-projection"' Cargo.toml &amp;&amp; grep -E '^version = "0\.2\.3[3-9]"' Cargo.toml &amp;&amp; ! grep -q '^version = "0.2.32"' Cargo.toml &amp;&amp; cargo metadata --no-deps --format-version 1 >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml` contains `"ferro-projection"` in `[workspace.members]`
    - `Cargo.toml` `[workspace.package] version` is `"0.2.33"` or higher (NEVER still `"0.2.32"` after this task)
    - All previous 24 workspace members are still present (none removed or reordered)
    - `cargo metadata --no-deps` exits 0
  </acceptance_criteria>
  <done>Workspace Cargo.toml registers ferro-projection and bumps version. Manifest still parses.</done>
</task>

<task type="auto">
  <name>Task 2: Add ferro-projection to .github/workflows/publish.yml Wave 1b (NOT Wave 1a)</name>
  <files>.github/workflows/publish.yml</files>
  <read_first>
    - .github/workflows/publish.yml line 236 (current `WAVE1B_CRATES` string with `ferro-reservation` appended at end after Phase 154)
    - .github/workflows/publish.yml line 201 (`WAVE1A_CRATES` — verify we do NOT modify this)
    - .planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-CONTEXT.md D-04 + D-55 (Wave 1b placement reasoning)
    - .planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-PATTERNS.md §".github/workflows/publish.yml"
  </read_first>
  <behavior>
    - `.github/workflows/publish.yml` line 236 (`WAVE1B_CRATES=...`) ends with `ferro-reservation ferro-projection"` — `ferro-projection` appended at the end
    - Line 201 (`WAVE1A_CRATES`) is UNCHANGED (no `ferro-projection` there)
    - Line 264 (`WAVE2_CRATES`) is UNCHANGED (no `ferro-projection` there)
    - No change to the loop body that consumes `$WAVE1B_CRATES`
    - The file is still valid YAML
  </behavior>
  <action>
    Edit `/Users/alberto/repositories/albertogferrario/ferro/.github/workflows/publish.yml`.

    **DO NOT modify line 201 (`WAVE1A_CRATES`).** ferro-projection has internal ferro-events + ferro-broadcast deps (D-04), so it MUST go in Wave 1b (D-05). Adding it to Wave 1a would publish ferro-projection in parallel with its own deps, causing dependency-resolution failures.

    Locate line 236:
    ```
              WAVE1B_CRATES="ferro-ai ferro-projections ferro-stripe ferro-whatsapp ferro-notifications ferro-reservation"
    ```

    Replace it with:
    ```
              WAVE1B_CRATES="ferro-ai ferro-projections ferro-stripe ferro-whatsapp ferro-notifications ferro-reservation ferro-projection"
    ```

    Append `ferro-projection` at the end of the existing list. Do not reorder existing crates.

    The loop body that consumes `$WAVE1B_CRATES` (lines below 236, with the `for CRATE in $WAVE1B_CRATES` and the `cargo publish || already-exists-skip` branches) is unchanged — it iterates each crate name and runs the same publish logic.
  </action>
  <verify>
    <automated>grep -q 'WAVE1B_CRATES=".*ferro-projection"' .github/workflows/publish.yml &amp;&amp; ! grep -q 'WAVE1A_CRATES=".*ferro-projection' .github/workflows/publish.yml &amp;&amp; ! grep -q 'WAVE2_CRATES=".*ferro-projection' .github/workflows/publish.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/publish.yml` `WAVE1B_CRATES` string contains `ferro-projection` at the end
    - `WAVE1A_CRATES` does NOT contain `ferro-projection`
    - `WAVE2_CRATES` does NOT contain `ferro-projection`
    - The file is still valid YAML (no other edits that break syntax)
  </acceptance_criteria>
  <done>ferro-projection is registered for auto-publish in Wave 1b. Subsequent versions (after the manual bootstrap in plan 155-07) auto-publish on master push.</done>
</task>

<task type="auto">
  <name>Task 3: Add ferro-projection row to CLAUDE.md Workspace Structure table (with disambiguation phrase)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md line 60 (existing `ferro-reservation` row in Workspace Structure table)
    - .planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-CONTEXT.md D-02 + §Specifics line 522 (locked row text)
    - .planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-PATTERNS.md §"CLAUDE.md"
  </read_first>
  <behavior>
    - CLAUDE.md `### Workspace Structure` table contains a new row for `ferro-projection`
    - The new row is positioned IMMEDIATELY AFTER the existing `ferro-reservation` row and BEFORE the `app` row
    - The row description contains the literal phrase "Not the same as `ferro-projections` (plural)"
    - All previous workspace structure rows are unchanged
  </behavior>
  <action>
    Use the Edit tool on `/Users/alberto/repositories/albertogferrario/ferro/CLAUDE.md`.

    Locate the row:
    ```markdown
    | `ferro-reservation` | Generic hold/commit/release reservation kernel | `src/lib.rs` |
    ```

    Insert a new row IMMEDIATELY AFTER it (and BEFORE the existing `| \`app\` |` row). The new row text is locked verbatim in CONTEXT.md §Specifics line 522:

    ```markdown
    | `ferro-projection` | Live read-model runtime: subscribe to domain events, persist per-key snapshots, broadcast deltas. **Not the same as `ferro-projections` (plural)** — see crate docs for the distinction. | `src/lib.rs` |
    ```

    Resulting block:
    ```markdown
    | `ferro-orm` | Atomic conditional updates and ORM primitives (`GuardedUpdate`) | `src/lib.rs` |
    | `ferro-audit` | Append-only structured before/after audit log with replay | `src/lib.rs` |
    | `ferro-reservation` | Generic hold/commit/release reservation kernel | `src/lib.rs` |
    | `ferro-projection` | Live read-model runtime: subscribe to domain events, persist per-key snapshots, broadcast deltas. **Not the same as `ferro-projections` (plural)** — see crate docs for the distinction. | `src/lib.rs` |
    | `app` | Sample application | Reference implementation |
    ```

    Do not modify any other CLAUDE.md content.
  </action>
  <verify>
    <automated>grep -q '| `ferro-projection` | Live read-model runtime' CLAUDE.md &amp;&amp; grep -q 'Not the same as `ferro-projections` (plural)' CLAUDE.md &amp;&amp; awk '/\| `ferro-reservation` \|/{a=NR} /\| `ferro-projection` \|/{b=NR} /\| `app` \|/{c=NR} END{exit (a&amp;&amp;b&amp;&amp;c&amp;&amp;a<b&amp;&amp;b<c)?0:1}' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` contains a new `ferro-projection` row in the Workspace Structure table
    - The row is positioned between `ferro-reservation` and `app`
    - The row description contains the literal phrase "Not the same as `ferro-projections` (plural)"
    - All other rows are unchanged
  </acceptance_criteria>
  <done>CLAUDE.md's Workspace Structure table includes ferro-projection with the locked disambiguation phrase.</done>
</task>

<task type="auto">
  <name>Task 4: Add ferro-projection bullet to workspace root README.md "What's included" list (with disambiguation phrase)</name>
  <files>README.md</files>
  <read_first>
    - README.md line 73 (existing `ferro-reservation` bullet in "What's included" list)
    - .planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-CONTEXT.md D-02
    - .planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-PATTERNS.md §"README.md (workspace root)"
  </read_first>
  <behavior>
    - Workspace root `README.md` "What's included" list contains a new bullet for `ferro-projection`
    - The new bullet is positioned IMMEDIATELY AFTER the existing `ferro-reservation` bullet
    - The bullet contains the literal disambiguation phrase "not the same as `ferro-projections` plural"
    - All previous bullets are unchanged
  </behavior>
  <action>
    Use the Edit tool on `/Users/alberto/repositories/albertogferrario/ferro/README.md`.

    Locate the bullet:
    ```markdown
    - **Resource reservations** — race-free hold/commit/release with TTL, audit, and event broadcast (`ferro-reservation`)
    ```

    Insert a new bullet IMMEDIATELY AFTER it (per PATTERNS.md §"README.md (workspace root)"):

    ```markdown
    - **Live read-models** — fold domain events into per-key materialized state with WebSocket delta broadcast (`ferro-projection` — not the same as `ferro-projections` plural)
    ```

    Do not modify any other README.md content.
  </action>
  <verify>
    <automated>grep -q '\*\*Live read-models\*\* — fold domain events' README.md &amp;&amp; grep -q 'ferro-projection. — not the same as .ferro-projections. plural' README.md &amp;&amp; awk '/Resource reservations/{a=NR} /Live read-models/{b=NR} END{exit (a&amp;&amp;b&amp;&amp;a<b)?0:1}' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains the new `- **Live read-models** — ...` bullet
    - The bullet is positioned immediately after the `Resource reservations` bullet
    - The bullet contains the literal phrase "not the same as `ferro-projections` plural"
    - All other README.md content is unchanged
  </acceptance_criteria>
  <done>Workspace root README.md surfaces ferro-projection with the locked disambiguation phrase.</done>
</task>

<task type="auto">
  <name>Task 5: Verify workspace builds cleanly with new member</name>
  <files></files>
  <read_first>
    - CLAUDE.md §"Testing & Linting" (exact gate commands)
    - .planning/phases/154-ferro-reservation-crate-generic-hold-commit-release-with-ttl/154-02-PLAN.md task 5 (analog gate)
  </read_first>
  <behavior>
    - `cargo build -p ferro-projection` exits 0 (replaces plan 01's `--manifest-path` workaround)
    - `cargo build --workspace` exits 0
    - `cargo test -p ferro-projection --lib error::tests` exits 0 with 5 passed
  </behavior>
  <action>
    Run the post-registration gate to confirm workspace membership is correctly wired.

    ```bash
    # 1. Build just ferro-projection via workspace package shorthand
    cargo build -p ferro-projection

    # 2. Build the entire workspace (catches any drift from other crates)
    cargo build --workspace

    # 3. Re-run the 5 error-module tests to confirm they still pass
    cargo test -p ferro-projection --lib error::tests
    ```

    Expected output from step 3:
    ```
    test result: ok. 5 passed; 0 failed; 0 ignored
    ```

    If `cargo build -p ferro-projection` fails:
    - "package not found" → re-check `Cargo.toml` `[workspace.members]` entry is exactly `"ferro-projection"`
    - Path resolution errors → verify `ferro-projection/Cargo.toml` exists from plan 01

    If `cargo build --workspace` fails on an unrelated crate:
    - Some other workspace crate failed to build — surface the error; this plan should not touch their files
  </action>
  <verify>
    <automated>cargo build -p ferro-projection 2>&amp;1 &amp;&amp; cargo build --workspace 2>&amp;1 &amp;&amp; cargo test -p ferro-projection --lib error::tests 2>&amp;1 | grep -E '^test result: ok\. 5 passed'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p ferro-projection` exits 0
    - `cargo build --workspace` exits 0
    - `cargo test -p ferro-projection --lib error::tests` reports 5 passed
  </acceptance_criteria>
  <done>ferro-projection is a recognized workspace member. The crate builds via `cargo -p` shorthand. Workspace build is green.</done>
</task>

</tasks>

<verification>
- `Cargo.toml` contains `"ferro-projection"` in `[workspace.members]` and `version = "0.2.33"` (or higher)
- `.github/workflows/publish.yml` `WAVE1B_CRATES` contains `ferro-projection` at the end
- `CLAUDE.md` Workspace Structure table contains a row for `ferro-projection` with the disambiguation phrase
- Workspace root `README.md` "What's included" list contains a `Live read-models` bullet with the disambiguation phrase
- `cargo build -p ferro-projection` exits 0
- `cargo build --workspace` exits 0
- `cargo test -p ferro-projection --lib error::tests` reports 5 passed
</verification>

<success_criteria>
ferro-projection is a recognized workspace member. The crate-name + disambiguation phrase surface is now extended across four documents: ferro-projection/Cargo.toml (plan 01), ferro-projection/README.md (plan 01), ferro-projection/src/lib.rs rustdoc (plan 01), CLAUDE.md (this plan), workspace README.md (this plan). Plan 07 will extend further to docs/src/features/live-read-models.md + CHANGELOG.md.

Every D-XX addressed by this plan is satisfied: D-04 (Wave 1b placement justification — internal ferro-* deps), D-05 (Wave 1b in publish.yml), D-54 (workspace version bump 0.2.32 → 0.2.33), D-55 (Wave 1b auto-publish slot reserved; manual bootstrap deferred to plan 07).
</success_criteria>

<output>
After completion, create `.planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-02-SUMMARY.md` documenting:
- The 4 files modified (Cargo.toml, .github/workflows/publish.yml, CLAUDE.md, README.md)
- The exact version bump applied (e.g., 0.2.32 → 0.2.33)
- Confirmation that `cargo build -p ferro-projection` and `cargo build --workspace` exited 0
- Confirmation that the 5 error::tests still pass post-registration
- A grep confirmation that the disambiguation phrase appears in BOTH CLAUDE.md and root README.md
</output>
