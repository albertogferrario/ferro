---
status: complete
phase: 155-ferro-projection-crate-live-read-model-from-domain-events-wi
source: [155-01-SUMMARY.md, 155-02-SUMMARY.md, 155-03-SUMMARY.md, 155-04-SUMMARY.md, 155-05-SUMMARY.md, 155-06-SUMMARY.md, 155-07-SUMMARY.md]
started: 2026-05-14T00:00:00Z
updated: 2026-05-14T12:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cargo build gate
expected: Run `cargo build -p ferro-projection` exits 0 with no errors.
result: pass

### 2. Lib unit tests — 25 passing
expected: Run `cargo test -p ferro-projection --lib`. Should report 25 passed, 0 failed across error, migration, entity, key, projection, and runtime modules.
result: pass

### 3. Integration tests — 35 total
expected: Run `cargo test -p ferro-projection`. Should report ~35 tests passing: 25 lib + 1 migration + 1 event_bus_integration + 1 projection_over_reservation_events + 3 concurrent_apply + 3 proptest properties.
result: pass

### 4. Workspace gate
expected: Run `cargo test --all-features`. All workspace tests should pass (the entire workspace builds and tests clean).
result: pass

### 5. Projection trait interface
expected: Open `ferro-projection/src/projection.rs`. The `Projection` trait should have: `type Event`, `type State: Default + ...`, `type Delta`, `const NAME: &'static str`, sync `fn key(...)`, sync `fn apply(...)`, defaulted `fn snapshot_interval() -> u32 { 100 }`, and defaulted `fn broadcast_event_name() -> &'static str { "delta" }`. No `#[async_trait]` on the trait.
result: pass

### 6. ProjectionRuntime public API
expected: Open `ferro-projection/src/runtime.rs`. `ProjectionRuntime<P>` should expose: `new(db, broadcaster, projection)`, `read(&self, key)`, `read_required(&self, key)`, `apply_event(&self, event)`, `register(self: Arc<Self>)`, and `rebuild<I>(&self, key, events)`.
result: pass

### 7. SeaORM migration schema
expected: Open `ferro-projection/src/migration.rs`. The migration should create a `projection_snapshots` table with 5 columns: `projection_name VARCHAR NOT NULL`, `key VARCHAR NOT NULL`, `state JSON NOT NULL`, `version BIGINT NOT NULL`, `updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP`. Composite PK on `(projection_name, key)`.
result: pass

### 8. Disambiguation in 8 surfaces
expected: Run `grep -r "ferro-projections" ferro-projection/ CLAUDE.md README.md docs/src/features/live-read-models.md CHANGELOG.md`. The disambiguation phrase should appear in: ferro-projection/Cargo.toml, ferro-projection/README.md, ferro-projection/src/lib.rs, ferro-projection/src/projection.rs, CLAUDE.md, root README.md, docs/src/features/live-read-models.md, and CHANGELOG.md — 8 surfaces total.
result: pass

### 9. Live read-models docs page
expected: Open `docs/src/features/live-read-models.md`. Should exist (354+ lines), open with "Not to be confused with `ferro-projections` (plural)" disambiguation lede, and contain the `ReservationCountProjection` worked example section. Also verify `docs/src/SUMMARY.md` has `- [Live Read-Models](features/live-read-models.md)` immediately after the Service Projections entry.
result: pass

### 10. CHANGELOG milestone-completion entry
expected: Open `CHANGELOG.md`. Should have `## ferro-projection` section above `## ferro-reservation`. The section should include `### [0.2.33] — 2026-05-14` and contain the exact line: "v11.11 Resource Reservation & Live Read-Model Primitives complete — ferro-orm GuardedUpdate (Phase 152), ferro-audit (Phase 153), ferro-reservation (Phase 154), ferro-projection (Phase 155) now all shipped."
result: pass

### 11. Workspace version 0.2.33
expected: Run `grep 'version = "0.2.33"' Cargo.toml`. Should find the version in `[workspace.package]`.
result: pass

### 12. CI publish registration
expected: Run `grep 'ferro-projection' .github/workflows/publish.yml`. Should appear in `WAVE1B_CRATES` (not WAVE1A_CRATES). Should NOT appear in WAVE1A_CRATES.
result: pass

## Summary

total: 12
passed: 12
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]
