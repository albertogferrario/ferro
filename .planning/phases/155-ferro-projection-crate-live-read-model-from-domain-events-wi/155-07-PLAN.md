---
phase: 155
plan: 07
type: execute
wave: 7
depends_on: [06]
files_modified:
  - docs/src/features/live-read-models.md
  - docs/src/SUMMARY.md
  - CHANGELOG.md
autonomous: false
requirements: [D-51, D-52, D-53, D-54, D-55, D-56]
requirements_addressed: [D-51, D-52, D-53, D-54, D-55, D-56]
user_setup:
  - service: crates.io
    why: "First publish of the new ferro-projection crate requires a personal publish-new-scoped token from the operator's local terminal. CI's CARGO_REGISTRY_TOKEN has publish-update scope only and cannot create new crates (RESEARCH §Environment Availability — same operational reality as Phase 151 ferro-wallet, Phase 152 ferro-orm, Phase 153 ferro-audit, Phase 154 ferro-reservation)."
    env_vars:
      - name: CARGO_REGISTRY_TOKEN
        source: "crates.io account → Account Settings → API Tokens → Create new token with publish-new scope. NOT the CI token (publish-update only)."
    dashboard_config:
      - task: "Verify ferro-projection name is available on crates.io before first publish"
        location: "https://crates.io/search?q=ferro-projection"
must_haves:
  truths:
    - "docs/src/features/live-read-models.md exists as the user-facing documentation page covering the full D-52 outline: opening paragraph (with the disambiguation lede 'Not the same as `ferro-projections` (plural)' per D-52) + the anti-pattern (hand-rolled load → apply → persist → broadcast) + the replacement (one-line Arc::new(Runtime::new(...)).register()) + per-key serialization diagram + Projection trait surface + apply_event vs register two-entry-points + broadcast channel contract + rebuild affordance + operational footguns (3 enumerated) + worked example mirroring the D-47 showcase test expanded into tutorial form"
    - "docs/src/SUMMARY.md gains a `- [Live Read-Models](features/live-read-models.md)` nav entry under the Features section, placed immediately after the existing `- [Service Projections](features/projections.md)` entry (nav text is 'Live Read-Models' NOT 'Projections' per D-52 — placement adjacency to the plural sibling reinforces the disambiguation)"
    - "CHANGELOG.md gains a new top-level `## ferro-projection` section ABOVE the existing `## ferro-reservation` section (newest-on-top per Phase 152 D-25 convention) with `### [{ACTUAL_VERSION}] — {DATE}` initial-release entry summarising the full public surface per D-56 AND a closing milestone-completion line: 'v11.11 Resource Reservation & Live Read-Model Primitives complete — ferro-orm GuardedUpdate (Phase 152), ferro-audit (Phase 153), ferro-reservation (Phase 154), ferro-projection (Phase 155) now all shipped.'"
    - "The literal disambiguation phrase 'Not the same as `ferro-projections` (plural)' (or its docs equivalent 'Not to be confused with `ferro-projections` (plural)') appears in both the docs page AND the CHANGELOG section — completing the 7-surface disambiguation coverage (Cargo.toml, README.md crate-local, lib.rs rustdoc, projection.rs rustdoc, CLAUDE.md row, root README.md bullet, docs page + CHANGELOG)"
    - "The pre-release gate is green: cargo fmt --all -- --check + cargo clippy --all --all-targets -- -D warnings + cargo build --workspace + cargo test --all-features + cargo doc --no-deps -p ferro-projection all exit 0 with zero warnings"
    - "First publish of ferro-projection 0.2.33 is bootstrapped from the operator's local terminal with a personal publish-new-scoped token (manual checkpoint — D-55; cannot be automated per RESEARCH §Environment Availability)"
    - "After bootstrap, subsequent ferro-projection versions auto-publish via the existing GH Actions workflow on master push (Wave 1b loop per plan 02)"
    - "The version published is whatever `[workspace.package] version` currently reads in Cargo.toml — the planner does NOT hand-bump beyond plan 02's 0.2.32 → 0.2.33"
    - "ferro-mcp introspection auto-includes ferro-projection in `installed_crates` (D-53 — no MCP code changes needed); db_schema picks up the projection_snapshots table; generation_context auto-includes the rustdoc"
  artifacts:
    - path: "docs/src/features/live-read-models.md"
      provides: "User-facing documentation page with disambiguation lede + worked example"
      contains: "# Live Read-Models"
    - path: "docs/src/SUMMARY.md"
      provides: "Nav entry for live-read-models.md (sibling of Service Projections)"
      contains: "live-read-models.md"
    - path: "CHANGELOG.md"
      provides: "ferro-projection initial release entry with milestone-completion line"
      contains: "## ferro-projection"
  key_links:
    - from: "Cargo.toml [workspace.package].version"
      to: "GH Actions publish.yml WAVE1B_CRATES loop"
      via: "post-merge auto-publish for subsequent versions (bootstrapped manually for the first version)"
      pattern: "version = "
    - from: "manual cargo publish -p ferro-projection"
      to: "crates.io ferro-projection first version"
      via: "personal publish-new token bootstrap"
      pattern: "cargo publish -p ferro-projection"
---

<objective>
Ship Phase 155 and complete the v11.11 milestone. Land the user-facing documentation page (`docs/src/features/live-read-models.md`) and its nav entry, update `CHANGELOG.md` with the `ferro-projection` initial-release section + milestone-completion line, run the full workspace pre-release gate, and bootstrap the first publish of `ferro-projection` to crates.io from the operator's local terminal.

The first publish is a **manual checkpoint** because CI's `CARGO_REGISTRY_TOKEN` has `publish-update` scope only (cannot create new crates) — same operational reality as Phases 151 `ferro-wallet`, 152 `ferro-orm`, 153 `ferro-audit`, 154 `ferro-reservation`. All other phase work is automated.

The workspace `[workspace.package] version` was bumped by plan 02 from `0.2.32` to `0.2.33`. If CI auto-bumped to a higher value between plan 02's commit and this plan's execution, use the actually-Cargo.toml-recorded value; do not hand-bump again.

**Milestone completion.** With Phase 152 (`ferro-orm`), Phase 153 (`ferro-audit`), Phase 154 (`ferro-reservation`) already on crates.io, Phase 155 closes the v11.11 substrate: race-free reservations + live read-models. Downstream consumers can now adopt the four primitives and compose them into a complete app skeleton (events → reservations → audit history → live dashboards) in one `Arc::new(Runtime::new(...)).register()` per projection. The CHANGELOG entry carries the explicit milestone-completion line.

**Final disambiguation surface extension.** Per D-02 and D-52, the literal disambiguation phrase ("Not the same as `ferro-projections` (plural)" or its docs-page equivalent "Not to be confused with `ferro-projections` (plural)") appears in BOTH the docs page AND the CHANGELOG section. This completes the 7-surface coverage:
1. `ferro-projection/Cargo.toml` description (plan 01)
2. `ferro-projection/README.md` (plan 01)
3. `ferro-projection/src/lib.rs` module-level rustdoc (plan 01)
4. `ferro-projection/src/projection.rs` module-level rustdoc (plan 04)
5. `CLAUDE.md` workspace structure row (plan 02)
6. `README.md` workspace root bullet (plan 02)
7. `docs/src/features/live-read-models.md` opening (this plan)
8. `CHANGELOG.md` ferro-projection section opening (this plan)

Output: docs page exists with full D-52 outline + disambiguation lede; SUMMARY.md nav entry positioned as sibling of Service Projections; CHANGELOG entry placed newest-on-top with milestone-completion line; workspace pre-release gate green; `ferro-projection` published on crates.io; STATE.md updated.
</objective>

<context>
@.planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-CONTEXT.md
@.planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-RESEARCH.md
@.planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-PATTERNS.md
@.planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-VALIDATION.md
@.planning/phases/154-ferro-reservation-crate-generic-hold-commit-release-with-ttl/154-07-PLAN.md
@.planning/phases/154-ferro-reservation-crate-generic-hold-commit-release-with-ttl/154-07-SUMMARY.md
@CLAUDE.md
@Cargo.toml
@CHANGELOG.md
@docs/src/SUMMARY.md
@docs/src/database/reservations.md
@docs/src/database/audit-log.md
@.github/workflows/publish.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create docs/src/features/live-read-models.md (D-52 user-facing doc page with disambiguation lede)</name>
  <files>docs/src/features/live-read-models.md</files>
  <read_first>
    - docs/src/database/reservations.md (Phase 154 analog — exact 14-section shape; the new page mirrors its structure with the disambiguation lede swapped in)
    - docs/src/database/audit-log.md (Phase 153 reference for tone)
    - .planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-CONTEXT.md D-02, D-52 (full outline + disambiguation lede requirement), §Specifics line 484-486 (one-line wiring example), §Specifics line 519 (milestone-completion line)
    - .planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-PATTERNS.md §"docs/src/features/live-read-models.md"
    - CLAUDE.md §"Repository documents must read as neutral" (no marketing trigger phrases; no tenant identifiers)
  </read_first>
  <behavior>
    - File starts with `# Live Read-Models` H1 (NOT "Projections" — D-52 nav-text lock)
    - **Opening paragraph LEADS with the disambiguation phrase** "Not to be confused with `ferro-projections` (plural)" + explanation that the plural crate covers the Service Projection abstraction
    - Sections cover the full D-52 outline:
      1. Title + opening paragraph with disambiguation lede (D-02, D-52)
      2. The Anti-Pattern (hand-rolled `load → apply → persist → broadcast` cycle)
      3. The Replacement (typed runtime — the one-line `Arc::new(Runtime::new(...)).register()` from CONTEXT.md §Specifics line 484-486)
      4. Per-key serialization diagram (ASCII)
      5. The Projection trait (associated types, const NAME, key, apply, defaults)
      6. Constructing the Runtime (`ProjectionRuntime::new(db, broadcaster, projection)`)
      7. Two entry points — `register()` (auto-listener) vs `apply_event()` (manual)
      8. The read path — `read` (returns Option) and `read_required` (returns StateNotFound)
      9. The rebuild path — discard + replay from caller-supplied iterator
      10. Broadcast channel contract — `projection.{NAME}.{key}`, event name from `broadcast_event_name()` (default "delta"), `"rebuild"` for replay
      11. Operational footguns (3 — broadcast failure does NOT roll back; single-instance assumption; register-twice = applies-twice)
      12. Worked example (the D-47 showcase test expanded into tutorial form — `ReservationCountProjection`)
      13. Schema (the 5 columns + composite PK)
      14. Errors (ProjectionError variants)
    - No marketing trigger phrases (killer feature, the bet, load-bearing, etc.)
    - No tenant identifiers (gestiscilo, etc.)
    - No version-milestone names (v11.11, "the bet")
    - Total length ~250-350 lines
  </behavior>
  <action>
    Create `/Users/alberto/repositories/albertogferrario/ferro/docs/src/features/live-read-models.md` with the contents below. Mirror the structural shape of `docs/src/database/reservations.md` (Phase 154's 14-section arc) but with the disambiguation lede as the opening move. Use the one-line wiring example verbatim from CONTEXT.md §Specifics line 484-486 (the `Arc::new(ProjectionRuntime::new(...)).register()` call).

    Do not include any tenant identifiers, marketing trigger phrases, or version-milestone names per CLAUDE.md §"Repository documents must read as neutral".

    ```markdown
    # Live Read-Models

    **Not to be confused with `ferro-projections` (plural).** That crate is
    the Service Projection abstraction (`ServiceDef → IntentGraph →
    JsonUiRenderer`) — it shapes how data renders as UI. This page covers
    `ferro-projection` (singular): the live read-model runtime that
    subscribes to domain events, maintains a materialized per-key state,
    and broadcasts deltas to WebSocket subscribers. The two abstractions
    are orthogonal; most apps will use both for different reasons.

    `ferro-projection` is the *live-read-model* primitive. Capacity-
    constrained apps with live dashboards (operator views, real-time
    counters, queue depth panels, kanban boards) all need the same code:
    subscribe to events → load the current state → fold the event into
    state → persist → fan a delta to the subscribed clients. Without a
    typed kernel, every consumer hand-rolls this loop, gets the locking
    wrong, and ships races. `ferro-projection` provides the kernel.

    ## The Anti-Pattern

    Without a runtime, every consumer writes the same fragile code:

    ```rust,ignore
    // BAD: load-check-write with manual broadcast — race-prone, easy to forget steps
    let current: DashboardState = db
        .query("SELECT state FROM dashboards WHERE id = ?", id)
        .await?
        .unwrap_or_default();
    let mut new_state = current.clone();
    new_state.apply(&event);
    db.execute("UPDATE dashboards SET state = ? WHERE id = ?", new_state, id).await?;
    broadcaster.send("dashboard-{id}", "delta", new_state).await?;
    ```

    Under concurrent load, two listeners can both read `current = old`,
    both `.apply(&event)`, both `UPDATE`, and the second write clobbers
    the first. The fix is invariant: serialize per-key, persist + broadcast
    under one lock.

    ## The Replacement

    ```rust,ignore
    use ferro_projection::{Projection, ProjectionKey, ProjectionRuntime};
    use ferro_events::Event;
    use ferro_broadcast::Broadcaster;
    use serde::{Deserialize, Serialize};
    use std::sync::Arc;

    // Consumer event (already implements ferro_events::Event)
    #[derive(Clone, Serialize, Deserialize)]
    struct InventoryAdjusted { warehouse: String, sku: String, delta: i32 }

    impl Event for InventoryAdjusted {
        fn name(&self) -> &'static str { "InventoryAdjusted" }
    }

    // Consumer projection state + delta
    #[derive(Clone, Default, Serialize, Deserialize)]
    struct WarehouseDashboard {
        totals: std::collections::HashMap<String, i64>,
    }

    #[derive(Clone, Serialize)]
    struct WarehouseDelta { sku: String, new_total: i64 }

    struct WarehouseProjection;

    impl Projection for WarehouseProjection {
        type Event = InventoryAdjusted;
        type State = WarehouseDashboard;
        type Delta = WarehouseDelta;
        const NAME: &'static str = "inventory.dashboard";

        fn key(&self, event: &Self::Event) -> ProjectionKey {
            ProjectionKey::new(event.warehouse.clone())
        }

        fn apply(&self, state: &mut Self::State, event: &Self::Event) -> Self::Delta {
            let new_total = state.totals.entry(event.sku.clone()).or_insert(0);
            *new_total += event.delta as i64;
            WarehouseDelta { sku: event.sku.clone(), new_total: *new_total }
        }
    }

    // One-line wiring at application startup
    let runtime = Arc::new(ProjectionRuntime::new(
        db.clone(),
        broadcaster.clone(),
        WarehouseProjection,
    ));
    runtime.clone().register();

    // Anywhere in the app:
    InventoryAdjusted { warehouse: "a".into(), sku: "sku-1".into(), delta: 5 }
        .dispatch()
        .await?;

    // Frontend subscribes to `projection.inventory.dashboard.a` and
    // receives event `"delta"` with payload `{ "sku": "sku-1", "new_total": 5 }`.
    ```

    ## Per-Key Serialization

    ```text
     Event::dispatch() ─┐
                        │
          ProjectionListener<P> ──┐
                                  │
                                  ▼
     ┌── per-key Mutex (DashMap<String, Arc<Mutex<()>>>) ──┐
     │   1. load snapshot from projection_snapshots        │
     │   2. apply(&mut state, &event) → Delta              │
     │   3. upsert snapshot (state, version+1)             │
     │   4. broadcast on projection.{name}.{key}            │
     └─────────────────────────────────────────────────────┘
                                  │
                                  ▼
                    WebSocket clients receive the delta
    ```

    Same-key applies serialize through the per-key Mutex; different-key
    applies run in parallel through different DashMap shards.

    ## The Projection Trait

    ```rust,ignore
    pub trait Projection: Send + Sync + 'static {
        type Event: ferro_events::Event + Serialize + DeserializeOwned;
        type State: Clone + Default + Serialize + DeserializeOwned + Send + Sync + 'static;
        type Delta: Serialize + Clone + Send + Sync + 'static;

        const NAME: &'static str;

        fn key(&self, event: &Self::Event) -> ProjectionKey;
        fn apply(&self, state: &mut Self::State, event: &Self::Event) -> Self::Delta;

        // Defaulted:
        fn snapshot_interval(&self) -> u32 { 100 }
        fn broadcast_event_name(&self) -> &'static str { "delta" }
    }
    ```

    - **`NAME`** is a dotted-namespace constant: `"inventory.dashboard"`,
      `"checkout.cart"`, `"orders.recent"`. Same convention as
      `ferro-audit`'s action namespace.
    - **`State: Default`** is required so a fresh key initializes from
      `State::default()` on first apply. If a state model has no sensible
      default, return an empty/zero variant from `Default`.
    - **`apply` is synchronous.** It runs inside the per-key Mutex; an
      async `apply` would let the lock cross await boundaries and
      serialize unrelated work. Heavy work (HTTP fetches, additional DB
      queries) happens BEFORE dispatch or AFTER receiving the broadcast
      delta on the client side.

    Multi-tenancy: bake the tenant identifier into the key string
    (`"tenant-7:warehouse-a"`); the runtime does not auto-scope by tenant.

    ## Constructing the Runtime

    ```rust,ignore
    let runtime = ProjectionRuntime::new(
        db_connection,
        broadcaster_arc,
        my_projection,
    );
    ```

    The runtime owns the database connection, the broadcaster handle, the
    projection impl, and the per-key Mutex registry. Wrap in `Arc` for
    sharing across tokio tasks; `register` requires `Arc<Self>`.

    ## Two Entry Points

    | Method | Use |
    |--------|-----|
    | `Arc<Runtime>::register()` | Auto-wires a listener into `ferro_events::global_dispatcher()`. Every `P::Event::dispatch().await` flows through the projection. The one-line wiring. |
    | `runtime.apply_event(&event).await` | Manual entry point — bypass the global dispatcher. For tests, replay scripts, custom dispatchers. |

    Both paths share the same per-key serialization, persistence, and
    broadcast logic. Mixing both in the same runtime is safe — the manual
    path doesn't interfere with the registered listener.

    ## The Read Path

    ```rust,ignore
    // Returns Result<Option<State>, ProjectionError>
    let maybe_state = runtime.read(&ProjectionKey::new("warehouse-a")).await?;

    // Returns Result<State, ProjectionError> with StateNotFound on miss
    let state = runtime.read_required(&ProjectionKey::new("warehouse-a")).await?;
    ```

    `read` does NOT acquire the per-key Mutex. Concurrent `read` +
    `apply_event` is safe; the SQL upsert is atomic at the DB level so
    readers see either the pre- or post-upsert state, never a torn read.

    ## The Rebuild Path

    ```rust,ignore
    let events: Vec<InventoryAdjusted> = audit_log_replay_for_key().collect();
    let state = runtime
        .rebuild(&ProjectionKey::new("warehouse-a"), events)
        .await?;
    ```

    `rebuild` discards the existing snapshot for the key, folds the
    supplied event sequence through `State::default()`, persists the final
    state, and broadcasts ONE `"rebuild"` frame carrying the full final
    state (overriding the default `broadcast_event_name`). Clients reset
    their local state on receipt of a `"rebuild"` frame.

    Empty iterator wipes the snapshot row and returns `State::default()`
    with no insert and no broadcast.

    ## Broadcast Channel Contract

    - **Channel:** `format!("projection.{}.{}", P::NAME, key.as_str())`
      — example: `"projection.inventory.dashboard.warehouse-a"`.
    - **Event name:** from `P::broadcast_event_name()`, default `"delta"`.
      Override to `"dashboard_updated"`, `"cart_changed"`, etc., if the
      frontend dispatches on event name.
    - **Payload:** raw JSON-serialized `P::Delta`. No envelope, no
      wrapping object — frontends receive the delta directly.
    - **Rebuild frame:** event name `"rebuild"`, payload is the full
      final state (not a delta).

    ## Operational Footguns

    1. **Broadcast failure does NOT roll back state.** If `Broadcast::send`
       fails (no subscribers, network error), the snapshot row is already
       persisted; the runtime logs at `tracing::warn!` and returns
       `ProjectionError::Broadcast`. Subscribers reconcile by re-reading
       the snapshot via `runtime.read(...)`.
    2. **Single-instance assumption.** v0 assumes a single application
       instance owns each projection's listener. Multi-instance
       deployments must elect a single projection-runner node or accept
       last-writer-wins behavior on concurrent applies to the same key
       from different nodes.
    3. **`register` is not idempotent on `Arc` identity.** Calling
       `Arc<ProjectionRuntime<P>>::register()` twice registers TWO
       listeners — both fire on each dispatch (same semantic as Laravel's
       `Event::listen`). Register once at app startup.

    ## Worked Example: Reservation Counts Dashboard

    A live counter dashboard that folds `ferro_reservation::ReservationEvent`
    into per-`resource_kind` `{held, committed, released}` counters:

    ```rust,ignore
    use ferro_projection::{Projection, ProjectionKey, ProjectionRuntime};
    use ferro_reservation::{ReleaseReason, ReservationEvent};
    use serde::{Deserialize, Serialize};
    use std::sync::Arc;

    #[derive(Clone, Default, Serialize, Deserialize)]
    struct ReservationCounters {
        held: u32,
        committed: u32,
        released: u32,
    }

    #[derive(Clone, Serialize)]
    struct CountersDelta {
        held: u32,
        committed: u32,
        released: u32,
    }

    struct ReservationCountProjection;

    impl Projection for ReservationCountProjection {
        type Event = ReservationEvent;
        type State = ReservationCounters;
        type Delta = CountersDelta;
        const NAME: &'static str = "reservations.counters";

        fn key(&self, event: &Self::Event) -> ProjectionKey {
            let rk = match event {
                ReservationEvent::Held { resource_kind, .. }
                | ReservationEvent::Committed { resource_kind, .. }
                | ReservationEvent::Released { resource_kind, .. }
                | ReservationEvent::Expired { resource_kind, .. } => resource_kind,
            };
            ProjectionKey::new(rk.clone())
        }

        fn apply(&self, state: &mut Self::State, event: &Self::Event) -> Self::Delta {
            match event {
                ReservationEvent::Held { .. } => state.held += 1,
                ReservationEvent::Committed { .. } => state.committed += 1,
                ReservationEvent::Released { .. } | ReservationEvent::Expired { .. } => {
                    state.released += 1
                }
            }
            CountersDelta {
                held: state.held,
                committed: state.committed,
                released: state.released,
            }
        }
    }

    // App startup
    let runtime = Arc::new(ProjectionRuntime::new(
        db.clone(),
        broadcaster.clone(),
        ReservationCountProjection,
    ));
    runtime.register();

    // Every reservation transition (hold/commit/release/expire) emitted
    // by ferro-reservation now flows into per-resource_kind counter state
    // and a delta on `projection.reservations.counters.{resource_kind}`.
    ```

    Each call to `Kernel::hold`, `Kernel::commit`, or `Kernel::release`
    dispatches a `ReservationEvent`; the projection folds it; a counter-
    update delta lands on the subscribed WebSocket channel; the operator
    dashboard updates in real time.

    ## Schema

    The migration creates a single `projection_snapshots` table:

    ```sql
    projection_snapshots
    ├── projection_name VARCHAR NOT NULL     -- P::NAME ("inventory.dashboard")
    ├── key             VARCHAR NOT NULL     -- ProjectionKey.as_str()
    ├── state           JSON NOT NULL        -- serialized P::State
    ├── version         BIGINT NOT NULL      -- monotonic counter
    ├── updated_at      TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
    ├── PRIMARY KEY (projection_name, key)   -- composite PK
    ```

    Register the migration in your `Migrator`:

    ```rust,ignore
    impl MigratorTrait for Migrator {
        fn migrations() -> Vec<Box<dyn MigrationTrait>> {
            vec![
                Box::new(ferro_projection::CreateProjectionSnapshotsTable),
                // ... your app migrations
            ]
        }
    }
    ```

    ## Errors

    `ProjectionError` is the single error enum:

    | Variant | When |
    |---------|------|
    | `Db(#[from] sea_orm::DbErr)` | underlying database error |
    | `Json(#[from] serde_json::Error)` | State serialization failure |
    | `Broadcast(String)` | broadcast publish failed (state already persisted) |
    | `Events(String)` | event-bus error from `ferro_events::Error` |
    | `StateNotFound { name, key }` | `read_required` hit a missing key |

    Display prefix is `"projection: …"` for grep-friendliness across the
    workspace.
    ```
  </action>
  <verify>
    <automated>test -f docs/src/features/live-read-models.md &amp;&amp; grep -q '^# Live Read-Models$' docs/src/features/live-read-models.md &amp;&amp; grep -q 'Not to be confused with .ferro-projections. (plural)' docs/src/features/live-read-models.md &amp;&amp; grep -q 'The Anti-Pattern' docs/src/features/live-read-models.md &amp;&amp; grep -q 'Per-Key Serialization' docs/src/features/live-read-models.md &amp;&amp; grep -q 'The Projection Trait' docs/src/features/live-read-models.md &amp;&amp; grep -q 'Two Entry Points' docs/src/features/live-read-models.md &amp;&amp; grep -q 'The Rebuild Path' docs/src/features/live-read-models.md &amp;&amp; grep -q 'Broadcast Channel Contract' docs/src/features/live-read-models.md &amp;&amp; grep -q 'Operational Footguns' docs/src/features/live-read-models.md &amp;&amp; grep -q 'Worked Example' docs/src/features/live-read-models.md &amp;&amp; grep -q 'ReservationCountProjection' docs/src/features/live-read-models.md &amp;&amp; ! grep -qiE 'killer feature|the bet|load-bearing|forcing function|gestiscilo|v11\.11' docs/src/features/live-read-models.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/features/live-read-models.md` exists
    - H1 is `# Live Read-Models` (NOT "Projections")
    - Opening paragraph leads with "Not to be confused with `ferro-projections` (plural)"
    - All 14 D-52 sections present: anti-pattern, replacement, per-key diagram, trait, construction, two entry points, read path, rebuild path, channel contract, operational footguns, worked example, schema, errors
    - Worked example is `ReservationCountProjection` (mirroring D-47 showcase test in tutorial form)
    - Does NOT contain marketing trigger phrases, tenant identifiers, or version-milestone names
    - Tone is neutral (matches docs/src/database/reservations.md)
  </acceptance_criteria>
  <done>The user-facing doc page exists with the full D-52 outline + disambiguation lede + worked example. Surface 7 of 8 disambiguation coverage is in place.</done>
</task>

<task type="auto">
  <name>Task 2: Add live-read-models.md nav entry to docs/src/SUMMARY.md</name>
  <files>docs/src/SUMMARY.md</files>
  <read_first>
    - docs/src/SUMMARY.md (current — has `- [Service Projections](features/projections.md)` near line 47, the immediate sibling)
    - .planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-CONTEXT.md D-52 (nav text "Live Read-Models" lock + placement adjacent to Service Projections)
    - .planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-PATTERNS.md §"docs/src/SUMMARY.md"
  </read_first>
  <behavior>
    - `docs/src/SUMMARY.md` contains a new line `- [Live Read-Models](features/live-read-models.md)` placed IMMEDIATELY AFTER the existing `- [Service Projections](features/projections.md)` line
    - Nav text is "Live Read-Models" (NOT "Projections" — D-52 lock; placement next to "Service Projections" reinforces the disambiguation through visual adjacency)
    - No other entries are removed or reordered
  </behavior>
  <action>
    Use the Edit tool on `/Users/alberto/repositories/albertogferrario/ferro/docs/src/SUMMARY.md`. Locate the line:

    ```markdown
    - [Service Projections](features/projections.md)
    ```

    Insert a new line IMMEDIATELY AFTER it:

    ```markdown
    - [Live Read-Models](features/live-read-models.md)
    ```

    Resulting block (an example surrounding context — your actual file context may differ slightly):
    ```markdown
    - [Service Projections](features/projections.md)
    - [Live Read-Models](features/live-read-models.md)
    - [AI & Confirmation](features/ai.md)
    ```

    Do not modify any other SUMMARY.md content.
  </action>
  <verify>
    <automated>grep -q '\- \[Live Read-Models\](features/live-read-models.md)' docs/src/SUMMARY.md &amp;&amp; awk '/\- \[Service Projections\]/{a=NR} /\- \[Live Read-Models\]/{b=NR} END{exit (a&amp;&amp;b&amp;&amp;a<b)?0:1}' docs/src/SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/SUMMARY.md` contains `- [Live Read-Models](features/live-read-models.md)`
    - The new line is positioned immediately after `- [Service Projections](features/projections.md)`
    - Nav text is "Live Read-Models" verbatim (NOT "Projections" or "Live Read Models" without the hyphen)
    - No other SUMMARY.md content modified
  </acceptance_criteria>
  <done>SUMMARY.md picks up the new live-read-models.md page as a sibling of Service Projections.</done>
</task>

<task type="auto">
  <name>Task 3: Add ## ferro-projection section to CHANGELOG.md (D-56) with milestone-completion line</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md lines 1-15 (verify current top-of-file format: H1 + intro paragraph + most recent `## ferro-reservation` section)
    - .planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-CONTEXT.md D-56 (full content list to summarize + milestone-completion line at §Specifics line 519)
    - .planning/phases/154-ferro-reservation-crate-generic-hold-commit-release-with-ttl/154-07-PLAN.md task 3 (analog CHANGELOG entry for ferro-reservation — same shape)
    - Cargo.toml (read `[workspace.package] version` at execution time for the `### [VERSION]` heading)
  </read_first>
  <behavior>
    - CHANGELOG.md gains a new `## ferro-projection` section placed IMMEDIATELY AFTER the introductory paragraph and BEFORE the existing `## ferro-reservation` section (newest-on-top per Phase 152 D-25 convention)
    - The section contains a `### [{ACTUAL_VERSION}] — {DATE}` subheading where `{ACTUAL_VERSION}` is the current `[workspace.package] version` from Cargo.toml (e.g., `0.2.33`) and `{DATE}` is today's date in `YYYY-MM-DD` format
    - The section body summarises the full public surface per D-56
    - The section opens with the disambiguation phrase "Not the same as `ferro-projections` (plural)"
    - The section closes with the literal milestone-completion line from CONTEXT.md §Specifics line 519: "v11.11 Resource Reservation & Live Read-Model Primitives complete — ferro-orm GuardedUpdate (Phase 152), ferro-audit (Phase 153), ferro-reservation (Phase 154), ferro-projection (Phase 155) now all shipped."
  </behavior>
  <action>
    Edit `/Users/alberto/repositories/albertogferrario/ferro/CHANGELOG.md`.

    **Step 1 — Read the current workspace version.**
    ```bash
    grep -E '^version = "' Cargo.toml | head -1 | sed 's/^version = "//; s/"$//'
    ```
    Expected: `0.2.33`. Record the value as `{ACTUAL_VERSION}`.

    **Step 2 — Read today's date.**
    ```bash
    date -u +%Y-%m-%d
    ```
    Record as `{DATE}`.

    **Step 3 — Insert the new section.** Locate the line that starts the most recent crate section (`## ferro-reservation`):
    ```markdown
    ## ferro-reservation
    ```

    Insert the entire block below IMMEDIATELY BEFORE that line (so the new section becomes the new top-of-file section):

    ```markdown
    ## ferro-projection

    ### [{ACTUAL_VERSION}] — {DATE}

    Initial release. Phase 155 — `ferro-projection` crate (live read-model
    runtime: subscribe to domain events, persist per-key snapshots,
    broadcast deltas).

    **Not the same as `ferro-projections` (plural).** That crate is the
    Service Projection abstraction (`ServiceDef → IntentGraph →
    JsonUiRenderer`). `ferro-projection` (singular) is the live read-model
    runtime described above. The two abstractions are orthogonal.

    #### Added

    - New crate `ferro-projection` exposing the `Projection` trait for
      consumer-implemented live read-models. Associated types `Event`
      (`ferro_events::Event + Serialize + DeserializeOwned`), `State`
      (`Clone + Default + Serialize + DeserializeOwned + Send + Sync +
      'static`), `Delta` (`Serialize + Clone + Send + Sync + 'static`).
      Const `NAME: &'static str` for the projection's dotted-namespace
      identifier. Sync `apply(&self, state: &mut State, event: &Event)
      -> Delta` (pure fold; runs inside per-key Mutex). Defaulted
      `snapshot_interval()` (returns 100) and `broadcast_event_name()`
      (returns `"delta"`).
    - `ProjectionRuntime<P: Projection>` orchestrator owning the
      database connection, the broadcaster handle, the projection impl,
      and the per-key Mutex registry. Two entry points: `register(self:
      Arc<Self>)` wires a `ProjectionListener<P>` into
      `ferro_events::global_dispatcher` (one-line wiring), and
      `apply_event(&self, event: &P::Event)` is the manual entry point
      for tests, replay scripts, or custom dispatchers.
    - `read(&self, key) -> Result<Option<State>, ProjectionError>` and
      `read_required(&self, key) -> Result<State, ProjectionError>`
      (returns `StateNotFound` on miss). Read path does NOT acquire the
      per-key Mutex.
    - `rebuild(&self, key, events: impl IntoIterator<Item = P::Event>)
      -> Result<State, ProjectionError>` discards the persisted snapshot,
      folds the supplied event sequence through `State::default()`,
      persists the final state, and broadcasts ONE `"rebuild"` frame
      carrying the full final state. Empty iterator wipes the row.
    - Per-key in-process serialization via
      `DashMap<String, Arc<tokio::sync::Mutex<()>>>` — each key gets its
      own Mutex; same-key applies serialize, different-key applies run
      in parallel. The shard-lock-drop-before-await pattern is the
      correctness mechanism.
    - Snapshot persistence via SeaORM `OnConflict::columns([projection_name,
      key]).update_columns([state, version, updated_at])` upsert on the
      composite primary key. Schema: `projection_snapshots` table with 5
      columns + composite PK on `(projection_name, key)`.
    - Delta broadcast on `projection.{NAME}.{key}` channels via
      `ferro_broadcast::Broadcast::new(...).channel(...).event(...).data(delta).send()`.
      Event name defaults to `"delta"` (consumer can override). Broadcast
      failure does NOT roll back the persisted state — log at
      `tracing::warn!` and surface `ProjectionError::Broadcast`.
    - `ProjectionError` — `Db(#[from] sea_orm::DbErr) | Json(#[from]
      serde_json::Error) | Broadcast(String) | Events(String) |
      StateNotFound { name, key }`. Display prefix `"projection: …"`.
      Hand-rolled `From<ferro_broadcast::Error>` and
      `From<ferro_events::Error>` impls (Phase 149 precedent for
      stringly-typed variants).
    - `ProjectionKey` opaque newtype around `String` with `new`, `as_str`,
      `Display`, `From<String>`, `From<&str>`, serde Serialize +
      Deserialize. Multi-tenancy lives inside the key string by
      convention.
    - `CreateProjectionSnapshotsTable` migration — consumers register it
      in their `Migrator` alongside other ferro-* crates' migrations.
    - Public SeaORM re-exports: `ProjectionSnapshotEntity`,
      `ProjectionSnapshotModel`, `ProjectionSnapshotActiveModel` —
      consumer-side queries against `projection_snapshots` use these.
    - Workspace member registered in `Cargo.toml`; auto-publish Wave 1b
      slot reserved in `.github/workflows/publish.yml`. First publish
      bootstrapped from a local terminal (CI publish token has
      `publish-update` scope only); subsequent versions auto-publish.
    - New documentation page `docs/src/features/live-read-models.md`
      covering the disambiguation from `ferro-projections` plural, the
      anti-pattern, the typed-runtime replacement, the trait surface, the
      two entry points (register + apply_event), the read + rebuild
      paths, the broadcast channel contract, operational footguns (3),
      and a worked example folding `ferro_reservation::ReservationEvent`
      into per-`resource_kind` counters.

    v11.11 Resource Reservation & Live Read-Model Primitives complete —
    ferro-orm GuardedUpdate (Phase 152), ferro-audit (Phase 153),
    ferro-reservation (Phase 154), ferro-projection (Phase 155) now all
    shipped.

    ```

    Replace `{ACTUAL_VERSION}` and `{DATE}` with the values from Steps 1 and 2.

    **Step 4 — Verify the file structure.**
    ```bash
    head -10 CHANGELOG.md
    ```
    Expected: H1 + intro paragraph + new `## ferro-projection` section at the top.

    The existing `## ferro-reservation`, `## ferro-audit`, and `## ferro-orm` sections remain UNCHANGED.
  </action>
  <verify>
    <automated>grep -q '^## ferro-projection$' CHANGELOG.md &amp;&amp; awk '/^## ferro-projection$/{a=NR} /^## ferro-reservation$/{b=NR} END{exit (a&amp;&amp;b&amp;&amp;a<b)?0:1}' CHANGELOG.md &amp;&amp; grep -q '### \[0\.2\.3[3-9]\]' CHANGELOG.md &amp;&amp; grep -q 'ProjectionRuntime' CHANGELOG.md &amp;&amp; grep -q 'CreateProjectionSnapshotsTable' CHANGELOG.md &amp;&amp; grep -q 'Not the same as .ferro-projections. (plural)' CHANGELOG.md &amp;&amp; grep -q 'v11.11 Resource Reservation .. Live Read-Model Primitives complete' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` contains a `## ferro-projection` section
    - The section is positioned BEFORE `## ferro-reservation` (newest-on-top)
    - Contains `### [X.Y.Z] — YYYY-MM-DD` subheading with the actual workspace version (≥ 0.2.33)
    - Contains the disambiguation phrase "Not the same as `ferro-projections` (plural)"
    - Contains the literal milestone-completion line "v11.11 Resource Reservation & Live Read-Model Primitives complete — ferro-orm GuardedUpdate (Phase 152), ferro-audit (Phase 153), ferro-reservation (Phase 154), ferro-projection (Phase 155) now all shipped."
    - Summarises the full public surface per D-56: Projection trait, ProjectionRuntime, ProjectionKey, ProjectionError, register, apply_event, read, read_required, rebuild, CreateProjectionSnapshotsTable, SeaORM entity re-exports, workspace registration, doc page
    - Pre-existing sections (`## ferro-reservation`, `## ferro-audit`, `## ferro-orm`) are unchanged
  </acceptance_criteria>
  <done>CHANGELOG.md has the ferro-projection initial-release entry positioned newest-on-top with the actual workspace version, the disambiguation phrase (surface 8 of 8), and the milestone-completion line. The 7-surface disambiguation coverage is now complete.</done>
</task>

<task type="auto">
  <name>Task 4: Run workspace pre-release gate (fmt + clippy + build + test + doc)</name>
  <files></files>
  <read_first>
    - CLAUDE.md §"Testing & Linting" (exact gate commands)
    - .planning/phases/154-ferro-reservation-crate-generic-hold-commit-release-with-ttl/154-07-PLAN.md task 4 (analog gate)
  </read_first>
  <behavior>
    - All four gate commands exit 0 with zero warnings
    - The full test suite passes including ferro-projection's lib tests + 4 integration test files + proptest_properties
    - cargo doc -p ferro-projection --no-deps produces docs without errors
  </behavior>
  <action>
    Run the workspace-wide pre-release gate sequence (mirrors Phase 154 plan 07 task 4). Each command must exit 0:

    ```bash
    # 1. Format check
    cargo fmt --all -- --check

    # 2. Clippy with -D warnings on all targets across the workspace
    cargo clippy --all --all-targets -- -D warnings

    # 3. Full workspace build
    cargo build --workspace

    # 4. Full test suite with all features
    cargo test --all-features

    # 5. Rustdoc check for ferro-projection
    cargo doc -p ferro-projection --no-deps
    ```

    If ANY command fails:
    - `cargo fmt --all -- --check` failure → run `cargo fmt --all` and re-commit (formatter fixes)
    - `cargo clippy ... -- -D warnings` failure → fix the warnings in the offending file(s)
    - `cargo test` failure → STOP. Surface the failing test name + output. Do NOT proceed to Task 5 (first publish) with failing tests
    - `cargo doc` failure → fix rustdoc syntax errors

    Record the exact test-count summary line from `cargo test --all-features` for the SUMMARY. ferro-projection's cumulative test count is expected ≥ 30 (≥ 23 lib + 2 rebuild + 1 event_bus_integration + 1 projection_over_reservation_events + 1 concurrent_apply + 3 proptest_properties = 31).
  </action>
  <verify>
    <automated>cargo fmt --all -- --check 2>&amp;1 &amp;&amp; cargo clippy --all --all-targets -- -D warnings 2>&amp;1 &amp;&amp; cargo build --workspace 2>&amp;1 &amp;&amp; cargo test --all-features 2>&amp;1 | grep -E 'test result: ok' &amp;&amp; cargo doc -p ferro-projection --no-deps 2>&amp;1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0 with no output
    - `cargo clippy --all --all-targets -- -D warnings` exits 0 with no warnings
    - `cargo build --workspace` exits 0
    - `cargo test --all-features` exits 0 with all tests passing (cumulative test count includes ferro-projection's ≥ 30 tests)
    - `cargo doc -p ferro-projection --no-deps` exits 0
  </acceptance_criteria>
  <done>Workspace pre-release gate is fully green. The repo is ready for first publish.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Manual first-publish bootstrap of ferro-projection to crates.io (D-55)</name>
  <files>(no file edits — manual `cargo publish` invocation from a local terminal)</files>
  <read_first>
    - .planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-CONTEXT.md D-55 (Wave 1b bootstrap reality)
    - .planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-RESEARCH.md §"Environment Availability" (personal publish-new token requirement)
    - .planning/phases/154-ferro-reservation-crate-generic-hold-commit-release-with-ttl/154-07-PLAN.md Task 5 (Phase 154 precedent — same pattern; reused verbatim with name swap)
  </read_first>
  <what-built>
    Plans 01-06 created and tested the ferro-projection crate (≥ 30 tests including lib + 4 integration + 3 proptest properties, full public API, user docs, CHANGELOG entry with milestone-completion line, workspace pre-release gate green). The crate is technically ready to publish. CI's `CARGO_REGISTRY_TOKEN` has `publish-update` scope only and cannot create new crates — this is the fifth time we run this pattern (Phase 151 `ferro-wallet`, Phase 152 `ferro-orm`, Phase 153 `ferro-audit`, Phase 154 `ferro-reservation`, now Phase 155 `ferro-projection`).

    Publishing `ferro-projection` closes out the v11.11 milestone — all four primitives (ferro-orm, ferro-audit, ferro-reservation, ferro-projection) are then live on crates.io and the killer-feature composability is in place.
  </what-built>
  <action>
    Pause for human action. The operator runs the following from a local terminal at the repo root (NOT inside Claude Code). Claude does NOT execute these commands; the resume-signal is the operator's reply confirming completion.

    Required manual steps:

    1. **Pre-flight check** — confirm the current workspace version and that `ferro-projection` is not already on crates.io:
       ```bash
       grep -E '^version = ' Cargo.toml | head -1
       cargo search ferro-projection | head -5
       ```
       Note the exact version string (expected: `0.2.33` or higher if CI auto-bumped between plan 02 and this task). The CHANGELOG entry from Task 3 should reference this same string. If a crate named `ferro-projection` is already published with the current workspace owner, the first publish has already happened — skip ahead to step 7 (push to master). If a crate named `ferro-projection` is owned by someone else, ABORT and surface to the user (name was available at CONTEXT gather time on 2026-05-13 per `feedback_validate_scope_premises.md`).

       **IMPORTANT:** `cargo search ferro-projection` may also list the EXISTING `ferro-projections` (plural) crate from v9.0. Confirm the search result is `ferro-projection` (singular) NOT `ferro-projections` (plural). The disambiguation matters here too.

    2. **Set the personal publish-new-scoped token:**
       ```bash
       # Either export for one command:
       export CARGO_REGISTRY_TOKEN="<your personal publish-new-scoped token>"
       # OR use cargo login to persist:
       cargo login <your personal publish-new-scoped token>
       ```
       The token must have **publish-new** scope. Create at:
       https://crates.io/me → API Tokens → "New Token" → check "publish-new" (NOT just "publish-update"). This is documented in `~/.claude/projects/-Users-alberto-repositories-albertogferrario-ferro/memory/project_ferro_publish_token_scoping.md`.

    3. **Dry run first** to confirm the manifest packages correctly:
       ```bash
       cargo publish -p ferro-projection --dry-run
       ```
       Expected: succeeds; "warning: aborting upload due to dry run" at the end. If you see errors about missing fields (description, repository, license), fix in `ferro-projection/Cargo.toml` and re-run plans 01 + the pre-release gate.

    4. **Real publish:**
       ```bash
       cargo publish -p ferro-projection
       ```
       This uploads the crate to crates.io. Wait for the upload to complete (typically 1-3 minutes; cargo prints "Uploading ferro-projection v0.2.33 ..." then "Updating crates.io index").

    5. **Verify on crates.io:**
       - Visit https://crates.io/crates/ferro-projection — the crate page should be live within 30 seconds.
       - Visit https://docs.rs/ferro-projection — docs.rs builds automatically; allow ~10 minutes for the docs.rs build job. If the build fails, check the docs.rs build log; common issues are missing crate metadata — but plan 01 already set these correctly.
       - **Sanity check the disambiguation:** open the docs.rs page. The module-level rustdoc should open with "Not to be confused with `ferro-projections` (plural)" — this is the rendered output of plan 01's lib.rs rustdoc.

    6. **Verify the crates.io index** picked up the new crate:
       ```bash
       cargo search ferro-projection | head -5
       ```
       Expected: `ferro-projection = "0.2.33"  # Live read-model runtime: subscribe to domain events, persist per-key snapshots, broadcast deltas (not the same as ferro-projections plural)`

    7. **Push to master** (this triggers GH Actions which will run Wave 1b auto-publish for any subsequent version bump):
       ```bash
       git push origin master
       ```
       The auto-publish loop in `.github/workflows/publish.yml` (Wave 1b: `ferro-ai ferro-projections ferro-stripe ferro-whatsapp ferro-notifications ferro-reservation ferro-projection`) will succeed for subsequent versions because the crate now exists on crates.io and the CI's `publish-update`-scoped token suffices.

    8. **Update STATE.md** to record phase completion. Run:
       ```bash
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" state update --phase 155 --status complete
       ```
       (Or manually edit `.planning/STATE.md` if gsd-tools is unavailable — set `progress.completed_phases` and `progress.completed_plans` to reflect this phase's completion AND record the v11.11 milestone completion.)

    9. **Reply with the resume-signal** below so the workflow continues.
  </action>
  <verify>
    <automated>cargo search ferro-projection 2>&amp;1 | grep -E '^ferro-projection = "0\.2\.[0-9]+"'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo search ferro-projection | head -5` shows the crate published with the current workspace version (≥ 0.2.33)
    - The search output confirms `ferro-projection` (singular) — NOT confused with the existing `ferro-projections` (plural)
    - `git push origin master` has completed successfully (operator-confirmed)
    - `.planning/STATE.md` records Phase 155 as complete AND records v11.11 milestone completion
    - The crates.io page https://crates.io/crates/ferro-projection is live with the disambiguation rustdoc rendering correctly
  </acceptance_criteria>
  <done>
    The operator has run `cargo publish -p ferro-projection` from a local terminal with a personal `publish-new`-scoped token; the crate is live on crates.io; the commits from this phase have been pushed to origin/master; STATE.md is updated to reflect Phase 155 complete and v11.11 milestone complete. The killer-feature composability (events + reservations + audit + projections in one app skeleton) is now consumer-accessible via `ferro-projection = "0.2"`.
  </done>
  <resume-signal>
    Type "published" once `cargo search ferro-projection` shows the crate live on crates.io and `git push origin master` has completed. If the publish fails (e.g., token has wrong scope, crate name conflict with the plural variant, network error), describe the error so the workflow can be adjusted.
  </resume-signal>
</task>

</tasks>

<verification>
- `docs/src/features/live-read-models.md` exists with the disambiguation lede + all 14 D-52 sections
- `docs/src/SUMMARY.md` contains the `- [Live Read-Models](features/live-read-models.md)` nav entry positioned after Service Projections
- `CHANGELOG.md` has a `## ferro-projection` section at the top with the actual workspace version, the disambiguation phrase, and the milestone-completion line
- `cargo fmt --all -- --check` exits 0
- `cargo clippy --all --all-targets -- -D warnings` exits 0
- `cargo build --workspace` exits 0
- `cargo test --all-features` exits 0 with all tests passing (cumulative count ≥ 30 in ferro-projection alone)
- `cargo doc -p ferro-projection --no-deps` exits 0
- After Task 5: `cargo search ferro-projection` shows the crate live on crates.io
- After Task 5: `git push origin master` completes; STATE.md records phase 155 complete + v11.11 milestone complete
- The 8-surface disambiguation coverage is complete (Cargo.toml + ferro-projection/README.md + lib.rs + projection.rs + CLAUDE.md + workspace README.md + docs page + CHANGELOG)
</verification>

<success_criteria>
Phase 155 ships. The `ferro-projection` crate is live on crates.io. **The v11.11 milestone is complete** — all four primitives (ferro-orm GuardedUpdate, ferro-audit, ferro-reservation, ferro-projection) are live and consumers can compose them: events emitted by any domain operation → audited automatically → folded into live read-models → deltas land on subscribed WebSocket channels. Capacity-constrained apps with live dashboards (operator views, real-time counters, kanban boards) can now wire the full pipeline in one `Arc::new(Runtime::new(...)).register()` per projection.

Every D-XX addressed by this plan is satisfied: D-51 (module-level rustdoc disambiguation — already in lib.rs from plan 01, reinforced here in the docs page), D-52 (user-facing doc page with disambiguation lede + full 14-section outline + worked example), D-53 (no MCP tool changes — ferro-mcp auto-includes the crate), D-54 (workspace version 0.2.33 — already bumped by plan 02), D-55 (Wave 1b auto-publish reserved — manual bootstrap for first publish), D-56 (CHANGELOG entry with full public surface summary + milestone-completion line).

The 8-surface disambiguation coverage is now complete:
1. ferro-projection/Cargo.toml description (plan 01)
2. ferro-projection/README.md (plan 01)
3. ferro-projection/src/lib.rs module rustdoc (plan 01)
4. ferro-projection/src/projection.rs module rustdoc (plan 04)
5. CLAUDE.md workspace structure row (plan 02)
6. README.md workspace root bullet (plan 02)
7. docs/src/features/live-read-models.md opening paragraph (this plan)
8. CHANGELOG.md ferro-projection section opening (this plan)

Every reader-visible mention of "ferro-projection" now also names "ferro-projections (plural)" with explicit framing of the orthogonality.
</success_criteria>

<output>
After completion, create `.planning/phases/155-ferro-projection-crate-live-read-model-from-domain-events-wi/155-07-SUMMARY.md` documenting:
- The 3 files modified (docs/src/features/live-read-models.md, docs/src/SUMMARY.md, CHANGELOG.md)
- The exact `[workspace.package] version` published (e.g., 0.2.33 — record the actual value)
- Confirmation that the workspace pre-release gate (fmt + clippy + build + test + doc) all exited 0
- The exact `cargo test --all-features` test-count summary line (cumulative across the workspace)
- The exact `ferro-projection`-only test count (expected ≥ 30: 5 error + 1 migration + 2 entity + 3 key + 2 projection + ≥ 10 runtime + 2 rebuild + 1 event_bus_integration + 1 projection_over_reservation_events + 1 concurrent_apply + 3 proptest_properties = ≥ 31)
- Confirmation that the first publish bootstrap succeeded (or, if it failed, the exact error and the planned recovery)
- A note that subsequent ferro-projection versions auto-publish via the existing GH Actions Wave 1b loop
- **A milestone-completion record:** v11.11 Resource Reservation & Live Read-Model Primitives shipped — ferro-orm (Phase 152), ferro-audit (Phase 153), ferro-reservation (Phase 154), ferro-projection (Phase 155) all live on crates.io
- STATE.md update — phase 155 marked complete AND v11.11 milestone marked complete
- A confirmation note that the 8-surface disambiguation coverage is in place (Cargo.toml, ferro-projection/README.md, lib.rs, projection.rs, CLAUDE.md, root README.md, docs page, CHANGELOG)
</output>
