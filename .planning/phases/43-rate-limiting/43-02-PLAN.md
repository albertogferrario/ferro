---
phase: 43-rate-limiting
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - framework/src/middleware/rate_limit.rs
  - framework/src/cache/memory.rs
autonomous: true
---

<objective>
Write comprehensive tests for the refactored rate limiting system.

Purpose: Validate cache-backed rate limiting, named limiters, multi-limit enforcement, TTL behavior, and fail-open semantics.

Output: Test module in rate_limit.rs covering all new functionality, plus a test for InMemoryCache expire().
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/43-rate-limiting/43-RESEARCH.md
@.planning/phases/43-rate-limiting/43-01-SUMMARY.md

@framework/src/middleware/rate_limit.rs
@framework/src/cache/memory.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add InMemoryCache expire() test</name>
  <files>framework/src/cache/memory.rs</files>
  <action>
Add test to the existing `tests` module in `memory.rs` (if none exists, create `#[cfg(test)] mod tests`):

1. `test_expire_sets_ttl` — Increment a key, call expire with 1-second TTL, verify the key exists. Then sleep 1.1s and verify the key is expired (get returns None or increment returns 1 as if new).

2. `test_expire_missing_key` — Call expire on a key that doesn't exist. Should return `Ok(false)`.

3. `test_increment_then_expire_preserves_value` — Increment to 5, expire with 10s TTL, increment again. Should return 6 (not 1). This verifies expire doesn't reset the value.

Use `#[tokio::test]` for async tests.
  </action>
  <verify>cargo test -p ferro-rs --lib cache::memory -- --nocapture passes</verify>
  <done>All three expire-related tests pass in memory.rs.</done>
</task>

<task type="auto">
  <name>Task 2: Write rate_limit.rs test module</name>
  <files>framework/src/middleware/rate_limit.rs</files>
  <action>
Add comprehensive `#[cfg(test)] mod tests` at the bottom of rate_limit.rs. Tests should use the framework's InMemoryCache bootstrapped via `App::bind`. Each test must set up cache beforehand.

**Helper:** Create a `setup_test_cache()` function that initializes InMemoryCache and binds it to the container:
```rust
fn setup_test_cache() {
    use crate::cache::{InMemoryCache, CacheStore};
    use crate::container::App;
    use std::sync::Arc;
    App::bind::<dyn CacheStore>(Arc::new(InMemoryCache::new()));
}
```

**Tests to write:**

1. **Limit builder tests** (sync, no cache needed):
   - `test_limit_per_minute` — `Limit::per_minute(60)` has max=60, window=60
   - `test_limit_per_hour` — `Limit::per_hour(1000)` has max=1000, window=3600
   - `test_limit_by_key` — `Limit::per_minute(60).by("user:1")` has key = Some("user:1")

2. **RateLimiter define/resolve tests** (use `#[serial]` from serial_test for global state):
   - `test_define_and_resolve` — Define "test", resolve returns Some with correct limits
   - `test_resolve_undefined` — Resolve unknown name returns None
   - `test_define_multiple_limits` — Define returning `vec![Limit::per_minute(500), Limit::per_minute(5).by("email")]`, resolve returns 2 limits

3. **Rate limit checking tests** (async, need cache):
   - `test_allows_within_limit` — Increment 5 times with limit 10, all should be Allowed with decreasing remaining
   - `test_exceeds_limit` — Increment 4 times with limit 3, 4th should be Exceeded
   - `test_separate_keys_independent` — Exhaust key_a, key_b should still have quota
   - `test_cache_failure_allows_request` — If cache not initialized, rate limit check should return Allowed (fail-open)

4. **Throttle builder tests** (sync):
   - `test_throttle_per_minute` — `Throttle::per_minute(60)` constructs correctly
   - `test_throttle_named` — `Throttle::named("api")` constructs correctly

Use `#[serial]` for tests that modify global state (RateLimiter registry, App container). Import from `serial_test::serial`.
  </action>
  <verify>cargo test -p ferro-rs --lib middleware::rate_limit -- --nocapture passes. All tests green.</verify>
  <done>All rate limit tests pass. Coverage: Limit builder, RateLimiter define/resolve, cache-backed rate checking, Throttle construction, fail-open behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p ferro-rs --lib cache::memory` passes (expire tests)
- [ ] `cargo test -p ferro-rs --lib middleware::rate_limit` passes (all rate limit tests)
- [ ] `cargo test --all-features` passes (no regressions)
- [ ] `cargo clippy -- -D warnings` passes
</verification>

<success_criteria>

- InMemoryCache expire() behavior validated with TTL expiration test
- Limit builder API tested (per_minute, per_hour, by)
- RateLimiter define/resolve tested with single and multiple limits
- Cache-backed rate checking tested for allow, exceed, and separate keys
- Fail-open behavior tested (cache unavailable → allow)
- All tests pass with no warnings
  </success_criteria>

<output>
After completion, create `.planning/phases/43-rate-limiting/43-02-SUMMARY.md`
</output>
