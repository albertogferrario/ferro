---
phase: 249.4
slug: mcp-service-scanner-named-impl-multiline-robustness
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-16
---

# Phase 249.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in `#[test]` + `cargo test` |
| **Config file** | none — in-file `#[cfg(test)] mod tests` in `ferro-mcp/src/tools/list_services.rs` |
| **Quick run command** | `cargo test -p ferro-mcp 2>&1` |
| **Full suite command** | `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features 2>&1` |
| **Estimated runtime** | ~60–180 s (quick `-p ferro-mcp` is seconds; full gate dominated by clippy/compile) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ferro-mcp 2>&1`
- **After every plan wave:** Run `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features 2>&1`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~10 s (quick per-crate test)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 249.4-01-01 | 01 | 1 | OFFLOAD-06 (join helper) | — | N/A | unit | `cargo test -p ferro-mcp normalize_service_lines` | ❌ W0 | ⬜ pending |
| 249.4-01-02 | 01 | 1 | OFFLOAD-06 SC#1 + SC#2 | — | N/A | unit (fixture) | `cargo test -p ferro-mcp scan_offload_methods_multiline` | ❌ W0 | ⬜ pending |
| 249.4-01-03 | 01 | 1 | OFFLOAD-06 SC#3 (regression) | — | N/A | regression | `cargo test -p ferro-mcp scan_offload_methods` | ✅ `:659` | ⬜ pending |
| 249.4-01-04 | 01 | 1 | WR-01 regression | — | N/A | unit | `cargo test -p ferro-mcp extract_service_impl_name` | ✅ `:617` | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `ferro-mcp/src/tools/list_services.rs` — add `normalize_service_lines` helper (joins multi-line `#[service(...)]` into one logical line via paren-depth balancing)
- [ ] `ferro-mcp/src/tools/list_services.rs` — add `normalize_service_lines` unit test (multi-line positional + named collapse; single-line and ordinary lines pass through unchanged)
- [ ] `ferro-mcp/src/tools/list_services.rs` — add multi-line fixture test (`scan_offload_methods_multiline...`) asserting the multi-line-attributed service is surfaced (SC#1) and its `#[offload]` methods correlate (SC#2)

*SC#3 and WR-01 regression coverage already exists in `:617-635` (`extract_service_impl_name`) and `:659` (`scan_offload_methods`) — keep passing, no new test required.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| — | — | — | — |

*All phase behaviors have automated verification.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s (quick per-crate run)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
