---
status: passed
phase: 249.4-mcp-service-scanner-named-impl-multiline-robustness
verified: 2026-08-16
must_haves_verified: 3
must_haves_total: 3
requirements: [OFFLOAD-06]
---

# Phase 249.4 — Verification

**Goal:** Make the `ferro-mcp` static offload scanner parse multi-line `#[service(...)]`
attributes (previously dropped because `(` and `)` were required on the same trimmed line),
scoped to WR-02 only. Hardens OFFLOAD-06.

**Verdict:** PASSED — all three success criteria are TRUE against the actual codebase.

## Success Criteria

| SC | Statement | Evidence | Status |
|----|-----------|----------|--------|
| SC#1 | A multi-line `#[service(...)]` attribute is parsed and its service surfaced, not dropped | `tools::list_services::tests::scan_offload_methods_multiline_service_attr` … ok (temp-dir fixture: named multi-line attribute with trailing comma → `ServiceItem` surfaced) | ✅ |
| SC#2 | The trait's `#[offload]` methods are correlated to the service parsed from the multi-line form | same fixture asserts the offloaded methods land on the correlated `svc.methods` … ok | ✅ |
| SC#3 | No regression to single-line `#[service]` / `#[service(Type)]` / `#[service(impl = X)]` parsing | `tools::list_services::tests::scan_offload_methods` … ok (pre-existing single-line guard) + `extract_service_impl_name_positional_and_named` … ok (WR-01 guard) | ✅ |

Named-test run (cache-warm, throttled `CARGO_BUILD_JOBS=1 --test-threads=2`):
`4 passed; 0 failed; 319 filtered out`. Full `ferro-mcp` suite: `323 passed; 0 failed`.

## must_haves (grep-verified against source)

- **Join helper exists** — `fn normalize_service_lines(content: &str) -> Vec<String>` at `ferro-mcp/src/tools/list_services.rs:285`.
- **Both fix sites consume the normalized stream** — `let logical_lines = normalize_service_lines(&content)` at `:146` (`scan_services_from_files`, fix site 1) and `:429` (`scan_offload_methods_from_files`, fix site 2). Single shared helper (CONTEXT.md D-03).
- **New tests present** — helper unit test `normalize_service_lines` (`:835`) and multi-line fixture `scan_offload_methods_multiline_service_attr` (`:785`).

## Scope & decision compliance

- **D-02 (stay line-based, no `syn`)** — `0` occurrences of `syn::` in the file. ✅
- **WR-01 not re-implemented** — `extract_service_impl_name` (`:264`) unchanged and reused verbatim on the joined inner. ✅
- **Single-file change** — only `ferro-mcp/src/tools/list_services.rs` modified; no `ferro-macros` / `ferro-queue` / runtime / docs changes. ✅
- **`FnCollecting` orthogonality** — the pre-pass joins only `#[service(` attributes; multi-line `fn` accumulation untouched (all 323 lib tests green, including the existing multi-line `fn` fixtures). ✅

## Gate evidence

- Full pre-commit gate `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` was run green by the executor's Task 3 (commit chain `b580baf1`→`6d8ce5a3`) on source identical to the verified state.
- This verification re-ran the phase's own test surface (`ferro-mcp`) cache-warm under a 2-core throttle: `323 passed; 0 failed`.

## Regressions

None. The pre-existing single-line and WR-01 guards pass; the full `ferro-mcp` suite is green.
