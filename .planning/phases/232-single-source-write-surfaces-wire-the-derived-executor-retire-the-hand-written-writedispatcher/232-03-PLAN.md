---
phase: 232-single-source-write-surfaces-wire-the-derived-executor-retire-the-hand-written-writedispatcher
plan: 03
type: execute
wave: 3
depends_on: ["232-01", "232-02"]
files_modified:
  - app/src/tests/single_source.rs
  - app/src/tests/mod.rs
autonomous: true
requirements: [EXEC-05]

must_haves:
  truths:
    - "ONE ServiceDef transition exercised through BOTH the MCP framing and the visual handler yields identical guard re-eval and identical persisted to_state (SC3)"
    - "Exactly one dispatch_write definition exists across the workspace (no per-channel executor)"
    - "No hand-authored match re-encoding StateMachine transitions exists on the write path (SC4)"
    - "The full workspace gate (fmt + clippy --all --all-targets -D warnings + test --all-features) is green"
  artifacts:
    - path: "app/src/tests/single_source.rs"
      provides: "Both-channels single-source integration test driving the same transition via MCP and visual"
      contains: "single_source_both_channels"
  key_links:
    - from: "app/src/tests/single_source.rs"
      to: "framework::write::dispatch_write (via both MCP framing and the visual handler)"
      via: "same ServiceDef transition exercised through both call paths"
      pattern: "single_source_both_channels"
---

<objective>
Prove single-source: ONE `submit` transition declared once on a `ServiceDef` is exercised through BOTH
write surfaces — (a) the MCP framing (`handle_write_call`) and (b) the new visual `POST /{service}/{action}`
handler — asserting identical guard re-evaluation and identical persisted derived `to_state`, with the
only difference being the audit channel (`mcp` vs `web`). This is the EXEC-05 coherence proof (SC3): one
declaration, one executor, two callers.

Then close SC4: grep the entire write path and prove no hand-authored `match` re-encoding a StateMachine
transition exists anywhere, and exactly one `dispatch_write` definition exists (no per-channel executor).
Finally run the full workspace gate as the phase acceptance.

This plan adds NO production logic — it is the single-source verification harness plus the structural
grep proof plus the final gate. Reuse the existing synthetic order/approval anchor; author NO new app models.

Purpose: Demonstrate the milestone's cross-modal claim is TRUE — writes route through one shared kernel in
every modality (SC3, SC4). Output: a both-channels test, a structural grep assertion, and a green full gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/232-single-source-write-surfaces-wire-the-derived-executor-retire-the-hand-written-writedispatcher/232-RESEARCH.md
@.planning/phases/232-single-source-write-surfaces-wire-the-derived-executor-retire-the-hand-written-writedispatcher/232-VALIDATION.md

<interfaces>
<!-- The two call paths the single-source test drives. Both end in framework::write::dispatch_write. -->

MCP path (Plan 01 result): `ferro-mcp-server::handle_write_call(...)` → `ferro::write::dispatch_write(..., channel="mcp", ...)`.
  Drive it as the existing MCP write tests do (`app/src/tests/mcp_write_dispatch.rs` — submit_persists_derived_to_state).

Visual path (Plan 02 result): `app::controllers::visual_action::handle(req)` for `POST /{service}/{action}`
  → `ferro::write::dispatch_write(..., channel="web", ...)`.
  Drive it as the Plan 02 tests do (`app/src/tests/visual_action.rs`).

Both resolve the same `(ServiceDef, ActionDef)` from `crate::controllers::mcp::exposed_services()` and
derive `to_state` from `ferro::derive_transition_plan(svc, &action.name).to_state`.

The synthetic anchor: the order/approval `ServiceDef` with a guarded `submit` transition (draft → submitted),
already used by the 231 MCP tests. Reuse it — do NOT create new models.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Both-channels single-source integration test (SC3)</name>
  <files>app/src/tests/single_source.rs, app/src/tests/mod.rs</files>
  <read_first>
    - app/src/tests/mcp_write_dispatch.rs (the MCP submit-transition fixture: how it constructs the action, dispatcher, tenant, and asserts persisted to_state)
    - app/src/tests/visual_action.rs (the Plan 02 visual fixture: how it issues POST /{service}/{action} and reads the persisted state)
    - app/src/controllers/mcp.rs (:68 make_write_dispatcher reused by both paths)
    - app/src/tests/mod.rs (test module registration)
    - .planning/phases/232-.../232-VALIDATION.md (SC3 row: single_source_both_channels)
  </read_first>
  <action>
    Create `app/src/tests/single_source.rs` with `single_source_both_channels`:
    1. Set up the synthetic order/approval anchor twice (two records in the same draft state, same tenant),
       OR run the same transition twice against fresh fixtures — one per channel. Reuse the helpers from
       `mcp_write_dispatch.rs` / `visual_action.rs`; do NOT duplicate model setup logic if a shared helper
       exists.
    2. Drive the `submit` transition through the MCP path (as `submit_persists_derived_to_state` does) and
       capture: the persisted `to_state`, and that the guard was re-evaluated (the transition succeeded
       only because the guard held).
    3. Drive the SAME `submit` transition through the visual path (POST /{service}/{action} via the Plan 02
       handler) and capture the persisted `to_state`.
    4. ASSERT identical semantics:
       - identical persisted `to_state` (both == the derived `Transition.to`, e.g. "submitted");
       - identical guard outcome (both required the guard to hold — drive a guard-failing variant through
         BOTH paths in a second sub-assertion / sibling test `single_source_guard_rejects_both` and assert
         BOTH reject and leave state unchanged);
       - the audit channel differs (`mcp.action.submit` vs `web.action.submit`) — the ONLY divergence,
         confirming a single kernel with a channel tag rather than two executors.
    5. Register `mod single_source;` in `app/src/tests/mod.rs`.
    6. Run `cargo test -p app single_source`.
  </action>
  <verify>
    <automated>cargo test -p app single_source</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/tests/single_source.rs` exists; `grep -n 'single_source_both_channels' app/src/tests/single_source.rs` matches.
    - `cargo test -p app single_source_both_channels` exits 0 (identical to_state + identical guard semantics across MCP and visual; audit channel the only difference).
    - The test drives BOTH `handle_write_call` (MCP) and `visual_action::handle` (visual) against the SAME ServiceDef transition — `grep -n 'visual_action\|handle_write_call\|mcp' app/src/tests/single_source.rs` shows both paths referenced.
  </acceptance_criteria>
  <done>SC3 holds: one declared transition, exercised through both the MCP and visual surfaces, persists the identical derived to_state with identical guard re-eval; the audit channel is the only divergence, proving a single shared kernel.</done>
</task>

<task type="auto">
  <name>Task 2: SC4 structural proof + full workspace gate (phase acceptance)</name>
  <files>(verification only — grep assertions + full workspace gate; no source files modified)</files>
  <read_first>
    - .planning/phases/232-.../232-VALIDATION.md (SC4 row + full suite command)
    - .planning/phases/232-.../232-RESEARCH.md (§Sampling Rate — disk-full caveat for the full gate)
    - MEMORY: project_ferro_disk_full_test_gate (check df + clean target/ before the full --all-features run)
  </read_first>
  <action>
    1. SC4 — structural single-source assertion (grep, must hold):
       - `grep -rn 'pub async fn dispatch_write' framework/src ferro-mcp-server/src app/src` → EXACTLY ONE
         match (in `framework/src/write/mod.rs`). Any second definition = a per-channel executor = FAIL.
       - `grep -rnE 'match .*action' app/src ferro-mcp-server/src framework/src/write` → NO match that maps
         an action name to a transition target/new-status. (Incidental `match` on unrelated enums is fine;
         the forbidden pattern is `match action_name { "submit" => "submitted", ... }` re-encoding the
         StateMachine. Eyeball any hits to confirm none re-encode a transition — research §SC4.)
       - `grep -rn 'match action_name' app/src ferro-mcp-server/src` → empty (the 231 deletion, preserved).
       Encode these as either a small `#[test]` that shells out / reads source, or document them as a CI
       grep step in the SUMMARY. If any grep fails, the prior plans introduced a duplicate executor or a
       match — fix there, do not weaken the assertion.
    2. Full workspace gate (phase acceptance). FIRST check disk per the memory note:
       `df -h .` and clean `target/` if low (`project_ferro_disk_full_test_gate` — ENOSPC is not a real
       defect; free space and retry). Then run, one CPU-intensive op at a time (do NOT parallelize):
       - `cargo fmt --all -- --check`
       - `cargo clippy --all --all-targets -- -D warnings`
       - `cargo test --all-features`
       All three MUST be clean/green. If `cargo test --all-features` ENOSPC-fails, free space and re-run;
       a green earlier `-p` run plus a clean fmt/clippy is the evidence trail, but the phase gate requires
       the full `--all-features` test to actually pass.
    3. If `cargo test` regenerates `docs/protocol/schemas/*.json` (the Phase 94 export test churn,
       MEMORY: project_schema_export_test_dirties_tree), `git checkout` those files — do NOT fold them
       into the phase commit.
  </action>
  <verify>
    <automated>grep -rn 'pub async fn dispatch_write' framework/src ferro-mcp-server/src app/src | wc -l | grep -qx '1' && grep -rn 'match action_name' app/src ferro-mcp-server/src; echo "exit:$?" ; cargo fmt --all -- --check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'pub async fn dispatch_write' framework/src ferro-mcp-server/src app/src` returns exactly ONE line (single kernel, no per-channel executor).
    - `grep -rn 'match action_name' app/src ferro-mcp-server/src` is empty (SC4: no transition-target match).
    - `cargo fmt --all -- --check` clean.
    - `cargo clippy --all --all-targets -- -D warnings` clean.
    - `cargo test --all-features` exits 0 (full gate green; disk checked).
  </acceptance_criteria>
  <done>SC4 holds: exactly one dispatch_write definition and no hand-authored transition match anywhere on the write path; the full workspace gate (fmt + clippy + test --all-features) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (verification plan) | No new runtime surface; this plan only exercises and asserts the Plan 01/02 surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-232-10 | Elevation of Privilege | a hidden second executor escaping review | mitigate | SC4 grep asserts exactly one `dispatch_write` definition; a second would be caught here before phase sign-off |
| T-232-11 | Tampering | divergent transition semantics between channels | mitigate | `single_source_both_channels` asserts identical to_state + guard outcome across MCP and visual; divergence fails the test |
</threat_model>

<verification>
- `cargo test -p app single_source` — both-channels test(s) green (SC3).
- `grep -rn 'pub async fn dispatch_write' framework/src ferro-mcp-server/src app/src` → exactly one (SC4).
- `grep -rn 'match action_name' app/src ferro-mcp-server/src` → empty (SC4).
- Full gate: `cargo fmt --all -- --check` + `cargo clippy --all --all-targets -- -D warnings` + `cargo test --all-features` all green.
</verification>

<success_criteria>
- SC3: one ServiceDef transition exercised through both the MCP and visual surfaces yields identical guard re-eval + identical persisted to_state; audit channel is the only difference.
- SC4: exactly one `dispatch_write` definition; no hand-authored `match` re-encoding a StateMachine transition on the write path.
- The full workspace gate is green.
</success_criteria>

<output>
After completion, create `.planning/phases/232-single-source-write-surfaces-wire-the-derived-executor-retire-the-hand-written-writedispatcher/232-03-SUMMARY.md`
</output>
