---
phase: 48-app-scaffold-auth-profiles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - app-proximity/Cargo.toml
  - app-proximity/src/main.rs
  - app-proximity/src/bootstrap.rs
  - app-proximity/src/routes.rs
  - app-proximity/src/models/mod.rs
  - app-proximity/src/models/entities/mod.rs
  - app-proximity/src/models/entities/users.rs
  - app-proximity/src/models/users.rs
  - app-proximity/src/migrations/mod.rs
  - app-proximity/src/migrations/m20260210_000001_create_users_table.rs
  - app-proximity/src/controllers/mod.rs
  - app-proximity/src/middleware/mod.rs
  - app-proximity/src/providers/mod.rs
  - app-proximity/src/resources/mod.rs
  - app-proximity/src/config/mod.rs
  - app-proximity/.env
autonomous: true
---

<objective>
Create the app-proximity workspace crate with User model including location fields.

Purpose: Establish the application skeleton that all subsequent phases build on. The User model includes latitude/longitude for proximity-based features.
Output: Compilable workspace crate with User migration, entity, model, and Authenticatable impl.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference patterns from existing app/ crate:
@app/Cargo.toml
@app/src/main.rs
@app/src/bootstrap.rs
@app/src/routes.rs
@app/src/models/mod.rs
@app/src/models/entities/mod.rs
@app/src/models/entities/users.rs
@app/src/models/users.rs
@app/src/migrations/mod.rs
@app/src/migrations/m20251208_160100_create_users_table.rs
@app/src/providers/auth_provider.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app-proximity workspace crate with directory structure</name>
  <files>
    Cargo.toml,
    app-proximity/Cargo.toml,
    app-proximity/src/main.rs,
    app-proximity/src/bootstrap.rs,
    app-proximity/src/routes.rs,
    app-proximity/src/models/mod.rs,
    app-proximity/src/models/entities/mod.rs,
    app-proximity/src/controllers/mod.rs,
    app-proximity/src/middleware/mod.rs,
    app-proximity/src/providers/mod.rs,
    app-proximity/src/resources/mod.rs,
    app-proximity/src/config/mod.rs,
    app-proximity/.env
  </files>
  <action>
    1. Add "app-proximity" to workspace members in root Cargo.toml.

    2. Create app-proximity/Cargo.toml following app/Cargo.toml pattern:
       - Package name: "app-proximity", binary name: "proximity"
       - Same dependencies as app/Cargo.toml (ferro path dep, tokio, sea-orm-migration, sea-orm, serde, async-trait, dotenvy, clap, chrono)

    3. Create app-proximity/src/main.rs following app/src/main.rs pattern exactly:
       - Same CLI structure (serve, db:migrate, db:status, db:rollback, db:fresh, schedule:*)
       - Same fail_with helper
       - Module declarations: mod bootstrap, config, controllers, middleware, migrations, models, providers, resources, routes
       - Config::init, config::register_all, bootstrap::register, routes::register

    4. Create app-proximity/src/bootstrap.rs as minimal bootstrap:
       - DB::init().await with error guidance
       - Empty middleware registration (no ShareInertiaData — this app uses JSON-UI only)
       - Comment placeholder for UserProvider binding (added in Plan 02)
       - Comment placeholder for RateLimiter (added later)

    5. Create app-proximity/src/routes.rs with empty routes! macro:
       - Import ferro route macros
       - Single placeholder: get!("/", controllers::home::health_check)
       - The health_check handler returns json_response!({"status": "ok"})

    6. Create module stubs (mod.rs files) for: models, models/entities, controllers, middleware, providers, resources, config
       - controllers/mod.rs should declare `pub mod home;`
       - Create controllers/home.rs with health_check handler

    7. Create app-proximity/src/config/mod.rs with empty register_all() function.

    8. Create app-proximity/.env with:
       - DATABASE_URL=sqlite://./proximity.db
       - SERVER_HOST=127.0.0.1
       - SERVER_PORT=8001 (different from app's default to avoid conflicts)

    Mirror the app/ structure exactly. This is a new app crate, not a framework crate — it depends on ferro framework.
  </action>
  <verify>cd app-proximity && cargo check 2>&1 | tail -5 — should compile without errors</verify>
  <done>app-proximity compiles, health check route defined, workspace member registered</done>
</task>

<task type="auto">
  <name>Task 2: Create User model with location fields and migration</name>
  <files>
    app-proximity/src/migrations/m20260210_000001_create_users_table.rs,
    app-proximity/src/migrations/mod.rs,
    app-proximity/src/models/entities/users.rs,
    app-proximity/src/models/entities/mod.rs,
    app-proximity/src/models/users.rs,
    app-proximity/src/models/mod.rs
  </files>
  <action>
    1. Create migration m20260210_000001_create_users_table.rs following app/src/migrations/ pattern:
       - id: integer, primary key, auto increment
       - name: string, not null
       - email: string, not null, unique index
       - password: string, not null
       - bio: string, nullable (short profile text)
       - latitude: double, nullable (user's location lat)
       - longitude: double, nullable (user's location lng)
       - avatar_url: string, nullable (profile picture URL)
       - remember_token: string, nullable
       - created_at: timestamp, not null, default current_timestamp
       - updated_at: timestamp, not null, default current_timestamp
       Use sea_orm ColumnDef::new(...).double() for lat/lng.

    2. Create migrations/mod.rs with Migrator struct listing the users migration.

    3. Create models/entities/users.rs following app/src/models/entities/users.rs pattern:
       - #[derive(Clone, Debug, PartialEq, DeriveEntityModel, Serialize, Deserialize, FerroModel)]
       - #[sea_orm(table_name = "users")]
       - Fields matching migration columns
       - latitude: Option<f64>, longitude: Option<f64>, bio: Option<String>, avatar_url: Option<String>
       - Empty Relation enum

    4. Update models/entities/mod.rs: pub mod users;

    5. Create models/users.rs following app/src/models/users.rs pattern:
       - Re-export entities::users::*
       - Type alias: pub type User = Model
       - find_by_email method
       - Authenticatable impl (auth_identifier returns id as i64)

    6. Update models/mod.rs: pub mod entities; pub mod users;
  </action>
  <verify>cd app-proximity && cargo check 2>&1 | tail -5 — should compile without errors. Run: cargo test -p app-proximity (even if no tests, confirms compilation)</verify>
  <done>User model with location fields compiles, migration defined, Authenticatable implemented</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check -p app-proximity` succeeds
- [ ] Workspace Cargo.toml includes "app-proximity"
- [ ] User entity has latitude, longitude, bio, avatar_url fields
- [ ] Migration creates users table with all columns
- [ ] Authenticatable trait implemented on User model
- [ ] Health check route compiles
</verification>

<success_criteria>

- app-proximity crate compiles as workspace member
- User model includes location fields (latitude, longitude)
- Migration is ready to run
- Authenticatable impl ready for auth setup in Plan 02
- Directory structure mirrors app/ conventions
</success_criteria>

<output>
After completion, create `.planning/phases/48-app-scaffold-auth-profiles/48-01-SUMMARY.md`
</output>
