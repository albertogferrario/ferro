---
phase: 87-service-relationships
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-projections/src/relationship.rs
  - ferro-projections/src/service.rs
  - ferro-projections/src/lib.rs
autonomous: true
---

<objective>
Add service relationship definitions to ferro-projections: RelationshipDef, Cardinality, NavigationHint types with builder API and ServiceDef integration.

Purpose: Declare service-to-service relationships as structural signals for intent derivation (Phase 88-89). Relationships carry two dimensions: structural (cardinality) and presentational (navigation hint).
Output: New relationship.rs module, updated ServiceDef with relationship builder methods, validation, comprehensive tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-service-relationships/RESEARCH.md

@ferro-projections/CLAUDE.md
@ferro-projections/src/lib.rs
@ferro-projections/src/service.rs
@ferro-projections/src/field.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create relationship types and ServiceDef integration</name>
  <files>ferro-projections/src/relationship.rs, ferro-projections/src/service.rs, ferro-projections/src/lib.rs</files>
  <action>
Create `ferro-projections/src/relationship.rs` with three types following established crate patterns:

**RelationshipDef struct:**
- Fields: name (String), target (String), cardinality (Cardinality), navigation (NavigationHint), foreign_key (Option<String>), inverse (Option<String>), description (Option<String>)
- Derive: Debug, Clone, Serialize, Deserialize, PartialEq, Eq, JsonSchema
- Use `#[serde(skip_serializing_if = "Option::is_none")]` on optional fields
- Builder methods (consuming `mut self -> Self`): `new(name, target, cardinality)` constructor, `.foreign_key()`, `.inverse()`, `.navigation()`, `.description()`
- Constructor sets `navigation` from `cardinality.default_navigation()` — override with `.navigation()`

**Cardinality enum:**
- Variants: OneToOne, OneToMany, ManyToOne, ManyToMany
- Derive: Debug, Clone, Copy, Serialize, Deserialize, PartialEq, Eq, JsonSchema
- `#[serde(rename_all = "snake_case")]`
- Method `default_navigation(&self) -> NavigationHint`: OneToOne→Inline, ManyToOne→Link, OneToMany→Nested, ManyToMany→Nested

**NavigationHint enum:**
- Variants: Inline, Link, Tab, Nested, Hidden
- Derive: Debug, Clone, Copy, Serialize, Deserialize, PartialEq, Eq, JsonSchema
- `#[serde(rename_all = "snake_case")]`

**ServiceDef integration (service.rs):**
- Add field: `pub relationships: Vec<RelationshipDef>` with `#[serde(default, skip_serializing_if = "Vec::is_empty")]`
- Initialize as `Vec::new()` in `ServiceDef::new()`
- Add builder method: `.relationship(rel: RelationshipDef) -> Self` — pushes to vec
- Add convenience shorthands (each creates RelationshipDef with appropriate Cardinality):
  - `.belongs_to(name, target) -> Self` — Cardinality::ManyToOne
  - `.has_many(name, target) -> Self` — Cardinality::OneToMany
  - `.has_one(name, target) -> Self` — Cardinality::OneToOne
  - `.belongs_to_many(name, target) -> Self` — Cardinality::ManyToMany

**lib.rs:** Add `mod relationship;` and `pub use relationship::{Cardinality, NavigationHint, RelationshipDef};`

Follow exact patterns from action.rs and field.rs for derives, serde attributes, and doc comments. Use `impl Into<String>` for string parameters in constructors/builders.
  </action>
  <verify>cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features</verify>
  <done>RelationshipDef, Cardinality, NavigationHint types compile. ServiceDef accepts relationships via builder. All existing 97 tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add validation rules and comprehensive test suite</name>
  <files>ferro-projections/src/service.rs, ferro-projections/src/relationship.rs</files>
  <action>
**Validation (service.rs — add to ServiceDef::validate()):**

Add two new validation checks after existing checks (steps 1-7):

8. Warn about duplicate relationship names within the service:
   - Collect relationship names, detect duplicates
   - Add Warning::DuplicateRelationship(String) variant to state.rs Warning enum
   - Push warning for each duplicate

9. Warn if ManyToMany relationship has foreign_key set (ManyToMany uses join tables, no single FK):
   - Add Warning::ManyToManyWithForeignKey { relationship: String } variant
   - Push warning if cardinality is ManyToMany and foreign_key is Some

**Tests in relationship.rs (cfg(test) mod tests):**

Type construction:
- `relationship_def_new_sets_defaults` — verify name, target, cardinality, default navigation from cardinality
- `relationship_def_builder_chain` — test all builder methods (foreign_key, inverse, navigation, description)
- `cardinality_default_navigation` — test all 4 cardinality→navigation mappings

Serde round-trip:
- `relationship_def_serde_round_trip` — full RelationshipDef with all fields set
- `relationship_def_json_omits_none_fields` — minimal RelationshipDef omits optional fields
- `cardinality_serde_values` — verify snake_case serialization (one_to_one, one_to_many, many_to_one, many_to_many)
- `navigation_hint_serde_values` — verify snake_case serialization (inline, link, tab, nested, hidden)

JSON Schema:
- `relationship_def_json_schema` — verify schema has expected properties

**Tests in service.rs (add to existing mod tests):**

Builder integration:
- `service_def_with_relationships_builder` — add via .relationship() method
- `service_def_belongs_to_convenience` — test .belongs_to() sets ManyToOne + Link navigation
- `service_def_has_many_convenience` — test .has_many() sets OneToMany + Nested navigation
- `service_def_has_one_convenience` — test .has_one() sets OneToOne + Inline navigation
- `service_def_belongs_to_many_convenience` — test .belongs_to_many() sets ManyToMany + Nested navigation
- `service_def_json_omits_empty_relationships` — empty relationships vec not in JSON
- `service_def_relationships_serde_round_trip` — service with relationships survives round-trip

Validation:
- `validate_warns_duplicate_relationship_names` — two relationships with same name produces warning
- `validate_warns_many_to_many_with_foreign_key` — ManyToMany + foreign_key produces warning
- `validate_passes_with_valid_relationships` — valid relationships produce no extra warnings

Full integration:
- `order_service_with_relationships_full_example` — order service with fields, actions, guards, state machine, AND relationships — validates clean, serde round-trip passes

JSON Schema:
- `service_def_json_schema_includes_relationships` — ServiceDef schema has 'relationships' property

Target: ~20 new tests bringing total to ~117+.
  </action>
  <verify>cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features</verify>
  <done>All new tests pass. Validation catches duplicate relationship names and ManyToMany+FK. Total test count ~117+. No warnings from clippy.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo fmt --all -- --check` passes
- [ ] `cargo clippy --all --all-targets -- -D warnings` passes
- [ ] `cargo test --all-features` passes (all ~117+ tests)
- [ ] RelationshipDef, Cardinality, NavigationHint are re-exported from lib.rs
- [ ] ServiceDef builder accepts relationships via .relationship(), .belongs_to(), .has_many(), .has_one(), .belongs_to_many()
- [ ] ServiceDef::validate() warns on duplicate relationship names and ManyToMany with FK
- [ ] JSON serialization omits empty relationships vec
- [ ] Serde round-trip works for all new types
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Relationship types follow crate conventions (derives, serde attributes, builder pattern)
- Convenience shorthands match research recommendations
- NavigationHint defaults from Cardinality (overridable)
- Validation integrated into ServiceDef::validate() entry point
  </success_criteria>

<output>
After completion, create `.planning/phases/87-service-relationships/87-01-SUMMARY.md`
</output>
