---
phase: 31-mcp-ui-tools
plan: 03
type: execute
wave: 2
depends_on: ["31-01", "31-02"]
files_modified:
  - ferro-mcp/src/service.rs
  - ferro-mcp/src/tools/generation_context.rs
  - ferro-mcp/src/tools/application_info.rs
autonomous: true
---

<objective>
Update MCP instructions and existing tools to be JSON-UI aware.

Purpose: Without instruction updates, agents won't discover JSON-UI tools. Without existing tool updates, standard discovery workflows (`application_info`, `generation_context`) won't mention JSON-UI capabilities. This plan bridges that gap.

Output: Updated FERRO_MCP_INSTRUCTIONS with JSON-UI tool category and workflows. Updated application_info and generation_context with JSON-UI awareness.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-mcp-ui-tools/31-RESEARCH.md

@ferro-mcp/src/service.rs
@ferro-mcp/src/tools/generation_context.rs
@ferro-mcp/src/tools/application_info.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update FERRO_MCP_INSTRUCTIONS with JSON-UI tools</name>
  <files>ferro-mcp/src/service.rs</files>
  <action>
Update the `FERRO_MCP_INSTRUCTIONS` const string in service.rs to include JSON-UI tools.

Add to the "About Ferro Framework" section:
- JSON-UI for server-rendered HTML views without frontend builds (views in src/views/)

Add a new workflow to "Tool Workflows":

```
### Building a JSON-UI View
1. `json_ui_catalog` - Browse available components and their props
2. `json_ui_generate` - Get generation context (models, routes, conventions)
3. `code_templates` (category: json_view) - Get boilerplate template
4. Implement the view using context and catalog
5. `json_ui_inspect` - Verify the view was created correctly

### Inspecting Existing JSON-UI Views
1. `json_ui_inspect` - List all views with metadata
2. `json_ui_catalog` (component: "ComponentName") - Look up specific component props
3. `get_handler` - See the handler that renders the view
```

Add "When to Use" sections for the new tools:

```
**USE json_ui_catalog** when:
- Building or modifying JSON-UI views
- Need to know component prop names and types
- Checking available variants for a component
- Understanding the component API before writing view code

**USE json_ui_inspect** when:
- Understanding existing JSON-UI views in a project
- Before adding new views (check what exists)
- Finding which components a view uses
- Checking which actions a view references

**USE json_ui_generate** when:
- Creating a new JSON-UI view from scratch
- Need model fields and routes for context
- Want conventions and a complete example
- Building views for specific models
```

Add a new tool category:

```
### JSON-UI (build and inspect views)
- json_ui_catalog: Component reference with props, variants, and builder API
- json_ui_inspect: List existing JSON-UI views with metadata
- json_ui_generate: Generation context (models, routes, catalog, example)
- code_templates (json_view): Boilerplate view templates
```

Update the "Code Generation" category to mention json_ui tools:
```
### Code Generation (write new code correctly)
- generation_context: Naming conventions, file structure, patterns, anti-patterns
- code_templates: Ready-to-use templates for handlers, models, migrations, middleware, json_view
- json_ui_catalog: Component reference for JSON-UI view development
- json_ui_generate: Context assembly for creating JSON-UI views
```
  </action>
  <verify>cargo build -p ferro-mcp succeeds (string compiles correctly)</verify>
  <done>FERRO_MCP_INSTRUCTIONS includes JSON-UI tool category, workflows, when-to-use guidance for all 3 new tools, and updated code generation category</done>
</task>

<task type="auto">
  <name>Task 2: Update generation_context and application_info for JSON-UI awareness</name>
  <files>ferro-mcp/src/tools/generation_context.rs, ferro-mcp/src/tools/application_info.rs</files>
  <action>
**generation_context.rs:**

Add JSON-UI specific entries:

1. In `NamingConventions`: add a `views` field (String) — value: `"snake_case singular function (user_list, user_form, dashboard)"`
2. In `FileStructure`: add a `views` field (String) — value: `"src/views/{name}.rs"`
3. In `CommonPatterns`: add a `json_ui_view` field (String) — value: a short pattern showing `pub fn view() -> JsonUiView { JsonUiView::new().title("...").layout("app").component(...) }`
4. In `ImportTemplates`: add a `json_ui_view` field (String) — value: `"use ferro::{Component, ComponentNode, JsonUiView, /* component-specific types */};"`
5. In `avoid` list: add `"Don't import unused JSON-UI component types - only import what the view actually uses"` and `"Don't forget .layout(\"app\") on JSON-UI views - views without layout render as raw HTML"`

Update all tests in the module to account for the new fields. The existing tests assert `!context.naming_conventions.models.is_empty()` etc — add assertions for the new view-related fields.

**application_info.rs:**

Read the application_info.rs source to understand its output structure. If it has a section listing capabilities or features, add JSON-UI views to it. Specifically:
- If there's a list of installed crates or features, check if ferro-json-ui is detected and mention it
- If there's a file/directory scan section, add `src/views/` scanning to report how many JSON-UI views exist

The goal is: when an agent calls `application_info`, it should see that the project has JSON-UI capabilities and how many views exist, so it knows to use the JSON-UI tools.

Read the file first before making changes — adapt to whatever structure exists.
  </action>
  <verify>cargo test -p ferro-mcp passes all tests (including updated generation_context tests)</verify>
  <done>generation_context includes JSON-UI naming, file structure, patterns, imports, and anti-patterns. application_info reports JSON-UI view count when src/views/ exists.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p ferro-mcp` passes all tests
- [ ] `cargo build -p ferro-mcp` succeeds without warnings
- [ ] `cargo clippy -p ferro-mcp` passes
- [ ] FERRO_MCP_INSTRUCTIONS includes JSON-UI tools documentation
- [ ] generation_context returns JSON-UI conventions and patterns
- [ ] application_info reports JSON-UI view count
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- MCP instructions document all 3 new tools with workflows and when-to-use
- generation_context provides complete JSON-UI development guidance
- application_info discovers JSON-UI views in projects
</success_criteria>

<output>
After completion, create `.planning/phases/31-mcp-ui-tools/31-03-SUMMARY.md`
</output>
