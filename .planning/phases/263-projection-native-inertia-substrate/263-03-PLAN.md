---
phase: 263-projection-native-inertia-substrate
plan: 03
type: execute
wave: 2
depends_on: [263-02]
files_modified:
  - framework/src/projection_read.rs
  - framework/src/lib.rs
  - ferro-mcp-server/src/dispatch.rs
  - ferro-mcp-server/src/schema.rs
  - ferro-mcp-server/src/lib.rs
autonomous: true
requirements: [SUBST-03]
tags: [framework, data-query, tenant-scoping, mcp, relocation]

must_haves:
  truths:
    - "The tenant-scoped data query (dispatch) lives in framework::projection_read"
    - "framework::projection_read::dispatch is tenant-scoped — cross-tenant rows are excluded"
    - "framework::projection_read enforces MAX_LIMIT (100) and offset pagination"
    - "ferro-mcp-server::dispatch is a thin wrapper delegating to framework, mapping the error type"
    - "MCP data-query behavior (rows/total/limit/offset + tenant scoping) is unchanged after relocation"
  artifacts:
    - path: "framework/src/projection_read.rs"
      provides: "dispatch fn + DispatchResult + ProjectionReadError + moved filter helpers"
      contains: "pub async fn dispatch"
      min_lines: 120
  key_links:
    - from: "ferro-mcp-server/src/dispatch.rs"
      to: "framework::projection_read::dispatch"
      via: "delegation wrapper with error mapping"
      pattern: "framework::projection_read::dispatch"
    - from: "framework/src/lib.rs"
      to: "projection_read module"
      via: "feature-gated pub use"
      pattern: "pub use projection_read::"
---

<objective>
Relocate the tenant-scoped data query — `dispatch` in `ferro-mcp-server/src/dispatch.rs` plus its
pure filter helpers (`is_filter_field`, `is_range_filter_field` from `schema.rs`, and the
in-file helpers) — into a new `framework::projection_read` module. Introduce a
`framework`-local `ProjectionReadError`. `ferro-mcp-server::dispatch` becomes a thin delegation
wrapper that maps `ProjectionReadError` → `ferro-mcp-server`'s `crate::Error`.

Purpose: The data query MUST live above both callers so the later Inertia wave can reach it
WITHOUT `ferro-inertia`/framework depending on `ferro-mcp-server` (the acyclicity constraint).
This is the enabling relocation for SUBST-03's tenant-scoped data. The query becomes the shared
read core; MCP re-uses it; the Inertia delivery helper (Plan 04) calls it.

Output: `framework/src/projection_read.rs`, re-exports in `framework/src/lib.rs`, a thin wrapper
in `ferro-mcp-server/src/dispatch.rs`, and a re-export in `ferro-mcp-server/src/lib.rs` for
back-compat.

CRITICAL — Wave 2. Depends on Plan 02 (263-02) because both edit `ferro-mcp-server/src/*` and
`framework/src/lib.rs`; sequencing prevents a file-ownership conflict. Also relocate BEFORE the
Plan 04 Inertia wave (cycle avoidance).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/263-projection-native-inertia-substrate/263-CONTEXT.md
@.planning/phases/263-projection-native-inertia-substrate/263-RESEARCH.md
@.planning/phases/263-projection-native-inertia-substrate/263-PATTERNS.md

<interfaces>
<!-- VERIFIED against source this session. -->

Signature to preserve — ferro-mcp-server/src/dispatch.rs:117 :
```rust
pub async fn dispatch(
    service: &ServiceDef,
    mut filters: serde_json::Value,
    limit: u64,
    offset: u64,
    db: &sea_orm::DatabaseConnection,
    tenant_id: Option<i64>,
) -> crate::Result<DispatchResult>
```

DispatchResult — dispatch.rs:19 :
```rust
#[derive(Debug, Serialize)]
pub struct DispatchResult {
    pub rows: Vec<serde_json::Value>,
    pub total: u64,
    pub limit: u64,
    pub offset: u64,
}
```

Facts (VERIFIED):
- `dispatch.rs` returns `crate::Result` and constructs `crate::Error::InvalidFilter` / `crate::Error::Database` — these are `ferro-mcp-server`-local and must become `framework::ProjectionReadError`.
- `dispatch.rs` imports `crate::schema::{is_filter_field, is_range_filter_field}` — these are PURE `FieldDef` functions (schema.rs:14, 52), no MCP types; they relocate together.
- In-file pure helpers to move verbatim: `MAX_LIMIT` (=100), `MAX_OFFSET`, `placeholder()`, `json_to_sea_value()`, `split_op_key()`, `rows_to_json()`.
- `framework` already has `sea-orm` under the `projections` feature — no new dep.
- A2 (RESEARCH) is CONFIRMED: `dispatch` uses NO rmcp types internally, only its error type + `crate::schema` helpers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create framework::projection_read (dispatch + helpers + ProjectionReadError)</name>
  <files>framework/src/projection_read.rs, framework/src/lib.rs</files>
  <read_first>
    - ferro-mcp-server/src/dispatch.rs (ENTIRE file — this is the code being relocated, ANALOG = exact relocation per 263-PATTERNS.md)
    - ferro-mcp-server/src/schema.rs lines 1-96 (`is_filter_field`, `is_range_filter_field` — pure FieldDef fns that move with dispatch)
    - framework/src/lib.rs lines 55-60 (the `projections` feature-gate pattern)
    - framework/Cargo.toml (confirm sea-orm present under `projections`)
    - 263-PATTERNS.md "framework/src/projection_read.rs" section (error-type + delegation guidance)
  </read_first>
  <action>
    Create `framework/src/projection_read.rs`. Move `dispatch` and ALL its pure helpers verbatim
    (`MAX_LIMIT`, `MAX_OFFSET`, `placeholder`, `json_to_sea_value`, `split_op_key`, `rows_to_json`),
    plus `is_filter_field` and `is_range_filter_field` (from `ferro-mcp-server/src/schema.rs`).
    Keep the `dispatch` signature identical EXCEPT the return type.

    Define a framework-local error type in this module:
    ```rust
    #[derive(Debug, thiserror::Error)]
    pub enum ProjectionReadError {
        #[error("invalid filter: {0}")]
        InvalidFilter(String),
        #[error("database error: {0}")]
        Database(String),
    }

    pub type ProjectionReadResult<T> = Result<T, ProjectionReadError>;
    ```
    Rewrite `dispatch` to return `ProjectionReadResult<DispatchResult>` and replace every
    `crate::Error::InvalidFilter(..)` → `ProjectionReadError::InvalidFilter(..)` and
    `crate::Error::Database(..)` → `ProjectionReadError::Database(..)`. Bring `DispatchResult`
    into this module (with `#[derive(Debug, Serialize)]`).

    Preserve exact semantics: `MAX_LIMIT = 100` hard cap regardless of caller; offset pagination;
    soft-delete filtering when `service.soft_delete_column.is_some()`; tenant predicate injected
    as a bound parameter when `service.tenant_column.is_some()`.

    Gate the module in `framework/src/lib.rs` inside the `projections` feature block:
    ```rust
    #[cfg(feature = "projections")]
    pub mod projection_read;
    #[cfg(feature = "projections")]
    pub use projection_read::{DispatchResult, ProjectionReadError, ProjectionReadResult};
    ```
    Move the inline `#[cfg(test)]` tenant-scoping tests from dispatch.rs into this module
    (they use `sqlite::memory:` + raw CREATE TABLE and do not depend on MCP types) so the
    tenant-scoping coverage travels with the code.
  </action>
  <verify>
    <automated>cargo test -p framework --features projections projection_read</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "pub async fn dispatch" framework/src/projection_read.rs`
    - `grep -q "pub enum ProjectionReadError" framework/src/projection_read.rs`
    - `grep -q "pub use projection_read::" framework/src/lib.rs`
    - `! grep -q "crate::Error::InvalidFilter\|crate::Error::Database" framework/src/projection_read.rs` (no ferro-mcp-server error refs leaked in)
    - `grep -q "MAX_LIMIT" framework/src/projection_read.rs` (cap preserved)
    - `cargo test -p framework --features projections projection_read` exits 0 (incl. moved tenant-scoping tests)
  </acceptance_criteria>
  <done>framework::projection_read holds dispatch + all helpers + ProjectionReadError; tenant-scoping tests pass; module feature-gated on `projections` and re-exported.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce ferro-mcp-server::dispatch to a delegating wrapper; keep schema.rs helpers re-exported</name>
  <files>ferro-mcp-server/src/dispatch.rs, ferro-mcp-server/src/schema.rs, ferro-mcp-server/src/lib.rs</files>
  <read_first>
    - ferro-mcp-server/src/dispatch.rs (now to be gutted to a wrapper)
    - ferro-mcp-server/src/schema.rs (remove the two moved fns OR re-export from framework; keep callers compiling)
    - ferro-mcp-server/src/lib.rs (re-export surface — add `pub use framework::projection_read::DispatchResult;` for back-compat)
    - callers of `dispatch` / `is_filter_field` / `is_range_filter_field` inside ferro-mcp-server (grep first — they must keep compiling against the wrapper/re-export)
    - 263-PATTERNS.md thin-delegation excerpt (error mapping via `match`)
  </read_first>
  <action>
    Rewrite `ferro-mcp-server/src/dispatch.rs` as a thin delegation preserving its public
    `crate::Result<DispatchResult>` signature so all MCP callers are untouched:
    ```rust
    pub use framework::projection_read::{DispatchResult, ProjectionReadError};

    pub async fn dispatch(
        service: &ServiceDef,
        filters: serde_json::Value,
        limit: u64,
        offset: u64,
        db: &sea_orm::DatabaseConnection,
        tenant_id: Option<i64>,
    ) -> crate::Result<DispatchResult> {
        framework::projection_read::dispatch(service, filters, limit, offset, db, tenant_id)
            .await
            .map_err(|e| match e {
                ProjectionReadError::InvalidFilter(m) => crate::Error::InvalidFilter(m),
                ProjectionReadError::Database(m) => crate::Error::Database(m),
            })
    }
    ```
    For `schema.rs`: the two relocated pure fns (`is_filter_field`, `is_range_filter_field`) now
    live in `framework::projection_read`. Either (a) delete them from `schema.rs` and update its
    in-crate callers to `framework::projection_read::is_filter_field`, or (b) re-export them from
    `schema.rs` (`pub use framework::projection_read::{is_filter_field, is_range_filter_field};`).
    Pick whichever keeps the MCP surface unchanged with the smallest diff; prefer re-export if
    there are multiple callers. In `ferro-mcp-server/src/lib.rs`, add
    `pub use framework::projection_read::DispatchResult;` if `DispatchResult` was previously part
    of the crate's public surface, to avoid a breaking re-export change.

    Run the FULL `ferro-mcp-server` test suite to confirm the relocation is behavior-neutral.
  </action>
  <verify>
    <automated>cargo test -p ferro-mcp-server</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "framework::projection_read::dispatch" ferro-mcp-server/src/dispatch.rs` (delegation in place)
    - `! grep -qE "fn split_op_key|fn placeholder|fn rows_to_json|fn json_to_sea_value" ferro-mcp-server/src/dispatch.rs` (relocated helpers no longer DEFINED here — `split_op_key`, `placeholder`, `rows_to_json`, `json_to_sea_value` are all real functions in the pre-relocation dispatch.rs and MUST have moved to framework::projection_read; a match means the move is incomplete)
    - `cargo tree -p ferro-mcp-server -e normal | grep -q "framework"` (still depends on framework — no new cycle)
    - `cargo test -p ferro-mcp-server` exits 0 (data-query + tenant-isolation behavior unchanged)
  </acceptance_criteria>
  <done>ferro-mcp-server::dispatch delegates to framework::projection_read with error mapping; schema helper callers compile; the full ferro-mcp-server suite is green, proving behavior-neutral relocation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller filters/limit → SQL query | `filters: serde_json::Value` and `limit`/`offset` are untrusted request input; the query builds a parameterized SQL statement. |
| tenant_id → row visibility | `tenant_id: Option<i64>` scopes every read; cross-tenant rows must be excluded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-263-06 | Information disclosure (cross-tenant) | `dispatch` returns rows outside the caller's tenant | mitigate | Tenant predicate injected as a BOUND parameter when `service.tenant_column.is_some()`; relocation preserves this verbatim. The moved tenant-scoping tests (`tenant_scoping`, `tenant_isolation`, `tenant_fail_closed`) assert cross-tenant exclusion and must stay green. Cross-tenant ids are "not found". |
| T-263-07 | Tampering (SQL injection) | Malicious `filters` keys/values reach raw SQL | mitigate | Filter values pass through `json_to_sea_value` / `placeholder` bound parameters; filter KEYS are validated against `is_filter_field`/`is_range_filter_field` allowlists (relocated unchanged). Invalid filters return `InvalidFilter`, not a raw query. |
| T-263-08 | Denial of service | Caller requests an unbounded page | mitigate | `MAX_LIMIT = 100` hard cap enforced inside `dispatch` regardless of caller; relocation preserves the cap (grep-asserted). `MAX_OFFSET` bound preserved. |
| T-263-09 | Regression (silent behavior change) | Error-type swap or helper move changes query semantics | mitigate | `ProjectionReadError` maps 1:1 back to `crate::Error` in the wrapper; full `cargo test -p ferro-mcp-server` pins unchanged behavior. |

No authorization decision is made here; this is a scoped READ. Guard enforcement remains at write time.
</threat_model>

<verification>
- `cargo test -p framework --features projections projection_read` green (incl. moved tenant-scoping tests).
- `cargo test -p ferro-mcp-server` green (delegation is behavior-neutral).
- `cargo tree -p ferro-mcp-server | grep framework` present; no cycle introduced.
- Pre-commit CI-exact gate: `cargo fmt --all -- --check` && `cargo clippy --all --all-targets -- -D warnings` && `cargo test --all-features`.
</verification>

<success_criteria>
- The tenant-scoped data query lives in `framework::projection_read`, above both callers (SUBST-03 data core).
- Tenant scoping, offset pagination (MAX_LIMIT=100), and soft-delete filtering are preserved.
- `ferro-mcp-server::dispatch` is a thin wrapper; the MCP suite proves behavior is unchanged.
- The query is now reachable by the Inertia wave without any `ferro-inertia → ferro-mcp-server` edge.
</success_criteria>

<output>
After completion, create `.planning/phases/263-projection-native-inertia-substrate/263-03-SUMMARY.md`
documenting the relocated helper list, the `ProjectionReadError` variants, the schema.rs
decision (delete-and-repoint vs re-export), and the `cargo tree` evidence of no new cycle.
</output>
</content>
</invoke>
