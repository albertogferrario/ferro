---
phase: 263-projection-native-inertia-substrate
plan: 05
type: execute
wave: 4
depends_on: [263-01, 263-02, 263-03, 263-04]
files_modified:
  - app/src/tests/permitted_actions_parity.rs
  - app/src/tests/data_tenant_scoping.rs
  - app/src/tests/single_source.rs
  - app/src/tests/mod.rs
autonomous: true
requirements: [SUBST-05]
tags: [tests, parity, single-source, tenant-scoping, verification]

must_haves:
  truths:
    - "permitted_actions parity: framework::permitted_actions set == guard-filtered MCP tools/list set for the same service + evaluated_guards"
    - "State/guard change flips both the Inertia permitted_actions set and the MCP tools/list set identically"
    - "Data tenant-scoping: framework::projection_read::dispatch excludes cross-tenant rows (cross-tenant ids not found)"
    - "Write parity: an Inertia-style POST /{service}/{action} reaches the same dispatch_write kernel MCP reaches, differing only by the audit channel tag (web. vs mcp.)"
    - "The full CI-exact gate is green before verify"
  artifacts:
    - path: "app/src/tests/permitted_actions_parity.rs"
      provides: "SUBST-02/05 parity test across framework::permitted_actions and MCP render_exposed_tools"
      contains: "permitted_actions"
    - path: "app/src/tests/data_tenant_scoping.rs"
      provides: "SUBST-03 tenant-isolation test via framework::projection_read::dispatch"
      contains: "dispatch"
  key_links:
    - from: "app/src/tests/permitted_actions_parity.rs"
      to: "framework::permitted_actions + ferro_mcp_server::render_exposed_tools"
      via: "set equality assertion"
      pattern: "render_exposed_tools|permitted_actions"
    - from: "app/src/tests/data_tenant_scoping.rs"
      to: "framework::projection_read::dispatch"
      via: "tenant-scoped query assertion"
      pattern: "projection_read::dispatch|dispatch\\("
---

<objective>
Prove single-source parity across the projection surfaces (SUBST-05):
(a) **permitted-actions parity** — `framework::permitted_actions(service, &guards)` returns the
same action-name set as the guard-filtered MCP `tools/list` (`render_exposed_tools`) for the same
service + `evaluated_guards`, and flipping a guard flips BOTH surfaces identically;
(b) **data tenant-scoping** — `framework::projection_read::dispatch` excludes cross-tenant rows;
(c) **write parity** — an Inertia-style `POST /{service}/{action}` reaches the same
`dispatch_write(channel="web")` kernel MCP reaches, differing only by the audit channel tag;
(d) the schema snapshot (SUBST-01) is already covered by Plan 01's test.

Purpose: SUBST-05. These tests are the executable proof that the Inertia substrate, the MCP
surface, and the write kernel are three projections of one declaration — not hand-maintained
against each other.

Output: `app/src/tests/permitted_actions_parity.rs` (Wave-0 gap), `app/src/tests/data_tenant_scoping.rs`
(Wave-0 gap — extend `crud_e2e.rs` if it already covers this), and an extension to
`app/src/tests/single_source.rs` for the Inertia write-parity assertion.

CRITICAL — Wave 4. Depends on all prior plans (the derivations + delivery helper must exist).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/263-projection-native-inertia-substrate/263-CONTEXT.md
@.planning/phases/263-projection-native-inertia-substrate/263-RESEARCH.md
@.planning/phases/263-projection-native-inertia-substrate/263-PATTERNS.md
@.planning/phases/263-projection-native-inertia-substrate/263-VALIDATION.md

<interfaces>
<!-- VERIFIED this session — the exact test-surface signatures. -->

ferro-mcp-server/src/renderer.rs:80 (the MCP tools/list surface for parity):
```rust
pub fn render_exposed_tools(
    services: &[ServiceDef],
    ctx: &McpContext,
) -> std::result::Result<Vec<Tool>, ProjError>
// Emits one read tool `list_<service>` + one write tool per ActionDef (guard-filtered).
// Filter action tools by `!t.name.starts_with("list_")` to compare action names.
```

McpContext (renderer.rs:23) — construct with evaluated_guards, tenant_id:
```rust
pub struct McpContext { pub tenant_id: Option<i64>, pub evaluated_guards: HashMap<String,bool>,
                        pub scope: Option<String>, pub write_authorized: Option<bool> }
```

framework::permitted_actions (Plan 02) and framework::projection_read::dispatch (Plan 03) are the
Inertia-side surfaces. `ferro::permitted_actions` is the facade re-export.

app/src/tests/single_source.rs (ANALOG for write-parity + fixtures — VERIFIED structure):
```rust
// drives ONE `submit` through dispatch_write(.., "mcp") and dispatch_write(.., "web"),
// asserts identical persisted to_state; audit channel is the only divergence.
// Helpers: setup_db(), seed(), order_service(), action_def(), make_test_write_dispatcher().
```

app/src/tests/crud_e2e.rs (VERIFIED) already has `seed_two_tenants` (orders 1-2 tenant 1,
3-4 tenant 2) + `crud_cross_tenant_non_disclosure` — REUSE these fixtures for tenant-scoping.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: permitted-actions parity test (Inertia set == MCP tools/list set; state-change flips both)</name>
  <files>app/src/tests/permitted_actions_parity.rs, app/src/tests/mod.rs</files>
  <read_first>
    - app/src/tests/single_source.rs (ANALOG from 263-PATTERNS.md: fixture + module structure to mirror)
    - app/src/tests/mod.rs (register the new test module)
    - ferro-mcp-server/src/renderer.rs lines 80-95 (`render_exposed_tools`) and the McpContext ctor
    - framework/src/permitted_actions.rs (Plan 02 — the Inertia-side surface + `ferro::permitted_actions` facade)
    - 263-PATTERNS.md "app/src/tests/permitted_actions_parity.rs" section (the two full test bodies)
  </read_first>
  <action>
    Create `app/src/tests/permitted_actions_parity.rs` mirroring `single_source.rs` module layout
    (`#[cfg(all(test, not(feature = "confirmation")))] mod tests { ... }` if that gate matches the
    sibling; otherwise use the plain `#[cfg(test)]` gate the app tests use). Register it in
    `app/src/tests/mod.rs`.

    Build a `ServiceDef` with `mcp_exposed(true)`, one guard `is_manager`, one guarded action
    `approve.precondition("is_manager")`, and one unguarded action `submit`. Implement the two
    tests from 263-PATTERNS.md:

    1. `permitted_actions_matches_mcp_tools_list`: with `evaluated_guards = {"is_manager": false}`,
       assert `ferro::permitted_actions(&service, &guards)` excludes `"approve"` and includes
       `"submit"`; then `render_exposed_tools(&[service], &ctx)` (same guards in `McpContext`),
       filter tool names by `!t.name.starts_with("list_")`, assert `"approve"` is ABSENT and
       `"submit"` is PRESENT. The two action-name sets MUST be equal.

    2. `state_change_updates_both_surfaces_identically`: run once with `{"is_manager": false}`
       (approve hidden on BOTH surfaces) and once with `HashMap::new()` (approve visible on BOTH
       surfaces); assert both surfaces flip identically.

    Compare as SETS (sort or use a HashSet) — order differences between the two surfaces must not
    fail the test; only membership matters. Use the real builder method names from ferro-projections.
  </action>
  <verify>
    <automated>cargo test -p app permitted_actions_parity</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/src/tests/permitted_actions_parity.rs`
    - `grep -q "render_exposed_tools" app/src/tests/permitted_actions_parity.rs`
    - `grep -q "permitted_actions" app/src/tests/permitted_actions_parity.rs`
    - `grep -q "permitted_actions_parity" app/src/tests/mod.rs` (module registered)
    - `cargo test -p app permitted_actions_parity` exits 0 (both parity tests pass)
  </acceptance_criteria>
  <done>Both parity tests pass: the Inertia permitted_actions set equals the guard-filtered MCP tools/list action set, and flipping a guard flips both surfaces identically.</done>
</task>

<task type="auto">
  <name>Task 2: data tenant-scoping test (dispatch excludes cross-tenant rows)</name>
  <files>app/src/tests/data_tenant_scoping.rs, app/src/tests/mod.rs</files>
  <read_first>
    - app/src/tests/crud_e2e.rs lines 34-180 (`seed_two_tenants`, `crud_cross_tenant_non_disclosure` — CHECK whether a `dispatch`-level tenant-scoping test already exists here; if it does, extend rather than duplicate)
    - ferro-mcp-server/src/dispatch.rs former inline tests (`tenant_scoping`, `tenant_isolation`, `tenant_fail_closed`) — now relocated to framework::projection_read (Plan 03); this app-level test asserts the SAME behavior through the framework path
    - framework/src/projection_read.rs (Plan 03 — the `dispatch` entry point + `DispatchResult`)
    - 263-PATTERNS.md "app/src/tests/data_tenant_scoping.rs" section
    - app/src/tests/mod.rs
  </read_first>
  <action>
    First determine if `crud_e2e.rs` already asserts `dispatch`-level tenant scoping (a call to the
    projection read returning only the caller's tenant rows). If YES, extend that file with a
    focused `dispatch`-path assertion and skip creating a new file (update files_modified in the
    SUMMARY accordingly). If NO, create `app/src/tests/data_tenant_scoping.rs` and register it in
    `app/src/tests/mod.rs`.

    Using the app `Migrator` for DB setup (mirror `single_source.rs`/`crud_e2e.rs`), seed two
    tenants' rows, then call `framework::projection_read::dispatch(&service, json!({}), 10, 0, &db,
    Some(1))` and assert:
    - `result.rows.len()` equals only tenant 1's row count;
    - every returned row's tenant column == 1;
    - a query with `tenant_id = Some(2)` returns only tenant 2's rows;
    - filtering by an id that belongs to tenant 2 while scoped to `tenant_id = Some(1)` returns
      ZERO rows (cross-tenant id is "not found").
    Use `ServiceDef::new(...).tenant_column("tenant_id")` so the tenant predicate is active.
  </action>
  <verify>
    <automated>cargo test -p app data_tenant_scoping</automated>
  </verify>
  <acceptance_criteria>
    - test target exists: `grep -rq "data_tenant_scoping\|dispatch_tenant_scoping" app/src/tests/` (new file OR extended crud_e2e)
    - `grep -rq "projection_read::dispatch\|framework::projection_read" app/src/tests/` (uses the framework path, not ferro-mcp-server)
    - the cross-tenant-id assertion is present: `grep -rq "not found\|len(), 0\|is_empty()" app/src/tests/data_tenant_scoping.rs app/src/tests/crud_e2e.rs`
    - `cargo test -p app data_tenant_scoping` exits 0
  </acceptance_criteria>
  <done>The tenant-scoping test proves framework::projection_read::dispatch returns only the caller's tenant rows and treats a cross-tenant id as not found.</done>
</task>

<task type="auto">
  <name>Task 3: Inertia write-parity extension + full-gate green</name>
  <files>app/src/tests/single_source.rs</files>
  <read_first>
    - app/src/tests/single_source.rs (ANALOG: extend the existing both-channels test with an Inertia-web-channel assertion)
    - app/src/controllers/visual_action.rs (`handle` → `dispatch_write(.., "web")` — the channel the Inertia frontend reuses)
    - framework/src/write/mod.rs (audit prefix is `format!("{channel}.action.{name}")` — web. vs mcp.)
  </read_first>
  <action>
    Extend `app/src/tests/single_source.rs` (do NOT create a competing test file) with an
    assertion that the Inertia customization frontend's write path is byte-identical to the visual
    channel: drive the same declared action through `dispatch_write(.., "web")` (the channel Inertia
    forms reuse via `POST /{service}/{action}`) and assert the persisted `to_state` matches the MCP
    channel's result, with the audit channel (`web.action.submit` vs `mcp.action.submit`) as the
    ONLY divergence. Name the added test/assert e.g. `single_source_inertia_reuses_web_channel`.
    Since the Inertia frontend uses the EXISTING web route (no new path — Plan 04 Task 2), this is
    the same `dispatch_write(.., "web")` call the visual surface already exercises; the test makes
    the reuse explicit for SUBST-05(b).

    Then run the FULL CI-exact gate and record the result in the SUMMARY:
    `cargo fmt --all -- --check` && `cargo clippy --all --all-targets -- -D warnings` &&
    `cargo test --all-features`.
  </action>
  <verify>
    <automated>cargo test -p app single_source</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "single_source_inertia_reuses_web_channel\|web.action" app/src/tests/single_source.rs`
    - `cargo test -p app single_source` exits 0
    - full gate green: `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` all exit 0 (record outputs in SUMMARY)
  </acceptance_criteria>
  <done>Write parity is asserted through the shared web channel; the full CI-exact gate (fmt + clippy --all-targets -D warnings + test --all-features) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture guards → both surfaces | The parity test drives `evaluated_guards` through both `permitted_actions` and `render_exposed_tools` to prove one source of truth. |
| test tenant_id → data query | The tenant-scoping test asserts cross-tenant rows are excluded — this is the security regression pin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-263-14 | Elevation of privilege (drift) | The Inertia surface and MCP surface silently diverge on which actions are shown | mitigate | The parity test asserts SET EQUALITY between `permitted_actions` and the guard-filtered `render_exposed_tools`, and that a guard flip changes both identically — divergence fails the build. |
| T-263-15 | Information disclosure (cross-tenant) | A regression reintroduces cross-tenant row leakage | mitigate | The tenant-scoping test asserts only the caller's tenant rows return and a cross-tenant id is "not found"; it runs in `cargo test --all-features`. |
| T-263-16 | Elevation of privilege (write bypass) | The Inertia write path silently diverges from the guarded kernel | mitigate | The write-parity assertion drives the same declared action through `dispatch_write(.., "web")` and asserts the identical persisted state as the MCP channel — the only divergence is the audit tag. No alternate write path can pass this test. |

These tests are the standing regression guard for the phase's security properties (tenant isolation, single guard-visibility source, single write kernel).
</threat_model>

<verification>
- `cargo test -p app permitted_actions_parity` green (set-equal parity + state-flip parity).
- `cargo test -p app data_tenant_scoping` green (cross-tenant exclusion + cross-tenant id not found).
- `cargo test -p app single_source` green (Inertia web-channel write parity).
- Full CI-exact gate green: `cargo fmt --all -- --check` && `cargo clippy --all --all-targets -- -D warnings` && `cargo test --all-features`.
</verification>

<success_criteria>
- SUBST-05 satisfied: (a) permitted-actions parity across Inertia and MCP with identical state-change behavior; (b) write parity through the shared `dispatch_write(channel="web")` kernel differing only by audit tag; (c) schema snapshot (Plan 01); (d) data tenant-scoping proven.
- All parity/scoping tests run under `cargo test --all-features`.
- The full CI-exact gate is green before `/gsd-verify-work`.
</success_criteria>

<output>
After completion, create `.planning/phases/263-projection-native-inertia-substrate/263-05-SUMMARY.md`
documenting each parity/scoping test's assertion, whether `data_tenant_scoping` was a new file or a
`crud_e2e.rs` extension, and the full CI-exact gate output (fmt/clippy/test all green).
</output>
