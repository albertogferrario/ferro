---
status: partial
phase: 188-ferro-storage-cdn-extension
source: [188-01-SUMMARY.md, 188-02-SUMMARY.md, 188-03-SUMMARY.md]
started: 2026-06-08
updated: 2026-06-08
---

## Current Test

[testing complete]

## Tests

### 1. CDN URL generation + origin fallback
expected: With AWS_CDN_URL set, cdn_url("assets/app.css") returns "{AWS_CDN_URL}/assets/app.css" (no double slash); with it unset, returns the origin url(). Storage::cdn_url delegates to the default disk.
result: pass
evidence: Live `cargo run --example cdn_url_demo` output — A=cdn.example.com/assets/app.css, B=origin.example.com/assets/app.css (fallback), C=cdn.example.com/index.html (double-slash collapsed), D=facade delegation matches. Mirrors 4 green automated tests.

### 2. DigitalOcean Spaces purge — LIVE integration
expected: With DO_SPACES_CDN_ID + DIGITALOCEAN_ACCESS_TOKEN set, DoSpacesCdn::new(DoSpacesCdnConfig::from_env()).purge(&["index.html".into()]).await returns Ok(()) and the object is purged from the DO CDN edge (confirm via DO dashboard or a re-fetch showing fresh content). With DO_SPACES_CDN_ID unset, purge() is a silent no-op (returns Ok, makes no HTTP call). NOTE: requires real DO credentials + a CDN endpoint — skip/block if unavailable; in-crate behavior is already covered by the wiremock test suite.
result: blocked
blocked_by: third-party
reason: "User deferred — needs live DO Spaces CDN endpoint + DO_SPACES_CDN_ID/DIGITALOCEAN_ACCESS_TOKEN credentials. In-crate behavior covered by wiremock suite (do_adapter_* tests green)."

### 3. Feature-gated Bunny/Cloudflare adapters
expected: `cargo build -p ferro-storage --features cdn-bunny,cdn-cloudflare` compiles the BunnyCdn and CloudflareCdn adapters; the default `cargo build -p ferro-storage` does not pull them into the dependency graph (cargo tree is identical with/without the features).
result: pass
evidence: `cargo build -p ferro-storage --features cdn-bunny,cdn-cloudflare` succeeds; `cargo tree -p ferro-storage` = 343 lines with AND without the features (identical) → zero default-graph impact.

### 4. CDN documentation
expected: docs/src/features/storage.md contains a CDN section documenting cdn_url(), the PurgeApi trait, the DigitalOcean/Bunny/Cloudflare adapters, and the promote→purge two-call sequence (with the B-02 "purge only non-hashed HTML" policy note).
result: pass
evidence: docs/src/features/storage.md has a `## CDN` section (line 377) — CDN Edge URLs, AWS_CDN_URL, cdn_url() examples, PurgeApi trait, DigitalOcean Spaces CDN Adapter (+ Bunny/Cloudflare); 33 CDN-keyword hits.

## Summary

total: 4
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 1

## Gaps

[none yet]
