---
phase: 96-stripe-integration
plan: 06
type: execute
wave: 5
depends_on: ["96-01", "96-02", "96-03", "96-04", "96-05"]
files_modified:
  - .github/workflows/publish.yml
  - docs/src/features/stripe.md
  - docs/src/SUMMARY.md
  - framework/src/lib.rs
autonomous: true
requirements: [STRIPE-12, STRIPE-13]

must_haves:
  truths:
    - "ferro-stripe is in the publish workflow Wave 1"
    - "Documentation covers subscription lifecycle, Connect setup, webhook configuration, and CLI scaffolding"
    - "Full workspace builds with all features enabled"
    - "All tests pass across the workspace"
  artifacts:
    - path: ".github/workflows/publish.yml"
      provides: "ferro-stripe in publish workflow"
      contains: "ferro-stripe"
    - path: "docs/src/features/stripe.md"
      provides: "Stripe integration documentation"
      contains: "Stripe"
  key_links:
    - from: ".github/workflows/publish.yml"
      to: "ferro-stripe/Cargo.toml"
      via: "crate name in Wave 1"
      pattern: "ferro-stripe"
    - from: "docs/src/features/stripe.md"
      to: "ferro-stripe public API"
      via: "code examples referencing ferro:: types"
      pattern: "ferro::Stripe"
---

<objective>
Complete framework integration with publish workflow, documentation, and full-workspace validation.

Purpose: Final integration step ensuring ferro-stripe is published correctly, documented for developers, and the entire workspace compiles and tests pass with the stripe feature.
Output: Updated publish workflow, comprehensive documentation, validated workspace build.
</objective>

<execution_context>
@/Users/alberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-stripe-integration/96-CONTEXT.md
@.planning/phases/96-stripe-integration/96-RESEARCH.md
@.planning/phases/96-stripe-integration/96-01-SUMMARY.md
@.planning/phases/96-stripe-integration/96-02-SUMMARY.md
@.planning/phases/96-stripe-integration/96-03-SUMMARY.md
@.planning/phases/96-stripe-integration/96-04-SUMMARY.md
@.planning/phases/96-stripe-integration/96-05-SUMMARY.md

<interfaces>
From .github/workflows/publish.yml:
```yaml
# Wave 1: Crates with no internal dependencies
WAVE1_CRATES="ferro-macros ferro-events ferro-queue ferro-notifications ferro-broadcast ferro-storage ferro-cache ferro-lang ferro-json-ui ferro-inertia ferro-projections ferro-api-mcp"
```

From docs/src/SUMMARY.md (if exists):
```markdown
# Summary
- [Features](./features/...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish workflow and workspace validation</name>
  <files>
    .github/workflows/publish.yml,
    framework/src/lib.rs
  </files>
  <action>
    1. Update .github/workflows/publish.yml:
       - Add "ferro-stripe" to Wave 1 WAVE1_CRATES list (ferro-stripe has no internal dependencies on other ferro crates except through features)
       - Actually, ferro-stripe depends on ferro-events and ferro-queue. Check if both are in Wave 1. Yes they are. So ferro-stripe can also go in Wave 1 since all its ferro deps are already there.
       - Add after ferro-projections: "ferro-stripe"

    2. Verify framework/src/lib.rs has correct re-exports (should be done in Plan 02, but verify):
       - #[cfg(feature = "stripe")] block re-exporting ferro-stripe types
       - Confirm all public types are re-exported

    3. Run full workspace validation:
       - cargo fmt --all -- --check
       - cargo clippy --all --all-targets --all-features -- -D warnings
       - cargo test --all-features
       - Verify no warnings, no test failures

    4. Fix any compilation issues found during full workspace build.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --all --all-targets --all-features -- -D warnings && cargo test --all-features</automated>
  </verify>
  <done>
    - ferro-stripe in publish workflow Wave 1
    - Full workspace builds with all features
    - All tests pass (including ferro-stripe tests)
    - No clippy warnings
  </done>
</task>

<task type="auto">
  <name>Task 2: Stripe integration documentation</name>
  <files>
    docs/src/features/stripe.md,
    docs/src/SUMMARY.md
  </files>
  <action>
    1. Create docs/src/features/stripe.md with sections:

       **Overview:**
       - Two billing dimensions: platform subscriptions and Stripe Connect
       - Feature-gated behind `stripe` feature in Cargo.toml

       **Quick Start:**
       - Run `ferro make:stripe` (and optionally `--connect`)
       - Set environment variables
       - Initialize in bootstrap.rs
       - Register webhook routes

       **Platform Subscriptions:**
       - Creating Checkout Sessions for plan upgrades
       - Billing Portal redirect for self-service management
       - Subscription lifecycle: trial -> active -> canceled
       - RequiresPlan middleware for plan-gating routes
       - Code examples for each operation

       **Stripe Connect:**
       - Connect onboarding via AccountLink
       - Destination charges with application fees
       - Connect webhook handling
       - Code examples

       **Webhook Configuration:**
       - Two webhook endpoints: /stripe/webhook and /stripe/connect/webhook
       - Signature verification (raw body requirement)
       - ferro-queue job dispatch for async processing (per locked decision)
       - Event listeners for subscription sync via ferro-events
       - Idempotency considerations

       **TenantContext Enrichment:**
       - SubscriptionInfo fields and helper methods
       - on_trial(), subscribed(), on_grace_period()
       - current_plan() for plan name access
       - Cache invalidation on subscription changes

       **Database Schema:**
       - tenant_billing table structure
       - Index recommendations

       **Testing:**
       - Test helpers: mock_subscription_active/trialing/canceled/etc.
       - signed_webhook_payload for webhook testing
       - Event fixture generators

       **Environment Variables:**
       - STRIPE_SECRET_KEY (required)
       - STRIPE_WEBHOOK_SECRET (required)
       - STRIPE_CONNECT_WEBHOOK_SECRET (optional, for Connect)
       - STRIPE_APPLICATION_FEE_PERCENT (optional, for Connect)

    2. Update docs/src/SUMMARY.md to include the new page (if SUMMARY.md exists).

    3. Verify documentation builds (if mdbook is available):
       - mdbook build docs/ (if applicable)
  </action>
  <verify>
    <automated>test -f docs/src/features/stripe.md && wc -l docs/src/features/stripe.md | awk '{if ($1 > 50) print "PASS"; else print "FAIL: doc too short"}'</automated>
  </verify>
  <done>
    - stripe.md covers all major topics: subscriptions, Connect, webhooks, middleware, testing
    - Code examples use ferro:: imports matching actual API
    - Webhook section documents ferro-queue dispatch pattern per locked decision
    - Environment variable reference is complete
    - SUMMARY.md updated (if applicable)
  </done>
</task>

</tasks>

<verification>
- `cargo fmt --all -- --check && cargo clippy --all --all-targets --all-features -- -D warnings && cargo test --all-features` all pass
- ferro-stripe in publish workflow
- Documentation file exists and is comprehensive
</verification>

<success_criteria>
- Full workspace compiles and passes tests with stripe feature enabled
- ferro-stripe added to publish workflow Wave 1
- Documentation covers subscriptions, Connect, webhooks, middleware, and testing
- Phase 96 is feature-complete
</success_criteria>

<output>
After completion, create `.planning/phases/96-stripe-integration/96-06-SUMMARY.md`
</output>
