---
phase: 96-stripe-integration
plan: 04
type: execute
wave: 2
depends_on: ["96-01"]
files_modified:
  - ferro-stripe/src/testing.rs
  - ferro-stripe/src/lib.rs
  - ferro-stripe/Cargo.toml
autonomous: true
requirements: [STRIPE-09]

must_haves:
  truths:
    - "Test helpers generate valid webhook payloads with correct HMAC signatures"
    - "Test helpers create mock SubscriptionInfo in various states"
    - "Test helpers are available behind a test-helpers feature or cfg(test)"
  artifacts:
    - path: "ferro-stripe/src/testing.rs"
      provides: "Test helper functions for Stripe integration testing"
      contains: "mock_subscription"
  key_links:
    - from: "ferro-stripe/src/testing.rs"
      to: "ferro-stripe/src/subscription/mod.rs"
      via: "constructs SubscriptionInfo for testing"
      pattern: "SubscriptionInfo"
---

<objective>
Add test helpers for ferro-stripe that enable downstream testing of webhook processing and subscription state verification.

Purpose: Provides mock webhook payloads with valid signatures and factory functions for SubscriptionInfo in various states. Used by application tests and ferro-cli scaffold tests.
Output: Test helper module with signed webhook generation and subscription state factories.
</objective>

<execution_context>
@/Users/alberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-stripe-integration/96-CONTEXT.md
@.planning/phases/96-stripe-integration/96-RESEARCH.md
@.planning/phases/96-stripe-integration/96-01-SUMMARY.md

<interfaces>
From ferro-stripe/src/subscription/mod.rs (Plan 01):
```rust
pub struct SubscriptionInfo { ... }
pub enum SubscriptionStatus { Trialing, Active, ... }
```

From ferro-stripe/src/webhook/mod.rs (Plan 03):
```rust
pub fn verify_webhook(raw_body: &str, signature: &str, secret: &str) -> Result<stripe::Event, Error>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test helper module for Stripe testing</name>
  <files>
    ferro-stripe/src/testing.rs,
    ferro-stripe/src/lib.rs,
    ferro-stripe/Cargo.toml
  </files>
  <behavior>
    - mock_subscription_active("pro") returns SubscriptionInfo with status=Active, plan="pro"
    - mock_subscription_trialing("pro") returns SubscriptionInfo with status=Trialing, trial_ends_at set to 14 days from now
    - mock_subscription_canceled("pro") returns SubscriptionInfo with status=Canceled
    - mock_subscription_past_due("pro") returns SubscriptionInfo with status=PastDue
    - mock_subscription_on_grace("pro") returns SubscriptionInfo with status=Active, cancel_at_period_end=true
    - signed_webhook_payload generates a payload that verify_webhook accepts
    - mock_checkout_completed_event produces valid JSON with correct event type
    - mock_subscription_updated_event produces valid JSON with correct event type
  </behavior>
  <action>
    1. Add hmac and sha2 as dev-dependencies in ferro-stripe/Cargo.toml (for generating test HMAC signatures). Actually check if async-stripe re-exports the needed crypto. If not, add hmac = "0.12" and sha2 = "0.10" as dependencies (they're small).

    2. Create ferro-stripe/src/testing.rs:
       - Feature-gated behind #[cfg(any(test, feature = "test-helpers"))]
       - Add "test-helpers" feature to ferro-stripe/Cargo.toml features section

       Subscription factories:
       - pub fn mock_subscription_active(plan: &str) -> SubscriptionInfo — Active status, current_period_end 30 days out, no trial, no cancel
       - pub fn mock_subscription_trialing(plan: &str) -> SubscriptionInfo — Trialing status, trial_ends_at 14 days out
       - pub fn mock_subscription_canceled(plan: &str) -> SubscriptionInfo — Canceled status
       - pub fn mock_subscription_past_due(plan: &str) -> SubscriptionInfo — PastDue status
       - pub fn mock_subscription_on_grace(plan: &str) -> SubscriptionInfo — Active + cancel_at_period_end=true
       - pub fn mock_subscription_with_connect(plan: &str, connect_id: &str) -> SubscriptionInfo — Active + stripe_connect_account_id set

       Webhook test helpers:
       - pub fn signed_webhook_payload(event_json: &str, secret: &str) -> (String, String)
         - Returns (body_string, signature_header)
         - Generates t={unix_timestamp},v1={hmac_hex} format
         - Uses HMAC-SHA256: key=secret, message="{timestamp}.{event_json}"

       Event fixture generators:
       - pub fn mock_checkout_completed_event(session_id: &str, customer_id: &str) -> String — returns JSON string
       - pub fn mock_subscription_updated_event(subscription_id: &str, customer_id: &str, status: &str) -> String
       - pub fn mock_subscription_deleted_event(subscription_id: &str, customer_id: &str) -> String
       - pub fn mock_invoice_paid_event(invoice_id: &str, customer_id: &str) -> String

    3. Update ferro-stripe/src/lib.rs:
       - Add #[cfg(any(test, feature = "test-helpers"))] pub mod testing;
       - Re-export testing module

    4. Write tests:
       - Each factory function returns correct status and plan
       - signed_webhook_payload generates a signature that verify_webhook accepts (round-trip test)
       - Event fixtures parse as valid JSON with correct "type" field

    5. Run cargo fmt, clippy, and tests.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy -p ferro-stripe --all-targets -- -D warnings && cargo test -p ferro-stripe testing::</automated>
  </verify>
  <done>
    - 6 subscription factory functions produce correct SubscriptionInfo variants
    - signed_webhook_payload round-trips through verify_webhook
    - 4 event fixture generators produce valid JSON
    - All test helpers are behind test-helpers feature flag
  </done>
</task>

</tasks>

<verification>
- `cargo test -p ferro-stripe` passes including testing module tests
- `cargo clippy -p ferro-stripe --all-targets -- -D warnings` clean
- Test helpers are not compiled in release builds (feature-gated)
</verification>

<success_criteria>
- Subscription state factories cover all common testing scenarios
- Webhook signature test helper generates valid HMAC signatures
- Event fixture generators produce correct Stripe event JSON
- All gated behind test-helpers feature or cfg(test)
</success_criteria>

<output>
After completion, create `.planning/phases/96-stripe-integration/96-04-SUMMARY.md`
</output>
