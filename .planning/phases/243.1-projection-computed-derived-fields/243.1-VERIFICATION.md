---
phase: "243.1-projection-computed-derived-fields"
verified: "2026-06-24T12:00:00Z"
status: passed
score: 4/4
overrides_applied: 0
---

# Phase 243.1: Projection Computed/Derived Fields — Verification Report

**Phase Goal:** A projection field declared read-only is excluded from `create_`/`update_` MCP input schemas and the write kernel, and a derived field's value (`order.total`) is recomputed server-side on every line-item write — demonstrated end-to-end against the sample app.
**Verified:** 2026-06-24T12:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `create_order` and `update_order` MCP input schemas EXCLUDE the read-only `total` field | VERIFIED | Gate F in `ferro-projections/src/service.rs:278` (`!field.writable → true`); `order.total` declared `.read_only_field()` in `app/src/projections/order.rs:25`; schema regression test at `ferro-mcp-server/src/schema.rs:870` (`read_only_field_absent_from_write_schemas`); e2e test `create_order_schema_omits_derived_total` passes |
| 2 | Creating an order with no line items yields `total = 0` | VERIFIED | `orders.total DEFAULT 0.0` in `app/src/migrations/m20260611_create_orders_table.rs`; `order_total_is_derived_from_line_items` asserts `total == 0.0` on the created order response — passes |
| 3 | `order.total` equals `SUM(line_items.amount)` after line-item create/update | VERIFIED | `recompute_order_total_hook` registered via `.with_override("create_line_item", ...)` and `.with_override("update_line_item", ...)` in `app/src/controllers/mcp.rs:238-240`; UPDATE uses `COALESCE(SUM(amount), 0)` tenant-scoped on both the inner SELECT and outer UPDATE (post code-review fix commit 92633be0); `order_total_is_derived_from_line_items` asserts `total == 15.5` after two line items — passes |
| 4 | Deleting a line item recomputes `order.total` (read-your-writes) | VERIFIED | `.with_override("delete_line_item", recompute_order_total_hook())` at `mcp.rs:240`; hook resolves `order_id` from soft-deleted row via tenant-scoped SELECT when `base_result` omits `order_id`; `order_total_is_derived_from_line_items` asserts `total == 5.5` after delete — passes |

**Score:** 4/4 truths verified

---

### Required Artifacts

| Artifact | Status | Details |
|----------|--------|---------|
| `ferro-projections/src/service.rs` — Gate F in `is_write_excluded_field` (honors `writable`) | VERIFIED | Gate F at line 278: `if !field.writable { return true; }`. Unit test `is_write_excluded_field_excludes_read_only` at line 2321 confirms gate behavior. |
| `app/src/projections/line_item.rs` — line_item CRUD projection | VERIFIED | Full ServiceDef: `creatable`, `updatable`, `deletable`, `tenant_column("tenant_id")`, `soft_delete_column("deleted_at")`, `mcp_ability("view-orders")`, `mcp_write_ability("manage-orders")`, fields `id` (read-only Identifier), `order_id` (ForeignKey), `amount` (Money), `created_at` (read-only, deviation from plan — needed for `execute_crud_plan`'s auto-injected `created_at`). |
| `app/src/controllers/mcp.rs` — `recompute_order_total_hook` via `with_override` | VERIFIED | Hook registered for `create_line_item`, `update_line_item`, `delete_line_item` at lines 238-240. Backend-aware placeholders (SQLite `?` / Postgres `$N`) and inner SUM tenant-scoped — both code-review findings (WR-01, WR-02) fixed in commit 92633be0. |
| `app/src/tests/computed_total_e2e.rs` — in-process e2e field test | VERIFIED | Two tests: `create_order_schema_omits_derived_total` (Truth 1) and `order_total_is_derived_from_line_items` (Truths 2-4, `#[cfg(not(feature = "confirmation"))]`). Both pass. Uses `exposed_services()` and `make_write_dispatcher()` from the real controller — not a fixture dispatcher. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `order.total` declaration | `is_write_excluded_field` Gate F | `read_only_field()` sets `writable: false`; Gate F checks `!field.writable` | WIRED | Chain: `order.rs:25 .read_only_field(...)` → `service.rs:278 if !field.writable { return true }` → schema builders exclude it |
| `create_line_item` / `update_line_item` / `delete_line_item` writes | `recompute_order_total_hook` | `WriteDispatcher::with_override(action, hook)` | WIRED | `mcp.rs:238-240` registers hook on all three verbs; hook fires post-persist, updates `orders.total` via raw SQL |
| Recompute SQL | Tenant boundary | `WHERE order_id = ? AND tenant_id = ?` on both inner SUM SELECT and outer UPDATE | WIRED | `mcp.rs:127-132` — inner SUM and outer UPDATE both carry `tenant_id` predicate (WR-02 fix in 92633be0) |
| Recompute SQL | Backend portability | `let (p1, p2, p3, p4) = match backend { Postgres => ("$1","$2","$3","$4"), _ => ("?","?","?","?") }` | WIRED | `mcp.rs:85-88` — WR-01 fix in 92633be0, mirrors the `check_is_manager` pattern |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `order.total` in `list_order` response | `total` column on `orders` row | SeaORM entity read (`list_order` CRUD list path) | Yes — DB column written by `UPDATE orders SET total = (SELECT COALESCE(SUM(amount), 0) ...)` in the hook | FLOWING |
| `orders.total` DB column | `total` after `create_order` | Migration sets `DEFAULT 0.0` on column; INSERT omits `total` (Gate F excluded it) | Yes — DB default 0.0 | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Truth 1: `create_order` schema omits `total` | `cargo test -p app computed_total -- --nocapture` | `create_order_schema_omits_derived_total ... ok` | PASS |
| Truths 2-4: full derived-total lifecycle | `cargo test -p app computed_total -- --nocapture` | `order_total_is_derived_from_line_items ... ok` | PASS |

**Full run output:**
```
running 2 tests
test tests::computed_total_e2e::tests::create_order_schema_omits_derived_total ... ok
test tests::computed_total_e2e::tests::order_total_is_derived_from_line_items ... ok

test result: ok. 2 passed; 0 failed; 0 ignored; 0 measured; 43 filtered out; finished in 0.01s
```

---

### Requirements Coverage

No `requirements:` frontmatter in this phase's PLAN.md — this phase is a standalone feature (no REQUIREMENTS.md line-item mapping). Truths are fully derived from the plan's `must_haves` contract and the phase goal. All four truths verified.

---

### Anti-Patterns Found

No stubs, placeholders, or incomplete implementations found. The only grep hit (`mcp.rs:81`) is a doc-comment explaining backend-aware placeholders — not a stub indicator.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | None found | — | — |

---

### Human Verification Required

None. The in-process e2e (`cargo test -p app computed_total`) is the defined behavioral gate for all four truths. The optional `:8090` live drive noted in the plan (Task 9, Step 2) is explicitly optional/manual and is covered by Phase 243's harness — not a required gate for this phase.

---

### Deferred Items

| Item | Addressed In | Evidence |
|------|-------------|----------|
| Per-field AX `description` (field-level "derived from line items" text in schema) | Future Direction B | Consciously deferred per plan Spec Deltas section. `FieldDef` has no `description` field today; adding one would ripple into serde, JsonSchema, protocol-schema export test, and every read-schema builder. No gap — documented deviation with explicit tracking. |

---

### Gaps Summary

No gaps. All four must-have truths verified through static code analysis and a green test run. The code-review findings from the REVIEW.md (WR-01, WR-02, IN-01) were all resolved in commit `92633be0` prior to this verification.

---

_Verified: 2026-06-24T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
