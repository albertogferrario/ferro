---
phase: 117.1
slug: schema-driven-projections
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-18
---

# Phase 117.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in `#[test]` via cargo test |
| **Config file** | none (workspace-level Cargo.toml) |
| **Quick run command** | `cargo test -p ferro-json-ui --features projections` |
| **Full suite command** | `cargo test --all-features` |
| **Estimated runtime** | ~30 seconds (scoped crate) / ~3 minutes (full suite) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ferro-json-ui --features projections`
- **After every plan wave:** Run `cargo test --all-features`
- **Before `/gsd-verify-work`:** Full suite must be green (`cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features`)
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 117.1-01-01 | 01 | 1 | PROJ-02 / D-01 / D-13 | — | MEANING_COMPONENT_TABLE entries all resolve in `global_catalog().components` | unit | `cargo test -p ferro-json-ui --features projections -- meaning_table_components_exist_in_catalog` | ❌ W0 | ⬜ pending |
| 117.1-02-00 | 02 | 2 | PROJ-01 / D-06 | — | `Spec::from_service_def` always calls `global_catalog().validate(&spec)` | unit | `cargo test -p ferro-json-ui --features projections -- from_service_def_validates` | ❌ W0 | ⬜ pending |
| 117.1-02-01 | 02 | 2 | PROJ-01 / D-08 | — | `Spec::from_service_def(service, intents, ctx)` returns Ok for a Browse/Display pair | unit | `cargo test -p ferro-json-ui --features projections -- from_service_def_browse_display` | ❌ W0 | ⬜ pending |
| 117.1-02-02 | 02 | 2 | PROJ-01 / D-11 | — | RenderMode::Input collapses every intent to Form | unit | `cargo test -p ferro-json-ui --features projections -- input_mode_always_form` | ❌ W0 | ⬜ pending |
| 117.1-02-03 | 02 | 2 | PROJ-03 / D-10 | — | System fields (Identifier/CreatedAt/UpdatedAt) excluded from `fields` slot emission | unit | `cargo test -p ferro-json-ui --features projections -- system_fields_excluded` | ❌ W0 | ⬜ pending |
| 117.1-02-04 | 02 | 2 | PROJ-03 / D-05 | — | `ctx.templates` override default `IntentModeTemplates` | unit | `cargo test -p ferro-json-ui --features projections -- template_override` | ❌ W0 | ⬜ pending |
| 117.1-03-01 | 03 | 2 | PROJ-01 / D-07 | — | `field_map.rs` and `relationship_map.rs` removed; crate still compiles with `projections` feature | compile | `cargo build -p ferro-json-ui --features projections` | existing | ⬜ pending |
| 117.1-03-02 | 03 | 3 | PROJ-01..03 | — | Whole workspace passes fmt + clippy -D warnings + full test suite | gate | `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` | existing | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `ferro-json-ui/src/projection/error.rs` — `ProjectionError` enum (thiserror-derived)
- [ ] `ferro-json-ui/src/projection/component_map.rs` — `MEANING_COMPONENT_TABLE` table + drift guard `#[test]`
- [ ] `ferro-json-ui/src/projection/intent_layout.rs` — `default_template(Intent)` + `IntentSlotTemplate`-based dispatch
- [ ] `ferro-json-ui/src/projection/builder.rs` — `Spec::from_service_def` skeleton (compiles, returns `EmptyIntents` until slots wired)
- [ ] `ferro-json-ui/tests/projection_from_service_def.rs` — integration-level tests for the new pipeline (may live alongside `#[cfg(test)]` module if preferred)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Sample-app visual sanity check (optional) | PROJ-01 | Visual confirmation that `render_projection` MCP tool still produces the expected HTML when the new pipeline is in place | Run `cargo run -p app` sample, exercise the existing projection route, confirm output HTML structure is unchanged for the reference `order` service |

*If the sample-app path is not exercised in Phase 117.1, mark N/A — all phase-critical behaviors have automated verification.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
