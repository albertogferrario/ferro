---
phase: 144
plan: 04
type: execute
wave: 2
depends_on: [144-02, 144-03]
files_modified:
  - framework/tests/routing_group_trailing_slash.rs
autonomous: true
requirements: [D-07, D-10, D-11]
tags: [routing, integration-test, middleware, mcp-surface]

must_haves:
  truths:
    - "get_registered_routes() contains exactly one RouteInfo per logical handler after registering a group with a root leaf (D-07, D-10)"
    - "Middleware attached to a group executes for BOTH /prefix and /prefix/ request variants (Strategy A end-to-end dispatch proof; blocks T-144-12 middleware bypass)"
    - "The gestiscilo-it reproducer (group!('/s/{slug}', { get!('/', root), get!('/index.html', idx), get!('/{*path}', asset) })) resolves all four URL shapes to the correct handler with slug extraction"
    - "Top-level get!('/', h) outside any group still produces exactly one RouteInfo (Pitfall 6 regression)"
    - "Integration tests do not interfere with other tests via the process-global REGISTERED_ROUTES state (use #[serial_test::serial])"
  artifacts:
    - path: framework/tests/routing_group_trailing_slash.rs
      provides: "Integration test file asserting D-07, D-10, middleware-runs-on-both-variants, gestiscilo reproducer, top-level regression"
      contains: "#[serial_test::serial]"
  key_links:
    - from: framework/tests/routing_group_trailing_slash.rs
      to: ferro_rs crate public API
      via: "extern crate ferro_rs as ferro; use ferro_rs::{Router, get_registered_routes, ...};"
      pattern: "extern crate ferro_rs"
---

<objective>
Add the Phase 144 integration test file `framework/tests/routing_group_trailing_slash.rs` that exercises the public ferro API end-to-end to prove:

1. D-07 / D-10: `get_registered_routes()` contains exactly ONE `RouteInfo` per logical handler even when the fix registers the handler under two matchit leaves.
2. T-144-12 mitigation (blocking): group middleware EXECUTES for requests to both `/prefix` and `/prefix/` — not just registers under the canonical key, but actually runs. This is the end-to-end proof that Strategy A (canonical-only middleware key + alias leaf storing canonical pattern) prevents middleware bypass.
3. The exact gestiscilo-it field-test reproducer: `group!("/s/{slug}", { get!("/", serve_root), get!("/index.html", serve_index), get!("/{*path}", serve_asset) })` — `/s/foo`, `/s/foo/`, `/s/foo/index.html`, `/s/foo/bar.css` each resolve to the expected handler with `slug=foo`.
4. Regression (Pitfall 6): top-level `get!("/", h)` produces exactly one `RouteInfo` (the fix is localized to GroupDef / GroupBuilder, not the non-grouped path).

Purpose: The unit tests in Plans 02 and 03 cover registry-level state and `match_route` returns. This integration test covers the actual server dispatch path and executes middleware, which is what catches T-144-12 (the primary blocking security threat for this phase).

Output: a single new `.rs` file in `framework/tests/` using `#[serial_test::serial]` to guard the process-global `REGISTERED_ROUTES` against bleed from other integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-fix-root-path-routing-in-group-routes/144-CONTEXT.md
@.planning/phases/144-fix-root-path-routing-in-group-routes/144-RESEARCH.md
@.planning/phases/144-fix-root-path-routing-in-group-routes/144-PATTERNS.md
@framework/src/routing/path.rs
@framework/src/routing/macros.rs
@framework/src/routing/group.rs
@framework/src/routing/router.rs
@framework/src/server.rs
@framework/tests/api_resource_derive.rs
@framework/Cargo.toml

<interfaces>
<!-- Existing framework/tests/ convention (api_resource_derive.rs lines 1-9): -->

    //! Integration tests for the ApiResource derive macro.
    //! Tests field selection, rename, skip, and From<Model> generation.

    extern crate ferro_rs as ferro;

    use ferro_rs::{ApiResource, Request, Resource};
    use serde_json::json;
    use std::sync::Arc;
    use tokio::sync::oneshot;

<!-- serial_test is already in framework/Cargo.toml [dev-dependencies] line 77: `serial_test = "3"`. Use #[serial_test::serial] on every test that inspects get_registered_routes() deltas. -->

<!-- Public API symbols expected to be in scope via `use ferro_rs::{...}`: -->
<!-- - Router, Request, Response -->
<!-- - get_registered_routes, RouteInfo -->
<!-- - The macro surface group!, get!, routes! (macros are re-exported at the framework crate root) -->
<!-- - HttpMethod (or equivalent from framework/src/http/) -->

<!-- Dispatch helper shape — calls the same match_route API used in Plans 02 and 03: -->

    fn dispatch(router: &Router, method: &hyper::Method, path: &str)
        -> Option<(HashMap<String, String>, String)>
    {
        router.match_route(method, path).map(|(_, params, pattern)| (params, pattern))
    }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create integration test file routing_group_trailing_slash.rs</name>
  <files>framework/tests/routing_group_trailing_slash.rs</files>
  <read_first>
    - framework/tests/api_resource_derive.rs lines 1-9 (header convention: //! doc, `extern crate ferro_rs as ferro;`, `use ferro_rs::{...}`)
    - framework/Cargo.toml lines 70-79 (confirm serial_test = "3" is in [dev-dependencies])
    - framework/src/middleware/mod.rs (to confirm the actual Middleware trait shape — async_trait vs alternative, method name `handle` or `process`, Next type)
    - framework/src/server.rs (to understand how to invoke dispatch through the public API — or whether a direct Router::match_route + manual middleware invocation is acceptable)
    - framework/src/routing/router.rs lines 200-210 (confirms `get_route_middleware` is pub and returns Vec<BoxedMiddleware>)
    - framework/src/lib.rs (confirm what's re-exported at the ferro_rs crate root — `group!`, `get!`, `routes!`, `Router`, `get_registered_routes`, `Request`, `Response`, `RouteInfo`)
    - .planning/phases/144-fix-root-path-routing-in-group-routes/144-PATTERNS.md §6 (integration test file conventions, header pattern, required test cases)
    - .planning/phases/144-fix-root-path-routing-in-group-routes/144-RESEARCH.md §Wave 0 Gaps lines 450-455 (the 5 required cases + the dispatch helper shape)
    - .planning/phases/144-fix-root-path-routing-in-group-routes/144-VALIDATION.md (test matrix with the exact test names to use: `no_duplicate_route_info`, `middleware_runs_for_both_variants`)
  </read_first>
  <behavior>
    - Test `no_duplicate_route_info` (D-07, D-10): register `group!("/api", { get!("/", h) })`, capture `get_registered_routes().len()` delta, assert delta == 1. Register also `group!("/api/v2", { get!("/", h), get!("/health", h) })` and assert delta == 2 (one per logical handler regardless of how many matchit leaves get inserted under the hood).
    - Test `middleware_runs_for_both_variants` (T-144-12 mitigation): register `group!("/api", { get!("/", h) }).middleware(CounterMw { counter })`, dispatch a request to `/api` then a second request to `/api/`, assert counter increased by 2. This requires driving the actual request pipeline — use whatever public entry point ferro exposes (either `framework::server` dispatch API, or simulate by manually looking up middleware via `router.get_route_middleware(route_pattern)` where `route_pattern` comes from `router.match_route(&method, path).2` and then executing the chain).
    - Test `gestiscilo_reproducer`: register `group!("/s/{slug}", { get!("/", serve_root), get!("/index.html", serve_index), get!("/{*path}", serve_asset) })`. Dispatch `/s/foo` → pattern `/s/{slug}` + `slug=foo`; `/s/foo/` → pattern `/s/{slug}` + `slug=foo`; `/s/foo/index.html` → pattern `/s/{slug}/index.html` + `slug=foo`; `/s/foo/bar.css` → pattern `/s/{slug}/{*path}` + `slug=foo` + `path=bar.css`.
    - Test `top_level_root_route_is_single_slash` (Pitfall 6 regression): register `routes! { get!("/", h) }` (NOT inside a group). Assert `get_registered_routes().len()` delta == 1 and `match_route(&Method::GET, "/")` returns `Some` while `match_route(&Method::GET, "//")` returns `None`.
  </behavior>
  <action>
Create a new file `framework/tests/routing_group_trailing_slash.rs` with exactly this scaffold. Adjust symbol names (Method variants, Middleware trait method name, macro re-export path) to match what is actually exported from the `ferro_rs` crate after you read framework/src/lib.rs and framework/src/middleware/mod.rs.

BASE SCAFFOLD (the structural skeleton — the executor MUST adapt symbol names during implementation):

    //! Integration tests for Phase 144 — group route trailing-slash semantics.
    //!
    //! Covers:
    //! - D-07 / D-10: get_registered_routes() returns one entry per logical
    //!   handler even when the handler is registered under two matchit leaves.
    //! - T-144-12 mitigation: middleware attached to a group executes for
    //!   both /prefix and /prefix/ request variants (Strategy A end-to-end).
    //! - Gestiscilo reproducer: group!("/s/{slug}", { get!("/", root),
    //!   get!("/index.html", idx), get!("/{*path}", asset) }) routes all four
    //!   URL shapes correctly.
    //! - Regression (Pitfall 6): top-level get!("/", h) outside a group still
    //!   produces exactly one RouteInfo.

    extern crate ferro_rs as ferro;

    use ferro_rs::{
        get_registered_routes, group, routes, Request, Response, Router,
    };
    use serial_test::serial;
    use std::collections::HashMap;
    use std::sync::atomic::{AtomicUsize, Ordering};
    use std::sync::Arc;

    // --------------------------------------------------------------------
    // Fixtures
    // --------------------------------------------------------------------

    async fn serve_root(_req: Request) -> Response {
        ferro_rs::text("root")
    }

    async fn serve_index(_req: Request) -> Response {
        ferro_rs::text("index")
    }

    async fn serve_asset(_req: Request) -> Response {
        ferro_rs::text("asset")
    }

    async fn hello(_req: Request) -> Response {
        ferro_rs::text("hello")
    }

    /// Maps a `(params, route_pattern)` out of `router.match_route` for
    /// assertion clarity. `router.match_route` takes `&hyper::Method`
    /// (`framework/src/routing/router.rs` line 365).
    fn dispatch(
        router: &Router,
        method: &hyper::Method,
        path: &str,
    ) -> Option<(HashMap<String, String>, String)> {
        router
            .match_route(method, path)
            .map(|(_, params, pattern)| (params, pattern))
    }

    // --------------------------------------------------------------------
    // D-07 / D-10: one RouteInfo per logical handler
    // --------------------------------------------------------------------

    #[test]
    #[serial]
    fn no_duplicate_route_info() {
        use ferro_rs::get;

        let before = get_registered_routes().len();

        // Single root-in-group handler — registers 2 matchit leaves but 1 RouteInfo.
        let _router: Router = routes! {
            group!("/api", {
                get!("/", hello)
            })
        };

        let after = get_registered_routes().len();
        assert_eq!(
            after - before,
            1,
            "group!('/api', {{ get!('/', h) }}) must produce exactly 1 RouteInfo (got delta {})",
            after - before
        );
    }

    #[test]
    #[serial]
    fn no_duplicate_route_info_multi_handler_group() {
        use ferro_rs::get;

        let before = get_registered_routes().len();

        // Group with two handlers: one root (alternate emitted), one non-root (no alternate).
        // Expected RouteInfo delta: 2 (one per logical handler).
        let _router: Router = routes! {
            group!("/v2", {
                get!("/", hello),
                get!("/health", hello)
            })
        };

        let after = get_registered_routes().len();
        assert_eq!(after - before, 2);
    }

    // --------------------------------------------------------------------
    // T-144-12 mitigation: middleware executes for both variants
    // --------------------------------------------------------------------
    //
    // This test asserts the middleware Vec returned for /api is non-empty
    // (registered under canonical per Strategy A), and then proves end-to-end
    // dispatch by invoking the handler+middleware pipeline for both URLs.
    //
    // If the ferro_rs public API does not offer a direct "dispatch a request
    // through the middleware chain" function, substitute the manual flow:
    //     let (handler, params, pattern) = router.match_route(&Method::GET, path)?;
    //     let middlewares = router.get_route_middleware(&pattern);
    //     // build a synthetic Request and drive `middlewares + handler` manually
    // Match whatever existing integration tests in framework/tests/ do.

    #[test]
    #[serial]
    fn middleware_runs_for_both_variants() {
        use ferro_rs::get;

        let counter = Arc::new(AtomicUsize::new(0));

        // IMPORTANT: the CounterMiddleware type must implement whatever
        // Middleware trait the ferro crate exposes. Read
        // framework/src/middleware/mod.rs and match its exact shape (method
        // name, async-trait vs other mechanism, Next / Response types).
        // If the framework uses `async_trait::async_trait`, use that; if it
        // uses BoxFuture or a concrete Future type, match that instead.
        //
        // The example shape below is a placeholder — ADAPT BEFORE COMPILING.
        struct CounterMw {
            counter: Arc<AtomicUsize>,
        }

        // ... impl Middleware for CounterMw with the correct trait shape ...

        let _router: Router = routes! {
            group!("/api", {
                get!("/", hello)
            })
            // .middleware(CounterMw { counter: counter.clone() })
        };

        // TODO during implementation: dispatch GET /api and GET /api/ through
        // the public ferro server-dispatch function (or the internal test
        // helper used by other integration tests), then assert:
        //     assert_eq!(counter.load(Ordering::SeqCst), 2);
        //
        // If no suitable dispatch helper exists in framework/tests/, fall back
        // to the structural assertion:
        //     - router.get_route_middleware("/api").len() == 1
        //     - Both `/api` and `/api/` `match_route` calls return a value
        //       whose third element (route_pattern) is "/api".
        // The structural assertion still proves Strategy A at the registry
        // level (alias leaf carries canonical pattern, so middleware lookup
        // goes through the same HashMap key). If Plan 03's
        // `builder_middleware_registered_under_canonical_only` test also
        // passes, the end-to-end coverage is complete even without a live
        // dispatch call.

        let _ = counter; // silence unused var if fallback assertion is used
    }

    // --------------------------------------------------------------------
    // Gestiscilo-it field-test reproducer
    // --------------------------------------------------------------------

    #[test]
    #[serial]
    fn gestiscilo_reproducer() {
        use ferro_rs::get;
        use hyper::Method;

        let router: Router = routes! {
            group!("/s/{slug}", {
                get!("/", serve_root),
                get!("/index.html", serve_index),
                get!("/{*path}", serve_asset)
            })
        };

        // /s/foo -> serve_root, slug=foo, pattern=/s/{slug}
        let (params, pattern) =
            dispatch(&router, &Method::GET, "/s/foo").expect("GET /s/foo must match");
        assert_eq!(params.get("slug").map(String::as_str), Some("foo"));
        assert_eq!(pattern, "/s/{slug}");

        // /s/foo/ -> serve_root, slug=foo, pattern=/s/{slug} (canonical preserved)
        let (params, pattern) =
            dispatch(&router, &Method::GET, "/s/foo/").expect("GET /s/foo/ must match");
        assert_eq!(params.get("slug").map(String::as_str), Some("foo"));
        assert_eq!(pattern, "/s/{slug}");

        // /s/foo/index.html -> serve_index
        let (params, pattern) = dispatch(&router, &Method::GET, "/s/foo/index.html")
            .expect("GET /s/foo/index.html must match");
        assert_eq!(params.get("slug").map(String::as_str), Some("foo"));
        assert_eq!(pattern, "/s/{slug}/index.html");

        // /s/foo/bar.css -> serve_asset
        let (params, pattern) = dispatch(&router, &Method::GET, "/s/foo/bar.css")
            .expect("GET /s/foo/bar.css must match");
        assert_eq!(params.get("slug").map(String::as_str), Some("foo"));
        assert_eq!(params.get("path").map(String::as_str), Some("bar.css"));
        assert_eq!(pattern, "/s/{slug}/{*path}");
    }

    // --------------------------------------------------------------------
    // Pitfall 6 regression: top-level root route is single-slot
    // --------------------------------------------------------------------

    #[test]
    #[serial]
    fn top_level_root_route_is_single_slash() {
        use ferro_rs::get;
        use hyper::Method;

        let before = get_registered_routes().len();

        let router: Router = routes! {
            get!("/", hello)
        };

        let after = get_registered_routes().len();
        assert_eq!(
            after - before,
            1,
            "top-level get!('/', h) must produce exactly 1 RouteInfo"
        );

        assert!(router.match_route(&Method::GET, "/").is_some());
        assert!(
            router.match_route(&Method::GET, "//").is_none(),
            "top-level root must not emit an alternate for //"
        );
    }

EXECUTOR NOTES:

1. `Method` is NOT re-exported from `ferro-rs` — confirmed by grepping `framework/src/lib.rs` for `Method` (no matches). `match_route` takes `&hyper::Method` (`framework/src/routing/router.rs` lines 363–373). Import via `use hyper::Method;` directly — `hyper` is a transitive dependency of `ferro-rs` (declared in `framework/Cargo.toml` line 25 as `hyper = { version = "1", features = ["full"] }`), so integration tests in `framework/tests/` resolve `hyper` without any extra `dev-dependencies` entry.
   - `ferro_rs::text` may be `ferro_rs::http::text` — check `framework/src/lib.rs` `pub use http::{...}` block around line 105 and adjust the three fixture functions accordingly.
   - `group!`, `get!`, `routes!` macros are `#[macro_export]`'d and therefore automatically in the crate root when using `extern crate ferro_rs as ferro;` — check `framework/src/routing/macros.rs` for `#[macro_export]` attributes.

2. The `middleware_runs_for_both_variants` test is the T-144-12 blocking mitigation. It has two acceptable implementations:
   - **Preferred:** drive a real request through the dispatch pipeline and assert `counter == 2`. Look at existing integration tests in `framework/tests/` for the pattern; `api_resource_derive.rs` uses `tokio::sync::oneshot` but is not a full dispatch — check if other test files (or the `framework/src/server.rs` test section) provide a dispatch helper.
   - **Fallback (acceptable):** structural assertion via `router.get_route_middleware("/api").len() == 1` and `match_route` pattern equality. This still proves Strategy A at the registry level. Plan 03's `builder_middleware_registered_under_canonical_only` unit test covers the builder surface; this integration test covers the macro surface equivalently. Document the choice in 144-04-SUMMARY.md.

3. The `Method` variants are SCREAMING-CASE (`Method::GET`, `Method::POST`, `Method::PUT`, `Method::PATCH`, `Method::DELETE`) because they come from `hyper::Method` (verified at `framework/src/routing/router.rs` lines 368–373). Do NOT use `Method::Get` / `Method::Post` etc. — those are private enum variants on `framework/src/routing/router.rs` line 154, not the `hyper::Method` the public `match_route` API accepts.

4. `#[serial_test::serial]` is required on every test that reads `get_registered_routes().len()` — the registry is a process-global `OnceLock<RwLock<Vec<...>>>` and parallel tests will see each other's writes. `serial_test = "3"` is already in `framework/Cargo.toml` `[dev-dependencies]` (line 77).

5. If the `get!` / `group!` / `routes!` macros are re-exported at the crate root, use them directly; if they live under a sub-module, adjust imports. In the worst case, `use ferro_rs::routing::{...}` gets you access to the macro surface.
  </action>
  <verify>
    <automated>cargo test -p ferro-rs --test routing_group_trailing_slash</automated>
  </verify>
  <acceptance_criteria>
    - `framework/tests/routing_group_trailing_slash.rs` exists
    - `grep -c "#\[serial\]" framework/tests/routing_group_trailing_slash.rs` returns at least `4` (all 4+ tests are serial-guarded)
    - `grep -c "fn no_duplicate_route_info" framework/tests/routing_group_trailing_slash.rs` returns `1`
    - `grep -c "fn middleware_runs_for_both_variants" framework/tests/routing_group_trailing_slash.rs` returns `1`
    - `grep -c "fn gestiscilo_reproducer" framework/tests/routing_group_trailing_slash.rs` returns `1`
    - `grep -c "fn top_level_root_route_is_single_slash" framework/tests/routing_group_trailing_slash.rs` returns `1`
    - `grep -c "extern crate ferro_rs" framework/tests/routing_group_trailing_slash.rs` returns `1`
    - `cargo test -p ferro-rs --test routing_group_trailing_slash -- no_duplicate_route_info` exits 0
    - `cargo test -p ferro-rs --test routing_group_trailing_slash -- gestiscilo_reproducer` exits 0
    - `cargo test -p ferro-rs --test routing_group_trailing_slash -- top_level_root_route_is_single_slash` exits 0
    - `cargo test -p ferro-rs --test routing_group_trailing_slash -- middleware_runs_for_both_variants` exits 0 (even if the fallback structural assertion is used, the test MUST pass)
    - `cargo clippy -p ferro-rs --all-targets -- -D warnings` produces no new warnings
  </acceptance_criteria>
  <done>
    Integration test file exists in `framework/tests/`, all four test functions pass under `cargo test -p ferro-rs --test routing_group_trailing_slash`, each test is `#[serial_test::serial]`-guarded, and the middleware-runs-for-both-variants test proves (either end-to-end or structurally) that T-144-12 is mitigated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Same as Plans 02 and 03. This plan does not change the framework surface; it tests the fixes applied there.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-31 | Elevation of Privilege / Middleware bypass | End-to-end middleware execution on alternate URL variant | **mitigate** (primary proof) | `middleware_runs_for_both_variants` test executes (or structurally asserts) that a group middleware fires for both `/prefix` and `/prefix/`. This is the authoritative end-to-end proof that the T-144-12 mitigation (canonical pattern stored in alias matchit leaf) actually works in the real dispatch path. **Blocking severity.** Without this test, the phase could ship with a silent security regression. |
| T-144-32 | Information Disclosure / RouteInfo leak | `get_registered_routes()` duplicate entries | **mitigate** (primary proof) | `no_duplicate_route_info` and `no_duplicate_route_info_multi_handler_group` tests assert the delta against multi-handler groups to guarantee D-07 holds across realistic registration patterns (including the gestiscilo 3-handler case). |
| T-144-33 | Test flakiness from shared global state | `REGISTERED_ROUTES` process-global | mitigate | Every test in this file is `#[serial_test::serial]`. `before`/`after` delta assertions (instead of absolute counts) provide a secondary guard. |
</threat_model>

<verification>
- Test file compiles: `cargo build -p ferro-rs --tests`
- All four integration tests pass: `cargo test -p ferro-rs --test routing_group_trailing_slash`
- No clippy warnings on test code: `cargo clippy -p ferro-rs --all-targets -- -D warnings`
- `#[serial_test::serial]` applied to every test: `grep -c "#\[serial\]" framework/tests/routing_group_trailing_slash.rs` >= 4
- The test file does NOT modify any production source — `git diff --stat framework/src/` reports no changes from this plan
</verification>

<success_criteria>
Plan 04 is complete when `framework/tests/routing_group_trailing_slash.rs` exists with all four tests passing, the file uses `#[serial_test::serial]` on every test, the middleware-runs-for-both-variants test proves T-144-12 mitigation (either end-to-end or via structural registry assertion as fallback), and the whole file passes `cargo clippy -- -D warnings`.
</success_criteria>

<output>
After completion, create `.planning/phases/144-fix-root-path-routing-in-group-routes/144-04-SUMMARY.md` documenting: (a) the final test names and what they cover, (b) whether the middleware test used live dispatch or structural fallback (and if fallback, why — e.g. no dispatch helper was available in framework/tests/), (c) any adjustments to the imports (Method location, text helper location, macro availability) that differed from the base scaffold.
</output>
