---
phase: 144
plan: 05
type: execute
wave: 3
depends_on: [144-02, 144-03, 144-04]
files_modified:
  - docs/src/the-basics/routing.md
  - docs/src/the-basics/middleware.md
  - framework/src/routing/macros.rs
  - CHANGELOG.md
  - Cargo.toml
autonomous: true
requirements: [D-12, D-13]
tags: [docs, changelog, release, version-bump]

must_haves:
  truths:
    - "docs/src/the-basics/routing.md explains the new '/ inside a group matches both variants' semantics with a worked example (D-01 user-facing doc)"
    - "docs/src/the-basics/middleware.md notes that group middleware applies to both trailing-slash variants (invariant from Strategy A)"
    - "The rustdoc 'Path Combination' section on GroupDef::register in macros.rs lines 596-606 reflects the new rules"
    - "CHANGELOG.md has a new `## ferro-rs` section with a 0.2.13 entry dated 2026-04-21, written in NEUTRAL voice (no gestiscilo-it mention per repository-document discipline)"
    - "Workspace version at /Cargo.toml line 27 is 0.2.13"
    - "Full workspace gate passes: cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features"
  artifacts:
    - path: CHANGELOG.md
      provides: "ferro-rs 0.2.13 entry under a new `## ferro-rs` top-level section"
      contains: "### [0.2.13]"
    - path: Cargo.toml
      provides: "workspace.package.version = \"0.2.13\""
      contains: "version = \"0.2.13\""
    - path: docs/src/the-basics/routing.md
      provides: "New subsection under Route Groups covering root-path trailing-slash semantics"
      contains: "Root routes inside a group"
  key_links:
    - from: Cargo.toml (line 27)
      to: every workspace crate Cargo.toml
      via: "version.workspace = true"
      pattern: "^version = \"0.2.13\""
---

<objective>
Close Phase 144 with the release artifacts:

1. **Documentation:** Update `docs/src/the-basics/routing.md` with a new subsection explaining the root-in-group dual-variant match (D-01 user-facing documentation). Add a one-sentence note to `docs/src/the-basics/middleware.md` confirming that group middleware applies to both trailing-slash variants (documentation of the Strategy A invariant). Update the rustdoc "Path Combination" block in `framework/src/routing/macros.rs` lines 596-606 to reflect the new rules.

2. **Changelog:** Add a new `## ferro-rs` top-level section at the top of `CHANGELOG.md` (above `## ferro-stripe`) with a `### [0.2.13] — 2026-04-21` entry. The entry MUST be written in **NEUTRAL voice** — per the project-instructions override: no "gestiscilo-it" mention; frame as "production field application" or "integration field report". This overrides the original D-13 wording.

3. **Version bump:** `/Cargo.toml` line 27: `version = "0.2.12"` → `version = "0.2.13"`. All workspace crates inherit via `version.workspace = true`. `cargo build` regenerates `Cargo.lock` automatically.

4. **Final gate:** `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` must exit 0 before this plan is considered complete.

Purpose: Phase 144 ships as a patch release. Docs reflect the new semantics; the changelog informs downstream consumers of the fix motivation in neutral, publishable language; the version bump is the signal crates.io publish automation needs (MEMORY: `project_ferro_publish_token_scoping.md` — publish-update token covers existing-crate version bumps automatically, no new-crate bootstrap needed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-fix-root-path-routing-in-group-routes/144-CONTEXT.md
@.planning/phases/144-fix-root-path-routing-in-group-routes/144-RESEARCH.md
@.planning/phases/144-fix-root-path-routing-in-group-routes/144-PATTERNS.md
@docs/src/the-basics/routing.md
@docs/src/the-basics/middleware.md
@framework/src/routing/macros.rs
@CHANGELOG.md
@Cargo.toml
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/src/the-basics/routing.md with root-in-group subsection</name>
  <files>docs/src/the-basics/routing.md</files>
  <read_first>
    - docs/src/the-basics/routing.md lines 40-80 (the "Route Groups" section at line 47 and the following "Named Routes" section at line 68 — insertion point is between them)
    - .planning/phases/144-fix-root-path-routing-in-group-routes/144-PATTERNS.md §9 (exact prose conventions: sentence-case, no emojis, ```rust fences, no marketing voice)
  </read_first>
  <action>
Open `docs/src/the-basics/routing.md`. The current "Route Groups" section ends around line 66 with the closing ``` of a code example; the "## Named Routes" heading is at line 68.

Insert the following NEW subsection immediately after line 66 (after the closing ``` of the existing group example, before the `## Named Routes` heading). Use ```rust code fences (not `rust,ignore`), sentence-case prose, no emojis, scientific tone:

    ### Root routes inside a group

    A `"/"` route inside a non-root group matches both the bare prefix and the
    prefix with a trailing slash. Other paths concatenate normally.

    ```rust
    Router::new()
        .group("/s/{slug}", |r| {
            r.get("/", show_item)        // matches /s/foo AND /s/foo/
             .get("/edit", edit_item)    // matches /s/foo/edit
        })
    ```

    A trailing slash on the group prefix is stripped before concatenation, so
    `group("/api/", ...)` with a child `/x` route produces `/api/x`, not
    `/api//x`. Route introspection via `get_registered_routes()` and
    `ferro-mcp list_routes` reports one entry per logical handler — the
    canonical path without trailing slash.

Do NOT modify anything else in this file. Confirm the existing "## Route Groups" heading (line 47) and "## Named Routes" heading (line 68) are unchanged, with your new `### Root routes inside a group` subsection nested under "## Route Groups".
  </action>
  <verify>
    <automated>grep -c "Root routes inside a group" docs/src/the-basics/routing.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Root routes inside a group" docs/src/the-basics/routing.md` returns `1`
    - `grep -c "matches /s/foo AND /s/foo/" docs/src/the-basics/routing.md` returns `1`
    - `grep -c "/api/x, not" docs/src/the-basics/routing.md` returns `1`
    - `grep -c "## Route Groups" docs/src/the-basics/routing.md` returns `1` (unchanged)
    - `grep -c "## Named Routes" docs/src/the-basics/routing.md` returns `1` (unchanged)
    - `grep -n "### Root routes inside a group" docs/src/the-basics/routing.md` reports a line number strictly between the line of "## Route Groups" and the line of "## Named Routes"
  </acceptance_criteria>
  <done>
    The new subsection is present, nested under "## Route Groups", before "## Named Routes"; existing content unchanged; tone is scientific and minimalistic per CLAUDE.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add group-middleware invariant note to docs/src/the-basics/middleware.md + update rustdoc in macros.rs</name>
  <files>docs/src/the-basics/middleware.md, framework/src/routing/macros.rs</files>
  <read_first>
    - docs/src/the-basics/middleware.md lines 170-200 (locate the group middleware example around lines 179-191 and the "## Middleware Execution Order" heading around line 194)
    - framework/src/routing/macros.rs lines 590-610 (the existing rustdoc "Path Combination" block on GroupDef::register at lines 596-606 — to be rewritten)
    - .planning/phases/144-fix-root-path-routing-in-group-routes/144-PATTERNS.md §10 and §11 (exact prose for both updates)
  </read_first>
  <action>
TWO EDITS.

EDIT A — `docs/src/the-basics/middleware.md`: Locate the existing group-middleware example block that ends at or near line 191, immediately before the `## Middleware Execution Order` heading (near line 194). Insert the following single-paragraph note on the line immediately before that heading:

    Middleware attached to a group applies uniformly to root-path routes inside
    the group, covering both `/prefix` and `/prefix/` variants.

Keep the insertion to a single paragraph. Do not modify other parts of the file.

EDIT B — `framework/src/routing/macros.rs`: Locate lines 596-606, the existing rustdoc block on `pub fn register` of `GroupDef`:

    /// # Path Combination
    ///
    /// - If route path is "/" (root), the full path is just the group prefix
    /// - Otherwise, prefix and route path are concatenated
    ///
    /// # Middleware Inheritance
    ///
    /// Parent group middleware is applied before child group middleware,
    /// which is applied before route-specific middleware.

Replace the `# Path Combination` section (leave `# Middleware Inheritance` and anything after it unchanged) with the new three-rule description:

    /// # Path Combination
    ///
    /// - If the route path is `"/"` and the accumulated group prefix is
    ///   non-empty, the handler is registered under both `/prefix` and
    ///   `/prefix/` so it is reachable at either URL. Route introspection
    ///   and named-route lookup still report one canonical entry at
    ///   `/prefix` (no trailing slash).
    /// - A single trailing `/` on the group prefix is stripped before
    ///   concatenation, so `group!("/api/", { get!("/x", ...) })` produces
    ///   `/api/x` rather than `/api//x`.
    /// - A root prefix `"/"` combined with a root route `"/"` stays `"/"`
    ///   (no `"//"` is ever registered).

Keep the scientific/minimalistic tone per CLAUDE.md. Do not add marketing language.
  </action>
  <verify>
    <automated>grep -c "applies uniformly to root-path routes" docs/src/the-basics/middleware.md && grep -c "reachable at either URL" framework/src/routing/macros.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "applies uniformly to root-path routes" docs/src/the-basics/middleware.md` returns `1`
    - `grep -c "## Middleware Execution Order" docs/src/the-basics/middleware.md` returns `1` (existing heading preserved)
    - `grep -c "reachable at either URL" framework/src/routing/macros.rs` returns `1`
    - `grep -c "A single trailing .*/.* on the group prefix is" framework/src/routing/macros.rs` returns at least `1`
    - `grep -c "the full path is just the group prefix" framework/src/routing/macros.rs` returns `0` (old wording gone)
    - `grep -c "# Middleware Inheritance" framework/src/routing/macros.rs` returns `1` (unchanged section preserved)
    - `cargo doc -p ferro-rs --no-deps` succeeds with no warnings (rustdoc still parses cleanly)
  </acceptance_criteria>
  <done>
    middleware.md has the single-sentence invariant note before "## Middleware Execution Order"; macros.rs has the updated three-rule Path Combination rustdoc block; the Middleware Inheritance section is untouched; `cargo doc` is clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write CHANGELOG.md entry in neutral voice</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md lines 1-30 (existing format: top-level `## <crate>` headings with nested `### [version] — date` subheaders; ferro-stripe precedent at lines 6-8)
    - CLAUDE.md global rules: "## Repository documents must read as neutral" (explicit override on D-13)
    - .planning/phases/144-fix-root-path-routing-in-group-routes/144-PATTERNS.md §8 (changelog format analog and neutral voice guidance)
  </read_first>
  <action>
Open `CHANGELOG.md`. The current file begins:

    # Changelog

    All notable changes to Ferro crates are documented here. Format loosely follows
    [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

    ## ferro-stripe

    ### [0.4.0] — 2026-04-20

    ...

Insert the following NEW `## ferro-rs` section immediately after the `# Changelog` header + intro paragraph and BEFORE the existing `## ferro-stripe` section. The new section must use NEUTRAL voice — do NOT name any downstream project (this overrides the original D-13 wording per the global CLAUDE.md "Repository documents must read as neutral" rule; reference the phase source neutrally as "a production field report" or "an integration field application"):

    ## ferro-rs

    ### [0.2.13] — 2026-04-21

    Bug fix: `get!("/", ...)` registered inside `group!("/prefix", { ... })`
    is now reachable at both `/prefix` and `/prefix/`. Previously only
    `/prefix/index.html`-style non-root paths matched; the trailing-slash
    variant of the root-in-group case returned 404. Discovered via a
    production field application that routes under `/s/{slug}/`.

    #### Fixed

    - Group path combination in both `GroupDef::register_with_inherited`
      (the macro-based `group!`) and `GroupBuilder::finalize` (the
      builder-based `Router::group`) now registers a leaf `get!("/", ...)`
      under both `/prefix` and `/prefix/`. A trailing slash on the group
      prefix is also correctly stripped, so `group!("/api/", { get!("/x", ...) })`
      produces `/api/x`, not `/api//x`.
    - Nested-group prefix accumulation strips a trailing slash on the
      parent prefix before concatenating the child prefix, so
      `group!("/a/", { group!("/b", { get!("/", h) }) })` accumulates to
      `/a/b` rather than `/a//b`.

    #### Unchanged

    - Top-level (non-grouped) `get!("/", ...)` behavior.
    - Route introspection: `get_registered_routes()` and
      `ferro-mcp list_routes` still show one entry per logical handler —
      the canonical path without trailing slash.
    - Named-route resolution: `route("foo", &[])` returns the canonical
      path.
    - Middleware attached to grouped routes fires for both trailing-slash
      variants.

CRITICAL CONSTRAINT from project_instructions and CLAUDE.md:

- Do NOT write "gestiscilo-it" anywhere in this entry.
- Do NOT name any specific downstream project.
- Do NOT add internal-voice phrases like "killer feature", "we're betting on", "the load-bearing weakness", etc.
- The phrase "production field application" OR "integration field report" is the approved neutral framing — pick one.
- Do NOT amend the CHANGELOG format: the existing file uses `## <crate>` top-level headings; the new entry must be `## ferro-rs` (matching the crates.io name from framework/Cargo.toml line 2) at that same heading level.
  </action>
  <verify>
    <automated>grep -c "^## ferro-rs$" CHANGELOG.md && grep -c "^### \[0.2.13\] — 2026-04-21$" CHANGELOG.md && ! grep -i "gestiscilo" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## ferro-rs$" CHANGELOG.md` returns `1`
    - `grep -c "^### \[0.2.13\] — 2026-04-21$" CHANGELOG.md` returns `1`
    - `grep -ci "gestiscilo" CHANGELOG.md` returns `0` (NEUTRAL voice — downstream project not named)
    - `grep -c "production field application\|integration field report" CHANGELOG.md` returns at least `1`
    - `grep -c "^## ferro-stripe$" CHANGELOG.md` returns `1` (existing section preserved)
    - The new `## ferro-rs` section appears BEFORE `## ferro-stripe` in the file (verify via `grep -n "^## " CHANGELOG.md` — first `## ferro-rs` line number < first `## ferro-stripe` line number)
    - `grep -c "#### Fixed" CHANGELOG.md` returns at least `1` (Keep-a-Changelog semantic heading present)
    - `grep -c "#### Unchanged" CHANGELOG.md` returns at least `1`
  </acceptance_criteria>
  <done>
    CHANGELOG.md has a new `## ferro-rs` section above `## ferro-stripe` with a `### [0.2.13] — 2026-04-21` entry; the entry is written in neutral voice (no "gestiscilo" mention); Fixed and Unchanged subsections describe the bug, the two code paths fixed, nested prefix accumulation, and the invariants preserved.
  </done>
</task>

<task type="auto">
  <name>Task 4: Bump workspace version 0.2.12 → 0.2.13 and run the full gate</name>
  <files>Cargo.toml, Cargo.lock</files>
  <read_first>
    - Cargo.toml lines 20-30 (the [workspace.package] block, specifically line 27 with `version = "0.2.12"`)
    - framework/Cargo.toml lines 1-10 (confirms `version.workspace = true` — the bump propagates automatically)
    - CLAUDE.md "Testing & Linting" section (the exact pre-commit command — `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features`)
    - MEMORY: `project_ferro_publish_token_scoping.md` (publish-update token handles existing-crate bumps; no new crates added in this phase, so no bootstrap needed)
  </read_first>
  <action>
TWO ACTIONS.

ACTION A: Edit `Cargo.toml` at line 27. The current line is:

    version = "0.2.12"

Change to:

    version = "0.2.13"

Do NOT modify any other line. Every workspace member crate has `version.workspace = true` (confirmed via `framework/Cargo.toml` line 3 and all other member Cargo.toml files), so this single-line change propagates the bump workspace-wide.

ACTION B: Run the full workspace gate to regenerate `Cargo.lock` and confirm everything still compiles and passes tests:

    cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features

This command MUST exit 0. `-D warnings` is CI-enforced (MEMORY: `feedback_ci_clippy_command_match.md` — match the exact CI command, not a lighter local variant). If any step fails:

- `cargo fmt --all -- --check` fails: run `cargo fmt --all` and investigate why the source wasn't pre-formatted.
- `cargo clippy` fails: address the warnings — do NOT use `#[allow(...)]` to silence them unless the warning is a known false positive documented elsewhere in the codebase.
- `cargo test --all-features` fails: a test in a crate unrelated to routing is broken by the version bump — investigate (highly unlikely, as `version.workspace = true` is inherited cleanly).

`Cargo.lock` will be automatically updated by `cargo build` / `cargo test` — do not hand-edit.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `grep "^version = " Cargo.toml | head -1` returns `version = "0.2.13"` exactly (the workspace.package.version at line 27)
    - `grep -c "version = \"0.2.12\"" Cargo.toml` returns `0` (old version gone)
    - `grep -c "version.workspace = true" framework/Cargo.toml` returns `1` (inheritance mechanism confirmed)
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy --all --all-targets -- -D warnings` exits 0 with no new warnings
    - `cargo test --all-features` exits 0
    - `cargo test -p ferro-rs --lib routing::path::tests::combine_group_path_matrix` exits 0 (sanity check that Plan 01's helper still works)
    - `cargo test -p ferro-rs --test routing_group_trailing_slash` exits 0 (Plan 04's integration tests still pass)
    - `Cargo.lock` is updated: `grep -A1 "name = \"ferro-rs\"" Cargo.lock | grep "0.2.13"` returns at least one match
  </acceptance_criteria>
  <done>
    Workspace version is 0.2.13, Cargo.lock reflects the bump, and the full workspace gate (`cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features`) exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan modifies documentation, changelog, and a version string — no executable code changes. No new trust boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-41 | Information Disclosure | Repository-public changelog | **mitigate** | The changelog entry is written in NEUTRAL voice per the CLAUDE.md "Repository documents must read as neutral" rule. Explicit acceptance criterion: `grep -ci "gestiscilo" CHANGELOG.md` returns `0`. This prevents leaking internal downstream-project identity into a public git-tracked artifact. Override of D-13 is documented inline in the task action. |
| T-144-42 | Tampering / Version mismatch | Cargo.lock | mitigate | The version bump is applied only to `Cargo.toml` line 27; `Cargo.lock` is regenerated by `cargo build`/`cargo test`. Hand-editing `Cargo.lock` is explicitly prohibited in the task action. Acceptance criterion asserts the ferro-rs entry in Cargo.lock reflects 0.2.13. |
| T-144-43 | Tampering / Accidental feature breakage | Full workspace test run | mitigate | Final acceptance criterion runs the full CI-equivalent gate (`fmt --check && clippy --all --all-targets -D warnings && test --all-features`). Plan 04's integration tests plus Plan 02/03's unit tests are re-verified as part of this gate. |
</threat_model>

<verification>
- Routing.md has the new subsection: `grep -c "Root routes inside a group" docs/src/the-basics/routing.md` == 1
- Middleware.md has the invariant note: `grep -c "applies uniformly to root-path routes" docs/src/the-basics/middleware.md` == 1
- macros.rs rustdoc updated: `grep -c "reachable at either URL" framework/src/routing/macros.rs` == 1
- Changelog neutral voice: `grep -ci "gestiscilo" CHANGELOG.md` == 0
- Changelog entry exists: `grep -c "^### \[0.2.13\] — 2026-04-21$" CHANGELOG.md` == 1
- Workspace version is 0.2.13: `grep "^version = \"0.2.13\"$" Cargo.toml` returns exactly 1 match
- Full gate passes: `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` exits 0
- Rustdoc clean: `cargo doc -p ferro-rs --no-deps` exits 0 without warnings
</verification>

<success_criteria>
Phase 144 ships when:

1. docs/src/the-basics/routing.md documents the dual-variant root-in-group semantics with a working example.
2. docs/src/the-basics/middleware.md notes the Strategy A invariant.
3. framework/src/routing/macros.rs rustdoc "Path Combination" block reflects the three new rules.
4. CHANGELOG.md has a `## ferro-rs` section with a 0.2.13 entry written in neutral voice (no "gestiscilo" mention).
5. Cargo.toml line 27 is `version = "0.2.13"` and Cargo.lock is regenerated.
6. `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` exits 0.

After this plan merges, the phase is ready for `cargo publish` automation to pick up the version bump via `.github/workflows/publish.yml`.
</success_criteria>

<output>
After completion, create `.planning/phases/144-fix-root-path-routing-in-group-routes/144-05-SUMMARY.md` documenting: (a) exact doc diffs applied, (b) the final CHANGELOG entry text (pasted verbatim) for auditability of the neutral-voice compliance, (c) the `cargo test --all-features` final count (total tests run, passed), (d) confirmation that Cargo.lock was regenerated automatically (not hand-edited).
</output>
