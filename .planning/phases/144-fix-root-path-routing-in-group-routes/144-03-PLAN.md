---
phase: 144
plan: 03
type: execute
wave: 1
depends_on: [144-01]
files_modified:
  - framework/src/routing/group.rs
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-11]
tags: [routing, builder-api, group, alias]

must_haves:
  truths:
    - "Router::new().group('/api', |r| r.get('/', h)) reaches h at both /api and /api/ (D-01 via D-05)"
    - "Router::new().group('/', |r| r.get('/', h)) registers exactly one / route (D-02)"
    - "Router::new().group('/api/', |r| r.get('/x', h)) produces /api/x, not /api//x (D-03)"
    - "Router::new().group('/api', |r| r.get('/users', h)) still produces /api/users (D-04 regression)"
    - "Middleware attached to a group reaches both URL variants via Strategy A (matchit alias leaf carries canonical pattern)"
    - "GroupBuilder::finalize and GroupDef::register_with_inherited produce equivalent registered routes for equivalent inputs (D-05, D-11 lockstep invariant)"
  artifacts:
    - path: framework/src/routing/group.rs
      provides: "GroupBuilder::finalize using combine_group_path + alias inserts + matching test module"
      contains: "use super::path::combine_group_path"
  key_links:
    - from: framework/src/routing/group.rs
      to: framework/src/routing/path.rs
      via: "use super::path::combine_group_path;"
      pattern: "use super::path::combine_group_path"
    - from: framework/src/routing/group.rs
      to: framework/src/routing/router.rs
      via: "self.outer_router.insert_{method}_alias(alt, handler, canonical)"
      pattern: "insert_(get|post|put|delete)_alias"
---

<objective>
Apply the Phase 144 fix to the builder-based `Router::group(prefix, |r| ...)` surface by updating `framework/src/routing/group.rs::GroupBuilder::finalize` (lines 62-91) to call `combine_group_path` and emit canonical + optional alias pairs. Add a new inline `#[cfg(test)] mod tests` mirroring the matrix from Plan 02 (D-11: both implementations are locked in sync by equivalent tests).

`group.rs` only supports GET / POST / PUT / DELETE (see the `GroupMethod` enum at lines 38-44 — no PATCH), so only four of the five alias methods from Plan 02 are wired here.

Purpose: Restore the framework invariant "both group APIs produce the same registered routes for equivalent definitions." The two implementations have drifted; mirrored tests here and in Plan 02 catch future drift.

Output: `group.rs::GroupBuilder::finalize` uses the helper from Plan 01 and the alias methods from Plan 02, and a new inline test module with the same matrix as Plan 02 (adapted to the builder API).

This plan runs in parallel with Plan 02 (both depend only on Plan 01; they touch different files with zero overlap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-fix-root-path-routing-in-group-routes/144-CONTEXT.md
@.planning/phases/144-fix-root-path-routing-in-group-routes/144-RESEARCH.md
@.planning/phases/144-fix-root-path-routing-in-group-routes/144-PATTERNS.md
@framework/src/routing/path.rs
@framework/src/routing/group.rs
@framework/src/routing/router.rs
@framework/src/routing/macros.rs

<interfaces>
<!-- combine_group_path signature (Plan 01): -->
`pub(crate) fn combine_group_path(prefix: &str, route_path: &str) -> (String, Option<String>);`

<!-- Plan 02 introduces five alias methods on Router. group.rs uses four (no PATCH in GroupMethod): -->
- `pub(crate) fn insert_get_alias(&mut self, alias_path: &str, handler: Arc<BoxedHandler>, canonical_path: &str)`
- `pub(crate) fn insert_post_alias(&mut self, alias_path: &str, handler: Arc<BoxedHandler>, canonical_path: &str)`
- `pub(crate) fn insert_put_alias(&mut self, alias_path: &str, handler: Arc<BoxedHandler>, canonical_path: &str)`
- `pub(crate) fn insert_delete_alias(&mut self, alias_path: &str, handler: Arc<BoxedHandler>, canonical_path: &str)`

<!-- GroupBuilder holds String-owned paths (lines 20-36). Unlike macros.rs, no Box::leak is needed — insert_* and add_middleware take &str, and local Strings live long enough for the call. The handler is already Arc<BoxedHandler> (line 35) so .clone() is cheap Arc refcount bump. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite GroupBuilder::finalize using combine_group_path + alias inserts; add mirrored test module</name>
  <files>framework/src/routing/group.rs</files>
  <read_first>
    - framework/src/routing/group.rs (full file, 215 lines — current finalize at 62-91; GroupBuilder / GroupRoute / GroupMethod definitions at 20-44; Router::group + From<GroupBuilder> at 166-203)
    - framework/src/routing/path.rs (Plan 01 output — the helper being imported)
    - framework/src/routing/router.rs (Plan 02 output — the alias methods being invoked)
    - framework/src/routing/macros.rs lines 1178-1224 (test module convention: `use super::*;`, `async fn test_handler`, synchronous `#[test]` with `router.match_route(&Method::GET, path)`)
    - framework/src/middleware/mod.rs (to confirm the Middleware trait shape before writing the CounterMiddleware test — match existing ferro convention, not async_trait if the project does not use it)
    - .planning/phases/144-fix-root-path-routing-in-group-routes/144-PATTERNS.md §5 (exact new-shape description for finalize + import placement + test layout)
    - .planning/phases/144-fix-root-path-routing-in-group-routes/144-RESEARCH.md §Pattern 2 Strategy A (middleware under canonical-only key)
    - .planning/phases/144-fix-root-path-routing-in-group-routes/144-CONTEXT.md §D-05 and §D-11 (lockstep requirement with macros.rs)
  </read_first>
  <behavior>
    - Test `builder_group_root_handler_matches_both_variants`: `Router::new().group("/api", |r| r.get("/", test_handler))` reaches `test_handler` at both `/api` and `/api/`; `get_registered_routes()` delta is exactly 1; the alternate leaf carries canonical pattern `"/api"` (D-01 via D-05)
    - Test `builder_root_prefix_root_handler_is_single_slash`: `Router::new().group("/", |r| r.get("/", test_handler))` matches `/` with pattern `"/"`; delta == 1 (D-02)
    - Test `builder_trailing_slash_prefix_is_stripped`: `Router::new().group("/api/", |r| r.get("/x", test_handler))` matches `/api/x`; does NOT match `/api//x` (D-03)
    - Test `builder_non_root_prefix_non_root_path_unchanged`: `Router::new().group("/api", |r| r.get("/users", test_handler))` matches `/api/users`; delta == 1; no alternate emitted (D-04 regression)
    - Test `builder_middleware_registered_under_canonical_only`: confirms `get_route_middleware("/api")` returns the registered middleware vec and `get_route_middleware("/api/")` returns empty — proves Strategy A is in force (one canonical HashMap key covers both leaves because the alias matchit value stores the canonical pattern)
    - Test `builder_post_and_put_and_delete_aliases_reach_handler`: verifies all four non-GET/GET verbs' alias paths are reachable at both `/api` and `/api/`
  </behavior>
  <action>
Edit `framework/src/routing/group.rs` with three changes.

CHANGE 1: Add helper import. At the top of the file, between `use super::{BoxedHandler, RouteBuilder, Router};` and `use crate::http::{Request, Response};`, insert:

    use super::path::combine_group_path;

Final import block should be (alphabetical within each source group — `super` first, then `crate`, then `std`):

    //! Route grouping with shared prefix and middleware

    use super::path::combine_group_path;
    use super::{BoxedHandler, RouteBuilder, Router};
    use crate::http::{Request, Response};
    use crate::middleware::{into_boxed, BoxedMiddleware, Middleware};
    use std::future::Future;
    use std::sync::Arc;

CHANGE 2: Replace the `finalize` function body (current lines 62-91).

Current code (the exact block to REPLACE):

    /// Finalize the group and merge routes into the outer router
    fn finalize(mut self) -> Router {
        // Insert all group routes into the outer router with the prefix
        for route in self.group_routes {
            let full_path = format!("{}{}", self.prefix, route.path);

            // Insert into the appropriate method router using public(crate) methods
            match route.method {
                GroupMethod::Get => {
                    self.outer_router.insert_get(&full_path, route.handler);
                }
                GroupMethod::Post => {
                    self.outer_router.insert_post(&full_path, route.handler);
                }
                GroupMethod::Put => {
                    self.outer_router.insert_put(&full_path, route.handler);
                }
                GroupMethod::Delete => {
                    self.outer_router.insert_delete(&full_path, route.handler);
                }
            }

            // Apply group middleware to each route
            for mw in &self.middleware {
                self.outer_router.add_middleware(&full_path, mw.clone());
            }
        }

        self.outer_router
    }

Replacement code:

    /// Finalize the group and merge routes into the outer router.
    ///
    /// Uses `combine_group_path` to compute the canonical registration path
    /// (and an optional trailing-slash alternate), so `r.get("/", h)` inside
    /// a non-root group reaches `h` at both `/prefix` and `/prefix/`. The
    /// alternate matchit leaf stores the canonical pattern string, which is
    /// what `server.rs` uses as the middleware-lookup key — so one
    /// `add_middleware(&canonical, ...)` call covers both variants
    /// (Strategy A).
    fn finalize(mut self) -> Router {
        for route in self.group_routes {
            let (canonical, alternate) = combine_group_path(&self.prefix, &route.path);

            match route.method {
                GroupMethod::Get => {
                    self.outer_router
                        .insert_get(&canonical, route.handler.clone());
                    if let Some(alt) = alternate.as_deref() {
                        self.outer_router
                            .insert_get_alias(alt, route.handler, &canonical);
                    }
                }
                GroupMethod::Post => {
                    self.outer_router
                        .insert_post(&canonical, route.handler.clone());
                    if let Some(alt) = alternate.as_deref() {
                        self.outer_router
                            .insert_post_alias(alt, route.handler, &canonical);
                    }
                }
                GroupMethod::Put => {
                    self.outer_router
                        .insert_put(&canonical, route.handler.clone());
                    if let Some(alt) = alternate.as_deref() {
                        self.outer_router
                            .insert_put_alias(alt, route.handler, &canonical);
                    }
                }
                GroupMethod::Delete => {
                    self.outer_router
                        .insert_delete(&canonical, route.handler.clone());
                    if let Some(alt) = alternate.as_deref() {
                        self.outer_router
                            .insert_delete_alias(alt, route.handler, &canonical);
                    }
                }
            }

            // Strategy A: add middleware under the canonical key only — the
            // alias leaf's matchit value stores the canonical pattern string,
            // so dispatch resolves middleware under this same key regardless
            // of which URL variant the request hit.
            for mw in &self.middleware {
                self.outer_router.add_middleware(&canonical, mw.clone());
            }
        }

        self.outer_router
    }

CHANGE 3: Append a new `#[cfg(test)] mod tests` block at the end of the file (after the closing brace of `impl RouteBuilder { ... }` that ends around line 214).

Template (inspect and adapt to match actual ferro conventions — see notes below):

    #[cfg(test)]
    mod tests {
        use super::*;
        use hyper::Method;
        use crate::routing::get_registered_routes;

        async fn test_handler(_req: Request) -> Response {
            crate::http::text("ok")
        }

        #[test]
        fn builder_group_root_handler_matches_both_variants() {
            let before = get_registered_routes().len();
            let router: Router = Router::new()
                .group("/api", |r| r.get("/", test_handler))
                .into();
            let after = get_registered_routes().len();

            assert_eq!(after - before, 1, "expected exactly 1 RouteInfo entry");

            let hit_canonical = router.match_route(&Method::GET, "/api");
            assert!(hit_canonical.is_some());
            assert_eq!(hit_canonical.unwrap().2, "/api");

            let hit_alternate = router.match_route(&Method::GET, "/api/");
            assert!(hit_alternate.is_some());
            assert_eq!(
                hit_alternate.unwrap().2,
                "/api",
                "alternate leaf must carry canonical pattern for middleware lookup"
            );
        }

        #[test]
        fn builder_root_prefix_root_handler_is_single_slash() {
            let before = get_registered_routes().len();
            let router: Router = Router::new()
                .group("/", |r| r.get("/", test_handler))
                .into();
            let after = get_registered_routes().len();

            assert_eq!(after - before, 1);
            assert!(router.match_route(&Method::GET, "/").is_some());
        }

        #[test]
        fn builder_trailing_slash_prefix_is_stripped() {
            let router: Router = Router::new()
                .group("/api/", |r| r.get("/x", test_handler))
                .into();

            assert!(router.match_route(&Method::GET, "/api/x").is_some());
            assert!(
                router.match_route(&Method::GET, "/api//x").is_none(),
                "unexpected match for /api//x — trailing slash not stripped"
            );
        }

        #[test]
        fn builder_non_root_prefix_non_root_path_unchanged() {
            let before = get_registered_routes().len();
            let router: Router = Router::new()
                .group("/api", |r| r.get("/users", test_handler))
                .into();
            let after = get_registered_routes().len();

            assert_eq!(after - before, 1);
            assert!(router.match_route(&Method::GET, "/api/users").is_some());
            assert!(
                router.match_route(&Method::GET, "/api/users/").is_none(),
                "non-root leaf must not emit an alternate"
            );
        }

        #[test]
        fn builder_middleware_registered_under_canonical_only() {
            // This confirms Strategy A at the registry level without needing
            // a full dispatch: one add_middleware call lands under the
            // canonical key; the alias leaf has no separate HashMap entry.
            // The integration test in Plan 04 proves both URL variants
            // actually execute the middleware.
            use crate::middleware::{into_boxed, Middleware};

            // Use a trivial pre-existing middleware type from the codebase,
            // or a minimal inline test impl of the Middleware trait — match
            // the shape used in framework/src/middleware/mod.rs.
            // If no simple middleware type is available for tests, rely on
            // the integration test in Plan 04 and remove this test from the
            // builder-level mod.
            let router: Router = Router::new()
                .group("/api", |r| r.get("/", test_handler))
                .into();

            let canonical_mw = router.get_route_middleware("/api");
            // Group has no middleware — both should be empty. The structural
            // assertion here is that the lookup API works and returns the
            // correct (empty) vec; the middleware-presence test is in Plan 04.
            assert!(canonical_mw.is_empty());
            assert!(router.get_route_middleware("/api/").is_empty());
        }

        #[test]
        fn builder_post_and_put_and_delete_aliases_reach_handler() {
            let router_post: Router = Router::new()
                .group("/api", |r| r.post("/", test_handler))
                .into();
            assert!(router_post.match_route(&Method::POST, "/api").is_some());
            assert!(router_post.match_route(&Method::POST, "/api/").is_some());

            let router_put: Router = Router::new()
                .group("/api", |r| r.put("/", test_handler))
                .into();
            assert!(router_put.match_route(&Method::PUT, "/api").is_some());
            assert!(router_put.match_route(&Method::PUT, "/api/").is_some());

            let router_delete: Router = Router::new()
                .group("/api", |r| r.delete("/", test_handler))
                .into();
            assert!(
                router_delete.match_route(&Method::DELETE, "/api").is_some()
            );
            assert!(
                router_delete.match_route(&Method::DELETE, "/api/").is_some()
            );
        }
    }

NOTES FOR THE EXECUTOR:

1. The `Method` type is `hyper::Method` — `router.match_route(&hyper::Method, &str)` per `framework/src/routing/router.rs` lines 363–373 (`hyper::Method` used in the match arms `hyper::Method::GET`, `hyper::Method::POST`, etc.). The `Method` enum in `framework/src/routing/router.rs` line 154 is a PRIVATE `enum Method { Get, Post, ... }` for internal `RouteBuilder._last_method` bookkeeping and is NOT what external tests use. Import via `use hyper::Method;` and use the SCREAMING-CASE variants `Method::GET`, `Method::POST`, `Method::PUT`, `Method::DELETE`.

2. Do NOT write a middleware-execution test here that depends on an async trait shape you have not confirmed. The real middleware-runs-on-both-variants assertion lives in the Plan 04 integration test, which has access to the full dispatch path. The `builder_middleware_registered_under_canonical_only` test above asserts registry-level structure only.

3. Use `get_registered_routes().len()` deltas (not absolute counts) because `REGISTERED_ROUTES` is a process-global `OnceLock<RwLock<Vec<...>>>` shared across tests within the same process.

4. If `Router::get_route_middleware` is not `pub` at the time you run this task, either (a) confirm it became `pub` in Plan 02 by reading router.rs, or (b) fall back to a simpler registry-only assertion via the returned Router's public API.

5. The test module is a new addition (group.rs currently has zero tests per PATTERNS.md §5). Place it at the very bottom of the file, after the last `}` of the `impl RouteBuilder { ... }` block.
  </action>
  <verify>
    <automated>cargo test -p ferro-rs --lib routing::group::tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "use super::path::combine_group_path" framework/src/routing/group.rs` returns `1`
    - `grep -c "combine_group_path(&self.prefix, &route.path)" framework/src/routing/group.rs` returns `1`
    - `grep -c "insert_get_alias\|insert_post_alias\|insert_put_alias\|insert_delete_alias" framework/src/routing/group.rs` returns `4`
    - `grep -c 'format!("{}{}", self.prefix, route.path)' framework/src/routing/group.rs` returns `0` (old bug site is gone)
    - `grep -c "add_middleware(&canonical, mw.clone())" framework/src/routing/group.rs` returns `1` (Strategy A: canonical-only middleware key)
    - `grep -c "add_middleware(&full_path" framework/src/routing/group.rs` returns `0` (old full_path reference removed)
    - `grep -c "^#\[cfg(test)\]" framework/src/routing/group.rs` returns `1` (new test module present)
    - `cargo test -p ferro-rs --lib routing::group::tests::builder_group_root_handler_matches_both_variants` exits 0
    - `cargo test -p ferro-rs --lib routing::group::tests::builder_root_prefix_root_handler_is_single_slash` exits 0
    - `cargo test -p ferro-rs --lib routing::group::tests::builder_trailing_slash_prefix_is_stripped` exits 0
    - `cargo test -p ferro-rs --lib routing::group::tests::builder_non_root_prefix_non_root_path_unchanged` exits 0
    - `cargo test -p ferro-rs --lib routing::group::tests::builder_post_and_put_and_delete_aliases_reach_handler` exits 0
    - `cargo clippy -p ferro-rs --all-targets -- -D warnings` produces no new warnings
  </acceptance_criteria>
  <done>
    `GroupBuilder::finalize` uses `combine_group_path`, emits canonical + optional alias pairs across GET/POST/PUT/DELETE, registers middleware under the canonical key only, and the new test module mirrors the Plan 02 matrix (D-11 lockstep).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Identical scope to Plan 02: this plan modifies a second, builder-based code path that ultimately shares the same matchit dispatch and middleware lookup as `macros.rs`. Trust boundaries are the same.

| Boundary | Description |
|----------|-------------|
| HTTP request → matchit dispatch | Untrusted request path crosses into `router.match_route(&method, path)`. |
| matchit leaf → middleware lookup | Internal boundary at `server.rs:260`. The matchit value tuple's second element (`route_pattern`) keys `router.get_route_middleware(&route_pattern)`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-21 | Elevation of Privilege / Middleware bypass | Builder-API alias leaf middleware lookup | **mitigate** | `insert_*_alias` calls pass the canonical path as the third argument (verifiable via `grep "insert_.*_alias" framework/src/routing/group.rs | grep "&canonical"` returns 4 matches). Request arriving at `/api/` matches the alias leaf, whose matchit value carries `"/api"`, so `server.rs:260` looks up middleware under `/api` — which was registered by the `add_middleware(&canonical, mw.clone())` call. **Blocking severity.** Plan 04 integration test `middleware_runs_for_both_variants` proves this at the dispatch level. |
| T-144-22 | Information Disclosure / RouteInfo leak | Duplicate introspection entries via builder API | **mitigate** | `insert_*_alias` methods (Plan 02) do not call `register_route`. Acceptance criterion in Plan 02 Task 1 confirms this. The Plan 03 unit test `builder_group_root_handler_matches_both_variants` also asserts `get_registered_routes().len()` delta == 1 as an independent check of the invariant on the builder surface. |
| T-144-23 | Tampering | `combine_group_path` on builder inputs | accept | Same as Plan 01 / Plan 02: inputs are compile-time-validated route paths starting with `/`. No runtime request data reaches the helper. |
</threat_model>

<verification>
- Compilation: `cargo build -p ferro-rs --lib`
- All new builder-level tests pass: `cargo test -p ferro-rs --lib routing::group::tests`
- No regressions in `macros.rs` tests (Plan 02's test suite): `cargo test -p ferro-rs --lib routing::macros::tests`
- Cross-implementation parity: the matrix in `routing::group::tests` passes the same invariants as the matrix in `routing::macros::tests` (D-05, D-11)
- No new clippy warnings: `cargo clippy -p ferro-rs --all-targets -- -D warnings`
</verification>

<success_criteria>
Plan 03 is complete when `GroupBuilder::finalize` uses the Plan-01 helper, emits canonical + optional alias pairs for all four builder-API HTTP verbs, registers middleware under the canonical key only (Strategy A), and the new mirrored test module passes every test with behavior equivalent to the matching `macros.rs` test cases from Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/144-fix-root-path-routing-in-group-routes/144-03-SUMMARY.md` documenting: (a) exact diffs applied to group.rs, (b) a parity table mapping each `routing::group::tests::builder_*` test to its `routing::macros::tests::*` counterpart (proving D-11 lockstep), (c) any test that was simplified (e.g. the middleware test reduced to registry-structure only) and why.
</output>
