---
phase: 160
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-json-ui/README.md
autonomous: true
requirements_addressed: [D-08, Pattern-6]
# NOTE: Phase 161 publish blocker — the README is rendered at the top of
# crates.io/crates/ferro-json-ui on next publish. Phase 161 publishes this crate.
# A non-compiling v1 example as the only Usage block is a first-impression bug.
must_haves:
  truths:
    - "ferro-json-ui/README.md Usage block uses the current public API (Spec::builder and JsonUi::render_file)"
    - "No JsonUiView, Component::, LayoutComponent::, ComponentNode, view.into_response() references remain in the README"
    - "Features list reflects the actual crate (41 built-in components, layout system + JSON Pointer data binding, $each/$if directives, plugin system)"
  artifacts:
    - path: "ferro-json-ui/README.md"
      provides: "Crates.io front-page Usage block matching the current v2 API"
      contains: "use ferro::{handler, JsonUi, Request, Response}; example + Spec::builder() example"
  key_links:
    - from: "ferro-json-ui/README.md Usage block"
      to: "ferro-json-ui/src/lib.rs:19-27 rustdoc example"
      via: "shape parity"
      pattern: "README example matches the verified-correct rustdoc example in lib.rs"
---

<objective>
Rewrite `ferro-json-ui/README.md` so the Usage block compiles against the current public API. The current README ships a v1 `JsonUiView { layout: LayoutComponent::Stack { ... } }` example that does not compile against the post-Phase-115 crate. This README is the first impression on `crates.io/crates/ferro-json-ui` after Phase 161 publishes.

Purpose: per RESEARCH Pattern 6 (a CONTEXT D-08 scope-expansion site), publish-blocker fix. The replacement Usage block uses the verified-correct examples from `ferro-json-ui/src/lib.rs:19-27` (Spec builder) and the framework `JsonUi::render_file` pattern from `ferro-cli/src/templates/make.rs:134-143`.

Output: README that accurately advertises the crate and serves as a valid copy-paste starting point.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-CONTEXT.md
@.planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-RESEARCH.md
@ferro-json-ui/README.md
@ferro-json-ui/src/lib.rs
@ferro-cli/src/templates/make.rs

<interfaces>
Verified-correct examples in source (paste-source for the README rewrite):

From ferro-json-ui/src/lib.rs:19-27 (existing rustdoc example):
    use ferro_json_ui::{Spec, Element};
    let spec = Spec::builder()
        .title("Demo")
        .element("root", Element::new("Text").prop("content", "Hi"))
        .build()
        .unwrap();

From ferro-cli/src/templates/make.rs (handler template):
    #[handler]
    pub async fn dashboard(req: Request) -> Response {
        let data = serde_json::json!({});
        JsonUi::render_file("views/dashboard.json", data)
    }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ferro-json-ui/README.md Usage block and Features list</name>
  <files>ferro-json-ui/README.md</files>
  <read_first>
    - ferro-json-ui/README.md (full file — current 52-line v1 example)
    - ferro-json-ui/src/lib.rs (lines 1-100 — verify current public API surface and rustdoc example)
    - .planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-RESEARCH.md (Pattern 6 — paste-ready replacement README)
  </read_first>
  <action>
Overwrite `ferro-json-ui/README.md` entirely with the verbatim Pattern 6 replacement from RESEARCH. The exact replacement body (no extra sections, no extra prose) is:

----- BEGIN replacement (literal markdown body to write to the file) -----

# ferro-json-ui

JSON-based server-driven UI schema types for the [Ferro](https://ferro-rs.dev) web framework.

Define UI as a JSON spec and have Ferro render it to HTML on the server — no
frontend build step required.

## Features

- 41 built-in components (Card, Form, DataTable, KanbanBoard, Modal, Tabs,
  Alert, Badge, Button, ...) and a plugin system for custom components
- Layout system (`dashboard`, `app`, `auth`) and ID-keyed element graph with
  parse-time structural validation
- Action system: navigate, submit form, call API, open modal
- Data binding via JSON Pointer (`{"$data": "/path"}`) and iteration directives
  (`$each`, `$if`)
- Compile-time schema validation via `schemars` on every typed `*Props`

## Usage

Serve a spec from a Rust handler:

```rust
use ferro::{handler, JsonUi, Request, Response};

#[handler]
pub async fn dashboard(req: Request) -> Response {
    let data = serde_json::json!({});
    JsonUi::render_file("views/dashboard.json", data)
}
```

Or construct a spec in Rust:

```rust
use ferro_json_ui::{Spec, Element};

let spec = Spec::builder()
    .title("Demo")
    .element("root", Element::new("Text").prop("content", "Hi"))
    .build()
    .unwrap();
```

## Documentation

Full documentation at [docs.ferro-rs.dev](https://docs.ferro-rs.dev).

## License

MIT

----- END replacement -----

No additional sections. No "v1", "v2", "legacy", "migration", or version-history framing. The Features list keeps "41 built-in components" matching the live `BUILTIN_TYPES.len() == 41` invariant in `ferro-json-ui/src/render/mod.rs`.

After writing, verify the file is well-formed markdown (no stray backticks at EOF, single trailing newline).
  </action>
  <verify>
    <automated>! grep -nE '\b(JsonUiView|ComponentNode|LayoutComponent)\b' ferro-json-ui/README.md && ! grep -n 'view\.into_response' ferro-json-ui/README.md && grep -q 'Spec::builder' ferro-json-ui/README.md && grep -q 'JsonUi::render_file' ferro-json-ui/README.md && grep -q '41 built-in components' ferro-json-ui/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\b(JsonUiView|ComponentNode|LayoutComponent)\b' ferro-json-ui/README.md` returns 0
    - `grep -c 'view\.into_response' ferro-json-ui/README.md` returns 0
    - `grep -c 'Spec::builder' ferro-json-ui/README.md` returns at least 1
    - `grep -c 'JsonUi::render_file' ferro-json-ui/README.md` returns at least 1
    - `grep -c '41 built-in components' ferro-json-ui/README.md` returns at least 1
    - `grep -c 'use ferro_json_ui::{Spec, Element}' ferro-json-ui/README.md` returns at least 1
    - File is valid markdown (no unmatched code fences); rough check: `grep -c '^```' ferro-json-ui/README.md` returns an even number
  </acceptance_criteria>
  <done>
    README rewritten with current v2 API examples; no v1 type references; matches Pattern 6 verbatim.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Documentation rewrite. No executable behavior change. No new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-08 | n/a | ferro-json-ui/README.md rewrite | accept | Pure documentation change. No production-code path touched. Verified by `cargo test -p ferro-json-ui` post-rewrite (passes unchanged). |
</threat_model>

<verification>
```bash
# Negative: no v1 type names
! grep -nE '\b(JsonUiView|ComponentNode|LayoutComponent)\b' ferro-json-ui/README.md

# Positive: v2 surface referenced
grep -q 'Spec::builder' ferro-json-ui/README.md
grep -q 'JsonUi::render_file' ferro-json-ui/README.md

# Crate still builds
cargo build -p ferro-json-ui
```
</verification>

<success_criteria>
- README contains Spec::builder and JsonUi::render_file examples
- README contains zero v1 type references
- Features list accurately reflects the current crate
- ferro-json-ui still builds
</success_criteria>

<output>
After completion, create `.planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-05-SUMMARY.md`.
</output>
