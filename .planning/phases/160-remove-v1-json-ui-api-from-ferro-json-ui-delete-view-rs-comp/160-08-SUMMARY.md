---
phase: 160
plan: "08"
subsystem: docs
tags: [docs, cli-reference, json-ui, v1-removal]
requires:
  - ferro-cli/src/templates/make.rs (source-of-truth for `ferro make:json-view` template output)
provides:
  - "docs/src/reference/cli.md `ferro make:json-view` section accurately documents current CLI output (JSON spec file + handler snippet)"
affects:
  - docs.book (mdbook HTML build)
tech-stack:
  added: []
  patterns:
    - "Doc example mirrors live template source (`ferro-cli/src/templates/make.rs:107-143`) verbatim for shape parity"
    - "Generated-file pair documented as JSON spec + handler snippet (not single Rust file)"
key-files:
  created: []
  modified:
    - docs/src/reference/cli.md
decisions:
  - "[160-08] Verbatim replacement (not substitution) — replaced the entire 23-line v1 Rust block with the JSON-spec + handler example exactly per RESEARCH Pattern 7; a sed-style rename would have left v1 type imports and the .rs filename intact"
metrics:
  duration: "1m 2s"
  completed_date: "2026-05-17"
  task_count: 1
  file_count: 1
---

# Phase 160 Plan 08: Rewrite make:json-view Example in cli.md Summary

CLI reference `make:json-view` example now matches the actual `ferro make:json-view <Name>` output: a JSON spec at `src/views/<name>.json` with the `"ferro-json-ui/v2"` schema URI plus a paired handler using `JsonUi::render_file(...)`. The previous example was doubly stale — claiming a `src/views/user_index.rs` file generated by a `JsonUiView::new()` chain over `ComponentNode { ... Component::Text(TextProps { ... }) ... }`, none of which exist in the post-Phase-115 crate.

## Scope

Single-file documentation rewrite. No code changes, no API impact.

**Modified:** `docs/src/reference/cli.md` (Generated file block for `ferro make:json-view`, lines 516-538 in pre-edit form).

The "Requirements:" block above and the `### ferro make:task` heading after stayed unchanged.

## Implementation

Task 1 — Rewrite cli.md make:json-view Generated file block — replaced both the `Generated file:` label and the full code block per RESEARCH Pattern 7. The replacement was sourced verbatim from the canonical template in `ferro-cli/src/templates/make.rs`:
- JSON spec body mirrors `json_view_template()` at lines 107-131.
- Handler snippet mirrors `json_view_handler_template()` at lines 134-143.

## Verification

All acceptance grep gates pass on `docs/src/reference/cli.md`:

| Check                                                                                                  | Expected | Actual |
| ------------------------------------------------------------------------------------------------------ | -------- | ------ |
| `grep -cE '\b(JsonUiView\|ComponentNode\|TableProps\|TextElement\|TextProps)\b'`                       | 0        | 0      |
| `grep -c 'Component::Text'`                                                                            | 0        | 0      |
| `grep -c 'src/views/user_index.rs'`                                                                    | 0        | 0      |
| `grep -c 'src/views/user_index.json'`                                                                  | ≥1       | 2      |
| `grep -c 'JsonUi::render_file'`                                                                        | ≥1       | 1      |
| `grep -c '"ferro-json-ui/v2"'`                                                                         | ≥1       | 1      |
| `grep -c 'ferro make:task'` (next heading still present)                                               | ≥1       | 3      |

Additionally, `mdbook build` under `docs/` completes cleanly:
```
INFO Book building has started
INFO Running the html backend
INFO HTML book written to `.../docs/book`
```

## Deviations from Plan

None — plan executed exactly as written. Verbatim replacement matched RESEARCH Pattern 7 with no adjustments.

## Decisions Made

- **[160-08]** Verbatim replacement, not substitution. The doc example was rewritten from scratch using the canonical template source, ensuring shape parity (field order, JSON nesting, handler signature) with what `ferro make:json-view` actually emits. A sed-style rename of `.rs → .json` would have left v1 type imports and the Rust body intact, perpetuating the drift.
- **[160-08]** Documented the generated artifact as a pair (spec file + handler snippet). The CLI now emits two artifacts of different shapes (data + behavior); one combined code block would have obscured the architectural change.

## Self-Check: PASSED

- `docs/src/reference/cli.md` exists and contains the new content (verified via `grep -c 'src/views/user_index.json'` returning 2)
- Commit `94a18636` exists (verified via `git log --oneline -1`)
- mdbook build under `docs/` exits 0
