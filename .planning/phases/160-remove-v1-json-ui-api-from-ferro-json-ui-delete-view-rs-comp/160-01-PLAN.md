---
phase: 160
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-json-ui/src/render/mod.rs
  - ferro-json-ui/src/render/atoms.rs
  - ferro-json-ui/src/render/containers.rs
  - ferro-json-ui/src/render/form.rs
  - ferro-json-ui/src/render/data.rs
  - ferro-json-ui/src/projection/builder.rs
  - ferro-json-ui/src/layout.rs
autonomous: true
requirements_addressed: [D-01, D-02, D-03, Pattern-1, Pattern-8]
must_haves:
  truths:
    - "ferro-json-ui/src/render/*.rs has zero `Port of v1`, `Differences from v1`, `ported from v1`, `ported verbatim from v1` framing in doc comments"
    - "ferro-json-ui/src/projection/builder.rs:42 stale `Plan 03` comment is removed"
    - "ferro-json-ui/src/layout.rs test fixture uses `ferro-json-ui/v2` (not `v1`) as the schema literal"
    - "All rewritten doc comments describe what the function does in present tense (props read, HTML emitted, edge cases)"
  artifacts:
    - path: "ferro-json-ui/src/render/atoms.rs"
      provides: "Neutral module-level doc + neutral SVG-icon section comment"
      contains: "no `v1`, no `Port of`, no `Phase 116`, no `Per CONTEXT D-` framing"
    - path: "ferro-json-ui/src/render/containers.rs"
      provides: "Neutral doc comments for render_card, render_modal, render_tabs, render_kanban_board, render_page_header, render_grid, render_collapsible, render_form_section, render_button_group + module-level doc"
      contains: "no `Port of v1`, no `Differences from v1`"
    - path: "ferro-json-ui/src/render/form.rs"
      provides: "Neutral doc comments for render_form + module-level doc"
      contains: "no `Port of v1`, no `Differences from v1`"
    - path: "ferro-json-ui/src/render/data.rs"
      provides: "Neutral doc comments for render_table, render_data_table and related helpers + module-level doc"
      contains: "no `Port of v1`, no `Matches v1`, no `(v1 verbatim)`, no `(v1 fallback)`, no `Legacy {row_key}`"
    - path: "ferro-json-ui/src/render/mod.rs"
      provides: "Neutral module doc, neutral BUILTIN_TYPES doc, neutral render_spec_to_html doc, neutral asset-collection helpers"
      contains: "no `Phase 116`, no `Replaces the Phase 115 placeholder`, no `Replaces v1's`, no `Ported from v1`"
  key_links:
    - from: "ferro-json-ui/src/render/atoms.rs"
      to: "Pattern 1 exemplar (`decode_diagnostic` at atoms.rs:26-37 — present-tense style)"
      via: "doc-comment style alignment"
      pattern: "describes what function emits + props read + edge cases, no historical narrative"
    - from: "ferro-json-ui/src/projection/builder.rs:42"
      to: "_plan_02_reserved dead code"
      via: "comment + dead code removal"
      pattern: "delete obsolete `Silence unused-import warnings until Plan 03` comment + delete `_plan_02_reserved` function if it is unused"
---

<objective>
Sweep `ferro-json-ui/src/` for every v1-framing doc comment introduced during the v2 cutover and rewrite each one in neutral present-tense voice. This is the largest single piece of Phase 160 (30+ sites). Files have no behavior change — only doc comments and one obsolete fixture-string update.

Purpose: enforce CONTEXT.md D-02 (two named sites) and D-03 (full sweep) per Pattern 1 (`atoms.rs::decode_diagnostic` style). Eliminates the trigger phrases the project's "Repository documents must read as neutral" rule flags (`v1`, `legacy`, `Port of`, `Differences from v1`, `removed in`).

Output: ferro-json-ui crate with neutral, agent-readable doc comments across the render pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-CONTEXT.md
@.planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-RESEARCH.md
@ferro-json-ui/src/render/atoms.rs
@ferro-json-ui/src/render/mod.rs
@ferro-json-ui/src/render/containers.rs
@ferro-json-ui/src/render/form.rs
@ferro-json-ui/src/render/data.rs
@ferro-json-ui/src/projection/builder.rs
@ferro-json-ui/src/layout.rs

<interfaces>
<!-- Pattern 1 exemplar from atoms.rs:26-37 — the target voice -->
/// Emits a `<!-- ferro-json-ui: failed to decode TYPE props: MSG -->` comment.
/// Used as the fallback on serde_json::from_value decode errors across every
/// atom renderer.
fn decode_diagnostic(type_name: &str, err: impl std::fmt::Display) -> String { ... }

<!-- Target voice properties: what it emits + what it reads + edge cases. No phase/decision provenance. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite render/mod.rs, atoms.rs, projection/builder.rs and layout.rs test fixture</name>
  <files>ferro-json-ui/src/render/mod.rs, ferro-json-ui/src/render/atoms.rs, ferro-json-ui/src/projection/builder.rs, ferro-json-ui/src/layout.rs</files>
  <read_first>
    - ferro-json-ui/src/render/atoms.rs (full file — Pattern 1 exemplar at lines 26-37)
    - ferro-json-ui/src/render/mod.rs (full file — sites at lines 1-12, 41, 88-94, 222-225, 245-248, 270-272)
    - ferro-json-ui/src/projection/builder.rs (lines 30-60 — the `_plan_02_reserved` placeholder context)
    - ferro-json-ui/src/layout.rs (line 727 — `view_json: "{\"schema\":\"v1\"}"` fixture)
    - .planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-RESEARCH.md (Pattern 1 + Pattern 8 §atoms.rs/mod.rs/builder.rs/layout.rs rows)
  </read_first>
  <action>
Rewrite the following sites in present-tense, what-the-function-does voice. No `v1`, no `Port of`, no `Phase 116`, no `Per CONTEXT D-XX`, no `Replaces the Phase 115 placeholder`, no `Ported from v1`.

**`ferro-json-ui/src/render/atoms.rs` — module doc (lines 1-9):**

CURRENT:
```rust
//! Phase 116: leaf renderers ported verbatim from v1 render.rs.
//!
//! Per CONTEXT D-21 the v1 HTML emission is the canonical contract; this module
//! changes only the function signature (now `(el, spec, data, depth)`) and adds
//! the D-12 props-decode diagnostic. Children-recursion is not used here —
//! atoms are leaves per RESEARCH §"Atoms".
//!
//! v1 reference: `git show 40385f32^:ferro-json-ui/src/render.rs`. Line ranges
//! for each ported function are recorded in the per-function doc comments.
```

REPLACE WITH:
```rust
//! Leaf-element renderers.
//!
//! Each public function in this module takes `(el, spec, data, depth)` and
//! returns an HTML fragment. Atoms have no recursive children — they decode
//! their typed `*Props`, escape and substitute text, and emit a self-contained
//! HTML element. Decode failures surface via [`decode_diagnostic`] as an HTML
//! comment rather than a panic.
```

**`ferro-json-ui/src/render/atoms.rs:57` — SVG icon section comment:**

CURRENT:
```rust
// ── SVG icon constants (ported verbatim from v1 render.rs) ──
```

REPLACE WITH:
```rust
// ── SVG icon constants ──
```

**`ferro-json-ui/src/render/mod.rs` — module doc (lines 1-12):** Read the current text and replace any narrative referencing "Phase 116", "walker", "Replaces the Phase 115 placeholder", or "v1" with a present-tense description of what `render/` does: "Renders a `Spec` to HTML. Walks `spec.root`, dispatches each `Element.type_name` against `BUILTIN_TYPES` (or registered plugins), and concatenates the emitted fragments. Atoms in `atoms.rs`; containers in `containers.rs`; data displays in `data.rs`; form controls in `form.rs`."

**`ferro-json-ui/src/render/mod.rs:41` — `BUILTIN_TYPES` doc:** Rewrite to: "Single source of truth for the 41 built-in element type names recognized by the renderer. Plugins cannot register a type name that shadows an entry here; the registration logic rejects collisions."

**`ferro-json-ui/src/render/mod.rs:88-94` — `render_spec_to_html` doc:** Rewrite to: "Renders an entire `Spec` to a complete HTML response body. Walks from `spec.root` outward, escaping text content and substituting data bindings via JSON Pointer. Top-level children are wrapped in a `flex-wrap` container; the renderer does not emit `<html>` / `<head>` / `<body>` tags — the layout system supplies those."

**`ferro-json-ui/src/render/mod.rs:222-225` — plugin-name collection helper:** Rewrite to remove `Replaces v1's …` framing. Present tense: "Walks `spec.elements` and collects every plugin type name encountered (i.e. every `Element.type_name` not present in `BUILTIN_TYPES`). Used by the asset-collection pipeline to determine which plugin CSS/JS to inject."

**`ferro-json-ui/src/render/mod.rs:245-248` — asset emission helper:** Rewrite to remove `Ported from v1 render.rs lines 200-221.`. Present tense: describe what the function emits (concatenated `<link>` and `<script>` tags for the collected plugin assets, deduplicated by URL).

**`ferro-json-ui/src/render/mod.rs:270-272` — init-script helper:** Rewrite to remove `Ported from v1 render.rs …`. Present tense: "Emits a `<script>{init}</script>` tag per registered plugin init script, once per page, in registration order."

**`ferro-json-ui/src/projection/builder.rs:42`:** Read the surrounding `_plan_02_reserved` context. The comment `// Silence unused-import warnings until Plan 03 rewires the legacy renderer.` is stale (Plan 03 ran long ago). Delete the comment. If `_plan_02_reserved` is unused dead code (verify by grepping for any caller within `ferro-json-ui/`), delete the function and its imports as well. If it has callers, keep the function but delete only the stale comment.

**`ferro-json-ui/src/layout.rs:727`:** Locate the test fixture line `view_json: "{\"schema\":\"v1\"}"` (or the closest matching string in the test) and change `"v1"` to `"ferro-json-ui/v2"`. The test does not assert on this literal value; the change is cosmetic but aligns with the no-v1-framing rule.

After all rewrites, run `cargo fmt -p ferro-json-ui` to trim any incidental trailing whitespace.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy -p ferro-json-ui --all-targets -- -D warnings && cargo test -p ferro-json-ui --all-features && ! grep -rnE 'Port of v1|Differences from v1|ported verbatim from v1|ported from v1|Replaces v1|Phase 116|Per CONTEXT D-' ferro-json-ui/src/render/mod.rs ferro-json-ui/src/render/atoms.rs && ! grep -n 'Silence unused-import warnings until Plan 03' ferro-json-ui/src/projection/builder.rs && ! grep -n '"schema":"v1"' ferro-json-ui/src/layout.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy -p ferro-json-ui --all-targets -- -D warnings` exits 0
    - `cargo test -p ferro-json-ui --all-features` exits 0
    - `grep -cE 'Port of v1|Differences from v1|ported verbatim from v1|ported from v1|Replaces v1|Phase 116' ferro-json-ui/src/render/atoms.rs` returns 0
    - `grep -cE 'Port of v1|Differences from v1|ported verbatim from v1|ported from v1|Replaces v1|Phase 116' ferro-json-ui/src/render/mod.rs` returns 0
    - `grep -c 'Silence unused-import warnings until Plan 03' ferro-json-ui/src/projection/builder.rs` returns 0
    - `grep -c '"schema":"v1"' ferro-json-ui/src/layout.rs` returns 0
    - `grep -c '"ferro-json-ui/v2"' ferro-json-ui/src/layout.rs` returns at least 1
  </acceptance_criteria>
  <done>
    Module doc comments and helpers in render/mod.rs, render/atoms.rs, projection/builder.rs, and layout.rs use neutral present-tense voice with no v1-framing or phase-reference narrative; tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite render/containers.rs and render/form.rs v1-framing comments</name>
  <files>ferro-json-ui/src/render/containers.rs, ferro-json-ui/src/render/form.rs</files>
  <read_first>
    - ferro-json-ui/src/render/atoms.rs (lines 26-37 — Pattern 1 exemplar)
    - ferro-json-ui/src/render/containers.rs (full file — sites at lines 1-10, 26-28, 74-77, 108-110, 177-180, 201, 293, 414, 484, 529, 536, 572, 631-635, 638-640)
    - ferro-json-ui/src/render/form.rs (full file — sites at lines 1-20, 33-39)
    - .planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-RESEARCH.md (Pattern 1 §containers.rs/form.rs proposed rewrites + Pattern 8 §containers/form rows)
  </read_first>
  <action>
Rewrite every doc comment in `containers.rs` and `form.rs` that uses v1 framing. Two named CONTEXT.md sites (D-02) plus all D-03 sweep sites. No `Port of v1`, no `Differences from v1`, no `v1 hard-codes`, no `Phase 116`, no `Per CONTEXT D-XX`. Use the Pattern 1 voice: describe what each function emits, what props it reads, and what edge cases it handles.

**`ferro-json-ui/src/render/containers.rs:631-635` (D-02 NAMED SITE) — paste this exact rewrite from RESEARCH Pattern 1:**

```rust
/// Horizontal button row. Renders each child ID in `el.children` and wraps the
/// resulting HTML in a `<div class="flex items-center gap-2 flex-wrap">` container.
/// `ButtonGroupProps.gap` is decoded for prop-shape diagnostics but does not
/// influence the emitted CSS (the gap is fixed at `gap-2`).
pub(crate) fn render_button_group(...) -> String { ... }
```

**`ferro-json-ui/src/render/containers.rs:638-640` (inside `render_button_group`) — the `// Decode-check for D-12 diagnostic discipline; ...` comment:** Rewrite to: `// Decode-check: malformed props surface via HTML comment rather than crash.` Drop the `D-12` reference and the `v1 hard-codes gap-2` aside.

**`ferro-json-ui/src/render/form.rs:33-39` (D-02 NAMED SITE) — paste this exact rewrite from RESEARCH Pattern 1:**

```rust
/// Renders a `<form>` element. Child IDs in `el.children` become the form body;
/// `FormProps.action` controls submission target and method (with HTTP-method
/// spoofing for PUT/PATCH/DELETE → POST + hidden `_method` input). When
/// `action.url` is `None`, the form falls back to `action="#"` and emits a
/// diagnostic HTML comment.
pub(crate) fn render_form(...) -> String { ... }
```

**All remaining D-03 sweep sites in `containers.rs` and `form.rs` (per RESEARCH Pattern 8 table):**

For each site, rewrite in the same voice. Concrete templates:

- `containers.rs:1-10` (module doc): "Container renderers. Each function takes `(el, spec, data, depth)` and emits the container's HTML wrapper plus the concatenated rendering of each child ID in `el.children`."
- `containers.rs:26-28` (`render_card` doc): "Renders a `Card`. Reads `CardProps` (title, description, footer, max_width, variant) and wraps child renderings in a bordered or elevated container per `variant`."
- `containers.rs:74-77` (inside `render_card`, body-wrapper comment): Replace `v1 gated the body wrapper on …; v2 gates on …` with `// Body wrapper is omitted when there are no children to render.`
- `containers.rs:108-110` (`render_modal` doc): "Renders a Modal element. Emits the modal markup with an open/close action wired to `ModalProps.toggle_id`."
- `containers.rs:177-180` (`render_tabs` doc): "Renders a Tabs container. Two behaviors worth flagging for plugin authors: (1) a single tab is rendered without the tab strip (body only); (2) the `default_tab` attribute selects the initially active panel."
- `containers.rs:201` (single-tab auto-hide inline comment): `// Single-tab specs render body-only (no tab strip).`
- `containers.rs:293` (`render_kanban_board` doc): "Renders a Kanban board. Reads `KanbanBoardProps.columns` (static) or `data_path` (dynamic) and emits one column per entry."
- `containers.rs:414` (`render_page_header` doc): "Renders a page header. Emits the title, optional subtitle, breadcrumb trail, and right-aligned action buttons."
- `containers.rs:484` (`render_grid` doc): "Renders a Grid container. Emits a CSS-grid `<div>` whose column count comes from `GridProps.columns`."
- `containers.rs:529` (Collapsible SVG chevron inline comment): `// SVG chevron used to indicate the collapsed/expanded state.`
- `containers.rs:536` (`render_collapsible` doc): "Renders a Collapsible section. Emits a `<details>` element with the title in `<summary>` and child renderings inside."
- `containers.rs:572` (`render_form_section` doc): "Renders a FormSection. Emits a `<fieldset>` with an optional legend and description; child IDs render as the section body."
- `form.rs:1-20` (module doc): "Form-control renderers. Each function emits a single typed form control or the surrounding `<form>` element. Decode failures surface as HTML diagnostic comments via the shared decode helper."

Drop every remaining instance of `Per CONTEXT D-XX`, `Phase 116`, `(render.rs L###-###)`, `v1 ...`, `ported …`. Keep technical accuracy — when a comment described a real behavior, the rewrite must preserve that behavior description in neutral voice.

After all rewrites, run `cargo fmt -p ferro-json-ui` to trim trailing whitespace.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy -p ferro-json-ui --all-targets -- -D warnings && cargo test -p ferro-json-ui --all-features && ! grep -nE 'Port of v1|Differences from v1|ported verbatim from v1|ported from v1|Replaces v1|Phase 116|Per CONTEXT D-|render\.rs L[0-9]' ferro-json-ui/src/render/containers.rs ferro-json-ui/src/render/form.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy -p ferro-json-ui --all-targets -- -D warnings` exits 0
    - `cargo test -p ferro-json-ui --all-features` exits 0
    - `grep -cE 'Port of v1|Differences from v1' ferro-json-ui/src/render/containers.rs` returns 0
    - `grep -cE 'Port of v1|Differences from v1' ferro-json-ui/src/render/form.rs` returns 0
    - `grep -cE 'Per CONTEXT D-|Phase 116' ferro-json-ui/src/render/containers.rs` returns 0
    - `grep -cE 'Per CONTEXT D-|Phase 116' ferro-json-ui/src/render/form.rs` returns 0
    - `grep -q 'Horizontal button row' ferro-json-ui/src/render/containers.rs` succeeds
    - `grep -q 'Renders a `<form>` element' ferro-json-ui/src/render/form.rs` succeeds
  </acceptance_criteria>
  <done>
    Both files use neutral, present-tense doc comments throughout; no v1 framing remains; tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite render/data.rs v1-framing comments</name>
  <files>ferro-json-ui/src/render/data.rs</files>
  <read_first>
    - ferro-json-ui/src/render/atoms.rs (lines 26-37 — Pattern 1 exemplar)
    - ferro-json-ui/src/render/data.rs (full file — sites at lines 1-15, 24-28, 119-129, 251, 265, 287, 307, 516)
    - .planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-RESEARCH.md (Pattern 8 §data.rs rows)
  </read_first>
  <action>
Rewrite every v1-framing doc comment and inline comment in `data.rs`. Same voice rules as Task 2: no `Port of v1`, no `Matches v1`, no `Legacy {row_key}`, no `(v1 verbatim)`, no `(v1 fallback)`, no `Phase 116`.

**Concrete rewrites (per RESEARCH Pattern 8 §data.rs rows):**

- `data.rs:1-15` (module doc): "Data-display renderers: Table and DataTable. Each function reads typed props, escapes and substitutes cell values, and emits HTML. Per-row actions resolve URL placeholders against the row's data at render time."
- `data.rs:24-28` (`render_table` doc): "Renders a simple Table element. Reads `TableProps.columns` and `TableProps.rows`. When per-row actions are present, an `Azioni` column is appended."
- `data.rs:119-129` (`render_data_table` doc): "Renders a DataTable. Supports declarative columns with formatters, per-row actions wrapped in a `DropdownMenu`, optional search bar, and per-row visibility filters. Row actions resolve `{column_key}` placeholders in their action URL against each row before emission."
- `data.rs:251` (`render_cell_value` doc): "Renders a single cell's value as a plain string. Handles numbers, booleans, nulls, and JSON-pointer-resolved bindings."
- `data.rs:265` (`resolve_row_key` doc): "Resolves the row-key value used to identify a row in per-row action URLs. Reads the value at `props.row_key` from the row data."
- `data.rs:287` (`{row_key}` substitution comment): `// `{row_key}` is substituted against the value at `props.row_key`.`
- `data.rs:307` (URL-from-handler fallback): `// URL fallback: when the action has no explicit `url`, the handler name is used to construct one.`
- `data.rs:516` (`{row_key}` semantics inline comment): `// `{row_key}` substitutes against the value at `props.row_key`.`

Drop every remaining `(render.rs:NNN-NNN)`, `(v1 verbatim)`, `(v1 fallback)`, `Matches v1`, `Legacy {row_key}`. Preserve all functional behavior descriptions in neutral voice.

After all rewrites, run `cargo fmt -p ferro-json-ui` to normalize trailing whitespace.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy -p ferro-json-ui --all-targets -- -D warnings && cargo test -p ferro-json-ui --all-features && ! grep -nE 'Port of v1|Matches v1|\(v1 verbatim\)|\(v1 fallback\)|Legacy \{row_key\}|Phase 116|Per CONTEXT D-|render\.rs:[0-9]' ferro-json-ui/src/render/data.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy -p ferro-json-ui --all-targets -- -D warnings` exits 0
    - `cargo test -p ferro-json-ui --all-features` exits 0
    - `grep -cE 'Port of v1|Matches v1|\(v1 verbatim\)|\(v1 fallback\)|Legacy \{row_key\}' ferro-json-ui/src/render/data.rs` returns 0
    - `grep -cE 'Phase 116|Per CONTEXT D-' ferro-json-ui/src/render/data.rs` returns 0
    - `grep -q 'Data-display renderers' ferro-json-ui/src/render/data.rs` succeeds
  </acceptance_criteria>
  <done>
    `data.rs` uses neutral, present-tense doc comments throughout; tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | This plan modifies only doc comments and one obsolete test-fixture literal. No executable behavior changes. No input handling introduced. No new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-01 | n/a | doc-comment rewrites in `ferro-json-ui/src/render/*.rs` | accept | No new attack surface. `cargo test -p ferro-json-ui --all-features` post-rewrite confirms no accidental code-path change. |
| T-160-02 | n/a | obsolete fixture-string update in `layout.rs:727` | accept | Test does not assert on the literal value; rewrite is cosmetic. `cargo test` confirms test still passes. |
| T-160-03 | n/a | optional dead-code deletion of `_plan_02_reserved` in `projection/builder.rs` | accept | `cargo clippy --all --all-targets -- -D warnings` would have failed on any live reference; deletion is safe when grep confirms no callers. |
</threat_model>

<verification>
After all three tasks complete:

```bash
# Full sweep gate
! grep -rnE 'Port of v1|Differences from v1|ported verbatim from v1|ported from v1|Replaces v1' ferro-json-ui/src/render/

# Workspace fmt + clippy + test
cargo fmt --all -- --check
cargo clippy -p ferro-json-ui --all-targets -- -D warnings
cargo test -p ferro-json-ui --all-features
```

All commands exit 0.
</verification>

<success_criteria>
- Zero `Port of v1`, `Differences from v1`, `ported verbatim from v1`, `Replaces v1`, `Matches v1`, `Phase 116`, `Per CONTEXT D-XX`, `(v1 verbatim)`, `(v1 fallback)` strings in `ferro-json-ui/src/render/{mod,atoms,containers,form,data}.rs`
- Zero `Silence unused-import warnings until Plan 03` strings in `ferro-json-ui/src/projection/builder.rs`
- Zero `"schema":"v1"` literals in `ferro-json-ui/src/layout.rs`
- `cargo fmt --all -- --check && cargo clippy -p ferro-json-ui --all-targets -- -D warnings && cargo test -p ferro-json-ui --all-features` all exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-01-SUMMARY.md`.
</output>
