---
phase: 160
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-mcp/src/tools/code_templates.rs
autonomous: true
requirements_addressed: [D-04, Pattern-3]
must_haves:
  truths:
    - "ferro-mcp returns no `migration_v1_to_v2` template category from the code_templates MCP tool"
    - "The `code_templates_returns_migration_patterns` test no longer exists"
    - "The remaining template categories (handler, controller, model, validation, middleware, rate_limiting, broadcasting, api) are unaffected"
  artifacts:
    - path: "ferro-mcp/src/tools/code_templates.rs"
      provides: "Template registry without v1→v2 migration patterns"
      contains: "no `migration_v1_to_v2` literal, no `migration_v1_to_v2_templates` symbol"
  key_links:
    - from: "build_templates() top-of-file registry"
      to: "removed migration_v1_to_v2_templates() fn"
      via: "deletion of `templates.extend(...)` call + comment"
      pattern: "single coordinated diff touches line ~78-79 (comment + extend call), lines ~1504-1697 (function body), lines ~1818-1830 (test)"
---

<objective>
Delete the v1→v2 migration template category from the ferro-mcp `code_templates` tool. Three sites — the registration comment and `templates.extend(...)` call near line 78-79, the entire `migration_v1_to_v2_templates()` function body (~lines 1504-1697), and the `code_templates_returns_migration_patterns` integration test (~lines 1818-1830).

Purpose: per CONTEXT.md D-04, the sole consumer (gestiscilo) is fully migrated; no v1 codebases remain for the MCP to assist. Per the user-naming constraint, no migration story belongs in agent-readable surface.

Output: ferro-mcp builds and tests green; `migration_v1_to_v2` is not a category the tool returns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-CONTEXT.md
@.planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-RESEARCH.md
@ferro-mcp/src/tools/code_templates.rs

<interfaces>
<!-- Three coordinated deletion sites in code_templates.rs -->

Site 1 — registration (lines ~78-79):
```rust
    // v1 → v2 migration patterns
    templates.extend(migration_v1_to_v2_templates());
```

Site 2 — function body (lines ~1504-1697):
```rust
fn migration_v1_to_v2_templates() -> Vec<CodeTemplate> { ... 7 templates ... }
```

Site 3 — integration test (lines ~1818-1830):
```rust
#[test]
fn code_templates_returns_migration_patterns() {
    let templates = execute(Some("migration_v1_to_v2"));
    assert!(templates.templates.len() >= 7, ...);
    ...
}
```

All three deletions in a single coordinated diff per Pitfall 2 from RESEARCH (avoid orphaned comment).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete migration_v1_to_v2 registration, function, and test in code_templates.rs</name>
  <files>ferro-mcp/src/tools/code_templates.rs</files>
  <read_first>
    - ferro-mcp/src/tools/code_templates.rs (lines 60-90 — `build_templates` registry; lines 1500-1710 — `migration_v1_to_v2_templates` function body; lines 1805-1840 — test functions)
    - .planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-RESEARCH.md (Pattern 3 + Pitfall 2)
  </read_first>
  <action>
Perform a single coordinated edit removing three sites:

**Site 1 — registration block in `build_templates()` (lines ~77-79):** Delete BOTH the comment line and the extend call:

```rust
    // v1 → v2 migration patterns
    templates.extend(migration_v1_to_v2_templates());
```

After deletion, the preceding `templates.extend(api_templates());` line should be followed directly by the trailing `templates` return expression and the closing brace of `build_templates()`. Verify no orphaned comment dangles between them.

**Site 2 — `migration_v1_to_v2_templates()` function (lines ~1504-1697):** Delete the entire `fn migration_v1_to_v2_templates() -> Vec<CodeTemplate> { vec![ ... 7 CodeTemplate literals ... ] }` definition. Use the function-name boundary; do not delete any neighboring function.

**Site 3 — integration test `code_templates_returns_migration_patterns` (lines ~1818-1830):** Delete the entire `#[test] fn code_templates_returns_migration_patterns() { ... }` block. Do not delete the surrounding `test_unknown_category_returns_empty` or any other test.

After the three deletions, run `cargo fmt -p ferro-mcp` and verify the file still parses.
  </action>
  <verify>
    <automated>! grep -n 'migration_v1_to_v2' ferro-mcp/src/tools/code_templates.rs && cargo fmt --all -- --check && cargo clippy -p ferro-mcp --all-targets -- -D warnings && cargo test -p ferro-mcp --all-features --lib code_templates</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'migration_v1_to_v2' ferro-mcp/src/tools/code_templates.rs` returns 0
    - `grep -c 'code_templates_returns_migration_patterns' ferro-mcp/src/tools/code_templates.rs` returns 0
    - `grep -c 'v1 → v2 migration patterns' ferro-mcp/src/tools/code_templates.rs` returns 0
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy -p ferro-mcp --all-targets -- -D warnings` exits 0
    - `cargo test -p ferro-mcp --all-features --lib code_templates` exits 0 (remaining tests still pass)
    - Remaining template categories (handler, controller, model, validation, middleware, rate_limiting, broadcasting, api) all still present in the file
  </acceptance_criteria>
  <done>
    Three sites deleted in one diff; no orphaned comment; ferro-mcp builds and remaining code_templates tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Deletion reduces MCP-tool surface area. No new attack surface; the removed function had no I/O and no untrusted-input handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-04 | n/a | `migration_v1_to_v2_templates` deletion | accept | No new attack surface. Function returned a `Vec<CodeTemplate>` of static template strings; no I/O, no untrusted-input handling. Deletion is a pure surface-area reduction. |
</threat_model>

<verification>
```bash
# Negative assertions
! grep -n 'migration_v1_to_v2' ferro-mcp/src/tools/code_templates.rs

# Positive — file still well-formed
cargo fmt --all -- --check
cargo clippy -p ferro-mcp --all-targets -- -D warnings
cargo test -p ferro-mcp --all-features
```
</verification>

<success_criteria>
- `migration_v1_to_v2` string absent from `ferro-mcp/src/tools/code_templates.rs`
- ferro-mcp builds and tests green
- No orphaned comment in `build_templates()`
- Remaining template categories intact
</success_criteria>

<output>
After completion, create `.planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-02-SUMMARY.md`.
</output>
