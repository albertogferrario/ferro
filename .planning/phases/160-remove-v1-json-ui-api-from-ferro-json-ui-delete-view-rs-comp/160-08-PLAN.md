---
phase: 160
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/reference/cli.md
autonomous: true
requirements_addressed: [D-08, Pattern-7]
must_haves:
  truths:
    - "docs/src/reference/cli.md `make:json-view` example shows the actual current CLI output (a JSON spec file under src/views/, not a Rust .rs file)"
    - "The example contains no `JsonUiView`, `ComponentNode`, `Component::Text(...)` references"
    - "The example shows the handler snippet using `JsonUi::render_file(...)`"
    - "`Generated file:` line shows `src/views/user_index.json` (not `user_index.rs`)"
  artifacts:
    - path: "docs/src/reference/cli.md"
      provides: "`make:json-view` reference matching the actual ferro-cli output"
      contains: "JSON spec example + handler example using JsonUi::render_file"
  key_links:
    - from: "docs/src/reference/cli.md `make:json-view` section"
      to: "ferro-cli/src/templates/make.rs:107-131 (current template output)"
      via: "shape parity with actual CLI output"
      pattern: "doc example matches the live template the CLI emits"
---

<objective>
Per RESEARCH Pattern 7 (D-08 scope-expansion site), the `docs/src/reference/cli.md` `make:json-view` example block (lines 516-538) is doubly inaccurate: (1) it claims the generated file is `src/views/user_index.rs` (the actual CLI emits `src/views/user_index.json`); (2) the example body is a v1 Rust `JsonUiView::new()` chain using `Component::Text(...)` and `ComponentNode { ... }` — none of which exist in the post-Phase-115 crate. Replace with a JSON-spec example + handler snippet that matches what `ferro make:json-view <Name>` actually produces today (sourced from `ferro-cli/src/templates/make.rs:107-131`).

Purpose: agent-readable CLI reference that accurately documents the current command's output.

Output: cli.md `make:json-view` section reflects the actual CLI behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-CONTEXT.md
@.planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-RESEARCH.md
@docs/src/reference/cli.md
@ferro-cli/src/templates/make.rs

<interfaces>
The actual current `ferro make:json-view` output (verified from ferro-cli/src/templates/make.rs:107-143):

- Emits a JSON spec file at `src/views/{name}.json`
- The JSON shape has fields: `$schema`, `title`, `layout`, `root`, `elements`
- The handler snippet uses `JsonUi::render_file("views/{name}.json", data)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite cli.md make:json-view Generated file block</name>
  <files>docs/src/reference/cli.md</files>
  <read_first>
    - docs/src/reference/cli.md (lines 505-545 — current `make:json-view` section including the v1 Rust example at 516-538)
    - ferro-cli/src/templates/make.rs (lines 107-143 — actual template body the CLI emits)
    - .planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-RESEARCH.md (Pattern 7 — paste-ready replacement)
  </read_first>
  <action>
Locate the current `make:json-view` Generated file block (lines 516-538). Replace both the "Generated file:" line AND the entire code block per RESEARCH Pattern 7.

CURRENT (delete lines 516-538):
```
**Generated file:** `src/views/user_index.rs`

```rust
use ferro::{
    Action, Component, ComponentNode, JsonUiView, TableColumn, TableProps,
    TextElement, TextProps,
};

pub fn view() -> JsonUiView {
    JsonUiView::new()
        .title("User Index")
        .layout("app")
        .component(ComponentNode {
            key: "heading".to_string(),
            component: Component::Text(TextProps {
                content: "User Index".to_string(),
                element: TextElement::H1,
            }),
            action: None,
            visibility: None,
        })
        // ... additional components based on AI context or static template
}
```
```

REPLACEMENT (write):
```
**Generated file:** `src/views/user_index.json`

```json
{
  "$schema": "ferro-json-ui/v2",
  "title": "User Index",
  "layout": "dashboard",
  "root": "root",
  "elements": {
    "root": {
      "type": "Card",
      "props": {
        "title": "User Index",
        "description": "Edit src/views/user_index.json to customize this view."
      },
      "children": ["heading"]
    },
    "heading": {
      "type": "Text",
      "props": { "content": "User Index", "element": "h1" }
    }
  }
}
```

**Generated handler usage:**

```rust
#[handler]
pub async fn user_index(req: Request) -> Response {
    let data = serde_json::json!({});
    JsonUi::render_file("views/user_index.json", data)
}
```
```

The "Requirements:" block above (lines 510-514) and the section header (around line 505) stay unchanged. The `### ferro make:task` heading after line 540 stays unchanged.

The replacement is grounded in `ferro-cli/src/templates/make.rs:107-131` (the JSON template) and `:134-143` (the handler template).
  </action>
  <verify>
    <automated>! grep -nE '\b(JsonUiView|ComponentNode|TableProps|TextElement|TextProps|Component::Text)\b' docs/src/reference/cli.md && grep -q 'src/views/user_index.json' docs/src/reference/cli.md && grep -q 'JsonUi::render_file' docs/src/reference/cli.md && grep -q '"ferro-json-ui/v2"' docs/src/reference/cli.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\b(JsonUiView|ComponentNode|TableProps|TextElement|TextProps)\b' docs/src/reference/cli.md` returns 0
    - `grep -c 'Component::Text' docs/src/reference/cli.md` returns 0
    - `grep -c 'src/views/user_index.json' docs/src/reference/cli.md` returns at least 1
    - `grep -c 'JsonUi::render_file' docs/src/reference/cli.md` returns at least 1
    - `grep -c '"ferro-json-ui/v2"' docs/src/reference/cli.md` returns at least 1
    - `grep -c 'src/views/user_index.rs' docs/src/reference/cli.md` returns 0 (the `.rs` extension is gone)
    - The `### ferro make:task` heading is still present after the replaced section
  </acceptance_criteria>
  <done>
    `make:json-view` example shows the actual JSON spec file + handler snippet; no v1 Rust types referenced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Documentation rewrite. No code change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-11 | n/a | docs/src/reference/cli.md `make:json-view` block | accept | Documentation correctness fix. Aligns docs with actual CLI output. No new attack surface. |
</threat_model>

<verification>
```bash
! grep -nE '\b(JsonUiView|ComponentNode|Component::Text)\b' docs/src/reference/cli.md
grep -q 'src/views/user_index.json' docs/src/reference/cli.md
grep -q 'JsonUi::render_file' docs/src/reference/cli.md
```
</verification>

<success_criteria>
- `make:json-view` section shows JSON spec output + handler snippet
- No v1 Rust type references
- File extension is `.json` not `.rs`
</success_criteria>

<output>
After completion, create `.planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-08-SUMMARY.md`.
</output>
