---
phase: 160
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-mcp/src/tools/json_ui_inspect.rs
autonomous: true
requirements_addressed: [D-06, Pattern-4]
must_haves:
  truths:
    - "`test_ignores_non_json_files` still exists and still asserts the scanner ignores non-JSON files in the views directory"
    - "The test fixture no longer uses `old_view.rs` / `// old v1 file` as the stale-file fixture; neutral names are used instead"
  artifacts:
    - path: "ferro-mcp/src/tools/json_ui_inspect.rs"
      provides: "test_ignores_non_json_files with neutral fixture names"
      contains: "no `old_view.rs`, no `old v1 file`, no `mod.rs` referencing `old`"
  key_links:
    - from: "test_ignores_non_json_files"
      to: "json_ui_inspect scanner behavior"
      via: "behavioral assertion preserved (scanner ignores non-.json files)"
      pattern: "rename, don't delete — per RESEARCH Pattern 4 recommendation"
---

<objective>
Per CONTEXT.md D-06 and RESEARCH Pattern 4 — the test at `ferro-mcp/src/tools/json_ui_inspect.rs:301-312` (`test_ignores_non_json_files`) exercises a meaningful behavior: the scanner ignores stray non-`.json` files in the views directory. RESEARCH recommends rename (not delete) because the behavioral assertion is still valid in v2-only mode. Replace the v1-framing fixture filenames (`old_view.rs`, `// old v1 file`, `mod.rs` containing `pub mod old;`) with neutral equivalents.

Purpose: keep regression coverage equivalent while eliminating v1 framing per the user-naming constraint.

Output: ferro-mcp test continues to pass; no v1 fixture strings remain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-CONTEXT.md
@.planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-RESEARCH.md
@ferro-mcp/src/tools/json_ui_inspect.rs

<interfaces>
<!-- Current test block (lines 301-312) -->
#[test]
fn test_ignores_non_json_files() {
    let tmp = TempDir::new().unwrap();
    let views_dir = tmp.path().join("src/views");
    fs::create_dir_all(&views_dir).unwrap();

    write_file(&views_dir, "old_view.rs", "// old v1 file");
    write_file(&views_dir, "mod.rs", "pub mod old;");

    let result = execute(tmp.path(), None);
    assert_eq!(result.total, 0);
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename test fixture filenames to neutral names</name>
  <files>ferro-mcp/src/tools/json_ui_inspect.rs</files>
  <read_first>
    - ferro-mcp/src/tools/json_ui_inspect.rs (lines 270-320 — test module surrounding `test_ignores_non_json_files`)
    - .planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-RESEARCH.md (Pattern 4 — paste-ready replacement)
  </read_first>
  <action>
Replace lines 301-312 (the `test_ignores_non_json_files` block) with the neutral version from RESEARCH Pattern 4:

```rust
#[test]
fn test_ignores_non_json_files() {
    let tmp = TempDir::new().unwrap();
    let views_dir = tmp.path().join("src/views");
    fs::create_dir_all(&views_dir).unwrap();

    write_file(&views_dir, "stale_artifact.rs", "// non-JSON artifact");
    write_file(&views_dir, "mod.rs", "pub mod stale_artifact;");

    let result = execute(tmp.path(), None);
    assert_eq!(result.total, 0);
}
```

The behavioral assertion (`result.total == 0`) is unchanged. The fixture filenames and content carry no v1 framing. No other test in the file should be touched.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy -p ferro-mcp --all-targets -- -D warnings && cargo test -p ferro-mcp --all-features --lib json_ui_inspect::tests::test_ignores_non_json_files && ! grep -n 'old_view.rs\|old v1 file\|pub mod old' ferro-mcp/src/tools/json_ui_inspect.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy -p ferro-mcp --all-targets -- -D warnings` exits 0
    - `cargo test -p ferro-mcp --all-features --lib json_ui_inspect::tests::test_ignores_non_json_files` exits 0
    - `grep -c 'old_view.rs' ferro-mcp/src/tools/json_ui_inspect.rs` returns 0
    - `grep -c 'old v1 file' ferro-mcp/src/tools/json_ui_inspect.rs` returns 0
    - `grep -c 'pub mod old' ferro-mcp/src/tools/json_ui_inspect.rs` returns 0
    - `grep -q 'stale_artifact' ferro-mcp/src/tools/json_ui_inspect.rs` succeeds
    - The function `fn test_ignores_non_json_files()` is still present (verified by `grep -q 'fn test_ignores_non_json_files' ferro-mcp/src/tools/json_ui_inspect.rs`)
  </acceptance_criteria>
  <done>
    Test renamed in-place; behavioral coverage preserved; no v1 fixture strings remain.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Test-fixture rename. No executable behavior change. No new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-07 | n/a | test_ignores_non_json_files fixture rename | accept | Pure string rename in a test fixture. No production code change. `cargo test` confirms test still passes. |
</threat_model>

<verification>
```bash
! grep -n 'old_view.rs\|old v1 file\|pub mod old' ferro-mcp/src/tools/json_ui_inspect.rs
cargo test -p ferro-mcp --all-features --lib json_ui_inspect
```
</verification>

<success_criteria>
- Test exists with neutral fixture names
- Test still passes
- No `old_view.rs`, `old v1 file`, `pub mod old` strings in file
</success_criteria>

<output>
After completion, create `.planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-04-SUMMARY.md`.
</output>
