---
phase: 160
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/protocol/src/terminology.md
  - docs/protocol/src/architecture.md
  - docs/protocol/src/rendering.md
autonomous: true
requirements_addressed: [D-07, Pattern-5]
must_haves:
  truths:
    - "docs/protocol/src/terminology.md JsonUiRenderer paragraph describes the v2 Spec output without contrasting against v1"
    - "docs/protocol/src/architecture.md JsonUiRenderer bullet describes the v2 Spec output without contrasting against v1"
    - "docs/protocol/src/rendering.md Output Format section describes the actual v2 Spec wire shape (schema/root/elements/title/layout/data)"
    - "No `ferro-json-ui/v1` literal remains in any of the three files"
  artifacts:
    - path: "docs/protocol/src/terminology.md"
      provides: "Renderer definition referencing the v2 schema URI"
      contains: "ferro-json-ui/v2 reference; no ferro-json-ui/v1"
    - path: "docs/protocol/src/architecture.md"
      provides: "JsonUiRenderer bullet citing the v2 Spec shape"
      contains: "ferro-json-ui/v2 reference; no ferro-json-ui/v1"
    - path: "docs/protocol/src/rendering.md"
      provides: "Output Format section describing the actual current Spec wire shape"
      contains: "schema/root/elements/title/layout/data; no `version` field; no `body` field; no ferro-json-ui/v1"
  key_links:
    - from: "docs/protocol/src/rendering.md Output Format"
      to: "ferro-json-ui/src/spec.rs:64-89 (Spec struct definition)"
      via: "shape parity"
      pattern: "documentation reflects the actual struct fields"
---

<objective>
Per CONTEXT.md D-07 and RESEARCH Pattern 5, rewrite the three protocol docs that still contrast the v2 renderer against v1. Each rewrite is a full paragraph reframe (not a `v1` → `v2` string substitution per Pattern-5 anti-pattern rule). The wire-shape sentence in `rendering.md` is also factually wrong about v1 ("envelope containing `schema`, `version`, `title`, `body` fields"); the rewrite both removes the v1 framing AND corrects the wire shape to match the actual `Spec` struct.

Purpose: agent-readable protocol docs that describe JSON-UI as it exists, not as it once was.

Output: three docs/protocol files with neutral, accurate descriptions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-CONTEXT.md
@.planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-RESEARCH.md
@docs/protocol/src/terminology.md
@docs/protocol/src/architecture.md
@docs/protocol/src/rendering.md
@ferro-json-ui/src/spec.rs

<interfaces>
Current Spec wire shape (verified at ferro-json-ui/src/spec.rs:64-89):
  - `schema` (renamed via `#[serde(rename = "$schema")]`)
  - `root: String`
  - `elements: HashMap<String, Element>`
  - `title: Option<TitleBinding>`
  - `layout: Option<String>`
  - `data: Value`

These are the fields the rewritten rendering.md must reference.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite docs/protocol/src/terminology.md Renderer definition</name>
  <files>docs/protocol/src/terminology.md</files>
  <read_first>
    - docs/protocol/src/terminology.md (lines 90-105 — current Renderer definition)
    - .planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-RESEARCH.md (Pattern 5 §(a) — verbatim replacement)
  </read_first>
  <action>
Locate the **Renderer** definition (currently at lines 94-100). Replace the paragraph verbatim per RESEARCH Pattern 5 §(a):

CURRENT (delete):
```
**Renderer**
: A component that transforms a Service Definition, its derived Intent
  Scores, and a Render Context into a UI component tree. Defined by the
  `Renderer` trait. The output format is implementation-specific:
  `JsonUiRenderer` produces ferro-json-ui/v1 component trees, but
  implementations MAY target A2UI, HTML, native components, or any other
  format.
```

REPLACEMENT (write):
```
**Renderer**
: A component that transforms a Service Definition, its derived Intent
  Scores, and a Render Context into a UI component tree. Defined by the
  `Renderer` trait. The output format is implementation-specific:
  `JsonUiRenderer` produces a `Spec` conforming to the
  `ferro-json-ui/v2` schema; implementations MAY target A2UI, HTML,
  native components, or any other format.
```

No other change to the file.
  </action>
  <verify>
    <automated>! grep -n 'ferro-json-ui/v1' docs/protocol/src/terminology.md && grep -q 'ferro-json-ui/v2' docs/protocol/src/terminology.md && grep -q 'Spec.*conforming to' docs/protocol/src/terminology.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ferro-json-ui/v1' docs/protocol/src/terminology.md` returns 0
    - `grep -c 'ferro-json-ui/v2' docs/protocol/src/terminology.md` returns at least 1
    - `grep -c 'A component that transforms a Service Definition' docs/protocol/src/terminology.md` returns exactly 1 (paragraph preserved)
    - The string `produces ferro-json-ui/v1 component trees, but` is absent
  </acceptance_criteria>
  <done>
    Renderer definition reframed to v2 wire shape; no `v1` reference; no `but` contrast clause.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite docs/protocol/src/architecture.md JsonUiRenderer bullet</name>
  <files>docs/protocol/src/architecture.md</files>
  <read_first>
    - docs/protocol/src/architecture.md (lines 165-180 — current JsonUiRenderer bullet within Pluggability section)
    - .planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-RESEARCH.md (Pattern 5 §(b) — verbatim replacement)
  </read_first>
  <action>
Locate the JsonUiRenderer bullet (currently at lines 172-173). Replace the entire bullet-list block (lines 172-178) verbatim per RESEARCH Pattern 5 §(b):

CURRENT (delete):
```
- **JsonUiRenderer** — The reference implementation. Produces ferro-json-ui/v1
  component trees (Table, Card, Form, Badge, Progress, etc.).
- **A2UI** — A potential implementation targeting A2UI component catalogs.
- **HTML** — A potential implementation producing static or server-rendered
  HTML.
- **Native** — A potential implementation producing native mobile component
  trees.
```

REPLACEMENT (write):
```
- **JsonUiRenderer** — The reference implementation. Produces a `Spec`
  conforming to the `ferro-json-ui/v2` schema: a flat ID-keyed element map
  with components such as Table, Card, Form, Badge, and Progress.
- **A2UI** — A potential implementation targeting A2UI component catalogs.
- **HTML** — A potential implementation producing static or server-rendered
  HTML.
- **Native** — A potential implementation producing native mobile component
  trees.
```

No other change to the file. The Pluggability section preamble (lines 167-170) and the closing paragraph (lines 180-182) stay as-is.
  </action>
  <verify>
    <automated>! grep -n 'ferro-json-ui/v1' docs/protocol/src/architecture.md && grep -q 'ferro-json-ui/v2' docs/protocol/src/architecture.md && grep -q 'flat ID-keyed element map' docs/protocol/src/architecture.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ferro-json-ui/v1' docs/protocol/src/architecture.md` returns 0
    - `grep -c 'ferro-json-ui/v2' docs/protocol/src/architecture.md` returns at least 1
    - `grep -c 'A2UI' docs/protocol/src/architecture.md` returns at least 2 (bullet + preamble references preserved)
    - The string `Produces ferro-json-ui/v1 component trees` is absent
  </acceptance_criteria>
  <done>
    JsonUiRenderer bullet reframed to v2 Spec shape; other A2UI/HTML/Native bullets preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite docs/protocol/src/rendering.md Output Format section</name>
  <files>docs/protocol/src/rendering.md</files>
  <read_first>
    - docs/protocol/src/rendering.md (lines 125-140 — current Output Format section)
    - ferro-json-ui/src/spec.rs (lines 64-89 — actual Spec struct fields used in the rewrite)
    - .planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-RESEARCH.md (Pattern 5 §(c) — verbatim replacement)
  </read_first>
  <action>
Locate the Output Format section (currently at lines 132-136). Replace the entire Output Format section (heading + two paragraphs) verbatim per RESEARCH Pattern 5 §(c):

CURRENT (delete lines 132-136):
```
## Output Format

The protocol does not prescribe a specific JSON output schema for renderers. Each renderer implementation defines its own component vocabulary and envelope structure.

The reference `JsonUiRenderer` produces output conforming to the `ferro-json-ui/v1` schema, with a top-level envelope containing `schema`, `version`, `title`, and `body` fields. Other renderers (e.g., A2UI, HTML) MAY produce entirely different output structures while remaining conformant, provided they implement the `Renderer` trait.
```

REPLACEMENT (write):
```
## Output Format

The protocol does not prescribe a specific JSON output schema for renderers. Each renderer implementation defines its own component vocabulary and envelope structure.

The reference `JsonUiRenderer` produces output conforming to the `ferro-json-ui/v2` schema: a top-level `Spec` with a `$schema` tag, a `root` element ID, a flat `elements` map keyed by ID, and optional `title`, `layout`, and `data` fields. Children inside `elements` are referenced by ID rather than by nesting. Other renderers (e.g., A2UI, HTML) MAY produce entirely different output structures while remaining conformant, provided they implement the `Renderer` trait.
```

The replacement is factually grounded in `ferro-json-ui/src/spec.rs:64-89` (Spec struct definition). It corrects two pre-existing inaccuracies: (1) the old text claims a `version` field exists (it does not); (2) the old text claims a `body` field exists (it does not — the field is `elements: HashMap<String, Element>`).

No other change to the file.
  </action>
  <verify>
    <automated>! grep -n 'ferro-json-ui/v1' docs/protocol/src/rendering.md && grep -q 'ferro-json-ui/v2' docs/protocol/src/rendering.md && grep -q '`elements` map keyed by ID' docs/protocol/src/rendering.md && ! grep -nE 'schema.*version.*title.*body' docs/protocol/src/rendering.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ferro-json-ui/v1' docs/protocol/src/rendering.md` returns 0
    - `grep -c 'ferro-json-ui/v2' docs/protocol/src/rendering.md` returns at least 1
    - `grep -c 'elements.*map keyed by ID' docs/protocol/src/rendering.md` returns at least 1
    - `grep -cE 'envelope containing.*schema.*version.*title.*body' docs/protocol/src/rendering.md` returns 0
  </acceptance_criteria>
  <done>
    Output Format section reframed to v2 Spec wire shape; references to non-existent `version` and `body` fields removed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Documentation rewrites. No executable behavior change. No new input handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-09 | n/a | three docs/protocol/src/*.md rewrites | accept | Pure prose rewrite. No code path touched. Mdbook build (if run) confirms valid markdown. |
</threat_model>

<verification>
```bash
# Negative: no v1 schema literal in any of the three files
! grep -n 'ferro-json-ui/v1' docs/protocol/src/terminology.md docs/protocol/src/architecture.md docs/protocol/src/rendering.md

# Positive: v2 schema literal present in all three
grep -l 'ferro-json-ui/v2' docs/protocol/src/terminology.md docs/protocol/src/architecture.md docs/protocol/src/rendering.md
```
</verification>

<success_criteria>
- Three files no longer contain `ferro-json-ui/v1`
- Three files all reference `ferro-json-ui/v2`
- Spec wire shape in `rendering.md` matches the actual `Spec` struct (no `version`, no `body` claims)
- No contrast clauses (`but ...`) remain
</success_criteria>

<output>
After completion, create `.planning/phases/160-remove-v1-json-ui-api-from-ferro-json-ui-delete-view-rs-comp/160-06-SUMMARY.md`.
</output>
