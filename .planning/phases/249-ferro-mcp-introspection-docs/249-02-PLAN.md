---
phase: 249-ferro-mcp-introspection-docs
plan: 02
type: execute
wave: 2
depends_on: [1]
files_modified:
  - docs/src/features/offload.md
  - docs/src/features/queues.md
  - docs/src/features/deployments.md
  - docs/src/SUMMARY.md
autonomous: true
requirements: [OFFLOAD-06]
nyquist_compliant: true

must_haves:
  truths:
    - "A developer reading the docs finds one canonical page (offload.md) covering authoring #[offload], the result-handle/streaming pattern, the deployable-worker scaling recipe, honest limitations, and the deferred elastic 2.0 non-goals"
    - "queues.md no longer duplicates the offload prose; it points to offload.md (no drift-prone second copy)"
    - "deployments.md cross-links the scaling recipe; the mdBook nav registers offload.md"
  artifacts:
    - path: "docs/src/features/offload.md"
      provides: "Canonical work-distribution page: authoring, result path, scaling model, honest limitations, 2.0 non-goals"
      contains: "## Honest limitations"
    - path: "docs/src/SUMMARY.md"
      provides: "Nav registration for offload.md between queues.md and notifications.md"
      contains: "features/offload.md"
    - path: "docs/src/features/queues.md"
      provides: "Offloading section reduced to a pointer into offload.md"
      contains: "offload.md"
    - path: "docs/src/features/deployments.md"
      provides: "See-also cross-link to the offload scaling model"
      contains: "offload.md"
  key_links:
    - from: "docs/src/SUMMARY.md"
      to: "docs/src/features/offload.md"
      via: "mdBook nav entry"
      pattern: "\\[Work Distribution \\(Offload\\)\\]\\(features/offload.md\\)"
    - from: "docs/src/features/queues.md"
      to: "offload.md"
      via: "pointer paragraph replacing the relocated section"
      pattern: "\\(offload.md\\)"
---

<objective>
Create the canonical `docs/src/features/offload.md` page for the work-distribution feature,
relocate the existing offload prose out of `queues.md` (reducing that section to a pointer),
cross-link the scaling recipe from `deployments.md`, and register the new page in the mdBook nav
(`docs/src/SUMMARY.md`).

Purpose: Deliverable B for OFFLOAD-06 (SC#2, SC#3) — a single canonical home for the authoring
surface, the result-handle/streaming pattern, the deployable-worker scaling model (the
"many-user" capacity answer), the honest limitations, and the deferred elastic 2.0 non-goals,
written in neutral public-repository voice.

Output:
- NEW `docs/src/features/offload.md`.
- `queues.md` §Offloading reduced to a cross-link.
- `deployments.md` "See also" callout to the offload scaling model.
- `docs/src/SUMMARY.md` nav entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/249-ferro-mcp-introspection-docs/249-CONTEXT.md
@.planning/phases/249-ferro-mcp-introspection-docs/249-RESEARCH.md
@.planning/phases/249-ferro-mcp-introspection-docs/249-PATTERNS.md
@docs/superpowers/specs/2026-06-24-offload-work-distribution-design.md
@.planning/phases/248-deployable-ferro-worker-runtime/248-CONTEXT.md

<facts_verified>
<!-- The four RESEARCH.md ASSUMED facts (A1–A4) were grep-verified against code on 2026-08-15.
     Task 1 re-verifies them before writing prose (audit-discrepancies discipline). -->
- A1 CONFIRMED: `enqueue_and_mark_pending`, `read_result`, `read_result_redacted`, `resolve` live
  in `framework/src/offload.rs`, resolvable at `::ferro::offload::*` (offload.rs L380 comment).
  The paths already used in queues.md prose are correct.
- A2 CONFIRMED: `CreateProjectionSnapshotsTable` is `pub use migration::Migration as
  CreateProjectionSnapshotsTable` in `ferro-projection/src/lib.rs:87`.
- A3 CONFIRMED: `--no-worker` flag exists in `app/src/main.rs` (`no_worker: bool`, L82; documented
  in the file header L12: `./app serve --no-worker`).
- A4 CONFIRMED: no inbound `queues.md#...` anchor links anywhere in `docs/src/` — relocation is
  safe (no cross-doc links break).
</facts_verified>

<relocation_source>
<!-- Exact line ranges in queues.md (verified 2026-08-15). -->
- `## Offloading Service Methods` — queues.md L188
- `## Subscribe and await an offloaded result` — queues.md L291
- Relocatable block ENDS at L380 (the `CreateProjectionSnapshotsTable` migration code block).
- `## WorkerLoop Configuration` (L382) onward STAYS in queues.md — do NOT relocate it.
</relocation_source>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep-verify the four ASSUMED scaling facts, then create the canonical offload.md</name>
  <files>docs/src/features/offload.md, docs/src/features/queues.md</files>
  <read_first>
    - docs/src/features/queues.md lines 188–380 (the two sections to relocate: `## Offloading Service Methods` and `## Subscribe and await an offloaded result`, ending at the `CreateProjectionSnapshotsTable` migration block just before `## WorkerLoop Configuration` at L382)
    - docs/src/features/caching.md lines 1–3 (opening-paragraph register: one factual sentence, no marketing)
    - docs/superpowers/specs/2026-06-24-offload-work-distribution-design.md (§Scaling model, §Honest limitations, §Future direction, §Non-Goals — the source for the new prose)
    - .planning/phases/248-deployable-ferro-worker-runtime/248-CONTEXT.md (decided worker CLI surface: D-02 `<app-bin> worker --queue <name>` repeatable, D-03 no-`--queue` = all queues, D-05 `serve --no-worker`, D-04 default queue `default`)
    - .planning/phases/249-ferro-mcp-introspection-docs/249-PATTERNS.md (§offload.md page structure + §Honest limitations content)
    - CLAUDE.md (repository-docs-must-read-as-neutral discipline — no "killer feature"/"the bet"/"load-bearing"/"we accept that" framing; 2.0 direction is future work, not commitments)
  </read_first>
  <action>
**Step A — re-verify the four ASSUMED facts (audit discipline) BEFORE writing prose.** Run each and
confirm the expected result; if any diverges from the `<facts_verified>` block, correct the prose
to match code (never write prose that contradicts the source):

```bash
grep -n "pub async fn enqueue_and_mark_pending\|pub async fn read_result\|pub async fn read_result_redacted\|pub async fn resolve" framework/src/offload.rs   # A1: all present
grep -n "pub use migration::Migration as CreateProjectionSnapshotsTable" ferro-projection/src/lib.rs   # A2
grep -n "no_worker" app/src/main.rs   # A3: flag exists
grep -rn "queues.md#" docs/src/   # A4: expect zero matches
```

**Step B — create `docs/src/features/offload.md`** with the structure below (neutral public voice
throughout). Relocate the authoring + result-path prose from queues.md L188–380 into the first two
sections, preserving every code example exactly; author the Scaling model and Non-goals sections
new from the spec + Phase 248 facts.

```markdown
# Work Distribution (Offload)

Ferro turns a `#[service]` trait method into a distributable unit of work by marking it
`#[offload]`: the framework derives a `ferro-queue` Job and a serializable payload from the method
signature, runs it on a horizontally scalable worker, and streams the result back through the
read-model and broadcast path — the originating request never blocks.

## Authoring an offloadable method
  <!-- relocate queues.md §"Offloading Service Methods" (L188–~289): authoring surface code
       example, typed handle, success-type contract table, serializable contract + compile-time
       enforcement. Preserve the code examples exactly. -->

## Result path
  <!-- relocate queues.md §"Subscribe and await an offloaded result" (~L291–380): channel
       convention, request side (`::ferro::offload::enqueue_and_mark_pending`), server-side
       race-safe `resolve`, client-side `read_result_redacted`, delta payload + redaction, and the
       `CreateProjectionSnapshotsTable` migration note. Keep the `::ferro::offload::*` paths and
       the `use ferro_projection::CreateProjectionSnapshotsTable;` example — all verified. -->

## Scaling model
Answer to "how does a Ferro app serve many users?": a stateless web tier + replicable workers +
cache + a shared queue/broadcast transport.

### Deploy recipe
- Web replicas run the application binary as `<app-bin> serve --no-worker` — the HTTP tier with the
  in-process worker loop disabled.
- Worker replicas run `<app-bin> worker --queue <class>` (the flag is repeatable for multiple
  queues; omitting `--queue` drains all registered queues). The worker is the application's own
  binary, not a separate `ferro` CLI binary — job handlers live in the app crate.
- Single-binary development runs `<app-bin> serve`, which retains the in-process worker loop and
  drains all queues.
- All replicas share the same queue backend and broadcast transport.
- Capacity scales by running more worker replicas; N is chosen by the operator / platform /
  cluster scheduler. The framework provides no autoscaler.

### Worker classes and fault isolation
A worker class is a queue set. Each class is an independent fault domain: a saturated `media` class
does not starve the `default` class, because they are consumed by separate worker replicas.

### Honest limitations
- **Connection ceiling.** `DB_MAX_CONNECTIONS` defaults to 10 per process; total database
  connections are roughly `10 × (web replicas + worker replicas)`. Against a typical Postgres
  ceiling near 100, a modest deployment (e.g. 5 web + 5 workers) already reaches it. For scale-out,
  place a connection pooler such as PgBouncer between the replicas and Postgres.
- **No built-in metrics export.** Generated deployment manifests (DigitalOcean App Platform YAML,
  Docker Compose) do not include an OpenTelemetry collector or Prometheus scrape configuration.
  Monitoring worker throughput and queue depth requires a separately provisioned observability
  stack; the framework does not emit one.
- **Latency is worker-scheduling-bound.** Result latency depends on worker scheduling, so the
  offload path is unsuited to sub-second interactive computation that must complete before a
  response renders. It is the right shape for deferred-result work — report generation, imports,
  model inference.

## Non-goals (2.0 direction)
The following are out of scope for the current design and recorded here as possible future work,
not commitments:
- Elastic scale-to-zero driven by queue depth (e.g. a KEDA `ScaledObject`).
- Warm-start / checkpoint-restore for faster scale-from-zero cold starts.
- Non-Kubernetes actuation through a worker-fleet provider port (e.g. Nomad).
- WASM/WASI isolates as a lighter execution unit.
```

Voice check before saving: no "killer feature", "the bet", "load-bearing", "we accept that", "our
risk", "forcing function". The Honest limitations and Non-goals sections state facts and mitigations
plainly, with no apologetic or strategic framing.
  </action>
  <verify>
    <automated>test -f docs/src/features/offload.md && grep -q "## Honest limitations" docs/src/features/offload.md && grep -q "## Non-goals" docs/src/features/offload.md && grep -q "serve --no-worker" docs/src/features/offload.md && grep -q "worker --queue" docs/src/features/offload.md && grep -q "CreateProjectionSnapshotsTable" docs/src/features/offload.md && echo OFFLOAD_MD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/features/offload.md` exists (`test -f` succeeds)
    - `grep -c "^## " docs/src/features/offload.md` >= 4 (Authoring, Result path, Scaling model, Non-goals)
    - `grep -q "### Honest limitations\|## Honest limitations" docs/src/features/offload.md` — the Honest Limitations subsection is present (D-10)
    - `grep -q "PgBouncer" docs/src/features/offload.md` — connection-ceiling mitigation present (D-10)
    - `grep -q "OpenTelemetry\|Prometheus\|metrics" docs/src/features/offload.md` — no-metrics limitation present (D-10)
    - `grep -q "serve --no-worker" docs/src/features/offload.md` AND `grep -q "worker --queue" docs/src/features/offload.md` — the deploy recipe describes the decided Phase 248 CLI surface (D-09)
    - `grep -qi "KEDA\|scale-to-zero\|WASM\|Nomad" docs/src/features/offload.md` — 2.0 non-goals present (D-11)
    - `grep -q "CreateProjectionSnapshotsTable" docs/src/features/offload.md` AND `grep -q "enqueue_and_mark_pending" docs/src/features/offload.md` — result-path prose relocated intact (A1/A2 paths preserved)
    - Neutral-voice check: `grep -Eic "killer feature|the bet|load-bearing|we accept that|forcing function" docs/src/features/offload.md` == 0
  </acceptance_criteria>
  <done>
    `offload.md` exists as the canonical page: authoring + result-path prose relocated verbatim
    (code examples and `::ferro::offload::*` / `CreateProjectionSnapshotsTable` paths intact), a
    new Scaling model section with the Phase 248 deploy recipe, an Honest limitations subsection
    (PgBouncer, no metrics, latency), and a 2.0 Non-goals section — all in neutral public voice.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reduce queues.md §Offloading to a pointer and register offload.md in SUMMARY.md nav</name>
  <files>docs/src/features/queues.md, docs/src/SUMMARY.md</files>
  <read_first>
    - docs/src/features/queues.md lines 188–382 (confirm the exact block to remove ends immediately before `## WorkerLoop Configuration` at L382)
    - docs/src/SUMMARY.md lines 21–59 (the `# Features` block; `queues.md` at L24, `notifications.md` at L25 — the insertion point is between them)
    - docs/src/features/offload.md (the page just created — target of the pointer)
  </read_first>
  <action>
**Part A — queues.md pointer reduction (D-08).** Delete the relocated block in
`docs/src/features/queues.md` — everything from `## Offloading Service Methods` (L188) through the
end of the `CreateProjectionSnapshotsTable` migration code block (L380, the line before the blank
line preceding `## WorkerLoop Configuration` at L382). Replace it with this exact pointer paragraph
(the `## WorkerLoop Configuration` section and everything after it must remain untouched):

```markdown
## Offloading Service Methods

For full documentation of `#[offload]` — authoring an offloadable method, the result-handle
subscribe/await streaming pattern, the deployable-worker scaling model, and the non-goals — see the
dedicated page: [Work Distribution (Offload)](offload.md).
```

Do NOT relocate or alter `## WorkerLoop Configuration`, `## CPU-Heavy Jobs`, `## Error Handling`,
`## Failed Job Inspection`, `## Migration Guide`, or `## MCP Tools` — those stay in `queues.md`.

**Part B — SUMMARY.md nav registration.** In `docs/src/SUMMARY.md`, insert the new nav entry
immediately after the `queues.md` line and before the `notifications.md` line, matching the exact
indentation of the surrounding `# Features` entries:

```markdown
- [Queues & Background Jobs](features/queues.md)
- [Work Distribution (Offload)](features/offload.md)
- [Notifications](features/notifications.md)
```

No `book.toml` change is needed — SUMMARY.md is the only nav edit point for a new features page.
  </action>
  <verify>
    <automated>grep -q "\[Work Distribution (Offload)\](features/offload.md)" docs/src/SUMMARY.md && grep -q "(offload.md)" docs/src/features/queues.md && ! grep -q "enqueue_and_mark_pending" docs/src/features/queues.md && grep -q "## WorkerLoop Configuration" docs/src/features/queues.md && echo QUEUES_NAV_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\[Work Distribution (Offload)\](features/offload.md)" docs/src/SUMMARY.md` == 1 (nav entry present)
    - The nav entry sits between queues and notifications — `grep -n "features/queues.md\|features/offload.md\|features/notifications.md" docs/src/SUMMARY.md` shows offload.md's line number is between the other two
    - `grep -q "(offload.md)" docs/src/features/queues.md` — pointer link present in queues.md
    - `grep -c "enqueue_and_mark_pending" docs/src/features/queues.md` == 0 — the relocated result-path prose is GONE from queues.md (no drift-prone second copy)
    - `grep -q "## Subscribe and await an offloaded result" docs/src/features/queues.md` returns nothing — the relocated section header is removed
    - `grep -q "## WorkerLoop Configuration" docs/src/features/queues.md` AND `grep -q "## MCP Tools" docs/src/features/queues.md` — the non-relocated sections remain intact
  </acceptance_criteria>
  <done>
    queues.md §Offloading is a single pointer paragraph into offload.md with no duplicated prose;
    the WorkerLoop/CPU/Error/Failed-Job/Migration/MCP sections are untouched; SUMMARY.md registers
    offload.md in the nav between queues and notifications.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cross-link the offload scaling model from deployments.md and validate the mdBook build</name>
  <files>docs/src/features/deployments.md, docs/src/features/offload.md</files>
  <read_first>
    - docs/src/features/deployments.md lines 1–40 (the page covers `ferro-deployments`: immutable deployment rows, artifact storage — a DIFFERENT subsystem from the offload worker; the cross-link is a brief "See also" callout, not a section)
    - docs/src/features/offload.md (confirm the `## Scaling model` heading anchor is `#scaling-model` for the deep link)
  </read_first>
  <action>
**Part A — deployments.md cross-link (D-08).** `deployments.md` documents `ferro-deployments`
(immutable deployment rows, artifact storage, atomic promote) — a distinct subsystem from the
offload worker fleet. Add a brief blockquote "See also" callout near the top of the page (after the
opening `# Deployments` intro, before `## Setup`) rather than a full section, so `deployments.md`
stays focused on the artifact/promote content it already covers:

```markdown
> **Horizontal scaling with background workers:** For running the application binary's `worker`
> subcommand at N replicas as an independent queue consumer (the "stateless web tier + replicable
> workers" recipe), see [Work Distribution (Offload)](offload.md#scaling-model).
```

**Part B — validate the whole docs deliverable builds.** Confirm mdBook renders the new nav and all
three edited pages without a broken-link error. If `mdbook` is on PATH, run `mdbook build docs`; if
it is not installed, fall back to the static link check below (both are acceptable — the goal is to
prove the nav entry resolves and no relocated anchor dangles):

```bash
# Preferred:
mdbook build docs 2>&1 | tail -20 || true
# Fallback link sanity (must all resolve):
test -f docs/src/features/offload.md          # nav target exists
grep -q "features/offload.md" docs/src/SUMMARY.md   # nav entry present
grep -q "(offload.md#scaling-model)" docs/src/features/deployments.md   # deep link present
grep -q "## Scaling model" docs/src/features/offload.md   # anchor target exists
```
  </action>
  <verify>
    <automated>grep -q "(offload.md#scaling-model)" docs/src/features/deployments.md && grep -q "## Scaling model" docs/src/features/offload.md && grep -q "features/offload.md" docs/src/SUMMARY.md && echo DEPLOY_XLINK_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "(offload.md#scaling-model)" docs/src/features/deployments.md` == 1 — the deep cross-link is present
    - `grep -q "## Scaling model" docs/src/features/offload.md` — the anchor the deep link points to exists (mdBook slugifies to `#scaling-model`)
    - `deployments.md` retains its own sections — `grep -q "## Artifact Storage" docs/src/features/deployments.md` AND `grep -q "## Atomic Promote Model" docs/src/features/deployments.md` (the callout did not displace existing content)
    - If `mdbook` is available: `mdbook build docs` completes without an ERROR line about a missing file or broken link (`mdbook build docs 2>&1 | grep -i "error" ; test $? -ne 0` — no error lines)
  </acceptance_criteria>
  <done>
    deployments.md carries a "See also" callout deep-linking `offload.md#scaling-model` without
    losing its own artifact/promote content; the docs build (or static link check) confirms the nav
    entry and cross-links resolve with no dangling anchor.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository docs → public reader | `docs/src/**` is treated as a public artifact (CLAUDE.md). The content boundary is editorial: prose must not leak internal-strategy voice or contradict shipped code. |

This plan authors and edits Markdown documentation only. It introduces no code, no runtime, no
network endpoint, no auth surface, no input-handling path, and no secret. There is no executable
attack surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-249-04 | Information disclosure | docs prose (public repository artifact) | mitigate | The neutral-public-voice discipline (CLAUDE.md) is enforced by the Task 1 voice check and an acceptance-criteria grep (`killer feature|the bet|load-bearing|we accept that|forcing function` count == 0). 2.0 directions are framed as future work, not commitments — no internal-strategy leakage. |
| T-249-05 | Tampering | doc claims vs shipped code | accept | Docs describe an existing shipped surface (Phases 244–248). The four ASSUMED facts are grep-verified against code in Task 1 before prose is written, so the page cannot document a nonexistent flag or path. Residual risk is stale prose if code changes later — accepted as normal doc-maintenance drift, not a security threat. |

No HIGH or MEDIUM severity threat. This is a documentation-only change with no executable surface;
the only meaningful discipline is editorial (neutral voice + code-accurate claims), both enforced
by acceptance criteria.
</threat_model>

<verification>
- `docs/src/features/offload.md` exists with all four `##` sections (Authoring, Result path, Scaling model, Non-goals), an Honest limitations subsection (PgBouncer / no-metrics / latency), and the 2.0 non-goals.
- `queues.md` §Offloading is a pointer paragraph with no duplicated result-path prose (`enqueue_and_mark_pending` count == 0); WorkerLoop/CPU/Error/Failed-Job/Migration/MCP sections untouched.
- `deployments.md` deep-links `offload.md#scaling-model` and keeps its own artifact/promote sections.
- `docs/src/SUMMARY.md` registers `features/offload.md` between queues and notifications.
- Four ASSUMED facts re-grepped before prose; neutral-voice grep clean; mdBook build (or static link check) resolves.
</verification>

<success_criteria>
- SC#2: docs cover authoring an `#[offload]` method, the result handle + subscribe/await streaming pattern, the deployable worker / scaling model, and the deferred elastic direction.
- SC#3: the "many-user" scaling answer (stateless tier + replicable workers + cache + queue) is documented as the framework's capacity story, with a concrete deploy recipe (D-09).
- D-07 honored: `offload.md` is the canonical home.
- D-08 honored: `queues.md` reduced to a pointer; `deployments.md` cross-links the scaling recipe; nav registers the page.
- D-09/D-10 honored: concrete deploy recipe + Honest Limitations (PgBouncer, no OTel/metrics, worker-scheduling-bound latency).
- D-11 honored: deferred elastic direction documented as 2.0 non-goals in neutral public voice.
</success_criteria>

<output>
After completion, create `.planning/phases/249-ferro-mcp-introspection-docs/249-02-SUMMARY.md`.
</output>
