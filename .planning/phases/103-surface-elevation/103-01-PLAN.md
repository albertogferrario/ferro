---
phase: 103-surface-elevation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-json-ui/src/render.rs
  - ferro-json-ui/src/layout.rs
  - ferro-theme/assets/default.css
autonomous: false
requirements: [SRF-01, SRF-02, SRF-03, SRF-04, SRF-05, SRF-06]

must_haves:
  truths:
    - "Card component renders with bg-card, not bg-background"
    - "Modal inner panel renders with bg-card"
    - "StatCard renders with bg-card"
    - "NotificationDropdown panel (render.rs and layout.rs) renders with bg-card"
    - "Checklist renders with bg-card (card-tier widget with shadow)"
    - "Sidebar remains bg-background (persistent frame, not elevated)"
    - "Table thead remains bg-surface (mid-tier panel stripe)"
    - "All 8 dark mode token pairs pass WCAG 4.5:1 contrast"
  artifacts:
    - path: "ferro-json-ui/src/render.rs"
      provides: "bg-card on Card, Modal, StatCard, NotificationDropdown, Checklist"
      contains: "bg-card"
    - path: "ferro-json-ui/src/layout.rs"
      provides: "bg-card on DashboardLayout notification dropdown panel"
      contains: "bg-card"
  key_links:
    - from: "ferro-theme/assets/default.css"
      to: "ferro-json-ui/src/render.rs"
      via: "Tailwind resolves bg-card to --color-card token"
      pattern: "bg-card"
---

<objective>
Apply three-tier surface elevation hierarchy to all elevated components.

Purpose: Cards, modals, stat cards, notification dropdowns, and checklists must be visually elevated above the page background using bg-card instead of bg-background. This creates visual depth: page background (lowest) -> surface (panels/headers) -> card (floating elements). Also verify all 8 dark mode token pairs pass WCAG 4.5:1 contrast.

Output: render.rs and layout.rs with correct surface tier assignments. Updated cosmetic test assertions. Verified dark mode contrast.
</objective>

<execution_context>
@/Users/alberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-surface-elevation/103-RESEARCH.md
@.planning/phases/102-foundation/102-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

Three-tier surface hierarchy (from ferro-theme/assets/default.css):
- bg-background: page-level (light: oklch(100% 0 0), dark: oklch(12% 0 0))
- bg-surface: panels/sidebar (light: oklch(97% 0 0), dark: oklch(17% 0 0))
- bg-card: cards/modals/dropdowns (light: oklch(95% 0 0), dark: oklch(20% 0 0))

Test helpers from Phase 102 (render.rs line 5187):
```rust
fn has_class(html: &str, class: &str) -> bool;
fn assert_element(html: &str, tag: &str, content: &str);
```

Existing cosmetic test assertions that reference bg-background and must be updated:
- Line 2873: `assert!(html.contains("rounded-lg border border-border bg-background shadow-sm"));`
- Line 3915: `assert!(html.contains("bg-background rounded-lg shadow-sm"));`
- Line 1963: `assert!(html.contains("border border-border bg-background text-text hover:bg-surface"));` (Outline button -- keep as-is, buttons are NOT elevated)

Components that should NOT change:
- Sidebar (line 1665): bg-background -- persistent frame
- Table tbody (line 611): bg-background -- flush with page
- Form select (line 836): bg-background -- inputs sit on containing surface
- Outline button (line 1084): bg-background -- button variant, not elevated surface
- Pagination links (lines 1287, 1306, 1318): bg-background -- inline nav elements
- Toggle switch after-pseudo (line 1035): bg-background -- knob indicator
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply bg-card to all elevated components and update cosmetic tests</name>
  <files>ferro-json-ui/src/render.rs, ferro-json-ui/src/layout.rs</files>
  <action>
Change bg-background to bg-card in these 6 locations (elevated floating surfaces):

1. **render_card** (render.rs ~line 381): `bg-background shadow-sm` -> `bg-card shadow-sm`
2. **render_modal** (render.rs ~line 423): `bg-background rounded-lg shadow-lg` -> `bg-card rounded-lg shadow-lg`
3. **render_stat_card** (render.rs ~line 1467): `bg-background rounded-lg shadow-sm` -> `bg-card rounded-lg shadow-sm`
4. **render_checklist** (render.rs ~line 1500): `bg-background rounded-lg shadow-sm` -> `bg-card rounded-lg shadow-sm`
5. **render_notification_dropdown** (render.rs ~line 1612): `bg-background rounded-lg shadow-lg` -> `bg-card rounded-lg shadow-lg`
6. **layout_header_html DashboardLayout** (layout.rs ~line 261): `bg-background rounded-lg shadow-lg` -> `bg-card rounded-lg shadow-lg`

Do NOT change these (they are correctly at background tier):
- Sidebar (render.rs ~line 1665): bg-background -- persistent layout frame
- Table tbody (render.rs ~line 611): bg-background -- page-flush content
- Form select (render.rs ~line 836): bg-background -- input fields
- Outline button (render.rs ~line 1084): bg-background -- button variant
- Pagination links (render.rs ~lines 1287, 1306, 1318): bg-background -- inline nav
- Toggle switch (render.rs ~line 1035): bg-background -- pseudo-element knob
- Header (render.rs ~line 1729): bg-background -- persistent frame
- Sidebar (layout.rs ~line 187): bg-background -- persistent frame

Update cosmetic test assertions in the same commit:
- Line 2873: change `bg-background shadow-sm` to `bg-card shadow-sm` in the contains string
- Line 3915: change `bg-background rounded-lg shadow-sm` to `bg-card rounded-lg shadow-sm`

Do NOT change the outline button test at line 1963 (outline buttons correctly use bg-background).
  </action>
  <verify>
    <automated>cd /Users/alberto/repositories/albertogferrario/ferro && cargo test -p ferro-json-ui 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 elevated components render bg-card. Sidebar, table, form inputs, buttons, pagination remain bg-background. All existing tests pass including updated cosmetic assertions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify dark mode contrast ratios (SRF-06)</name>
  <action>
Surface elevation hierarchy has been applied. User must now verify the 8 critical dark mode token pairs pass WCAG 4.5:1 contrast at oddcontrast.com using oklch values from ferro-theme/assets/default.css dark mode block.
  </action>
  <verify>User reports all 8 pairs pass >= 4.5:1, or identifies failing pairs for token adjustment</verify>
  <done>All 8 dark mode token pairs verified at >= 4.5:1 contrast ratio. If any pair failed, the token L value in default.css was adjusted and re-verified.</done>
  <what-built>Surface elevation hierarchy applied to all card-tier components. Dark mode contrast verification needed.</what-built>
  <how-to-verify>
    Verify the 8 critical dark mode token pairs pass WCAG 4.5:1 contrast at oddcontrast.com.

    Enter each pair using oklch values from ferro-theme/assets/default.css dark mode block:

    | # | Text | Background | Values |
    |---|------|------------|--------|
    | 1 | text | background | oklch(95% 0 0) on oklch(12% 0 0) |
    | 2 | text | surface | oklch(95% 0 0) on oklch(17% 0 0) |
    | 3 | text | card | oklch(95% 0 0) on oklch(20% 0 0) |
    | 4 | text-muted | background | oklch(60% 0 0) on oklch(12% 0 0) |
    | 5 | primary-fg | primary | oklch(100% 0 0) on oklch(65% 0.2 250) |
    | 6 | primary | background | oklch(65% 0.2 250) on oklch(12% 0 0) |
    | 7 | primary-fg | destructive | oklch(100% 0 0) on oklch(60% 0.22 25) |
    | 8 | secondary-fg | secondary | oklch(95% 0 0) on oklch(60% 0.05 250) |

    All pairs must achieve >= 4.5:1.

    If any pair fails: report which pair and its ratio. We will adjust the token L value in default.css.
  </how-to-verify>
  <resume-signal>Type "all pass" if all 8 pairs pass, or report failing pairs with their ratios</resume-signal>
</task>

</tasks>

<verification>
- `cargo test -p ferro-json-ui` passes (all ~400+ tests)
- `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` passes
- grep for `bg-background` in render.rs still returns results (sidebar, table, form, button, pagination, toggle -- these should NOT have changed)
- grep for `bg-card` in render.rs returns Card, Modal, StatCard, Checklist, NotificationDropdown
- grep for `bg-card` in layout.rs returns DashboardLayout notification dropdown
</verification>

<success_criteria>
- Card, Modal, StatCard, Checklist, NotificationDropdown all render bg-card (6 locations changed)
- Sidebar, table body, form inputs, buttons, pagination, toggle remain at bg-background
- DashboardLayout notification dropdown in layout.rs uses bg-card
- Cosmetic tests at lines 2873 and 3915 updated and passing
- All 8 dark mode token pairs verified at >= 4.5:1 contrast ratio
</success_criteria>

<output>
After completion, create `.planning/phases/103-surface-elevation/103-01-SUMMARY.md`
</output>
