---
phase: 156
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/frontend/src/types/inertia-props.ts
  - app/frontend/src/types/routes.ts
  - ferro-cli/src/templates/files/root/gitignore.tpl
  - ferro-cli/src/commands/generate_types.rs
autonomous: true
requirements:
  - D-05
  - D-06
  - D-18
must_haves:
  truths:
    - "git ls-files reports no entries under app/frontend/src/types/"
    - "gitignore.tpl carries an explicit load-bearing comment naming the convention"
    - "generate_types.rs header directs custom types to frontend/src/lib/types/ (not frontend/src/types/)"
  artifacts:
    - path: "ferro-cli/src/templates/files/root/gitignore.tpl"
      provides: "Strengthened load-bearing comment on frontend/src/types/ rule"
      contains: "load-bearing"
    - path: "ferro-cli/src/commands/generate_types.rs"
      provides: "Corrected header comment pointing to frontend/src/lib/types/"
      contains: "frontend/src/lib/types/"
  key_links:
    - from: "gitignore.tpl line 14 comment"
      to: "docs/src/cli/frontend-types.md"
      via: "doc cross-reference in the comment body"
      pattern: "docs/src/cli/frontend-types.md"
    - from: "generate_types.rs lines ~710-711"
      to: "scaffold convention"
      via: "hardcoded output.push_str(\"// frontend/src/lib/types/\\n\")"
      pattern: "frontend/src/lib/types/"
---

<objective>
Untrack the two generated TypeScript files in Ferro's reference `app/`, strengthen the scaffolder gitignore comment to make the convention explicit, and fix the wrong-path comment in the type generator's emitted header. Three independent surface-level edits + one git operation.

Purpose: The phase's first observable truth — generator output is not tracked, the convention is documented at the template, and the generator itself stops misdirecting users — must hold before downstream plans add enforcement (doctor check) or build-time satisfaction (Dockerfile stage).
Output: Two files removed from the git index (working tree intact), one template comment expanded, one generator comment corrected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/156-frontend-types-directory-generator-owned-convention/156-CONTEXT.md
@.planning/phases/156-frontend-types-directory-generator-owned-convention/156-RESEARCH.md
@.planning/phases/156-frontend-types-directory-generator-owned-convention/156-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Untrack the two generated TS files in the reference app</name>
  <files>app/frontend/src/types/inertia-props.ts, app/frontend/src/types/routes.ts</files>
  <read_first>
    - app/frontend/src/types/inertia-props.ts (confirm it exists on disk)
    - app/frontend/src/types/routes.ts (confirm it exists on disk)
    - .planning/phases/156-frontend-types-directory-generator-owned-convention/156-CONTEXT.md (D-05)
  </read_first>
  <action>
Run the exact command:

```
git rm --cached app/frontend/src/types/inertia-props.ts app/frontend/src/types/routes.ts
```

This removes both files from the git index but leaves them on disk. Do NOT delete them with `rm`. Do NOT touch any other files under `app/frontend/src/types/`. Do NOT edit the scaffolder template `ferro-cli/src/templates/files/frontend/src/types/inertia-props.ts.tpl` — that is a separate scaffold artifact, not the reference app file (per Pitfall 4 in RESEARCH.md).

After running `git rm --cached`, verify the gitignore rule already in place (`ferro-cli/src/templates/files/root/gitignore.tpl` line 15 `frontend/src/types/`) is mirrored in the workspace `.gitignore` at the repo root. Run `grep -E 'frontend/src/types' .gitignore` — if it does NOT appear, do not modify the workspace `.gitignore` in this task (D-07 forbids edits to existing scaffolded gitignores; the workspace `.gitignore` is part of the ferro repo itself, not a scaffolded project — but adding the line is OUT OF SCOPE for this plan: the per D-05/D-06 work scope is git-rm-cached + scaffolder template comment only).

After the `git rm --cached`, run `git status --short` and confirm the only staged changes are exactly these two deletions: `D  app/frontend/src/types/inertia-props.ts` and `D  app/frontend/src/types/routes.ts`. Do not stage anything else in this task.

NOTE: this task does NOT make a commit; the phase-level commit step at the end of execute-plan handles staging and committing. The `git rm --cached` operation only updates the index.
  </action>
  <verify>
    <automated>git ls-files app/frontend/src/types/ | wc -l | tr -d ' ' | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files app/frontend/src/types/` returns zero lines
    - `app/frontend/src/types/inertia-props.ts` still exists on disk (verify with `test -f app/frontend/src/types/inertia-props.ts`)
    - `app/frontend/src/types/routes.ts` still exists on disk (verify with `test -f app/frontend/src/types/routes.ts`)
    - `git status --short app/frontend/src/types/` shows exactly two `D ` (staged deletion) entries and nothing else
  </acceptance_criteria>
  <done>Both files removed from the git index; both files retained on disk; no other path touched.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen the gitignore.tpl comment to load-bearing</name>
  <files>ferro-cli/src/templates/files/root/gitignore.tpl</files>
  <read_first>
    - ferro-cli/src/templates/files/root/gitignore.tpl (full file, current state — lines 14-15)
    - .planning/phases/156-frontend-types-directory-generator-owned-convention/156-CONTEXT.md (D-06)
    - .planning/phases/156-frontend-types-directory-generator-owned-convention/156-PATTERNS.md (gitignore.tpl section)
  </read_first>
  <action>
Replace exactly these two lines (currently lines 14-15):

```
# generated_types
frontend/src/types/
```

With these three lines:

```
# generated_types — load-bearing: frontend/src/types/ is owned by `ferro generate-types`.
# Removing this rule breaks the generator-owned convention (see docs/src/cli/frontend-types.md).
frontend/src/types/
```

Do not modify any other line in `gitignore.tpl`. Preserve the trailing blank line (if any) and surrounding `# node`, `# build` section blocks. The `frontend/src/types/` rule itself must remain byte-identical.
  </action>
  <verify>
    <automated>grep -q "load-bearing: frontend/src/types/ is owned by" ferro-cli/src/templates/files/root/gitignore.tpl && grep -q "see docs/src/cli/frontend-types.md" ferro-cli/src/templates/files/root/gitignore.tpl && grep -qE "^frontend/src/types/$" ferro-cli/src/templates/files/root/gitignore.tpl</automated>
  </verify>
  <acceptance_criteria>
    - File contains the literal string `load-bearing: frontend/src/types/ is owned by`
    - File contains the literal string `see docs/src/cli/frontend-types.md`
    - File contains a line exactly matching the regex `^frontend/src/types/$` (the rule itself, unchanged)
    - File still contains the existing sections (`# rust`, `# node`, `# build`, `# ide`, `# env`, `# sqlite`, `# planning`, `# storage`, `# secrets`, `# logs`) — none deleted
    - Verified via: `for s in "# rust" "# node" "# build" "# ide" "# env" "# sqlite" "# planning" "# storage" "# secrets" "# logs"; do grep -qFx "$s" ferro-cli/src/templates/files/root/gitignore.tpl || echo "MISSING: $s"; done` — no MISSING output
  </acceptance_criteria>
  <done>Three-line replacement applied; rule line itself unchanged; surrounding sections intact.</done>
</task>

<task type="auto">
  <name>Task 3: Fix the generate_types.rs header comment path</name>
  <files>ferro-cli/src/commands/generate_types.rs</files>
  <read_first>
    - ferro-cli/src/commands/generate_types.rs (lines 700-720)
    - .planning/phases/156-frontend-types-directory-generator-owned-convention/156-CONTEXT.md (D-18)
    - .planning/phases/156-frontend-types-directory-generator-owned-convention/156-PATTERNS.md (generate_types.rs section)
  </read_first>
  <action>
At lines 710-711 of `ferro-cli/src/commands/generate_types.rs`, the current text is:

```rust
output.push_str("// For custom types not generated here, create manual type files in:\n");
output.push_str("// frontend/src/types/\n");
```

Change line 711 only. The new line 711 must read:

```rust
output.push_str("// frontend/src/lib/types/\n");
```

Do not touch line 710 or any other line in this function. The emitted header comment block (lines 700-714) must otherwise remain byte-identical. The change is exactly one path substitution: `frontend/src/types/` → `frontend/src/lib/types/`.

Run `cargo fmt --all` after the edit (the change is inside a string literal so formatting should be a no-op, but run it anyway to confirm).
  </action>
  <verify>
    <automated>grep -q 'output.push_str("// frontend/src/lib/types/\\\\n");' ferro-cli/src/commands/generate_types.rs && ! grep -q 'output.push_str("// frontend/src/types/\\\\n");' ferro-cli/src/commands/generate_types.rs && cargo fmt --all -- --check</automated>
  </verify>
  <acceptance_criteria>
    - File contains the literal line `output.push_str("// frontend/src/lib/types/\n");`
    - File does NOT contain the literal line `output.push_str("// frontend/src/types/\n");` anywhere
    - `cargo fmt --all -- --check` exits 0
    - The surrounding lines 700, 704, 707, 708, 712 are unchanged (verify by reading the file)
  </acceptance_criteria>
  <done>Single-line path fix applied; fmt clean; no other code touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ferro-cli embedded template -> scaffolded user repo | Comment text in `gitignore.tpl` is copied verbatim into the user's project at `ferro new` time |
| generate_types.rs -> user codebase | Header comment text is emitted verbatim into the user's `inertia-props.ts` / `routes.ts` |
| git index manipulation | `git rm --cached` only changes Ferro's own reference app index; no consumer impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-01-01 | Tampering | gitignore.tpl comment | accept | Comment is informational; even if injected with malicious content it cannot execute. Template is plain text. |
| T-156-01-02 | Information disclosure | git rm --cached on tracked files | accept | Files remain in git history (intentional — D-05 explicitly untracks-only). History exposure pre-dates this phase; no new disclosure created. |
| T-156-01-03 | Repudiation | generate_types.rs comment fix | accept | Single-line string substitution in an auto-generated header. No security-relevant code path. |
</threat_model>

<verification>
Run the workspace gate after all three tasks:

```
cargo fmt --all -- --check
cargo clippy --all --all-targets -- -D warnings
cargo test --all-features
```

All three commands must exit 0. The clippy/test runs validate that the `generate_types.rs` string change did not break any test that asserts on the emitted header text (none exists today per RESEARCH.md, but the workspace gate is cheap to run).

Also confirm:
- `git ls-files app/frontend/src/types/` returns empty
- `grep -c "load-bearing" ferro-cli/src/templates/files/root/gitignore.tpl` returns at least 1
- `grep -c "frontend/src/lib/types" ferro-cli/src/commands/generate_types.rs` returns at least 1
</verification>

<success_criteria>
- Reference app types directory is empty in `git ls-files`.
- `gitignore.tpl` carries the load-bearing annotation referencing `docs/src/cli/frontend-types.md`.
- `generate_types.rs` header comment points to `frontend/src/lib/types/`.
- `cargo fmt --all -- --check` clean.
- `cargo clippy --all --all-targets -- -D warnings` clean.
- `cargo test --all-features` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/156-frontend-types-directory-generator-owned-convention/156-01-SUMMARY.md` per the template.
</output>
