---
phase: 112-agent-first-philosophy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/features/events.md
  - docs/src/features/queues.md
  - docs/src/features/notifications.md
  - docs/src/features/broadcasting.md
  - docs/src/features/storage.md
  - docs/src/features/caching.md
  - docs/src/features/authentication.md
  - docs/src/features/api-resources.md
  - docs/src/features/api.md
  - docs/src/features/rate-limiting.md
  - docs/src/features/database.md
  - docs/src/features/derive-macros.md
  - docs/src/features/validation.md
  - docs/src/features/localization.md
  - docs/src/features/inertia.md
  - docs/src/features/json-ui.md
  - docs/src/features/stripe.md
  - docs/src/features/whatsapp.md
  - docs/src/features/projections.md
  - docs/src/features/ai.md
autonomous: true
requirements: [PHIL-03]

must_haves:
  truths:
    - "Every feature page with relevant MCP tools has a '## MCP Tools' section at the bottom"
    - "Pages with no relevant MCP tools (multi-tenancy.md, testing.md, static-files.md, themes.md) have no MCP section"
    - "Existing MCP sections in api.md and whatsapp.md are standardized to use '## MCP Tools' heading"
    - "ai.md MCP section format verified to match the standard pattern"
  artifacts:
    - path: "docs/src/features/events.md"
      provides: "MCP Tools section for events"
      contains: "## MCP Tools"
    - path: "docs/src/features/database.md"
      provides: "MCP Tools section for database (rich section)"
      contains: "## MCP Tools"
    - path: "docs/src/features/projections.md"
      provides: "MCP Tools section for projections (rich section)"
      contains: "## MCP Tools"
    - path: "docs/src/features/api.md"
      provides: "Standardized MCP Tools section"
      contains: "## MCP Tools"
    - path: "docs/src/features/whatsapp.md"
      provides: "Standardized MCP Tools section"
      contains: "## MCP Tools"
  key_links:
    - from: "docs/src/features/*.md"
      to: "ferro-mcp tools"
      via: "MCP tool name references in ## MCP Tools sections"
      pattern: "## MCP Tools"
---

<objective>
Add `## MCP Tools` sections to all feature documentation pages that have relevant ferro-mcp tools, and standardize existing MCP sections to use the new heading pattern.

Purpose: Only 4 of 25 feature pages currently mention MCP tools. Agents and developers reading feature docs cannot discover the MCP tools available for each feature. This plan makes every relevant MCP tool discoverable from the feature it serves.

Output: 16 feature pages with new `## MCP Tools` sections, 3 existing sections standardized, 4 pages correctly skipped.
</objective>

<execution_context>
@/Users/alberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-agent-first-philosophy/112-CONTEXT.md
@.planning/phases/112-agent-first-philosophy/112-RESEARCH.md

<interfaces>
<!-- Authoritative tool-to-feature mapping from 112-RESEARCH.md -->

Pages that need NEW `## MCP Tools` sections (16 pages):
| Feature Page | MCP Tools |
|---|---|
| events.md | `list_events` |
| queues.md | `list_jobs`, `job_history`, `queue_status` |
| notifications.md | `code_templates` (notifications category) |
| broadcasting.md | `list_broadcast_channels` |
| storage.md | `code_templates` (storage category) |
| caching.md | `cache_inspect` |
| authentication.md | `list_policies`, `session_inspect` |
| api-resources.md | `list_resources` |
| rate-limiting.md | `list_rate_limiters` |
| database.md | `database_schema`, `database_query`, `list_migrations`, `list_models`, `explain_model`, `model_usages`, `relation_map` |
| derive-macros.md | `explain_model` |
| validation.md | `code_templates` (validation category) |
| localization.md | `list_lang_files` |
| inertia.md | `inspect_props`, `list_props`, `generate_types` (verify at execution) |
| json-ui.md | `json_ui_catalog`, `json_ui_inspect`, `json_ui_generate` |
| projections.md | `list_projections`, `inspect_projection`, `render_projection`, `validate_projection`, `projection_coverage` |

Pages that need EXISTING sections STANDARDIZED (3 pages):
| Feature Page | Current Heading | Action |
|---|---|---|
| api.md | `## MCP Integration` | Rename to `## MCP Tools` |
| whatsapp.md | `## MCP Introspection` | Rename to `## MCP Tools` |
| ai.md | `## MCP Tools` | Verify format matches standard (heading already correct) |

Pages with SPECIAL handling (1 page):
| Feature Page | Action |
|---|---|
| stripe.md | Read `ferro-mcp/src/tools/stripe.rs` first to confirm tools exist, then add section |

Pages to SKIP (4 pages):
| Feature Page | Reason |
|---|---|
| multi-tenancy.md | No relevant ferro-mcp tools |
| testing.md | No relevant ferro-mcp tools |
| static-files.md | No relevant ferro-mcp tools |
| themes.md | No relevant ferro-mcp tools |

api-mcp.md: Already a dedicated MCP page — no changes needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MCP Tools sections to 16 feature pages and handle stripe.md</name>
  <files>docs/src/features/events.md, docs/src/features/queues.md, docs/src/features/notifications.md, docs/src/features/broadcasting.md, docs/src/features/storage.md, docs/src/features/caching.md, docs/src/features/authentication.md, docs/src/features/api-resources.md, docs/src/features/rate-limiting.md, docs/src/features/database.md, docs/src/features/derive-macros.md, docs/src/features/validation.md, docs/src/features/localization.md, docs/src/features/inertia.md, docs/src/features/json-ui.md, docs/src/features/projections.md, docs/src/features/stripe.md</files>
  <action>
For each of the 16 feature pages listed in the interfaces section, append a `## MCP Tools` section at the END of the file (after all existing content).

**Section format (standard pattern from research):**

```markdown
## MCP Tools

[One-sentence context: what these tools help agents do for this feature]

### `tool_name`

[Description — what it returns and when to use it]
```

**Detail level (per user decision — at Claude's discretion):**
- Simple tools (one tool, obvious purpose): one-liner description, no subsection header needed
- Medium tools (2-3 tools): subsection headers with 1-2 line descriptions
- Rich sections (database, projections, json-ui with 5+ tools): subsection headers with "When to use" and "Returns" bullets

**Specific guidance per page:**

- **events.md**: `list_events` — one-liner
- **queues.md**: `list_jobs`, `job_history`, `queue_status` — medium detail with subsections
- **notifications.md**: `code_templates` (notifications category) — one-liner referencing code_templates
- **broadcasting.md**: `list_broadcast_channels` — one-liner
- **storage.md**: `code_templates` (storage category) — one-liner referencing code_templates
- **caching.md**: `cache_inspect` — one-liner
- **authentication.md**: `list_policies`, `session_inspect` — medium detail
- **api-resources.md**: `list_resources` — one-liner
- **rate-limiting.md**: `list_rate_limiters` — one-liner
- **database.md**: `database_schema`, `database_query`, `list_migrations`, `list_models`, `explain_model`, `model_usages`, `relation_map` — RICH section with subsections and bullets (7 tools)
- **derive-macros.md**: `explain_model` — one-liner noting overlap with database
- **validation.md**: `code_templates` (validation category) — one-liner referencing code_templates
- **localization.md**: `list_lang_files` — one-liner
- **inertia.md**: Check `ferro-mcp/src/tools/` for actual inertia-related tools before writing. If `inspect_props`/`list_props`/`generate_types` exist as MCP tools, add medium section. If `generate_types` is CLI-only, omit it.
- **json-ui.md**: `json_ui_catalog`, `json_ui_inspect`, `json_ui_generate` — RICH section
- **projections.md**: `list_projections`, `inspect_projection`, `render_projection`, `validate_projection`, `projection_coverage` — RICH section

**stripe.md special handling:** Read `ferro-mcp/src/tools/stripe.rs` at execution time to confirm what tools it exposes. If tools exist, add an MCP section. If the file only contains stubs, add a minimal one-liner noting the tools are available.

Do NOT add empty MCP sections. Do NOT modify multi-tenancy.md, testing.md, static-files.md, or themes.md.
  </action>
  <verify>
    <automated>grep -rl "## MCP Tools" docs/src/features/ | wc -l</automated>
  </verify>
  <done>At least 19 feature pages contain a `## MCP Tools` section (16 new + 3 existing). The 4 skipped pages (multi-tenancy, testing, static-files, themes) have no MCP section. Each MCP section lists accurate tool names matching ferro-mcp/src/tools/.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize existing MCP sections in api.md, whatsapp.md, and ai.md</name>
  <files>docs/src/features/api.md, docs/src/features/whatsapp.md, docs/src/features/ai.md</files>
  <action>
**api.md:** Find the existing `## MCP Integration` section. Rename the heading to `## MCP Tools`. Review the content — if it already lists `crud_create`, `crud_list`, `crud_update`, `crud_delete` with descriptions, keep the content and just fix the heading. If the format diverges significantly from the standard pattern (subsection headers per tool, brief descriptions), adjust to match.

**whatsapp.md:** Find the existing `## MCP Introspection` section. Rename the heading to `## MCP Tools`. Review the content — if it lists `whatsapp_config_status` and `whatsapp_webhook_events`, keep the content and just fix the heading. Adjust format to match the standard pattern if needed.

**ai.md:** The heading is already `## MCP Tools` (correct). Read the section and verify the format matches the standard pattern. If it lists `test_classifier` and `list_pending_confirmations` with reasonable descriptions, leave it as-is. If the format diverges, adjust minimally.

The goal is consistency: all three should use `## MCP Tools` as the heading and follow the same general pattern (tool name as subsection or bold, brief description of what it does and when to use it).
  </action>
  <verify>
    <automated>grep -n "## MCP" docs/src/features/api.md docs/src/features/whatsapp.md docs/src/features/ai.md</automated>
  </verify>
  <done>All three pages use `## MCP Tools` as the heading (not `## MCP Integration` or `## MCP Introspection`). Section format is consistent across all three pages.</done>
</task>

</tasks>

<verification>
- `grep -rl "## MCP Tools" docs/src/features/ | wc -l` returns >= 19 (16 new + 3 standardized)
- `grep -l "## MCP Integration\|## MCP Introspection" docs/src/features/` returns no results (all standardized)
- `grep -L "## MCP Tools" docs/src/features/multi-tenancy.md docs/src/features/testing.md docs/src/features/static-files.md docs/src/features/themes.md` returns all 4 (correctly skipped)
- `mdbook build docs/ 2>&1 | tail -5` shows no errors
</verification>

<success_criteria>
- 19+ feature pages have `## MCP Tools` sections with accurate tool names
- 4 pages correctly have no MCP section (multi-tenancy, testing, static-files, themes)
- api-mcp.md is untouched
- All existing MCP sections use standardized `## MCP Tools` heading
- mdbook builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/112-agent-first-philosophy/112-02-SUMMARY.md`
</output>
