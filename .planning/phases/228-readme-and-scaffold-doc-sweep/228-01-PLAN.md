---
phase: 228-readme-and-scaffold-doc-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - ferro-cli/src/templates/files/root/README.md.tpl
  - scripts/install.sh
  - scripts/create-app.sh
  - .planning/phases/228-readme-and-scaffold-doc-sweep/tap-README-draft.md
autonomous: true
requirements: []   # intentional — REQUIREMENTS.md has no IDs scoped to this phase (TBD in ROADMAP)
user_setup: []

must_haves:
  truths:
    - "Root README Status line no longer pins a stale version (v0.2.0) or milestone (v12.0 spec-driven)"
    - "Scaffold-generated README leads install with brew, states Rust 1.88+ MSRV and Node 18+"
    - "scripts/install.sh prints the real migrate command (ferro db:migrate), not the phantom 'ferro migrate'"
    - "scripts/create-app.sh next-steps use ferro db:migrate / ferro serve and lead permanent install with brew"
    - "A ready-to-paste tap README draft exists inside the ferro repo (no push to the separate tap repo)"
    - "The toolchain-free-CLI vs Rust-1.88+-to-build distinction reads consistently with installation.md across all edited files"
  artifacts:
    - path: "README.md"
      provides: "Neutral, version-pinless Status section"
      contains: "Pre-1.0"
    - path: "ferro-cli/src/templates/files/root/README.md.tpl"
      provides: "Brew-first scaffold README with 1.88+/Node 18+"
      contains: "brew install albertogferrario/ferro/ferro"
    - path: "scripts/install.sh"
      provides: "Correct migrate command in next-steps"
      contains: "ferro db:migrate"
    - path: "scripts/create-app.sh"
      provides: "Correct commands + brew-first permanent install"
      contains: "ferro serve"
    - path: ".planning/phases/228-readme-and-scaffold-doc-sweep/tap-README-draft.md"
      provides: "Tap repo README draft (in-repo artifact only)"
      contains: "brew install albertogferrario/ferro/ferro"
  key_links:
    - from: "ferro-cli/src/templates/files/root/README.md.tpl"
      to: "ferro-cli/src/templates/mod.rs::test_readme_substitution"
      via: "include_str! render gate"
      pattern: "ferro serve|ferro db:migrate|cd my-app"
---

<objective>
Correct factual drift between four user-facing files (root README, scaffold README template,
scripts/install.sh, scripts/create-app.sh) and the audited install oracle
`docs/src/getting-started/installation.md`, and produce a ready-to-paste tap-repo README draft
inside the ferro repo.

Purpose: After v0.2.61 / brew-first distribution and the rustls/SQLite scaffold, the READMEs and
installer scripts still lead with `cargo install`, name stale versions, and emit phantom commands
(`ferro migrate`, `cargo run -- migrate`). This phase aligns every user-facing entry point to the
real flow.

Output: Edits to 4 existing files + 1 new draft artifact. No feature code. Single Rust test gate
(scaffold template render); all other verification is grep-based.

Scope guardrails (locked from CONTEXT.md):
- D-01: factual accuracy + install-method consistency only — NO prose rewrites.
- D-02: Homebrew leads everywhere; cargo/source kept as alternates.
- D-03: CLI is toolchain-free via brew; Rust 1.88+ needed only to build a scaffolded app.
- D-04: replace the stale Status line with neutral, version-pinless phrasing.
- D-05: tap README is a DRAFT artifact in this repo only — do NOT push/commit to
  `albertogferrario/homebrew-ferro` (separate repo, not in this tree).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/228-readme-and-scaffold-doc-sweep/228-CONTEXT.md
@.planning/phases/228-readme-and-scaffold-doc-sweep/228-RESEARCH.md
@.planning/phases/228-readme-and-scaffold-doc-sweep/228-VALIDATION.md
@docs/src/getting-started/installation.md

<oracle>
The install oracle (`docs/src/getting-started/installation.md`) encodes the canonical phrasing all
edits reconcile to:
- "The Ferro CLI installs toolchain-free via Homebrew ... only needed to build and run a scaffolded app:"
- "Rust 1.88+ (with Cargo) — to build the app (no OpenSSL needed; the scaffold uses rustls)"
- "Node.js 18+ (for the frontend dev server)"
- "PostgreSQL, SQLite, or MySQL — SQLite is the default, no setup required"
- Primary install: `brew install albertogferrario/ferro/ferro`
- Flow: `ferro new my-app` -> `cd my-app` -> `ferro serve`
- Migrate: `ferro db:migrate`

The scaffold README template is rendered by `ferro-cli/src/templates/project.rs:221` (`readme()`,
three `.replace()` calls). The gate test `test_readme_substitution`
(`ferro-cli/src/templates/mod.rs:582`) asserts these five literal strings survive substitution and
MUST remain present after editing the `.tpl`: `# My App`, `A test description`, `cd my-app`,
`ferro serve`, `ferro db:migrate`. Editing prose around them is safe; do not remove any of them.
</oracle>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix scaffold README template (brew-first, MSRV 1.88+, Node 18+, ferro serve)</name>
  <files>ferro-cli/src/templates/files/root/README.md.tpl</files>
  <read_first>
    - ferro-cli/src/templates/files/root/README.md.tpl (target — full file, 88 lines)
    - docs/src/getting-started/installation.md (oracle — Requirements + Installing the CLI sections)
    - ferro-cli/src/templates/mod.rs (the test_readme_substitution gate near line 582 — confirm the 5 asserted strings)
  </read_first>
  <action>
    Apply five exact edits to the `.tpl`. Do NOT touch lines containing `{project_title}`,
    `{description}`, or `{project_name}` substitution tokens. Do NOT remove any of the five
    test-asserted strings (`# My App`, `A test description`, `cd my-app`, `ferro serve`,
    `ferro db:migrate`) — none of them appear on the lines being edited, so leave the rest intact.

    Edit 1 — line 9 (MSRV bump, per D-03 / RESEARCH tpl:9):
      OLD: `- **Rust** (stable, 1.75+) — install via [rustup](https://rustup.rs)`
      NEW: `- **Rust** (stable, 1.88+) — install via [rustup](https://rustup.rs)`

    Edit 2 — line 10 (brew-first install, per D-02 / RESEARCH tpl:10):
      OLD: `- **Ferro CLI** — ` + backtick-cargo install ferro-cli-backtick + ` (or build from source)`
      NEW: `- **Ferro CLI** — ` + backtick-brew install albertogferrario/ferro/ferro-backtick + ` (toolchain-free; no Rust needed). Alternatives: ` + backtick-cargo install ferro-cli-backtick + ` or build from source.`
      (Use literal backticks in the file around each command, exactly as the OLD line used backticks around `cargo install ferro-cli`.)

    Edit 3 — line 11 (Node minimum, per D-03 / RESEARCH tpl:11):
      OLD: `- **Node.js** 20+ and **npm** — for the frontend (Vite + Inertia)`
      NEW: `- **Node.js** 18+ and **npm** — for the frontend (Vite + Inertia)`

    Edit 4 — line 82 (troubleshooting brew-first, per D-02 / RESEARCH tpl:82):
      OLD: `- **` + backtick-ferro: command not found-backtick + `** — install with ` + backtick-cargo install ferro-cli-backtick + `.`
      NEW: `- **` + backtick-ferro: command not found-backtick + `** — install with ` + backtick-brew install albertogferrario/ferro/ferro-backtick + ` (or ` + backtick-cargo install ferro-cli-backtick + `).`

    Edit 5 — line 85 (replace phantom `cargo run` with `ferro serve`, per RESEARCH tpl:85 / Open Q1):
      OLD clause within line 85: `run ` + backtick-cargo run-backtick + ` once to generate types before running ` + backtick-npm run dev-backtick + `. Types are regenerated automatically on each server start.`
      NEW clause within line 85: `run ` + backtick-ferro serve-backtick + ` to start the server — types are regenerated automatically on each start.`
      (Preserve the rest of line 85, including the trailing reference to `cli/frontend-types.md`.)

    Do NOT alter line 12 (SQLite-default claim — verified correct against Cargo.toml.tpl rustls config).
  </action>
  <acceptance_criteria>
    Run from repo root. Present-AND-absent grep conditions:
    - PRESENT: `grep -c "1.88+" ferro-cli/src/templates/files/root/README.md.tpl` returns >= 1
    - ABSENT:  `grep -c "1.75" ferro-cli/src/templates/files/root/README.md.tpl` returns 0
    - PRESENT: `grep -c "brew install albertogferrario/ferro/ferro" ferro-cli/src/templates/files/root/README.md.tpl` returns >= 2 (line 10 + line 82)
    - ABSENT:  `grep -c "Node.js\*\* 20+" ferro-cli/src/templates/files/root/README.md.tpl` returns 0
    - ABSENT:  `grep -c "cargo run" ferro-cli/src/templates/files/root/README.md.tpl` returns 0
    - PRESENT (test-asserted strings survive): `grep -c "ferro serve" README.md.tpl` >= 1 AND `grep -c "ferro db:migrate" README.md.tpl` >= 1 AND `grep -c "cd {project_name}" README.md.tpl` >= 1
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p ferro-cli -- test_readme_substitution</automated>
  </verify>
  <done>Template edits applied; `test_readme_substitution` passes green (substitution intact); all grep present/absent conditions hold.</done>
</task>

<task type="auto">
  <name>Task 2: Fix root README stale Status line (D-04)</name>
  <files>README.md</files>
  <read_first>
    - README.md (target — read the Status section around line 183-185)
    - docs/src/getting-started/installation.md (oracle — confirm no version pin is needed in neutral copy)
  </read_first>
  <action>
    Single edit, line 185 (per D-04 / RESEARCH README.md:185). Replace the stale version- and
    milestone-pinned sentence with neutral, low-churn phrasing that names neither a workspace
    version nor a milestone number (both rot).

      OLD: `v0.2.0 — pre-1.0. Breaking changes are allowed between minor versions until 1.0. Current milestone work targets v12.0 spec-driven rendering.`
      NEW: `Pre-1.0. Breaking changes are allowed between minor versions until 1.0.`

    Do not edit the install block (lines 17-25) — RESEARCH confirms it is already brew-first with
    `cargo install` correctly positioned as an inline alternate (acceptable per D-02). No other
    stale version strings exist in README.md (RESEARCH grep-confirmed).
  </action>
  <acceptance_criteria>
    Run from repo root. Present-AND-absent:
    - ABSENT:  `grep -c "v0.2.0" README.md` returns 0
    - ABSENT:  `grep -c "v12.0 spec-driven" README.md` returns 0
    - ABSENT:  `grep -c "Current milestone work targets" README.md` returns 0
    - PRESENT: `grep -c "Pre-1.0. Breaking changes are allowed between minor versions until 1.0." README.md` returns 1
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Pre-1.0. Breaking changes are allowed between minor versions until 1.0." README.md && ! grep -qE "v0\.2\.0|v12\.0 spec-driven" README.md && echo PASS</automated>
  </verify>
  <done>Status line replaced with neutral phrasing; no `v0.2.0` / `v12.0 spec-driven` / `Current milestone work targets` strings remain.</done>
</task>

<task type="auto">
  <name>Task 3: Fix installer scripts (phantom commands + brew-first permanent install)</name>
  <files>scripts/install.sh, scripts/create-app.sh</files>
  <read_first>
    - scripts/install.sh (target — next-steps block around lines 178-185)
    - scripts/create-app.sh (target — next-steps block around lines 134-143)
    - docs/src/getting-started/installation.md (oracle — brew-first + ferro db:migrate / ferro serve)
    - ferro-cli/src/main.rs (oracle — confirm `db:migrate` is the registered command, no bare `migrate` alias)
  </read_first>
  <action>
    Edit ONLY echo/printf message strings and documented command names. Do NOT change download URLs,
    platform detection, extraction, exec paths, or any control flow (per threat model — these are
    user-facing installers; only the messaging changes).

    --- scripts/install.sh (1 edit, line 184, per RESEARCH install.sh:184) ---
    `ferro migrate` is a phantom command (only `ferro db:migrate` is registered in main.rs).
      OLD: `        printf "  ${CYAN}ferro migrate${NC}\n"`
      NEW: `        printf "  ${CYAN}ferro db:migrate${NC}\n"`
    (Leave line 183 `cd frontend && npm install && cd ..` and line 185 `ferro serve` unchanged.)

    --- scripts/create-app.sh (edits at lines 138-139 + 141-142, per RESEARCH create-app.sh:138-142) ---
    Reframe the next-steps so they use the installed CLI commands and lead permanent install with brew.

    Edit A — line 138 (phantom `cargo run -- migrate` -> real command):
      OLD: `    printf "  ${CYAN}cargo run -- migrate${NC}\n"`
      NEW: `    printf "  ${CYAN}ferro db:migrate${NC}\n"`

    Edit B — line 139 (`cargo run -- serve` compiles from source -> use the CLI):
      OLD: `    printf "  ${CYAN}cargo run -- serve${NC}\n"`
      NEW: `    printf "  ${CYAN}ferro serve${NC}\n"`

    Edit C — lines 141-142 (brew-first permanent install, keep cargo as alternate per D-02):
      OLD line 141: `    printf "Or install Ferro CLI globally:\n"`
      OLD line 142: `    printf "  ${CYAN}cargo install ferro-cli${NC}\n"`
      NEW (replace those two lines with three):
        `    printf "Or install the Ferro CLI permanently (toolchain-free):\n"`
        `    printf "  ${CYAN}brew install albertogferrario/ferro/ferro${NC}\n"`
        `    printf "  ${CYAN}cargo install ferro-cli${NC}  # alternate (requires Rust)\n"`

    Keep line 137 (`cd frontend && npm install && cd ..`) — required for the Inertia frontend
    (RESEARCH Open Q2: this is correct, not a discrepancy).
  </action>
  <acceptance_criteria>
    Run from repo root. Present-AND-absent (note: `ferro db:migrate` does not match the phantom
    pattern `ferro migrate ` because of the `db:` segment — verify the phantom is gone with -n):
    - ABSENT:  `grep -n "ferro migrate" scripts/install.sh` prints nothing (0 lines)
    - PRESENT: `grep -c "ferro db:migrate" scripts/install.sh` returns 1
    - ABSENT:  `grep -c "cargo run --" scripts/create-app.sh` returns 0
    - PRESENT: `grep -c "ferro db:migrate" scripts/create-app.sh` returns 1
    - PRESENT: `grep -c "ferro serve" scripts/create-app.sh` returns 1
    - PRESENT: `grep -c "brew install albertogferrario/ferro/ferro" scripts/create-app.sh` returns 1
    - PRESENT (cargo kept as alternate, not removed): `grep -c "cargo install ferro-cli" scripts/create-app.sh` returns 1
    - CONTROL-FLOW UNCHANGED: `grep -c "npm install" scripts/create-app.sh` returns >= 1 (frontend step retained)
  </acceptance_criteria>
  <verify>
    <automated>! grep -qn "ferro migrate" scripts/install.sh && grep -q "ferro db:migrate" scripts/install.sh && ! grep -q "cargo run --" scripts/create-app.sh && grep -q "ferro serve" scripts/create-app.sh && grep -q "brew install albertogferrario/ferro/ferro" scripts/create-app.sh && echo PASS</automated>
  </verify>
  <done>install.sh emits `ferro db:migrate`; create-app.sh next-steps use `ferro db:migrate`/`ferro serve` and lead permanent install with brew (cargo kept as alternate); npm-install frontend step retained; no download/exec/control-flow lines changed.</done>
</task>

<task type="auto">
  <name>Task 4: Create tap-repo README draft (in-repo artifact only, D-05)</name>
  <files>.planning/phases/228-readme-and-scaffold-doc-sweep/tap-README-draft.md</files>
  <read_first>
    - .planning/phases/228-readme-and-scaffold-doc-sweep/228-RESEARCH.md (the "Tap-repo README Draft (D-05)" section — the 6 required facts)
    - docs/src/getting-started/installation.md (oracle — brew one-liner + toolchain-free phrasing to mirror)
  </read_first>
  <action>
    Create a NEW draft file at `.planning/phases/228-readme-and-scaffold-doc-sweep/tap-README-draft.md`.
    This is a ready-to-paste README for the SEPARATE `albertogferrario/homebrew-ferro` tap repo.

    HARD BOUNDARY (D-05): This task creates a draft file inside the ferro repo ONLY. It MUST NOT
    push, commit, clone, or otherwise touch the `albertogferrario/homebrew-ferro` repository — that
    repo is not in this working tree and the cross-repo commit is a separate, user-performed action.
    The file is an artifact for the user to paste manually. State this boundary at the top of the
    draft as an HTML comment so it is unmistakable if the file is ever opened out of context.

    Content (cover all 6 facts from RESEARCH "Tap-repo README Draft (D-05)"), neutral voice per
    CLAUDE.md (no marketing/strategy language):
    1. Title + one-liner: `brew install albertogferrario/ferro/ferro`
    2. What it installs: the Ferro CLI binary — toolchain-free (no Rust required to install the CLI).
    3. How the tap works: the formula is auto-bumped on each ferro GitHub release (token-free via
       `workflow_run` CI); manual formula updates are not needed.
    4. After install: `ferro new my-app`, `ferro serve`, `ferro --help`.
    5. Building an app: Rust 1.88+ is required to build/run a scaffolded app (not to use the CLI).
    6. Links: main ferro repo (https://github.com/albertogferrario/ferro) and full docs
       (https://docs.ferro-rs.dev/).

    Begin the file with this exact HTML comment as the first line:
      <!-- DRAFT — paste target: albertogferrario/homebrew-ferro README. Do NOT commit from the ferro repo session (cross-repo boundary, Phase 228 D-05). -->
  </action>
  <acceptance_criteria>
    Run from repo root. File-existence + content:
    - PRESENT: `test -f .planning/phases/228-readme-and-scaffold-doc-sweep/tap-README-draft.md && echo exists` prints `exists`
    - PRESENT: `grep -c "brew install albertogferrario/ferro/ferro" .planning/phases/228-readme-and-scaffold-doc-sweep/tap-README-draft.md` returns >= 1
    - PRESENT: `grep -c "1.88+" .planning/phases/228-readme-and-scaffold-doc-sweep/tap-README-draft.md` returns >= 1
    - PRESENT (boundary marker): `grep -c "Do NOT commit from the ferro repo session" .planning/phases/228-readme-and-scaffold-doc-sweep/tap-README-draft.md` returns 1
    - BOUNDARY: the tap repo was not added as a remote — `git remote -v | grep -c "homebrew-ferro"` returns 0
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/228-readme-and-scaffold-doc-sweep/tap-README-draft.md && grep -q "brew install albertogferrario/ferro/ferro" .planning/phases/228-readme-and-scaffold-doc-sweep/tap-README-draft.md && grep -q "Do NOT commit from the ferro repo session" .planning/phases/228-readme-and-scaffold-doc-sweep/tap-README-draft.md && echo PASS</automated>
  </verify>
  <done>Draft file exists with all 6 required facts, the brew one-liner, the 1.88+ build requirement, and the cross-repo boundary comment; no commit/push/remote to the tap repo occurred.</done>
</task>

<task type="auto">
  <name>Task 5: Goal-backward consistency sweep (proof across all edited files)</name>
  <files>README.md, ferro-cli/src/templates/files/root/README.md.tpl, scripts/install.sh, scripts/create-app.sh</files>
  <read_first>
    - .planning/phases/228-readme-and-scaffold-doc-sweep/228-RESEARCH.md ("Verification Mechanism (per Fix)" table)
    - docs/src/getting-started/installation.md (oracle — final cross-check that edits read consistently with it)
  </read_first>
  <action>
    This is the goal-backward proof task. Make no edits unless the sweep finds a residual issue;
    if it does, fix the residual in the owning file (Tasks 1-3) and re-run the sweep.

    Run the four sweeps below and confirm each. They prove the three phase truths: (a) no phantom
    commands remain, (b) no stale version/milestone pins remain, (c) brew leads every permanent
    install location (cargo only as an acknowledged alternate).

    Sweep 1 — phantom commands gone (must return 0 matches each):
      grep -rn "ferro migrate" scripts/ ferro-cli/src/templates/files/root/README.md.tpl   # the bare phantom; db:migrate has the db: segment
      grep -rn "cargo run -- migrate" scripts/
      grep -rn "cargo run -- serve" scripts/
      grep -rn "cargo run" ferro-cli/src/templates/files/root/README.md.tpl

    Sweep 2 — stale version/milestone pins gone (must return 0 matches each):
      grep -n "v0.2.0" README.md
      grep -n "v12.0 spec-driven" README.md
      grep -n "1.75" ferro-cli/src/templates/files/root/README.md.tpl

    Sweep 3 — brew leads permanent-install locations (must return >= 1 each):
      grep -c "brew install albertogferrario/ferro/ferro" ferro-cli/src/templates/files/root/README.md.tpl   # >= 2
      grep -c "brew install albertogferrario/ferro/ferro" scripts/create-app.sh                              # >= 1
    And cargo retained only as alternate (still present, not removed):
      grep -c "cargo install ferro-cli" scripts/create-app.sh   # 1
      grep -c "cargo install ferro-cli" README.md                # 1 (the inline alternate at line 21)

    Sweep 4 — toolchain-free distinction reads consistently (must return >= 1 each):
      grep -c "1.88+" ferro-cli/src/templates/files/root/README.md.tpl
      grep -c "1.88+" .planning/phases/228-readme-and-scaffold-doc-sweep/tap-README-draft.md
      grep -c "Node.js\*\* 18+" ferro-cli/src/templates/files/root/README.md.tpl

    Record the sweep output (pass/fail per line) in the plan SUMMARY.
  </action>
  <acceptance_criteria>
    Run from repo root. The whole phase passes iff:
    - ZERO phantom commands: `grep -rn "ferro migrate" scripts/ ferro-cli/src/templates/files/root/README.md.tpl | wc -l` returns 0 AND `grep -rn "cargo run" scripts/ ferro-cli/src/templates/files/root/README.md.tpl | wc -l` returns 0
    - ZERO stale pins: `grep -REn "v0\.2\.0|v12\.0 spec-driven" README.md | wc -l` returns 0 AND `grep -n "1.75" ferro-cli/src/templates/files/root/README.md.tpl | wc -l` returns 0
    - BREW LEADS: `grep -c "brew install albertogferrario/ferro/ferro" ferro-cli/src/templates/files/root/README.md.tpl` >= 2 AND `grep -c "brew install albertogferrario/ferro/ferro" scripts/create-app.sh` >= 1
    - MSRV CONSISTENT: `grep -c "1.88+" ferro-cli/src/templates/files/root/README.md.tpl` >= 1
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -rn "ferro migrate" scripts/ ferro-cli/src/templates/files/root/README.md.tpl | wc -l) -eq 0 && test $(grep -rn "cargo run" scripts/ ferro-cli/src/templates/files/root/README.md.tpl | wc -l) -eq 0 && test $(grep -REn "v0\.2\.0|v12\.0 spec-driven" README.md | wc -l) -eq 0 && test $(grep -c "brew install albertogferrario/ferro/ferro" scripts/create-app.sh) -ge 1 && echo SWEEP_PASS</automated>
  </verify>
  <done>All four sweeps pass: zero phantom commands, zero stale pins, brew leads every permanent-install location, MSRV/Node distinction consistent with installation.md. Sweep output recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user shell -> installer scripts | `install.sh` / `create-app.sh` download and exec a pre-built binary; the download URL and exec path are the security-relevant surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-228-01 | Tampering | scripts/install.sh, scripts/create-app.sh | accept | This phase edits ONLY echo/printf message strings and documented command names in the "Next steps" text. Download URLs, platform detection, archive extraction, and exec paths are NOT modified. No change to script control flow. If any task is found to touch download/exec logic, halt and flag — it is out of scope. |
| T-228-02 | Information disclosure | tap-README-draft.md | accept | Draft is an in-repo planning artifact (the whole tree is treated as public per CLAUDE.md). Contains only the public brew one-liner and public repo/doc links. No secrets. |

N/A beyond the above: this is a docs/scripts-messaging phase with no new code paths, no network logic changes, and no credential handling.
</threat_model>

<verification>
- `cargo test -p ferro-cli -- test_readme_substitution` is green after the `.tpl` edit (Task 1).
- Scoped to `ferro-cli` ONLY — never `cargo test --all-features` (docs phase; full suite is disk/thermally expensive, per project memory).
- All other verification is grep-based (Tasks 2-5).
- Final goal-backward sweep (Task 5) proves zero phantom commands, zero stale pins, brew-first everywhere.
</verification>

<success_criteria>
- Scaffold README template: Rust 1.88+, Node 18+, brew-first install (line 10) and troubleshooting (line 82), `ferro serve` (line 85); `test_readme_substitution` green.
- Root README: stale Status line replaced with neutral, version-pinless phrasing; no `v0.2.0`/`v12.0 spec-driven`.
- scripts/install.sh: `ferro db:migrate` (no phantom `ferro migrate`).
- scripts/create-app.sh: `ferro db:migrate` / `ferro serve`; brew-first permanent install with cargo as alternate; npm-install frontend step retained.
- tap-README-draft.md: created in-repo with all 6 facts + cross-repo boundary comment; no push/commit/remote to the tap repo.
- Toolchain-free-CLI vs Rust-1.88+-to-build distinction reads consistently with installation.md across all files.
- No script control flow, download URLs, or exec paths changed.
</success_criteria>

<output>
After completion, create `.planning/phases/228-readme-and-scaffold-doc-sweep/228-01-SUMMARY.md`
(record the Task 5 sweep output verbatim).
</output>
