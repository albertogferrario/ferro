---
phase: 121
plan: 06
type: execute
wave: 4
depends_on:
  - "121-01"
files_modified:
  - app/src/views/pagamenti.json
  - app/src/controllers/pagamenti.rs
  - app/src/controllers/mod.rs
  - app/src/routes.rs
autonomous: true
requirements:
  - FIELD-01

must_haves:
  truths:
    - "pagamenti.json is a valid v2 spec file that passes catalog validation at load time"
    - "pagamenti.rs handler assembles data only — zero component-building code"
    - "Handler calls JsonUi::render_file (not JsonUi::render or load_cached directly)"
    - "Route /pagamenti is registered and named pagamenti.index"
    - "App compiles without warnings after the addition"
  artifacts:
    - path: "app/src/views/pagamenti.json"
      provides: "v2 spec file for pagamenti dashboard page"
      contains: "ferro-json-ui/v2"
    - path: "app/src/controllers/pagamenti.rs"
      provides: "data-only handler using JsonUi::render_file"
      contains: "render_file"
    - path: "app/src/controllers/mod.rs"
      provides: "pub mod pagamenti declaration"
      contains: "pub mod pagamenti"
    - path: "app/src/routes.rs"
      provides: "GET /pagamenti route"
      contains: "pagamenti.index"
  key_links:
    - from: "app/src/controllers/pagamenti.rs index handler"
      to: "JsonUi::render_file"
      via: "direct call"
      pattern: "render_file"
    - from: "app/src/routes.rs"
      to: "controllers::pagamenti::index"
      via: "get! macro"
      pattern: "pagamenti"
    - from: "JsonUi::render_file"
      to: "app/src/views/pagamenti.json"
      via: "path argument"
      pattern: "views/pagamenti.json"
---

<objective>
Add the pagamenti field test to the ferro sample app — a complete v2 JSON spec file plus data-only handler.

Purpose: Proves the full v2 pipeline works end-to-end in the sample app. Demonstrates the pattern where the spec file defines the UI and the Rust handler supplies only data.
Output: pagamenti.json (v2 spec), pagamenti.rs (data-only handler), route registration — the minimum needed to render a working payments dashboard page.
</objective>

<execution_context>
@/Users/alberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/121-documentation-and-field-test/121-RESEARCH.md
@.planning/phases/121-documentation-and-field-test/121-01-SUMMARY.md

<interfaces>
<!-- From 121-RESEARCH.md: verified component type names in the v2 catalog -->

Use these exact component type names (case-sensitive, catalog-validated):
- "Card" — wrapper container
- "StatCard" — single metric display (label, value props)
- "DataTable" — tabular data (columns, data_path props)
- "PageHeader" — page title section (title, description props)

DataTable columns format:
```json
"columns": [
  { "key": "data", "label": "Data" },
  { "key": "descrizione", "label": "Descrizione" },
  { "key": "importo", "label": "Importo" },
  { "key": "stato", "label": "Stato" }
]
```

NOTE: Verify exact DataTable props by reading ferro-json-ui/src/component.rs DataTableProps before writing pagamenti.json.
Specifically confirm: is `data_path` a plain string or a $data expression? In DataTable it is a plain string (JSON Pointer path that the renderer reads from data). Only element props marked as needing expressions should use $data.

<!-- From 121-RESEARCH.md: confirmed app structure -->

app/src/controllers/mod.rs currently has:
  pub mod auth_controller;
  pub mod config_example;
  pub mod home;
  pub mod todo;
  pub mod user;
Add: pub mod pagamenti;

app/src/routes.rs uses get! macro. Add inside routes! block:
  get!("/pagamenti", controllers::pagamenti::index).name("pagamenti.index"),

Spec file path passed to render_file: "views/pagamenti.json"
(relative to the app working directory when the server runs)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pagamenti.json v2 spec file</name>
  <files>app/src/views/pagamenti.json</files>
  <read_first>
    - ferro-json-ui/src/component.rs (search for "DataTableProps\|StatCardProps\|PageHeaderProps" — confirm exact prop names and types)
    - ferro-json-ui/src/catalog.rs (search for BUILTIN_SPECS or the registered component names — confirm "DataTable", "StatCard", "Card", "PageHeader" exist)
    - app/src/views/ (ls to confirm the directory exists; create it if missing)
  </read_first>
  <action>
1. Confirm `app/src/views/` directory exists. If not, create it.

2. Confirm the exact catalog component type names by reading ferro-json-ui/src/catalog.rs. The spec will fail validation if any type name doesn't match the catalog exactly.

3. Create `app/src/views/pagamenti.json` with this structure:

```json
{
  "$schema": "ferro-json-ui/v2",
  "title": "Pagamenti",
  "layout": "dashboard",
  "root": "root",
  "elements": {
    "root": {
      "type": "Card",
      "props": { "title": "Pagamenti" },
      "children": ["stats_row", "payments_table"]
    },
    "stats_row": {
      "type": "StatCard",
      "props": {
        "label": "Totale",
        "value": { "$data": "/meta/totale_formattato" }
      }
    },
    "payments_table": {
      "type": "DataTable",
      "props": {
        "columns": [
          { "key": "data", "label": "Data" },
          { "key": "descrizione", "label": "Descrizione" },
          { "key": "importo", "label": "Importo" },
          { "key": "stato", "label": "Stato" }
        ],
        "data_path": "/pagamenti",
        "empty_message": "Nessun pagamento trovato."
      }
    }
  }
}
```

IMPORTANT: If catalog validation shows "StatCard" or "DataTable" are not the correct type names (step 2), use the correct names found in the catalog. Do not guess — read the catalog source.

4. Verify the JSON is valid (no syntax errors): the file must parse as valid JSON.
  </action>
  <verify>
    <automated>python3 -c "import json,sys; json.load(open('app/src/views/pagamenti.json')); print('valid JSON')"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import json; json.load(open('app/src/views/pagamenti.json'))"` exits 0 (valid JSON)
    - `grep -c '"$schema": "ferro-json-ui/v2"' app/src/views/pagamenti.json` returns 1
    - `grep -c '"root": "root"' app/src/views/pagamenti.json` returns 1
    - `grep -c '"elements"' app/src/views/pagamenti.json` returns 1
    - `grep -c '"$data"' app/src/views/pagamenti.json` returns >= 1
  </acceptance_criteria>
  <done>pagamenti.json is valid JSON using ferro-json-ui/v2 schema with root, elements, and $data expression.</done>
</task>

<task type="auto">
  <name>Task 2: Create pagamenti handler and wire route</name>
  <files>app/src/controllers/pagamenti.rs, app/src/controllers/mod.rs, app/src/routes.rs</files>
  <read_first>
    - app/src/controllers/home.rs (reference — see handler pattern for this app)
    - app/src/controllers/mod.rs (add pub mod pagamenti)
    - app/src/routes.rs (find insertion point in routes! block)
  </read_first>
  <action>
1. Create `app/src/controllers/pagamenti.rs`:

```rust
use ferro::{handler, JsonUi, Response};

/// List payments — data-only handler for the pagamenti JSON-UI spec.
///
/// All UI structure is in src/views/pagamenti.json.
/// This handler assembles only the data the spec needs.
#[handler]
pub async fn index() -> Response {
    let data = serde_json::json!({
        "meta": {
            "totale_formattato": "€ 1.245,00"
        },
        "pagamenti": [
            {
                "data": "2026-04-20",
                "descrizione": "Abbonamento mensile",
                "importo": "€ 99,00",
                "stato": "Completato"
            },
            {
                "data": "2026-04-15",
                "descrizione": "Ordine #1042",
                "importo": "€ 246,00",
                "stato": "Completato"
            },
            {
                "data": "2026-04-10",
                "descrizione": "Ordine #1038",
                "importo": "€ 900,00",
                "stato": "In attesa"
            }
        ]
    });
    JsonUi::render_file("views/pagamenti.json", data)
}
```

2. Edit `app/src/controllers/mod.rs` — add `pub mod pagamenti;` in alphabetical order with the existing module declarations.

3. Edit `app/src/routes.rs` — add the pagamenti route inside the `routes! { ... }` block. Place it after the existing standalone get! routes and before any group! blocks:
   ```rust
   get!("/pagamenti", controllers::pagamenti::index).name("pagamenti.index"),
   ```

4. Run:
   ```bash
   cargo fmt --all -- --check
   cargo clippy --all --all-targets -- -D warnings
   cargo build -p app 2>&1 | tail -20
   ```
   Fix any compilation errors. Common issue: if `JsonUi::render_file` is not yet in scope from Plan 01, check that `framework/src/json_ui/mod.rs` has the method and that `framework/src/lib.rs` re-exports it via the `JsonUi` re-export.
  </action>
  <verify>
    <automated>cargo build -p app 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "render_file" app/src/controllers/pagamenti.rs` returns 1
    - `grep -c "serde_json::json!" app/src/controllers/pagamenti.rs` returns 1
    - `grep -c "ComponentNode\|JsonUiView\|Component::" app/src/controllers/pagamenti.rs` returns 0
    - `grep -c "pub mod pagamenti" app/src/controllers/mod.rs` returns 1
    - `grep -c "pagamenti.index" app/src/routes.rs` returns 1
    - `cargo build -p app` exits 0 (compiles without errors)
    - `cargo clippy -p app --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>pagamenti handler, mod registration, and route all added; app compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET /pagamenti → pagamenti.json spec | Spec file is app source artifact (trusted); handler data is hardcoded (trusted) |
| render_file → load_cached → filesystem | Reads spec from disk; path is app-controlled, not user-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-06-01 | Tampering | pagamenti.json on disk | accept | Same trust level as Rust source; app source files are not user-writable at runtime |
| T-121-06-02 | Information Disclosure | handler data in rendered HTML | accept | Field test uses hardcoded sample data; no real PII |
</threat_model>

<verification>
```bash
python3 -c "import json; json.load(open('app/src/views/pagamenti.json')); print('JSON valid')"
grep -n "render_file" app/src/controllers/pagamenti.rs
grep -n "pub mod pagamenti" app/src/controllers/mod.rs
grep -n "pagamenti.index" app/src/routes.rs
cargo build -p app 2>&1 | tail -5
cargo clippy --all --all-targets -- -D warnings 2>&1 | grep -c "^error" || echo "0 errors"
```
</verification>

<success_criteria>
- pagamenti.json is valid JSON with "$schema": "ferro-json-ui/v2", root element, elements map, $data expression
- pagamenti.rs contains zero component-building code; only serde_json::json! data and JsonUi::render_file
- mod.rs and routes.rs updated; GET /pagamenti registered as "pagamenti.index"
- Full cargo build passes without errors or warnings
</success_criteria>

<output>
After completion, create `.planning/phases/121-documentation-and-field-test/121-06-SUMMARY.md`
</output>
