---
phase: 256-component-renderers-builtin-lockstep
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - ferro-json-ui/src/render/atoms.rs
  - ferro-json-ui/src/render/containers.rs
  - ferro-json-ui/src/render/mod.rs
  - ferro-json-ui/src/catalog.rs
  - ferro-mcp/src/tools/json_ui_catalog.rs
autonomous: true
requirements: [POS-01, POS-03]
must_haves:
  truths:
    - "TileGrid is a registered builtin (BUILTIN_TYPES count 48): it emits data-filter-scope always, an integrated filter-tab strip populated from categories_path when set, a client-side search input when search:true, and renders its Tile children via the standard pipeline."
    - "TileGrid's integrated strip is populated at render time by resolving categories_path against the bound data (crate::data::resolve_path) — a spec whose data carries the category array gets a strip with one tab per category (SC-5)."
    - "FilterTabs is a registered builtin (count 49): a standalone tab strip whose 'All' tab carries data-filter-tab=\"\" and item tabs carry data-filter-tab=\"{token}\", inactive-state classes exactly matching what updateFilterTabClasses toggles, targets ≥44px."
    - "One shared Rust helper renders the tab-strip markup for BOTH FilterTabs and TileGrid's integrated strip (single markup source)."
    - "Both drift-guard counts (catalog.rs canonical, json_ui_catalog.rs mirror) agree at 49 and were bumped once per component addition, each with its History-comment line."
  artifacts:
    - path: "ferro-json-ui/src/render/containers.rs"
      provides: "render_tile_grid — container: data-filter-scope wrapper + integrated strip (from categories_path) + search + child pipeline"
      contains: "data-filter-scope"
    - path: "ferro-json-ui/src/render/atoms.rs"
      provides: "render_filter_tabs + pub(crate) render_filter_tab_strip shared helper"
      contains: "data-filter-tab"
    - path: "ferro-json-ui/src/catalog.rs"
      provides: "BUILTIN_SPECS entries for TileGrid+FilterTabs; count 47→48→49 with History comments"
      contains: "TileGrid"
  key_links:
    - from: "render_tile_grid categories_path strip / render_filter_tabs tabs"
      to: "runtime/filters.rs setupFilters + updateFilterTabClasses"
      via: "data-filter-scope / data-filter-tab / data-filter-search attributes + matching inactive classes"
      pattern: "data-filter-tab"
    - from: "render_tile_grid categories_path"
      to: "bound data category array"
      via: "crate::data::resolve_path(data, path)"
      pattern: "resolve_path"
    - from: "catalog.rs count guard"
      to: "ferro-mcp json_ui_catalog.rs mirror count + expected names"
      via: "same-commit lockstep bump"
      pattern: "catalog.components.len()"
---

<objective>
Register the first two POS builtins with full lockstep discipline: `TileGrid`
(POS-01) and `FilterTabs` (POS-03). Deliver the ONE shared tab-strip helper that
both the standalone FilterTabs and TileGrid's integrated category strip render
from (D-17), the client-side search input (D-19), and the SC-5 end-to-end
composition path — TileGrid resolves `categories_path` against the bound data at
render time to populate a real, tab-per-category strip that filters visible tiles.

Purpose: Make TileGrid + FilterTabs first-class catalog members so Phase 257's
projector output referencing them passes `catalog_validate`, and so the register
screen has its product-grid + filter surface.

Output: `render_tile_grid` (containers.rs), `render_filter_tabs` +
`render_filter_tab_strip` helper (atoms.rs), two BUILTIN_TYPES/dispatch/
BUILTIN_SPECS registrations, both count guards at 49, RULE_COMPONENTS extended
for TileGrid, HTML-assertion + SC-5 integration tests. TWO commits (one per
component, D-25).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/256-component-renderers-builtin-lockstep/256-CONTEXT.md
@.planning/phases/256-component-renderers-builtin-lockstep/256-RESEARCH.md
@.planning/phases/256-component-renderers-builtin-lockstep/256-PATTERNS.md
@.planning/phases/256-component-renderers-builtin-lockstep/256-01-SUMMARY.md

<interfaces>
<!-- Registration touchpoints — exact locations verified on this branch. -->

TileGridProps (component.rs:1394): data_path: String; form_id: String;
categories_path: Option<String>; columns: Option<u8>; search: Option<bool>.
FilterTabsProps (component.rs:1426): items: Vec<String>; all_label: Option<String> (default "All").

Data-path resolution: `crate::data::resolve_path(data: &Value, path: &str) -> Option<&Value>` is the
shared renderer helper (used by render_image :434, render_table data.rs:67, render_button_group
containers.rs:421). render_tile_grid uses it to resolve categories_path → an array of category strings.

BUILTIN_TYPES (render/mod.rs:44) — const &[&str], 47 entries; "Tile" at :67 ends the
atoms section, "SelectionPanel" NOT present yet. Add TileGrid to the containers
section, FilterTabs to the atoms section. Order in BUILTIN_TYPES MUST match the
dispatch match order (render/mod.rs:177–231) AND the BUILTIN_SPECS order (catalog.rs).

Dispatch match (render/mod.rs:177): one arm per type name →
`"TileGrid" => containers::render_tile_grid(el, spec, data, depth),`
`"FilterTabs" => atoms::render_filter_tabs(el, spec, data, depth),`.

catalog.rs BUILTIN_SPECS static (:124): tuples `(name, description, || to_value(schema_for!(Props)).unwrap(), &[slot_fields])`.
Count guard (catalog.rs:1219): `assert_eq!(crate::render::BUILTIN_TYPES.len(), 47);`
History comment (catalog.rs above :1219): `... → 47 (DropdownMenu replaced by ActionGroup).`
Structural drift (catalog.rs:576): BUILTIN_SPECS.len() must equal BUILTIN_TYPES.len().
prompt_under_size_budget (catalog.rs:2144): 12 KB cap — adding components grows prompt(); bump if it fails.

ferro-mcp json_ui_catalog.rs: mirror count (:403) `assert_eq!(catalog.components.len(), 47, ...)`;
expected names array (:409); RULE_COMPONENTS (:81) — currently
`("register-fill-viewport", &["Grid"])`, `("register-grid-fill", &["Grid"])`,
`("register-selection-present", &["Grid"])`. component_rule_mapping_is_exhaustive (:747):
every rule id must exist in design::rules() AND every mapped component name must be a real builtin.

runtime/filters.rs updateFilterTabClasses toggles:
  active:   add border-primary text-primary font-semibold, aria-selected=true
  inactive: add border-transparent text-text-muted hover:text-text, aria-selected=false
The tab-strip helper MUST emit the inactive set as initial state (except the All tab = active initial).
Token normalization: item token = item.replace(' ', "-") (mirrors render_tile / filters.rs).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: render_tile_grid + register TileGrid (commit, count 47→48)</name>
  <files>ferro-json-ui/src/render/containers.rs, ferro-json-ui/src/render/atoms.rs, ferro-json-ui/src/render/mod.rs, ferro-json-ui/src/catalog.rs, ferro-mcp/src/tools/json_ui_catalog.rs</files>
  <read_first>
    - ferro-json-ui/src/render/containers.rs (render_grid child-render pipeline :824–849; serde_json::from_value decode convention; `use crate::data::resolve_path` :21; render_button_group data-path use :421; imports :14–26; build_spec test helper :1415)
    - ferro-json-ui/src/render/atoms.rs (where render_filter_tab_strip helper will live; html_escape usage; render_image resolve_path use :434 as the resolve_path idiom)
    - ferro-json-ui/src/data.rs (resolve_path signature + behavior)
    - ferro-json-ui/src/render/mod.rs (BUILTIN_TYPES :44–96 containers section; dispatch :177–231)
    - ferro-json-ui/src/catalog.rs (BUILTIN_SPECS :124; count guard + History :1213–1219; structural drift :576; prompt budget :2144)
    - ferro-mcp/src/tools/json_ui_catalog.rs (RULE_COMPONENTS :81–101; mirror count :403; expected names :409; component_rule_mapping_is_exhaustive :747)
    - ferro-json-ui/src/runtime/filters.rs (attribute contract header + updateFilterTabClasses active/inactive class sets)
    - ferro-json-ui/src/design/rules.rs (register-fill-viewport/register-grid-fill rule ids + REGISTER_TRIGGER_TYPES :443)
    - .planning/phases/256-component-renderers-builtin-lockstep/256-CONTEXT.md (D-16 grid+strip+search, D-17 shared helper, D-19 search input, D-25 lockstep, D-26 RULE_COMPONENTS)
    - .planning/phases/256-component-renderers-builtin-lockstep/256-RESEARCH.md (Q2 child pipeline, Q3 registration touchpoints, code example tab-strip helper)
  </read_first>
  <behavior>
    - render_tile_grid emits `data-filter-scope` on its root div ALWAYS.
    - With search:true → emits an input `data-filter-search type="search"` carrying `text-base` (16px, iOS) and `{HIT_TARGET_MIN}`.
    - With categories_path Some AND the path resolves to a string array in `data` → emits the integrated strip via render_filter_tab_strip with one tab per resolved category (All tab active-initial + item tabs inactive-initial). With categories_path None → no strip. With categories_path set but unresolved/empty → strip renders the All tab only (graceful).
    - Renders el.children via render_element (the Tiles) inside the grid; base column default 2 (`grid-cols-2`), overridable via columns (exhaustive full-literal grid-cols-N match).
    - BUILTIN_TYPES.len() == 48; catalog builds; ferro-mcp catalog count == 48; History/expected/RULE_COMPONENTS updated.
  </behavior>
  <action>
Write `render_tile_grid` in containers.rs (it IS a container — iterates children via render_element; use `serde_json::from_value(el.props.clone())` per the containers convention, diagnostic-comment on error). Structure (D-16/D-19):
- Root `<div data-filter-scope class="...">` (fill/flex layout consistent with register composition; `{TOUCH_ACTION}`). Import HIT_TARGET_MIN/TOUCH_ACTION from super::classes.
- Search input (D-19): when `props.search == Some(true)` → prepend `<input data-filter-search type="search" placeholder="" class="{HIT_TARGET_MIN} text-base w-full rounded-md border border-border bg-surface px-3 text-text {INTERACTIVE_BASE}">`. `text-base` is MANDATORY (iOS 16px zoom pitfall). Neutral empty placeholder (no locale string).
- Integrated category strip (D-16, SC-5 — REAL population, not deferred): when `props.categories_path.is_some()`, resolve it against `data`:
```rust
let categories: Vec<String> = props
    .categories_path
    .as_deref()
    .and_then(|p| crate::data::resolve_path(data, p))
    .and_then(|v| v.as_array())
    .map(|arr| arr.iter().filter_map(|x| x.as_str().map(String::from)).collect())
    .unwrap_or_default();
```
Then render the strip: `atoms::render_filter_tab_strip(&categories, "All")` (empty `categories` → the helper still emits the All tab). Only emit the strip element when `categories_path.is_some()`; None → no strip element at all.
- Column classes: exhaustive full-literal match on `props.columns` — `Some(1) => "grid-cols-1"`, `Some(2)|None => "grid-cols-2"`, `Some(3) => "grid-cols-3"`, `Some(4) => "grid-cols-4"`, `_ => "grid-cols-2"`. NEVER `format!("grid-cols-{}", n)`.
- Grid body: iterate `el.children` via `render_element(cid, spec, data, depth + 1)` (Q2 pattern), wrapped in `<div class="grid {col_class} gap-3">`.

Add the shared helper in atoms.rs (D-17), `pub(crate) fn render_filter_tab_strip(items: &[String], all_label: &str) -> String` — base it on the RESEARCH.md code example. It emits `<div class="flex overflow-x-auto" role="tablist">`, the All tab first (`data-filter-tab=""`, active-initial classes `border-primary text-primary font-semibold`, aria-selected=true, `{HIT_TARGET_MIN} {TOUCH_ACTION} {INTERACTIVE_BASE}` + `px-4 text-sm font-medium border-b-2`), then one tab per item (`data-filter-tab="{token}"` with token = item.replace(' ', "-"), inactive-initial classes `border-transparent text-text-muted hover:text-text`, aria-selected=false). html_escape all_label + item labels + tokens.

Register TileGrid (lockstep — ALL in this ONE commit):
1. render/mod.rs BUILTIN_TYPES: add `"TileGrid",` in the containers section (e.g. after "Grid").
2. render/mod.rs dispatch: add `"TileGrid" => containers::render_tile_grid(el, spec, data, depth),` at the matching position.
3. catalog.rs BUILTIN_SPECS: add the entry at the SAME relative position (containers section):
   `("TileGrid", "Touch-first responsive tile grid with integrated category filter strip and client-side text search; Tile children iterate via $each.", || to_value(schema_for!(TileGridProps)).unwrap(), &[])`. Add `TileGridProps` to the `use crate::component::{...}` import.
4. catalog.rs count guard: change `47` → `48` and append `// → 48 (TileGrid)` to the History comment line.
5. ferro-mcp json_ui_catalog.rs: mirror count `47` → `48` (update the count AND the message text/name list); add `"TileGrid"` to the `expected` names array.
6. ferro-mcp RULE_COMPONENTS (D-26): extend `register-fill-viewport` → `&["Grid", "TileGrid"]` and `register-grid-fill` → `&["Grid", "TileGrid"]`. Do NOT invent new rule ids; verify these ids exist in design::rules().

Tests (containers.rs): `tile_grid_emits_filter_scope` (data-filter-scope present), `tile_grid_search_input_is_16px` (search:true → `data-filter-search` + `text-base`), `tile_grid_categories_path_populates_strip` (categories_path "/cats" + data `{"cats":["Drinks","Food"]}` → strip has `data-filter-tab="Drinks"` AND `data-filter-tab="Food"`; categories_path None → no strip), `tile_grid_columns_are_full_literals` (columns:3 → `grid-cols-3`). Run the CI-exact gate for BOTH crates (serialized): `cargo fmt --all -- --check`, `cargo clippy --all --all-targets --all-features -- -D warnings`, `cargo test --all-features`. If `prompt_under_size_budget` fails, bump the 12 KB cap by 1 KB with a History-style comment naming Phase 256. Commit: `feat(256): register TileGrid builtin (count 48)`.
  </action>
  <verify>
    <automated>cargo test -p ferro-json-ui --all-features tile_grid 2>&1 | tail -20 && cargo test -p ferro-mcp --all-features test_all_components_present 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"TileGrid"' ferro-json-ui/src/render/mod.rs` returns ≥2 (BUILTIN_TYPES entry + dispatch arm).
    - `grep -n "render_tile_grid" ferro-json-ui/src/render/containers.rs ferro-json-ui/src/render/mod.rs` shows definition + dispatch.
    - `grep -n "resolve_path(data" ferro-json-ui/src/render/containers.rs` shows categories_path resolution inside render_tile_grid.
    - `grep -n "pub(crate) fn render_filter_tab_strip" ferro-json-ui/src/render/atoms.rs` returns the shared helper.
    - `grep -n "assert_eq!(crate::render::BUILTIN_TYPES.len(), 48)" ferro-json-ui/src/catalog.rs` returns the bumped canonical guard; `grep -n "→ 48 (TileGrid)" ferro-json-ui/src/catalog.rs` returns the History line.
    - `grep -n "catalog.components.len()," ferro-mcp/src/tools/json_ui_catalog.rs` context shows `48`; `grep -c '"TileGrid"' ferro-mcp/src/tools/json_ui_catalog.rs` ≥2 (expected array + RULE_COMPONENTS).
    - `grep -n 'register-fill-viewport", &\["Grid", "TileGrid"\]' ferro-mcp/src/tools/json_ui_catalog.rs` returns the extended mapping.
    - `grep -c 'format!("grid-cols-{' ferro-json-ui/src/render/containers.rs` returns 0.
    - `cargo test --all-features` green across ferro-json-ui + ferro-mcp; one commit made for TileGrid.
  </acceptance_criteria>
  <done>TileGrid renders (scope+search+strip-from-categories_path+children), is registered at count 48 in both catalogs with History + expected + RULE_COMPONENTS updated, columns are full literals, SC-5 strip population tested, single commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: render_filter_tabs + register FilterTabs (commit, count 48→49) + SC-5</name>
  <files>ferro-json-ui/src/render/atoms.rs, ferro-json-ui/src/render/mod.rs, ferro-json-ui/src/catalog.rs, ferro-mcp/src/tools/json_ui_catalog.rs</files>
  <read_first>
    - ferro-json-ui/src/render/atoms.rs (render_filter_tab_strip helper added in Task 1; decode_props convention for atoms; test module spec_with_root)
    - ferro-json-ui/src/render/mod.rs (BUILTIN_TYPES atoms section; dispatch)
    - ferro-json-ui/src/catalog.rs (count guard now at 48; History; BUILTIN_SPECS atoms section)
    - ferro-mcp/src/tools/json_ui_catalog.rs (mirror count now 48; expected names)
    - ferro-json-ui/src/runtime/filters.rs (updateFilterTabClasses — the exact inactive class set the strip must emit)
    - .planning/phases/256-component-renderers-builtin-lockstep/256-CONTEXT.md (D-17 shared helper, D-18 nearest-scope semantics, D-20 no uncategorized sentinel, D-28 all_label "All")
    - .planning/phases/256-component-renderers-builtin-lockstep/256-PATTERNS.md (render_filter_tabs pattern; all_label default)
  </read_first>
  <behavior>
    - render_filter_tabs emits the shared strip inside a `data-filter-scope` wrapper (a standalone FilterTabs emits its own scope so setupFilters binds it; document the nearest-ancestor composition rustdoc note, D-18).
    - The All tab carries `data-filter-tab=""` with active-initial classes + aria-selected="true"; each item tab carries `data-filter-tab="{token}"` (space→hyphen) with inactive-initial classes matching updateFilterTabClasses (`border-transparent text-text-muted hover:text-text`) + aria-selected="false".
    - all_label default is "All" when props.all_label is None (D-28); a passed all_label is used and html_escaped.
    - Every tab is ≥44px (`HIT_TARGET_MIN`).
    - SC-5: a FilterTabs with items ["Drinks","Food"] emits `data-filter-tab="Drinks"` and `data-filter-tab="Food"`; paired with a Tile carrying data-filter-tokens the shipped setupFilters filters visibility (assert the emitted attributes align).
    - Count 49 in both catalogs.
  </behavior>
  <action>
Write `render_filter_tabs` in atoms.rs (use `decode_props`, `decode_diagnostic("FilterTabs", e)` on error). Body:
- `let all_label = props.all_label.as_deref().unwrap_or("All");` (D-28).
- Wrap the shared strip in a scope: `format!("<div data-filter-scope class=\"...\">{}</div>", render_filter_tab_strip(&props.items, all_label))`. The wrapper provides the `data-filter-scope` so a standalone FilterTabs is directly bindable (D-18); add a rustdoc note that placing FilterTabs inside an existing filter scope also works via nearest-ancestor semantics.

Register FilterTabs (lockstep, ONE commit):
1. render/mod.rs BUILTIN_TYPES: add `"FilterTabs",` in the atoms section.
2. render/mod.rs dispatch: `"FilterTabs" => atoms::render_filter_tabs(el, spec, data, depth),`.
3. catalog.rs BUILTIN_SPECS: add at the matching atoms position: `("FilterTabs", "Standalone touch filter-tab strip that filters visible tiles client-side by category token; renders an All tab plus one tab per item.", || to_value(schema_for!(FilterTabsProps)).unwrap(), &[])`; import FilterTabsProps.
4. catalog.rs count guard: `48` → `49`; append `// → 49 (FilterTabs)`.
5. ferro-mcp: mirror count `48` → `49` (+ message); add `"FilterTabs"` to expected names. FilterTabs is NOT added to any RULE_COMPONENTS rule (it is not a register-trigger component; the exhaustive test does not require it).

Tests (atoms.rs): `filter_tabs_all_label_defaults_to_all` (no all_label → `>All<` present, no "Tutte"), `filter_tabs_emits_scope_and_tabs` (`data-filter-scope` + `data-filter-tab=""` + item tabs), `filter_tabs_inactive_classes_match_runtime` (item tab has `border-transparent text-text-muted hover:text-text` — the exact set updateFilterTabClasses toggles), `filter_tabs_targets_are_44px` (`HIT_TARGET_MIN` on tabs). Add the SC-5 pairing test `filter_tabs_tokens_match_tile_tokens` asserting a FilterTabs items strip emits `data-filter-tab="Drinks"` and a Tile carries `data-filter-tokens` containing "drinks" (normalized). Run the CI-exact gate (serialized) for both crates; bump prompt budget if needed. Commit: `feat(256): register FilterTabs builtin (count 49)`.
  </action>
  <verify>
    <automated>cargo test -p ferro-json-ui --all-features filter_tabs 2>&1 | tail -20 && cargo test -p ferro-mcp --all-features test_all_components_present 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"FilterTabs"' ferro-json-ui/src/render/mod.rs` returns ≥2.
    - `grep -n "render_filter_tabs" ferro-json-ui/src/render/atoms.rs ferro-json-ui/src/render/mod.rs` shows definition + dispatch.
    - `grep -n "unwrap_or(\"All\")" ferro-json-ui/src/render/atoms.rs` shows the neutral default.
    - `grep -n "assert_eq!(crate::render::BUILTIN_TYPES.len(), 49)" ferro-json-ui/src/catalog.rs` and `grep -n "→ 49 (FilterTabs)" ferro-json-ui/src/catalog.rs` both return.
    - ferro-mcp mirror count context shows `49`; `grep -c '"FilterTabs"' ferro-mcp/src/tools/json_ui_catalog.rs` ≥1.
    - `border-transparent text-text-muted hover:text-text` literal present in atoms.rs (matches updateFilterTabClasses).
    - `cargo test --all-features` green across both crates; single commit for FilterTabs.
  </acceptance_criteria>
  <done>FilterTabs renders the shared strip inside its own scope with All+item tabs, "All" neutral default, runtime-matching inactive classes, ≥44px; registered at count 49 both catalogs; SC-5 composition asserted; single commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec author props → rendered HTML | data_path, form_id, categories_path, items, all_label, tokens interpolated into HTML/attributes at SSR time. |
| bound data → category strip | categories_path resolves against `data`; resolved category strings become tab labels/tokens. |
| filter tab → tile visibility (browser) | Tab token values feed setupFilters `data-filter-tab` matching; tokens compared against tile `data-filter-tokens`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-256-05 | Tampering | render_tile_grid / render_filter_tabs prop + resolved-category interpolation | mitigate | `html_escape()` on all_label, item labels, resolved category strings, tokens, and any prop reaching an attribute; tokens additionally normalized (space→hyphen) mirroring render_tile so no space breaks the space-separated attribute contract. |
| T-256-06 | Tampering | column class construction | mitigate | Exhaustive full-literal match on `columns: Option<u8>` → `grid-cols-{1,2,3,4}`; NEVER `format!("grid-cols-{}", n)` (SC-3 — closes the missing-class production break). |
| T-256-07 | Information disclosure | catalog count/mirror drift | mitigate | Both count guards bumped in the same commit per component; History comment is the audit trail; component_rule_mapping_is_exhaustive keeps RULE_COMPONENTS names valid builtins. |
</threat_model>

<verification>
- `cargo test --all-features` green across ferro-json-ui and ferro-mcp (BUILTIN drift guards at 49, mapping exhaustive, prompt budget within cap).
- `grep -rn 'format!(".*-{}' ferro-json-ui/src/render/atoms.rs ferro-json-ui/src/render/containers.rs` shows no dynamic class construction.
- Two commits exist, one per component, each bumping BOTH counts by one with a History line.
</verification>

<success_criteria>
- TileGrid and FilterTabs are registered builtins; canonical + mirror counts both read 49; History comment records `→ 48 (TileGrid)` and `→ 49 (FilterTabs)`.
- The shared render_filter_tab_strip helper is the single markup source for both the standalone FilterTabs and TileGrid's integrated strip.
- TileGrid emits data-filter-scope, a 16px search input (when search:true), a category strip populated from categories_path via resolve_path (SC-5), full-literal column classes, and recurses Tile children; FilterTabs emits All + item tabs with runtime-matching inactive classes and "All" neutral default.
- RULE_COMPONENTS extended for TileGrid only; no dynamic class construction.
</success_criteria>

<output>
After completion, create `.planning/phases/256-component-renderers-builtin-lockstep/256-02-SUMMARY.md`.
Record: the render_filter_tab_strip signature/location, the categories_path resolve_path idiom used, the exact grid-cols ladder, whether the prompt budget was bumped, and the current canonical count (49) so Plan 03 continues the lockstep from the correct number.
</output>
