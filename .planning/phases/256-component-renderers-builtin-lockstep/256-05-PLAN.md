---
phase: 256-component-renderers-builtin-lockstep
plan: 05
type: execute
wave: 5
depends_on: ["04"]
files_modified:
  - ferro-json-ui/assets/ferro-base.css
  - docs/src/json-ui/components.md
autonomous: true
requirements: [POS-01, POS-03, POS-04, POS-05, POS-06, POS-09]
must_haves:
  truths:
    - "ferro-base.css is regenerated ONCE after all five renderers + selection.rs land; every class emitted by the new render functions and selection.rs JS literals is present in the generated CSS (no missing-class layout break)."
    - "The docs/src/json-ui/components.md v16.6 migration table gains one note recording the Tile tap-to-add interaction redesign (on-tile stepper markup replaced; tile root is now a tap-to-add button); no full new-component docs (those are Phase 258)."
    - "Schema export churn is resolved per D-30: docs/protocol/schemas/*.json has NO real content change (new props are ferro-json-ui types, not ferro-projections types) and is discarded, not committed — unless a real diff is proven."
    - "The full CI-exact gate is green across the workspace: fmt --check, clippy --all --all-targets --all-features -D warnings, test --all-features, and cargo doc clean."
  artifacts:
    - path: "ferro-json-ui/assets/ferro-base.css"
      provides: "regenerated stylesheet covering all Phase 256 class literals"
    - path: "docs/src/json-ui/components.md"
      provides: "one-line tap-to-add migration note under the v16.6 migration table"
      contains: "tap-to-add"
  key_links:
    - from: "render_* full-literal classes + selection.rs JS class strings"
      to: "ferro-base.css"
      via: "scripts/gen-ferro-base-css.sh Tailwind scan of src/**/*.rs"
      pattern: "scripts/gen-ferro-base-css.sh"
---

<objective>
Close the phase: regenerate `ferro-base.css` once (D-29) now that all five
renderers and the selection runtime have introduced their class literals, add the
single Tile tap-to-add migration note to the components doc (D-02), resolve the
schema-export question (D-30) by verifying it is a no-op, and run the full
workspace CI-exact gate.

Purpose: Ship a coherent, drift-free phase — no missing CSS classes, docs reflect
the redesign, and the tree is clean of unrelated schema churn.

Output: Regenerated `ferro-base.css` (committed only if changed), a one-line
components.md migration note, a verified-clean schema-export state, and a green
full gate. Final phase commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/256-component-renderers-builtin-lockstep/256-CONTEXT.md
@.planning/phases/256-component-renderers-builtin-lockstep/256-RESEARCH.md
@.planning/phases/256-component-renderers-builtin-lockstep/256-PATTERNS.md
@.planning/phases/256-component-renderers-builtin-lockstep/256-01-SUMMARY.md
@.planning/phases/256-component-renderers-builtin-lockstep/256-04-SUMMARY.md

<interfaces>
<!-- Closeout mechanics. -->

scripts/gen-ferro-base-css.sh runs:
  .tooling/bin/tailwindcss -i ferro-json-ui/assets/input.css -o ferro-json-ui/assets/ferro-base.css --minify
The Tailwind scanner sources ferro-json-ui/src/**/*.rs — full-literal class strings in render
functions AND in runtime/*.rs SOURCE consts are picked up automatically (confirmed: existing
filters.rs classes are already in the generated CSS). Only format!()-built classes would need a
safelist entry — this phase uses none (SC-3), so no input.css safelist edits are expected.

docs/src/json-ui/components.md: "## Component rename migration (v16.6)" table (~:103–112).
Append ONE row/note recording the tap-to-add redesign. Do NOT author full props tables (Phase 258).

Schema export (D-30 resolution): ferro-projections/tests/generate_schemas.rs regenerates
docs/protocol/schemas/*.json from ferro-projections types ONLY. Phase 256 props (price_cents,
SelectionPanelProps.currency, the five component props) are ferro-json-ui types → NO real schema
change expected. RESEARCH Q12 confirms D-30's "commit them" expectation is overstated. Verify, then
`git checkout docs/protocol/schemas/` if the diff is only churn; commit only a proven real change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate ferro-base.css + verify no class is missing (commit if changed)</name>
  <files>ferro-json-ui/assets/ferro-base.css</files>
  <read_first>
    - scripts/gen-ferro-base-css.sh (exact command + scanner sources)
    - ferro-json-ui/assets/input.css (existing @utility/@source declarations; safelist — confirm the phase added no format!()-built classes needing entries)
    - .planning/phases/256-component-renderers-builtin-lockstep/256-CONTEXT.md (D-29 single regen; "any class that turns out dynamic is a design error, not a safelist entry")
    - .planning/phases/256-component-renderers-builtin-lockstep/256-04-SUMMARY.md (any semantic-token class literals selection.rs introduced)
    - .planning/phases/256-component-renderers-builtin-lockstep/256-RESEARCH.md (Q11 regen mechanics; A4 — scanner sources .rs runtime files)
  </read_first>
  <action>
Run `bash scripts/gen-ferro-base-css.sh` from the repo root (ONE regen for the phase, D-29). Then:
1. `git diff --stat ferro-json-ui/assets/ferro-base.css` — inspect what changed.
2. Spot-check that the NEW class literals introduced this phase are present in the generated CSS. Grep the generated file for a representative sample from each new surface, e.g.: `aspect-square`, `object-cover`, `grid-cols-3`, `grid-cols-4`, `border-success` (tile Tone accent), `overscroll-contain` (already present), and any semantic-token class selection.rs introduced (per 256-04-SUMMARY). Note: `grid-template-rows` is an inline style, NOT a class — it must NOT appear as a utility and needs no scan.
3. If any expected class is ABSENT from the generated CSS: this indicates a dynamic (format!()-built) class construction slipped through — do NOT add a safelist entry (D-29). Instead grep `grep -rn 'format!(".*-{}' ferro-json-ui/src/render/` to find the offending construction and STOP: report it (the render function must use a full literal; this is a Plan 02/03 defect to fix, not a css-safelist workaround).
4. Commit the regenerated `ferro-base.css` ONLY if it changed: `chore(256): regenerate ferro-base.css for POS component classes`. If unchanged, note that and make no commit.
  </action>
  <verify>
    <automated>bash scripts/gen-ferro-base-css.sh && git diff --stat ferro-json-ui/assets/ferro-base.css</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/gen-ferro-base-css.sh` runs clean (exit 0).
    - `grep -c "aspect-square" ferro-json-ui/assets/ferro-base.css` returns ≥1 (tile image class present).
    - `grep -c "grid-cols-3\|grid-cols-4" ferro-json-ui/assets/ferro-base.css` returns ≥1 (TileGrid column ladder present).
    - `grep -rn 'format!(".*-{}' ferro-json-ui/src/render/` returns no unaccounted dynamic class construction (SC-3).
    - ferro-base.css is either unchanged (documented) or committed with the regen message; no input.css safelist entries were added for Phase 256 classes.
  </acceptance_criteria>
  <done>ferro-base.css regenerated once; all Phase 256 full-literal classes present; no dynamic class construction; committed only if changed.</done>
</task>

<task type="auto">
  <name>Task 2: Tile tap-to-add migration note + schema-export verify + full gate (commit)</name>
  <files>docs/src/json-ui/components.md</files>
  <read_first>
    - docs/src/json-ui/components.md ("## Component rename migration (v16.6)" table ~:103–112; the ProductTile→Tile row)
    - .planning/phases/256-component-renderers-builtin-lockstep/256-CONTEXT.md (D-02 migration note wording scope, D-30 schema, D-31 gate)
    - .planning/phases/256-component-renderers-builtin-lockstep/256-RESEARCH.md (Q12 schema-export scope — ferro-projections types only)
    - .planning/phases/256-component-renderers-builtin-lockstep/256-PATTERNS.md (§components.md migration-note pattern)
    - MEMORY project_schema_export_test_dirties_tree.md context: `cargo test` regenerates docs/protocol/schemas/*.json; git-checkout unrelated churn.
  </read_first>
  <action>
1. Docs (D-02): append ONE note/row to the "## Component rename migration (v16.6)" table in docs/src/json-ui/components.md recording the tap-to-add redesign. Use neutral architectural language, e.g.:
   `| Tile interaction (on-tile +/- stepper) | Tile (tap-to-add) | The on-tile quantity stepper markup was replaced in v16.6 (Phase 256): the tile root is now a tap-to-add button (one tap adds one unit). Per-line quantity editing moved to the SelectionPanel. |`
   Do NOT write full props tables for the five new components — that is Phase 258.

2. Schema-export resolution (D-30): run `cargo test --all-features` (the full suite regenerates docs/protocol/schemas/*.json as a side effect). Then `git diff --stat docs/protocol/schemas/`:
   - EXPECTED per RESEARCH Q12: NO real content change (the new props are ferro-json-ui types, not ferro-projections types). If the diff is empty or only ordering/whitespace churn → `git checkout docs/protocol/schemas/` (discard; do NOT commit — D-30's "commit them" clause is superseded by the code evidence, an audit-fix per feedback_audit_report_fix_discrepancies).
   - If (unexpectedly) a REAL ferro-projections schema change appears → investigate the cause before committing; a real change would mean something outside this phase's scope moved. Report it rather than silently committing.

3. Full CI-exact gate (D-31), serialized (one CPU op at a time — never parallelize cargo):
   `cargo fmt --all -- --check` → `cargo clippy --all --all-targets --all-features -- -D warnings` → `cargo test --all-features` → `cargo doc --no-deps` clean. Re-run fmt after the docs edit. Fix any failure before committing.

4. Commit the docs note (and nothing else unless ferro-base.css was committed in Task 1): `docs(256): note Tile tap-to-add interaction redesign in v16.6 migration table`.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo test --all-features 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tap-to-add" docs/src/json-ui/components.md` returns ≥1 (migration note present).
    - `git status --porcelain docs/protocol/schemas/` returns empty after the verify step (churn discarded; no schema files staged/committed) — OR a documented real change with justification.
    - `cargo fmt --all -- --check` exits 0; `cargo clippy --all --all-targets --all-features -- -D warnings` exits 0; `cargo test --all-features` green; `cargo doc --no-deps` clean.
    - The docs commit contains only components.md (schema churn not included).
  </acceptance_criteria>
  <done>One tap-to-add migration note added; schema export verified as a no-op and discarded; full CI-exact gate green; final phase commit made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build tooling → generated CSS | The regen consumes only in-repo source class literals; no external/user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-256-16 | Tampering | missing/dynamic CSS class → silent production layout break | mitigate | The regen + explicit presence grep for new classes closes the "class absent from ferro-base.css" gap; SC-3 grep proves no format!()-built class exists. No safelist workaround (D-29 — dynamic class is a design error to fix upstream). |
| T-256-17 | Repudiation | committing unrelated schema churn | accept | D-30 resolution: verify + discard non-real schema churn so the phase commit reflects only Phase 256 substance (audit-fix per feedback_audit_report_fix_discrepancies). |
</threat_model>

<verification>
- Full CI-exact gate green: fmt --check, clippy --all --all-targets --all-features -D warnings, test --all-features, cargo doc clean.
- ferro-base.css contains all Phase 256 full-literal classes; no dynamic class construction anywhere in src/render.
- docs/protocol/schemas/ is clean (churn discarded) unless a real, justified change is committed.
- components.md carries exactly one tap-to-add migration note; no full new-component docs (Phase 258 boundary respected).
</verification>

<success_criteria>
- ferro-base.css regenerated once and coherent (every new class present); committed only if changed; no safelist workaround.
- One neutral tap-to-add migration note in components.md.
- Schema-export question (D-30) resolved: verified no-op, churn discarded.
- Full workspace CI-exact gate green — phase ready for /gsd-verify-work.
</success_criteria>

<output>
After completion, create `.planning/phases/256-component-renderers-builtin-lockstep/256-05-SUMMARY.md`.
Record: whether ferro-base.css changed (and the new classes surfaced), the exact migration-note text, the schema-export outcome (no-op confirmed / real change committed), and the final green-gate evidence (counts: BUILTIN 52, drift guards, mapping exhaustive).
</output>
