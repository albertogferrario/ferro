---
phase: 94-protocol-patent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/protocol/book.toml
  - docs/protocol/src/SUMMARY.md
  - docs/protocol/schemas/service-def.json
  - docs/protocol/schemas/field-def.json
  - docs/protocol/schemas/data-type.json
  - docs/protocol/schemas/field-meaning.json
  - docs/protocol/schemas/state-machine.json
  - docs/protocol/schemas/state-def.json
  - docs/protocol/schemas/transition.json
  - docs/protocol/schemas/action-def.json
  - docs/protocol/schemas/input-def.json
  - docs/protocol/schemas/guard-def.json
  - docs/protocol/schemas/relationship-def.json
  - docs/protocol/schemas/cardinality.json
  - docs/protocol/schemas/navigation-hint.json
  - docs/protocol/schemas/intent.json
  - docs/protocol/schemas/intent-score.json
  - docs/protocol/schemas/intent-hint.json
  - docs/protocol/schemas/warning.json
  - ferro-projections/tests/generate_schemas.rs
autonomous: true
---

<objective>
Set up the protocol specification infrastructure: mdBook project, JSON Schema generation from Rust types, and full directory structure.

Purpose: Establish the foundation that all other protocol specification plans write into. JSON Schema files are generated from schemars derives already on all ferro-projections public types — never hand-written.
Output: Working mdBook project at docs/protocol/ with generated JSON Schema files and complete table of contents.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-protocol-patent/94-RESEARCH.md

@ferro-projections/src/lib.rs
@ferro-projections/src/field.rs
@ferro-projections/src/service.rs
@ferro-projections/src/state.rs
@ferro-projections/src/action.rs
@ferro-projections/src/relationship.rs
@ferro-projections/src/intent.rs
@ferro-projections/src/error.rs
@ferro-projections/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up mdBook project structure</name>
  <files>docs/protocol/book.toml, docs/protocol/src/SUMMARY.md, docs/protocol/src/.gitkeep files for subdirectories</files>
  <action>
Create the mdBook project at docs/protocol/ (separate from existing user docs at docs/):

1. Create docs/protocol/book.toml with:
   - title: "Ferro Projections Protocol Specification"
   - authors: ferro-rs contributors
   - language: en
   - src: src
   - build-dir: book
   - No custom theme needed — default mdBook theme is clean

2. Create docs/protocol/src/SUMMARY.md with the full table of contents:
   ```
   # Summary

   [Introduction](introduction.md)

   # Protocol

   - [Terminology](terminology.md)
   - [Architecture](architecture.md)
   - [Data Model](data-model/README.md)
     - [ServiceDef](data-model/service-def.md)
     - [Fields & Types](data-model/field-def.md)
     - [State Machines](data-model/state-machine.md)
     - [Actions & Guards](data-model/actions.md)
     - [Relationships](data-model/relationships.md)
     - [Intents](data-model/intent.md)
   - [Intent Derivation](derivation.md)
   - [Rendering](rendering.md)
   - [Validation](validation.md)

   # Protocol Governance

   - [Extensions](extensions.md)
   - [Conformance](conformance.md)
   - [Security Considerations](security.md)
   - [Related Work](related-work.md)

   # Appendix

   - [Examples](appendix/examples.md)
   - [JSON Schema Reference](appendix/json-schema.md)
   - [Changelog](appendix/changelog.md)
   ```

3. Create all subdirectories: docs/protocol/src/data-model/, docs/protocol/src/appendix/
4. Create placeholder .md files for each entry (empty or single-line heading) so mdBook builds without errors.
5. Create docs/protocol/schemas/ directory for generated JSON Schema files.
6. Add docs/protocol/book/ to .gitignore (mdBook build output).

Do NOT install mdBook — that's the user's responsibility. Just create the project structure.
  </action>
  <verify>All directories and placeholder files exist. `ls -R docs/protocol/src/` shows complete structure matching SUMMARY.md.</verify>
  <done>mdBook project structure at docs/protocol/ with SUMMARY.md containing full TOC, all placeholder pages created, schemas/ directory ready.</done>
</task>

<task type="auto">
  <name>Task 2: Create JSON Schema generation test and generate all schemas</name>
  <files>ferro-projections/tests/generate_schemas.rs, docs/protocol/schemas/*.json</files>
  <action>
Create an integration test in ferro-projections that generates JSON Schema for all public types using schemars.

1. Create ferro-projections/tests/generate_schemas.rs:
   - Use `schemars::schema_for!()` for each of the following types:
     - ServiceDef, FieldDef, DataType, FieldMeaning
     - StateMachine, StateDef, Transition, Warning
     - ActionDef, InputDef, GuardDef
     - RelationshipDef, Cardinality, NavigationHint
     - Intent, IntentScore, IntentHint
   - For each schema, set `$id` to `https://ferro-rs.dev/protocol/2026-03-01/{type-name}.json` (date-based versioning per MCP pattern)
   - Write each schema to `docs/protocol/schemas/{type-name}.json` using `serde_json::to_string_pretty()`
   - The test function should be named `generate_protocol_schemas` and marked `#[test]`
   - Use `env!("CARGO_MANIFEST_DIR")` to resolve the path to the workspace root, then navigate to `../../docs/protocol/schemas/`
   - Create the schemas/ directory if it doesn't exist

2. The test should also generate a combined `docs/protocol/schemas/protocol.json` that bundles all type schemas under `$defs` for consumers who want a single file.

3. Run the test to generate all schema files.

Do NOT add `jsonschema` as a dependency — schema validation is not needed at this stage. Only schemars (already a dependency) is used.

Type names in filenames should use kebab-case: service-def.json, field-def.json, data-type.json, field-meaning.json, state-machine.json, state-def.json, transition.json, action-def.json, input-def.json, guard-def.json, relationship-def.json, cardinality.json, navigation-hint.json, intent.json, intent-score.json, intent-hint.json, warning.json.
  </action>
  <verify>`cargo test -p ferro-projections --test generate_schemas` passes. All 17 individual schema files + 1 combined protocol.json exist in docs/protocol/schemas/. Each schema has a valid `$schema` and `$id` field.</verify>
  <done>Integration test generates 18 JSON Schema files from Rust types. Schemas are reproducible (running the test regenerates them identically).</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] docs/protocol/book.toml exists with correct configuration
- [ ] docs/protocol/src/SUMMARY.md has complete TOC
- [ ] All placeholder .md files exist matching SUMMARY.md entries
- [ ] docs/protocol/schemas/ contains 17 individual + 1 combined JSON Schema files
- [ ] `cargo test -p ferro-projections --test generate_schemas` passes
- [ ] `cargo test --all-features` still passes (no regressions)
- [ ] `cargo clippy --all --all-targets -- -D warnings` passes
</verification>

<success_criteria>
- mdBook project structure complete at docs/protocol/
- JSON Schema generation automated via integration test
- All 22 ferro-projections public types have corresponding JSON Schema files
- No manual schema editing — Rust types are source of truth
</success_criteria>

<output>
After completion, create `.planning/phases/94-protocol-patent/94-01-SUMMARY.md`
</output>
