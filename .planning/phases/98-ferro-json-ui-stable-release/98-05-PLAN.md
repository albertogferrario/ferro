---
phase: 98-ferro-json-ui-stable-release
plan: 05
type: execute
wave: 5
depends_on: ["98-01", "98-02", "98-03", "98-04"]
files_modified:
  - docs/src/json-ui/components.md
  - docs/src/json-ui/layouts.md
  - docs/src/json-ui/getting-started.md
  - docs/src/json-ui/plugins.md
  - docs/src/SUMMARY.md
  - ferro-json-ui/src/lib.rs
  - ferro-json-ui/src/component.rs
  - ferro-json-ui/src/view.rs
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03]

must_haves:
  truths:
    - "Component catalog page documents all 26 components with props tables and code examples"
    - "Plugin guide explains how to create, register, and use plugins"
    - "DashboardLayout documentation covers sidebar/header configuration and JS runtime"
    - "Rustdoc builds without warnings"
  artifacts:
    - path: "docs/src/json-ui/components.md"
      provides: "Full component catalog with all 26 components"
      contains: "StatCard"
    - path: "docs/src/json-ui/plugins.md"
      provides: "Dedicated plugin guide"
      contains: "JsonUiPlugin"
    - path: "docs/src/json-ui/layouts.md"
      provides: "Updated layout docs with DashboardLayout"
      contains: "DashboardLayout"
  key_links:
    - from: "docs/src/SUMMARY.md"
      to: "docs/src/json-ui/plugins.md"
      via: "mdBook SUMMARY entry"
      pattern: "Plugins.*plugins.md"
---

<objective>
Write comprehensive documentation: full component catalog, plugin guide, DashboardLayout docs, and clean rustdoc output.

Purpose: Documentation completes the stable release. Users and agents can discover all components, their props, and usage patterns.
Output: Updated component catalog (26 components), new plugin guide, updated layout docs with DashboardLayout, clean rustdoc.
</objective>

<execution_context>
@/Users/alberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-ferro-json-ui-stable-release/98-CONTEXT.md
@.planning/phases/98-ferro-json-ui-stable-release/98-01-SUMMARY.md
@.planning/phases/98-ferro-json-ui-stable-release/98-02-SUMMARY.md
@.planning/phases/98-ferro-json-ui-stable-release/98-03-SUMMARY.md

@docs/src/json-ui/components.md
@docs/src/json-ui/layouts.md
@docs/src/json-ui/getting-started.md
@docs/src/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update component catalog and add plugin guide</name>
  <files>docs/src/json-ui/components.md, docs/src/json-ui/plugins.md, docs/src/SUMMARY.md</files>
  <action>
**Update components.md** — Rewrite to cover all 26 component types. Structure:

For each component, provide:
- Component name and one-line description
- Props table (field name, type, required/optional, description)
- Rust code example using the convenience constructor
- JSON output example showing the serialized shape

Group components into sections:
1. **Layout** — Card, Tabs, Separator, Modal, Skeleton
2. **Data Display** — Table, DescriptionList, Badge, Avatar, Text, Progress, Breadcrumb, Pagination, StatCard
3. **Forms** — Form, Input, Select, Checkbox, Switch, Button
4. **Feedback** — Alert, Toast
5. **Navigation** — Sidebar, Header, NotificationDropdown
6. **Onboarding** — Checklist
7. **Extensible** — Plugin (brief, points to plugin guide)

For the 6 new components, include detailed examples matching gestiscilo usage patterns. For StatCard, show the `sse_target` field and explain how it connects to the JS runtime for live updates.

**Create plugins.md** — New file. Plugin guide covering:
1. What plugins are (extend component catalog with custom types)
2. The `JsonUiPlugin` trait — `plugin_type()`, `render()`, `props_schema()`, `css_assets()`, `js_assets()`
3. Registering a plugin: `register_plugin(MyPlugin)`
4. Using a plugin in a view: `Component::Plugin(PluginProps { ... })`
5. How assets are collected and injected
6. Example: Building a chart plugin (hypothetical, showing the trait impl pattern)
7. Built-in plugins: MapPlugin (Leaflet integration)

**Update SUMMARY.md** — Add "Plugins" entry to the JSON-UI section:
```
- [Plugins](json-ui/plugins.md)
```
Place it after "Layouts" in the JSON-UI section.
  </action>
  <verify>
    <automated>cd /Users/alberto/repositories/albertogferrario/ferro && test -f docs/src/json-ui/plugins.md && grep -c "StatCard\|Checklist\|Toast\|NotificationDropdown\|Sidebar\|Header" docs/src/json-ui/components.md</automated>
  </verify>
  <done>components.md covers all 26 components with props/examples/JSON, plugins.md explains the full plugin system, SUMMARY.md updated.</done>
</task>

<task type="auto">
  <name>Task 2: Update layout docs, getting-started, and clean rustdoc</name>
  <files>docs/src/json-ui/layouts.md, docs/src/json-ui/getting-started.md, ferro-json-ui/src/lib.rs, ferro-json-ui/src/component.rs, ferro-json-ui/src/view.rs</files>
  <action>
**Update layouts.md** — Add DashboardLayout section:
1. What DashboardLayout provides (persistent sidebar + header shell)
2. DashboardLayoutConfig fields: sidebar (SidebarProps), header (HeaderProps), sse_url
3. Registering the dashboard layout at app startup:
```rust
register_layout("dashboard", DashboardLayout::new(DashboardLayoutConfig {
    sidebar: SidebarProps { ... },
    header: HeaderProps { ... },
    sse_url: Some("/dashboard/events".into()),
}));
```
4. Using it in a view: `JsonUiView::new().layout("dashboard")`
5. Mobile behavior: sidebar auto-collapses, hamburger toggle
6. JS runtime: auto-initialized, handles SSE/toast/live-value

Remove references to the old AppLayout/AuthLayout from the user-facing docs (they are now framework-internal). Mention DefaultLayout briefly as the fallback.

**Update getting-started.md** — Refresh the quick start example to use convenience constructors:
```rust
let view = JsonUiView::new()
    .title("Dashboard")
    .layout("dashboard")
    .component(ComponentNode::card("welcome", CardProps { ... }))
    .component(ComponentNode::stat_card("orders", StatCardProps { ... }));
```

**Rustdoc cleanup:**
- Ensure lib.rs module doc describes the stable API (no "experimental" language)
- Add doc comments on all 6 new Props structs (already should exist from Plan 01, verify)
- Add doc comments on DashboardLayout, DashboardLayoutConfig
- Add `#![warn(missing_docs)]` is NOT needed (too aggressive for this crate), but verify `cargo doc -p ferro-json-ui --no-deps` builds without warnings
- Add brief rustdoc examples on key types: JsonUiView (already has one), ComponentNode (add one showing convenience constructor), StatCardProps (add one showing SSE target usage)
  </action>
  <verify>
    <automated>cd /Users/alberto/repositories/albertogferrario/ferro && cargo doc -p ferro-json-ui --no-deps 2>&1 | grep -c "warning" || echo "0 warnings"</automated>
  </verify>
  <done>layouts.md covers DashboardLayout with full setup example, getting-started.md uses convenience constructors, rustdoc builds without warnings, key types have doc examples.</done>
</task>

</tasks>

<verification>
- `cargo doc -p ferro-json-ui --no-deps` builds without warnings
- components.md mentions all 26 component types
- plugins.md explains the full plugin lifecycle
- layouts.md covers DashboardLayout configuration
- getting-started.md uses convenience constructors
</verification>

<success_criteria>
- Full component catalog: all 26 components documented with props, code examples, JSON shape
- Plugin guide: trait, registration, rendering, assets, example
- DashboardLayout: configuration, registration, JS runtime, mobile behavior
- Rustdoc clean (no warnings)
- SUMMARY.md includes Plugins entry
</success_criteria>

<output>
After completion, create `.planning/phases/98-ferro-json-ui-stable-release/98-05-SUMMARY.md`
</output>
