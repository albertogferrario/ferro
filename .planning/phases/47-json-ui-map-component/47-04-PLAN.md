---
phase: 47-json-ui-map-component
plan: 04
type: execute
wave: 4
depends_on: ["47-03"]
files_modified:
  - ferro-mcp/src/tools/json_ui_catalog.rs
  - ferro-mcp/src/tools/json_ui_inspect.rs
  - ferro-mcp/src/tools/json_ui_generate.rs
  - ferro-mcp/src/tools/code_templates.rs
  - ferro-cli/src/ai.rs
  - docs/src/features/json-ui.md
autonomous: true
---

<objective>
Update MCP tools, CLI, and documentation to expose the plugin system and Map component to agents and developers.

Purpose: Agents must be able to discover the Map component via MCP, generate views containing it, and understand how to create custom plugins. Developers need documentation for the plugin system.
Output: MCP tools return Map component info, CLI AI context includes Map, docs cover plugin system + Map component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-json-ui-map-component/47-03-SUMMARY.md

@ferro-mcp/src/tools/json_ui_catalog.rs
@ferro-mcp/src/tools/json_ui_inspect.rs
@ferro-mcp/src/tools/json_ui_generate.rs
@ferro-mcp/src/tools/code_templates.rs
@ferro-cli/src/ai.rs
@docs/src/features/json-ui.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MCP JSON-UI tools for plugin + Map component discovery</name>
  <files>ferro-mcp/src/tools/json_ui_catalog.rs, ferro-mcp/src/tools/json_ui_inspect.rs, ferro-mcp/src/tools/json_ui_generate.rs</files>
  <action>
1. **json_ui_catalog.rs** — Add Map component to the catalog:
   - Add a "Plugin Components" section after the existing 20 built-in components
   - Include Map with: type name, description ("Interactive map with Leaflet"), props (center, zoom, markers, height, tile_url, attribution, max_zoom), and example JSON
   - Note that plugin components use the same `{"type": "Map", ...}` syntax as built-in components

2. **json_ui_inspect.rs** — Handle Map component inspection:
   - When inspecting a "Map" component, return its props_schema from the plugin registry
   - For unknown plugin types, query the plugin registry and return the schema if registered

3. **json_ui_generate.rs** — Update AI generation context:
   - Add Map to the component list in the generation prompt
   - Include Map props structure and usage example
   - Ensure the AI can generate views containing Map components

The COMPONENT_CATALOG string (duplicated in json_ui_catalog.rs and ferro-cli/src/ai.rs) must be updated in both locations — add Map component entry.
  </action>
  <verify>cargo check -p ferro-mcp 2>&1 | tail -10</verify>
  <done>MCP tools return Map component in catalog, support Map inspection, and include Map in generation context.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLI AI context and documentation</name>
  <files>ferro-cli/src/ai.rs, docs/src/features/json-ui.md</files>
  <action>
1. **ferro-cli/src/ai.rs** — Update the COMPONENT_CATALOG constant:
   - Add Map component entry matching the one in json_ui_catalog.rs
   - Include props structure and example

2. **docs/src/features/json-ui.md** — Add two new sections:

   **"Plugin System" section** (after the existing component documentation):
   - Explain the JsonUiPlugin trait and its methods
   - Show how to create a custom plugin (implement trait, register, use in views)
   - Explain asset loading: CSS in head, JS before body, init scripts
   - Show PluginRegistry registration in app bootstrap

   **"Map Component" section** (after Plugin System):
   - Component type: `Map`
   - Props table: center, zoom, height, markers, tile_url, attribution, max_zoom
   - Basic example: simple map with center and markers
   - Advanced example: custom tiles, custom height
   - Note about Leaflet being loaded via CDN automatically
   - Note about maps inside Tabs/Modals (IntersectionObserver handles invalidateSize)
   - CSP requirements note (Leaflet needs specific CSP rules for tiles and styles)

Keep documentation concise and example-focused.
  </action>
  <verify>cargo check -p ferro-cli 2>&1 | tail -10</verify>
  <done>CLI AI context includes Map component. Documentation covers plugin system and Map component with examples. COMPONENT_CATALOG synced between ferro-mcp and ferro-cli.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check --all-features` passes
- [ ] `cargo test --all-features` — all tests pass
- [ ] `cargo clippy --all-features` — no new warnings
- [ ] MCP json_ui_catalog includes Map component
- [ ] MCP json_ui_inspect handles Map component
- [ ] CLI COMPONENT_CATALOG includes Map
- [ ] Documentation covers plugin system and Map component
</verification>

<success_criteria>
- Agents can discover Map via `json_ui_catalog` MCP tool
- Agents can inspect Map props via `json_ui_inspect` MCP tool
- AI generation context includes Map in component list
- Documentation covers: plugin trait, custom plugin creation, Map props, examples, CSP notes
- COMPONENT_CATALOG consistent between ferro-mcp and ferro-cli
</success_criteria>

<output>
After completion, create `.planning/phases/47-json-ui-map-component/47-04-SUMMARY.md`
</output>
