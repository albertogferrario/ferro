---
phase: 251-component-variant-discipline-interactive-state-pass
plan: 04
type: execute
wave: 4
depends_on: ["251-01-PLAN.md", "251-02-PLAN.md", "251-03-PLAN.md"]
files_modified:
  - docs/src/json-ui/components.md
  - docs/src/json-ui/actions.md
  - docs/src/json-ui/forms.md
  - ferro-json-ui/assets/input.css
  - ferro-json-ui/assets/ferro-base.css
autonomous: false
requirements: [DS-03, DS-04]
must_haves:
  truths:
    - "A consumer can read a public `Component vocabulary migration` table mapping every renamed prop and value old → new (D-17)."
    - "The docs `Shared Enum Values` section describes exactly three enums (`variant`/`tone`/`size`) with the canonical value sets — no `xs`/`default`/`link`/`info`/`error`/`button_variant`/`badge_variant`."
    - "`ferro-base.css` contains every class the migrated components emit (`ring-ring`, `duration-fast`, `duration-base`, `ease-base`, `disabled:pointer-events-none`, `border-l-success`, neutral tone classes) — the UI renders styled."
    - "The full CI-exact workspace gate is green (fmt + clippy --all-features + test --all-features)."
    - "The sample app renders correctly in light and dark with the intended visual deltas and no unstyled/broken interactive states."
  artifacts:
    - path: "docs/src/json-ui/components.md"
      provides: "Collapsed canonical enum section + the D-17 migration table"
      contains: "Component vocabulary migration"
    - path: "ferro-json-ui/assets/ferro-base.css"
      provides: "Regenerated stylesheet covering all emitted classes"
      contains: "ring-ring"
  key_links:
    - from: "ferro-json-ui/assets/input.css @source safelist + render-source literals"
      to: "ferro-json-ui/assets/ferro-base.css"
      via: "scripts/gen-ferro-base-css.sh Tailwind scan"
      pattern: "ring-ring|duration-fast"
    - from: "RESEARCH migration skeleton + Plan 01/02 SUMMARY deltas"
      to: "docs/src/json-ui/components.md migration table"
      via: "verbatim old→new rows"
      pattern: "Component vocabulary migration"
---

<objective>
Ship the consumer-facing artifacts and close the phase gate: publish the migration table (D-17) that
gestiscilo Phase 232 depends on, collapse the docs enum section to the three canonical enums, regenerate
`ferro-base.css` once now that every class change is in tree (D-04), run the full CI-exact workspace
gate, and verify the sample app renders correctly in light and dark with the intended visual deltas.

Purpose: DS-03's migration-table requirement + DS-04's regen requirement + the phase-level "workspace
gate green" success criterion. No version bump / publish this phase (single publish at Phase 253 —
friction-loop release cadence).

Output: updated docs with the migration table, a regenerated `ferro-base.css`, a green full gate, and a
visual sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/251-component-variant-discipline-interactive-state-pass/251-CONTEXT.md
@.planning/phases/251-component-variant-discipline-interactive-state-pass/251-RESEARCH.md
@.planning/phases/251-component-variant-discipline-interactive-state-pass/251-PATTERNS.md
@.planning/phases/251-component-variant-discipline-interactive-state-pass/251-01-SUMMARY.md
@.planning/phases/251-component-variant-discipline-interactive-state-pass/251-02-SUMMARY.md
@.planning/phases/251-component-variant-discipline-interactive-state-pass/251-03-SUMMARY.md

<migration_table_content>
<!-- D-17 public table. Content source = RESEARCH.md Migration Table Skeleton (verbatim), reconciled -->
<!-- with the actual choices recorded in the Plan 01/02 SUMMARYs (neutral-badge treatment, ActionCard -->
<!-- neutral border, relationship-button ghost). Format = the existing pipe tables in components.md. -->

| Component | Old prop | Old value | New prop | New value |
|-----------|----------|-----------|----------|-----------|
| Button, ActionGroup item | `variant` | `default` | `variant` | `primary` |
| Button, ActionGroup item | `variant` | `link` | `variant` | `ghost` (link style removed) |
| Button, ActionGroup item | `variant` | `secondary`/`outline`/`ghost`/`destructive` | `variant` | unchanged |
| Button, Avatar, SegmentedControl | `size` | `xs` | `size` | `sm` |
| Button, Avatar, SegmentedControl | `size` | `default` | `size` | `md` |
| Alert, Toast | `variant` | `info` | `tone` | `neutral` |
| Alert, Toast | `variant` | `error` | `tone` | `destructive` |
| Alert, Toast | `variant` | `success`/`warning` | `tone` | unchanged |
| Badge | `variant` | `default`/`secondary`/`outline` | `tone` | `neutral` |
| Badge | `variant` | `warning`/`destructive` | `tone` | unchanged |
| Card | `variant` | `bordered`/`elevated` | `appearance` | unchanged values |
| ActionCard | `variant` | `default`/`setup`/`danger` | `tone` | `neutral`/`warning`/`destructive` (+ `success`) |
| DataTable badge column | row `{"variant": ..}` | BadgeVariant values | row `{"tone": ..}` | canonical tone values |
| MediaCardGrid | `badge_variant_key` | `outline`/`destructive`/`default` | `badge_tone_key` | canonical tone values |
| ConfirmDialog | `variant` | `default`/`danger` | `tone` | `neutral`/`destructive` |
| Notify outcome | `variant` | `info`/`error` (+`success`/`warning`) | `tone` | `neutral`/`destructive` (+unchanged) |

Also note the behavior/visual deltas (from the Plan 01/02 SUMMARYs) so consumers expect them:
Badge default was primary-tinted → now neutral; Alert `info` was primary-tinted → now neutral;
relationship (Link) buttons lose the underline-link look → ghost; ActionCard neutral border changed
from `border-l-primary`. One-line note: CalendarCell `dot_colors` still take raw Tailwind classes
(pre-existing; out of this phase, candidate for the Phase 252 lint).
</migration_table_content>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the docs enum section, add the migration table, and sweep old values across json-ui docs</name>
  <files>docs/src/json-ui/components.md, docs/src/json-ui/actions.md, docs/src/json-ui/forms.md</files>
  <read_first>
    - docs/src/json-ui/components.md ("Shared Enum Values" section :40-58; size doc :44; badge_variant :50 (omits `warning`); column_format :52 (omits `badge`/`image`/`icon`); GapSize `xs` :182; other hits :169,:504,:518,:955,:977-979,:1055,:1077,:1290,:1544)
    - docs/src/json-ui/actions.md (:21,:58,:65,:77,:135 action-level variant → tone)
    - docs/src/json-ui/forms.md (:138 `.prop("variant", "error")` builder example)
    - 251-PATTERNS.md §15 (docs analog + pre-existing drift to fix) and the <migration_table_content> block above
    - the three plan SUMMARYs (for the recorded neutral-badge treatment and documented deltas)
  </read_first>
  <action>
components.md "Shared Enum Values" (:40-58): collapse the multiple entries (`size`, `button_variant`,
`alert_variant`, `badge_variant`, …) to exactly three, matching the file's existing pipe/bold format:
- **variant** (weight of interactive elements) — `"primary"` | `"secondary"` | `"outline"` | `"ghost"` | `"destructive"`
- **tone** (status color of stateful display components) — `"neutral"` | `"success"` | `"warning"` | `"destructive"`
- **size** — `"sm"` | `"md"` | `"lg"`
State one line: "variant = weight, tone = status, size = sm/md/lg — one word, one meaning across all
components."

Add a new `## Component vocabulary migration` section (D-17) using the `<migration_table_content>`
table VERBATIM plus the behavior/visual-delta notes. Reference it from the phase summary.

Sweep the rest of the file for stale values: every `"xs"`/`"default"`(size), `button_variant`/
`badge_variant`/`alert_variant`/`toast_variant`/`card_variant` reference, Card `variant`→`appearance`,
`badge_variant_key`→`badge_tone_key`, and Alert/Toast/Badge value updates. Fix the pre-existing doc
drift in the same sweep: badge list :50 (add nothing — it's now `tone`), column_format :52 (add the
missing `badge`/`image`/`icon` formats), and the nonexistent GapSize `"xs"` at :182 (GapSize is
`none|sm|md|lg|xl` — correct it; note GapSize is prop `gap`, NOT the canonical `size`).

actions.md: `ConfirmDialog`/`Notify` `variant` → `tone` with values `neutral`/`destructive` (and
`success`/`warning` unchanged for Notify); update the :21/:58/:65/:77/:135 hits.

forms.md :138: `.prop("variant", "error")` — this is an Alert/status builder example; update to
`.prop("tone", "destructive")` (or the correct canonical tone for the example's intent).

Do NOT edit docs/src/features/json-ui.md (RESEARCH: clean). Verify the mdBook builds if a build step
exists; otherwise a grep sweep suffices.
  </action>
  <verify>
    <automated>grep -rn "button_variant\|badge_variant\|alert_variant\|toast_variant\|card_variant\|\"xs\"\|ring-primary" docs/src/json-ui | grep -v "badge_tone_key" | wc -l; grep -n "Component vocabulary migration" docs/src/json-ui/components.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Component vocabulary migration" docs/src/json-ui/components.md` returns a hit; the table contains rows for `variant default→primary`, `variant link→ghost`, `size xs→sm`, `Card variant→appearance`, `Badge variant→tone`, `badge_variant_key→badge_tone_key`.
    - `grep -rn "button_variant\|badge_variant\|alert_variant\|toast_variant\|card_variant" docs/src/json-ui` returns 0 hits (excluding the migration table's own old→new cells, which reference them as retired names).
    - `grep -rn "\"xs\"" docs/src/json-ui/components.md` returns 0 hits except inside the migration table.
    - The "Shared Enum Values" section lists exactly `variant`, `tone`, `size` with the canonical values.
    - `grep -n "\\.prop(\"tone\"" docs/src/json-ui/forms.md` returns a hit (the builder example migrated).
  </acceptance_criteria>
  <done>Public docs describe the canonical three-enum vocabulary and carry the full migration table; every stale value across components.md/actions.md/forms.md is updated; pre-existing doc drift fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate ferro-base.css and verify the safelist covers every emitted class</name>
  <files>ferro-json-ui/assets/input.css, ferro-json-ui/assets/ferro-base.css</files>
  <read_first>
    - ferro-json-ui/assets/input.css (@source scanner :4; @source inline safelist :66-73; @utility duration-* :80-88; reduced-motion collapse)
    - scripts/gen-ferro-base-css.sh (regen workflow — auto-installs pinned Tailwind CLI into .tooling/bin/)
    - 251-RESEARCH.md §Regen + Safelist Mechanics (which classes must appear as source literals; the match-arm rule) and 251-PATTERNS.md §16
    - 251-02-SUMMARY.md (the confirmed list of new/changed emitted classes to look for)
  </read_first>
  <action>
First verify the safelist: `focus-visible:ring-ring`, `duration-fast`, `duration-base`, `duration-slow`,
`ease-base` are covered (the bare tokens are already in `@source inline(...)` from Phase 250; the
`focus-visible:ring-ring` variant is generated from the render-source literals because the scanner reads
`@source "../../ferro-json-ui/src"`). Confirm NO task introduced dynamic `format!("...{}", x)` class
construction (that would be purged) — grep the render sources; every emitted class must be a complete
literal. If any newly-emitted tone/border class is NOT reachable as a source literal, add it to the
`@source inline(...)` line in input.css (expected: none, since tone classes come from existing token
families and match-arm literals).

Then regenerate ONCE (D-04 — after all class changes are in tree): `bash scripts/gen-ferro-base-css.sh`.
Smoke-check the output contains the new/changed classes.
  </action>
  <verify>
    <automated>bash scripts/gen-ferro-base-css.sh 2>&1 | tail -5; grep -c "ring-ring\|duration-fast\|duration-base\|border-l-success" ferro-json-ui/assets/ferro-base.css</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/gen-ferro-base-css.sh` completes without error.
    - `grep -c "ring-ring" ferro-json-ui/assets/ferro-base.css` ≥ 1 AND `grep -c "duration-fast" ferro-json-ui/assets/ferro-base.css` ≥ 1.
    - `grep -c "border-l-success" ferro-json-ui/assets/ferro-base.css` ≥ 1 (ActionCard success arm) OR a documented note that the class is covered by an existing safelist entry.
    - No dynamic class construction in render sources: `grep -rn "format!(\"[^\"]*bg-{" ferro-json-ui/src/render` returns 0 hits.
  </acceptance_criteria>
  <done>ferro-base.css regenerated after all class changes and contains every emitted interactive/tone class; safelist confirmed complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full CI-exact workspace gate + Chrome MCP before/after visual pass (light + dark)</name>
  <what-built>
    The complete canonical-vocabulary + interactive-state pass across ferro-json-ui, framework re-exports,
    ferro-mcp, docs, and the regenerated ferro-base.css. This task runs the full CI-exact gate and captures
    before/after screenshots of the sample app to confirm the intended visual deltas and that no interactive
    state renders unstyled or broken.
  </what-built>
  <read_first>
    - 251-RESEARCH.md §Common Pitfalls (Pitfall 5 default-value visual deltas; Pitfall 6 doc-churn hygiene; Pitfall 7 full-gate environment) and §Test Surface
    - the three plan SUMMARYs (the documented expected visual deltas to confirm)
    - app/src/views/login.json, app/src/views/login_confirm.json (the auth-layout pages to screenshot)
  </read_first>
  <action>
Run the full CI-exact gate, serialized (one cargo op at a time; check `df` headroom and clean `target/`
first — known ENOSPC risk):
1. `cargo fmt --all -- --check`
2. `cargo clippy --all --all-targets --all-features -- -D warnings`
3. `cargo test --all-features`

Schema-churn hygiene: after the full test run, `git status docs/protocol/schemas/`. NOTE (audit finding):
the export test (`ferro-projections/tests/generate_schemas.rs`) exports ferro-projections types only —
NOT the ferro-json-ui `Variant`/`Tone`/`Size` enums — so these schemas are NOT expected to change from
this phase. Any diff there is the known unrelated Phase-94 export churn → `git checkout docs/protocol/schemas/`
(do NOT fold into phase commits). If, contrary to this expectation, a diff genuinely reflects a canonical-
enum change, commit only those specific files.

Then drive Chrome MCP (per Phase 250 practice): serve/open the sample app auth pages, capture screenshots
in BOTH light and dark, and compare against the pre-phase look. Confirm the intended deltas are present
and correct (Badge/Alert neutral, ActionCard neutral border, relationship-button ghost, focus-visible
ring visible on tab, hover/disabled treatments) and that nothing renders unstyled, pops, or reflows during
a transition. If a dev server must be started, ASK before starting it (CLAUDE.md server rule).
  </action>
  <how-to-verify>
    1. Confirm `cargo fmt --all -- --check` exits 0, `cargo clippy --all --all-targets --all-features -- -D warnings` exits 0, `cargo test --all-features` exits 0 (incl. the new `variant_tone_size_enum_sets_drift_guard` and the 47-count mirror).
    2. Confirm `docs/protocol/schemas/` is clean (churn discarded) and the working tree contains only intended phase changes.
    3. Review the light + dark before/after screenshots of the sample app: focus-visible ring appears on keyboard focus; buttons/badges/alerts/actioncards show the canonical treatment; disabled controls are non-interactive; no unstyled toasts/tabs; no pop/reflow on transitions.
    4. Confirm the migration table renders correctly in the docs.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the visual/gate issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| render-source literals → generated CSS | The Tailwind scanner turns source-literal classes into `ferro-base.css`; a dynamically-constructed class is silently purged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-251-08 | Denial of Service (visual) | Emitted class purged from ferro-base.css (dynamic construction) → unstyled/invisible interactive state | mitigate | Task 2 enforces the match-arm full-literal rule (grep for `format!("...bg-{")` = 0) and safelist coverage; the visual pass (Task 3) confirms no unstyled interactive states. |
| T-251-09 | Tampering | Docs/migration table (public artifact) | accept | Documentation only; no runtime input surface. Reviewed for neutral, accurate old→new mapping. |
</threat_model>

<verification>
- Full CI-exact triple green: `cargo fmt --all -- --check`, `cargo clippy --all --all-targets --all-features -- -D warnings`, `cargo test --all-features`.
- `ferro-base.css` regenerated; contains `ring-ring`, `duration-fast`, `duration-base`, `border-l-success` (or documented safelist coverage).
- Docs carry the migration table and the collapsed three-enum section; zero stale old-value mentions outside the migration table.
- `docs/protocol/schemas/` clean (unrelated export churn discarded).
- Chrome MCP light + dark visual sign-off obtained; intended deltas confirmed, no unstyled/broken states.
- No version bump / publish (Phase 253 owns it).
</verification>

<success_criteria>
The phase ships its consumer-facing artifacts: a public migration table (D-17) and a canonical three-enum
docs section; `ferro-base.css` regenerated after all class changes (D-04); the full CI-exact workspace
gate green; and a light+dark visual confirmation that every interactive component renders at the quality
bar with the intended deltas. No publish this phase.
</success_criteria>

<output>
After completion, create `.planning/phases/251-component-variant-discipline-interactive-state-pass/251-04-SUMMARY.md`.
Record: the full-gate result, the schema-churn disposition (discarded per the audit finding), the
ferro-base.css smoke-grep counts, and a one-line pointer to the migration table for gestiscilo Phase 232.
</output>
