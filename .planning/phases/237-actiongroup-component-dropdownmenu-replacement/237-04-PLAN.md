---
phase: 237-actiongroup-component-dropdownmenu-replacement
plan: 04
type: execute
wave: 4
depends_on: ["237-03"]
files_modified:
  - ferro-json-ui/assets/ferro-base.css
  - Cargo.toml
autonomous: false
requirements: [SC-6]
must_haves:
  truths:
    - "ferro-base.css is regenerated after the component code lands"
    - "The workspace version is bumped 0.2.72 → 0.2.73"
    - "The full workspace gate (fmt + clippy -D warnings + test --all-features) is green before the publish step"
    - "ferro-json-ui (+ ferro-rs re-export) is published to crates.io via the operator-gated step"
  artifacts:
    - path: "Cargo.toml"
      provides: "workspace version 0.2.73"
      contains: "version = \"0.2.73\""
  key_links:
    - from: "Cargo.toml workspace version"
      to: "all ferro-* crates"
      via: "version.workspace = true inheritance"
      pattern: "version = \"0.2.73\""
---

<objective>
Close out the phase: regenerate `ferro-base.css` after the ActionGroup component code has
landed (per the json-ui add/remove checklist), bump the workspace version `0.2.72 → 0.2.73`
(RESEARCH drift correction — the workspace is ALREADY at 0.2.72; SC-6's "next unpublished
version" is 0.2.73, not the stale "0.2.72" in CONTEXT/ROADMAP), verify the full workspace gate
is green, then flag the operator-gated crates.io publish.

Per project convention (cross-repo phase-split + operator-gated publish), this plan does NOT
run an automated `cargo publish`. ferro-json-ui is in Wave 1A of `.github/workflows/publish.yml`;
the publish is triggered by the operator after the version-bump commit + tag land. The plan ends
with a checkpoint presenting the exact commands.

Purpose: SC-6 (CSS regen + version bump 0.2.73 + operator-gated publish of ferro-json-ui + ferro-rs).
Output: regenerated CSS, bumped version, green gate, operator publish checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/237-actiongroup-component-dropdownmenu-replacement/237-RESEARCH.md
@.planning/phases/237-actiongroup-component-dropdownmenu-replacement/237-03-SUMMARY.md

<interfaces>
<!-- Verified against current source (0.2.72). -->

Cargo.toml workspace version: line 46 — `version = "0.2.72"` → `version = "0.2.73"`.
  All crates inherit via `version.workspace = true`; no per-crate Cargo.toml edits.

CSS regen: `scripts/gen-ferro-base-css.sh` runs Tailwind v4 CLI against
  `ferro-json-ui/assets/input.css` → `ferro-json-ui/assets/ferro-base.css`. Run AFTER the
  component code lands (checklist requirement). ActionGroup reuses existing classes
  (Button + kebab) so output may be unchanged — commit regardless (the step is mandatory).
  The script self-installs Tailwind via scripts/install-tailwind.sh on first run.

Publish: ferro-json-ui is in Wave 1A of .github/workflows/publish.yml (line ~211, alongside the
  other leaf crates incl. ferro-orm/ferro-migration/ferro-assets). ferro-rs (the facade) re-exports
  ferro-json-ui and publishes in a later wave. Per MEMORY.md: publish is operator-gated; the CI
  Publish workflow triggers on a pushed version-bump + tag. Verify the live version on crates.io
  after the operator triggers it — local origin/master recurrently lies; do NOT trust stale refs.

Full gate (CLAUDE.md, matches CI exactly):
  cargo fmt --all -- --check
  cargo clippy --all --all-targets -- -D warnings
  cargo test --all-features
  (Also CI Docs: cargo doc --no-deps -Dwarnings — run before the publish push.)
  Disk note (MEMORY.md): `cargo test --all-features` can ENOSPC; check `df` / clean target/ if so.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate ferro-base.css after component code lands</name>
  <read_first>
    - scripts/gen-ferro-base-css.sh (regen entrypoint)
    - .planning/phases/237-actiongroup-component-dropdownmenu-replacement/237-03-SUMMARY.md (confirm component code is in tree)
  </read_first>
  <files>ferro-json-ui/assets/ferro-base.css</files>
  <action>
    Run `bash scripts/gen-ferro-base-css.sh` (it self-installs Tailwind v4 CLI on first run via
    scripts/install-tailwind.sh). This must run AFTER the ActionGroup render code landed (plans 01-03)
    so any new utility classes are picked up. ActionGroup is expected to reuse existing Button + kebab
    classes, so ferro-base.css may be byte-identical — commit the result either way (the regen is a
    mandatory checklist step, not conditional on a diff). If the script errors on Tailwind install,
    report the failure; do not hand-edit ferro-base.css.
  </action>
  <verify>
    <automated>bash scripts/gen-ferro-base-css.sh && test -f ferro-json-ui/assets/ferro-base.css</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/gen-ferro-base-css.sh` exits 0
    - `ferro-json-ui/assets/ferro-base.css` exists and is non-empty (`test -s`)
    - No manual edits to ferro-base.css (regenerated only)
  </acceptance_criteria>
  <done>ferro-base.css regenerated cleanly via the script after the component code landed.</done>
</task>

<task type="auto">
  <name>Task 2: Bump workspace version 0.2.72 → 0.2.73 and run the full gate</name>
  <read_first>
    - Cargo.toml:46 (workspace version line)
  </read_first>
  <files>Cargo.toml</files>
  <action>
    Edit `Cargo.toml` line 46: `version = "0.2.72"` → `version = "0.2.73"` (workspace-level only;
    crates inherit via `version.workspace = true`). This is the RESEARCH-corrected target — the
    workspace is already at 0.2.72 (shipped 2026-06-21 with ferro-payments 0.1.2); the next
    unpublished version is 0.2.73, NOT the "0.2.72" the CONTEXT/ROADMAP states.
    Then run the full workspace gate exactly as CI does:
      cargo fmt --all -- --check
      cargo clippy --all --all-targets -- -D warnings
      cargo test --all-features
      cargo doc --no-deps   (CI Docs gate)
    If `cargo test --all-features` ENOSPC-fails (known recurring issue), check `df`, clean `target/`,
    and re-run — it is an environment artifact, not a code defect. Per MEMORY.md, run these CPU-heavy
    steps one at a time, not chained/parallelized. The schema-export test may dirty
    `docs/protocol/schemas/*.json` — `git checkout` those if unrelated to this phase.
  </action>
  <verify>
    <automated>grep -n 'version = "0.2.73"' Cargo.toml && cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'version = "0.2.73"' Cargo.toml` matches at line 46
    - `grep -n 'version = "0.2.72"' Cargo.toml` returns NOTHING at the workspace version line
    - `cargo fmt --all -- --check` clean
    - `cargo clippy --all --all-targets -- -D warnings` exits 0
    - `cargo test --all-features` green (full suite)
  </acceptance_criteria>
  <done>Workspace version is 0.2.73; full CI-matching gate (fmt + clippy + test + doc) is green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator-gated crates.io publish of ferro-json-ui + ferro-rs (0.2.73)</name>
  <files>(none — operator action; no file edits by Claude)</files>
  <action>
    CHECKPOINT — operator-gated publish. Claude does NOT run `cargo publish` and does NOT push.
    Present the steps in &lt;how-to-verify&gt; to the operator and PAUSE until the operator confirms
    the live crates.io versions. The publish is triggered by the operator pushing the version-bump
    commit + release tag, which fires CI `publish.yml` Wave 1A (ferro-json-ui) then the facade wave
    (ferro-rs). This is non-automatable from a plan per project convention.
  </action>
  <what-built>
    ActionGroup component shipped and DropdownMenu removed from the public surface; ferro-base.css
    regenerated; workspace bumped to 0.2.73; full CI-matching gate green. The code is ready to publish.
  </what-built>
  <how-to-verify>
    This is an operator-gated publish (CI `publish.yml` Wave 1A) — NOT automatable from a plan.
    The operator performs:

    1. Confirm the gate is green locally (fmt + clippy -D warnings + test --all-features + doc).
    2. Commit the phase: version bump + ferro-base.css + all phase code/doc changes.
       (Use the project commit gate — NO co-author lines.)
    3. Push master, then tag and push the release tag, e.g.:
         git tag v0.2.73
         git push origin v0.2.73
       (Push via the gh HTTPS credential helper — SSH is denied in this repo;
        `git -c credential.helper='!gh auth git-credential' push https://github.com/albertogferrario/ferro.git master`.)
    4. The CI Publish workflow (publish.yml) publishes Wave 1A (incl. ferro-json-ui) then later waves
       (incl. the ferro-rs facade re-exporting ferro-json-ui).
    5. VERIFY the live versions on crates.io (do NOT trust local origin/master — it recurrently lies):
         curl -s https://crates.io/api/v1/crates/ferro-json-ui | jq -r '.crate.max_version'   # expect 0.2.73
         curl -s https://crates.io/api/v1/crates/ferro-rs       | jq -r '.crate.max_version'   # expect 0.2.73
       After a verified push, repair the local ref:
         git update-ref refs/remotes/origin/master HEAD

    Do NOT plan or run an automated `cargo publish` — the operator triggers CI.
  </how-to-verify>
  <verify>Operator confirms the crates.io API shows ferro-json-ui 0.2.73 and ferro-rs 0.2.73.</verify>
  <done>ferro-json-ui 0.2.73 + ferro-rs 0.2.73 live on crates.io, verified via the crates.io API (not local refs); local origin/master ref repaired.</done>
  <resume-signal>Type "published" once crates.io shows ferro-json-ui 0.2.73 (+ ferro-rs 0.2.73), or describe any publish failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local tree → crates.io | Publishing immutably releases the crate to the public registry; an unvetted publish cannot be unpublished. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-237-09 | T (premature/broken publish) | crates.io publish | mitigate | Publish is gated behind a green full CI-matching gate (fmt + clippy -D warnings + test --all-features + doc) AND a human operator checkpoint. No automated `cargo publish` in the plan. |
| T-237-10 | R (version repudiation / wrong version) | workspace version bump | mitigate | Version target corrected to 0.2.73 (RESEARCH verified the tree is already at 0.2.72); acceptance grep asserts 0.2.73 present and 0.2.72 absent. Live crates.io version verified post-publish, not from stale local refs. |
| T-237-11 | I (stale-ref masking) | origin/master ref drift | accept→mitigate | Known recurring local-ref lie (MEMORY.md); mitigation is the post-publish crates.io API check + `git update-ref` repair documented in the checkpoint. |
</threat_model>

<verification>
- `grep -n 'version = "0.2.73"' Cargo.toml` matches.
- Full gate green (fmt + clippy -D warnings + test --all-features + doc).
- ferro-base.css regenerated and committed.
- Post-publish: crates.io API shows ferro-json-ui 0.2.73 and ferro-rs 0.2.73.
</verification>

<success_criteria>
- SC-6: ferro-base.css regenerated; workspace at 0.2.73; full gate green; ferro-json-ui +
  ferro-rs published to crates.io via the operator-gated step (verified live, not from local refs).
</success_criteria>

<output>
After completion, create `.planning/phases/237-actiongroup-component-dropdownmenu-replacement/237-04-SUMMARY.md`
</output>
