---
phase: 237-actiongroup-component-dropdownmenu-replacement
plan: 02
type: execute
wave: 2
depends_on: ["237-01"]
files_modified:
  - ferro-json-ui/src/render/mod.rs
  - ferro-json-ui/src/lib.rs
  - ferro-json-ui/src/catalog.rs
  - ferro-json-ui/src/component.rs
  - ferro-mcp/src/tools/json_ui_catalog.rs
autonomous: true
requirements: [SC-4]
must_haves:
  truths:
    - "DropdownMenu no longer appears in BUILTIN_TYPES, BUILTIN_SPECS, the public lib.rs export, or the catalog"
    - "ActionGroup is dispatchable and present in the catalog"
    - "Both drift guards pass with count staying at 47 (one-for-one swap)"
    - "The json-ui schema export lists ActionGroup and omits DropdownMenu"
    - "ferro-mcp expected[] name list contains ActionGroup (not DropdownMenu) and the full 47 names incl. SegmentedControl + SidebarLayout"
  artifacts:
    - path: "ferro-json-ui/src/render/mod.rs"
      provides: "ActionGroup in BUILTIN_TYPES + dispatch arm; DropdownMenu removed"
      contains: "\"ActionGroup\" => containers::render_action_group"
    - path: "ferro-json-ui/src/catalog.rs"
      provides: "ActionGroup BUILTIN_SPECS entry; DropdownMenu removed; count guard at 47"
      contains: "\"ActionGroup\""
  key_links:
    - from: "ferro-json-ui/src/render/mod.rs dispatch"
      to: "ferro-json-ui/src/render/containers.rs render_action_group"
      via: "match arm"
      pattern: "ActionGroup.*render_action_group"
    - from: "ferro-mcp/src/tools/json_ui_catalog.rs expected[]"
      to: "ferro-json-ui BUILTIN_TYPES"
      via: "mirrored 47-name list"
      pattern: "ActionGroup"
---

<objective>
Atomic one-for-one registration swap: ADD `ActionGroup` and REMOVE the public `DropdownMenu`
across the full 8-touchpoint registration surface in lockstep, plus the ferro-mcp name-list
mirror. The builtin count stays exactly 47 (D-14). `DropdownMenuAction` (the row-action
carrier) and the internal `render_dropdown_menu`/`render_menu_item` helpers are RETAINED
(D-10/D-11) — only `DropdownMenuProps` (the public component props) is deleted.

CRITICAL lockstep invariant: the runtime drift guard (catalog.rs:576) PANICS if
`BUILTIN_SPECS.len() != BUILTIN_TYPES.len()`, and `builtin_types_count_drift_guard` asserts
exactly 47. Adding ActionGroup without removing DropdownMenu → 48 → guard fails. So the
add and remove MUST land together in this single plan; the tree must compile and all drift
guards must pass at the plan's commit boundary.

Purpose: SC-4 (DropdownMenu absent from all public registries; ActionGroup present;
both drift guards pass at 47; schema export updated; ferro-mcp mirror corrected).
Output: registered, dispatchable ActionGroup; DropdownMenu removed from the public surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/237-actiongroup-component-dropdownmenu-replacement/237-RESEARCH.md
@.planning/phases/237-actiongroup-component-dropdownmenu-replacement/237-PATTERNS.md
@.planning/phases/237-actiongroup-component-dropdownmenu-replacement/237-01-SUMMARY.md

<interfaces>
<!-- Exact swap sites verified against current source (0.2.72). -->

render/mod.rs BUILTIN_TYPES: REMOVE "DropdownMenu" (~:64, atoms section); ADD "ActionGroup"
  in the containers section after "SidebarLayout" (~:82). Order MUST match BUILTIN_SPECS.

render/mod.rs dispatch (~:176-230): REMOVE
  `"DropdownMenu" => atoms::render_dropdown_menu(el, spec, data, depth),` (~:197)
  ADD in the Containers block (after "SidebarLayout" ~:215):
  `"ActionGroup" => containers::render_action_group(el, spec, data, depth),`

lib.rs export block (~:49-65): REMOVE `DropdownMenuProps` from the `pub use component::{…}`
  list; ADD `ActionGroupProps, ActionItem` (alphabetically before `ActionCardProps`).
  KEEP `DropdownMenuAction` (D-11 — still used by DataTable/Kanban/MediaCardGrid row_actions).

catalog.rs import block (~:29-38): REMOVE `DropdownMenuProps`; ADD `ActionGroupProps, ActionItem`.
  (Pitfall 5: an orphaned DropdownMenuProps import → unused-import → -D warnings build break.)

catalog.rs BUILTIN_SPECS (~:240-245): REMOVE the DropdownMenu tuple; ADD at the same ordinal
  position as ActionGroup in BUILTIN_TYPES:
```rust
(
    "ActionGroup",
    "Ordered action list: inline buttons up to max_inline, trailing overflow kebab for the rest; destructive items forced into the kebab last.",
    || to_value(schema_for!(ActionGroupProps)).unwrap(),
    &[],
),
```

catalog.rs builtin_types_count_drift_guard (~:1093): assertion STAYS `assert_eq!(..., 47)`.
  APPEND to the history comment: `→ 47 (DropdownMenu replaced by ActionGroup)`.

catalog.rs DataTable description (~:404): "per-row DropdownMenu" → "per-row action menu".

component.rs: DELETE `DropdownMenuProps` struct (~:1074-1082). KEEP `DropdownMenuAction`
  (~:1059-1072). REPLACE the `schema_for_dropdown_menu_props_generates` test (~:1513) — but the
  ActionGroupProps schema test already lives there from plan 01, so just DELETE the
  `schema_for_dropdown_menu_props_generates` test (its subject is gone). KEEP
  `schema_for_dropdown_menu_action_generates` (struct retained).

ferro-mcp/src/tools/json_ui_catalog.rs `test_all_components_present` expected[] (~:300-346):
  Currently 45 entries — has "DropdownMenu", MISSING "SegmentedControl" + "SidebarLayout"
  (pre-existing bug). FIX: remove "DropdownMenu"; add "ActionGroup", "SegmentedControl",
  "SidebarLayout" → 47 entries. Count assertion at ~:292 stays 47.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap registration surface in ferro-json-ui (BUILTIN_TYPES, dispatch, lib.rs, catalog, delete DropdownMenuProps)</name>
  <read_first>
    - ferro-json-ui/src/render/mod.rs:43-82 (BUILTIN_TYPES array), :176-230 (dispatch match)
    - ferro-json-ui/src/lib.rs:49-65 (export block)
    - ferro-json-ui/src/catalog.rs:29-38 (imports), :124+ and :240-245 (BUILTIN_SPECS DropdownMenu tuple), :404 (DataTable desc), :576 (runtime guard), :1093-1101 (count drift guard)
    - ferro-json-ui/src/component.rs:1074-1082 (DropdownMenuProps to delete), :1059-1072 (DropdownMenuAction to KEEP), :1513 (schema_for_dropdown_menu_props_generates test to delete)
  </read_first>
  <files>ferro-json-ui/src/render/mod.rs, ferro-json-ui/src/lib.rs, ferro-json-ui/src/catalog.rs, ferro-json-ui/src/component.rs</files>
  <action>
    Apply all swap sites from the &lt;interfaces&gt; block IN ONE COMMIT (lockstep — never leave
    BUILTIN_TYPES and BUILTIN_SPECS divergent):
    1. render/mod.rs: remove `"DropdownMenu",` from BUILTIN_TYPES; add `"ActionGroup",` in the
       containers section after "SidebarLayout". Remove the DropdownMenu dispatch arm; add the
       ActionGroup dispatch arm `"ActionGroup" => containers::render_action_group(el, spec, data, depth),`.
    2. lib.rs: remove `DropdownMenuProps` from the export; add `ActionGroupProps, ActionItem`
       (alphabetical, before ActionCardProps). KEEP `DropdownMenuAction`.
    3. catalog.rs: swap the import (`DropdownMenuProps` → `ActionGroupProps, ActionItem`); replace
       the DropdownMenu BUILTIN_SPECS tuple with the ActionGroup tuple (verbatim from interfaces) at
       the ordinal matching ActionGroup's BUILTIN_TYPES position; append the drift-guard history
       comment `→ 47 (DropdownMenu replaced by ActionGroup)` (keep assertion 47); update the
       DataTable description ":404" "per-row DropdownMenu" → "per-row action menu".
    4. component.rs: DELETE the `DropdownMenuProps` struct (D-09/D-11 — keep `DropdownMenuAction`).
       DELETE the now-orphaned `schema_for_dropdown_menu_props_generates` test (its subject is gone;
       the ActionGroupProps schema test from plan 01 covers the replacement). KEEP
       `schema_for_dropdown_menu_action_generates`.
    Do NOT touch atoms.rs `render_dropdown_menu`/`render_menu_item` (kept per D-10 — dead-code removal
    of render_dropdown_menu is plan 03's job, sequenced after the dispatch arm is gone). Do NOT touch
    projection/builder.rs (plan 03).
  </action>
  <verify>
    <automated>cargo test -p ferro-json-ui builtin_types_count_drift_guard builtin_specs_len_matches_dispatch build_populates_all_builtins</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "\"DropdownMenu\"" ferro-json-ui/src/render/mod.rs ferro-json-ui/src/catalog.rs` returns NOTHING
    - `grep -n "DropdownMenuProps" ferro-json-ui/src/lib.rs ferro-json-ui/src/catalog.rs ferro-json-ui/src/component.rs` returns NOTHING
    - `grep -n "DropdownMenuAction" ferro-json-ui/src/lib.rs` STILL matches (retained)
    - `grep -n "\"ActionGroup\" => containers::render_action_group" ferro-json-ui/src/render/mod.rs` matches
    - `grep -n "assert_eq!(crate::render::BUILTIN_TYPES.len(), 47)" ferro-json-ui/src/catalog.rs` matches (count unchanged)
    - `cargo test -p ferro-json-ui builtin_types_count_drift_guard builtin_specs_len_matches_dispatch` passes
    - `cargo build -p ferro-json-ui` succeeds (no unused-import from orphaned DropdownMenuProps)
  </acceptance_criteria>
  <done>ActionGroup registered and dispatchable; DropdownMenu gone from all public registries; DropdownMenuProps struct deleted; DropdownMenuAction + internal kebab helpers retained; both drift guards pass at 47; crate compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ferro-mcp expected[] name-list mirror (swap + 45→47 gap)</name>
  <read_first>
    - ferro-mcp/src/tools/json_ui_catalog.rs:285-349 (test_all_components_present — count assertion ~:292, expected[] array ~:300-346)
  </read_first>
  <files>ferro-mcp/src/tools/json_ui_catalog.rs</files>
  <action>
    In `test_all_components_present`, edit the `expected[]` array: REMOVE `"DropdownMenu"`; ADD
    `"ActionGroup"`, `"SegmentedControl"`, `"SidebarLayout"`. This both performs the DropdownMenu→
    ActionGroup swap AND fixes the pre-existing 45-vs-47 gap (SegmentedControl/SidebarLayout were added
    in 0.2.69 but never added to this mirror). The array must end at exactly 47 entries. Leave the count
    assertion (~:292) at 47. Optionally update its inline comment to mention ActionGroup.
    This is the documented cross-crate mirror (BUILTIN_TYPES is pub(crate), so ferro-mcp cannot
    reference it directly — STATE.md notes ferro-mcp stays a documented mirror).
  </action>
  <verify>
    <automated>cargo test -p ferro-mcp test_all_components_present</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\"DropdownMenu\"" ferro-mcp/src/tools/json_ui_catalog.rs` returns NOTHING
    - `grep -c "\"ActionGroup\"\|\"SegmentedControl\"\|\"SidebarLayout\"" ferro-mcp/src/tools/json_ui_catalog.rs` shows all three present
    - `cargo test -p ferro-mcp test_all_components_present` passes (loops over all 47 names successfully)
  </acceptance_criteria>
  <done>ferro-mcp expected[] mirror contains all 47 names including ActionGroup, SegmentedControl, SidebarLayout; DropdownMenu removed; test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer spec → public component surface | After this swap, no consumer can author a `DropdownMenu` component spec (removed from BUILTIN_TYPES/dispatch/catalog). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-237-04 | D (Denial of service / startup crash) | BUILTIN_TYPES ↔ BUILTIN_SPECS lockstep | mitigate | Both arrays edited in one commit; runtime drift guard (catalog.rs:576) + `builtin_specs_len_matches_dispatch` + `builtin_types_count_drift_guard` (count 47) all asserted before the task is considered done. A divergence panics `Catalog::build()` at startup — the gate test fails first. |
| T-237-05 | T (registry/mirror drift) | ferro-mcp expected[] name list | accept→mitigate | Pre-existing 45-vs-47 gap silently under-tested the catalog; this plan closes it to a full 47-name loop. No new threat — corrects an existing blind spot. Pinned by `test_all_components_present`. |
</threat_model>

<verification>
- `cargo test -p ferro-json-ui` green (drift guards, catalog build, schema export).
- `cargo test -p ferro-mcp test_all_components_present` green.
- `cargo clippy --all --all-targets -- -D warnings` exits 0 (no orphaned imports).
- `cargo fmt --all -- --check` clean.
</verification>

<success_criteria>
- SC-4: DropdownMenu absent from BUILTIN_TYPES/BUILTIN_SPECS/lib.rs export/catalog; ActionGroup
  present and dispatchable; both drift guards pass at 47; schema export lists ActionGroup, omits
  DropdownMenu; ferro-mcp mirror corrected to 47 names.
</success_criteria>

<output>
After completion, create `.planning/phases/237-actiongroup-component-dropdownmenu-replacement/237-02-SUMMARY.md`
</output>
