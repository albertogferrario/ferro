---
phase: 237-actiongroup-component-dropdownmenu-replacement
plan: 03
type: execute
wave: 3
depends_on: ["237-02"]
files_modified:
  - ferro-json-ui/src/projection/builder.rs
  - ferro-json-ui/src/render/atoms.rs
  - docs/src/json-ui/components.md
  - docs/src/features/projections.md
  - docs/src/json-ui/expressions.md
autonomous: true
requirements: [SC-5]
must_haves:
  truths:
    - "Projection codegen emit_actions_placeholder emits an ActionGroup element, not DropdownMenu"
    - "ferro-internal/example/test specs and json-ui docs no longer reference DropdownMenu"
    - "The now-dead render_dropdown_menu helper and its tests are deleted (no deprecated code, -D warnings clean)"
  artifacts:
    - path: "ferro-json-ui/src/projection/builder.rs"
      provides: "emit_actions_placeholder emitting ActionGroup + updated test"
      contains: "element_with_props(\"ActionGroup\""
    - path: "docs/src/json-ui/components.md"
      provides: "ActionGroup component docs replacing DropdownMenu section"
      contains: "ActionGroup"
  key_links:
    - from: "ferro-json-ui/src/projection/builder.rs emit_actions_placeholder"
      to: "ActionGroup component"
      via: "element_with_props"
      pattern: "ActionGroup"
---

<objective>
Migrate the last DropdownMenu consumers inside ferro: the projection codegen
(`emit_actions_placeholder` → emits `ActionGroup`, D-12), the json-ui docs (D-13), and delete
the now-dead `render_dropdown_menu` helper + its atoms.rs tests. After plan 02 removed the
DropdownMenu dispatch arm, `render_dropdown_menu` has no remaining call site (DataTable/Kanban
use `render_inline_dropdown`, not `render_dropdown_menu`) — per "delete old code completely" +
CI `-D warnings`, it and its tests are removed here.

This REFINES CONTEXT D-10: the reused building blocks are `render_menu_item` + the kebab
trigger/panel HTML (now embedded in `render_action_group` from plan 01); `render_dropdown_menu`
itself is NOT reused and is deleted. `render_menu_item` and `DropdownMenuAction` are KEPT.

Purpose: SC-5 (projection emits ActionGroup; no remaining ferro-internal/doc DropdownMenu refs).
Output: migrated codegen + tests, migrated docs, dead-code removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/237-actiongroup-component-dropdownmenu-replacement/237-RESEARCH.md
@.planning/phases/237-actiongroup-component-dropdownmenu-replacement/237-PATTERNS.md
@.planning/phases/237-actiongroup-component-dropdownmenu-replacement/237-02-SUMMARY.md

<interfaces>
<!-- Migration sites verified against current source (0.2.72). -->

projection/builder.rs import (~:25-31): SWAP `DropdownMenuProps` → `ActionGroupProps`.
  KEEP `DropdownMenuAction` (still used by DataTable emitters ~:300, :463).

projection/builder.rs emit_actions_placeholder (~:667-699) — migrate body:
```rust
let items: Vec<ActionItem> = service.actions.iter().map(|a| ActionItem {
    label: a.display_name.as_deref().unwrap_or(&a.name).to_string(),
    action: Action::new(format!("/{}/{}", service.name, a.name)),
    destructive: false,
    variant: None,
    icon: None,
    visible_if: None,
}).collect();
let props = serde_json::to_value(ActionGroupProps {
    items,
    menu_id: format!("actions_{}", service.name),
    max_inline: None,
    overflow_label: None,
    row_key: None,
}).expect("ActionGroupProps serialization cannot fail");
let id = "actions_menu".to_string();
aux.push((id.clone(), element_with_props("ActionGroup", props)));
children_out.push(id);
```
Update the fn doc comment to reference ActionGroup.

projection/builder.rs test (~:1220-1237): rename `actions_slot_emits_dropdown_from_service_actions`
  → `actions_slot_emits_action_group_from_service_actions`; swap `use ...DropdownMenuProps` →
  `ActionGroupProps`; decode as `ActionGroupProps`; update the `.expect("DropdownMenu must be emitted")`
  message. The `props.items.len()` + `props.items[0].label == "View"` assertions stay identical.

render/atoms.rs: DELETE `render_dropdown_menu` (~:1154-1195) AND its two tests
  `dropdown_menu_emits_actions` + `dropdown_menu_get_action_renders_anchor` (~:2018-2064).
  After deletion, fix the atoms.rs import (~:15): `render_dropdown_menu` used `DropdownMenuProps`;
  if no remaining atoms.rs code references `DropdownMenuProps`, remove it from the import (keep
  `DropdownMenuAction` — still used by `render_menu_item`). Verify with -D warnings.
  KEEP `render_menu_item` (still called by render_action_group / DataTable / Kanban).

docs/src/json-ui/components.md:29 — Forms category row: `DropdownMenu` → `ActionGroup`.
docs/src/json-ui/components.md:985-1014 — REPLACE the `### DropdownMenu` section with an
  `### ActionGroup` section documenting D-05/D-06 props: items, menu_id (required), max_inline
  (default 2), overflow_label (default "Azioni"), row_key; each item: label, action, destructive
  (default false), variant, icon, visible_if. Include a JSON example showing inline + overflow.
docs/src/features/projections.md:504 — `DropdownMenu item` → `ActionGroup item`.
docs/src/json-ui/expressions.md:156 — `DropdownMenu` incidental mention → `ActionGroup` (or generic).

NOTE: docs/book/* are GENERATED mdbook output — do NOT hand-edit them (regenerated from docs/src).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate emit_actions_placeholder to ActionGroup + delete dead render_dropdown_menu</name>
  <read_first>
    - ferro-json-ui/src/projection/builder.rs:25-31 (imports), :667-699 (emit_actions_placeholder), :1220-1237 (its test)
    - ferro-json-ui/src/render/atoms.rs:15 (import line), :1154-1195 (render_dropdown_menu to delete), :2018-2064 (its two tests to delete)
  </read_first>
  <files>ferro-json-ui/src/projection/builder.rs, ferro-json-ui/src/render/atoms.rs</files>
  <behavior>
    - actions_slot_emits_action_group_from_service_actions: emit_actions_placeholder pushes an element decodable as ActionGroupProps; props.items.len() == service.actions.len(); props.items[0].label == "View"
    - the emitted element type_name is "ActionGroup" (not "DropdownMenu")
  </behavior>
  <action>
    1. builder.rs: swap the import (`DropdownMenuProps` → `ActionGroupProps`; keep `DropdownMenuAction`).
       Migrate `emit_actions_placeholder` body to build `Vec<ActionItem>` + `ActionGroupProps` and
       `element_with_props("ActionGroup", props)` (verbatim from the &lt;interfaces&gt; block). Update
       the fn doc comment to say ActionGroup. Update the unit test: rename to
       `actions_slot_emits_action_group_from_service_actions`, decode as `ActionGroupProps`, fix the
       expect message; keep the len + label assertions.
    2. atoms.rs: DELETE `render_dropdown_menu` (now dead — its only call site, the dispatch arm, was
       removed in plan 02) and its two tests `dropdown_menu_emits_actions` +
       `dropdown_menu_get_action_renders_anchor`. Remove `DropdownMenuProps` from the atoms.rs import
       if nothing else in atoms.rs references it (keep `DropdownMenuAction` — render_menu_item uses it).
       KEEP `render_menu_item`.
    Run clippy `-D warnings` to confirm no dead-code / unused-import remains.
  </action>
  <verify>
    <automated>cargo test -p ferro-json-ui actions_slot_emits_action_group && cargo clippy -p ferro-json-ui --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "element_with_props(\"ActionGroup\"" ferro-json-ui/src/projection/builder.rs` matches
    - `grep -rn "DropdownMenu\b" ferro-json-ui/src/projection/builder.rs` returns NOTHING except possibly `DropdownMenuAction` in retained DataTable emitters
    - `grep -n "fn render_dropdown_menu" ferro-json-ui/src/render/atoms.rs` returns NOTHING
    - `grep -n "fn dropdown_menu_emits_actions\|fn dropdown_menu_get_action_renders_anchor" ferro-json-ui/src/render/atoms.rs` returns NOTHING
    - `grep -n "fn render_menu_item" ferro-json-ui/src/render/atoms.rs` STILL matches (retained)
    - `cargo test -p ferro-json-ui actions_slot_emits_action_group` passes
    - `cargo clippy -p ferro-json-ui --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>Projection codegen emits ActionGroup; its test green; render_dropdown_menu + its 2 tests deleted; render_menu_item + DropdownMenuAction retained; crate clippy-clean under -D warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate json-ui docs from DropdownMenu to ActionGroup</name>
  <read_first>
    - docs/src/json-ui/components.md:29 (Forms category table), :985-1014 (DropdownMenu section)
    - docs/src/features/projections.md:504 (action routes table)
    - docs/src/json-ui/expressions.md:156 (incidental mention)
  </read_first>
  <files>docs/src/json-ui/components.md, docs/src/features/projections.md, docs/src/json-ui/expressions.md</files>
  <action>
    1. components.md:29 — change `DropdownMenu` → `ActionGroup` in the Forms category list.
    2. components.md:985-1014 — replace the entire `### DropdownMenu` section with an `### ActionGroup`
       section. Document the props (D-05/D-06): `items` (array or {"$data":"/path"} binding), `menu_id`
       (required), `max_inline` (default 2), `overflow_label` (default "Azioni"), `row_key`; per-item
       fields `label`, `action`, `destructive` (default false), `variant`, `icon`, `visible_if`
       (fail-closed row gate). Include a JSON example showing 2 inline buttons + a kebab holding an
       overflow + a destructive action. Voice: neutral architectural documentation (no marketing).
    3. projections.md:504 — `DropdownMenu item` → `ActionGroup item`.
    4. expressions.md:156 — `DropdownMenu` → `ActionGroup` (or make the sentence generic).
    Do NOT edit docs/book/* (generated mdbook output).
  </action>
  <verify>
    <automated>! grep -rn "DropdownMenu" docs/src/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "DropdownMenu" docs/src/` returns NOTHING
    - `grep -n "### ActionGroup" docs/src/json-ui/components.md` matches
    - `grep -n "overflow_label\|max_inline\|menu_id" docs/src/json-ui/components.md` matches (props documented)
    - `grep -n "ActionGroup item" docs/src/features/projections.md` matches
  </acceptance_criteria>
  <done>No DropdownMenu references remain in docs/src/; ActionGroup is documented with all D-05/D-06 props + a JSON example; generated docs/book untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| projection codegen → rendered view | emit_actions_placeholder generates the action element for projection-rendered Process/Summarize views; switching to ActionGroup routes through the plan-01 escaping path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-237-06 | T (injection via codegen) | emit_actions_placeholder | mitigate | Codegen builds typed `ActionItem`/`ActionGroupProps` (no raw HTML); rendering routes through render_action_group's html_escape path (T-237-02). Parity with the prior DropdownMenu codegen — no new surface. |
| T-237-07 | I (stale-doc leakage) | docs/src DropdownMenu references | accept | Doc-only; `grep -rn DropdownMenu docs/src/` == empty is the gate. No runtime impact. |
| T-237-08 | D (dead-code rot) | render_dropdown_menu removal | mitigate | Deleting the unreachable helper + tests keeps the kebab logic single-sourced in render_action_group; `-D warnings` confirms no orphaned references. |
</threat_model>

<verification>
- `cargo test -p ferro-json-ui` green (projection builder test + full crate).
- `cargo clippy --all --all-targets -- -D warnings` exits 0.
- `grep -rn "DropdownMenu" docs/src/` empty.
- `grep -rn "DropdownMenuProps" ferro-json-ui/src/` empty (public props fully gone);
  `DropdownMenuAction` still present where retained.
</verification>

<success_criteria>
- SC-5: emit_actions_placeholder emits ActionGroup; no ferro-internal or doc DropdownMenu refs
  remain; dead render_dropdown_menu + its tests removed; render_menu_item + DropdownMenuAction kept.
</success_criteria>

<output>
After completion, create `.planning/phases/237-actiongroup-component-dropdownmenu-replacement/237-03-SUMMARY.md`
</output>
