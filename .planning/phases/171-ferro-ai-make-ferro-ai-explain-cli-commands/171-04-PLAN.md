---
phase: 171-ferro-ai-make-ferro-ai-explain-cli-commands
plan: 04
type: execute
wave: 4
depends_on: ["171-01", "171-02", "171-03"]
files_modified: []
autonomous: false
requirements: [AICLI-01, AICLI-02, AICLI-03]
tags: [gate, ci, manual-verify, ai, cli]

must_haves:
  truths:
    - "The full CI gate (fmt + clippy -D warnings --all-targets + test --all-features) is green for the whole workspace"
    - "ferro ai:make --help and ferro ai:explain --help both list the commands (clap registration wired end-to-end)"
    - "A live ai:make --dry-run against a real provider produces a ferro-consistent ServiceDef referencing real project facts (SC#6, human-verified)"
    - "A live ai:explain against an existing service returns projection-framed prose (SC#4, human-verified)"
  artifacts:
    - path: ".planning/phases/171-ferro-ai-make-ferro-ai-explain-cli-commands/171-04-SUMMARY.md"
      provides: "phase gate sign-off + manual verification record"
  key_links:
    - from: "ferro-cli + ferro-ai"
      to: "CI gate"
      via: "cargo fmt + clippy + test --all-features"
      pattern: "cargo test --all-features"
---

<objective>
Close Phase 171: prove the whole workspace passes the CI gate after the three implementation
plans, confirm both new commands are wired end-to-end through clap, and human-verify the two
non-deterministic live behaviors that unit tests cannot cover (SC#6 ferro-consistency of
ai:make output, SC#4 projection-framed ai:explain prose).

Purpose: ai:make and ai:explain are the milestone killer feature; the only behaviors not provable
by deterministic unit tests are the live-LLM quality checks. This plan runs the exact CI command
(CLAUDE.md / MEMORY.md convention) and gates the phase on a human confirming live output quality.
Output: green gate + a SUMMARY recording the manual verification result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/171-ferro-ai-make-ferro-ai-explain-cli-commands/171-VALIDATION.md
@.planning/phases/171-ferro-ai-make-ferro-ai-explain-cli-commands/171-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full CI gate + command-registration smoke check</name>
  <files>ferro-cli/, ferro-ai/ (no edits expected — gate + fix-on-failure only)</files>
  <read_first>
    - .planning/phases/171-ferro-ai-make-ferro-ai-explain-cli-commands/171-VALIDATION.md §"Test Infrastructure" (the exact full-suite command)
    - CLAUDE.md §"Testing & Linting" (the CI-exact command: fmt --check + clippy --all --all-targets -D warnings + test --all-features)
  </read_first>
  <action>
    Run the workspace gate exactly as CI runs it (MEMORY.md `feedback_ci_clippy_command_match`):
    ```
    cargo fmt --all -- --check
    cargo clippy --all --all-targets -- -D warnings
    cargo test --all-features
    ```
    Run them ONE AT A TIME, not chained — serialize CPU-intensive operations (MEMORY.md
    `feedback_one_cpu_op_at_a_time`). If `cargo test --all-features` hits an ENOSPC / disk-full
    link error (MEMORY.md `project_ferro_disk_full_test_gate`), check `df -h`, clean stale
    `target/` artifacts, and re-run — those are environment errors, not real failures.

    Fix any fmt/clippy/test failure surfaced by the gate (these would be defects introduced by
    Plans 01-03). Do NOT bump crate versions — the workspace uses `version.workspace = true`
    (verified: ferro-cli/ferro-ai both inherit workspace version 0.2.46); no per-crate bump is
    part of this phase, and publishing is a separate milestone step.

    Then smoke-check command registration (no LLM needed — `--help` does not call a provider):
    ```
    cargo run -p ferro-cli -- ai:make --help
    cargo run -p ferro-cli -- ai:explain --help
    ```
    Both must print usage including the documented flags (`--dry-run`; `ai:explain` also `--type`).
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings 2>&1 | tail -5 && cargo test --all-features 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy --all --all-targets -- -D warnings` exits 0 (zero warnings)
    - `cargo test --all-features` exits 0 (all workspace tests pass)
    - `cargo run -p ferro-cli -- ai:make --help` exits 0 and output contains `--dry-run`
    - `cargo run -p ferro-cli -- ai:explain --help` exits 0 and output contains `--dry-run` and `--type`
  </acceptance_criteria>
  <done>The full CI-exact gate is green across the workspace and both ai:* commands are registered and discoverable via --help.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify live ai:make and ai:explain quality (SC#6, SC#4)</name>
  <files>none (manual verification checkpoint — no file edits)</files>
  <what-built>
    `ferro ai:make <description>` (produces a typed ServiceDef from NL, in-process introspection,
    single src/projections file, --dry-run JSON preview) and `ferro ai:explain <target>`
    (service-first resolution, projection-framed prose, --dry-run prompt preview). Unit tests cover
    determinism (emitter round-trip, dry-run no-write, path-traversal rejection, resolution order,
    max_tokens, fail-fast). The two checks below are the non-deterministic live-LLM quality gates
    from 171-VALIDATION.md §"Manual-Only Verifications" — they require a real provider + a sample
    ferro project and cannot be unit-tested.
  </what-built>
  <how-to-verify>
    Prerequisite: export FERRO_AI_PROVIDER, FERRO_AI_API_KEY, FERRO_AI_MODEL for a real provider,
    in a sample ferro app directory (one with at least one model + ideally an existing projection).

    1. ai:make (SC#1, SC#2, SC#6) — run:
         ferro ai:make "track customer orders with pending/paid/shipped states" --dry-run
       Confirm the printed ServiceDef JSON:
         - uses real `FieldMeaning` values (e.g. Status, Money, EntityName) — not invented strings
         - includes a `state_machine` with pending/paid/shipped states
         - references model/field names that actually exist in the sample project (not generic templates)
         - is a SINGLE ServiceDef — no handler/model/route/migration files were written (it is --dry-run; also confirm a non-dry-run run writes ONLY src/projections/<name>.rs)

    2. ai:explain (SC#4) — pick an existing service/projection name in the project and run:
         ferro ai:explain <service-name>
       Confirm the prose is projection-framed: it names the service's Intents, the fields whose
       FieldMeanings drive rendering, the ActionDefs and the GuardDefs they sit under, and any
       StateMachine transitions — and references only what the service actually defines (SC#6).

    3. (optional) Confirm cost guard: set FERRO_AI_MAX_TOKENS_PER_COMMAND=256 and re-run ai:explain;
       the response should be truncated/shorter, proving the cap is applied.

    If output is generic, references nonexistent fields, or ai:explain returns plain code prose for
    a target that HAS a projection (should be projection-framed), describe the issue instead of approving.
  </how-to-verify>
  <action>
    Pause for the operator to run the two live-LLM checks in `<how-to-verify>` against a real
    provider in a sample ferro project, then report the result. This is a human-verify checkpoint —
    the agent presents the steps and waits for the resume signal; it does not run live provider calls.
  </action>
  <verify>
    <automated>echo "MANUAL — human-verify checkpoint: live ai:make/ai:explain quality (SC#6, SC#4); outcome recorded via resume-signal"</automated>
  </verify>
  <done>Operator has run the live ai:make --dry-run and ai:explain checks and typed "approved", or reported issues that are folded into a follow-up gap.</done>
  <resume-signal>Type "approved" if both live checks pass, or describe the issues observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live provider | this plan is the only one that exercises a real LLM provider end-to-end (manual checkpoint) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-GATE | Tampering | introduced-by-Plans-01-03 defects | mitigate | The full clippy `-D warnings` + `test --all-features` gate is the structural catch for any defect (including the security tests path-traversal/dry-run-no-write from Plan 02) before the phase is marked complete. |
| T-171-LIVE | Denial of Service | live cost during manual check | accept | The manual checks use `--dry-run` for ai:make (no provider call) and a single short ai:explain run bounded by the default 2048 max_tokens; cost is negligible and operator-initiated. |
</threat_model>

<verification>
- Full CI gate green (fmt + clippy -D warnings + test --all-features)
- Both commands discoverable via --help
- Human approval recorded for the two live-LLM quality checks
</verification>

<success_criteria>
- All of AICLI-01/02/03 proven end-to-end: deterministic behavior by the green test suite, live quality by human verification
- SC#1-6 satisfied (SC#1 lexically-relevant noted as v1 per D-02; embedding rerank deferred)
- Phase ready for /gsd-verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/171-ferro-ai-make-ferro-ai-explain-cli-commands/171-04-SUMMARY.md` recording the gate result and the human verification outcome.
</output>
