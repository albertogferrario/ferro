---
phase: 183
plan: 04
slug: publish-bootstrap
type: execute
wave: 4
depends_on: [03]
files_modified:
  - .planning/phases/183-ferro-bundle-capability-new-crate/183-04-SUMMARY.md
autonomous: false
requirements: [BUNDLE-06]
user_setup:
  - service: crates.io
    why: "First publish of ferro-bundle requires the maintainer's local cargo registry token (publish-new scope); the CI token has publish-update only per project memory project_ferro_publish_token_scoping.md"
    env_vars:
      - name: CARGO_REGISTRY_TOKEN
        source: "Already configured locally via `cargo login` from a prior interactive session — no new value needed; the existing local token has publish-new scope."
    dashboard_config:
      - task: "Run `cargo publish -p ferro-bundle` from local terminal at the workspace root after Plan 03 lands on master"
        location: "Local developer machine (NOT CI)"
must_haves:
  truths:
    - "The full lint+test gate is green (cargo fmt + cargo clippy --all --all-targets --all-features -- -D warnings + cargo test --all-features) before any publish action"
    - "`cargo publish -p ferro-bundle --dry-run` exits 0 from the workspace root"
    - "The manual bootstrap command, expected output, and recovery actions are documented in this plan's SUMMARY for future reference"
    - "After the user runs `cargo publish -p ferro-bundle` from local terminal, the crate exists on crates.io and subsequent CI Wave 3 publishes succeed automatically"
  artifacts:
    - path: ".planning/phases/183-ferro-bundle-capability-new-crate/183-04-SUMMARY.md"
      provides: "Manual bootstrap command + expected output + recovery procedure + verification of crates.io listing"
      contains: "cargo publish -p ferro-bundle"
  key_links:
    - from: "Plan 01's publish.yml Wave 3 entry"
      to: "crates.io ferro-bundle listing"
      via: "first publish bootstrapped manually, subsequent publishes automated"
      pattern: "WAVE3_CRATES"
---

<objective>
Close the phase by gating on a green `cargo publish --dry-run`, documenting the manual first-publish bootstrap (D-12), and capturing the bootstrap command and recovery procedure in the plan SUMMARY so future new-crate phases follow the same flow.

Purpose: D-12 requires that the very first `cargo publish -p ferro-bundle` runs from the maintainer's local terminal, because the CI token has `publish-update` scope only — it cannot create new crates on crates.io. This plan executes the verifiable parts (dry-run + full gate), then pauses for the human action (real publish), then records the outcome in the SUMMARY so the manual step is auditable in future audits.

Output:
- A clean `cargo publish -p ferro-bundle --dry-run` exit-0 run.
- A human-action checkpoint where the user runs `cargo publish -p ferro-bundle` from their local terminal at the workspace root.
- `.planning/phases/183-ferro-bundle-capability-new-crate/183-04-SUMMARY.md` with the bootstrap command, expected output, recovery procedure, and post-bootstrap verification (crates.io listing).

Why this plan is `autonomous: false`: Task 2 is a `checkpoint:human-action` because `cargo publish` (non-dry-run) writes to crates.io and the CI token cannot perform this action. The Claude executor MUST NOT attempt the real publish; the user runs it locally and reports back.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/183-ferro-bundle-capability-new-crate/183-CONTEXT.md
@.planning/phases/183-ferro-bundle-capability-new-crate/183-RESEARCH.md
@.planning/phases/183-ferro-bundle-capability-new-crate/183-VALIDATION.md
@.planning/phases/183-ferro-bundle-capability-new-crate/183-03-SUMMARY.md
@ferro-bundle/Cargo.toml
@ferro-bundle/src/lib.rs
@ferro-bundle/README.md
@.github/workflows/publish.yml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run the full lint+test gate and `cargo publish --dry-run` to gate the bootstrap</name>
  <files>
  </files>
  <read_first>
    ferro-bundle/Cargo.toml,
    .planning/phases/183-ferro-bundle-capability-new-crate/183-VALIDATION.md
  </read_first>
  <action>
Run the workspace-wide gate matching CI exactly (per memory `feedback_ci_clippy_command_match.md`), then run `cargo publish -p ferro-bundle --dry-run`.

**Step 1 — full CI-matching gate:**
```bash
cargo fmt --all -- --check && \
cargo clippy --all --all-targets --all-features -- -D warnings && \
cargo test --all-features
```

Expected: all three commands exit 0. `cargo test --all-features` runs the entire workspace test suite (every crate). ferro-bundle's 5 unit tests + 3 integration tests are part of this run.

If `cargo test --all-features` fails in any OTHER crate (not ferro-bundle):
- Out of scope for Phase 183. Surface in the SUMMARY as a pre-existing condition. Do not "fix" the failing crate here.

If `cargo test --all-features` fails inside ferro-bundle:
- Regression of Plan 02 or Plan 03 — investigate and either fix in place (small) or escalate (large).

**Step 2 — publish dry-run:**
```bash
cargo publish -p ferro-bundle --dry-run
```

Expected output ends with:
```
   Verifying ferro-bundle v0.2.43 (...)
   ...
   Compiling ferro-bundle v0.2.43 (...)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in Xs
    Packaged N files, X.X KiB (X.X KiB compressed)
   Upload skipped, due to --dry-run.
```

What `--dry-run` verifies:
- Cargo.toml is valid for publication (all required `[package]` fields present).
- All declared deps resolve (path + version) — including `ferro-rs = { path = "../framework", version = "0.2" }`.
- Source files compile under the packaged-source layout (no `include_str!` from outside the crate, no missing files).
- The crate compiles standalone (not just in-workspace).

If `--dry-run` fails with `no matching package named 'ferro-rs' found, required by package 'ferro-bundle'`:
- The `ferro-rs = "0.2"` requirement could not be satisfied. Verify the workspace version is `0.2.43` (Plan 01 Task 2) and that `ferro-rs` (the framework crate) has been published to crates.io at version `0.2.x` historically. Cargo's dry-run resolves against crates.io, not the local workspace path.

If `--dry-run` fails with `package contains uncommitted changes`:
- Plan 03 left changes uncommitted. Commit Plan 03's artifacts before dry-running.

If `--dry-run` succeeds, the crate is ready for the real publish. Proceed to Task 2.

Do NOT run the real `cargo publish` (without `--dry-run`) in this task. The real publish is the user's local-terminal action in Task 2.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --all --all-targets --all-features -- -D warnings && cargo test --all-features 2>&1 | tail -5 | grep -E 'test result: ok' && cargo publish -p ferro-bundle --dry-run 2>&1 | grep -E 'Upload skipped, due to --dry-run\.|Packaged'</automated>
  </verify>
  <acceptance_criteria>
- `cargo fmt --all -- --check` exits 0.
- `cargo clippy --all --all-targets --all-features -- -D warnings` exits 0.
- `cargo test --all-features` exits 0.
- `cargo publish -p ferro-bundle --dry-run` exits 0.
- The dry-run output contains the literal string `Upload skipped, due to --dry-run`.
- No file modifications occurred during this task (gate-only).
  </acceptance_criteria>
  <done>The phase has passed every automated gate. The only remaining action is the human-only first publish (Task 2).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Manual first-publish bootstrap (user runs from local terminal)</name>
  <files>
    (none — this task records a human action, not a file edit. The outcome is captured in Task 3's SUMMARY.)
  </files>
  <read_first>
    ferro-bundle/Cargo.toml,
    .github/workflows/publish.yml
  </read_first>
  <action>
This is a `checkpoint:human-action`. The Claude executor MUST NOT run `cargo publish` (non-dry-run) against ferro-bundle. The action below is performed by the user from their local terminal — Claude pauses and waits for the resume signal.

**What was built (context for the user):**
Plans 01-03 produced a fully-implemented, lint-clean, test-green `ferro-bundle` crate at workspace version 0.2.43. Task 1 of this plan verified `cargo publish -p ferro-bundle --dry-run` exits 0.

**Human action (user runs from local terminal):**

The user runs the following command from their local terminal at the workspace root (NOT through Claude — this requires the local `CARGO_REGISTRY_TOKEN` with `publish-new` scope, which the CI token does not have, per project memory `project_ferro_publish_token_scoping.md`):

```bash
cd /Users/alberto/repositories/albertogferrario/ferro
cargo publish -p ferro-bundle
```

Expected output ends with:
```
   Uploading ferro-bundle v0.2.43 (...)
   Uploaded ferro-bundle v0.2.43 to registry `crates-io`
    note: waiting for ferro-bundle v0.2.43 to be available at registry `crates-io`.
    ...
   Published ferro-bundle v0.2.43 at registry `crates-io`
```

After the upload, verify the listing exists:

```bash
curl -sI https://crates.io/api/v1/crates/ferro-bundle | grep -E '^HTTP/'
# Expected: HTTP/2 200
```

Or visit https://crates.io/crates/ferro-bundle in a browser — the version 0.2.43 listing should appear within ~60 seconds of the publish call returning.

**Failure modes and recovery:**

- `error: failed to publish ... 403 Forbidden` — the local `CARGO_REGISTRY_TOKEN` lacks `publish-new` scope. Generate a new token at https://crates.io/me with the `publish-new` checkbox enabled, then `cargo login <token>` and retry.
- `error: failed to publish ... 422 Unprocessable Entity` with "crate name X is already taken" — the crate name was claimed by someone else. Escalate; this requires a name discussion before any further publish attempt.

**Why a user action is the only option:** This phase explicitly cannot automate the real publish. Project memory `project_ferro_publish_token_scoping.md` records that the CI token's scope was deliberately limited to `publish-update` only, and only the maintainer's local token has `publish-new`. There is no CLI path Claude can take to obtain `publish-new` rights from the running session.

**After the real publish succeeds:** the next merge to master that bumps `workspace.package.version` will trigger CI Wave 3 to publish ferro-bundle 0.2.44 (or whatever version is current) automatically. No further manual bootstraps are needed for this crate.

**Resume signal — user reports back with ONE of:**
- `published` — the crate is live on crates.io at 0.2.43.
- `failed: <error summary>` — for the executor to record in SUMMARY.
- `skipped` — to defer the publish (acceptable; SUMMARY records the deferral and Phase 183 can be marked code-complete-but-not-yet-published).

Claude does not proceed to Task 3 until one of the three resume signals is received.
  </action>
  <verify>
    <automated>MISSING — Task 2 is `checkpoint:human-action`. There is no Claude-runnable verification. The user-side verification is `curl -sI https://crates.io/api/v1/crates/ferro-bundle | grep -E '^HTTP/2 200'` after the publish completes; the outcome is captured in Task 3's SUMMARY via the user's reported resume signal.</automated>
  </verify>
  <acceptance_criteria>
- The user has run `cargo publish -p ferro-bundle` from the workspace root (or has explicitly chosen to defer).
- The user has reported back with one of `published`, `failed: <error>`, or `skipped`.
- If `published`: the crates.io listing for ferro-bundle at version 0.2.43 returns HTTP 200.
- The Claude executor has NOT attempted `cargo publish` (non-dry-run) during this task.
  </acceptance_criteria>
  <done>The user-side bootstrap action is complete (or explicitly deferred), and the resume signal has been received so Task 3 can record the outcome.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write 183-04-SUMMARY.md capturing bootstrap outcome + recovery procedure</name>
  <files>
    .planning/phases/183-ferro-bundle-capability-new-crate/183-04-SUMMARY.md
  </files>
  <read_first>
    .planning/phases/183-ferro-bundle-capability-new-crate/183-01-SUMMARY.md,
    .planning/phases/183-ferro-bundle-capability-new-crate/183-02-SUMMARY.md,
    .planning/phases/183-ferro-bundle-capability-new-crate/183-03-SUMMARY.md
  </read_first>
  <action>
Write `.planning/phases/183-ferro-bundle-capability-new-crate/183-04-SUMMARY.md` capturing:

1. The full gate run output (fmt, clippy, test --all-features, dry-run) with timestamps.
2. The Task 2 outcome reported by the user (`published` / `failed` / `skipped`).
3. The exact command the user ran: `cargo publish -p ferro-bundle` from `/Users/alberto/repositories/albertogferrario/ferro`.
4. Post-publish verification (curl crates.io API or browser visit).
5. Recovery procedure for future new-crate phases — distilled into a runbook section so the next new ferro-* crate can follow the same flow without re-reading Plan 04 in full:

   **Runbook: New ferro-* crate first-publish bootstrap**
   - Land all scaffold plans (manifest, README, lib.rs, workspace member, publish.yml entry, version bump) on master.
   - From local terminal at workspace root: `cargo publish -p <new-crate>`.
   - Verify crates.io listing within ~60 seconds.
   - Subsequent versions ship via CI Wave-N for that crate automatically.

6. The publish-wave shape chosen for Phase 183 (Shape B per RESEARCH recommendation: `WAVE3_CRATES="ferro-cli ferro-bundle"`) and the rationale (smaller diff than inserting Wave 2.5; matches the "post-framework consumers" semantic).

7. A traceability table mapping the 6 success criteria (BUNDLE-01..06) to the plans that delivered them:

   | Success Criterion | Plan | Verification |
   |-------------------|------|--------------|
   | BUNDLE-01 (deterministic hash → URL) | 02 | `cargo test -p ferro-bundle --lib hash_is_deterministic` |
   | BUNDLE-02 (200 cold + 304 fast-path) | 03 | `cargo test -p ferro-bundle --test serve_cold --test serve_304` |
   | BUNDLE-03 (alias 301 redirect) | 03 | `cargo test -p ferro-bundle --test alias_redirect` |
   | BUNDLE-04 (default content-type) | 02 | `cargo test -p ferro-bundle --lib default_content_type_is_octet_stream` |
   | BUNDLE-05 (README documents split) | 01 | `grep -F 'do not fold these' ferro-bundle/README.md` |
   | BUNDLE-06 (publishes via GH Actions) | 01 (CI wiring) + 04 (manual bootstrap) | `grep -F 'ferro-bundle' .github/workflows/publish.yml` + crates.io listing |

8. Any pre-existing workspace warnings surfaced by the gate that were noted but not fixed (out of scope per Phase 183 boundary).

9. Hand-off to the next phase: STATE.md update to "Phase 183 complete", `Next: push master, await CI Wave 2 to publish ferro-rs 0.2.43, then merge Phase 183 dependents`.

If Task 2 reported `failed` or `skipped`:
- The SUMMARY records the failure mode or deferral rationale verbatim.
- A "Reopen action" subsection lists what needs to happen before the crate ships (e.g., "Generate token with publish-new scope, then `cargo login <new-token>`, then re-run `cargo publish -p ferro-bundle`").
- Phase 183's code is still complete and shippable — only the crates.io publication is incomplete.

Voice: neutral architectural documentation per CLAUDE.md. No trigger phrases. No "killer feature" / "load-bearing" language outside quoted technical phrases like "load-bearing design assertion" (which is acceptable as it labels a class of decisions, not a personal-conviction trigger).
  </action>
  <verify>
    <automated>test -f .planning/phases/183-ferro-bundle-capability-new-crate/183-04-SUMMARY.md && grep -F 'cargo publish -p ferro-bundle' .planning/phases/183-ferro-bundle-capability-new-crate/183-04-SUMMARY.md && grep -F 'BUNDLE-01' .planning/phases/183-ferro-bundle-capability-new-crate/183-04-SUMMARY.md && grep -F 'BUNDLE-06' .planning/phases/183-ferro-bundle-capability-new-crate/183-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
- `.planning/phases/183-ferro-bundle-capability-new-crate/183-04-SUMMARY.md` exists.
- The SUMMARY contains the exact bootstrap command (`cargo publish -p ferro-bundle`) and its working directory.
- The SUMMARY contains a traceability table mapping BUNDLE-01..06 to the plans that delivered them.
- The SUMMARY records the Task 2 outcome (`published`, `failed: ...`, or `skipped`).
- The SUMMARY contains a "Runbook: New ferro-* crate first-publish bootstrap" subsection for future reuse.
- The SUMMARY notes the publish-wave choice (Shape B) and rationale.
  </acceptance_criteria>
  <done>The phase has a durable record of the bootstrap action (or its deferral). Future audits read the SUMMARY instead of re-deriving the manual step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local terminal → crates.io | The user's `cargo publish` runs under the local `CARGO_REGISTRY_TOKEN` which has `publish-new` scope. The token is held by the maintainer and not exposed to CI or to Claude. |
| Claude → user-only action | This plan explicitly does NOT run `cargo publish` (non-dry-run). The dry-run is safe (no network write); the real publish is gated behind a `checkpoint:human-action`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-04-01 | E (Elevation of privilege) | `cargo publish` real run | mitigate | Plan is `autonomous: false`. The publish task is `checkpoint:human-action`. Claude executor MUST NOT attempt the real publish. The user runs it locally with their token. |
| T-183-04-02 | T (Tampering) | crates.io listing for ferro-bundle | accept | crates.io publication is irreversible (a published version cannot be un-published; only yanked). The dry-run gate (Task 1) verifies the package contents before the real publish, reducing the risk of publishing a broken version. Workspace version 0.2.43 is correct per Plan 01. |
| T-183-04-03 | I (Information disclosure) | SUMMARY.md captures publish outcome | accept | The SUMMARY is committed under `.planning/` (potentially-public per CLAUDE.md repository-documents rule). No tokens, secrets, or PII are recorded — only the public command, the public crates.io listing URL, and pass/fail status. |
</threat_model>

<verification>
- Task 1 fully automated and gated.
- Task 2 is a `checkpoint:human-action` (Claude does NOT execute `cargo publish` non-dry-run).
- Task 3 captures the bootstrap outcome in a durable file.
- `cargo publish -p ferro-bundle --dry-run` exits 0.
- If user reports "published": crates.io listing at https://crates.io/crates/ferro-bundle returns HTTP 200 for version 0.2.43.
- If user reports "failed" or "skipped": SUMMARY documents the deferral and the reopen path.
</verification>

<success_criteria>
BUNDLE-06 (publishes via existing GH Actions workflow) is satisfied at two levels:
- CI wiring: Plan 01 added ferro-bundle to publish.yml Wave 3 alongside ferro-cli.
- First publish: This plan executes the manual bootstrap (Task 2) or records its deferral.

After Task 2 reports `published`, every subsequent workspace version bump triggers CI Wave 3 to publish a new ferro-bundle version automatically — no further manual intervention.

Phase 183 is now complete. All six success criteria (BUNDLE-01..06) are delivered:
| Plan 01 | BUNDLE-05, BUNDLE-06 (CI wiring) |
| Plan 02 | BUNDLE-01, BUNDLE-04 |
| Plan 03 | BUNDLE-02, BUNDLE-03 |
| Plan 04 | BUNDLE-06 (manual bootstrap completion) |
</success_criteria>

<output>
After completion, the plan SUMMARY at `.planning/phases/183-ferro-bundle-capability-new-crate/183-04-SUMMARY.md` is the durable record. STATE.md should be updated to mark Phase 183 complete and point at v12.1 AI Phase 165 as the next target (per the pre-existing roadmap in STATE.md).
</output>
