---
phase: 183
plan: 01
slug: scaffold
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-bundle/Cargo.toml
  - ferro-bundle/src/lib.rs
  - ferro-bundle/README.md
  - Cargo.toml
  - .github/workflows/publish.yml
autonomous: true
requirements: [BUNDLE-05, BUNDLE-06]
user_setup: []
must_haves:
  truths:
    - "Workspace builds green with `ferro-bundle` registered as a member crate"
    - "Cargo.lock reflects workspace.package.version 0.2.43 for every workspace crate"
    - "`.github/workflows/publish.yml` knows to publish ferro-bundle after framework (`ferro-rs`)"
    - "README documents the bundle-vs-filesystem split with the load-bearing wording from D-10"
  artifacts:
    - path: "ferro-bundle/Cargo.toml"
      provides: "Crate manifest with locked deps (sha2 0.10, hex 0.4, dashmap 6, bytes 1, thiserror 2, ferro-rs path+version dep)"
      contains: "name = \"ferro-bundle\""
    - path: "ferro-bundle/src/lib.rs"
      provides: "Compilable placeholder lib with crate-level docs"
      contains: "//!"
    - path: "ferro-bundle/README.md"
      provides: "Crate README with the bundle-vs-filesystem split section per D-10"
      contains: "do not fold"
    - path: "Cargo.toml"
      provides: "Workspace registers ferro-bundle as member; version bumped 0.2.42 -> 0.2.43"
      contains: "\"ferro-bundle\","
    - path: ".github/workflows/publish.yml"
      provides: "Wave 3 publishes ferro-bundle alongside ferro-cli (both are post-framework consumers)"
      contains: "ferro-bundle"
  key_links:
    - from: "Cargo.toml [workspace] members"
      to: "ferro-bundle/Cargo.toml"
      via: "workspace member registration"
      pattern: "\"ferro-bundle\","
    - from: "ferro-bundle/Cargo.toml [dependencies]"
      to: "framework/Cargo.toml (crate name ferro-rs)"
      via: "path + version inter-crate dep"
      pattern: "ferro-rs = \\{ path = \"../framework\", version = \"0.2\" \\}"
    - from: ".github/workflows/publish.yml Wave 3"
      to: "ferro-bundle"
      via: "post-framework publish wave"
      pattern: "WAVE3_CRATES=\"ferro-cli ferro-bundle\""
---

<objective>
Scaffold the new `ferro-bundle` workspace crate: manifest, README, lib.rs stub (no implementation yet), workspace member registration, workspace version bump 0.2.42 -> 0.2.43, and publish.yml Wave 3 entry alongside `ferro-cli`.

Purpose: Lays the workspace and publish-pipeline foundation Plan 02 (core impl) and Plan 03 (integration tests) build on. By the end of this plan, `cargo build --workspace` is green, Cargo.lock has been synced, and the publish workflow knows the new crate.

Output:
- `ferro-bundle/Cargo.toml` with full dep set verbatim from RESEARCH §Standard Stack.
- `ferro-bundle/src/lib.rs` with crate-level `//!` docs; no public types yet (Plan 02 adds `Bundle`).
- `ferro-bundle/README.md` with bundle-vs-filesystem split per D-10 (load-bearing "do not fold" wording).
- `Cargo.toml` workspace members list grows by one; workspace.package.version 0.2.42 -> 0.2.43.
- `Cargo.lock` regenerated by `cargo build --workspace` (no manual edit).
- `.github/workflows/publish.yml` Wave 3 step renamed to "framework-consumers" and converted to a `WAVE3_CRATES="ferro-cli ferro-bundle"` for-loop matching the Wave 2 pattern.

Wave assignment correction (per RESEARCH §critical finding):
- CONTEXT.md D-09/D-11 incorrectly placed ferro-bundle in Wave 1B.
- Actual constraint: `ferro-rs` is published in publish.yml line 274 (Wave 2). ferro-bundle depends on `ferro-rs` and MUST publish AFTER Wave 2.
- Shape B picked (per RESEARCH recommendation): append `ferro-bundle` to existing Wave 3 alongside `ferro-cli`. Both are "post-framework consumers". Smaller diff than inserting a new Wave 2.5 block; cleaner semantic than overloading Wave 3 as "CLI only".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/183-ferro-bundle-capability-new-crate/183-CONTEXT.md
@.planning/phases/183-ferro-bundle-capability-new-crate/183-RESEARCH.md
@.planning/phases/183-ferro-bundle-capability-new-crate/183-PATTERNS.md
@.planning/phases/183-ferro-bundle-capability-new-crate/183-VALIDATION.md
@Cargo.toml
@.github/workflows/publish.yml
@ferro-wallet/Cargo.toml
@ferro-wallet/README.md
@ferro-storage/README.md
@ferro-ai/Cargo.toml
@ferro-macros/Cargo.toml

<interfaces>
<!-- Sibling crate deps already pinned at workspace-compatible versions. -->
<!-- Cargo.lock confirms: sha2 0.10.9, hex 0.4.3, dashmap 6.1.0, bytes 1.x, thiserror 2.x. -->
<!-- framework/Cargo.toml [package] name = "ferro-rs"; directory is `framework/`. -->
<!-- ferro-macros/Cargo.toml line 25: `ferro-rs = { path = "../framework" }` (dev-dep, version omitted). -->
<!-- ferro-bundle is a NON-dev dep on ferro-rs; MUST include `version = "0.2"` for publish-time resolution. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create ferro-bundle/Cargo.toml, lib.rs stub, and README</name>
  <files>
    ferro-bundle/Cargo.toml,
    ferro-bundle/src/lib.rs,
    ferro-bundle/README.md
  </files>
  <read_first>
    ferro-wallet/Cargo.toml,
    ferro-wallet/README.md,
    ferro-storage/README.md,
    ferro-ai/Cargo.toml,
    ferro-macros/Cargo.toml,
    .planning/phases/183-ferro-bundle-capability-new-crate/183-CONTEXT.md,
    .planning/phases/183-ferro-bundle-capability-new-crate/183-RESEARCH.md
  </read_first>
  <action>
Create the directory `ferro-bundle/` (and `ferro-bundle/src/`) and write three files verbatim.

**File 1: `ferro-bundle/Cargo.toml`** — full content:

```toml
[package]
name = "ferro-bundle"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "In-memory immutable byte-blob serving with content-hashed URLs for the Ferro framework"
repository = "https://github.com/albertogferrario/ferro"
keywords = ["bundle", "static", "cache", "etag", "ferro"]
categories = ["web-programming"]
readme = "README.md"

[dependencies]
sha2 = "0.10"
hex = "0.4"
dashmap = "6"
bytes = "1"
thiserror = "2"
ferro-rs = { path = "../framework", version = "0.2" }

[dev-dependencies]
# Integration tests construct synthetic registry state and call a private
# serve_inner helper directly (per RESEARCH OQ #3 resolution). No additional
# test crates needed.
```

Notes:
- `name = "ferro-bundle"` is the crates.io name.
- `version.workspace = true` inherits 0.2.43 from the workspace bump in Task 2.
- The `ferro-rs` dep MUST include `version = "0.2"` — this is non-dev (publish-time crates.io resolution requires it). The lone in-repo precedent at `ferro-macros/Cargo.toml:25` omits version because it is a dev-dep; ferro-bundle does NOT mirror that omission.
- Keywords: exactly 5 lowercase, no marketing language.
- Categories: `"web-programming"` only.

**File 2: `ferro-bundle/src/lib.rs`** — placeholder body, full content:

```rust
//! In-memory immutable byte blobs with content-hashed URLs and one-year immutable caching.
//!
//! See the crate README for the bundle-vs-filesystem split: ferro-bundle handles
//! compile-time-embedded immutable assets; the framework's filesystem static-file
//! handler at `ferro_rs::static_files` handles mutable on-disk tenant assets.
//!
//! The public API will be added in Plan 02 (`Bundle::new`, `.content_type`,
//! `.with_alias`, `.hashed_url`, `Bundle::serve`).
```

Notes:
- No `pub` items yet; Plan 02 adds the `Bundle` struct, registries, `Error` enum, and tests.
- Crate-level `//!` docs only. `cargo build -p ferro-bundle` will succeed against this body.

**File 3: `ferro-bundle/README.md`** — full content (neutral architectural voice, no trigger phrases; the phrase "do not fold these" is acceptable as design rationale):

```markdown
# ferro-bundle

In-memory immutable byte-blob serving with content-hashed URLs for the Ferro framework.

The crate exposes a single `Bundle` type that registers compile-time-included bytes (`include_bytes!`) at boot, exposes a content-hashed URL of the form `/bundles/{name}.{sha8}.{ext}`, and serves the bytes with a one-year `Cache-Control: public, max-age=31536000, immutable` header plus a strong SHA-256 `ETag` and 304 fast-path on `If-None-Match` exact match. A `.with_alias("/path")` mechanism registers stable plain URLs that 301-redirect to the current hashed URL.

## Features

- SHA-256 content hashing (first 8 hex chars in the URL, full 64-hex digest in the ETag)
- One-year immutable `Cache-Control` headers with `immutable` directive (RFC 8246)
- Strong, quoted ETag per RFC 7232 with 304 fast-path on `If-None-Match` exact match
- Stable alias URLs that 301-redirect to the current hashed URL

## Usage

```rust
use ferro_bundle::Bundle;

// At boot, register a compile-time-embedded SDK bundle.
let sdk = Bundle::new("embed-v1", include_bytes!("../assets/embed-v1.js"))
    .content_type("application/javascript")
    .with_alias("/embed/v1.js");

// The hashed URL is deterministic per byte contents.
assert!(sdk.hashed_url().starts_with("/bundles/embed-v1."));

// Inside a request handler mounted on `/bundles/{filename}` (and on each
// registered alias path), dispatch via Bundle::serve.
// ferro-bundle does not own routing; the consumer wires the handler.
async fn serve_bundle(req: ferro_rs::Request) -> ferro_rs::HttpResponse {
    Bundle::serve(req)
}
```

## Bundle vs filesystem static files

ferro-bundle and the framework's filesystem static-file handler at `ferro_rs::static_files` are two parallel asset-serving paths. They target different freshness models and are intentionally not folded into one.

| Path | Freshness model | Cache lifetime | Use for |
|------|-----------------|----------------|---------|
| `ferro-bundle` | content hash in URL | one year, `immutable` | SDK bundles, embedded fonts, versioned static assets included via `include_bytes!` |
| `ferro_rs::static_files` | `bust_asset_urls` timestamp query param | shorter, revalidated | tenant-customizable CSS, theme assets, on-disk user uploads |

**Do not fold these — they target different freshness models.** A content-hashed URL is a stable handle to an immutable blob; a timestamp-busted URL is a freshness marker on a mutable file. Collapsing the two paths erodes both contracts.

## Security note

The caller provides `content_type` at registration. Serving caller-supplied bytes as `text/html` from a cookie-authenticated origin can introduce an XSS vector if the bundle bytes are themselves user-controlled. The locked `&'static [u8]` API restricts bytes to compile-time inclusion, which mitigates this; downstream consumers should keep that invariant when extending the crate in future phases.

## License

MIT — see the [ferro](https://github.com/albertogferrario/ferro) workspace README for documentation links (`https://docs.rs/ferro-bundle`).
```

After writing all three files, do NOT run `cargo build` here — Task 2 adds the workspace member entry and version bump, and Task 3 runs the build that regenerates Cargo.lock. Until Task 2 commits, the crate is not yet a workspace member.
  </action>
  <verify>
    <automated>test -f ferro-bundle/Cargo.toml && test -f ferro-bundle/src/lib.rs && test -f ferro-bundle/README.md && grep -F 'ferro-rs = { path = "../framework", version = "0.2" }' ferro-bundle/Cargo.toml && grep -F 'do not fold these' ferro-bundle/README.md && grep -F '//! In-memory immutable byte blobs' ferro-bundle/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
- `ferro-bundle/Cargo.toml` exists and contains exactly the 7 dep lines under `[dependencies]` listed above.
- `ferro-bundle/src/lib.rs` contains only crate-level `//!` docs (no `pub` items, no `fn`).
- `ferro-bundle/README.md` contains the literal phrase `do not fold these` (D-10 load-bearing).
- `ferro-bundle/README.md` contains a `## Bundle vs filesystem static files` section header.
- All three files committed (no other diffs at this point).
  </acceptance_criteria>
  <done>The crate directory exists with manifest + stub lib + README. Workspace registration and publish.yml entry happen in Task 2; build verification happens in Task 3.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Register ferro-bundle in workspace + bump workspace version + add to publish.yml Wave 3</name>
  <files>
    Cargo.toml,
    .github/workflows/publish.yml
  </files>
  <read_first>
    Cargo.toml,
    .github/workflows/publish.yml,
    .planning/phases/183-ferro-bundle-capability-new-crate/183-RESEARCH.md
  </read_first>
  <action>
Two file edits.

**Edit 1: `Cargo.toml` (workspace root) — TWO changes:**

(a) Append `    "ferro-bundle",` to the `[workspace] members` list. Current state ends at line 29 with `    "ferro-projection",` followed by `]` on line 30. After the edit, the closing `]` moves one line down and the new entry sits between `"ferro-projection",` and `]`. Append-not-alphabetize convention per Phases 151-155.

(b) Bump `version = "0.2.42"` on line 33 to `version = "0.2.43"`. This is the SOLE version-bump action — every workspace crate inherits via `version.workspace = true`, including the new `ferro-bundle/Cargo.toml`.

Apply both edits via the Edit tool with two separate replacements on the same file. The post-edit `[workspace]` block looks like:

```toml
[workspace]
resolver = "2"
members = [
    "framework",
    "app",
    "ferro-cli",
    "ferro-macros",
    "ferro-events",
    "ferro-queue",
    "ferro-notifications",
    "ferro-broadcast",
    "ferro-storage",
    "ferro-cache",
    "ferro-mcp",
    "ferro-inertia",
    "ferro-json-ui",
    "ferro-lang",
    "ferro-api-mcp",
    "ferro-projections",
    "ferro-stripe",
    "ferro-theme",
    "ferro-ai",
    "ferro-whatsapp",
    "ferro-wallet",
    "ferro-orm",
    "ferro-audit",
    "ferro-migration",
    "ferro-reservation",
    "ferro-projection",
    "ferro-bundle",
]

[workspace.package]
version = "0.2.43"
edition = "2021"
rust-version = "1.88.0"
license = "MIT"
```

**Edit 2: `.github/workflows/publish.yml` Wave 3 — convert single-crate inline step to a for-loop matching Wave 2:**

Current state (lines 297-310):
```yaml
      - name: Publish Wave 3 (CLI)
        run: |
          echo "Publishing ferro-cli..."
          if OUTPUT=$(cargo publish -p ferro-cli --no-verify 2>&1); then
            echo "ferro-cli published successfully"
          else
            if echo "$OUTPUT" | grep -q "already exists\|already uploaded"; then
              echo "ferro-cli already published, skipping"
            else
              echo "Failed to publish ferro-cli"
              echo "$OUTPUT"
              exit 1
            fi
          fi
```

Replace those 14 lines with the following block (Shape B per RESEARCH recommendation — clone Wave 2's for-loop shape, rename step to "framework-consumers", crate list `"ferro-cli ferro-bundle"`):

```yaml
      - name: Publish Wave 3 (framework-consumers)
        run: |
          echo "Publishing Wave 3 crates..."

          # Wave 3: Framework consumers (depend on Wave 2's ferro-rs)
          WAVE3_CRATES="ferro-cli ferro-bundle"

          for crate in $WAVE3_CRATES; do
            echo "Publishing $crate..."
            if OUTPUT=$(cargo publish -p $crate --no-verify 2>&1); then
              echo "$crate published successfully"
            else
              if echo "$OUTPUT" | grep -q "already exists\|already uploaded"; then
                echo "$crate already published, skipping"
              else
                echo "Failed to publish $crate"
                echo "$OUTPUT"
                exit 1
              fi
            fi
            sleep 5
          done
```

Notes:
- Step name `Publish Wave 3 (framework-consumers)` reflects the new semantic (both crates are post-`ferro-rs` consumers). Per RESEARCH §critical finding, CONTEXT.md D-09's `WAVE1B_CRATES` assignment was wrong because `ferro-rs` itself publishes in Wave 2 (line 274).
- The for-loop pattern mirrors Wave 2 verbatim (lines 276-290) for consistency. The `sleep 5` between crates within a wave matches the existing convention.
- The trailing "Create and push tag" step (line 312 onwards) is untouched.
- ferro-bundle's first publish under this CI step WILL fail because the CI token has `publish-update` only (see Plan 04 manual bootstrap). On the first run after merge, the `already exists\|already uploaded` branch catches the "crate not found" error from the token rejection and the workflow exits 1 — Plan 04 documents the human action to recover.

Do NOT touch Waves 1a, 1b, or 2. Do NOT touch the `Wait for crates.io index update` steps. Do NOT modify the trigger conditions.
  </action>
  <verify>
    <automated>grep -F '"ferro-bundle",' Cargo.toml && grep -F 'version = "0.2.43"' Cargo.toml && grep -F 'WAVE3_CRATES="ferro-cli ferro-bundle"' .github/workflows/publish.yml && grep -F 'Publish Wave 3 (framework-consumers)' .github/workflows/publish.yml && ! grep -F 'version = "0.2.42"' Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
- `Cargo.toml` contains the literal string `"ferro-bundle",` exactly once.
- `Cargo.toml` contains `version = "0.2.43"` and does NOT contain `version = "0.2.42"` (workspace.package only — other version strings in Cargo.toml are dep version constraints, not workspace.package).
- `.github/workflows/publish.yml` contains `WAVE3_CRATES="ferro-cli ferro-bundle"` exactly once.
- `.github/workflows/publish.yml` contains `Publish Wave 3 (framework-consumers)` exactly once.
- `.github/workflows/publish.yml` does NOT contain the old `Publish Wave 3 (CLI)` step name or the bare `cargo publish -p ferro-cli` single-crate inlined invocation.
  </acceptance_criteria>
  <done>Workspace knows ferro-bundle, version bumped to 0.2.43, and CI is wired to attempt publishing it after `ferro-rs` lands in Wave 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build workspace, sync Cargo.lock, and run lint+test gate</name>
  <files>
    Cargo.lock
  </files>
  <read_first>
    Cargo.toml,
    ferro-bundle/Cargo.toml
  </read_first>
  <action>
Run the standard build + sync + lint + test sequence (CI-matching command per CLAUDE.md).

**Command sequence (single shell pipeline):**

```bash
cargo build --workspace && \
cargo fmt --all -- --check && \
cargo clippy --all --all-targets --all-features -- -D warnings && \
cargo test -p ferro-bundle
```

Expected outcomes:
- `cargo build --workspace` succeeds (the placeholder lib.rs has no compile errors).
- Cargo.lock is auto-updated with the new `ferro-bundle 0.2.43` entry and every other workspace crate bumped 0.2.42 -> 0.2.43. This precedent matches Phase 182's commit `7c64996e` ("chore: sync Cargo.lock to workspace version 0.2.41"). No manual edit to Cargo.lock.
- `cargo fmt --all -- --check` exits 0 (placeholder lib.rs is already formatted; README is markdown).
- `cargo clippy --all --all-targets --all-features -- -D warnings` exits 0 (empty placeholder lib.rs has no clippy lints to violate).
- `cargo test -p ferro-bundle` exits 0 with 0 tests run (Plan 02 adds the unit tests).

If `cargo build --workspace` fails with "no matching package named 'ferro-rs' found":
- The error indicates `ferro-bundle/Cargo.toml`'s `ferro-rs = { path = "../framework", version = "0.2" }` line did not resolve. Verify Task 1's file content and the directory structure (`ferro-bundle/Cargo.toml` is at the workspace root level).

If clippy fails with warnings against OTHER crates in the workspace:
- Out of scope for Phase 183. Do not "fix" pre-existing warnings here. Surface the failure in the SUMMARY as a pre-existing condition; the planner may scope a cleanup phase.

If clippy fails with warnings against `ferro-bundle/src/lib.rs`:
- The placeholder body has nothing to lint. A failure here is unexpected — investigate before continuing.

After the sequence passes, Cargo.lock has been modified by `cargo build`. The file is auto-generated, no manual edit. Commit it as part of this task.
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -1 | grep -E 'Finished|Compiling' && cargo fmt --all -- --check && cargo clippy --all --all-targets --all-features -- -D warnings && cargo test -p ferro-bundle 2>&1 | grep -E 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
- `cargo build --workspace` exits 0.
- `cargo fmt --all -- --check` exits 0.
- `cargo clippy --all --all-targets --all-features -- -D warnings` exits 0.
- `cargo test -p ferro-bundle` exits 0 (0 tests run is acceptable at this stage).
- Cargo.lock contains the line `name = "ferro-bundle"` exactly once.
- Cargo.lock version for `ferro-bundle` is `0.2.43`.
  </acceptance_criteria>
  <done>The new crate is part of the workspace; CI publish pipeline knows about it; the workspace is lint-clean and test-green. Plan 02 can land core implementation against a stable scaffold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline | `cargo build`, `cargo publish`, and `.github/workflows/publish.yml` are trusted local-toolchain or CI processes; no untrusted input flows through them in this plan. |

## STRIDE Threat Register

This plan is pure scaffolding (manifest + stub + README + workspace registration + publish.yml). No runtime input is handled. The placeholder `lib.rs` is empty of behavior. Security analysis defers to Plan 02 (where `Bundle::serve` is implemented and runtime input enters the picture).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-183-01-01 | T (Tampering) | publish.yml `WAVE3_CRATES` list | mitigate | The list is plain shell text reviewed in PR; CI runs under `GITHUB_TOKEN` with publish scope only. Workflow file changes are visible in `git diff` and require a merge. |
| T-183-01-02 | I (Information disclosure) | crate README + Cargo.toml metadata | accept | All published metadata is intentionally public; the crate description and keywords carry no tenant identity (per CLAUDE.md project-agnostic-crates rule). |

No `Bundle::serve` runtime threats apply at this plan boundary; deferred to Plan 02.
</threat_model>

<verification>
- `ferro-bundle/` directory exists with `Cargo.toml`, `src/lib.rs`, `README.md`.
- `Cargo.toml` workspace members lists `"ferro-bundle",`.
- `Cargo.toml` workspace.package version is `0.2.43`.
- `Cargo.lock` is synced to version 0.2.43 across all crates and contains `name = "ferro-bundle"`.
- `.github/workflows/publish.yml` Wave 3 step is named `Publish Wave 3 (framework-consumers)` and uses `WAVE3_CRATES="ferro-cli ferro-bundle"` in a for-loop matching the Wave 2 pattern.
- `cargo build --workspace` exits 0.
- `cargo fmt --all -- --check` exits 0.
- `cargo clippy --all --all-targets --all-features -- -D warnings` exits 0.
- `cargo test -p ferro-bundle` exits 0.
- `ferro-bundle/README.md` contains the load-bearing phrase `do not fold these` (D-10).
</verification>

<success_criteria>
BUNDLE-05 (README documents the split) and BUNDLE-06 (publish pipeline knows the crate) are satisfied by this plan. BUNDLE-01..BUNDLE-04 are deferred to Plans 02 and 03. BUNDLE-06 manual bootstrap is deferred to Plan 04.
</success_criteria>

<output>
After completion, create `.planning/phases/183-ferro-bundle-capability-new-crate/183-01-SUMMARY.md` summarizing:
- Files created and their sizes.
- Workspace version transition (0.2.42 -> 0.2.43).
- Publish-wave shape chosen (Shape B: ferro-bundle appended to Wave 3 alongside ferro-cli; Wave 1B assignment from CONTEXT.md D-09 corrected).
- Cargo.lock sync confirmation.
- Any pre-existing clippy warnings surfaced (should be none).
- Hand-off to Plan 02 (core implementation).
</output>
