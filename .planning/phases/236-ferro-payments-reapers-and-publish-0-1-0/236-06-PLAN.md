---
phase: 236-ferro-payments-reapers-and-publish-0-1-0
plan: 06
type: execute
wave: 4
depends_on: [04]
files_modified:
  - docs/src/features/payments.md
  - docs/src/SUMMARY.md
  - docs/src/features/stripe.md
autonomous: true
requirements: [PAY-POLY-REAP-04]
must_haves:
  truths:
    - "docs/src/features/payments.md exists and leads with the consumer one-call recovery story"
    - "The page documents: register migrations, wire_dispatcher, schedule the two reapers via cron, the auto-refund + reconcile recovery model"
    - "docs/src/SUMMARY.md links the new page after the Stripe entry"
    - "docs/src/features/stripe.md cross-links to the payments page"
    - "cargo doc -Dwarnings stays green (Rust docs build, the CI gate that has bitten prior publishes)"
  artifacts:
    - path: "docs/src/features/payments.md"
      provides: "consumer-facing payments + reaper recovery documentation"
      contains: "wire_dispatcher"
      min_lines: 50
    - path: "docs/src/SUMMARY.md"
      provides: "payments page nav link"
      contains: "features/payments.md"
  key_links:
    - from: "docs/src/SUMMARY.md"
      to: "docs/src/features/payments.md"
      via: "mdbook nav entry after the Stripe line"
      pattern: "features/payments.md"
    - from: "docs/src/features/stripe.md"
      to: "features/payments.md"
      via: "cross-link"
      pattern: "payments.md"
---

<objective>
Write the consumer-facing payments documentation page that leads with the killer
artifact: a consumer schedules recovery in three lines — register migrations,
`wire_dispatcher`, and two cron entries for the reaper jobs — and every money-stuck
edge case (unpaid expiry, refund-in-flight) self-heals.

Purpose: The docs page is the consumer's entry point to the ferro-payments crate
shipping in this phase. It must present the one-call story and the recovery model
(auto-refund on webhook + reconcile reaper) so a consumer can adopt the crate
without reading the source.
Output: `docs/src/features/payments.md`, a SUMMARY.md nav link, and a cross-link
from `stripe.md`. The `cargo doc -Dwarnings` Rust-docs gate must stay green (it has
bitten prior publishes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/236-ferro-payments-reapers-and-publish-0-1-0/236-CONTEXT.md
@.planning/phases/236-ferro-payments-reapers-and-publish-0-1-0/236-PATTERNS.md

<interfaces>
<!-- The public API the docs must describe (already in tree after Plans 03/04) +
     the docs analog + the SUMMARY insertion point. -->

Public consumer surface to document (from ferro-payments/src/lib.rs re-exports):
```rust
ferro_payments::PaymentService          // ::new(...), start_checkout, release_expired, reconcile_refunds_in_flight
ferro_payments::wire_dispatcher          // registers the three webhook handlers
ferro_payments::ReleaseExpiredPaymentIntents  // ferro-queue Job — schedule via cron
ferro_payments::ReconcileRefundsInFlight      // ferro-queue Job — schedule via cron
ferro_payments::migration::CreatePaymentIntentsTable  // register in the consumer migrator
ferro_payments::{Billable, BillableLoader, BillableKind}
```

docs/src/SUMMARY.md — the Stripe nav line to insert AFTER (line 49):
```markdown
- [Stripe](features/stripe.md)
```
Insert immediately below:
```markdown
- [Payments](features/payments.md)
```

docs/src/features/stripe.md — the structural/voice analog (intro paragraph,
`## Quick Start`, feature sections, `## Environment Variables` table). Mirror its
heading depth and code-block style; add a cross-link to payments.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/src/features/payments.md, wire SUMMARY.md, cross-link stripe.md</name>
  <read_first>
    - docs/src/features/stripe.md (the structure/voice analog — read the intro,
      Quick Start, feature sections, Environment Variables table, heading depth)
    - docs/src/SUMMARY.md (the nav file — read around line 49, the Stripe entry)
    - ferro-payments/src/lib.rs (the public re-exports the docs must describe)
    - ferro-payments/src/reaper.rs (the two Job structs the cron section schedules)
    - .planning/phases/236-ferro-payments-reapers-and-publish-0-1-0/236-PATTERNS.md
      (payments.md section lines 455-486 — the exact structure to mirror)
  </read_first>
  <action>
    1. Create `docs/src/features/payments.md` mirroring `stripe.md`'s structure and
       voice (neutral architectural documentation, no marketing language — per
       CLAUDE.md). Required sections:
       - `# Payments` + one-paragraph intro: ferro-payments adds polymorphic payment
         intent tracking + self-healing recovery on top of ferro-stripe.
       - `## Quick Start` — the three-line consumer story:
         (a) register `ferro_payments::migration::CreatePaymentIntentsTable` in the
             consumer migrator;
         (b) build `PaymentService::new(...)` over the consumer's `BillableLoader` and
             call `ferro_payments::wire_dispatcher(&mut dispatcher, service.clone())`;
         (c) schedule the two reaper Jobs via the consumer's ferro-queue cron —
             `ReleaseExpiredPaymentIntents::new(service.clone())` and
             `ReconcileRefundsInFlight::new(service.clone())` (default 1h cadence,
             configurable purely by the cron expression — no crate-side knob).
       - `## Recovery model` — explain the two money-stuck edge cases and how they
         self-heal:
         * Unpaid expiry → `ReleaseExpiredPaymentIntents` releases reserved intents
           whose hold expired (per-intent transaction, failure-isolated).
         * Refund-in-flight → on `charge.refunded` the webhook resolves; if a refund
           gets stuck (snapshot set, never confirmed), `ReconcileRefundsInFlight` polls
           Stripe and resolves succeeded refunds. State the double-refund guard
           explicitly: reconcile is an IDEMPOTENT QUERY — a failed refund is logged
           for operator action, NEVER auto-retried.
       - `## Environment Variables` — table referencing `STRIPE_SECRET_KEY` /
         `STRIPE_WEBHOOK_SECRET`, cross-referencing the Stripe page.
       - A closing cross-link: `See also [Stripe Integration](stripe.md).`

       Code blocks must be illustrative consumer snippets (Rust). They are mdBook
       fenced blocks (` ```rust `), NOT doctests in the crate — they do not need to
       compile under `cargo test`. Keep them faithful to the real public signatures
       (constructor + the two `::new(service.clone())` schedule calls).

    2. In `docs/src/SUMMARY.md`, add `- [Payments](features/payments.md)` immediately
       after the `- [Stripe](features/stripe.md)` line (line 49).

    3. In `docs/src/features/stripe.md`, add a cross-link to the new page (e.g. a line
       under the intro or in a "Related" note: `For the polymorphic payment-intent
       layer built on top of this integration, see [Payments](payments.md).`).

    4. Verify the Rust-docs CI gate stays green: run `cargo doc -p ferro-payments
       --no-deps` and the workspace `cargo doc --all-features --no-deps` with warnings
       as errors (set `RUSTDOCFLAGS="-D warnings"`). This catches broken intra-doc
       links in the crate's own rustdoc (the mdBook prose lives outside cargo doc, but
       the publish gate is `cargo doc -Dwarnings` per D-15 / CLAUDE.md, so prove it
       passes now). Do NOT introduce any new rustdoc warnings in the crate source.
  </action>
  <verify>
    <automated>RUSTDOCFLAGS="-D warnings" cargo doc -p ferro-payments --no-deps</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/src/features/payments.md`
    - `grep -q "wire_dispatcher" docs/src/features/payments.md`
    - `grep -q "ReleaseExpiredPaymentIntents" docs/src/features/payments.md`
    - `grep -q "ReconcileRefundsInFlight" docs/src/features/payments.md`
    - `grep -qi "double-refund\|never auto-retr\|idempotent" docs/src/features/payments.md`
    - `grep -q "features/payments.md" docs/src/SUMMARY.md`
    - `grep -q "payments.md" docs/src/features/stripe.md`
    - `RUSTDOCFLAGS="-D warnings" cargo doc -p ferro-payments --no-deps` exits 0
  </acceptance_criteria>
  <done>
    payments.md exists with the three-line Quick Start, the recovery model incl. the
    explicit double-refund guard, and the env-var table; SUMMARY.md links it after the
    Stripe entry; stripe.md cross-links to it; the crate's rustdoc builds with warnings
    as errors.
  </done>
</task>

</tasks>

<verification>
payments.md present with all required sections; SUMMARY + stripe.md links in place;
`cargo doc -Dwarnings` green for ferro-payments. No new rustdoc warnings introduced.
</verification>

<success_criteria>
The consumer-facing payments page leads with the one-call recovery story and
documents the auto-refund + reconcile model (with the double-refund guard stated
explicitly); the page is wired into SUMMARY and cross-linked from stripe.md; the
Rust-docs publish gate stays green.
</success_criteria>

<output>
After completion, create `.planning/phases/236-ferro-payments-reapers-and-publish-0-1-0/236-06-SUMMARY.md`
</output>
