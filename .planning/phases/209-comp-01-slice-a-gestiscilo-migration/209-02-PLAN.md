---
phase: 209-comp-01-slice-a-gestiscilo-migration
plan: 02
type: execute
wave: 2
depends_on: [209-01-PLAN]
files_modified:
  - /Users/alberto/repositories/gestiscilo-it/app/src/controllers/staff/list.rs
  - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-staff-browse.md
autonomous: false
requirements: [COMP-01]
user_setup: []

must_haves:
  truths:
    - "The Staff list controller renders via ServiceDef + JsonUiRenderer instead of JsonUi::render_file"
    - "The old JsonUi::render_file call for the Staff list is deleted (SC#1)"
    - "Before/after screenshots prove functional parity for the Staff list primary use case (SC#2)"
    - "The Staff migration is its own gestiscilo branch, merged to gestiscilo master before the next entity's branch opens (SC#1, SC#3, D-03)"
    - "The migrated handler preserves the original tenant scoping and auth guard"
  artifacts:
    - path: "/Users/alberto/repositories/gestiscilo-it/app/src/controllers/staff/list.rs"
      provides: "projection-driven Staff list index handler"
      contains: "JsonUiRenderer"
    - path: ".planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-staff-browse.md"
      provides: "filled Browse equivalence record with before/after screenshots"
      contains: "PASS"
  key_links:
    - from: "staff/list.rs index handler"
      to: "JsonUiRenderer::render"
      via: "ServiceDef -> derive_intents -> render -> merge_data -> JsonUi::render"
      pattern: "JsonUiRenderer"
    - from: "staff/list.rs index handler"
      to: "resolve_tenant / find_all_for_tenant"
      via: "tenant scoping preserved unchanged"
      pattern: "find_all_for_tenant"
---

<objective>
Migrate the gestiscilo Staff list (Browse intent) from `JsonUi::render_file("src/views/staff/list.json", data)` to projection-driven rendering via `ServiceDef` + `JsonUiRenderer`. This is the FIRST migration — it establishes the wiring pattern the other two entities follow (Orders, Statistics).

Per D-03 this is its own short-lived gestiscilo branch, merged to gestiscilo master before Plan 03 (Orders) opens its branch. The render path is replaced (the `render_file` call is DELETED, SC#1); the controller's tenant scoping, auth, and per-row data construction (signed avatar URLs, initials) are preserved unchanged.

Purpose: validate that a clean, flat Browse entity migrates to the projection abstraction with functional parity. Staff is the structurally cleanest exemplar (LOW bespoke-HTML risk).
Output: staff/list.rs renders via projection; old render_file deleted; EQUIV-staff-browse.md filled with before/after screenshots + checklist; gestiscilo branch merged to master.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-CONTEXT.md
@.planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-RESEARCH.md
@.planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-VALIDATION.md

<interfaces>
<!-- Projection render path — verified at ferro-rs 0.2.54 (RESEARCH §2.3, §3). -->
<!-- All imported through the ferro:: re-export (projections feature, enabled in Plan 01). -->

use ferro::{
    derive_intents, DataType, FieldMeaning, IntentHint, Intent,
    JsonUiRenderer, ServiceDef, VisualContext,
};
use ferro_projections::render::Renderer;   // brings .render() into scope

// Render pipeline:
let intents = derive_intents(&service);
let spec = JsonUiRenderer.render(&service, &intents, &VisualContext::default())?;  // Spec
let merged = spec.merge_data(json!({ ... }));   // shallow-merge runtime data (rows, sidebar, flash)
JsonUi::render(&merged, &merged.data.clone())   // Spec -> HTML  (NOT render_file)

// merge_data is already used in gestiscilo dashboard.rs:595:
//   let spec = (*arc_spec).clone().merge_data(data);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Open Staff branch and swap render_file for projection render</name>
  <files>/Users/alberto/repositories/gestiscilo-it/app/src/controllers/staff/list.rs</files>
  <read_first>
    - /Users/alberto/repositories/gestiscilo-it/app/src/controllers/staff/list.rs (the full index handler; line 110 is the render_file call to delete)
    - /Users/alberto/repositories/gestiscilo-it/app/src/views/staff/list.json (the before spec — for comparison only; do NOT replicate it inside the builder, RESEARCH Pitfall 4)
    - /Users/alberto/repositories/gestiscilo-it/app/src/models/entities/staff.rs (model field shapes)
    - /Users/alberto/repositories/gestiscilo-it/app/src/controllers/dashboard.rs (line 595 — the established merge_data pattern)
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-RESEARCH.md §2.2 (before), §2.3 (after — copy this verbatim), §2.4 (transformation points), §5 Gap 2 (signed-URL workaround), Pitfall 3 (IntentHint::Primary(Browse) required)
  </read_first>
  <action>
    Work in the gestiscilo repo (`/Users/alberto/repositories/gestiscilo-it/app`). First open a short-lived branch for this entity ONLY (D-03):
    `git -C /Users/alberto/repositories/gestiscilo-it/app checkout -b feat/209-staff-projection-migration`

    In `src/controllers/staff/list.rs`, rewrite the `index` handler to render via projection. Keep ALL of: `resolve_tenant()`, the auth/sidebar/notification/flash setup, `Staff::find_all_for_tenant(business.id)`, the per-row JSON construction (signed avatar URLs via `signed_url(key, 3600)`, `avatar_initial_color(s.id)` initials) — these are runtime data and stay controller-side (RESEARCH §5 Gap 2: presigned URLs have no ServiceDef hook; the per-row loop is preserved).

    Add at the top of the file:
    ```rust
    use ferro::{
        derive_intents, DataType, FieldMeaning, IntentHint, Intent,
        JsonUiRenderer, ServiceDef, VisualContext,
    };
    use ferro_projections::render::Renderer;
    ```

    Replace the body from the `let data = json!({...})` + `JsonUi::render_file(...)` lines with the projection path (RESEARCH §2.3 verbatim, adapted to the actual flash/sidebar/_sse_url keys this handler already builds):
    ```rust
    // 1. Build the Staff ServiceDef (Browse). bio+avatar pull toward Focus,
    //    so force Browse with an explicit IntentHint (RESEARCH Pitfall 3 / Risk 1).
    let service = ServiceDef::new("staff")
        .display_name("Staff")
        .read_only_field("id", DataType::Integer, FieldMeaning::Identifier)
        .field("name", DataType::String, FieldMeaning::EntityName)
        .optional_field("bio", DataType::String, FieldMeaning::FreeText)
        .optional_field("avatar_url", DataType::String, FieldMeaning::ImageUrl)
        .field("sort_order", DataType::Integer, FieldMeaning::Quantity)
        .field("active", DataType::Boolean, FieldMeaning::Status)
        .read_only_field("created_at", DataType::DateTime, FieldMeaning::CreatedAt)
        .intent_hint(IntentHint::Primary(Intent::Browse));

    // 2. Derive intents and render the structural spec.
    let intents = derive_intents(&service);
    let spec = JsonUiRenderer
        .render(&service, &intents, &VisualContext::default())
        .map_err(|e| /* existing error_response helper */ )?;

    // 3. Merge runtime data (same keys the before handler passed to render_file).
    let merged = spec.merge_data(json!({
        "_sidebar": /* unchanged */,
        "_header":  /* unchanged */,
        "_sse_url": /* unchanged */,
        "staff": rows_json,           // the existing per-row Vec with signed URLs + initials
        "has_staff": !is_empty,
        "is_empty": is_empty,
        "flash_message": flash_message,
        "flash_variant": flash_variant,
    }));

    let render_data = merged.data.clone();
    JsonUi::render(&merged, &render_data)   // <- replaces JsonUi::render_file
    ```
    DELETE the original `JsonUi::render_file("src/views/staff/list.json", data)` call entirely (SC#1 — not a fallback, not commented out; CLAUDE.md "delete old code completely").

    Match the exact error-response helper this controller already uses for the `.map_err` arm (read the file — do not invent a new error helper).

    If `JsonUiRenderer`'s default Browse layout differs from the before view such that a runtime key the spec expects is absent (e.g. the rendered spec's data-binding paths differ from the hand-authored `staff/list.json` paths), record the delta in EQUIV-staff-browse.md item 5 (intentional visual deltas) rather than reshaping the spec to byte-match (D-02: functional parity, not pixel-identity).

    Then build in the gestiscilo repo (CPU-intensive, run alone): `cargo build` from the gestiscilo root.
  </action>
  <verify>
    <automated>test "$(grep -c 'JsonUi::render_file' /Users/alberto/repositories/gestiscilo-it/app/src/controllers/staff/list.rs)" = "0" && grep -q 'JsonUiRenderer' /Users/alberto/repositories/gestiscilo-it/app/src/controllers/staff/list.rs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "JsonUi::render_file" /Users/alberto/repositories/gestiscilo-it/app/src/controllers/staff/list.rs` returns 0 (deletion proof, SC#1)
    - `grep -c "JsonUiRenderer" .../staff/list.rs` returns ≥ 1
    - `grep -c "IntentHint::Primary(Intent::Browse)" .../staff/list.rs` returns 1 (Browse forced)
    - `grep -c "find_all_for_tenant" .../staff/list.rs` returns ≥ 1 (tenant scoping preserved)
    - `grep -c "resolve_tenant" .../staff/list.rs` returns ≥ 1 (auth/tenant guard preserved)
    - `cargo build` from the gestiscilo repo root exits 0
    - Branch `feat/209-staff-projection-migration` exists (`git -C .../app rev-parse --abbrev-ref HEAD` == that branch)
  </acceptance_criteria>
  <done>staff/list.rs index renders via ServiceDef+JsonUiRenderer with IntentHint::Primary(Browse); render_file deleted; tenant scoping unchanged; gestiscilo builds on a dedicated branch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture before/after screenshots and fill the Staff equivalence record</name>
  <files>.planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-staff-browse.md</files>
  <read_first>
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-staff-browse.md (the stub from Plan 01)
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-VALIDATION.md "Per-Entity Functional Checklist" + "Manual-Only Verifications"
  </read_first>
  <what-built>
    The Staff list now renders via the projection pipeline (Task 1). The render_file call is deleted; tenant scoping preserved. The ferro intent test `staff_browse_intent` (Plan 01) already proves Browse derivation.
  </what-built>
  <how-to-verify>
    The user runs the gestiscilo server (per CLAUDE.md: server is always run by the user). Then, with Chrome DevTools MCP (`mcp__chrome-devtools*`):
    1. BEFORE: check out gestiscilo master (pre-migration) or use a saved capture; navigate to the Staff list page; screenshot. (If master is already switched, capture from the recorded before-state screenshot.)
    2. AFTER: on `feat/209-staff-projection-migration`, navigate to the same Staff list page; screenshot.
    3. Fill `EQUIV-staff-browse.md` against the five-assertion checklist:
       - (1) Data fields shown: every column in before appears in after (values match; names may differ).
       - (2) Actions available: View, Edit, Toggle Active, Delete all reachable after.
       - (3) Primary-use-case flow: clicking a staff row opens its detail (the most common operator action) works.
       - (4) Intent confirmation: cite `cargo test -p ferro-projections --test catalog staff_browse_intent` (green from Plan 01).
       - (5) Intentional visual deltas: list every layout/markup difference (projection composes intent templates → markup differs by design). UNLISTED differences block the merge.
    4. Record any abstraction-gap friction (e.g. the signed-URL workaround, RESEARCH §5 Gap 2) in the "Abstraction gaps surfaced" section — this feeds the phase-close weakness note (SC#5).
    5. Confirm: a FUNCTIONAL regression (missing data, missing action, broken primary flow) BLOCKS the merge (D-02). Only intentional visual deltas are acceptable.
  </how-to-verify>
  <action>
    Drive the how-to-verify steps: with the user's running gestiscilo server, capture before/after Staff-list screenshots via Chrome DevTools MCP, then fill EQUIV-staff-browse.md against the five-assertion checklist and record any signed-URL gap friction. Claude performs the capture and fills the record; the user confirms functional parity at the checkpoint.
  </action>
  <verify>
    <automated>grep -q "PASS" .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-staff-browse.md && echo "record updated (human confirms parity)"</automated>
  </verify>
  <acceptance_criteria>
    - EQUIV-staff-browse.md items 1–4 each marked PASS (or the merge is blocked and the regression documented)
    - Item 5 lists the intentional visual deltas explicitly
    - Both a before and an after screenshot are referenced in the Evidence section
    - The gestiscilo migration commit/PR link is filled in the header
  </acceptance_criteria>
  <done>EQUIV-staff-browse.md is filled with passing functional checks (items 1–4) and before/after screenshots, or a blocking functional regression is documented.</done>
  <resume-signal>Type "approved" once the equivalence record is filled and all functional checks pass, or describe the functional regression that blocks the merge.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Merge the Staff migration to gestiscilo master before the next entity</name>
  <files>/Users/alberto/repositories/gestiscilo-it/app/src/controllers/staff/list.rs</files>
  <read_first>
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-CONTEXT.md D-03, D-04
    - ROADMAP Phase 209 SC#1, SC#3
  </read_first>
  <what-built>
    The Staff migration branch `feat/209-staff-projection-migration` is complete and functionally verified (Task 2). It must merge to gestiscilo master BEFORE Plan 03 (Orders) opens its branch — strictly one entity per merge (D-03), and no branch alive > 2 weeks (SC#3).
  </what-built>
  <how-to-verify>
    The user (merge authority) merges `feat/209-staff-projection-migration` into gestiscilo master:
    1. Ensure the gestiscilo commit gate passed on the branch: `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo build` (serialize CPU — one at a time).
    2. Merge to gestiscilo master (PR or fast-forward, user's choice).
    3. Confirm NO ferro master API change was made while this branch was open (D-04 / SC#3) — ferro source is untouched this wave.
    4. Record the merge commit SHA in EQUIV-staff-browse.md header.
  </how-to-verify>
  <action>
    Present the merge checkpoint to the user (merge authority). Confirm the gestiscilo commit gate passed on the branch, then the user merges `feat/209-staff-projection-migration` to gestiscilo master. Record the merge SHA in EQUIV-staff-browse.md. This blocks Plan 03 until Staff is merged (D-03 one-per-merge).
  </action>
  <verify>
    <automated>test "$(git -C /Users/alberto/repositories/gestiscilo-it/app rev-parse --abbrev-ref HEAD)" = "master" && grep -c "JsonUi::render_file" /Users/alberto/repositories/gestiscilo-it/app/src/controllers/staff/list.rs | grep -q '^0$' && echo "merged: render_file gone on master"</automated>
  </verify>
  <acceptance_criteria>
    - `feat/209-staff-projection-migration` is merged into gestiscilo master
    - gestiscilo master at the merge SHA: `grep -c "JsonUi::render_file" src/controllers/staff/list.rs` returns 0
    - EQUIV-staff-browse.md header references the merge commit SHA
    - No ferro master API change occurred during the branch's lifetime (D-04)
  </acceptance_criteria>
  <done>The Staff migration is on gestiscilo master, render_file is gone on master, and the merge SHA is recorded; Plan 03 may open its branch.</done>
  <resume-signal>Type "merged" once the Staff migration is on gestiscilo master and the SHA is recorded; Plan 03 (Orders) may then open its branch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → gestiscilo Staff list handler | Existing authenticated, tenant-scoped request path. This migration swaps the render mechanism only; the boundary is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-209-02-01 | Elevation of privilege | staff/list.rs index — dropped tenant scope | mitigate | Acceptance criteria assert `resolve_tenant` and `find_all_for_tenant(business.id)` remain in the migrated handler; the row query is unchanged, so tenant isolation is preserved. The migration touches only the render call. |
| T-209-02-02 | Information disclosure | projection render exposes fields the before view filtered | mitigate | The ServiceDef declares exactly the fields the before view showed (id, name, bio, avatar_url, sort_order, active, created_at); runtime data is the SAME `rows_json` the before handler built (no new columns selected). EQUIV checklist item 1 verifies no field beyond the before view appears. No raw DB row is passed to the renderer. |
| T-209-02-03 | Information disclosure | presigned avatar URL scope/TTL change | accept | The signed-URL loop (`signed_url(key, 3600)`) is preserved verbatim controller-side; TTL and key derivation are unchanged by the render swap. |
</threat_model>

<verification>
- `grep -c "JsonUi::render_file" .../staff/list.rs` == 0 (SC#1 deletion).
- `JsonUiRenderer` and `IntentHint::Primary(Intent::Browse)` present in the handler.
- `resolve_tenant` + `find_all_for_tenant` preserved (auth/tenant-scope preservation — threat model).
- gestiscilo `cargo build` exits 0 on the branch.
- EQUIV-staff-browse.md: items 1–4 PASS, item 5 deltas listed, before+after screenshots referenced, merge SHA recorded.
- Branch merged to gestiscilo master before Plan 03 opens (D-03).
- `cargo test -p ferro-projections --test catalog staff_browse_intent` green (from Plan 01).
</verification>

<success_criteria>
- Staff list renders via projection; render_file deleted (SC#1).
- Functional parity proven by before/after screenshots (SC#2).
- Tenant scoping and auth preserved (threat model).
- Migration merged to gestiscilo master as its own slice before the next entity (SC#1, SC#3, D-03).
</success_criteria>

<output>
After completion, create `.planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-02-SUMMARY.md`
</output>
