---
phase: 209-comp-01-slice-a-gestiscilo-migration
plan: 02
type: execute
wave: 2
depends_on: [209-01-PLAN]
external_depends_on: "gestiscilo migration phase (see GESTISCILO-MIGRATION-BRIEF.md) — the three entity migrations must be merged to gestiscilo master before this plan's evidence can be recorded"
files_modified:
  - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-staff-browse.md
  - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-orders-process.md
  - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-stats-summarize.md
  - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/WEAKNESS-NOTE.md
  - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/PUBLISH-DECISION.md
autonomous: false
requirements: [COMP-01]
user_setup: []

must_haves:
  truths:
    - "Each of the three equivalence records is filled with before/after screenshots and the five-assertion functional checklist, referencing the gestiscilo migration merge SHA (SC#2)"
    - "A 'what the migration revealed' weakness note names at least one real abstraction gap (SC#5 — empty note fails the phase)"
    - "A publish decision records which D-06 path was taken (default: zero ferro changes, no publish) (SC#4)"
    - "Branch discipline is confirmed from the gestiscilo phase history: sequential one-per-merge, no branch > 2 weeks, no ferro master API change while a migration branch was open (SC#1, SC#3, D-04)"
    - "Ferro records evidence only — this plan modifies no gestiscilo file"
  artifacts:
    - path: ".planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-staff-browse.md"
      provides: "filled Browse equivalence record"
      contains: "PASS"
    - path: ".planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-orders-process.md"
      provides: "filled Process equivalence record"
      contains: "PASS"
    - path: ".planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-stats-summarize.md"
      provides: "filled Summarize equivalence record"
      contains: "PASS"
    - path: ".planning/phases/209-comp-01-slice-a-gestiscilo-migration/WEAKNESS-NOTE.md"
      provides: "the SC#5 'what the migration revealed' deliverable"
      contains: "Abstraction gap"
      min_lines: 20
    - path: ".planning/phases/209-comp-01-slice-a-gestiscilo-migration/PUBLISH-DECISION.md"
      provides: "the D-06/SC#4 publish-path record"
      contains: "Publish path"
  key_links:
    - from: "WEAKNESS-NOTE.md"
      to: "EQUIV-staff-browse.md / EQUIV-orders-process.md / EQUIV-stats-summarize.md"
      via: "synthesizes the per-entity 'Abstraction gaps surfaced' sections"
      pattern: "Gap"
---

<objective>
Record the COMP-01 validation evidence and sign off the slice. This plan is **ferro-side validation only** — it consumes the outputs of the gestiscilo migration (performed in the gestiscilo repo per `GESTISCILO-MIGRATION-BRIEF.md`) and turns them into the auditable evidence that backs the v1.0 criterion-#2 claim. It produces no code and modifies no gestiscilo file.

Three deliverables:
1. **Equivalence records (SC#2)** — fill the three EQUIV stubs (from Plan 01) with before/after screenshots and the five-assertion functional checklist for each migrated entity, referencing the gestiscilo migration merge SHA. Before/after capture uses Chrome DevTools MCP against the user's running gestiscilo server (CLAUDE.md: the server is run by the user).
2. **Weakness note (SC#5)** — synthesize the per-entity "Abstraction gaps surfaced" sections into a single "what the migration revealed" note naming ≥1 real abstraction gap. An empty note FAILS the phase (SC#5); finding nothing is a red flag, not a success. RESEARCH §5 forecasts three concrete candidates (SVG chart with no FieldMeaning — Gap 1; computed presigned URLs with no ServiceDef hook — Gap 2; kanban column labels with no projection representation — Gap 3).
3. **Publish decision (SC#4 / D-06)** — record which path the slice took. Default expectation is ZERO ferro source changes → no publish (the migration consumes the existing 0.2.54 surface). A single publish (SC#4) is exercised ONLY if a discovered gap forced a minimal ferro fix all three slices re-verified against. Also confirm branch discipline (SC#1/#3) from the gestiscilo phase history.

**Gating:** this plan can only be completed after the gestiscilo migration phase has merged all three entities to gestiscilo master. Until then it stays open — that external dependency is recorded in `external_depends_on`.
Output: three filled EQUIV records, WEAKNESS-NOTE.md (≥1 named gap), PUBLISH-DECISION.md (path + branch discipline).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-CONTEXT.md
@.planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-RESEARCH.md
@.planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-VALIDATION.md
@.planning/phases/209-comp-01-slice-a-gestiscilo-migration/GESTISCILO-MIGRATION-BRIEF.md
@.planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-staff-browse.md
@.planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-orders-process.md
@.planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-stats-summarize.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Capture before/after screenshots and fill the three equivalence records (SC#2)</name>
  <files>
    .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-staff-browse.md,
    .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-orders-process.md,
    .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-stats-summarize.md
  </files>
  <read_first>
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-staff-browse.md (stub from Plan 01)
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-orders-process.md (stub from Plan 01)
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-stats-summarize.md (stub from Plan 01)
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-VALIDATION.md "Per-Entity Functional Checklist" + "Manual-Only Verifications"
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/GESTISCILO-MIGRATION-BRIEF.md (which entity maps to which controller/view; the three migration merge SHAs to reference)
  </read_first>
  <what-built>
    The gestiscilo migration phase has migrated the three entities (Staff/Browse, Orders/Process, Statistics/Summarize) to projection-driven rendering and merged each to gestiscilo master. The ferro intent tests (Plan 01) already prove each ServiceDef derives the expected primary intent. This task records the visual + functional equivalence evidence.
  </what-built>
  <how-to-verify>
    The user runs the gestiscilo server (CLAUDE.md: server is always run by the user). For each of the three entities, with Chrome DevTools MCP (`mcp__chrome-devtools*`):
    1. BEFORE: from a pre-migration commit/screenshot of the entity's view, capture the before state. (If the gestiscilo phase saved before-screenshots, reuse them.)
    2. AFTER: on gestiscilo master (post-migration), navigate to the same view; screenshot.
    3. Fill the entity's `EQUIV-*.md` against the five-assertion checklist (D-02 — functional parity, not pixel-identity):
       - (1) Data fields shown: every column in before appears in after (values match; names may differ).
       - (2) Actions available: every before action (View, Edit, Delete, status transitions) reachable after.
       - (3) Primary-use-case flow: the most common operator action works in the migrated view.
       - (4) Intent confirmation: cite the entity's `cargo test -p ferro-projections --test catalog {test_name}` (green from Plan 01).
       - (5) Intentional visual deltas: list every layout/markup difference (projection composes intent templates → markup differs by design). UNLISTED differences are treated as regressions.
    4. Record any abstraction-gap friction surfaced by the gestiscilo migration in each file's "Abstraction gaps surfaced" section (this feeds Task 2's weakness note, SC#5).
    5. Reference the gestiscilo migration merge SHA in each record's header.
    A FUNCTIONAL regression (missing data, missing action, broken primary flow) is a finding to document, not a thing ferro fixes here — ferro records evidence; remediation is the gestiscilo phase's.
  </how-to-verify>
  <action>
    With the user's running gestiscilo server, capture before/after screenshots for the three migrated views via Chrome DevTools MCP and fill the three EQUIV records against the five-assertion checklist, referencing each gestiscilo merge SHA. Claude performs the captures and fills the records; the user confirms functional parity at the checkpoint. Record gap friction in each record's "Abstraction gaps surfaced" section.
  </action>
  <verify>
    <automated>for f in EQUIV-staff-browse EQUIV-orders-process EQUIV-stats-summarize; do grep -q "PASS" ".planning/phases/209-comp-01-slice-a-gestiscilo-migration/$f.md" || { echo "INCOMPLETE $f"; exit 1; }; done && echo "records updated (human confirms parity)"</automated>
  </verify>
  <acceptance_criteria>
    - Each EQUIV record has items 1–4 marked PASS (or a functional regression is documented for that entity)
    - Each record's item 5 lists the intentional visual deltas explicitly
    - Each record references both a before and an after screenshot in its Evidence section
    - Each record's header references the gestiscilo migration merge SHA
  </acceptance_criteria>
  <done>All three EQUIV records are filled with passing functional checks and before/after screenshots (or documented functional regressions), each linked to its gestiscilo merge SHA.</done>
  <resume-signal>Type "approved" once the three equivalence records are filled and functional parity is confirmed (or describe the functional regressions found).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Synthesize the "what the migration revealed" weakness note (SC#5)</name>
  <files>.planning/phases/209-comp-01-slice-a-gestiscilo-migration/WEAKNESS-NOTE.md</files>
  <read_first>
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-staff-browse.md ("Abstraction gaps surfaced")
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-orders-process.md ("Abstraction gaps surfaced")
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-stats-summarize.md ("Abstraction gaps surfaced")
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-RESEARCH.md §5 (the three forecast gaps + their suggested weakness-note wording)
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-CONTEXT.md D-05 (note-and-workaround; empty note fails)
  </read_first>
  <action>
    Create `WEAKNESS-NOTE.md` in the phase directory. Synthesize the gaps recorded across the three EQUIV files into a single auditable note. Voice: neutral architectural documentation (per CLAUDE.md repository-document discipline — this file is committed).

    Structure:
    ```markdown
    # Phase 209 — What the Migration Revealed (SC#5)

    > First real-world validation of the projection/intent abstraction against a production
    > codebase (gestiscilo). Three entities migrated: Staff (Browse), Orders (Process),
    > Statistics (Summarize). This note records the abstraction gaps surfaced. An empty note
    > fails the phase (SC#5); finding nothing is a red flag, not a success.

    ## Abstraction gaps surfaced

    ### Gap N: <name>
    - **Entity / intent:** <which migration hit it>
    - **What happened:** <the concrete friction — a ServiceDef field with no clean mapping, a renderer output needing a workaround>
    - **Why it is structural:** <why the current ServiceDef/FieldMeaning/renderer surface cannot express it>
    - **Workaround applied (gestiscilo-side, D-05):** <the smallest workaround used to keep moving>
    - **Evidence:** <EQUIV file reference + screenshot/commit ref>
    - **Deferred ferro follow-up:** <the candidate ferro fix, explicitly deferred to a later v13.x slice per D-04/D-05 — NOT done here>

    ## Weak-signal findings (non-blocking observations)
    - <e.g. Staff Browse required an explicit IntentHint::Primary(Browse) because bio+avatar pull toward Focus — the Browse classification is forced, not structurally derived (RESEARCH Risk 1)>

    ## Assessment
    <1–2 paragraphs: does the projection/intent abstraction hold up against a real codebase for these three intents? Where is it structurally incomplete? This is the compressive-dimension validation signal.>
    ```

    Requirements:
    - At least ONE gap must be a real, named abstraction gap with evidence (SC#5). The Statistics SVG-chart gap (Gap 1) is the highest-confidence candidate; the Staff signed-URL gap (Gap 2) and the Orders kanban-column gap (Gap 3) are the others. Include every gap actually hit — do not pad with the forecast if it did not materialize, and do not omit a real one.
    - The Staff `IntentHint::Primary(Browse)` necessity is recorded as a weak-signal finding (a genuine "the structural signal alone did not classify correctly" observation).
    - Each ferro follow-up is explicitly DEFERRED (D-04/D-05) — this note does not fix ferro.
  </action>
  <verify>
    <automated>test "$(grep -c '^### Gap' .planning/phases/209-comp-01-slice-a-gestiscilo-migration/WEAKNESS-NOTE.md)" -ge 1 && grep -q "Assessment" .planning/phases/209-comp-01-slice-a-gestiscilo-migration/WEAKNESS-NOTE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### Gap' WEAKNESS-NOTE.md` returns ≥ 1 (at least one named abstraction gap — SC#5)
    - Each gap entry has "What happened", "Why it is structural", "Workaround applied", and "Deferred ferro follow-up" sub-fields
    - The note references at least one EQUIV file as evidence (`grep -c "EQUIV-" WEAKNESS-NOTE.md` ≥ 1)
    - The note has an "## Assessment" section
    - File is ≥ 20 lines (non-empty deliverable, SC#5)
  </acceptance_criteria>
  <done>WEAKNESS-NOTE.md synthesizes the per-entity gaps into ≥1 named, evidenced abstraction gap with deferred ferro follow-ups and a compressive-dimension assessment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Record the publish decision and confirm branch discipline (D-06, SC#1, SC#3, SC#4)</name>
  <files>.planning/phases/209-comp-01-slice-a-gestiscilo-migration/PUBLISH-DECISION.md</files>
  <read_first>
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-CONTEXT.md D-04, D-06
    - ROADMAP Phase 209 SC#1, SC#3, SC#4
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/WEAKNESS-NOTE.md (did any gap force a ferro fix?)
  </read_first>
  <what-built>
    The three migrations are merged on gestiscilo master and the weakness note is synthesized (Task 2). This task records the publish path (D-06) and confirms the branch-discipline success criteria — both require human knowledge of the git history across the ferro and gestiscilo repos.
  </what-built>
  <how-to-verify>
    Determine and record the publish path, and confirm branch discipline.

    1. PUBLISH PATH (D-06):
       - **Path A (default, expected):** Zero ferro source changes. The migrations consumed the existing 0.2.54 ServiceDef + JsonUiRenderer surface. No ferro version bump, no publish. All gaps were note-and-workaround (D-05), deferred to a later v13.x slice.
       - **Path B (only if forced):** A discovered gap forced a minimal, safe ferro fix that all three slices re-verified against → a SINGLE publish at slice end (not mid-series). Record the published version and confirm all three entities were re-verified against it.
       Confirm via `git -C /Users/alberto/repositories/albertogferrario/ferro log` that ferro source (outside `.planning/` and `ferro-projections/tests/catalog.rs`) was NOT changed during the slice for Path A. (The catalog.rs test addition from Plan 01 is a test fixture, not an API change — it does not trigger a publish.)

    2. BRANCH DISCIPLINE (SC#1, SC#3), read from the gestiscilo phase history:
       - The three migration branches were sequential — each merged to gestiscilo master before the next opened (Staff → Orders → Statistics).
       - No branch was alive > 2 weeks.
       - No ferro master API change was made while any gestiscilo migration branch was open (D-04).

    3. Write `PUBLISH-DECISION.md`:
       ```markdown
       # Phase 209 — Publish Decision (D-06 / SC#4) + Branch Discipline (SC#1, SC#3)

       ## Publish path
       **Path taken:** A (no publish) | B (single publish at slice end)
       **Rationale:** <why — default expectation is zero ferro changes per D-06>
       **ferro version migrated against:** 0.2.54  (all three slices)
       **Published version (Path B only):** <version + confirmation all three re-verified>

       ## Branch discipline (SC#1, SC#3)
       - Staff branch merged SHA: <sha> (opened <date>, merged <date>)
       - Orders branch merged SHA: <sha> (opened after Staff merged)
       - Statistics branch merged SHA: <sha> (opened after Orders merged)
       - Longest branch lifetime: <duration> (≤ 2 weeks: yes/no)
       - ferro master API unchanged while any branch open (D-04): yes/no
       ```
  </how-to-verify>
  <action>
    Drive the how-to-verify steps: inspect ferro and gestiscilo git history to determine the D-06 publish path (default A: zero ferro changes / no publish), then write PUBLISH-DECISION.md recording the path, the migrated-against version (0.2.54), and the three sequential merge SHAs with branch-lifetime confirmation. The user confirms the cross-repo git facts at the checkpoint.
  </action>
  <verify>
    <automated>grep -q "Publish path" .planning/phases/209-comp-01-slice-a-gestiscilo-migration/PUBLISH-DECISION.md && grep -q "Branch discipline" .planning/phases/209-comp-01-slice-a-gestiscilo-migration/PUBLISH-DECISION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - PUBLISH-DECISION.md exists with a "Publish path" stating A or B
    - For Path A: the rationale confirms zero ferro source changes and 0.2.54 as the migrated-against version
    - The three merge SHAs are recorded in sequential order
    - SC#3 confirmations present: longest branch ≤ 2 weeks; no ferro API change while a branch was open
  </acceptance_criteria>
  <done>PUBLISH-DECISION.md records the D-06 path, the migrated-against version, the three sequential merge SHAs, and the SC#1/SC#3 branch-discipline confirmations, or the blocking violation is documented.</done>
  <resume-signal>Type "approved" once PUBLISH-DECISION.md records the path and branch discipline is confirmed (or describe the SC#1/SC#3/SC#4 violation that blocks phase close).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan produces planning/documentation artifacts only (screenshots, equivalence records, notes). No code, no request path, no auth or data-scoping surface. No file in any other repo is modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-209-02-01 | Information disclosure | committed weakness/publish notes leak internal strategy | mitigate | Per CLAUDE.md repository-document discipline, both notes use neutral architectural voice (gaps as design observations, not internal strategy). Reviewer scans for trigger phrases before commit. |
| T-209-02-02 | Information disclosure | equivalence screenshots embed tenant/customer data | mitigate | Capture against non-sensitive seed/demo data where possible; if production-shaped data appears, crop or redact before committing the screenshot reference. |
</threat_model>

<verification>
- Three EQUIV records filled: items 1–4 PASS, item 5 deltas listed, before+after screenshots referenced, gestiscilo merge SHA recorded (SC#2).
- WEAKNESS-NOTE.md names ≥1 real abstraction gap with evidence and deferred follow-ups (SC#5).
- PUBLISH-DECISION.md records the D-06 path (default A: no publish) and the migrated-against version 0.2.54 (SC#4).
- Branch discipline confirmed: sequential merges, ≤ 2-week branch lifetime, no ferro API change while a branch was open (SC#1, SC#3, D-04).
- `git status` shows no modifications under `/Users/alberto/repositories/gestiscilo-it/` (repo boundary honored).
</verification>

<success_criteria>
- The render-equivalence evidence (three records) is complete and auditable (SC#2).
- The "what the migration revealed" weakness note is non-empty and names ≥1 real abstraction gap (SC#5).
- The publish decision is recorded; default is zero ferro changes / no publish (D-06); SC#4 single-publish path documented if forced.
- Branch discipline (SC#1, SC#3) confirmed across the gestiscilo and ferro repos.
- The COMP-01 Slice A validation signal is auditable end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-02-SUMMARY.md`
</output>
