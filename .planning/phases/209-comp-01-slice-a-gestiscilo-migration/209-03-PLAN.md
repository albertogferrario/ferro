---
phase: 209-comp-01-slice-a-gestiscilo-migration
plan: 03
type: execute
wave: 3
depends_on: [209-02-PLAN]
files_modified:
  - /Users/alberto/repositories/gestiscilo-it/app/src/controllers/cassa/orders.rs
  - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-orders-process.md
autonomous: false
requirements: [COMP-01]
user_setup: []

must_haves:
  truths:
    - "The Orders kanban index renders via ServiceDef + JsonUiRenderer instead of JsonUi::render_file"
    - "The old JsonUi::render_file call for the Orders kanban is deleted (SC#1)"
    - "Before/after screenshots prove functional parity for the Orders kanban primary use case (SC#2)"
    - "The Orders migration is its own gestiscilo branch, opened only after Staff merged, merged to master before Statistics opens (SC#1, SC#3, D-03)"
    - "The migrated handler preserves the original tenant scoping and per-card guarded action set"
  artifacts:
    - path: "/Users/alberto/repositories/gestiscilo-it/app/src/controllers/cassa/orders.rs"
      provides: "projection-driven Orders kanban index handler"
      contains: "JsonUiRenderer"
    - path: ".planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-orders-process.md"
      provides: "filled Process equivalence record with before/after screenshots"
      contains: "PASS"
  key_links:
    - from: "cassa/orders.rs index handler"
      to: "JsonUiRenderer::render"
      via: "ServiceDef(state_machine) -> derive_intents -> render -> merge_data(kanban_columns) -> JsonUi::render"
      pattern: "JsonUiRenderer"
    - from: "cassa/orders.rs index handler"
      to: "build_status_kanban_columns / find_all_kanban"
      via: "kanban column construction preserved controller-side (RESEARCH §5 Gap 3)"
      pattern: "kanban_columns"
---

<objective>
Migrate the gestiscilo Orders kanban (Process intent) from `JsonUi::render_file("src/views/cassa/orders_index.json", data)` to projection-driven rendering via `ServiceDef` + `JsonUiRenderer`. This is the SECOND migration — it validates the Process intent + guarded state-machine mapping, following the wiring pattern Plan 02 established.

Per D-03 this branch opens ONLY after the Staff migration (Plan 02) is merged to gestiscilo master, and merges to master before Plan 04 (Statistics) opens. The render_file call is DELETED (SC#1); the kanban column construction (`build_status_kanban_columns`), per-card guarded actions (`build_order_kanban_actions`), and tenant scoping are preserved controller-side.

Purpose: validate that a guarded branching state machine drives Process derivation, and surface the kanban column-label mapping as a real abstraction gap (RESEARCH §5 Gap 3) — candidate weakness-note material for SC#5.
Output: cassa/orders.rs renders via projection; old render_file deleted; EQUIV-orders-process.md filled; gestiscilo branch merged to master.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-CONTEXT.md
@.planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-RESEARCH.md
@.planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-VALIDATION.md

<interfaces>
<!-- Same projection render path as Plan 02 (RESEARCH §2.3). The Orders ServiceDef adds a -->
<!-- StateMachine to fire the Process derivation; the kanban column array stays in merge_data. -->

use ferro::{ derive_intents, DataType, FieldMeaning, JsonUiRenderer, ServiceDef, VisualContext };
use ferro_projections::render::Renderer;
use ferro_projections::{ StateMachine, StateDef, Transition, GuardDef };  // for the state machine

let intents = derive_intents(&service);
let spec = JsonUiRenderer.render(&service, &intents, &VisualContext::default())?;
let merged = spec.merge_data(json!({ "kanban_columns": cols, "_sidebar": ..., ... }));
JsonUi::render(&merged, &merged.data.clone())

// The Plan 01 fixture `orders_process()` in catalog.rs is the reference ServiceDef shape —
// reuse the same field meanings + state machine here in the controller.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Open Orders branch and swap render_file for projection render</name>
  <files>/Users/alberto/repositories/gestiscilo-it/app/src/controllers/cassa/orders.rs</files>
  <read_first>
    - /Users/alberto/repositories/gestiscilo-it/app/src/controllers/cassa/orders.rs (the `index` handler at line 158; render_file at line 213; the build_status_kanban_columns / build_order_kanban_actions / build_order_kanban_description helpers)
    - /Users/alberto/repositories/gestiscilo-it/app/src/views/cassa/orders_index.json (the 58-line before spec — comparison only, do NOT replicate, RESEARCH Pitfall 4)
    - /Users/alberto/repositories/gestiscilo-it/app/src/models/entities/orders.rs + /Users/alberto/repositories/gestiscilo-it/app/src/models/order_status.rs (the state machine: Confermato->InCorso->Rientrato->Chiuso, Annullato terminal; guards can_cancel/can_pay/is_editable)
    - ferro-projections/tests/catalog.rs real_world_slice_a::orders_process (the reference ServiceDef from Plan 01 — copy its field+state_machine shape)
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-RESEARCH.md §1.2, §2.3 (after pattern), §5 Gap 3 (kanban column workaround)
  </read_first>
  <action>
    Work in the gestiscilo repo. Confirm Plan 02 (Staff) is already merged to gestiscilo master before starting (D-03 — one entity per merge). Then open a dedicated branch for Orders ONLY:
    `git -C /Users/alberto/repositories/gestiscilo-it/app checkout master && git -C /Users/alberto/repositories/gestiscilo-it/app pull && git -C /Users/alberto/repositories/gestiscilo-it/app checkout -b feat/209-orders-projection-migration`

    In `src/controllers/cassa/orders.rs`, rewrite the `index` handler (line 158) to render via projection. Keep ALL of: tenant resolution, `Order::find_all_kanban(business.id)`, the per-row JSON mapping, and the helpers `build_status_kanban_columns()`, `build_order_kanban_actions()`, `build_order_kanban_description()`. The kanban column array stays controller-side — the state machine in the ServiceDef expresses the workflow for INTENT DERIVATION only; the display column labels (Italian "Confermati", "In corso", etc.) and ordering have no projection representation (RESEARCH §5 Gap 3 — record this gap).

    Add imports:
    ```rust
    use ferro::{ derive_intents, DataType, FieldMeaning, JsonUiRenderer, ServiceDef, VisualContext };
    use ferro_projections::render::Renderer;
    use ferro_projections::{ StateMachine, StateDef, Transition, GuardDef };
    ```

    Build the Order ServiceDef matching the Plan 01 `orders_process()` fixture (the guarded branching state machine is what fires Process — RESEARCH §1.2):
    ```rust
    let service = ServiceDef::new("order")
        .display_name("Order")
        .read_only_field("id", DataType::Integer, FieldMeaning::Identifier)
        .field("status", DataType::String, FieldMeaning::Status)
        .field("payment_state", DataType::String, FieldMeaning::Status)
        .read_only_field("total_cents", DataType::Integer, FieldMeaning::Money)
        .optional_field("customer_name", DataType::String, FieldMeaning::EntityName)
        .state_machine(
            StateMachine::new("order_lifecycle")
                // Confermato -> InCorso -> Rientrato -> Chiuso; Annullato terminal from any.
                // Confermato branches (advance + cancel); attach guards (can_cancel/can_pay)
                // so the guarded-transition Process signal fires. Use the SAME StateDef/
                // Transition/GuardDef shape as the Plan 01 orders_process() fixture.
        );
    ```
    Then the render path (identical structure to Plan 02), with the kanban columns merged as runtime data:
    ```rust
    let intents = derive_intents(&service);
    let spec = JsonUiRenderer
        .render(&service, &intents, &VisualContext::default())
        .map_err(|e| /* existing error helper */ )?;
    let cols = build_status_kanban_columns(/* existing args */);
    let merged = spec.merge_data(json!({
        "_sidebar": /* unchanged */,
        "_header":  /* unchanged */,
        "_sse_url": /* unchanged */,
        "kanban_columns": cols,        // RESEARCH §5 Gap 3: column labels stay controller-side
        /* any other keys the before handler passed */
    }));
    let render_data = merged.data.clone();
    JsonUi::render(&merged, &render_data)   // replaces render_file
    ```
    DELETE the original `JsonUi::render_file("src/views/cassa/orders_index.json", data)` call entirely (SC#1).

    Match the existing error-response helper this controller uses. If the rendered Process spec does not natively place a KanbanBoard bound to `kanban_columns`, document the structural delta in EQUIV-orders-process.md item 5 and (if needed) merge a supplementary KanbanBoard element binding — record this as the Gap 3 friction, do NOT reshape derive.rs.

    Build (CPU-intensive, run alone): `cargo build` from the gestiscilo root.
  </action>
  <verify>
    <automated>test "$(grep -c 'JsonUi::render_file' /Users/alberto/repositories/gestiscilo-it/app/src/controllers/cassa/orders.rs)" = "0" && grep -q 'JsonUiRenderer' /Users/alberto/repositories/gestiscilo-it/app/src/controllers/cassa/orders.rs && grep -q 'state_machine' /Users/alberto/repositories/gestiscilo-it/app/src/controllers/cassa/orders.rs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "JsonUi::render_file" /Users/alberto/repositories/gestiscilo-it/app/src/controllers/cassa/orders.rs` returns 0 (deletion proof, SC#1)
    - `grep -c "JsonUiRenderer" .../cassa/orders.rs` returns ≥ 1
    - `grep -c "state_machine" .../cassa/orders.rs` returns ≥ 1 (Process driver present)
    - `grep -c "kanban_columns" .../cassa/orders.rs` returns ≥ 1 (column construction preserved controller-side, Gap 3)
    - tenant scoping preserved: `grep -c "find_all_kanban" .../cassa/orders.rs` returns ≥ 1
    - `cargo build` from the gestiscilo repo root exits 0
    - Branch `feat/209-orders-projection-migration` exists and was created from up-to-date master (Staff already merged)
  </acceptance_criteria>
  <done>cassa/orders.rs index renders via ServiceDef(state_machine)+JsonUiRenderer; render_file deleted; kanban columns + tenant scoping preserved; gestiscilo builds on a dedicated branch opened after Staff merged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture before/after screenshots and fill the Orders equivalence record</name>
  <files>.planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-orders-process.md</files>
  <read_first>
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-orders-process.md (the stub from Plan 01)
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-VALIDATION.md "Per-Entity Functional Checklist" + "Manual-Only Verifications"
  </read_first>
  <what-built>
    The Orders kanban now renders via the projection pipeline with a guarded state machine (Task 1). The render_file call is deleted; tenant scoping and per-card guarded actions preserved. The ferro intent test `orders_process_intent` (Plan 01) already proves Process derivation.
  </what-built>
  <how-to-verify>
    The user runs the gestiscilo server. With Chrome DevTools MCP:
    1. BEFORE: navigate to the Orders kanban (`/cassa/orders` or equivalent) on master pre-migration; screenshot all four columns.
    2. AFTER: on `feat/209-orders-projection-migration`, navigate to the same page; screenshot.
    3. Fill `EQUIV-orders-process.md` against the five-assertion checklist:
       - (1) Data fields shown: order number, total, customer name, status appear per card.
       - (2) Actions available: per-card guarded actions (advance, revert, cancel, pay where allowed) reachable — verify the DropdownMenu items match the before state per card status.
       - (3) Primary-use-case flow: advancing an order through a status transition works (the canonical Process operator action).
       - (4) Intent confirmation: cite `cargo test -p ferro-projections --test catalog orders_process_intent` (green from Plan 01).
       - (5) Intentional visual deltas: list layout/markup differences. CRITICAL: the kanban column-label mapping (RESEARCH §5 Gap 3) is the expected friction — record exactly how the column labels/ordering had to stay controller-side.
    4. Record the Gap 3 friction (kanban column labels have no projection representation) in "Abstraction gaps surfaced" — this is prime SC#5 weakness-note material.
    5. A functional regression (a guarded action missing, a status transition broken) BLOCKS the merge (D-02).
  </how-to-verify>
  <action>
    Drive the how-to-verify steps: with the user's running gestiscilo server, capture before/after Orders-kanban screenshots via Chrome DevTools MCP, then fill EQUIV-orders-process.md against the five-assertion checklist and record the kanban column-label gap (Gap 3). Claude performs the capture and fills the record; the user confirms functional parity (all four columns, per-card guarded actions, status-advance flow) at the checkpoint.
  </action>
  <verify>
    <automated>grep -q "PASS" .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-orders-process.md && echo "record updated (human confirms parity)"</automated>
  </verify>
  <acceptance_criteria>
    - EQUIV-orders-process.md items 1–4 each marked PASS (or the merge is blocked and the regression documented)
    - Item 5 lists intentional visual deltas, including the kanban column-label handling
    - "Abstraction gaps surfaced" names the Gap 3 column-label friction (or whatever real gap was hit)
    - Both before and after screenshots referenced; migration commit/PR link filled
  </acceptance_criteria>
  <done>EQUIV-orders-process.md is filled with passing functional checks (items 1–4), the kanban column-label gap recorded, and before/after screenshots, or a blocking functional regression is documented.</done>
  <resume-signal>Type "approved" once the equivalence record is filled and all functional checks pass, or describe the functional regression that blocks the merge.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Merge the Orders migration to gestiscilo master before Statistics</name>
  <files>/Users/alberto/repositories/gestiscilo-it/app/src/controllers/cassa/orders.rs</files>
  <read_first>
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-CONTEXT.md D-03, D-04
    - ROADMAP Phase 209 SC#1, SC#3
  </read_first>
  <what-built>
    The Orders migration branch `feat/209-orders-projection-migration` is complete and functionally verified (Task 2). It must merge to gestiscilo master BEFORE Plan 04 (Statistics) opens its branch — strictly one entity per merge (D-03); no branch alive > 2 weeks (SC#3).
  </what-built>
  <how-to-verify>
    The user merges `feat/209-orders-projection-migration` into gestiscilo master:
    1. Confirm the gestiscilo commit gate passed: `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo build` (serialize CPU).
    2. Merge to gestiscilo master.
    3. Confirm NO ferro master API change was made while this branch was open (D-04 / SC#3).
    4. Record the merge commit SHA in EQUIV-orders-process.md header.
  </how-to-verify>
  <action>
    Present the merge checkpoint to the user (merge authority). Confirm the gestiscilo commit gate passed, then the user merges `feat/209-orders-projection-migration` to gestiscilo master. Record the merge SHA in EQUIV-orders-process.md. This blocks Plan 04 until Orders is merged (D-03 one-per-merge).
  </action>
  <verify>
    <automated>test "$(git -C /Users/alberto/repositories/gestiscilo-it/app rev-parse --abbrev-ref HEAD)" = "master" && grep -c "JsonUi::render_file" /Users/alberto/repositories/gestiscilo-it/app/src/controllers/cassa/orders.rs | grep -q '^0$' && echo "merged: render_file gone on master"</automated>
  </verify>
  <acceptance_criteria>
    - `feat/209-orders-projection-migration` is merged into gestiscilo master
    - gestiscilo master at merge SHA: `grep -c "JsonUi::render_file" src/controllers/cassa/orders.rs` returns 0
    - EQUIV-orders-process.md header references the merge commit SHA
    - No ferro master API change occurred during the branch's lifetime (D-04)
  </acceptance_criteria>
  <done>The Orders migration is on gestiscilo master, render_file is gone on master, and the merge SHA is recorded; Plan 04 may open its branch.</done>
  <resume-signal>Type "merged" once the Orders migration is on gestiscilo master and the SHA is recorded; Plan 04 (Statistics) may then open its branch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → gestiscilo Orders kanban handler | Existing authenticated, tenant-scoped request path. Migration swaps the render mechanism only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-209-03-01 | Elevation of privilege | orders.rs index — dropped tenant scope | mitigate | Acceptance criteria assert `find_all_kanban(business.id)` and tenant resolution remain; the order query is unchanged, preserving tenant isolation. |
| T-209-03-02 | Information disclosure | projection render exposes order fields the before view filtered | mitigate | The ServiceDef declares exactly the kanban-card fields (id, status, payment_state, total_cents, customer_name); runtime card data is the SAME mapped rows the before handler built. EQUIV item 1 verifies no extra field surfaces (e.g. stripe_session_id / email must NOT appear on a card if the before view hid them). |
| T-209-03-03 | Tampering | per-card guarded action set widened | mitigate | The guarded action set comes from `build_order_kanban_actions()` (unchanged); the state machine in the ServiceDef is for intent derivation only and does not drive the client-side action availability. EQUIV item 2 verifies the per-status action set matches the before state. |
</threat_model>

<verification>
- `grep -c "JsonUi::render_file" .../cassa/orders.rs` == 0 (SC#1 deletion).
- `JsonUiRenderer`, `state_machine`, `kanban_columns` present in the handler.
- `find_all_kanban` preserved (tenant-scope preservation — threat model).
- gestiscilo `cargo build` exits 0 on the branch.
- EQUIV-orders-process.md: items 1–4 PASS, item 5 deltas listed, Gap 3 recorded, screenshots referenced, merge SHA recorded.
- Branch opened after Staff merged and merged to master before Plan 04 (D-03).
- `cargo test -p ferro-projections --test catalog orders_process_intent` green (from Plan 01).
</verification>

<success_criteria>
- Orders kanban renders via projection; render_file deleted (SC#1).
- Functional parity proven by before/after screenshots (SC#2).
- Tenant scoping and guarded action set preserved (threat model).
- Migration merged to gestiscilo master as its own slice, after Staff and before Statistics (SC#1, SC#3, D-03).
- Kanban column-label gap recorded as SC#5 weakness-note material.
</success_criteria>

<output>
After completion, create `.planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-03-SUMMARY.md`
</output>
