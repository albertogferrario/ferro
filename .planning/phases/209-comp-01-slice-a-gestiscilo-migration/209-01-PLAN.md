---
phase: 209-comp-01-slice-a-gestiscilo-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-projections/tests/catalog.rs
  - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-staff-browse.md
  - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-orders-process.md
  - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-stats-summarize.md
autonomous: true
requirements: [COMP-01]
user_setup: []

must_haves:
  truths:
    - "A Rust test asserts the Staff ServiceDef derives Browse as primary intent"
    - "A Rust test asserts the Orders ServiceDef derives Process as primary intent"
    - "A Rust test asserts the SummaryStats ServiceDef derives Summarize as primary intent"
    - "The intent fixtures live in ferro-projections' own test crate — no gestiscilo file is touched by this plan"
    - "Three empty equivalence-record stubs exist in the phase directory, one per entity"
  artifacts:
    - path: "ferro-projections/tests/catalog.rs"
      provides: "real_world_slice_a module with three derive_intents assertions"
      contains: "mod real_world_slice_a"
    - path: ".planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-staff-browse.md"
      provides: "Browse equivalence-record stub"
      contains: "Data fields shown"
    - path: ".planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-orders-process.md"
      provides: "Process equivalence-record stub"
      contains: "Data fields shown"
    - path: ".planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-stats-summarize.md"
      provides: "Summarize equivalence-record stub"
      contains: "Data fields shown"
  key_links:
    - from: "ferro-projections/tests/catalog.rs real_world_slice_a"
      to: "derive_intents()"
      via: "direct call on each fixture ServiceDef"
      pattern: "derive_intents\\(&"
---

<objective>
Ferro-side validation scaffold for the COMP-01 Slice A gestiscilo migration. This phase is **validation-only** — the actual view migration is gestiscilo-repo work (see `GESTISCILO-MIGRATION-BRIEF.md`, executed in a gestiscilo session). Ferro 209 owns the abstraction proof and the equivalence evidence.

Two ferro-native deliverables, both in the ferro repo, no gestiscilo tree touched:

1. Add three canonical gestiscilo `ServiceDef` fixtures (staff / order / stats) with `derive_intents()` assertions to `ferro-projections/tests/catalog.rs` as a `real_world_slice_a` sub-module. These are the ferro-side automated proof that each candidate entity's structural shape derives its expected primary intent (Browse / Process / Summarize) — the compressive-dimension claim under test. The fixtures mirror the real gestiscilo model shapes (RESEARCH §1) but are pure ferro-projections test code.
2. Create three empty equivalence-record stub markdown files in this phase directory (D-08 deliverable location), filled in Plan 02 from the gestiscilo migration outputs.

These fixtures test `ferro-projections` directly and require NO gestiscilo change — the `projections` feature flag is a gestiscilo-consumer concern handled in the gestiscilo migration phase, not here.
Output: catalog.rs real_world_slice_a module green, three EQUIV stubs filed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-CONTEXT.md
@.planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-RESEARCH.md
@.planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-VALIDATION.md

<interfaces>
<!-- ServiceDef builder API (ferro-projections/src/service.rs) — verified present at 0.2.54. -->
<!-- Use these directly; no codebase exploration needed. -->

ServiceDef::new(name) -> Self
  .display_name(name) -> Self
  .field(name, DataType, FieldMeaning) -> Self          // writable
  .optional_field(name, DataType, FieldMeaning) -> Self  // writable, optional
  .read_only_field(name, DataType, FieldMeaning) -> Self
  .action(ActionDef::new("...")) -> Self
  .has_many(name, target) -> Self
  .intent_hint(IntentHint::Primary(Intent::Browse)) -> Self
  .state_machine(StateMachine::new("...")...) -> Self

derive_intents(&ServiceDef) -> Vec<IntentScore>   // [0].intent is the primary
IntentScore { intent: Intent, confidence: f64, signals: ... }

// Existing imports in catalog.rs (reuse — do NOT re-import):
use ferro_projections::derive_intents;
use ferro_projections::{
    ActionDef, Cardinality, DataType, FieldMeaning, GuardDef, Intent, IntentScore, ServiceDef,
    StateDef, StateMachine, Transition,
};
// IntentHint is NOT yet imported in catalog.rs — add it to the use list:
//   use ferro_projections::{... , IntentHint};

// Existing fixture pattern in catalog.rs: `pub mod fixtures { pub fn browse_catalog() -> ServiceDef {...} }`
// Existing StateMachine fixture: see catalog.rs:119 `process_workflow()` and :128 `StateMachine::new("approval_lifecycle")` for the exact transition-builder shape to copy for the Orders fixture.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add real_world_slice_a intent-assertion fixtures to catalog.rs</name>
  <files>ferro-projections/tests/catalog.rs</files>
  <behavior>
    - Test `staff_browse_intent`: `derive_intents(&staff_browse())[0].intent == Intent::Browse`
    - Test `orders_process_intent`: `derive_intents(&orders_process())[0].intent == Intent::Process`
    - Test `stats_summarize_intent`: `derive_intents(&stats_summarize())[0].intent == Intent::Summarize`
  </behavior>
  <read_first>
    - ferro-projections/tests/catalog.rs (existing fixtures module ~line 70; process_workflow fixture line 119; StateMachine builder usage line 128)
    - ferro-projections/src/derive.rs (signal weights — confirm the model→signal mapping in RESEARCH §1.1/§1.2/§1.3)
    - ferro-projections/src/service.rs (builder methods — confirmed: field/optional_field/read_only_field/intent_hint/state_machine)
    - ferro-projections/src/intent.rs (IntentHint::Primary; READ-ONLY, must not change)
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-RESEARCH.md §1 (the three model field shapes + which signals fire), Pitfall 3 (Staff needs IntentHint::Primary(Browse))
  </read_first>
  <action>
    Add a new `mod real_world_slice_a { ... }` sub-module at the end of `ferro-projections/tests/catalog.rs`. Reuse the file's existing `use` imports; add `IntentHint` to the `use ferro_projections::{...}` list (it is not yet imported).

    The system under test (`derive.rs`, `intent.rs`) is READ-ONLY — do not change it. If a fixture does not derive the expected intent, the fix is in the FIXTURE (field meanings / intent_hint), never in derive.rs.

    Inside the module, define three `pub fn` fixtures mirroring the verified gestiscilo model shapes (RESEARCH §1), then three `#[test]` functions.

    Staff fixture (Browse — note: bio+avatar pull toward Focus, so an explicit `IntentHint::Primary(Intent::Browse)` is REQUIRED per RESEARCH Risk 1 / Pitfall 3):
    ```rust
    pub fn staff_browse() -> ServiceDef {
        ServiceDef::new("staff")
            .display_name("Staff")
            .read_only_field("id", DataType::Integer, FieldMeaning::Identifier)
            .field("name", DataType::String, FieldMeaning::EntityName)
            .optional_field("bio", DataType::String, FieldMeaning::FreeText)
            .optional_field("avatar_url", DataType::String, FieldMeaning::ImageUrl)
            .field("sort_order", DataType::Integer, FieldMeaning::Quantity)
            .field("active", DataType::Boolean, FieldMeaning::Status)
            .read_only_field("created_at", DataType::DateTime, FieldMeaning::CreatedAt)
            .intent_hint(IntentHint::Primary(Intent::Browse))
    }
    ```

    Orders fixture (Process — guarded branching state machine drives Process; copy the StateMachine/Transition builder shape from catalog.rs:128 `process_workflow`):
    ```rust
    pub fn orders_process() -> ServiceDef {
        ServiceDef::new("order")
            .display_name("Order")
            .read_only_field("id", DataType::Integer, FieldMeaning::Identifier)
            .field("status", DataType::String, FieldMeaning::Status)
            .field("payment_state", DataType::String, FieldMeaning::Status)
            .read_only_field("total_cents", DataType::Integer, FieldMeaning::Money)
            .optional_field("customer_name", DataType::String, FieldMeaning::EntityName)
            .state_machine(
                StateMachine::new("order_lifecycle")
                    // States: Confermato -> InCorso -> Rientrato -> Chiuso; Annullato terminal.
                    // Confermato branches (advance OR cancel) — branching is the Process driver.
                    // Use StateDef/Transition exactly as process_workflow() at catalog.rs:128 does;
                    // attach a guard (GuardDef) to the cancel/advance transitions to fire the
                    // guarded-transition Process signal (can_cancel / can_pay in order_status.rs).
            )
    }
    ```
    Fill the state_machine body using the same `StateDef::new(...)` / `Transition::new(...)` / `GuardDef` API the existing `process_workflow()` fixture uses — match its exact method names. The states are `Confermato, InCorso, Rientrato, Chiuso, Annullato`; include the branching transition out of `Confermato` (advance + cancel) and at least one guard so the guarded-transition signal fires (RESEARCH §1.2).

    Stats fixture (Summarize — Money×2 + Quantity + all read-only drives Summarize decisively, no hint needed):
    ```rust
    pub fn stats_summarize() -> ServiceDef {
        ServiceDef::new("summary_stats")
            .display_name("Statistics")
            .read_only_field("total_revenue_cents", DataType::Integer, FieldMeaning::Money)
            .read_only_field("order_count", DataType::Integer, FieldMeaning::Quantity)
            .read_only_field("average_order_cents", DataType::Integer, FieldMeaning::Money)
    }
    ```

    Then three tests:
    ```rust
    #[test]
    fn staff_browse_intent() {
        let scores = derive_intents(&staff_browse());
        assert_eq!(scores[0].intent, Intent::Browse,
            "Staff ServiceDef must derive Browse as primary (with IntentHint::Primary(Browse)); got {:?}", scores[0].intent);
    }
    #[test]
    fn orders_process_intent() {
        let scores = derive_intents(&orders_process());
        assert_eq!(scores[0].intent, Intent::Process,
            "Order ServiceDef must derive Process as primary (guarded branching state machine); got {:?}", scores[0].intent);
    }
    #[test]
    fn stats_summarize_intent() {
        let scores = derive_intents(&stats_summarize());
        assert_eq!(scores[0].intent, Intent::Summarize,
            "SummaryStats ServiceDef must derive Summarize as primary (Money+Quantity+read-only); got {:?}", scores[0].intent);
    }
    ```
    If `Intent::Custom` matching is needed for the enum variant equality, use the verified variant names from intent.rs.

    Run ONLY this test module (serialize CPU — do not chain with other cargo runs):
    `cargo test -p ferro-projections --test catalog real_world_slice_a`
  </action>
  <verify>
    <automated>cargo test -p ferro-projections --test catalog real_world_slice_a</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p ferro-projections --test catalog staff_browse_intent` exits 0
    - `cargo test -p ferro-projections --test catalog orders_process_intent` exits 0
    - `cargo test -p ferro-projections --test catalog stats_summarize_intent` exits 0
    - `grep -c "mod real_world_slice_a" ferro-projections/tests/catalog.rs` returns 1
    - `git diff --name-only ferro-projections/src/` shows NO changes (derive.rs / intent.rs untouched — read-only constraint)
    - No path under `/Users/alberto/repositories/gestiscilo-it/` is modified by this task
  </acceptance_criteria>
  <done>The real_world_slice_a module exists in catalog.rs with three fixtures and three passing intent-assertion tests; no ferro-projections source changed; no gestiscilo file touched.</done>
</task>

<task type="auto">
  <name>Task 2: Create three equivalence-record stubs in the phase directory</name>
  <files>
    .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-staff-browse.md,
    .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-orders-process.md,
    .planning/phases/209-comp-01-slice-a-gestiscilo-migration/EQUIV-stats-summarize.md
  </files>
  <read_first>
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-VALIDATION.md "Per-Entity Functional Checklist (equivalence record contract)" (the five assertions each record must hold)
    - .planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-CONTEXT.md D-01, D-02, D-08
  </read_first>
  <action>
    Create three stub markdown files in this phase directory, one per entity. Each stub is a fill-in template; Plan 02 populates them from the gestiscilo migration outputs (before/after screenshots + the migration merge SHA).

    Each file uses this exact structure (substitute the entity name / intent / source paths per the table below):
    ```markdown
    # Equivalence Record — {Entity} ({Intent})

    **Status:** stub (filled in Plan 02 from the gestiscilo migration outputs)
    **gestiscilo migration commit/PR:** <link to the gestiscilo migration merge commit>
    **ferro intent test:** `cargo test -p ferro-projections --test catalog {test_name}`

    ## Source (gestiscilo repo)
    - Controller (before): `{controller path}`
    - View JSON (before, deleted after migration): `{view json path}`
    - Backing model: `{model path}`

    ## Functional Checklist (D-02: functional parity, not pixel-identity)
    1. **Data fields shown:** <every data column in the before screenshot appears in the after screenshot; field names may differ, values must match> — [ ] PASS
    2. **Actions available:** <every before action reachable after> — [ ] PASS
    3. **Primary-use-case flow:** <most common operator action works in the migrated view> — [ ] PASS
    4. **Intent confirmation:** `derive_intents(&service)[0].intent == {Intent}` — [ ] PASS (ref: ferro test `{test_name}`)
    5. **Intentional visual deltas documented:** <list every layout/markup difference; unlisted differences block the merge>

    ## Evidence
    - Before screenshot: <path / chrome-devtools MCP capture ref>
    - After screenshot: <path / chrome-devtools MCP capture ref>

    ## Abstraction gaps surfaced
    <record any ServiceDef/renderer friction hit during this migration; feeds the phase-close weakness note (SC#5)>
    ```

    Per-file substitutions:
    | File | Entity | Intent | test_name | controller | view json | model |
    |------|--------|--------|-----------|-----------|-----------|-------|
    | EQUIV-staff-browse.md | Staff list | Browse | staff_browse_intent | src/controllers/staff/list.rs | src/views/staff/list.json | src/models/entities/staff.rs |
    | EQUIV-orders-process.md | Orders kanban | Process | orders_process_intent | src/controllers/cassa/orders.rs | src/views/cassa/orders_index.json | src/models/entities/orders.rs |
    | EQUIV-stats-summarize.md | Statistics dashboard | Summarize | stats_summarize_intent | src/controllers/statistiche.rs | src/views/statistiche/index.json | src/models/analytics.rs |

    (Controller/view/model paths are relative to the gestiscilo repo root `/Users/alberto/repositories/gestiscilo-it/app/`.)
  </action>
  <verify>
    <automated>for f in EQUIV-staff-browse EQUIV-orders-process EQUIV-stats-summarize; do grep -q "Data fields shown" ".planning/phases/209-comp-01-slice-a-gestiscilo-migration/$f.md" || { echo "MISSING $f"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist in `.planning/phases/209-comp-01-slice-a-gestiscilo-migration/`
    - Each contains all five checklist headings ("Data fields shown", "Actions available", "Primary-use-case flow", "Intent confirmation", "Intentional visual deltas documented")
    - Each references its correct `cargo test ... {test_name}` command
  </acceptance_criteria>
  <done>Three EQUIV stub files filed with the five-assertion checklist template and correct per-entity gestiscilo source paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan adds ferro-projections test fixtures and creates planning docs. No request path, no input parsing, no auth or data-scoping surface is touched. No code in any other repo is modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-209-01-01 | Information disclosure | catalog.rs intent fixtures | accept | Fixtures are synthetic field-shape declarations (no tenant data, no secrets); they exercise `derive_intents()` only. Module name `real_world_slice_a` carries no application-identity string (project-agnostic crate rule honored). |
</threat_model>

<verification>
- `cargo test -p ferro-projections --test catalog real_world_slice_a` is green (three intent assertions pass).
- `git diff --name-only ferro-projections/src/` is empty (read-only derive.rs/intent.rs honored).
- `git status` shows no modifications under `/Users/alberto/repositories/gestiscilo-it/` (repo boundary honored).
- Three EQUIV stubs exist with the five-assertion checklist.
- ferro-side commit gate: `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings` — run serialized, one CPU op at a time.
</verification>

<success_criteria>
- Three intent-assertion tests exist and pass on the canonical fixtures (the ferro-side abstraction proof).
- Three equivalence-record stubs filed in the phase directory.
- No ferro source (derive.rs/intent.rs) changed; no gestiscilo file touched.
</success_criteria>

<output>
After completion, create `.planning/phases/209-comp-01-slice-a-gestiscilo-migration/209-01-SUMMARY.md`
</output>
