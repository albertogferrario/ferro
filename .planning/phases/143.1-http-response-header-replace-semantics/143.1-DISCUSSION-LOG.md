# Phase 143.1: HttpResponse header replace semantics — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-21
**Phase:** 143.1-http-response-header-replace-semantics
**Mode:** --auto (all options auto-selected)
**Areas discussed:** Public headers accessor, append_header visibility, ResponseExt trait, header storage, tests, version bump

---

## Public headers accessor

| Option | Description | Selected |
|--------|-------------|----------|
| Add `pub fn headers()` | Clean API surface; matches success-criteria wording | ✓ |
| Test via private field only | Simpler, no API surface change | |

**Auto-selected:** Add public accessor
**Notes:** Success criteria reference `.headers()` — adding it avoids a gap between spec and API.

---

## append_header() visibility

| Option | Description | Selected |
|--------|-------------|----------|
| `pub` | Externally callable; escape hatch for Set-Cookie, Vary, Link | ✓ |
| `pub(crate)` / private | Internal only; cookie() is the only caller | |

**Auto-selected:** `pub`
**Notes:** User code may legitimately need multi-value headers (e.g. `Vary`, `Link`, multiple `Set-Cookie` from middleware).

---

## ResponseExt trait

| Option | Description | Selected |
|--------|-------------|----------|
| No change | Delegates to HttpResponse::header(); gets replace semantics automatically | ✓ |
| Update ResponseExt::header() explicitly | Extra work with no benefit | |

**Auto-selected:** No change needed
**Notes:** The delegation chain means the fix propagates without touching ResponseExt.

---

## Header storage

| Option | Description | Selected |
|--------|-------------|----------|
| Keep Vec<(String, String)> | Works correctly once replace-semantics lands; no migration needed | ✓ |
| Migrate to HashMap or IndexMap | Better for lookup; out of scope for this patch | |

**Auto-selected:** Keep Vec
**Notes:** Explicitly out of scope per existing CONTEXT.md.

---

## Claude's Discretion

- Whether to add a direct test for `append_header()` beyond the cookie multi-value test.
- Docstring wording for updated methods.

## Deferred Ideas

- HashMap-backed header store (future refactor)
- Full call-site audit across all crates (not needed — framework-level fix handles transparently)
- Phase 143 rollback (rejected — static CSS pipeline has independent value)
