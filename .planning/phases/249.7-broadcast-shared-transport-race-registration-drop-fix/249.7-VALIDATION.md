---
phase: 249.7
slug: broadcast-shared-transport-race-registration-drop-fix
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-17
---

# Phase 249.7 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in `cargo test` (`#[tokio::test]`) |
| **Config file** | none |
| **Quick run command** | `cargo test -p ferro-broadcast` |
| **Full suite command** | `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` |
| **Estimated runtime** | ~30–90 seconds (broadcast crate); full gate longer |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ferro-broadcast`
- **After every plan wave:** Run `cargo clippy --all --all-targets -- -D warnings && cargo test -p ferro-broadcast` plus the Phase 247 suite (`framework` offload delta-broadcast integration test)
- **Before `/gsd-verify-work`:** Full gate (`cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features`) must be green
- **Max feedback latency:** ~90 seconds for the scoped broadcast run

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Success Criterion | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------------|------------|-----------------|-----------|-------------------|-------------|--------|
| SC#1 | TBD | TBD | Delta published immediately after subscribe is delivered with no `sleep` (would time out under old timing) | — | Readiness signal gates first publish on real attach | integration | `cargo test -p ferro-broadcast --test shared_transport` | ❌ W0 | ⬜ pending |
| SC#2 | TBD | TBD | Client/channel registered before `with_transport` still receives deltas after install; clone taken pre-install fans out | — | State survives transport installation; clones coherent | integration | `cargo test -p ferro-broadcast --test shared_transport` | ❌ W0 | ⬜ pending |
| SC#3 | TBD | TBD | No regression to single-process in-memory broadcast; Phase 247 integration suite passes | — | `transport == None` path unchanged | integration | `cargo test -p ferro-broadcast` + Phase 247 offload delta-broadcast suite | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*The planner refines Task IDs, plan/wave assignment, and exact test names; this maps the three ROADMAP success criteria to runnable assertions.*

---

## Wave 0 Requirements

- [ ] New SC#1/SC#2 tests added to `ferro-broadcast/tests/shared_transport.rs` (sleep-free delivery + pre-registration + clone-divergence guard)
- [ ] `FailingTransport` mock in `shared_transport.rs` updated for the new `subscribe_loop` readiness parameter

*Existing infrastructure (cargo test, `InMemoryTransport`) covers the rest — no framework install needed.*

---

## Manual-Only Verifications

| Behavior | Success Criterion | Why Manual | Test Instructions |
|----------|-------------------|------------|-------------------|
| Live cross-replica Redis delivery (two OS processes, live redis) | SC#1/SC#3 under `redis-transport` | CI cannot provision live Redis + two processes (tracked outside the phase list) | Run `redis_integration.rs` with `REDIS_URL` set against a live Redis, two replicas; confirm delivery with no `sleep` |

*The in-memory `InMemoryTransport` proves SC#1/SC#2 deterministically in CI without live Redis (246.1 D-11).*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
