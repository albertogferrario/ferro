---
phase: 81-consumer-dx-polish
plan: 03
type: execute
wave: 2
depends_on: ["81-01", "81-02"]
files_modified: [docs/src/features/api-mcp.md, docs/src/SUMMARY.md]
autonomous: true
---

<objective>
Create consumer-facing documentation for ferro-api-mcp setup and usage.

Purpose: Consumers need a single reference for setting up ferro-api-mcp, configuring it with MCP hosts (Claude Desktop, Cursor, etc.), and troubleshooting common issues. This documentation covers the final behavior after Plans 01 and 02 are complete.
Output: New docs/src/features/api-mcp.md page added to the documentation book.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/81-consumer-dx-polish/81-01-SUMMARY.md
@.planning/phases/81-consumer-dx-polish/81-02-SUMMARY.md

@ferro-api-mcp/src/main.rs
@ferro-api-mcp/Cargo.toml
@docs/src/SUMMARY.md
@docs/src/features/api.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ferro-api-mcp documentation page</name>
  <files>docs/src/features/api-mcp.md, docs/src/SUMMARY.md</files>
  <action>
Create `docs/src/features/api-mcp.md` with these sections:

**# MCP Bridge (ferro-api-mcp)**

Brief intro: ferro-api-mcp is a standalone binary that bridges any Ferro REST API to the Model Context Protocol (MCP). AI agents can discover and call API endpoints as MCP tools.

**## How It Works**
- Reads the OpenAPI spec from your Ferro app's `/api/docs/openapi.json` endpoint
- Converts each API operation into an MCP tool with typed input schemas
- Runs as a stdio MCP server that AI agents connect to
- Supports x-mcp vendor extensions for customizing tool names, descriptions, hints, and visibility

**## Prerequisites**
- A Ferro app with `make:api` scaffold (generates OpenAPI spec + API key auth)
- The API running and accessible (e.g., `ferro serve` on localhost:8080)
- An API key (generated via `ferro make:api`)

**## Setup**

### Building
```bash
cargo build --release -p ferro-api-mcp
```
Binary location: `target/release/ferro-api-mcp`

### CLI Options
```
ferro-api-mcp [OPTIONS] --spec-url <URL>

Options:
  --spec-url <URL>    URL to fetch the OpenAPI spec from
  --api-key <KEY>     API key for Authorization header (optional)
  --base-url <URL>    Override the base URL for API calls
  --log-level <LEVEL> Log level: debug, info, warn, error [default: info]
  --dry-run           Validate spec and print tool summary without starting server
```

### Validating Setup
```bash
# Dry run to check everything works
ferro-api-mcp --spec-url http://localhost:8080/api/docs/openapi.json \
  --api-key your-api-key \
  --dry-run
```
Expected output shows API name, base URL, tool count, and tool list.

**## MCP Host Configuration**

### Claude Desktop
```json
{
  "mcpServers": {
    "my-app": {
      "command": "/path/to/ferro-api-mcp",
      "args": [
        "--spec-url", "http://localhost:8080/api/docs/openapi.json",
        "--api-key", "your-api-key"
      ]
    }
  }
}
```
Location: `~/Library/Application Support/Claude/claude_desktop_config.json` (macOS)

### Claude Code
Add to `.claude.json` or `~/.claude.json`:
```json
{
  "mcpServers": {
    "my-app": {
      "command": "/path/to/ferro-api-mcp",
      "args": [
        "--spec-url", "http://localhost:8080/api/docs/openapi.json",
        "--api-key", "your-api-key"
      ]
    }
  }
}
```

### Cursor
Add to Cursor MCP settings (Settings → MCP Servers):
```json
{
  "my-app": {
    "command": "/path/to/ferro-api-mcp",
    "args": [
      "--spec-url", "http://localhost:8080/api/docs/openapi.json",
      "--api-key", "your-api-key"
    ]
  }
}
```

**## x-mcp Extensions**
Ferro's `build_openapi_spec()` automatically emits x-mcp vendor extensions on each operation:
- `x-mcp-tool-name`: AI-friendly snake_case tool name (e.g., `list_users`)
- `x-mcp-description`: AI-optimized description
- `x-mcp-hint`: Usage hints appended to tool descriptions
- `x-mcp-hidden`: Set to `true` to exclude operations from MCP tools

These are emitted automatically — no configuration needed.

**## Troubleshooting**

Table format with Problem / Cause / Solution columns:

| Problem | Cause | Solution |
|---------|-------|----------|
| "cannot connect to {url}" | API server not running | Start the server with `ferro serve` |
| "HTTP 401" on tool calls | Missing or invalid API key | Check `--api-key` matches key in database |
| "HTTP 404" on tool calls | Endpoint doesn't exist | Verify API is running and spec is current |
| "request timed out" | API slow or network issue | Check server logs, increase timeout |
| "spec parsed but 0 operations" | Empty or malformed spec | Check `/api/docs/openapi.json` manually |
| "unsupported OpenAPI version" | Spec is not 3.0.x | ferro-api-mcp requires OpenAPI 3.0.x |
| Tool arguments rejected | Missing required fields | Check tool input schema for required params |

**## Base URL Resolution**
ferro-api-mcp resolves the API base URL in this order:
1. `--base-url` flag (explicit override)
2. `servers[0].url` from the OpenAPI spec
3. Origin of the `--spec-url` (scheme + host + port)

Also update `docs/src/SUMMARY.md` to add the new page after "REST API":
```
- [MCP Bridge](features/api-mcp.md)
```

Keep the writing style consistent with existing docs pages (concise, code-heavy, no marketing language).
  </action>
  <verify>
  Read the created file and verify it has all sections.
  Check SUMMARY.md includes the new page entry.
  </verify>
  <done>
  docs/src/features/api-mcp.md exists with setup, configuration, extensions, and troubleshooting sections.
  docs/src/SUMMARY.md includes MCP Bridge entry.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docs/src/features/api-mcp.md` exists and is complete
- [ ] `docs/src/SUMMARY.md` references the new page
- [ ] Documentation covers: setup, CLI options, MCP host configs, extensions, troubleshooting
</verification>

<success_criteria>

- All tasks completed
- Documentation is comprehensive and matches implemented behavior from Plans 01 and 02
- Writing style matches existing docs pages
</success_criteria>

<output>
After completion, create `.planning/phases/81-consumer-dx-polish/81-03-SUMMARY.md`
</output>
