---
phase: 207-comp-02-synthetic-regression-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-projections/Cargo.toml
  - ferro-projections/tests/catalog.rs
  - ferro-projections/tests/snapshots/catalog__canonical_browse.snap
  - ferro-projections/tests/snapshots/catalog__canonical_focus.snap
  - ferro-projections/tests/snapshots/catalog__canonical_collect.snap
  - ferro-projections/tests/snapshots/catalog__canonical_process.snap
  - ferro-projections/tests/snapshots/catalog__canonical_summarize.snap
  - ferro-projections/tests/snapshots/catalog__canonical_analyze.snap
  - ferro-projections/tests/snapshots/catalog__canonical_track.snap
autonomous: true
requirements: [COMP-02]

must_haves:
  truths:
    - "Running `cargo test -p ferro-projections --test catalog` exits 0 with all 7 canonical tests, 4 adversarial tests, 7 snapshot tests, and the proptest passing"
    - "Each of the 7 structural intents has a named ServiceDef builder fn whose derive_intents()[0].intent equals the expected intent"
    - "A future regression in derive.rs that flips any canonical primary intent produces a named, legible test failure (no #[ignore], runs in the default CI gate)"
    - "ferro-projections/src/intent.rs and ferro-projections/src/derive.rs are byte-for-byte unchanged (read-only system under test)"
    - "Structural-invariant assertions outnumber insta snapshot assertions in catalog.rs"
    - "A 'discovered weaknesses' note naming >=1 real derivation limitation exists (module doc block in catalog.rs)"
  artifacts:
    - path: "ferro-projections/Cargo.toml"
      provides: "[dev-dependencies] section with insta (yaml) + proptest"
      contains: "[dev-dependencies]"
    - path: "ferro-projections/tests/catalog.rs"
      provides: "7 canonical fixtures + per-intent tests + 4 adversarial tests + 7 snapshot tests + proptest engine-invariant test + discovered-weaknesses doc block"
      min_lines: 400
    - path: "ferro-projections/tests/snapshots/catalog__canonical_browse.snap"
      provides: "Committed insta snapshot of the ranked (intent, signals) projection (no confidence floats)"
      contains: "Browse"
  key_links:
    - from: "ferro-projections/tests/catalog.rs"
      to: "ferro_projections::derive_intents"
      via: "direct function call on each fixture ServiceDef"
      pattern: "derive_intents\\(&"
    - from: "ferro-projections/Cargo.toml"
      to: "insta + proptest dev-deps"
      via: "[dev-dependencies] section"
      pattern: "insta = \\{ version = \"1\""
---

<objective>
Build `ferro-projections/tests/catalog.rs` — the permanent, machine-checkable regression catalog
for `derive_intents()`. Seven canonical `ServiceDef` builder functions (one per structural intent:
Browse / Focus / Collect / Process / Summarize / Analyze / Track), each with a test asserting the
primary derived intent + a calibrated confidence floor + margin + a structural invariant; four
adversarial competing-signal fixtures resolving confusable pairs; an `insta` snapshot of each
canonical shape (signals only, no confidence floats); a `proptest` engine-robustness invariant; and
a "discovered weaknesses" module-doc note.

Purpose: COMP-02 — empirically validate the projection/intent abstraction with a regression oracle
that fails legibly when `derive.rs` regresses. This catalog is also the ground-truth source Phase 210
(agent harness) consumes.

Output: one new test file, one `[dev-dependencies]` manifest edit, seven committed `.snap` files.

CRITICAL SCOPE CONSTRAINTS:
- The system under test (`ferro-projections/src/derive.rs`, `intent.rs`) is READ-ONLY. Do NOT modify it.
- Do NOT add `ferro-json-ui` or any renderer dependency to `ferro-projections` (CLAUDE.md:9, D-01).
- No `#[ignore]`, no `#[allow]` shortcuts, no unused imports — CI runs `--all-targets -D warnings` (D-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/207-comp-02-synthetic-regression-catalog/207-CONTEXT.md
@.planning/phases/207-comp-02-synthetic-regression-catalog/207-RESEARCH.md
@.planning/phases/207-comp-02-synthetic-regression-catalog/207-PATTERNS.md
@.planning/phases/207-comp-02-synthetic-regression-catalog/207-VALIDATION.md

<interfaces>
<!-- Verified from ferro-projections/src/service.rs + the existing test import surface.
     Use these directly — no codebase exploration needed. -->

Import surface (copy verbatim from ferro-projections/tests/generate_schemas.rs:4-8, add derive_intents):
```rust
use ferro_projections::{
    ActionDef, Cardinality, DataType, FieldMeaning, GuardDef, Intent, IntentScore,
    ServiceDef, StateDef, StateMachine, Transition,
};
use ferro_projections::derive_intents;
```
Only import what is actually used — `--all-targets -D warnings` rejects unused imports. Add/remove
names from the list above to match the symbols the final file references (e.g. drop `GuardDef` if a
fixture inlines no guards; add `Cardinality` only if a structural invariant matches on it).

ServiceDef builder (service.rs — all consuming `mut self -> Self`):
```rust
ServiceDef::new(name: impl Into<String>) -> Self
.field(name, DataType, FieldMeaning) -> Self          // required, readable=true, writable=true
.optional_field(name, DataType, FieldMeaning) -> Self // required=false
.read_only_field(name, DataType, FieldMeaning) -> Self // writable=false  (service.rs:206)
.write_only_field(name, DataType, FieldMeaning) -> Self // readable=false (service.rs:227)
.action(ActionDef) -> Self
.guard(GuardDef) -> Self
.belongs_to(name, target) -> Self      // ManyToOne   (service.rs:264)
.has_many(name, target) -> Self        // OneToMany   (service.rs:269)
.has_one(name, target) -> Self         // OneToOne, NavigationHint::Inline (service.rs:274)
.state_machine(StateMachine) -> Self   // service.rs:290
.validate(&self) -> Result<Vec<Warning>, ferro_projections::Error>  // service.rs:336
```

Public field access on the built ServiceDef (for structural-invariant assertions):
```rust
svc.fields: Vec<FieldDef>          // service.rs:69
svc.relationships: Vec<RelationshipDef>  // service.rs:75
svc.state_machine: Option<StateMachine>  // service.rs:79
// FieldDef public fields: name, data_type, meaning, required, is_list, readable, writable
```

derive_intents (derive.rs:75) — pure, synchronous, total. Returns Vec<IntentScore> sorted
descending by confidence; never empty (empty ServiceDef → Focus@0.5).
```rust
IntentScore { intent: Intent, confidence: f64, matching_signals: Vec<String> }
```

Builder helpers (state machine — service.rs / state.rs):
```rust
StateMachine::new(name).initial(state).state(StateDef).transition(Transition)
StateDef::new(name)            // non-final by default
StateDef::new(name).final_state()
Transition::new(from, action, to)
Transition::new(from, action, to).guard("guard_name")  // guard must be declared via .guard(GuardDef::new(...))
ActionDef::new(name).precondition("guard").transition_trigger("action_name")
GuardDef::new(name)
```

Matching-signal string literals (private SIGNAL_* constants in derive.rs — match by substring;
verified at PATTERNS.md:111-115 / derive.rs:9-47):
`"entity_name"`, `"category_field"`, `"collection_relationships"`, `"linear_states"`,
`"guarded_transitions"`, `"datetime_numeric_cooccurrence"`, `"high_writable_ratio"`,
`"write_only_fields"`, `"money_fields"`, `"percentage_fields"`, `"quantity_fields"`,
`"status_field"`, `"baseline"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dev-deps + scaffold all 7 fixtures and primary-identity-only tests (NO numeric assertions)</name>
  <files>ferro-projections/Cargo.toml, ferro-projections/tests/catalog.rs</files>
  <read_first>
    - ferro-projections/Cargo.toml (current manifest — no [dev-dependencies] section exists; section must be created after [dependencies] which ends at line 17)
    - ferro-projections/tests/generate_schemas.rs:4-8 (exact public import surface to copy)
    - ferro-projections/src/service.rs:147-300 (field / read_only_field / write_only_field / has_many / has_one / belongs_to / state_machine / validate signatures)
    - 207-RESEARCH.md:152-435 (the seven per-intent fixture recipes — copy the builder chains VERBATIM, they encode hand-computed signal arithmetic)
    - 207-PATTERNS.md:84-102 (state-machine builder style for Process/Track fixtures), :265-272 (validate() assertion pattern)
  </read_first>
  <action>
    Step 1 — Edit `ferro-projections/Cargo.toml`. Append AFTER the existing `[dependencies]` block
    (which ends at line 17 with `thiserror = "1.0"`):
    ```toml

    [dev-dependencies]
    insta = { version = "1", features = ["yaml"] }
    proptest = "1"
    ```
    (Per D-03/D-05 and PATTERNS.md:209-215. The `yaml` feature is required for assert_yaml_snapshot!.
    No tokio/sea-orm — this crate is synchronous.)

    Step 2 — Create `ferro-projections/tests/catalog.rs`. Start with the module doc block placeholder
    (the "discovered weaknesses" note is filled in Task 3 after observations exist):
    ```rust
    //! Synthetic regression catalog for `derive_intents()` (COMP-02).
    //!
    //! Seven canonical `ServiceDef` fixtures (one per structural intent), structural-invariant
    //! assertions, adversarial competing-signal fixtures, an `insta` snapshot per canonical shape,
    //! and a `proptest` engine-robustness invariant. The system under test
    //! (`ferro-projections/src/derive.rs`) is read-only.
    //!
    //! ## Discovered weaknesses
    //! TODO(Task 3): record >=1 real derivation limitation observed during calibration.
    ```
    Then the import surface (copy from generate_schemas.rs:4-8, plus `use ferro_projections::derive_intents;`;
    trim to only the symbols actually used so `-D warnings` stays clean).

    Step 3 — Add a `mod fixtures { ... }` with all SEVEN builder functions, names EXACTLY:
    `browse_catalog`, `focus_detail`, `collect_form`, `process_workflow`, `summarize_dashboard`,
    `analyze_timeseries`, `track_timeline`. Copy each builder chain VERBATIM from 207-RESEARCH.md:
    Browse :179-193, Focus :215-226, Collect :244-254, Process :276-319 (includes the 3 `.guard(GuardDef::new(...))`
    declarations the transitions/preconditions reference — keep them), Summarize :342-352, Analyze :383-392,
    Track :414-434. Keep stable builder names — Phase 210 addresses them as ground truth.

    Step 4 — Add seven `#[test]` functions `canonical_browse` … `canonical_track`. In THIS task assert
    ONLY: (a) `assert!(!scores.is_empty())`, (b) `assert_eq!(scores[0].intent, Intent::Browse)` (etc per
    fixture), and (c) for the Process and Track fixtures ONLY, `assert!(svc.validate().is_ok(), "{:?}", svc.validate())`
    BEFORE deriving (Pitfall 1/6, PATTERNS.md:265-272). DO NOT add confidence floor or margin assertions yet —
    those are calibrated in Task 2. Add the structural-invariant assertions now (they need no calibration):
      - Browse: count fields with `matches!(f.meaning, FieldMeaning::EntityName | FieldMeaning::Category)` >= 2 AND >=1 has_many relationship (RESEARCH:175, :726-736)
      - Focus: count fields with `matches!(f.meaning, FieldMeaning::FreeText | FieldMeaning::ImageUrl | FieldMeaning::Url)` >= 2 (RESEARCH:211)
      - Collect: non-system field count >= 4 AND majority writable (RESEARCH:240)
      - Process: `svc.state_machine.is_some()` AND >=2 guarded transitions AND >=2 branching states (RESEARCH:272)
      - Summarize: >=3 fields with `Money | Percentage | Quantity` AND all domain fields non-writable (RESEARCH:338)
      - Analyze: >=1 `DateTime` field AND `scores[0].matching_signals.iter().any(|s| s.contains("datetime_numeric_cooccurrence"))` (RESEARCH:379)
      - Track: `state_machine.is_some()` AND no transition has a guard AND >=3 non-final states AND signal contains `"linear_states"` (RESEARCH:410)
  </action>
  <verify>
    <automated>cargo test -p ferro-projections --test catalog -- canonical_</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[dev-dependencies\]' ferro-projections/Cargo.toml` == 1
    - `grep -q 'insta = { version = "1", features = \["yaml"\] }' ferro-projections/Cargo.toml`
    - All present: `grep -E 'fn (browse_catalog|focus_detail|collect_form|process_workflow|summarize_dashboard|analyze_timeseries|track_timeline)\(' ferro-projections/tests/catalog.rs` returns 7 lines
    - `grep -E 'fn canonical_(browse|focus|collect|process|summarize|analyze|track)\b' ferro-projections/tests/catalog.rs` returns 7 lines
    - `cargo test -p ferro-projections --test catalog -- canonical_` exits 0 (all 7 primary-identity + structural assertions pass)
    - `git diff --stat ferro-projections/src/intent.rs ferro-projections/src/derive.rs` shows NO changes
    - `cargo clippy -p ferro-projections --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    [dev-dependencies] added; catalog.rs has 7 named fixtures + 7 canonical tests passing primary-identity
    and structural-invariant assertions (no numeric thresholds yet); Process/Track fixtures validate clean;
    intent.rs/derive.rs untouched; clippy clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: CALIBRATE confidence floors/margins, then harden the canonical tests with numeric assertions (D-07)</name>
  <files>ferro-projections/tests/catalog.rs</files>
  <read_first>
    - ferro-projections/tests/catalog.rs (the file Task 1 created — the 7 canonical tests to harden)
    - 207-RESEARCH.md:452-463 (the exact calibration protocol: observe → floor = observed-0.15, margin = gap-0.10)
    - 207-RESEARCH.md:765-787 (Assumptions Log A1/A3 + Open Questions — the Analyze>Summarize and Collect>Focus margins are estimates that calibration must CONFIRM or force a fixture tweak)
    - 207-PATTERNS.md:242-263 (named-constant floor/margin pattern + per-test assertion shape)
    - ferro-projections/src/derive.rs:75 onward (read-only — reference for WHY a confidence comes out as observed, to justify a fixture tweak if a margin is too thin)
  </read_first>
  <action>
    This is a TWO-STEP observe-then-harden task. Do NOT pre-bake numbers.

    Step 1 (OBSERVE) — Temporarily add `eprintln!("{}: primary={} runner_up={}", "browse", scores[0].confidence,
    scores.get(1).map(|s| s.confidence).unwrap_or(0.0));` (one per canonical test, naming the intent) inside
    each of the 7 canonical tests. Run:
    ```
    cargo test -p ferro-projections --test catalog -- canonical_ --nocapture
    ```
    Record the printed primary confidence and runner-up confidence for each of the 7 intents.

    Step 1b (VALIDATE ASSUMPTIONS) — Confirm the two estimated-margin fixtures actually win:
      - Analyze (RESEARCH A1): `analyze_timeseries` primary MUST be Analyze, not Summarize. If Summarize
        won, the fixture must add DateTime domain fields and/or drop the Money field to 0 and use a single
        Quantity (RESEARCH:782 / Open Question 1) — adjust the FIXTURE in catalog.rs, not derive.rs, and re-run.
      - Collect (RESEARCH A3): `collect_form` primary MUST be Collect, not Focus. If Focus won, increase the
        writable/write_only ratio per RESEARCH:504-513 — adjust the fixture, re-run.

    Step 2 (HARDEN) — For each intent define two `const` floats near the top of the file (PATTERNS.md:247-253),
    e.g. `const BROWSE_FLOOR: f64 = <observed_primary - 0.15>;` and
    `const BROWSE_MARGIN: f64 = <observed_primary - observed_runner_up - 0.10>;`
    (clamp any negative computed value to a small positive like 0.0 only if the runner-up legitimately ties —
    but a tie means the fixture is weak; prefer strengthening the fixture). Round observed values to 2 dp.
    Then add to each canonical test, after the identity assertion (PATTERNS.md:256-262):
    ```rust
    assert!(scores[0].confidence >= BROWSE_FLOOR,
        "Browse confidence {} below floor {}", scores[0].confidence, BROWSE_FLOOR);
    if scores.len() > 1 {
        assert!(scores[0].confidence - scores[1].confidence >= BROWSE_MARGIN,
            "Browse margin too narrow: {} vs {}", scores[0].confidence, scores[1].confidence);
    }
    ```

    Step 3 — Remove ALL temporary `eprintln!` debug lines (Step 6 of the protocol). Confirm none remain:
    the file must have zero `eprintln!`.
  </action>
  <verify>
    <automated>cargo test -p ferro-projections --test catalog -- canonical_</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'eprintln!' ferro-projections/tests/catalog.rs` == 0 (all debug prints removed)
    - `grep -cE '_(FLOOR|MARGIN): f64' ferro-projections/tests/catalog.rs` >= 14 (a floor + margin const per intent)
    - `cargo test -p ferro-projections --test catalog -- canonical_` exits 0 with floor + margin assertions active
    - `analyze_timeseries` derives Intent::Analyze as primary AND `collect_form` derives Intent::Collect as primary (assumptions A1/A3 confirmed or fixtures corrected)
    - `git diff --stat ferro-projections/src/derive.rs` shows NO changes (any margin fix was a fixture edit, never a derive.rs edit)
    - `cargo clippy -p ferro-projections --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    Floors/margins calibrated from a real run (floor = observed-0.15, margin = gap-0.10); all 7 canonical
    tests pass with hardened numeric assertions; A1/A3 assumptions confirmed (Analyze and Collect win their
    fixtures); no eprintln! debug residue; derive.rs untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Adversarial fixtures (4 confusable pairs), proptest engine invariants, insta snapshots, and the discovered-weaknesses note</name>
  <files>ferro-projections/tests/catalog.rs, ferro-projections/tests/snapshots/catalog__canonical_browse.snap, ferro-projections/tests/snapshots/catalog__canonical_focus.snap, ferro-projections/tests/snapshots/catalog__canonical_collect.snap, ferro-projections/tests/snapshots/catalog__canonical_process.snap, ferro-projections/tests/snapshots/catalog__canonical_summarize.snap, ferro-projections/tests/snapshots/catalog__canonical_analyze.snap, ferro-projections/tests/snapshots/catalog__canonical_track.snap</files>
  <read_first>
    - ferro-projections/tests/catalog.rs (file from Tasks 1-2 — append the new tests + finalize the doc block)
    - 207-RESEARCH.md:467-513 (the 4 adversarial pairs with exact setups, winners, and the verbatim inline comment for each)
    - 207-RESEARCH.md:517-606 (the proptest Strategy: arb_field_meaning / arb_data_type / arb_service_def + the 4 engine invariants + ProptestConfig cases:256)
    - 207-RESEARCH.md:610-647 (the IntentSignals redacted snapshot struct + assert_yaml_snapshot! idiom — signals only, NO confidence floats)
    - 207-PATTERNS.md:127-183 (proptest! block shape + prop_map enumerate() caveat: `.field()` consumes svc, use the enumerated index for `f_{i}` names)
    - 207-PATTERNS.md:221-281 (serde::Serialize redaction struct + adversarial inline-comment placement)
  </read_first>
  <action>
    Step 1 — ADVERSARIAL (D-06). Add 4 `#[test]` functions: `adversarial_browse_vs_summarize`,
    `adversarial_process_vs_track`, `adversarial_analyze_vs_summarize`, `adversarial_collect_vs_focus`.
    Each builds an inline competing-signal ServiceDef (setups from RESEARCH:471-513), then asserts
    `assert_eq!(scores[0].intent, Intent::<Winner>)`. Place the VERBATIM inline comment from RESEARCH
    immediately above each fixture builder (PATTERNS.md:274-281 — `// competing: <A> vs <B>; <winner> must
    win because <reason>`). Winners: Summarize, Process, Analyze, Collect respectively. Each fixture must
    be non-trivial (>=3 domain fields). If any adversarial does not resolve to the named winner, adjust the
    ADVERSARIAL FIXTURE's signal counts (never derive.rs) until it does, and update the comment's reasoning
    to match the corrected arithmetic.

    Step 2 — PROPTEST (D-05). Add `use proptest::prelude::*;`. Add `arb_field_meaning()`, `arb_data_type()`,
    `arb_service_def()` strategies and the `proptest! { #![proptest_config(ProptestConfig { cases: 256, ..
    ProptestConfig::default() })] #[test] fn engine_never_panics_returns_valid_scores(...) }` block — copy
    from RESEARCH:523-603. In `arb_service_def` use `enumerate()` for `f_{i}` field names (PATTERNS.md:173-183 —
    `.field()` consumes svc so `svc.fields.len()` mid-chain won't compile). Assert all four invariants:
    non-empty, every confidence ∈ [0.0,1.0], sorted descending, no duplicate Intent (Intent: Hash, RESEARCH:606 —
    a `HashSet` dedupe or the `format!("{:?}")` dedupe both work).

    Step 3 — SNAPSHOTS (D-03/D-04). Add the `#[derive(serde::Serialize)] struct IntentSignals<'a> { intent:
    String, signals: &'a [String] }` + `redacted_signals(scores)` helper (RESEARCH:616-627). Add 7 snapshot
    tests `snapshot_canonical_browse` … `snapshot_canonical_track`, each:
    `insta::assert_yaml_snapshot!("canonical_browse", redacted_signals(&derive_intents(&fixtures::browse_catalog())));`
    Snapshot the ranked (intent, signals) projection ONLY — confidence floats MUST NOT appear in the payload
    (Pitfall 4). Generate the snapshot files: run `INSTA_UPDATE=always cargo test -p ferro-projections --test
    catalog -- snapshot_` (or `cargo insta test --accept`) to create the 7 `.snap` files under
    `ferro-projections/tests/snapshots/`. COMMIT the `.snap` files (they are the regression baseline).
    Inspect each generated `.snap`: it must contain intent names + signal strings and contain NO float that
    looks like a confidence value.

    Step 4 — DISCOVERED WEAKNESSES (D-09 / SC#5). Replace the `TODO(Task 3)` placeholder in the module doc
    block with a real `## Discovered weaknesses` section naming >=1 concrete limitation observed across Tasks
    1-3 (e.g. the Analyze↔Summarize margin is thin — Analyze wins by only ~0.05 raw because the
    datetime_numeric_cooccurrence signal is a single flat 0.35 that any second Money field overturns; or the
    Focus baseline makes weak Summarize fixtures fragile). Cite the concrete observed confidence/margin. An
    empty or boilerplate section fails phase close — name a REAL limitation surfaced by the calibration run.

    Step 5 — Assert the SC#2 ratio invariant holds by construction: structural-invariant `assert!`/`assert_eq!`
    calls (7 canonical tests each carry multiple, plus 4 adversarial, plus 4 proptest invariants) MUST
    outnumber the 7 `assert_yaml_snapshot!` calls. Verify with the grep in acceptance_criteria; if the count
    is too close, the canonical structural assertions from Task 1 already provide the margin.
  </action>
  <verify>
    <automated>cargo test -p ferro-projections --test catalog</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E 'fn adversarial_(browse_vs_summarize|process_vs_track|analyze_vs_summarize|collect_vs_focus)\b' ferro-projections/tests/catalog.rs` returns 4 lines
    - `grep -c '// competing:' ferro-projections/tests/catalog.rs` == 4 (one inline rationale per adversarial pair)
    - `grep -q 'cases: 256' ferro-projections/tests/catalog.rs` AND `grep -q 'engine_never_panics_returns_valid_scores' ferro-projections/tests/catalog.rs`
    - `ls ferro-projections/tests/snapshots/catalog__canonical_*.snap | wc -l` == 7
    - No confidence floats in snapshots: `grep -RiE 'confidence|0\.[0-9]{2,}' ferro-projections/tests/snapshots/` returns nothing
    - SC#2 ratio: `grep -cE 'assert!|assert_eq!|prop_assert' ferro-projections/tests/catalog.rs` is strictly greater than `grep -c 'assert_yaml_snapshot' ferro-projections/tests/catalog.rs`
    - Discovered-weaknesses note present and non-empty: `grep -A3 'Discovered weaknesses' ferro-projections/tests/catalog.rs` shows a real limitation, no `TODO`
    - `grep -c 'TODO(Task 3)' ferro-projections/tests/catalog.rs` == 0
    - `cargo test -p ferro-projections --test catalog` exits 0 (all canonical + adversarial + snapshot + proptest pass)
    - `grep -c '#\[ignore\]' ferro-projections/tests/catalog.rs` == 0 (D-08)
    - `git diff --stat ferro-projections/src/intent.rs ferro-projections/src/derive.rs` shows NO changes
  </acceptance_criteria>
  <done>
    4 adversarial confusable-pair tests (each with verbatim inline rationale) pass; proptest engine-invariant
    test (256 cases, 4 invariants) passes; 7 committed insta snapshots capture signals-only ranked projections
    with zero confidence floats; structural assertions outnumber snapshot assertions; discovered-weaknesses note
    names a real calibrated limitation; no #[ignore]; derive.rs/intent.rs untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This phase adds only `[dev-dependencies]` and an integration test file to `ferro-projections`. There is no externally-reachable surface, no network I/O, no untrusted input, no authentication, no session state, and no persistence. `derive_intents()` (the system under test) is a pure, synchronous, total function over an in-memory `ServiceDef`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-207-01 | Tampering | `catalog.rs` regression oracle integrity | mitigate | A wrong assertion would silently mask a real `derive.rs` regression (the one genuine integrity concern). Mitigated structurally: floors/margins are CALIBRATED from a real run (not invented), `intent.rs`/`derive.rs` are read-only (verified by `git diff --stat` in every task's acceptance_criteria), and snapshots capture signals only so benign re-tuning does not force a blind `cargo insta accept` that could rubber-stamp a regression. |
| T-207-02 | Information Disclosure | committed `.snap` files | accept | Snapshots contain only synthetic intent names + signal strings from in-repo fixtures. No PII, no secrets, no tenant identifiers. Low-value, no untrusted data. |

No ASVS L1 category (V2–V6) applies — confirmed in 207-RESEARCH.md "Security Domain" (test-only crate code,
no auth/session/crypto/untrusted-input/external-service surface). No HIGH-severity threats.
</threat_model>

<verification>
Phase-level checks (VALIDATION.md sampling map):
- Per task commit: `cargo test -p ferro-projections --test catalog`
- Per wave merge: `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test -p ferro-projections`
- Phase gate (before /gsd-verify-work): `cargo test --all-features` full green
- SC#2 ratio gate: `grep -cE 'assert!|assert_eq!|prop_assert' catalog.rs` > `grep -c 'assert_yaml_snapshot' catalog.rs`
- Read-only gate: `git diff --stat ferro-projections/src/intent.rs ferro-projections/src/derive.rs` empty
- SC#5 gate: "Discovered weaknesses" section in catalog.rs module doc names a real limitation (no TODO/boilerplate)
</verification>

<success_criteria>
Phase complete when (mapped to ROADMAP SC#1–SC#5):
- SC#1: 7 named canonical builder fns + 7 `canonical_*` tests; each asserts `derive_intents(&svc)[0].intent == Expected` + a calibrated confidence floor.
- SC#2: structural-invariant assertions outnumber `assert_yaml_snapshot!` calls; every fixture is non-trivial (≥3 domain fields); ≥1 structural property asserted per intent.
- SC#3: ≥1 non-trivial fixture per intent + 4 explicitly adversarial competing-signal fixtures (Browse↔Summarize, Process↔Track, Analyze↔Summarize, Collect↔Focus), each with an inline rationale comment.
- SC#4: all tests pass under `cargo test --all-features`, no `#[ignore]`, in the default CI gate.
- SC#5: "discovered weaknesses" note names ≥1 real, calibration-surfaced limitation.
</success_criteria>

<output>
After completion, create `.planning/phases/207-comp-02-synthetic-regression-catalog/207-01-SUMMARY.md`
</output>
