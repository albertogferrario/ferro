---
phase: 163-json-ui-improvements-batch-2-cassa-and-calendario-field-test
plan: 10
type: execute
wave: 7
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: []
decisions_addressed: []
must_haves:
  truths:
    - "CHANGELOG.md has a new entry under the Unreleased section documenting the Phase 163 additions"
    - "Entry describes $each, $if, validator gates, SpecBuilder ergonomic layer, codemod, and MCP catalog reflection"
    - "Entry voice is neutral — no marketing language, no killer-feature framing, no strategic positioning"
    - "No workspace version bump in this plan (per CONTEXT release cadence — single publish at Phase 161)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Unreleased entry documenting Phase 163 surface additions"
      contains: "$each"
  key_links: []
---

<objective>
Record Phase 163's additions in `CHANGELOG.md` under the Unreleased section. Per CONTEXT release cadence (Phase 162 D-23/D-24/D-25, inherited by Phase 163), there is NO publish at the end of this phase — the single v12.0 publish happens at Phase 161. This plan only logs the changes for the eventual release notes.

Purpose: Future readers (and the eventual `cargo publish` consumer) need a record of what shipped in v12.0. Without this plan, the changelog has a gap between Phase 162 and Phase 161 that hides this phase's surface.

Output: Updated `CHANGELOG.md` with a new entry under `## [Unreleased]` (or whatever the existing format uses) summarizing Phase 163's surface additions in neutral voice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/163-json-ui-improvements-batch-2-cassa-and-calendario-field-test/163-CONTEXT.md
</context>

<threat_model>
(no security surface — documentation only)
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Append Phase 163 entry to CHANGELOG.md under the Unreleased section</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (full file — check the existing format: `## [Unreleased]` heading, subsection style (`### Added` / `### Changed` / `### Fixed`), and how Phase 162's entries are structured if already present).
    - .planning/phases/163-.../163-CONTEXT.md "Release cadence" (no mid-loop publish; single v12.0 publish at Phase 161).
    - ./CLAUDE.md "Repository documents must read as neutral" — strict voice requirements.
  </read_first>
  <action>
**Step 1 — Read `CHANGELOG.md` and locate the Unreleased section.**

If the file uses Keep-a-Changelog format (`## [Unreleased]` with `### Added` / `### Changed` / `### Fixed` subsections), follow that pattern. If it uses a different format (e.g., per-version sections only), append a new `## [Unreleased]` section at the top if not present.

**Step 2 — Append the following entry under `### Added` of the Unreleased section.**

If Phase 162 entries are already in the Unreleased section, append AFTER them (chronological-within-section). If no Unreleased section exists, create one at the top of the file.

```markdown
### Added

- `ferro-json-ui`: `$each` element-level iteration directive. Templated elements expand at resolve time into N clones with auto-suffixed IDs `{id}-0`..`{id}-(N-1)`. Loop variable bound by `as` scopes `$data` paths starting with `/{as}/...` to the current row. Sibling templates sharing the same `{path, as}` produce correlated indexes; mismatched-each siblings are rejected at validation.
- `ferro-json-ui`: `$if` element-level conditional emission directive. Falsy predicates remove the element from the spec at resolve time (distinct from `visible` which renders hidden DOM). Reuses the existing `Visibility` enum, including `and`/`or`/`not` composition.
- `ferro-json-ui`: `expand_directives` public function in `ferro_json_ui::resolve`. Runs before `resolve_actions` in the resolve pipeline.
- `ferro-json-ui`: Five new `SpecError` variants for directive validation — `EachPathNotArray`, `IfPathMissing`, `EachAsReservedName`, `NestedEach`, `MismatchedEach`. Validation fires at `Spec::from_json` time (best-effort against `spec.data` when non-null).
- `ferro-json-ui`: `SpecBuilder::element_nested` and `NestedElement` builder type. Ergonomic nested-tree construction for cases where neither static JSON nor `$each` nor `$if` express the element graph. Child IDs auto-generated by structural position; the runtime `Spec` shape is unchanged.
- `ferro-cli`: `json-ui:migrate-v1` subcommand. AST-based codemod that converts v1 controller files (`make_node` + `JsonUiView::new`) to v2 (flat JSON spec + `JsonUi::render_file`). Single file per invocation; idempotent; `--dry-run` flag for preview; cases that cannot be auto-translated produce a `// TODO: ferro json-ui:migrate-v1 could not auto-translate this handler` marker.
- `ferro-mcp`: `json_ui_catalog` tool output now includes a `directives` field listing the `$each` and `$if` directives with name, description, syntax example, and validation-error references.
- `docs`: New `docs/src/json-ui/spec-construction.md` with the four-quadrant decision rubric (static / `$each` / `$if` / `SpecBuilder`). `docs/src/json-ui/expressions.md` extended with `$each` and `$if` sections.

### Changed

- `ferro-json-ui`: `Element` struct gains two optional fields — `each: Option<EachDirective>` (serde-renamed `$each`) and `if_: Option<Visibility>` (serde-renamed `$if`). Both fields use `skip_serializing_if = Option::is_none`; existing specs without directives serialize identically.
- `framework`: `JsonUi::resolve` pipeline runs `expand_directives` before `resolve_actions` and `resolve_expressions`. Specs without directives produce identical output to pre-163 behavior.
```

**Step 3 — Verify no workspace version bump.**

The Phase 163 changes ship in v12.0 alongside Phases 159-162 and 164. The single publish happens at Phase 161 (master merge). Confirm no edit to `Cargo.toml` `version` fields in this plan.

**Voice check before saving:** Run the trigger-phrase scan from `~/.claude/CLAUDE.md` "Repository documents must read as neutral":
- No "killer feature", "the bet", "betting on", "load-bearing weakness".
- No "we accept that", "no stop-loss", "maximum-quality stance".
- No personal-pronoun ownership ("we are shipping X").
- Session/date-narrative framing is acceptable in a CHANGELOG entry's prose but not required.

The entries above are written in package-changelog voice (subject-verb-object describing the surface change). Confirm by re-reading before saving.
  </action>
  <verify>
    <automated>cd /Users/alberto/repositories/albertogferrario/ferro &amp;&amp; grep -q "\\$each" CHANGELOG.md &amp;&amp; grep -q "\\$if" CHANGELOG.md &amp;&amp; grep -q "json-ui:migrate-v1" CHANGELOG.md &amp;&amp; grep -q "expand_directives" CHANGELOG.md &amp;&amp; ! grep -qiE "killer feature|the bet|betting on|maximum-quality|no stop-loss" CHANGELOG.md &amp;&amp; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\\$each" /Users/alberto/repositories/albertogferrario/ferro/CHANGELOG.md` >= 2 (mentioned in Added + Changed sections)
    - `grep -c "\\$if" /Users/alberto/repositories/albertogferrario/ferro/CHANGELOG.md` >= 2
    - `grep -c "json-ui:migrate-v1" /Users/alberto/repositories/albertogferrario/ferro/CHANGELOG.md` >= 1
    - `grep -c "expand_directives" /Users/alberto/repositories/albertogferrario/ferro/CHANGELOG.md` >= 1
    - `grep -c "EachPathNotArray\|IfPathMissing\|EachAsReservedName\|NestedEach\|MismatchedEach" /Users/alberto/repositories/albertogferrario/ferro/CHANGELOG.md` >= 3 (validator surface mentioned)
    - `grep -c "element_nested\|NestedElement" /Users/alberto/repositories/albertogferrario/ferro/CHANGELOG.md` >= 1
    - **Voice check (must return 0):** `grep -ciE "killer feature|the bet|betting on|maximum-quality|no stop-loss|load-bearing weakness" /Users/alberto/repositories/albertogferrario/ferro/CHANGELOG.md` returns 0
    - **No version bump:** `git diff Cargo.toml | grep -c "^[+-]version"` returns 0 (in the workspace context; if `git diff` is run, no version-line changes)
  </acceptance_criteria>
  <done>CHANGELOG.md has a Phase 163 entry under Unreleased covering all six surface additions; voice is neutral; no workspace version bump.</done>
</task>

</tasks>

<verification>
- `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` exits 0 (full-suite gate after Plan 10 closes the phase).
- Voice scan against the trigger-phrase list returns no hits.
</verification>

<success_criteria>
- Phase 163 surface is recorded in the changelog for future v12.0 release notes.
- Voice neutrality preserved (no marketing language, no strategic framing).
- No mid-loop publish — version bump deferred to Phase 161.
</success_criteria>

<output>
After completion, create `.planning/phases/163-json-ui-improvements-batch-2-cassa-and-calendario-field-test/163-10-SUMMARY.md` per the template.
</output>
