---
phase: 77-validate-fix-api-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-cli/src/commands/make_api.rs
  - ferro-mcp/src/tools/code_templates.rs
  - docs/src/features/api.md
autonomous: true
---

<objective>
Fix all generated code template bugs found during Phase 76 audit.

Purpose: Three separate code generation sources (make_api CLI, MCP code_templates, documentation) contain the same two bugs: calling `.await` on the sync `DB::connection()` function, and passing `Vec<serde_json::Value>` to `ResourceCollection::paginated()` which requires `T: Resource`. These produce code that won't compile.

Output: All template strings and documentation examples generate correct, compilable Rust code.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-default-api-scaffold/76-01-SUMMARY.md
@.planning/phases/76-default-api-scaffold/76-03-SUMMARY.md

Source files:
@ferro-cli/src/commands/make_api.rs
@ferro-mcp/src/tools/code_templates.rs
@docs/src/features/api.md
@framework/src/database/mod.rs (DB::connection() signature — sync, returns Result<DbConnection, FrameworkError>)
@framework/src/http/resources/resource_collection.rs (ResourceCollection<T: Resource>)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DB::connection().await → DB::connection()? in all templates</name>
  <files>ferro-cli/src/commands/make_api.rs, ferro-mcp/src/tools/code_templates.rs, docs/src/features/api.md</files>
  <action>
`DB::connection()` is sync (returns `Result<DbConnection, FrameworkError>`). Three files use `.await` on it in template/example strings:

1. **ferro-cli/src/commands/make_api.rs line ~282** (index handler template):
   `{pascal}::find().paginate(&ferro::DB::connection().await, per_page)` →
   Store connection in a variable first, then use it:
   ```
   let db = ferro::DB::connection()?;
   let paginator = {pascal}::find().paginate(&db, per_page);
   ```

2. **ferro-cli/src/commands/make_api.rs line ~1216** (ApiKeyProviderImpl template):
   `.one(&ferro::DB::connection().await)` →
   ```
   let db = ferro::DB::connection()?;
   ... .one(&db)
   ```

3. **ferro-mcp/src/tools/code_templates.rs line ~1363** (api controller template):
   `{{Entity}}::find().paginate(&ferro::DB::connection().await, per_page)` →
   ```
   let db = ferro::DB::connection()?;
   let paginator = {{Entity}}::find().paginate(&db, per_page);
   ```

4. **docs/src/features/api.md line ~534** (customization example):
   `.all(&ferro::DB::connection().await)` →
   ```
   let db = ferro::DB::connection()?;
   ... .all(&db)
   ```

Search for any other `connection().await` patterns in template strings across the codebase to catch any missed instances.
  </action>
  <verify>grep -r "connection().await" ferro-cli/src/commands/make_api.rs ferro-mcp/src/tools/code_templates.rs docs/src/features/api.md returns no matches</verify>
  <done>Zero occurrences of `DB::connection().await` remain in any template string or documentation example. All use `DB::connection()?` with explicit variable binding.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ResourceCollection type mismatch in index handler templates</name>
  <files>ferro-cli/src/commands/make_api.rs, ferro-mcp/src/tools/code_templates.rs</files>
  <action>
`ResourceCollection<T: Resource>` requires `T` to implement the `Resource` trait. The current templates map items to `Vec<serde_json::Value>` via `to_resource()`, then pass that to `ResourceCollection::paginated()`. Since `serde_json::Value` does not implement `Resource`, this won't compile.

**Fix in ferro-cli/src/commands/make_api.rs** (index handler, ~lines 291-296):
Replace:
```
let resources: Vec<serde_json::Value> = items
    .iter()
    .map(|m| ferro::Resource::to_resource(&{pascal}Resource::from(m), &req))
    .collect();
let meta = ferro::PaginationMeta::new(page, per_page, total);
Ok(ferro::ResourceCollection::paginated(resources, meta).to_response(&req))
```
With:
```
let resources: Vec<{pascal}Resource> = items.iter().map(|m| {pascal}Resource::from(m)).collect();
let meta = ferro::PaginationMeta::new(page, per_page, total);
Ok(ferro::ResourceCollection::paginated(resources, meta).to_response(&req))
```

`ResourceCollection::to_response()` calls `to_resource()` on each item internally, so the pre-conversion to JSON values is both wrong and unnecessary.

**Fix in ferro-mcp/src/tools/code_templates.rs** (~lines 1368-1372):
Same pattern — replace `Vec<serde_json::Value>` mapping with typed `Vec<{{Entity}}Resource>`:
```
let resources: Vec<{{Entity}}Resource> = items.iter().map(|m| {{Entity}}Resource::from(m)).collect();
let meta = ferro::PaginationMeta::new(page, per_page, total);
Ok(ferro::ResourceCollection::paginated(resources, meta).to_response(&req))
```
  </action>
  <verify>cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features</verify>
  <done>Both template sources generate index handlers that pass typed Resource vecs to ResourceCollection::paginated(). No `Vec<serde_json::Value>` passed to ResourceCollection anywhere in template strings. Full test suite passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo fmt --all -- --check` passes
- [ ] `cargo clippy --all --all-targets -- -D warnings` passes
- [ ] `cargo test --all-features` passes (all existing tests still pass)
- [ ] Zero occurrences of `.await` on `DB::connection()` in any template string or doc example
- [ ] Zero occurrences of `Vec<serde_json::Value>` passed to `ResourceCollection::paginated` in templates
</verification>

<success_criteria>
- All generated code templates produce compilable Rust (no `.await` on sync, no type mismatches)
- Documentation examples are correct
- All 3 sources fixed: make_api.rs, code_templates.rs, api.md
- Full test suite passes unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/77-validate-fix-api-scaffold/77-01-SUMMARY.md`
</output>
