---
phase: 122.2-deploy-simplification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-cli/src/deploy/env_example.rs
  - ferro-cli/src/deploy/classify.rs
  - ferro-cli/src/deploy/ferro_deps.rs
  - ferro-cli/src/deploy/runtime_deps.rs
  - ferro-cli/src/deploy/mod.rs
  - ferro-cli/src/commands/deploy_check.rs
  - ferro-cli/src/commands/ignore_sync.rs
  - ferro-cli/src/commands/mod.rs
  - ferro-cli/src/templates/ignore_patterns.rs
  - ferro-cli/src/templates/mod.rs
  - ferro-cli/src/templates/files/root/ignore_patterns.toml
  - ferro-cli/tests/golden.rs
  - ferro-cli/tests/fixtures
  - ferro-cli/src/main.rs
  - Cargo.toml
autonomous: true
requirements: [SCOPE-6, SCOPE-8, SCOPE-11, SCOPE-13]
must_haves:
  truths:
    - "env_example, classify, ferro_deps, runtime_deps, ignore_patterns modules no longer exist"
    - "deploy_check and ignore_sync CLI commands removed from main.rs"
    - "Golden tests and fixture trees deleted, workspace exclude line removed"
  artifacts:
    - path: "ferro-cli/src/main.rs"
      provides: "CLI command dispatch without deploy_check/ignore_sync"
  key_links:
    - from: "workspace Cargo.toml"
      to: "excluded paths"
      via: "exclude field"
      pattern: "exclude"
---

<objective>
Delete obsolete ferro-cli deploy modules, the `deploy:check` and `ignore:sync` commands, the ignore_patterns unified renderer, and the golden fixture test suite (SCOPE §6, §8, §11, §13). Produces stubs where necessary so Wave 2 plans can rewrite against a clean surface.
Purpose: Clear dead code before new renderers land — minimizes merge churn on docker.rs / do.rs / docker_init.rs / do_init.rs during Wave 2.
Output: ferro-cli compiles (with stubbed docker_init and do_init still in place from Plan 04); obsolete modules gone.
</objective>

<context>
@.planning/phases/122.2-deploy-simplification/SCOPE.md
@ferro-cli/src/deploy/env_example.rs
@ferro-cli/src/deploy/classify.rs
@ferro-cli/src/deploy/ferro_deps.rs
@ferro-cli/src/deploy/runtime_deps.rs
@ferro-cli/src/deploy/mod.rs
@ferro-cli/src/commands/deploy_check.rs
@ferro-cli/src/commands/ignore_sync.rs
@ferro-cli/src/templates/ignore_patterns.rs
@ferro-cli/src/templates/files/root/ignore_patterns.toml
@ferro-cli/tests/golden.rs
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete obsolete deploy and template modules and their mod.rs references</name>
  <read_first>ferro-cli/src/deploy/mod.rs, ferro-cli/src/templates/mod.rs, ferro-cli/src/commands/mod.rs, ferro-cli/src/deploy/env_example.rs, ferro-cli/src/deploy/classify.rs, ferro-cli/src/deploy/ferro_deps.rs, ferro-cli/src/deploy/runtime_deps.rs, ferro-cli/src/templates/ignore_patterns.rs</read_first>
  <files>ferro-cli/src/deploy/env_example.rs, ferro-cli/src/deploy/classify.rs, ferro-cli/src/deploy/ferro_deps.rs, ferro-cli/src/deploy/runtime_deps.rs, ferro-cli/src/deploy/mod.rs, ferro-cli/src/templates/ignore_patterns.rs, ferro-cli/src/templates/files/root/ignore_patterns.toml, ferro-cli/src/templates/mod.rs, ferro-cli/src/commands/deploy_check.rs, ferro-cli/src/commands/ignore_sync.rs, ferro-cli/src/commands/mod.rs</files>
  <action>
    Delete these files with `rm`:
    - ferro-cli/src/deploy/env_example.rs
    - ferro-cli/src/deploy/classify.rs
    - ferro-cli/src/deploy/ferro_deps.rs  (entire file — shell-script generator variant)
    - ferro-cli/src/deploy/runtime_deps.rs  (crate→apt registry)
    - ferro-cli/src/templates/ignore_patterns.rs
    - ferro-cli/src/templates/files/root/ignore_patterns.toml
    - ferro-cli/src/commands/deploy_check.rs
    - ferro-cli/src/commands/ignore_sync.rs

    Edit ferro-cli/src/deploy/mod.rs: remove `pub mod env_example;`, `pub mod classify;`, `pub mod ferro_deps;`, `pub mod runtime_deps;` lines.

    Edit ferro-cli/src/templates/mod.rs: remove `pub mod ignore_patterns;` line.

    Edit ferro-cli/src/commands/mod.rs: remove `pub mod deploy_check;` and `pub mod ignore_sync;` lines.

    Per SCOPE §6 (env_example/classify), §8 (ignore_patterns), §11 (deploy_check), §9 (runtime_deps).
  </action>
  <acceptance_criteria>
    - `test ! -f ferro-cli/src/deploy/env_example.rs && test ! -f ferro-cli/src/deploy/classify.rs && test ! -f ferro-cli/src/deploy/ferro_deps.rs && test ! -f ferro-cli/src/deploy/runtime_deps.rs`
    - `test ! -f ferro-cli/src/templates/ignore_patterns.rs && test ! -f ferro-cli/src/templates/files/root/ignore_patterns.toml`
    - `test ! -f ferro-cli/src/commands/deploy_check.rs && test ! -f ferro-cli/src/commands/ignore_sync.rs`
    - `grep -E "env_example|classify|runtime_deps|ferro_deps" ferro-cli/src/deploy/mod.rs` → no matches
    - `grep "ignore_patterns" ferro-cli/src/templates/mod.rs` → no matches
    - `grep -E "deploy_check|ignore_sync" ferro-cli/src/commands/mod.rs` → no matches
  </acceptance_criteria>
  <verify>
    <automated>test ! -f ferro-cli/src/deploy/env_example.rs && test ! -f ferro-cli/src/commands/deploy_check.rs && test ! -f ferro-cli/src/templates/ignore_patterns.rs</automated>
  </verify>
  <done>All listed files deleted, all mod.rs files updated.</done>
</task>

<task type="auto">
  <name>Task 2: Remove deploy:check and ignore:sync subcommand registrations from main.rs</name>
  <read_first>ferro-cli/src/main.rs</read_first>
  <files>ferro-cli/src/main.rs</files>
  <action>
    Open ferro-cli/src/main.rs. Delete every clap subcommand definition, match arm, and use-statement referencing:
    - `deploy:check` / `deploy_check` / `DeployCheck`
    - `ignore:sync` / `ignore_sync` / `IgnoreSync`

    Leave `docker:init`, `do:init`, `mcp`, `doctor`, `ci:init` and all other subcommands untouched. Per SCOPE §8 and §11.
  </action>
  <acceptance_criteria>
    - `grep -E "deploy[_:]check|DeployCheck" ferro-cli/src/main.rs` → no matches
    - `grep -E "ignore[_:]sync|IgnoreSync" ferro-cli/src/main.rs` → no matches
    - `cargo check -p ferro-cli` → succeeds
  </acceptance_criteria>
  <verify>
    <automated>cargo check -p ferro-cli 2>&1 | tail -5</automated>
  </verify>
  <done>main.rs no longer registers deploy:check or ignore:sync; ferro-cli type-checks.</done>
</task>

<task type="auto">
  <name>Task 3: Delete golden tests, fixtures tree, and workspace exclude</name>
  <read_first>ferro-cli/tests/golden.rs, Cargo.toml</read_first>
  <files>ferro-cli/tests/golden.rs, ferro-cli/tests/fixtures, Cargo.toml</files>
  <action>
    1. Delete `ferro-cli/tests/golden.rs` with `rm`.
    2. Delete the entire `ferro-cli/tests/fixtures/` directory with `rm -rf`.
    3. Edit workspace `Cargo.toml` (root): remove the line matching `exclude = ["ferro-cli/tests/fixtures"]` (or the entry inside the `exclude = [...]` array if it's part of a list — delete just that entry, keep the rest).

    Per SCOPE §13.
  </action>
  <acceptance_criteria>
    - `test ! -f ferro-cli/tests/golden.rs && test ! -d ferro-cli/tests/fixtures`
    - `grep "ferro-cli/tests/fixtures" Cargo.toml` → no matches
    - `cargo test -p ferro-cli --no-run` → succeeds (no missing golden test file error)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p ferro-cli --no-run 2>&1 | tail -5</automated>
  </verify>
  <done>Golden suite removed, workspace Cargo.toml cleaned.</done>
</task>

</tasks>

<verification>
- `cargo check -p ferro-cli` succeeds (assumes Plan 04 stubs docker_init/do_init so references resolve)
- No grep matches for deleted module names in ferro-cli source
</verification>

<success_criteria>
ferro-cli has zero obsolete deploy modules, zero golden tests, and no registered deploy:check / ignore:sync commands. Ready for Wave 2 to add new simpler renderers.
</success_criteria>

<output>
Create `.planning/phases/122.2-deploy-simplification/122.2-03-SUMMARY.md` after completion.
</output>
