---
phase: 122.2-deploy-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-mcp/src/tools/deploy_check.rs
  - ferro-mcp/src/tools/deploy_diff_env.rs
  - ferro-mcp/src/tools/runtime_requirements.rs
  - ferro-mcp/src/tools/deploy_common.rs
  - ferro-mcp/src/tools/mod.rs
  - ferro-mcp/src/service.rs
autonomous: true
requirements: [SCOPE-9]
must_haves:
  truths:
    - "ferro-mcp no longer exposes deploy_check, deploy_diff_env, or runtime_requirements tools"
    - "ferro-mcp still builds and all non-deploy tools still register"
  artifacts:
    - path: "ferro-mcp/src/tools/mod.rs"
      provides: "Tool module registry without deploy modules"
      contains: "pub mod"
  key_links:
    - from: "ferro-mcp/src/service.rs"
      to: "tool registry"
      via: "register_tool calls"
      pattern: "register_tool"
---

<objective>
Delete the three deploy MCP tools and their shared bridge module per SCOPE §9.
Purpose: Remove dead deploy surface from ferro-mcp before refactoring ferro-cli, so Wave 2 plans don't need to maintain obsolete cross-crate contracts.
Output: ferro-mcp with deploy tools fully removed, service registry clean, crate compiles.
</objective>

<context>
@.planning/phases/122.2-deploy-simplification/SCOPE.md
@.planning/phases/122.2-deploy-simplification/122.2-CONTEXT.md
@ferro-mcp/src/tools/deploy_check.rs
@ferro-mcp/src/tools/deploy_diff_env.rs
@ferro-mcp/src/tools/runtime_requirements.rs
@ferro-mcp/src/tools/deploy_common.rs
@ferro-mcp/src/service.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete deploy tool source files and their tests</name>
  <read_first>ferro-mcp/src/tools/deploy_check.rs, ferro-mcp/src/tools/deploy_diff_env.rs, ferro-mcp/src/tools/runtime_requirements.rs, ferro-mcp/src/tools/deploy_common.rs, ferro-mcp/src/tools/mod.rs</read_first>
  <files>ferro-mcp/src/tools/deploy_check.rs, ferro-mcp/src/tools/deploy_diff_env.rs, ferro-mcp/src/tools/runtime_requirements.rs, ferro-mcp/src/tools/deploy_common.rs, ferro-mcp/src/tools/mod.rs</files>
  <action>
    Delete these four files entirely via `rm`:
    - ferro-mcp/src/tools/deploy_check.rs
    - ferro-mcp/src/tools/deploy_diff_env.rs
    - ferro-mcp/src/tools/runtime_requirements.rs
    - ferro-mcp/src/tools/deploy_common.rs

    Edit ferro-mcp/src/tools/mod.rs: remove `pub mod deploy_check;`, `pub mod deploy_diff_env;`, `pub mod runtime_requirements;`, `pub mod deploy_common;` lines. Leave all other `pub mod` lines intact.

    Per SCOPE §9.
  </action>
  <acceptance_criteria>
    - `ls ferro-mcp/src/tools/deploy_check.rs` → No such file
    - `ls ferro-mcp/src/tools/deploy_diff_env.rs` → No such file
    - `ls ferro-mcp/src/tools/runtime_requirements.rs` → No such file
    - `ls ferro-mcp/src/tools/deploy_common.rs` → No such file
    - `grep -E "deploy_check|deploy_diff_env|runtime_requirements|deploy_common" ferro-mcp/src/tools/mod.rs` → no matches
  </acceptance_criteria>
  <verify>
    <automated>test ! -f ferro-mcp/src/tools/deploy_check.rs && test ! -f ferro-mcp/src/tools/deploy_diff_env.rs && test ! -f ferro-mcp/src/tools/runtime_requirements.rs && test ! -f ferro-mcp/src/tools/deploy_common.rs</automated>
  </verify>
  <done>Four files deleted, mod.rs no longer references them.</done>
</task>

<task type="auto">
  <name>Task 2: Remove tool registrations from service.rs and verify ferro-mcp builds</name>
  <read_first>ferro-mcp/src/service.rs</read_first>
  <files>ferro-mcp/src/service.rs</files>
  <action>
    In ferro-mcp/src/service.rs, remove every register/dispatch entry referencing the deleted tools. Search for `deploy_check`, `deploy_diff_env`, `runtime_requirements`, `deploy_common` and delete matching `use` lines, match arms, `register_tool(...)` calls, and any helper functions that exclusively served them. Leave every non-deploy tool registration untouched (application_info, list_routes, db_schema, explain_route, explain_model, generation_context, json_ui_catalog, etc.).

    Per SCOPE §9. Delete-before-create: no replacement.
  </action>
  <acceptance_criteria>
    - `grep -E "deploy_check|deploy_diff_env|runtime_requirements|deploy_common" ferro-mcp/src/service.rs` → no matches
    - `cargo build -p ferro-mcp` → success
    - `cargo test -p ferro-mcp` → success (remaining tool tests still pass)
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p ferro-mcp 2>&1 | tail -5 && cargo test -p ferro-mcp 2>&1 | tail -10</automated>
  </verify>
  <done>ferro-mcp compiles, only non-deploy tools register.</done>
</task>

</tasks>

<verification>
- `cargo build -p ferro-mcp` succeeds
- `cargo test -p ferro-mcp` succeeds
- No references to deleted modules remain in ferro-mcp source
</verification>

<success_criteria>
ferro-mcp has zero deploy-related code. Wave 2 can safely refactor ferro-cli without maintaining MCP deploy contracts.
</success_criteria>

<output>
Create `.planning/phases/122.2-deploy-simplification/122.2-01-SUMMARY.md` after completion.
</output>
