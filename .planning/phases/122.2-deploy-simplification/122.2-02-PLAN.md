---
phase: 122.2-deploy-simplification
plan: 02
type: execute
wave: 2
depends_on: ["122.2-01"]
files_modified:
  - ferro-mcp/src/bin/ferro-mcp.rs
  - ferro-mcp/Cargo.toml
  - ferro-cli/Cargo.toml
  - ferro-cli/src/commands/mcp.rs
autonomous: true
requirements: [SCOPE-10]
must_haves:
  truths:
    - "ferro-mcp has no ferro-cli dependency"
    - "ferro-cli depends on ferro-mcp and launches it in-process"
    - "`ferro mcp` subcommand still starts the MCP server"
  artifacts:
    - path: "ferro-cli/src/commands/mcp.rs"
      provides: "In-process launcher for ferro-mcp server"
      min_lines: 10
    - path: "ferro-cli/Cargo.toml"
      provides: "ferro-mcp dependency declaration"
      contains: "ferro-mcp"
  key_links:
    - from: "ferro-cli/src/commands/mcp.rs"
      to: "ferro_mcp::run (or equivalent entrypoint)"
      via: "direct function call"
      pattern: "ferro_mcp::"
---

<objective>
Revert the ferro-cli ↔ ferro-mcp circular dependency workaround from Phase 123 (SCOPE §10). Subprocess architecture is no longer needed after Plan 01 deleted the deploy tools that required cross-crate access.
Purpose: Simplify dependency graph; restore in-process launch for faster startup and simpler debugging.
Output: ferro-mcp becomes a library-only crate; ferro-cli imports it and calls its entrypoint directly.
</objective>

<context>
@.planning/phases/122.2-deploy-simplification/SCOPE.md
@ferro-mcp/src/bin/ferro-mcp.rs
@ferro-mcp/Cargo.toml
@ferro-cli/Cargo.toml
@ferro-cli/src/commands/mcp.rs
@ferro-mcp/src/service.rs
</context>

<interfaces>
ferro-mcp must export a public async entrypoint that the CLI can call. Look in ferro-mcp/src/lib.rs or service.rs for an existing `run()` / `serve()` / `Service::new().run()` function. If only the binary had the bootstrap logic, move that logic into `pub async fn run() -> Result<()>` in ferro-mcp/src/lib.rs before deletion.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Move bootstrap from binary to library and delete standalone binary</name>
  <read_first>ferro-mcp/src/bin/ferro-mcp.rs, ferro-mcp/src/lib.rs, ferro-mcp/src/service.rs, ferro-mcp/Cargo.toml</read_first>
  <files>ferro-mcp/src/bin/ferro-mcp.rs, ferro-mcp/src/lib.rs, ferro-mcp/Cargo.toml</files>
  <action>
    1. Read ferro-mcp/src/bin/ferro-mcp.rs. Extract its main() body (server bootstrap: transport wiring, service init, serve loop).
    2. Add `pub async fn run() -> anyhow::Result<()>` (or appropriate error type) to ferro-mcp/src/lib.rs containing that bootstrap logic. Reuse whatever existing Service struct ferro-mcp/src/service.rs defines.
    3. Delete ferro-mcp/src/bin/ferro-mcp.rs entirely.
    4. Edit ferro-mcp/Cargo.toml:
       - Remove any `[[bin]] name = "ferro-mcp"` block
       - Remove `ferro-cli = { ... }` from `[dependencies]` (the circular dep added in Phase 123)
    5. Confirm ferro-mcp is library-only: only `[lib]` or default lib target.

    Per SCOPE §10.
  </action>
  <acceptance_criteria>
    - `test ! -f ferro-mcp/src/bin/ferro-mcp.rs` → true
    - `grep -E "^\[\[bin\]\]" ferro-mcp/Cargo.toml` → no matches
    - `grep -E "^ferro-cli" ferro-mcp/Cargo.toml` → no matches
    - `grep "pub async fn run" ferro-mcp/src/lib.rs` → exactly one match
    - `cargo build -p ferro-mcp` → success
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p ferro-mcp 2>&1 | tail -5</automated>
  </verify>
  <done>ferro-mcp is library-only, exposes pub async fn run(), no ferro-cli dep.</done>
</task>

<task type="auto">
  <name>Task 2: Re-add ferro-mcp dep to ferro-cli and rewrite mcp.rs for in-process launch</name>
  <read_first>ferro-cli/Cargo.toml, ferro-cli/src/commands/mcp.rs</read_first>
  <files>ferro-cli/Cargo.toml, ferro-cli/src/commands/mcp.rs</files>
  <action>
    1. Edit ferro-cli/Cargo.toml: add under `[dependencies]`:
       ```
       ferro-mcp = { path = "../ferro-mcp" }
       ```
       (match the path style used by other workspace deps in the file.)

    2. Rewrite ferro-cli/src/commands/mcp.rs. Delete the current subprocess implementation (`std::process::Command::new("ferro-mcp")` or similar). Replace with:
       ```rust
       pub async fn run() -> anyhow::Result<()> {
           ferro_mcp::run().await
       }
       ```
       Preserve the existing command registration surface: whatever function signature main.rs expects, keep that signature, just change the body.

    3. Delete any now-dead helpers (subprocess spawning, exit code forwarding, signal handling for the child).

    Per SCOPE §10.
  </action>
  <acceptance_criteria>
    - `grep "ferro-mcp" ferro-cli/Cargo.toml` → at least one match in dependencies
    - `grep "ferro_mcp::run" ferro-cli/src/commands/mcp.rs` → exactly one match
    - `grep -E "Command::new|spawn" ferro-cli/src/commands/mcp.rs` → no matches
    - `cargo build -p ferro-cli` → success
    - `cargo build --workspace` → success (no circular dep)
  </acceptance_criteria>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -10</automated>
  </verify>
  <done>ferro-cli depends on ferro-mcp, mcp subcommand calls ferro_mcp::run() directly, workspace compiles.</done>
</task>

</tasks>

<verification>
- `cargo build --workspace` succeeds
- No circular deps (ferro-mcp does not list ferro-cli)
- `ferro mcp` would route to `ferro_mcp::run()` in-process
</verification>

<success_criteria>
Circular dep reverted, in-process launch restored, workspace builds clean.
</success_criteria>

<output>
Create `.planning/phases/122.2-deploy-simplification/122.2-02-SUMMARY.md` after completion.
</output>
