---
phase: 122.2-deploy-simplification
plan: 09
type: execute
wave: 4
depends_on: ["122.2-01", "122.2-02", "122.2-03", "122.2-04", "122.2-05", "122.2-06", "122.2-07", "122.2-08"]
files_modified:
  - .planning/phases/122.2-deploy-simplification/122.2-09-UAT.md
autonomous: false
requirements: [SCOPE-VERIFICATION]
must_haves:
  truths:
    - "Workspace-wide fmt + clippy + test gate is green"
    - "ferro docker:init produces expected Dockerfile + Cargo.docker.toml against gestiscilo-it/app"
    - "ferro do:init produces expected .do/app.yaml against gestiscilo-it/app"
    - "ferro doctor runs all 9 checks and returns sensible results"
    - "ferro mcp still launches the MCP server in-process"
    - "Post-simplification LOC of deploy code approaches ~375 target"
  artifacts:
    - path: ".planning/phases/122.2-deploy-simplification/122.2-09-UAT.md"
      provides: "UAT findings and LOC accounting"
      min_lines: 30
  key_links:
    - from: "gestiscilo-it/app generated artifacts"
      to: "SCOPE §Verification bullets"
      via: "manual diff"
      pattern: "Dockerfile|Cargo.docker.toml|.do/app.yaml"
---

<objective>
Phase-end verification. Run the mandatory gate, regenerate scaffolding against the real `../../gestiscilo-it/app`, and verify every SCOPE §Verification bullet. Record findings in a UAT document.
Purpose: Prove the simplification actually works in production-shaped usage, not just unit tests.
Output: UAT report with pass/fail per verification bullet, LOC accounting, any deferred follow-ups.
</objective>

<context>
@.planning/phases/122.2-deploy-simplification/SCOPE.md
@.planning/phases/122.2-deploy-simplification/122.2-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace-wide phase-end gate</name>
  <read_first>CLAUDE.md</read_first>
  <files>none</files>
  <action>
    Run from the ferro workspace root:
    ```
    cargo fmt --all -- --check
    cargo clippy --all --all-targets -- -D warnings
    cargo test --all-features
    ```

    All three must succeed. If any fail, fix the underlying issue in the relevant Plan 01-08 artifacts and re-run. Do NOT suppress warnings, do NOT skip tests. Per CLAUDE.md Testing & Linting.
  </action>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exit 0
    - `cargo clippy --all --all-targets -- -D warnings` exit 0
    - `cargo test --all-features` exit 0
  </acceptance_criteria>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features 2>&1 | tail -20</automated>
  </verify>
  <done>Phase-end gate green across the workspace.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 122.2 deploy simplification:
    - ferro-mcp deploy tools deleted, in-process launch restored
    - ferro-cli deploy modules deleted (env_example, classify, runtime_deps, ignore_patterns, deploy_check, ignore_sync, golden tests)
    - New primitives: FerroDeployMetadata reader, env_production parser, rewrite_ferro_version rewriter
    - New Dockerfile renderer + docker:init command
    - New .do/app.yaml renderer + do:init command (decoupled from ci.yml)
    - ferro doctor with 9 checks per SCOPE §12
  </what-built>
  <how-to-verify>
    Build a release binary and test against the real gestiscilo project:

    1. **Build**:
       ```
       cargo build --release -p ferro-cli
       FERRO=$(pwd)/target/release/ferro
       ```

    2. **Prep gestiscilo**:
       ```
       cd ../../gestiscilo-it/app
       # Ensure .env.production exists. If missing:
       cp .env.example .env.production  # then edit values as needed
       ```

    3. **docker:init**:
       ```
       $FERRO docker:init --force
       ```
       Verify:
       - `Dockerfile` is created and contains:
         - Multi-bin build lines for `gestiscilo` and `screenshot-worker`
         - A runtime apt block with `chromium fonts-liberation` (the `# ferro:runtime-apt` marker)
         - No frontend stage (gestiscilo has no `frontend/package.json`)
         - `COPY Cargo.docker.toml Cargo.toml` in planner and builder stages
         - No `GITHUB_TOKEN`, no `insteadOf`, no `rewrite-ferro-deps.sh`
       - `.dockerignore` is created and contains `database.db`, `.planning/`, `storage/`, `data/`
       - `Cargo.docker.toml` is created and ferro* deps use `version = "..."` instead of `path = "..."`

    4. **do:init**:
       ```
       $FERRO do:init --force
       ```
       Verify `.do/app.yaml`:
       - `name: gestiscilo`
       - `region: fra1`
       - `github.repo` auto-detected from `git remote get-url origin`
       - Web service: `name: web`, dockerfile_path set
       - `workers:` block contains `screenshot-worker` only (no test_/dev_/debug_ bins)
       - `envs:` block contains one commented line per key found in `.env.production`
       - No `databases:` block
       - `.github/workflows/ci.yml` NOT created by this command

    5. **doctor**:
       ```
       $FERRO doctor
       ```
       Verify all 9 checks run and the output is sensible.

    6. **mcp in-process**:
       ```
       $FERRO mcp --help
       ```
       Verify it launches (will start the MCP server; exit via Ctrl-C).

    7. **LOC accounting**:
       Count LOC of the new deploy code:
       ```
       wc -l \
         ferro-cli/src/deploy/env_production.rs \
         ferro-cli/src/deploy/rewrite_ferro_version.rs \
         ferro-cli/src/templates/docker.rs \
         ferro-cli/src/templates/do.rs \
         ferro-cli/src/commands/docker_init.rs \
         ferro-cli/src/commands/do_init.rs \
         ferro-cli/src/doctor/checks/*.rs \
         ferro-cli/src/doctor/mod.rs
       ```
       Target ~375 LOC; document actual.

    Record every finding in a new file `.planning/phases/122.2-deploy-simplification/122.2-09-UAT.md` with one section per verification bullet (pass/fail + notes).

    Approve only when every SCOPE §Verification bullet passes. Any failure returns to the relevant earlier plan for fix-up.
  </how-to-verify>
  <resume-signal>Type "approved" when all SCOPE §Verification bullets pass, or describe failures to feed back into Plans 01-08.</resume-signal>
</task>

</tasks>

<verification>
- Phase-end gate green (cargo fmt/clippy/test)
- Live scaffolding against gestiscilo-it/app matches SCOPE §Verification bullets
- UAT report committed
</verification>

<success_criteria>
Phase 122.2 is releasable. Every SCOPE §Verification bullet is proven against a real project. LOC target documented.
</success_criteria>

<output>
Create `.planning/phases/122.2-deploy-simplification/122.2-09-SUMMARY.md` with pointers to the UAT report.
</output>
