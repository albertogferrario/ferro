---
phase: 122.2-deploy-simplification
plan: 08
type: execute
wave: 3
depends_on: ["122.2-05"]
files_modified:
  - ferro-cli/src/doctor/mod.rs
  - ferro-cli/src/doctor/checks
autonomous: true
requirements: [SCOPE-12]
must_haves:
  truths:
    - "ferro doctor runs exactly the 9 SCOPE §12 checks"
    - "Workspace cargo-chef target-dirs check is deleted"
    - "path-deps-warn-for-prod framing is deleted"
    - "Deploy env parity, Cargo.docker.toml staleness, generated artifacts presence, DATABASE_URL sqlite-in-prod, dirty git tree checks exist"
    - "Registry pattern, JSON output, exit-code contract preserved"
  artifacts:
    - path: "ferro-cli/src/doctor/mod.rs"
      provides: "Check registry listing the 9 checks"
      contains: "toolchain_match"
  key_links:
    - from: "ferro-cli/src/doctor/mod.rs"
      to: "individual check modules"
      via: "register calls"
      pattern: "register"
---

<objective>
Replace the Phase 124 doctor check list with the 9 checks from SCOPE §12. Delete the obsolete workspace cargo-chef check and path-deps framing.
Purpose: Fold surviving deploy verification into `ferro doctor` as a single honest entry point.
Output: `ferro doctor` runs 9 checks, preserves JSON output and exit-code semantics.
</objective>

<context>
@.planning/phases/122.2-deploy-simplification/SCOPE.md
@ferro-cli/src/doctor
@ferro-cli/src/project.rs
@ferro-cli/src/deploy/env_production.rs
</context>

<interfaces>
Uses from Plan 05:
```rust
pub fn read_env_production_keys(path: &Path) -> anyhow::Result<Vec<String>>;
pub fn read_deploy_metadata(project_root: &Path) -> anyhow::Result<FerroDeployMetadata>;
```

Existing doctor check trait/registry in ferro-cli/src/doctor/ — reuse as-is.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Inventory existing doctor checks and delete obsolete ones</name>
  <read_first>ferro-cli/src/doctor/mod.rs, ferro-cli/src/doctor</read_first>
  <files>ferro-cli/src/doctor/mod.rs, ferro-cli/src/doctor/checks</files>
  <action>
    1. Read ferro-cli/src/doctor/mod.rs to understand the Check trait signature and registry mechanism from Phase 124.

    2. Enumerate current check files under ferro-cli/src/doctor/ (likely in a `checks/` subdirectory).

    3. Delete any existing checks corresponding to the deprecated concepts:
       - Workspace cargo-chef target dir check (obsoleted by the cargo-chef changes in Plan 06)
       - Path-deps "warn for prod" framing (obsoleted by Cargo.docker.toml staleness)
       - Any deploy_check-related logic now that the CLI command is gone

    4. Update mod.rs registry to stop registering deleted checks.

    Per SCOPE §12 (deletions).
  </action>
  <acceptance_criteria>
    - `grep -Ei "workspace_cargo_chef|cargo_chef_target|path_deps_warn_for_prod" ferro-cli/src/doctor -r` → no matches
    - `cargo check -p ferro-cli` → succeeds
  </acceptance_criteria>
  <verify>
    <automated>cargo check -p ferro-cli 2>&1 | tail -5</automated>
  </verify>
  <done>Obsolete doctor checks removed, registry still compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the 9 new doctor checks and register them</name>
  <read_first>ferro-cli/src/doctor/mod.rs, ferro-cli/src/doctor/checks</read_first>
  <files>ferro-cli/src/doctor/checks, ferro-cli/src/doctor/mod.rs</files>
  <behavior>
    Each of the 9 checks is registered and returns Ok/Warn/Err with a message:
    1. toolchain_match — compare rustc -V against rust-toolchain.toml channel if present; Ok when absent.
    2. db_connection — open DATABASE_URL and run SELECT 1; Err on failure; Skipped when DATABASE_URL unset.
    3. migrations_pending — compare pending vs applied count.
    4. local_env_parity — keys in .env.example vs .env.
    5. deploy_env_parity — keys in .env.production vs commented scaffold in .do/app.yaml. Skip when either file absent.
    6. cargo_docker_toml_staleness — if Cargo.docker.toml exists, verify each ferro* dep version still matches path-dep workspace version in Cargo.toml. Warn on drift. Skip when Cargo.docker.toml absent.
    7. generated_artifacts — Dockerfile / .dockerignore / .do/app.yaml presence. Warn level for absent.
    8. database_url_sqlite_in_prod — if .env.production exists and its DATABASE_URL starts with `sqlite:` → Err. Skip when absent.
    9. dirty_git_tree — git status --porcelain non-empty OR git log @{upstream}..HEAD non-empty → Warn.
  </behavior>
  <action>
    Create one file per check under ferro-cli/src/doctor/checks/ using the existing Check trait. Suggested filenames:
    - toolchain_match.rs
    - db_connection.rs
    - migrations_pending.rs
    - local_env_parity.rs
    - deploy_env_parity.rs
    - cargo_docker_toml_staleness.rs
    - generated_artifacts.rs
    - database_url_sqlite_in_prod.rs
    - dirty_git_tree.rs

    If some checks (toolchain_match, db_connection, migrations_pending, local_env_parity) already exist from Phase 124 in serviceable form, KEEP those files and only edit their registrations. Do not rewrite working code.

    New checks to author fully:
    - deploy_env_parity.rs: use `crate::deploy::env_production::read_env_production_keys`, read .do/app.yaml as text, look for each env key as a commented line inside the `envs:` block. Missing keys → Warn with list.
    - cargo_docker_toml_staleness.rs: read Cargo.docker.toml + Cargo.toml, for each ferro* entry compare version field vs the path-dep's current workspace version.
    - generated_artifacts.rs: check file existence, Warn on absent.
    - database_url_sqlite_in_prod.rs: tiny one-line check on .env.production.
    - dirty_git_tree.rs: shell out to git.

    Update ferro-cli/src/doctor/mod.rs to register exactly these 9 checks in order. Preserve existing JSON output format and exit-code contract (non-zero on any Err, zero on Warn/Ok).

    For each new check, add a `#[cfg(test)] mod tests` block with at least one passing-case and one failing-case test using tempfile where applicable. Network/DB/git-dependent checks can be tested only for their pure logic branches.

    Per SCOPE §12.
  </action>
  <acceptance_criteria>
    - `ls ferro-cli/src/doctor/checks/ | wc -l` → 9 (or the existing layout with 9 registered check module names)
    - `grep -E "toolchain_match|db_connection|migrations_pending|local_env_parity|deploy_env_parity|cargo_docker_toml_staleness|generated_artifacts|database_url_sqlite_in_prod|dirty_git_tree" ferro-cli/src/doctor/mod.rs` → 9 hits
    - `cargo test -p ferro-cli --lib doctor` → all pass
    - `cargo clippy -p ferro-cli --all-targets -- -D warnings` → clean
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p ferro-cli --lib doctor 2>&1 | tail -30</automated>
  </verify>
  <done>9 checks registered, tests green, JSON+exit-code contract preserved.</done>
</task>

</tasks>

<verification>
- `cargo test -p ferro-cli --lib doctor` passes
- `cargo clippy -p ferro-cli --all-targets -- -D warnings` clean
</verification>

<success_criteria>
`ferro doctor` aligns with SCOPE §12 exactly: 9 checks, obsolete ones deleted.
</success_criteria>

<output>
Create `.planning/phases/122.2-deploy-simplification/122.2-08-SUMMARY.md` after completion.
</output>
