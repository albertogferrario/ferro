---
phase: 122.2-deploy-simplification
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-cli/src/commands/docker_init.rs
  - ferro-cli/src/commands/do_init.rs
  - ferro-cli/src/templates/docker.rs
  - ferro-cli/src/templates/do.rs
  - ferro-cli/src/templates/files/docker/Dockerfile.tpl
  - ferro-cli/src/templates/files/do/app.yaml.tpl
autonomous: true
requirements: [SCOPE-2, SCOPE-3, SCOPE-4, SCOPE-5]
must_haves:
  truths:
    - "docker_init and do_init command bodies are stubbed (compile-only, return Ok(()))"
    - "Old docker.rs and do.rs renderer bodies are removed"
    - "Old Dockerfile.tpl and app.yaml.tpl template files are truncated to empty markers"
  artifacts:
    - path: "ferro-cli/src/commands/docker_init.rs"
      provides: "Stub command placeholder (re-implemented in Plan 06)"
      min_lines: 5
    - path: "ferro-cli/src/commands/do_init.rs"
      provides: "Stub command placeholder (re-implemented in Plan 07)"
      min_lines: 5
  key_links:
    - from: "ferro-cli/src/main.rs"
      to: "commands::docker_init::run and commands::do_init::run"
      via: "clap subcommand dispatch"
      pattern: "docker_init|do_init"
---

<objective>
Strip the old docker_init, do_init, and their templates down to stubs that compile (SCOPE §2-§5). Plans 06/07 will re-implement them against the new contract. This plan runs in Wave 1 parallel with Plans 01 and 03 because it touches disjoint files.
Purpose: Delete heuristic-heavy legacy code early. Compiling stubs let Plan 03's main.rs edits and the workspace build stay green between waves.
Output: ferro-cli compiles with empty-but-callable docker:init and do:init subcommands.
</objective>

<context>
@.planning/phases/122.2-deploy-simplification/SCOPE.md
@ferro-cli/src/commands/docker_init.rs
@ferro-cli/src/commands/do_init.rs
@ferro-cli/src/templates/docker.rs
@ferro-cli/src/templates/do.rs
@ferro-cli/src/templates/files/docker/Dockerfile.tpl
@ferro-cli/src/templates/files/do/app.yaml.tpl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stub docker_init command and wipe docker renderer + template</name>
  <read_first>ferro-cli/src/commands/docker_init.rs, ferro-cli/src/templates/docker.rs, ferro-cli/src/templates/files/docker/Dockerfile.tpl</read_first>
  <files>ferro-cli/src/commands/docker_init.rs, ferro-cli/src/templates/docker.rs, ferro-cli/src/templates/files/docker/Dockerfile.tpl</files>
  <action>
    1. Read the current ferro-cli/src/commands/docker_init.rs to learn the exact function signature currently exported (probably `pub fn run(args: DockerInitArgs) -> Result<()>` or similar). Preserve any clap-derive args struct the CLI main.rs depends on, but reduce the struct to only `pub force: bool` (the only surviving flag per SCOPE §3). Remove `--ferro-ref`, `--runtime-deps` fields.

    2. Replace the function body with a stub that returns `Ok(())` immediately and logs a TODO-phase-122.2 note, e.g.:
       ```rust
       pub fn run(_args: DockerInitArgs) -> anyhow::Result<()> {
           // Stub — rewritten in Phase 122.2 Plan 06.
           Ok(())
       }
       ```

    3. Replace ferro-cli/src/templates/docker.rs entire contents with a minimal placeholder:
       ```rust
       // Rewritten in Phase 122.2 Plan 06.
       ```
       If other modules import functions from docker.rs, leave those function signatures as stubs returning `String::new()` or `unimplemented!()` — but check first which are actually called. Goal: crate compiles.

    4. Truncate ferro-cli/src/templates/files/docker/Dockerfile.tpl to the single line:
       ```
       # Rewritten in Phase 122.2 Plan 06
       ```

    Per SCOPE §2, §3.
  </action>
  <acceptance_criteria>
    - `grep -E "ferro-ref|runtime-deps|runtime_deps" ferro-cli/src/commands/docker_init.rs` → no matches
    - `wc -l ferro-cli/src/templates/files/docker/Dockerfile.tpl` → 1 line
    - `cargo check -p ferro-cli` → succeeds
  </acceptance_criteria>
  <verify>
    <automated>cargo check -p ferro-cli 2>&1 | tail -5</automated>
  </verify>
  <done>docker_init stubbed, docker renderer gutted, Dockerfile.tpl emptied, crate compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Stub do_init command and wipe do renderer + template</name>
  <read_first>ferro-cli/src/commands/do_init.rs, ferro-cli/src/templates/do.rs, ferro-cli/src/templates/files/do/app.yaml.tpl</read_first>
  <files>ferro-cli/src/commands/do_init.rs, ferro-cli/src/templates/do.rs, ferro-cli/src/templates/files/do/app.yaml.tpl</files>
  <action>
    1. Reduce ferro-cli/src/commands/do_init.rs DoInitArgs struct to `pub force: bool` only (remove `--region`, `--repo`, `--ferro-ref` per SCOPE §5). Replace `run()` body with a stub returning `Ok(())`.

    2. Replace ferro-cli/src/templates/do.rs entire contents with:
       ```rust
       // Rewritten in Phase 122.2 Plan 07.
       ```
       Stub any exported functions that other modules call with `unimplemented!()` if needed — check callers first.

    3. Truncate ferro-cli/src/templates/files/do/app.yaml.tpl to:
       ```
       # Rewritten in Phase 122.2 Plan 07
       ```

    Per SCOPE §4, §5.
  </action>
  <acceptance_criteria>
    - `grep -E "region|--repo|ferro-ref|ferro_ref" ferro-cli/src/commands/do_init.rs` → no matches
    - `wc -l ferro-cli/src/templates/files/do/app.yaml.tpl` → 1 line
    - `cargo check -p ferro-cli` → succeeds
  </acceptance_criteria>
  <verify>
    <automated>cargo check -p ferro-cli 2>&1 | tail -5 && wc -l ferro-cli/src/templates/files/do/app.yaml.tpl</automated>
  </verify>
  <done>do_init stubbed, do renderer gutted, app.yaml.tpl emptied, crate compiles.</done>
</task>

</tasks>

<verification>
- `cargo check -p ferro-cli` succeeds
- `ferro docker:init` and `ferro do:init` are valid subcommands but no-op
</verification>

<success_criteria>
All legacy docker/do rendering code deleted. Stubs in place so Wave 1 finishes with a green build. Plans 06 and 07 will fill in the new implementations.
</success_criteria>

<output>
Create `.planning/phases/122.2-deploy-simplification/122.2-04-SUMMARY.md` after completion.
</output>
