---
phase: 122.2-deploy-simplification
plan: 01
subsystem: ferro-mcp
tags: [deploy, cleanup, mcp]
requires: []
provides:
  - "ferro-mcp without deploy tool surface"
affects:
  - ferro-mcp/src/tools/mod.rs
  - ferro-mcp/src/service.rs
tech-stack:
  added: []
  patterns: []
key-files:
  created: []
  modified:
    - ferro-mcp/src/tools/mod.rs
    - ferro-mcp/src/service.rs
  deleted:
    - ferro-mcp/src/tools/deploy_check.rs
    - ferro-mcp/src/tools/deploy_diff_env.rs
    - ferro-mcp/src/tools/runtime_requirements.rs
    - ferro-mcp/src/tools/deploy_common.rs
decisions:
  - "Delete-before-create: no replacement for deploy MCP tools; surviving checks absorbed into ferro doctor in later plans."
metrics:
  duration: "~3 min"
  tasks: 2
  files_changed: 6
  completed: 2026-04-07
---

# Phase 122.2 Plan 01: Delete ferro-mcp deploy tools Summary

Removed the three deploy MCP tools (deploy_check, deploy_diff_env, runtime_requirements) and their shared deploy_common bridge module from ferro-mcp, unregistering their #[tool] handlers in service.rs.

## Tasks

1. Deleted four source files and dropped their `pub mod` lines from `ferro-mcp/src/tools/mod.rs`. Commit `0bcb888a`.
2. Removed the three `#[tool]` handler methods (`deploy_check`, `deploy_diff_env`, `runtime_requirements`) and their descriptions from `ferro-mcp/src/service.rs`. Verified `cargo build`, `cargo fmt`, `cargo clippy --all-targets -D warnings`, and `cargo test -p ferro-mcp` (201 passed). Commit `693fe0b3`.

## Deviations from Plan

None - plan executed exactly as written.

## Verification

- `cargo build -p ferro-mcp` — success
- `cargo fmt -p ferro-mcp` — clean
- `cargo clippy -p ferro-mcp --all-targets --no-deps -- -D warnings` — clean
- `cargo test -p ferro-mcp` — 201 passed, 0 failed
- No references to `deploy_check|deploy_diff_env|runtime_requirements|deploy_common` remain under `ferro-mcp/src/`

## Self-Check: PASSED

- FOUND: commit 0bcb888a
- FOUND: commit 693fe0b3
- MISSING (as intended): ferro-mcp/src/tools/deploy_check.rs, deploy_diff_env.rs, runtime_requirements.rs, deploy_common.rs
