---
phase: 196-dogfood-acceptance-hardening
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - ferro-mcp/src/tools/checkpoint_projection.rs
  - ferro-mcp/src/service.rs
  - docs/src/agents/checkpoint-projection.md
autonomous: true
requirements: [CHK-10]
must_haves:
  truths:
    - "Each wrapper seam that found nothing across all dogfood inputs reports not_checked by default, not a vacuous pass"
    - "The not_checked-by-default seams are documented in the MCP tool description and the agent doc"
    - "Seams that did find something (and seam 2) remain active"
    - "The stale not_implemented_phase_195 reason string is removed from the agent doc"
  artifacts:
    - path: "ferro-mcp/src/tools/checkpoint_projection.rs"
      provides: "demoted wrapper seam(s) returning NotChecked with an unproven reason"
      contains: "unproven_against_real_inputs"
    - path: "ferro-mcp/src/service.rs"
      provides: "tool description documenting not_checked-by-default seams"
      contains: "not_checked"
    - path: "docs/src/agents/checkpoint-projection.md"
      provides: "agent-facing seam-coverage documentation, stale stub removed"
      contains: "not_checked"
  key_links:
    - from: "196-ACCEPTANCE.md tally"
      to: "demoted seam functions"
      via: "only zero-finding seams are demoted"
      pattern: "SeamStatus::NotChecked"
    - from: "demoted seam SeamResult.source"
      to: "delegating validator name"
      via: "source names the validator, not checkpoint"
      pattern: "source: \"validate_"
---

<objective>
Apply evidence-driven seam demotion (D-04 / SC-4): for each wrapper seam (1, 3, 4, 5) that
produced ZERO findings across both dogfood inputs (poisoned fixture + `app/` live run, per
`196-ACCEPTANCE.md`), change its default outcome to report `not_checked` with a reason
explaining it is unproven against real inputs — rather than emitting a vacuous `pass` — and
document it as `not_checked`-by-default in the MCP tool description and the agent doc.

Purpose: a checkpoint that reports `pass` on something it never proved trains the agent to
trust a lie (the coverage-honesty invariant, CHK-03). Demotion is data-driven: only seams
that actually found nothing are demoted; seam 2 is exempt (poisoned fixture proves it), and
any wrapper seam that DID find something stays active.
Output: demoted seam dispatch + tool-description + agent-doc updates, all consistent with
the recorded tally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/196-dogfood-acceptance-hardening/196-CONTEXT.md
@.planning/phases/196-dogfood-acceptance-hardening/196-RESEARCH.md
@.planning/phases/196-dogfood-acceptance-hardening/196-PATTERNS.md
@.planning/phases/196-dogfood-acceptance-hardening/196-ACCEPTANCE.md

<interfaces>
<!-- The demotion SET is NOT known until 196-ACCEPTANCE.md is read. This plan is data-driven. -->

DECISION INPUT: read `196-ACCEPTANCE.md` (produced by Plan 03). Its per-seam tally and
"zero-finding demotion candidates" list determine EXACTLY which wrapper seam functions to
demote. Demote a seam if and only if it had 0 findings across BOTH the poisoned fixture and
the app/ live run. Do not demote a seam that found anything. Seam 2 (field_to_column) is
always exempt.

Expected from research (CONFIRM against ACCEPTANCE.md, do not assume):
- action_to_route: expected > 0 findings (unregistered feedback_form/order actions) → likely STAYS ACTIVE.
- projection_well_formed, rendered_view, props_to_contract: likely 0 findings → likely DEMOTED.
- field_to_column: exempt (poisoned fixture).

Wrapper seam functions (checkpoint_projection.rs):
```rust
fn projection_well_formed_seam(project_root: &Path, name: &str) -> SeamResult   // :355  source "validate_projection"
fn action_to_route_seam(...) -> SeamResult                                       // :409  source "json_ui_verify_action"
fn rendered_view_seam(project_root: &Path, name: &str) -> SeamResult            // :492  source "render_projection" / "json_ui_validate_spec"
fn props_to_contract_seam(project_root: &Path, service_name: &str) -> SeamResult // :580 source "validate_contracts"
```

NotChecked SeamResult shape (reason always Some, source = delegating validator, findings empty):
```rust
SeamResult {
    seam: "<canonical_name>".to_string(),
    status: SeamStatus::NotChecked,
    source: "<delegating_validator>".to_string(),
    findings: vec![],
    reason: Some("unproven_against_real_inputs".to_string()),
}
```

service.rs tool description (lines 1600-1610): append a `**Seam coverage:**` paragraph
using `\n\n`, matching the existing `**Bold label:** sentence.` style.

docs/src/agents/checkpoint-projection.md:
- line 46: stale `"reason": "not_implemented_phase_195"` — replace with a current real reason.
- after the not_checked section: add a seam-coverage note listing demoted seams + why.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demote zero-finding wrapper seams to not_checked-by-default (code)</name>
  <files>ferro-mcp/src/tools/checkpoint_projection.rs</files>
  <read_first>
    - .planning/phases/196-dogfood-acceptance-hardening/196-ACCEPTANCE.md (the per-seam tally + demotion candidate list — this is the authoritative decision input)
    - ferro-mcp/src/tools/checkpoint_projection.rs (lines 355-490 for the four wrapper seam bodies and their current source strings; lines 250-258 for the NotChecked literal shape)
    - .planning/phases/196-dogfood-acceptance-hardening/196-PATTERNS.md (Zero-finding seam demotion section, lines 217-247; NotChecked construction, lines 300-315)
  </read_first>
  <action>
    Read `196-ACCEPTANCE.md`. For EACH wrapper seam (1 projection_well_formed,
    3 action_to_route, 4 rendered_view, 5 props_to_contract) whose recorded tally is 0
    across BOTH dogfood inputs, replace that seam function's body with a `not_checked`
    literal return. Preserve the function signature; prefix now-unused params with `_` to
    satisfy clippy. The `source` field MUST keep the delegating validator name (NOT
    "checkpoint") per SC-4. Example for projection_well_formed (apply the same shape only to
    the seams ACCEPTANCE.md marks zero-finding):

    ```rust
    fn projection_well_formed_seam(_project_root: &Path, _name: &str) -> SeamResult {
        // Demoted (Phase 196 D-04): produced zero findings across all dogfood inputs
        // (poisoned fixture + app/ live run, see 196-ACCEPTANCE.md). Reported as
        // not_checked to preserve the coverage-honesty invariant (CHK-03) rather than
        // emitting a vacuous pass for an unproven check.
        SeamResult {
            seam: "projection_well_formed".to_string(),
            status: SeamStatus::NotChecked,
            source: "validate_projection".to_string(),
            findings: vec![],
            reason: Some("unproven_against_real_inputs".to_string()),
        }
    }
    ```

    For each demoted seam use its own canonical name + delegating source:
    - projection_well_formed → source "validate_projection"
    - action_to_route → source "json_ui_verify_action"
    - rendered_view → source "render_projection" (match the seam's existing source string)
    - props_to_contract → source "validate_contracts"

    DO NOT demote: seam 2 (field_to_column, exempt), and any wrapper seam ACCEPTANCE.md
    records with ≥1 finding (expected: action_to_route stays active). Leave active seams
    exactly as they are.

    Then update the tests so they remain green: any existing test that exercised a now-demoted
    seam expecting Pass/Fail must be updated to expect `SeamStatus::NotChecked` with reason
    `Some("unproven_against_real_inputs")`. The dogfood test (Plan 03) iterates real seams
    and tallies findings; a demoted seam contributes 0 findings, so total_findings stays > 0
    as long as the active seam(s) still fire — confirm the dogfood test still passes.
  </action>
  <verify>
    <automated>grep -c "unproven_against_real_inputs" ferro-mcp/src/tools/checkpoint_projection.rs returns >= 1; cargo test -p ferro-mcp checkpoint_projection -- --nocapture exits 0</automated>
  </verify>
  <done>Exactly the zero-finding wrapper seams (per ACCEPTANCE.md) return NotChecked with reason "unproven_against_real_inputs" and the delegating source; active seams unchanged; all checkpoint tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Document not_checked-by-default seams in service.rs and the agent doc</name>
  <files>ferro-mcp/src/service.rs, docs/src/agents/checkpoint-projection.md</files>
  <read_first>
    - .planning/phases/196-dogfood-acceptance-hardening/196-ACCEPTANCE.md (which seams were demoted)
    - ferro-mcp/src/service.rs (lines 1598-1621 — the checkpoint_projection tool description block)
    - docs/src/agents/checkpoint-projection.md (line 46 stale stub; the not_checked section around lines 40-65 and 120-130)
    - .planning/phases/196-dogfood-acceptance-hardening/196-PATTERNS.md (service.rs + docs update sections, lines 251-277)
  </read_first>
  <action>
    1. ferro-mcp/src/service.rs — append a `**Seam coverage:**` paragraph to the existing
       `checkpoint_projection` tool description string (after the `**Combine with:**`
       paragraph, joined with `\n\n`). It must NAME the seams that are not_checked-by-default
       (the exact set demoted in Task 1) and state they were unproven across dogfood inputs
       and are reported not_checked rather than passing vacuously. Match the existing
       `**Bold label:** sentence.` style. Do not restructure the existing paragraphs.
       Example wording (adjust seam list to the actual demoted set):
       `\n\n**Seam coverage:** Seams reported not_checked-by-default — {list} — were unproven \
       across dogfood inputs (Phase 196) and are surfaced as not_checked rather than passed, \
       preserving the coverage-honesty invariant. Seam field_to_column is proven by an \
       acceptance fixture; action_to_route is proven against the in-repo sample.`

    2. docs/src/agents/checkpoint-projection.md:
       a. Line 46: replace the stale example reason `"not_implemented_phase_195"` with a
          current real reason — `"unproven_against_real_inputs"` if that seam was demoted,
          otherwise an actual reason emitted by a live seam (e.g. `"source_model_unresolved"`).
       b. Add a short seam-coverage note (a paragraph or table row) after the existing
          not_checked material listing the demoted seams and the one-line rationale, citing
          the dogfood evidence. Keep seam names consistent with the Phase 195 canonical set
          (projection_well_formed, field_to_column, action_to_route, rendered_view,
          props_to_contract).

    Voice: neutral architectural documentation. No marketing language, no internal-strategy
    framing (these files are public-facing per repository-document discipline).
  </action>
  <verify>
    <automated>grep -c "not_checked" ferro-mcp/src/service.rs returns >= 1; grep -c "not_implemented_phase_195" docs/src/agents/checkpoint-projection.md returns 0; grep -c "not_checked" docs/src/agents/checkpoint-projection.md returns >= 1</automated>
  </verify>
  <done>service.rs tool description names the not_checked-by-default seams; the agent doc has a seam-coverage note and no stale `not_implemented_phase_195` string; both consistent with the demoted set in ACCEPTANCE.md.</done>
</task>

<task type="auto">
  <name>Task 3: Full quality gate + roadmap success-criteria confirmation</name>
  <files>(workspace-wide verification — no single file)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 196 success criteria, lines 2417-2421)
    - CLAUDE.md (Testing & Linting section — exact gate command)
  </read_first>
  <action>
    Run the full workspace gate (serially, one CPU-intensive op at a time per the memory
    note; check `df` / clean `target/` first if disk-full risk):
    `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features`

    Confirm all four Phase 196 success criteria hold:
    - SC-1: poisoned_projection_dangling_field_acceptance passes (Plan 02).
    - SC-2: dogfood_app_projections passes / 196-ACCEPTANCE.md is GO (Plan 03).
    - SC-3: next_steps_cap_at_five passes; cap is 5 everywhere (Plan 01).
    - SC-4: demoted seams are documented not_checked-by-default in service.rs + the agent doc (Tasks 1-2).

    If the full gate surfaces any clippy warning or test failure introduced by this phase,
    fix it here — do not defer.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features</automated>
  </verify>
  <done>Full workspace gate green; all four Phase 196 success criteria confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Changes seam default outcomes (read-only introspection), a tool-description string, and documentation. No new input surface, auth, or egress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-196-05 | Information Disclosure | demoted seams reporting not_checked | mitigate | Demotion makes the verdict MORE honest: it reports not_checked instead of a vacuous pass, reducing the risk of an agent acting on a false assurance. The change strictly narrows what the tool claims to have verified. |

No high-severity threats introduced. Coverage-honesty invariant (CHK-03) is reinforced, not weakened.
</threat_model>

<verification>
- `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` green
- Demoted seams (per ACCEPTANCE.md) return NotChecked with reason "unproven_against_real_inputs" and delegating source
- `grep -c "not_implemented_phase_195" docs/src/agents/checkpoint-projection.md` returns 0
- service.rs tool description and the agent doc name the not_checked-by-default seams
</verification>

<success_criteria>
- SC-4 satisfied: every zero-finding wrapper seam is not_checked-by-default in code AND documented (not silently omitted); active seams retained; seam 2 exempt.
- All four Phase 196 success criteria confirmed under the full gate.
</success_criteria>

<output>
After completion, create `.planning/phases/196-dogfood-acceptance-hardening/196-04-SUMMARY.md`
(list exactly which seams were demoted and which stayed active, with the ACCEPTANCE.md tally).
</output>
