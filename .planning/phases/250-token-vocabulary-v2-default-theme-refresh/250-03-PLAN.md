---
phase: 250-token-vocabulary-v2-default-theme-refresh
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - docs/src/features/themes.md
  - ferro-theme/assets/default.css
autonomous: false
requirements: [DS-02]

must_haves:
  truths:
    - "themes.md documents 30 semantic token slots with v2 token tables (density, motion, focus ring, display font)"
    - "themes.md documents the root-font-size type-scaling recipe"
    - "The refreshed default theme reads as designed — cool-tinted neutrals, single sparing accent, dark not gloomy — verified visually in light and dark"
    - "The full CI-exact gate (fmt + clippy --all-features + test --all-features) is green before sign-off"
  artifacts:
    - path: "docs/src/features/themes.md"
      provides: "v2 token reference (30 slots) + type-scaling section"
      contains: "--motion-duration-fast"
    - path: "ferro-theme/assets/default.css"
      provides: "final oklch values approved by visual verification (nudged only if the operator requests)"
      contains: "--color-ring:"
  key_links:
    - from: "docs/src/features/themes.md"
      to: "ferro-theme/src/token.rs"
      via: "token names + defaults documented must match the shipped constants"
      pattern: "motion-duration"
    - from: "ferro-theme/assets/default.css"
      to: "sample app/ render"
      via: "ThemeMiddleware default_theme injection, verified via Chrome MCP"
      pattern: "default_theme"
---

<objective>
Close DS-02: document the v2 vocabulary (30 slots) and the root-font-size type-scaling
recipe in `themes.md`, then visually verify that the refreshed default theme reads as
the documented design language in both light and dark before running the full CI gate.

Purpose: The docs are the human-facing surface of the token system, and the visual
sign-off is the only non-mechanical acceptance criterion in this phase — the aesthetic
judgment that "a scaffolded ferro app reads as designed, not templated." This plan is
the last wave; it also runs the full CI-exact gate before verify-work.

Output: updated `themes.md`, an approved (possibly nudged) `default.css`, and screenshot
evidence of the light/dark refresh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/250-token-vocabulary-v2-default-theme-refresh/250-CONTEXT.md
@.planning/phases/250-token-vocabulary-v2-default-theme-refresh/250-PATTERNS.md
@.planning/phases/250-token-vocabulary-v2-default-theme-refresh/250-VALIDATION.md
@docs/superpowers/specs/2026-07-03-json-ui-design-system-design.md

<interfaces>
<!-- Current state of docs/src/features/themes.md -->
- Line 9: "...defining the 23 semantic token slots (colors, shapes, shadows, typography)"
- Line 29: "...with all 23 token slots pre-filled with defaults..."
- Line 55: "## Token Reference"
- Line 57: "All 23 semantic token slots. Components use these class names..."
- Token tables: Surface (6), Role (8), Shape (4), Shadow (3), Typography (2) — markdown tables Token | Default | Purpose
- BUG in current doc (lines 104-105): the Typography table lists `--font-family-sans` / `--font-family-mono`, but the real token constants are `--font-sans` / `--font-mono` (see ferro-theme/src/token.rs). Fix this discrepancy while editing.
- Dark Mode section (line 107+): shows `@theme` inside `@media` — note the scaffold actually emits plain `:root`, not `@theme` (pre-existing doc drift; correcting is optional, out of this phase's required scope).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document token vocabulary v2 + type-scaling recipe in themes.md</name>
  <files>docs/src/features/themes.md</files>
  <read_first>
    - docs/src/features/themes.md (full file — the three "23" occurrences, the token tables, the Typography table bug)
    - docs/superpowers/specs/2026-07-03-json-ui-design-system-design.md §1 (v2 token table + design-language defaults + exclusions — source of truth for documented values)
    - .planning/phases/250-token-vocabulary-v2-default-theme-refresh/250-PATTERNS.md (themes.md section — exact tables + type-scaling recipe to add)
  </read_first>
  <action>
Edit `docs/src/features/themes.md`:

1. Update the three count references: `23 semantic token slots` -> `30 semantic token slots` (line 9), `all 23 token slots pre-filled` -> `all 30 token slots pre-filled` (line 29), `All 23 semantic token slots.` -> `All 30 semantic token slots.` (line 57).

2. Fix the Typography table token-name discrepancy — change `--font-family-sans` -> `--font-sans` and `--font-family-mono` -> `--font-mono` (they must match the constants in ferro-theme/src/token.rs).

3. After the Typography Tokens (2) table, add four new token-reference sections:
```markdown
### Density Token (1)

| Token | Default | Purpose |
|-------|---------|---------|
| `--spacing` | `0.25rem` | Base spacing unit; all spacing utilities scale as `calc(var(--spacing) * N)` — one knob rescales all padding/margin/gap |

### Motion Tokens (4)

| Token | Default | Purpose |
|-------|---------|---------|
| `--motion-duration-fast` | `120ms` | Micro-interactions: hover, toggles, controls (anything repeated) |
| `--motion-duration-base` | `220ms` | Standard transitions: dropdowns, modals, toasts |
| `--motion-duration-slow` | `320ms` | Large surfaces: drawers, page-level reveals |
| `--motion-ease` | `cubic-bezier(0.2, 0, 0.38, 0.9)` | Standard easing curve — calm, settled, no bounce |

Base CSS collapses all three durations to `0.01ms` under `prefers-reduced-motion: reduce`.

### Focus Ring Token (1)

| Token | Default (light) | Purpose |
|-------|-----------------|---------|
| `--color-ring` | primary-family oklch (`oklch(55% 0.2 250)`) | Uniform `focus-visible` ring for interactive components — accessibility as a token |

### Display Font Token (1)

| Token | Default | Purpose |
|-------|---------|---------|
| `--font-display` | `var(--font-sans)` | Display/heading font family |
```

4. Add a "Type Scaling" section (per DS-02 / the deliberate exclusion of per-size type tokens — root `font-size` is the documented mechanism):
```markdown
## Type Scaling

The token vocabulary deliberately omits per-size type tokens. To scale all text,
set the root `font-size` in your theme's `tokens.css` — Tailwind's text sizes are
`rem`-based, so they all shift relative to the browser default:

    :root {
      font-size: 14px;   /* compact — all rem-based sizes scale down */
    }

Spacing scales independently via the `--spacing` density token; root `font-size`
controls the type `rem` anchor. Font weight is likewise not tokenized — there is no
demonstrated theming need.
```

5. Add a one-line migration note near the Token Reference intro stating that v2 is fully backward compatible: every valid v1 theme remains a valid v2 theme unchanged (new slots ship with defaults).
  </action>
  <verify>
    <automated>grep -q '30 semantic token slots' docs/src/features/themes.md && grep -q -- '--motion-duration-fast' docs/src/features/themes.md && grep -q '## Type Scaling' docs/src/features/themes.md && grep -q 'font-size: 14px' docs/src/features/themes.md && echo THEMES_DOC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '30 semantic token slots' docs/src/features/themes.md` returns at least 2
    - `grep -c '23 semantic token slots\|all 23 token slots' docs/src/features/themes.md` returns 0 (no stale count)
    - `grep -- '--motion-duration-fast' docs/src/features/themes.md` matches AND `grep -- '--spacing' docs/src/features/themes.md` matches AND `grep -- '--color-ring' docs/src/features/themes.md` matches AND `grep -- '--font-display' docs/src/features/themes.md` matches
    - `grep '## Type Scaling' docs/src/features/themes.md` matches AND `grep 'font-size: 14px' docs/src/features/themes.md` matches
    - `grep -c -- '--font-family-sans\|--font-family-mono' docs/src/features/themes.md` returns 0 (Typography name bug fixed); `grep -- '--font-sans' docs/src/features/themes.md` matches
  </acceptance_criteria>
  <done>themes.md documents 30 slots with v2 token tables, the type-scaling recipe, the backward-compat note, and the Typography token-name bug is fixed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual sign-off of the refreshed default theme + full CI gate</name>
  <files>ferro-theme/assets/default.css</files>
  <read_first>
    - ferro-theme/assets/default.css (the refreshed values from Plan 02 — the subject of the visual check)
    - docs/superpowers/specs/2026-07-03-json-ui-design-system-design.md §1 "Design language defaults" (the aesthetic bar: cool-tinted neutrals, single sparing accent, dark not gloomy, separation via spacing/contrast before borders)
    - CLAUDE.md "UI Design Principles" + "Server Management" (ask before starting servers; test UI with Chrome MCP)
  </read_first>
  <action>
This is a human-verify checkpoint. Plan 01 regenerated ferro-base.css with the new
utilities; Plan 02 refreshed default.css to cool-tinted neutrals + a harmonized hue-250
accent + the focus-ring token. Confirm the default theme reads as designed in both
light and dark, and run the full CI-exact gate before phase sign-off.

Executor steps, performed BEFORE pausing for the human:

1. Run the full CI-exact gate, one CPU-intensive command at a time (never chained/parallel — project serialization convention):
   - `cargo fmt --all -- --check`
   - `cargo clippy --all --all-targets --all-features -- -D warnings`
   - `cargo test --all-features`
   If `cargo test --all-features` disk-full-fails (ENOSPC), check `df` and clean `target/`, then retry — an environment issue, not a defect. A full `cargo test` may regenerate `docs/protocol/schemas/*.json`; `git checkout` that unrelated churn rather than folding it into the phase commit.
2. ASK the operator for approval to start the sample app dev server (project convention — never start a server unprompted). Only after approval, build and start the sample `app/`.
3. Using Chrome MCP, capture screenshots of representative `app/` pages in BOTH light and dark (toggle `data-theme="dark"` on `<html>` or OS preference): a list/table page, a form page, and a dashboard/detail page if available.
4. Present the screenshots and confirm against the design bar:
   - neutrals read cool-tinted (not flat grey); dark is dark but not gloomy (tinted, lightness not crushed);
   - one focal accent used sparingly — the view reads as one dominant neutral field with a single focal color;
   - focus rings (if any interactive element is focused) show `--color-ring` at visible contrast;
   - nothing looks broken relative to the pre-refresh baseline (spacing/radii/shadows intact).

If the operator requests oklch nudges, apply them to `ferro-theme/assets/default.css`, re-run `cargo test -p ferro-theme default_theme_returns_all_30_token_slots -- --exact`, re-screenshot, and re-present until approved. Stop the dev server when done.

Resume signal: operator types "approved" to accept the refresh, or describes the specific oklch nudges to apply.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo test -p ferro-theme default_theme_returns_all_30_token_slots -- --exact</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy --all --all-targets --all-features -- -D warnings` exits 0
    - `cargo test --all-features` exits 0
    - Screenshots captured for light AND dark and presented to the operator
    - Operator has typed "approved" (or the executor applied requested nudges to default.css and re-verified until approved)
    - If default.css was nudged: `cargo test -p ferro-theme default_theme_returns_all_30_token_slots -- --exact` exits 0 after the final edit
  </acceptance_criteria>
  <done>Full CI-exact gate green; light + dark screenshots presented; operator approved the design refresh (with any requested oklch nudges applied and re-verified).</done>
  <resume-signal>Type "approved" to accept the design refresh, or describe the specific oklch nudges to apply.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan edits documentation and CSS custom-property values and runs the local test gate; no runtime trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-250-04 | (n/a) | docs + default.css oklch values | accept | Documentation and CSS token values carry no authentication, network, storage, or privilege surface. The visual verification is an aesthetic gate, not a security control. RESEARCH Security Domain concluded "not applicable." |

Spoofing, Tampering, Repudiation, Information disclosure, DoS, and Elevation are not applicable to this plan.
</threat_model>

<verification>
Full CI-exact gate (run once, sequentially — this is the last wave):
- `cargo fmt --all -- --check`
- `cargo clippy --all --all-targets --all-features -- -D warnings`
- `cargo test --all-features`

Docs check: `grep '30 semantic token slots' docs/src/features/themes.md` and `grep '## Type Scaling' docs/src/features/themes.md`.

Visual sign-off: operator approval on light + dark screenshots of the sample `app/`.
</verification>

<success_criteria>
- themes.md documents 30 slots, the v2 token tables, and the root-font-size type-scaling recipe; the Typography token-name bug is fixed.
- The default theme is visually confirmed as the documented design language (cool-tinted neutrals, single sparing accent, dark not gloomy) in both modes.
- The full CI-exact gate is green.
</success_criteria>

<output>
After completion, create `.planning/phases/250-token-vocabulary-v2-default-theme-refresh/250-03-SUMMARY.md`
</output>
