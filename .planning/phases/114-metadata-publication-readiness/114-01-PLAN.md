---
phase: 114-metadata-publication-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-broadcast/Cargo.toml
  - ferro-theme/Cargo.toml
  - ferro-projections/Cargo.toml
  - framework/Cargo.toml
  - ferro-json-ui/README.md
  - ferro-lang/README.md
  - ferro-whatsapp/README.md
  - ferro-json-ui/Cargo.toml
  - ferro-lang/Cargo.toml
  - ferro-whatsapp/Cargo.toml
autonomous: true
requirements:
  - META-01
  - META-03
  - META-04

must_haves:
  truths:
    - "ferro-broadcast, ferro-theme, and ferro-projections Cargo.toml files have readme, homepage, and categories fields"
    - "framework Cargo.toml homepage points to https://ferro-rs.dev (not GitHub)"
    - "ferro-json-ui, ferro-lang, and ferro-whatsapp READMEs contain 30-50 lines with title, description, features, usage example, docs link, and license"
    - "ferro-json-ui and ferro-lang lib.rs have crate-level //! doc comments (already satisfied, verified)"
  artifacts:
    - path: "ferro-broadcast/Cargo.toml"
      provides: "Complete publication metadata"
      contains: "homepage"
    - path: "ferro-theme/Cargo.toml"
      provides: "Complete publication metadata"
      contains: "categories"
    - path: "ferro-projections/Cargo.toml"
      provides: "Complete publication metadata"
      contains: "categories"
    - path: "ferro-json-ui/README.md"
      provides: "Expanded crate README"
      min_lines: 30
    - path: "ferro-lang/README.md"
      provides: "Expanded crate README"
      min_lines: 30
    - path: "ferro-whatsapp/README.md"
      provides: "Expanded crate README"
      min_lines: 30
  key_links: []
---

<objective>
Fix Cargo.toml metadata gaps across publication-target crates and expand stub READMEs to meaningful content.

Purpose: All crates need complete metadata for crates.io publication. Three crates have stub READMEs that provide no useful information.
Output: Complete Cargo.toml metadata on all gap crates, three expanded READMEs, META-04 verified as already satisfied.
</objective>

<execution_context>
@/Users/alberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-metadata-publication-readiness/114-CONTEXT.md
@.planning/phases/114-metadata-publication-readiness/114-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Cargo.toml metadata gaps across all target crates</name>
  <files>ferro-broadcast/Cargo.toml, ferro-theme/Cargo.toml, ferro-projections/Cargo.toml, framework/Cargo.toml, ferro-json-ui/Cargo.toml, ferro-lang/Cargo.toml, ferro-whatsapp/Cargo.toml</files>
  <action>
Add missing metadata fields to Cargo.toml files. Per user decision, `homepage = "https://ferro-rs.dev"` for all crates.

**ferro-broadcast/Cargo.toml** — add after `categories` line:
- `homepage = "https://ferro-rs.dev"`
- `readme = "README.md"`
(categories already present: `["web-programming", "asynchronous"]`)

**ferro-theme/Cargo.toml** — add after `keywords` line:
- `homepage = "https://ferro-rs.dev"`
- `readme = "README.md"`
- `categories = ["web-programming"]`
(Add 5th keyword "css" for consistency: `keywords = ["theme", "tokens", "ferro", "sdui", "css"]`)

**ferro-projections/Cargo.toml** — add after `keywords` line:
- `homepage = "https://ferro-rs.dev"`
- `readme = "README.md"`
- `categories = ["web-programming", "data-structures"]`

**framework/Cargo.toml** — change homepage from `"https://github.com/albertogferrario/ferro"` to `"https://ferro-rs.dev"` (Pitfall 3 from research).

**ferro-json-ui/Cargo.toml** — change homepage from `"https://github.com/albertogferrario/ferro"` to `"https://ferro-rs.dev"`.

**ferro-lang/Cargo.toml** — add `homepage = "https://ferro-rs.dev"` (has readme and categories already).

**ferro-whatsapp/Cargo.toml** — add `homepage = "https://ferro-rs.dev"` (has readme and categories already).

Do NOT touch other crates not listed here (ferro-ai, ferro-api-mcp, ferro-cache, etc.) — scope limited to META-01 targets plus crates also touched by META-03.
  </action>
  <verify>
    <automated>grep -E "^(homepage|readme|categories)" ferro-broadcast/Cargo.toml ferro-theme/Cargo.toml ferro-projections/Cargo.toml framework/Cargo.toml ferro-json-ui/Cargo.toml ferro-lang/Cargo.toml ferro-whatsapp/Cargo.toml</automated>
  </verify>
  <done>ferro-broadcast, ferro-theme, ferro-projections have all three fields (readme, homepage, categories). framework and ferro-json-ui homepages point to ferro-rs.dev. ferro-lang and ferro-whatsapp have homepage field.</done>
</task>

<task type="auto">
  <name>Task 2: Expand stub READMEs and verify META-04</name>
  <files>ferro-json-ui/README.md, ferro-lang/README.md, ferro-whatsapp/README.md</files>
  <action>
Expand each README to ~30-50 lines using the consistent template from user decisions: title, description, features list, usage example, link to docs, license.

**ferro-json-ui/README.md** — Currently 9 lines. Expand to cover:
- Title: `# ferro-json-ui`
- Description: JSON-based server-driven UI schema types for the Ferro framework. Define UI components, layouts, and data bindings as structured JSON that Ferro renders to HTML.
- Features list: 30+ built-in components (tables, forms, cards, alerts, etc.), layout system (stack, grid, sidebar, dashboard), action system (navigate, submit, API calls), data binding with validation error resolution, plugin system for custom components, compile-time schema validation
- Usage example (illustrative): Show creating a simple page with `JsonUiView` containing a header and table component
- Documentation link: `https://docs.ferro-rs.dev`
- License: MIT

**ferro-lang/README.md** — Currently 9 lines. Expand to cover:
- Title: `# ferro-lang`
- Description: Localization crate for the Ferro framework. JSON-based translation loading with per-request locale detection, interpolation, and pluralization.
- Features list: JSON translation files, per-request locale detection (Accept-Language, query param, session), string interpolation with `:name` syntax, pluralization rules, validation message localization, middleware for automatic locale setting
- Usage example (illustrative): Show loading translations and using `t("messages.welcome", &[("name", "World")])` pattern
- Documentation link: `https://docs.ferro-rs.dev`
- License: MIT

**ferro-whatsapp/README.md** — Currently 3 lines (just title + description). Expand to cover:
- Title: `# ferro-whatsapp`
- Description: WhatsApp Business Cloud API integration for the Ferro framework. Send text and template messages, process inbound webhooks with HMAC-SHA256 verification, and deduplicate messages by wamid.
- Features list: Outbound messaging (text + templates), inbound webhook processing, HMAC-SHA256 signature verification, message deduplication with pluggable store trait, event-driven architecture (WhatsAppTextReceived, WhatsAppStatusUpdate events), queue integration for async sending
- Usage example (illustrative): Show configuring WhatsApp and sending a text message
- Documentation link: `https://docs.ferro-rs.dev`
- License: MIT

**META-04 verification:** After writing READMEs, verify that `ferro-json-ui/src/lib.rs` and `ferro-lang/src/lib.rs` both contain `//!` crate-level doc comment blocks. Research confirms they already do (39 lines and 23 lines respectively). No changes needed — just verify and note in summary.
  </action>
  <verify>
    <automated>wc -l ferro-json-ui/README.md ferro-lang/README.md ferro-whatsapp/README.md && echo "---" && grep -c "^//!" ferro-json-ui/src/lib.rs ferro-lang/src/lib.rs</automated>
  </verify>
  <done>All three READMEs contain 30+ lines with title, description, features, usage example, docs link, and license. ferro-json-ui/src/lib.rs and ferro-lang/src/lib.rs confirmed to have crate-level //! doc comments (META-04 verified, not re-implemented).</done>
</task>

</tasks>

<verification>
- `cargo check --all-features` passes (Cargo.toml changes don't break anything)
- All three META-01 target Cargo.toml files have `readme`, `homepage`, `categories` fields
- framework/Cargo.toml homepage is `https://ferro-rs.dev`
- All three META-03 target READMEs exceed 30 lines
- META-04 verified: both lib.rs files have `//!` blocks
</verification>

<success_criteria>
- ferro-broadcast, ferro-theme, ferro-projections Cargo.toml files have no missing metadata fields
- framework homepage updated from GitHub URL to ferro-rs.dev
- ferro-json-ui, ferro-lang, ferro-whatsapp READMEs contain 30-50 lines of meaningful content
- ferro-json-ui and ferro-lang lib.rs confirmed to have //! doc comments
</success_criteria>

<output>
After completion, create `.planning/phases/114-metadata-publication-readiness/114-01-SUMMARY.md`
</output>
