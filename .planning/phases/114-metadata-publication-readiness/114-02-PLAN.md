---
phase: 114-metadata-publication-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - framework/src/lib.rs
  - framework/src/validation/rules.rs
  - framework/src/routing/macros.rs
  - framework/src/http/resources/pagination.rs
  - framework/src/inertia/context.rs
  - framework/src/http/cookie.rs
  - framework/src/session/driver/database.rs
  - framework/src/metrics/mod.rs
  - framework/src/schedule/expression.rs
  - framework/src/debug/mod.rs
  - framework/src/server.rs
  - framework/src/http/request.rs
  - framework/src/http/response.rs
  - framework/src/config/mod.rs
  - framework/src/config/env.rs
  - framework/src/routing/router.rs
  - framework/src/middleware/metrics.rs
  - framework/src/http/mod.rs
  - framework/src/database/config.rs
autonomous: true
requirements:
  - META-02

must_haves:
  truths:
    - "framework crate compiles with #![warn(missing_docs)] without any warnings"
    - "All public items in framework/src/ have doc comments"
    - "The #![warn(missing_docs)] attribute is permanently present in framework/src/lib.rs"
  artifacts:
    - path: "framework/src/lib.rs"
      provides: "#![warn(missing_docs)] attribute + doc comments on macros and module declarations"
      contains: "#![warn(missing_docs)]"
    - path: "framework/src/validation/rules.rs"
      provides: "Doc comments on 24 pub items (rule structs, const fn constructors)"
    - path: "framework/src/routing/macros.rs"
      provides: "Doc comments on 13 pub macro re-exports"
    - path: "framework/src/http/resources/pagination.rs"
      provides: "Doc comments on 10 struct fields"
    - path: "framework/src/http/cookie.rs"
      provides: "Doc comments on 9 enum variants and struct fields"
  key_links:
    - from: "framework/src/lib.rs"
      to: "all framework/src/**/*.rs"
      via: "#![warn(missing_docs)] lint attribute"
      pattern: "#!\\[warn\\(missing_docs\\)\\]"
---

<objective>
Add `#![warn(missing_docs)]` to the framework crate and fix all 136 documentation warnings so the crate compiles cleanly with the lint enabled.

Purpose: Publication-ready crates need doc coverage enforcement. The framework is the main public crate and must lead by example.
Output: Zero `missing_docs` warnings across all 19 framework source files. Lint attribute permanently in lib.rs.
</objective>

<execution_context>
@/Users/alberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-metadata-publication-readiness/114-CONTEXT.md
@.planning/phases/114-metadata-publication-readiness/114-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add #![warn(missing_docs)] and fix all warnings in lib.rs and high-count files</name>
  <files>framework/src/lib.rs, framework/src/validation/rules.rs, framework/src/routing/macros.rs, framework/src/http/resources/pagination.rs, framework/src/inertia/context.rs, framework/src/http/cookie.rs, framework/src/session/driver/database.rs, framework/src/metrics/mod.rs, framework/src/schedule/expression.rs, framework/src/debug/mod.rs</files>
  <action>
Add `#![warn(missing_docs)]` as the first line of `framework/src/lib.rs`, before all `pub mod` declarations.

Then fix all missing doc warnings in the 10 highest-count files (106 of 136 warnings):

**framework/src/lib.rs (6 warnings):**
- Add crate-level `//!` doc comment block (title + one-line description)
- Add `/// ...` to `pub mod` declarations that lack them (config, debug, metrics, etc.)
- Add `/// ...` to `json_response!` and `text_response!` macros (Pitfall 2 from research)

**framework/src/validation/rules.rs (24 warnings):**
- Add one-liner `/// ...` doc comments to each pub struct (rule types like `Required`, `Email`, `Min`, etc.)
- Add `/// ...` to each const fn constructor (`required()`, `email()`, `min()`, etc.)
- Style: "Validates that the field {behavior}." for structs, "Creates a {rule_name} validation rule." for constructors

**framework/src/routing/macros.rs (13 warnings):**
- Add `/// ...` to each pub macro_rules! or pub fn used for routing
- Style: brief one-liner describing what the macro generates

**framework/src/http/resources/pagination.rs (10 warnings):**
- Add `/// ...` to struct fields on `PaginationMeta`, `PaginationLinks`, etc.
- Style: "The {meaning} of the paginated result."

**framework/src/inertia/context.rs (9 warnings):**
- Add `/// ...` to struct fields
- Style: brief field descriptions

**framework/src/http/cookie.rs (9 warnings):**
- Add `/// ...` to `SameSite` enum variants and `CookieOptions` struct fields
- Style: "The {variant/field} {behavior}."

**framework/src/session/driver/database.rs (8 warnings):**
- Add `/// ...` to associated functions and struct fields

**framework/src/metrics/mod.rs (8 warnings):**
- Add `/// ...` to `MetricsSnapshot`, `RouteMetrics`, etc. struct fields and functions

**framework/src/schedule/expression.rs (7 warnings):**
- Add `/// ...` to `DayOfWeek` enum variants
- Style: "Represents {day}."

**framework/src/debug/mod.rs (7 warnings):**
- Add `/// ...` to debug info struct fields

Doc comment guidelines (per user decisions):
- One-liners for obvious items (struct fields, enum variants, simple functions)
- More detail for complex APIs (multi-line with examples if the function signature is non-obvious)
- Scientific and minimalistic style (per CLAUDE.md: "no marketing language")

After fixing these 10 files, run `cargo rustc --package ferro-rs --lib -- -W missing-docs 2>&1 | grep "^warning:" | wc -l` to confirm remaining count is ~30.
  </action>
  <verify>
    <automated>cargo rustc --package ferro-rs --lib -- -W missing-docs 2>&1 | grep "^warning:" | wc -l</automated>
  </verify>
  <done>Remaining warning count is 30 or fewer. The 10 highest-count files are fully documented. #![warn(missing_docs)] is present in lib.rs.</done>
</task>

<task type="auto">
  <name>Task 2: Fix remaining missing_docs warnings in 9 lower-count files</name>
  <files>framework/src/server.rs, framework/src/http/request.rs, framework/src/http/response.rs, framework/src/config/mod.rs, framework/src/config/env.rs, framework/src/routing/router.rs, framework/src/middleware/metrics.rs, framework/src/http/mod.rs, framework/src/database/config.rs</files>
  <action>
Fix all remaining missing_docs warnings in the 9 lower-count files (30 of 136 warnings):

**framework/src/server.rs (6 warnings):**
- Add `/// ...` to pub methods on Server struct
- Style: "Starts the HTTP server on the configured address." etc.

**framework/src/http/request.rs (4 warnings):**
- Add `/// ...` to associated functions and struct fields

**framework/src/http/response.rs (3 warnings):**
- Add `/// ...` to response helper functions

**framework/src/config/mod.rs (3 warnings):**
- Add `/// ...` to submodule `pub mod` declarations

**framework/src/config/env.rs (2 warnings):**
- Add `/// ...` to `Environment` enum variants that lack docs

**framework/src/routing/router.rs (1 warning):**
- Add `/// ...` to the undocumented item

**framework/src/middleware/metrics.rs (1 warning):**
- Add `/// ...` to the undocumented item

**framework/src/http/mod.rs (1 warning):**
- Add `/// ...` to the undocumented item

**framework/src/database/config.rs (1 warning):**
- Add `/// ...` to the undocumented enum variant

Same doc comment style as Task 1. After fixing all files, the warning count must be exactly 0.

Final validation: run `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` to ensure nothing is broken.
  </action>
  <verify>
    <automated>cargo rustc --package ferro-rs --lib -- -W missing-docs 2>&1 | grep "^warning:" | wc -l</automated>
  </verify>
  <done>Warning count is exactly 0. All 136 missing_docs warnings are resolved. Framework crate compiles cleanly with the lint attribute. fmt/clippy/test pass.</done>
</task>

</tasks>

<verification>
- `cargo rustc --package ferro-rs --lib -- -W missing-docs 2>&1 | grep "^warning:" | wc -l` outputs `0`
- `cargo fmt --all -- --check` passes
- `cargo clippy --all --all-targets -- -D warnings` passes
- `cargo test --all-features` passes
- `grep '#!\[warn(missing_docs)\]' framework/src/lib.rs` finds the attribute
</verification>

<success_criteria>
- framework crate compiles with `#![warn(missing_docs)]` producing zero warnings
- All 136 previously undocumented public items have `///` doc comments
- No regressions in fmt, clippy, or tests
</success_criteria>

<output>
After completion, create `.planning/phases/114-metadata-publication-readiness/114-02-SUMMARY.md`
</output>
