---
phase: 131-scaffolder-multibin-copydirs-runtime-apt
plan: 02
type: execute
wave: 2
depends_on: ["131-01"]
files_modified:
  - ferro-cli/src/deploy/app_yaml_existing.rs
  - ferro-cli/src/deploy/mod.rs
  - ferro-cli/src/templates/do.rs
  - ferro-cli/src/templates/files/do/app.yaml.tpl
  - ferro-cli/src/commands/do_init.rs
  - ferro-cli/src/doctor/checks/docker_template_drift.rs
  - ferro-cli/src/doctor/checks/mod.rs
  - ferro-cli/src/doctor/registry.rs
  - ferro-cli/tests/gestiscilo_fixture.rs
autonomous: true
requirements:
  - REQ-131-04
  - REQ-131-06
  - REQ-131-07
  - REQ-131-10
  - REQ-131-11
must_haves:
  truths:
    - "Running `do:init --force` against an existing `.do/app.yaml` preserves top-level `name:`, top-level `region:`, and the web service's `github: { repo, branch }` binding"
    - "A new doctor check `docker_template_drift` re-renders the Dockerfile in-memory and warns (not errors) when the committed file differs from scaffolder output"
    - "The drift check is registered in `default_checks()` and returns `CheckCategory::Deploy`"
    - "Byte-identical regeneration tests from 131-01 (`dockerfile_matches_gestiscilo_6f6d397`, `app_yaml_matches_gestiscilo_6f6d397`) pass WITHOUT `#[ignore]`"
    - "A test for identity preservation proves that a synthetic existing `.do/app.yaml` with non-default name/region/repo/branch survives re-render"
    - "A test for the drift check proves: matching file → Ok, drifted file → Warn with actionable details"
  artifacts:
    - path: "ferro-cli/src/deploy/app_yaml_existing.rs"
      provides: "parse_existing → PreservedAppYamlIdentity line-scanner for name/region/github.repo/github.branch"
      min_lines: 60
    - path: "ferro-cli/src/doctor/checks/docker_template_drift.rs"
      provides: "DockerTemplateDriftCheck (severity Warn, category Deploy)"
      min_lines: 70
  key_links:
    - from: "ferro-cli/src/commands/do_init.rs"
      to: "ferro-cli/src/deploy/app_yaml_existing.rs"
      via: "parse_existing() called before building AppYamlContext"
      pattern: "app_yaml_existing::parse_existing"
    - from: "ferro-cli/src/templates/do.rs"
      to: "AppYamlContext"
      via: "new optional fields for preserved identity substituted into template tokens"
      pattern: "preserved_name|preserved_region|preserved_repo|preserved_branch"
    - from: "ferro-cli/src/doctor/registry.rs"
      to: "DockerTemplateDriftCheck"
      via: "default_checks() push"
      pattern: "DockerTemplateDriftCheck"
---

<objective>
Close the real Wave 0 gap list: preserve `.do/app.yaml` identity fields across `--force` re-renders and add the `docker_template_drift` doctor check. Unignore the byte-identical gestiscilo tests from 131-01 — they must pass at the end of this plan.

Purpose: Downstream projects (gestiscilo, future field apps) must be able to re-run the scaffolder without losing their DO App Platform binding, and must be able to see drift between committed Dockerfiles and the current scaffolder output without silent staleness.
Output: New `deploy::app_yaml_existing` module, extended `AppYamlContext`, new doctor check, updated `do_init` command wiring, and green byte-identical tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-scaffolder-multibin-copydirs-runtime-apt/131-RESEARCH.md
@.planning/phases/131-scaffolder-multibin-copydirs-runtime-apt/131-01-SUMMARY.md
@ferro-cli/src/templates/do.rs
@ferro-cli/src/templates/files/do/app.yaml.tpl
@ferro-cli/src/commands/do_init.rs
@ferro-cli/src/doctor/check.rs
@ferro-cli/src/doctor/registry.rs
@ferro-cli/src/doctor/checks/copy_dirs_dockerignore_collision.rs
@ferro-cli/src/templates/docker.rs
@ferro-cli/src/project.rs
@ferro-cli/src/deploy/bin_detect.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Identity preservation in .do/app.yaml on --force</name>
  <files>
    ferro-cli/src/deploy/app_yaml_existing.rs,
    ferro-cli/src/deploy/mod.rs,
    ferro-cli/src/templates/do.rs,
    ferro-cli/src/templates/files/do/app.yaml.tpl,
    ferro-cli/src/commands/do_init.rs,
    ferro-cli/tests/gestiscilo_fixture.rs
  </files>
  <behavior>
    - `parse_existing("/path/to/app.yaml")` returns `Some(PreservedAppYamlIdentity { name: Some("gestiscilo-it"), region: Some("ams3"), repo: Some("alberto/gestiscilo"), branch: Some("production") })` when the file contains those values at the documented positions; returns all `None` for a file that looks like a fresh scaffolder output.
    - `parse_existing` returns `None` for a missing file.
    - Top-level `name:` and `region:` are ONLY recognized at column 0 (no leading whitespace). Indented `name:` / `region:` inside services are ignored.
    - `github: { repo, branch }` values under the web service are recognized whether emitted block-style (`github:\n  repo: ...`) or inline.
    - `AppYamlContext` gains optional `preserved_name`, `preserved_region`, `preserved_github_repo`, `preserved_github_branch`. When `Some`, `render_app_yaml` substitutes them; when `None`, the existing defaults (derived name, `fra1`, derived git remote, `main`) are used.
    - `do_init::run` calls `parse_existing` before building the context; when `--force` is set AND the existing file is present, preserved values flow through.
    - Regression test `do_init_preserves_identity`: create a tempdir with a seeded `.do/app.yaml` containing non-default name/region/repo/branch, run `do_init` with `--force`, re-read the written file, assert all four fields survived.
    - The 131-01 `app_yaml_matches_gestiscilo_6f6d397` test is un-ignored and passes: construct `AppYamlContext` with gestiscilo's real name/region/repo/branch passed in via the preserved fields, render, assert byte-identical to the fixture (trim_end normalization).
  </behavior>
  <action>
    Create `ferro-cli/src/deploy/app_yaml_existing.rs` following the sketch in `131-RESEARCH.md` (Pattern for `.do/app.yaml` identity preservation). Use a line-scanner — DO NOT add `serde_yaml` to `Cargo.toml`. Rationale per research: four fields, stable template shape, avoiding a new workspace dep for three values.

    Disambiguate top-level vs service-level `name:` / `region:` by checking `line.starts_with("name: ")` (no indent). For `repo:` / `branch:`, accept any indentation since the scaffolder-emitted template only produces them under `github:` in the web service block. If multiple matches exist for `name:` / `region:`, take the first (document this in a doc comment).

    Extend `AppYamlContext` in `ferro-cli/src/templates/do.rs`:
      ```rust
      pub struct AppYamlContext {
          // ...existing fields
          pub preserved_name: Option<String>,
          pub preserved_region: Option<String>,
          pub preserved_github_repo: Option<String>,
          pub preserved_github_branch: Option<String>,
      }
      ```
    In `render_app_yaml`, when building the substituted values, use `preserved_name.unwrap_or(derived_name)` etc.

    Check `ferro-cli/src/templates/files/do/app.yaml.tpl` for the current tokens. If the template hardcodes `region: fra1` or `branch: main`, convert them to tokens (`{{REGION}}`, `{{GITHUB_BRANCH}}`) and substitute in Rust. Do NOT introduce a new conditional template path — one code path, values resolved at the caller.

    In `ferro-cli/src/commands/do_init.rs`:
      - Before constructing `AppYamlContext`, call `crate::deploy::app_yaml_existing::parse_existing(&root.join(".do/app.yaml"))`.
      - Thread the parsed `Option` fields into the context.
      - Preserve existing `--force` semantics: without `--force`, the file-exists path already refuses; with `--force`, the preserved identity fields are honored.

    Register the module: add `pub mod app_yaml_existing;` to `ferro-cli/src/deploy/mod.rs`.

    Write the two tests described in <behavior> into `ferro-cli/tests/gestiscilo_fixture.rs` (or `ferro-cli/src/deploy/app_yaml_existing.rs` for the unit-level parse test — unit where possible, integration for the `do_init` round-trip). Remove `#[ignore]` from `app_yaml_matches_gestiscilo_6f6d397` once it passes.

    Per CLAUDE.md no-backcompat rule: if an existing field name or helper signature is in the way, change it cleanly. No `_v2` variants.
  </action>
  <verify>
    <automated>cargo test -p ferro-cli app_yaml_existing do_init_preserves_identity app_yaml_matches_gestiscilo_6f6d397 --all-features</automated>
  </verify>
  <done>
    `parse_existing` unit tests pass. `do_init_preserves_identity` passes. `app_yaml_matches_gestiscilo_6f6d397` is un-ignored and passes. `cargo clippy --all --all-targets -- -D warnings` is clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: docker_template_drift doctor check</name>
  <files>
    ferro-cli/src/doctor/checks/docker_template_drift.rs,
    ferro-cli/src/doctor/checks/mod.rs,
    ferro-cli/src/doctor/registry.rs,
    ferro-cli/tests/gestiscilo_fixture.rs
  </files>
  <behavior>
    - Check name: `docker_template_drift`. Category: `CheckCategory::Deploy`. Severity on mismatch: `Warn`.
    - `check_impl(root)` returns Ok when `Dockerfile` is absent (message: "skipped (Dockerfile absent)").
    - Reconstructs `DockerContext` via the same readers used by `docker:init` (`read_deploy_metadata`, `read_bins`, `detect_web_bin`, `read_rust_channel`, `copy_dirs_present` filter).
    - Calls `render_dockerfile` (pure), compares `rendered.trim_end() == committed.trim_end()`.
    - On match → `Ok("Dockerfile matches scaffolder output")`.
    - On mismatch → `Warn("Dockerfile has drifted from scaffolder")` with details `"run \`ferro docker:init --dry-run\` to inspect the delta"`.
    - Registered in `default_checks()` (in declared order, after the existing `copy_dirs_dockerignore_collision`).
    - Test `docker_template_drift_ok_on_matching_fixture`: uses the gestiscilo fixture dir + a freshly-rendered Dockerfile written to disk → check passes.
    - Test `docker_template_drift_warn_on_mutation`: seeds a tempdir with gestiscilo fixture, appends a spurious comment line to `Dockerfile`, asserts `Warn` result.
    - Test `docker_template_drift_ok_when_dockerfile_absent`: empty tempdir with only `Cargo.toml` → Ok/skipped.
    - The 131-01 `dockerfile_matches_gestiscilo_6f6d397` test is un-ignored and passes (this task ensures it passes by using the same render path the check uses — if the test fails, fix the render path, not the test).
  </behavior>
  <action>
    Create `ferro-cli/src/doctor/checks/docker_template_drift.rs` following the sketch in `131-RESEARCH.md` (Pattern for drift check). Match the struct/fn shape of `copy_dirs_dockerignore_collision.rs` exactly for consistency.

    Per research Pitfall 4: the drift check must NOT read `git remote get-url origin` dynamically. For the Dockerfile, this is fine — the Dockerfile does not embed a git remote. For a future `app_yaml_template_drift` check, identity-field preservation (Task 1) solves the same problem. This task only ships the Dockerfile drift check; app.yaml drift is deferred unless Wave 0 diff demands it.

    Per research Pitfall 2: normalize trailing newlines with `trim_end()` before comparison.

    Wire-up:
      1. Add `pub mod docker_template_drift;` to `ferro-cli/src/doctor/checks/mod.rs`.
      2. In `ferro-cli/src/doctor/registry.rs::default_checks()`, push `Box::new(DockerTemplateDriftCheck)` after the existing checks.
      3. Verify `ferro doctor` lists it by running `cargo run -p ferro-cli -- doctor --json` in a tempdir and confirming the check appears (can be done as a smoke test, not committed).

    Write the three tests into `ferro-cli/tests/gestiscilo_fixture.rs` (or inline at the bottom of `docker_template_drift.rs` as unit tests — prefer unit tests for speed). Unignore `dockerfile_matches_gestiscilo_6f6d397` once the render path matches the fixture.

    If the byte-identical Dockerfile test still fails after wiring, the gap is inside `render_dockerfile` itself — investigate and fix in this task (it is the direct responsibility of this plan to deliver byte-identical Dockerfile regeneration). Likely culprits per research: trailing newline, default `copy_dirs` list (`["themes", "lang", "public", "migrations"]`) emitting `COPY` lines for directories gestiscilo does not ship, `runtime_apt` ordering. Fix in place; no new tokens unless strictly required.
  </action>
  <verify>
    <automated>cargo test -p ferro-cli docker_template_drift dockerfile_matches_gestiscilo_6f6d397 --all-features</automated>
  </verify>
  <done>
    All three `docker_template_drift_*` tests pass. `dockerfile_matches_gestiscilo_6f6d397` is un-ignored and passes. Check is discoverable via `ferro doctor`. Clippy is clean.
  </done>
</task>

</tasks>

<verification>
  - `cargo fmt --all -- --check`
  - `cargo clippy --all --all-targets -- -D warnings`
  - `cargo test --all-features` — ALL tests green, including the previously-ignored byte-identical tests.
  - Smoke: `cargo run -p ferro-cli -- doctor` in the gestiscilo fixture dir prints `docker_template_drift` as `ok`.
</verification>

<success_criteria>
  - REQ-131-06, REQ-131-07, REQ-131-10, REQ-131-11 satisfied per 131-RESEARCH requirements table.
  - Byte-identical regeneration of gestiscilo `6f6d397` achieved: tests pass without `#[ignore]`.
  - No new workspace dependencies added (no `serde_yaml`, no `similar`).
  - `docker_template_drift` emits `Warn` (not `Error`) — hand-editing remains legitimate.
</success_criteria>

<output>
After completion, create `.planning/phases/131-scaffolder-multibin-copydirs-runtime-apt/131-02-SUMMARY.md` covering:
  - Exact set of files changed in `render_dockerfile` / `render_app_yaml` (if any) to achieve byte-identity.
  - The parse_existing line-scanner rules (documented for future maintainers).
  - Why `Warn` over `Error` for the drift check (cite research open question 4).
  - Confirmation the byte-identical tests are now green without `#[ignore]`.
</output>
