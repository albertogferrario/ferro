---
phase: 131-scaffolder-multibin-copydirs-runtime-apt
plan: 03
type: execute
wave: 3
depends_on: ["131-02"]
files_modified:
  - ferro-cli/src/templates/docker.rs
  - ferro-cli/src/project.rs
  - ferro-cli/src/commands/docker_init.rs
  - ferro-cli/src/doctor/checks/docker_template_drift.rs
autonomous: true
requirements:
  - REQ-131-01
  - REQ-131-02
must_haves:
  truths:
    - "A single `read_bins` function exists in the crate; the duplicate is deleted"
    - "Every caller that previously used `templates::docker::read_bins` now uses `project::read_bins` (or vice versa — whichever is selected as canonical)"
    - "Dockerfile and .do/app.yaml continue to agree on the bin set (tested by the 131-01 regression tests)"
    - "No stale `Vec<String>` → `Vec<BinEntry>` adapter code remains"
  artifacts:
    - path: "ferro-cli/src/project.rs"
      provides: "Canonical read_bins returning Vec<BinEntry> (or the selected shape)"
    - path: "ferro-cli/src/templates/docker.rs"
      provides: "No longer declares read_bins; imports from project.rs"
  key_links:
    - from: "ferro-cli/src/templates/docker.rs"
      to: "ferro-cli/src/project.rs"
      via: "use crate::project::read_bins"
      pattern: "use crate::project::read_bins"
    - from: "ferro-cli/src/commands/docker_init.rs"
      to: "ferro-cli/src/project.rs"
      via: "read_bins call site"
      pattern: "project::read_bins"
---

<objective>
Collapse the latent `read_bins` duplication between `ferro-cli/src/project.rs` and `ferro-cli/src/templates/docker.rs` into a single canonical reader. Per `131-RESEARCH.md` "Don't Hand-Roll" table: two readers with different return shapes coexist today; this is the continuous-coherence refactor CLAUDE.md asks for.

Purpose: Guarantee that Dockerfile and `.do/app.yaml` cannot disagree about which bins exist. Prevent a class of bugs where adding a bin is picked up by one renderer but not the other.
Output: One `read_bins`, every call site updated, all tests still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-scaffolder-multibin-copydirs-runtime-apt/131-RESEARCH.md
@.planning/phases/131-scaffolder-multibin-copydirs-runtime-apt/131-02-SUMMARY.md
@ferro-cli/src/project.rs
@ferro-cli/src/templates/docker.rs
@ferro-cli/src/commands/docker_init.rs
@ferro-cli/src/commands/do_init.rs
@ferro-cli/src/deploy/bin_detect.rs
@ferro-cli/src/doctor/checks/docker_template_drift.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pick canonical read_bins and delete the duplicate</name>
  <files>
    ferro-cli/src/project.rs,
    ferro-cli/src/templates/docker.rs,
    ferro-cli/src/commands/docker_init.rs,
    ferro-cli/src/doctor/checks/docker_template_drift.rs
  </files>
  <action>
    Read both implementations and grep every call site:
      - `ferro-cli/src/project.rs::read_bins` returns `Vec<BinEntry>`.
      - `ferro-cli/src/templates/docker.rs::read_bins` returns `Vec<String>`.

    Selection rule: **`project::read_bins` wins** (richer type, already referenced by `deploy::bin_detect::detect_web_bin`, lives in the "pure reader" module rather than the "template" module). `templates::docker::read_bins` is deleted.

    Migration steps:
      1. In `templates/docker.rs`, delete `pub fn read_bins(...)`. Delete any private helpers only used by it.
      2. If `DockerContext.bins` is currently `Vec<String>`, change it to `Vec<BinEntry>` — OR keep `Vec<String>` at the context boundary and convert at the call site (`bins: bins.into_iter().map(|b| b.name).collect()`). Prefer the conversion approach: `render_dockerfile` only needs the names, so keeping the context narrow matches "pure render" separation. Document the choice in a doc comment on `DockerContext`.
      3. Update `commands/docker_init.rs` to call `project::read_bins` instead of `templates::docker::read_bins`, then convert to the shape `DockerContext` expects.
      4. Update `doctor/checks/docker_template_drift.rs` (from plan 131-02) to call `project::read_bins` as well. Both `docker:init` and the drift check MUST go through the same function — that is the whole point of this plan.
      5. Grep the full crate for any remaining `templates::docker::read_bins` references: `rg "templates::docker::read_bins|templates::docker::\\{.*read_bins" ferro-cli/src`. Zero results required.
      6. Verify no internal visibility changes are needed beyond deletions.

    Per CLAUDE.md: no deprecation shim, no re-export for backwards compat. Delete and update.

    Run the 131-01 regression suite as the acceptance gate — if any of those tests fail after the refactor, the consolidation broke the bin set contract and must be fixed before proceeding.
  </action>
  <verify>
    <automated>cargo test -p ferro-cli --all-features && cargo clippy --all --all-targets -- -D warnings</automated>
  </verify>
  <done>
    Exactly one `read_bins` exists in the crate. `templates::docker::read_bins` is gone. All 131-01 and 131-02 tests still pass. Clippy clean. The gestiscilo byte-identical tests still pass.
  </done>
</task>

</tasks>

<verification>
  - `cargo fmt --all -- --check`
  - `cargo clippy --all --all-targets -- -D warnings`
  - `cargo test --all-features`
  - Manual grep: `rg "fn read_bins" ferro-cli/src` returns exactly one match.
</verification>

<success_criteria>
  - Single canonical `read_bins` in `ferro-cli/src/project.rs`.
  - `docker:init`, `do:init`, and `docker_template_drift` all call it.
  - Phase gate from 131-01/131-02 (gestiscilo byte-identical) still holds.
</success_criteria>

<output>
After completion, create `.planning/phases/131-scaffolder-multibin-copydirs-runtime-apt/131-03-SUMMARY.md` recording:
  - Which function was kept and why.
  - The exact set of call sites updated.
  - Confirmation the gestiscilo regression suite is still green end-to-end across all three plans.
</output>
