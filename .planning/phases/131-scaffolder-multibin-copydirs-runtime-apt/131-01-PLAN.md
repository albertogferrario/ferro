---
phase: 131-scaffolder-multibin-copydirs-runtime-apt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-cli/tests/fixtures/gestiscilo/Dockerfile
  - ferro-cli/tests/fixtures/gestiscilo/app.yaml
  - ferro-cli/tests/fixtures/gestiscilo/Cargo.toml
  - ferro-cli/tests/fixtures/gestiscilo/README.md
  - ferro-cli/tests/gestiscilo_fixture.rs
autonomous: true
requirements:
  - REQ-131-01
  - REQ-131-02
  - REQ-131-03
  - REQ-131-05
  - REQ-131-08
  - REQ-131-09
  - REQ-131-11
must_haves:
  truths:
    - "Gestiscilo Dockerfile (commit 6f6d397) committed verbatim as fixture"
    - "Gestiscilo .do/app.yaml (commit 6f6d397) committed verbatim as fixture"
    - "A fixture Cargo.toml reproducing gestiscilo's [[bin]] + [package.metadata.ferro.deploy] shape is committed"
    - "A test runs render_dockerfile/render_app_yaml against the reconstructed context and diffs against the committed files"
    - "The actual delta set (byte-level) is captured in test assertions — even if the test currently fails, the failure output enumerates the real gaps"
    - "A regression test asserts .do/app.yaml does NOT emit `health_check:`"
    - "A regression test asserts no frontend builder stage when fixture has no frontend/package.json"
  artifacts:
    - path: "ferro-cli/tests/fixtures/gestiscilo/Dockerfile"
      provides: "Frozen reference Dockerfile from gestiscilo 6f6d397"
    - path: "ferro-cli/tests/fixtures/gestiscilo/app.yaml"
      provides: "Frozen reference .do/app.yaml from gestiscilo 6f6d397"
    - path: "ferro-cli/tests/fixtures/gestiscilo/Cargo.toml"
      provides: "Minimal Cargo.toml reproducing bins + deploy metadata"
    - path: "ferro-cli/tests/gestiscilo_fixture.rs"
      provides: "Byte-identical regeneration tests + health_check/frontend regressions"
  key_links:
    - from: "ferro-cli/tests/gestiscilo_fixture.rs"
      to: "ferro_cli::templates::docker::render_dockerfile"
      via: "direct call with reconstructed DockerContext"
      pattern: "render_dockerfile\\("
    - from: "ferro-cli/tests/gestiscilo_fixture.rs"
      to: "ferro_cli::templates::do_::render_app_yaml"
      via: "direct call with reconstructed AppYamlContext"
      pattern: "render_app_yaml\\("
---

<objective>
Lock gestiscilo's hand-maintained Dockerfile and .do/app.yaml (commit 6f6d397) into the ferro-cli test tree as frozen fixtures, and write the byte-identical regeneration tests that define success for Phase 131. This is the Wave 0 verification gate mandated by research — the real delta list between scaffolder output and gestiscilo's hand-maintained files becomes visible only after these tests exist and run.

Purpose: Establish an objective measuring stick. Every downstream plan is justified only by test failures this plan surfaces.
Output: Committed fixtures, a new integration test file, and a pass/fail readout that enumerates the actual gaps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-scaffolder-multibin-copydirs-runtime-apt/131-RESEARCH.md
@ferro-cli/src/templates/docker.rs
@ferro-cli/src/templates/do.rs
@ferro-cli/src/project.rs
@ferro-cli/src/deploy/bin_detect.rs

<interfaces>
From ferro-cli/src/templates/docker.rs (public surface needed by this plan):
```rust
pub struct DockerContext {
    pub rust_channel: String,
    pub has_frontend: bool,
    pub bins: Vec<String>,      // or Vec<BinEntry> — confirm at read time
    pub web_bin: String,
    pub copy_dirs_present: Vec<String>,
    pub runtime_apt: Vec<String>,
}
pub fn render_dockerfile(ctx: &DockerContext) -> String;
pub fn read_bins(root: &Path) -> anyhow::Result<Vec<String>>;
pub fn read_rust_channel(root: &Path) -> String;
```

From ferro-cli/src/templates/do.rs:
```rust
pub struct AppYamlContext { /* name, region, bins, web_bin, envs_block, github_repo, ... */ }
pub fn render_app_yaml(ctx: &AppYamlContext) -> String;
```

From ferro-cli/src/project.rs:
```rust
pub fn read_deploy_metadata(root: &Path) -> anyhow::Result<FerroDeployMetadata>;
pub struct FerroDeployMetadata { pub copy_dirs: Vec<String>, pub runtime_apt: Vec<String>, pub web_bin: Option<String> }
```

Executor MUST read the exact current signatures from the source files before writing the test — these are the best-effort contracts from research. If a field differs, use the source of truth.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit gestiscilo reference fixtures verbatim</name>
  <files>
    ferro-cli/tests/fixtures/gestiscilo/Dockerfile,
    ferro-cli/tests/fixtures/gestiscilo/app.yaml,
    ferro-cli/tests/fixtures/gestiscilo/Cargo.toml,
    ferro-cli/tests/fixtures/gestiscilo/README.md
  </files>
  <action>
    Obtain the two files from gestiscilo-it commit `6f6d397`:
      - `Dockerfile` → commit verbatim (including final newline, exact whitespace) to `ferro-cli/tests/fixtures/gestiscilo/Dockerfile`.
      - `.do/app.yaml` → commit verbatim to `ferro-cli/tests/fixtures/gestiscilo/app.yaml` (note: renamed from `.do/app.yaml` to `app.yaml` because test fixtures are not a real deploy tree).

    Source options, in order of preference:
      1. If a gestiscilo checkout is reachable on the host, `git -C <path> show 6f6d397:Dockerfile` and `git -C <path> show 6f6d397:.do/app.yaml`.
      2. If not, ask the user for the two files and paste them verbatim.
      DO NOT reconstruct or synthesize these files. They are the ground truth — any typo invalidates the phase.

    Create `ferro-cli/tests/fixtures/gestiscilo/Cargo.toml` reconstructing the minimal shape that drives the scaffolder:
      - `[package]` with `name = "gestiscilo"`, a plausible version, edition 2021.
      - Two `[[bin]]` entries in the order gestiscilo declares them (web first, then the worker). Infer bin names from the committed Dockerfile `COPY target/release/<name>` lines — this is deterministic.
      - `[package.metadata.ferro.deploy]` with `copy_dirs`, `runtime_apt`, and `web_bin` matching what the committed Dockerfile evidences (e.g. `COPY themes themes` → copy_dirs includes `themes`; `apt-get install -y <pkg>` lines → runtime_apt list; first `[[bin]]` → web_bin unless the committed `.do/app.yaml` web service points elsewhere).
      - No `[dependencies]` needed — the scaffolder does not read them.

    Create `ferro-cli/tests/fixtures/gestiscilo/README.md` (one paragraph) explaining: this directory freezes gestiscilo `6f6d397` as the phase-131 reference; do not edit these files to make tests pass — fix the scaffolder instead.

    Do NOT create a `frontend/` subdirectory. Gestiscilo is server-rendered; its absence is part of the contract.
  </action>
  <verify>
    <automated>test -f ferro-cli/tests/fixtures/gestiscilo/Dockerfile && test -f ferro-cli/tests/fixtures/gestiscilo/app.yaml && test -f ferro-cli/tests/fixtures/gestiscilo/Cargo.toml && test -f ferro-cli/tests/fixtures/gestiscilo/README.md</automated>
  </verify>
  <done>
    All four fixture files exist; Dockerfile and app.yaml are byte-identical to gestiscilo 6f6d397; Cargo.toml accurately reflects the bins and deploy metadata those files imply.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write byte-identical regeneration + regression tests</name>
  <files>ferro-cli/tests/gestiscilo_fixture.rs</files>
  <behavior>
    - Test `dockerfile_matches_gestiscilo_6f6d397`: constructs a `DockerContext` from the fixture Cargo.toml + on-disk layout (using the crate's real `read_bins` / `read_deploy_metadata` / `detect_web_bin` / `read_rust_channel` helpers), calls `render_dockerfile`, asserts output (trailing-newline-normalized) equals the committed fixture Dockerfile. EXPECTED TO FAIL at this point — the failure diff IS the Wave 0 gap report.
    - Test `app_yaml_matches_gestiscilo_6f6d397`: constructs an `AppYamlContext` the same way, calls `render_app_yaml`, asserts equality against fixture `app.yaml`. EXPECTED TO FAIL.
    - Test `app_yaml_never_emits_health_check`: renders the gestiscilo context and asserts `!rendered.contains("health_check:")`. This protects against a reintroduction of the stale pattern — must PASS today per research (template does not emit it).
    - Test `dockerfile_has_no_frontend_builder_when_frontend_absent`: renders the gestiscilo context (fixture has no `frontend/package.json`) and asserts the rendered Dockerfile does not contain `node:` or a `FROM node` stage. Must PASS today per research.
    - Test `dockerfile_covers_every_bin`: for each bin in the fixture Cargo.toml, asserts the rendered Dockerfile contains a `COPY target/release/<bin>` (or equivalent) line. Must PASS today per research.
    - Test `app_yaml_workers_from_non_web_bins`: asserts each non-web bin appears under a `workers:` entry in rendered output. Must PASS today.
    - Test `dockerfile_copy_dirs_emitted`: for each `copy_dirs` entry in the fixture Cargo.toml, asserts `COPY {dir} {dir}` is in the rendered Dockerfile.
    - Test `dockerfile_runtime_apt_layer`: if fixture `runtime_apt` is non-empty, asserts `# ferro:runtime-apt` marker and each package name appears.
  </behavior>
  <action>
    Create `ferro-cli/tests/gestiscilo_fixture.rs` as an integration test (binary integration tests live under `tests/`, not `src/`). Use `std::path::PathBuf` pointing at `env!("CARGO_MANIFEST_DIR").join("tests/fixtures/gestiscilo")`.

    Because integration tests import the crate by name, verify `ferro-cli` is consumable as a library (`src/lib.rs`) or which items are `pub`. If `render_dockerfile` / `render_app_yaml` are not exposed through `lib.rs`, add them to the library's public surface BEFORE writing the tests. Prefer adding the minimum re-exports (`pub use templates::docker::{DockerContext, render_dockerfile, read_bins, read_rust_channel};` etc.) in `ferro-cli/src/lib.rs` — do NOT widen visibility of internals you don't need.

    For tests that are known to fail pending downstream plans (`dockerfile_matches_gestiscilo_6f6d397`, `app_yaml_matches_gestiscilo_6f6d397`), mark them `#[ignore = "Wave 0 diff — unignored once 131-02 lands identity preservation and any Dockerfile deltas are resolved"]` so the full suite still goes green. The point of this plan is to MAKE the deltas visible via `cargo test -- --ignored`, not to block the suite.

    Normalize trailing whitespace/newline before comparing (per research Pitfall 2): compare `rendered.trim_end_matches('\n')` against `fixture.trim_end_matches('\n')`.

    On assertion failure, produce a clear diff-like message: print line-by-line differences using a small inline helper (no `similar` crate dependency needed — simple `zip` over lines with markers is sufficient).

    After writing the test, run it with `cargo test -p ferro-cli --test gestiscilo_fixture -- --ignored --nocapture` and CAPTURE THE DIFF OUTPUT in the task SUMMARY. That diff output is the canonical input for plan 131-02.
  </action>
  <verify>
    <automated>cargo test -p ferro-cli --test gestiscilo_fixture</automated>
  </verify>
  <done>
    Non-ignored tests pass. The two `#[ignore]`'d byte-identical tests exist and, when run with `--ignored`, produce a readable diff. The SUMMARY for this plan records the exact deltas surfaced by those diffs so plan 131-02 can act on them.
  </done>
</task>

</tasks>

<verification>
  - `cargo fmt --all -- --check`
  - `cargo clippy --all --all-targets -- -D warnings`
  - `cargo test --all-features` (all non-ignored tests pass)
  - `cargo test -p ferro-cli --test gestiscilo_fixture -- --ignored --nocapture` (produces gap report; non-zero exit is expected and OK — this is the input for 131-02)
</verification>

<success_criteria>
  - Fixtures committed verbatim from gestiscilo 6f6d397.
  - Byte-identical tests exist (even if ignored pending 131-02 fixes).
  - Regression tests (no health_check, no frontend builder, bins covered, copy_dirs emitted, runtime_apt layer) all pass TODAY, locking in the claims research marked as "already implemented".
  - Wave 0 gap readout is recorded in the plan SUMMARY verbatim.
</success_criteria>

<output>
After completion, create `.planning/phases/131-scaffolder-multibin-copydirs-runtime-apt/131-01-SUMMARY.md` including:
  - Exact source of the fixture files (git sha + path) and verification that they are byte-identical to upstream.
  - The raw `--ignored` diff output from the byte-identical tests (this becomes the authoritative gap list).
  - An explicit statement of which regression assertions PASSED today (validating the research's "already implemented" claims).
  - A one-line recommendation for plan 131-02 scope based on the diff.
</output>
