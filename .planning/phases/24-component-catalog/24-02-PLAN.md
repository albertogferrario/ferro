---
phase: 24-component-catalog
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - ferro-json-ui/src/component.rs
  - ferro-json-ui/src/lib.rs
autonomous: true
---

<objective>
Add form field components (Checkbox, Switch) and utility components (Separator, DescriptionList) to the component catalog.

Purpose: Form fields beyond Input/Select are essential for CRUD pages (boolean toggles, checkboxes). Separator and DescriptionList cover detail/show page patterns. These 4 components fill the most critical gaps in the catalog.
Output: 4 new components with props structs, added to the Component enum, with tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-component-catalog/24-RESEARCH.md
@.planning/phases/24-component-catalog/24-01-SUMMARY.md
@ferro-json-ui/src/component.rs
@ferro-json-ui/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Checkbox, Switch, Separator, and DescriptionList components</name>
  <files>ferro-json-ui/src/component.rs, ferro-json-ui/src/lib.rs</files>
  <action>
  **Step 1: Add CheckboxProps struct:**
  ```
  field: String
  label: String
  description: Option<String>  (skip_serializing_if None)
  checked: Option<bool>        (skip_serializing_if None)
  required: Option<bool>       (skip_serializing_if None)
  disabled: Option<bool>       (skip_serializing_if None)
  error: Option<String>        (skip_serializing_if None)
  ```
  Derives: `Debug, Clone, PartialEq, Serialize, Deserialize`

  **Step 2: Add SwitchProps struct:**
  Same fields as CheckboxProps. Switch is visually different but structurally identical for the schema.
  ```
  field: String
  label: String
  description: Option<String>  (skip_serializing_if None)
  checked: Option<bool>        (skip_serializing_if None)
  required: Option<bool>       (skip_serializing_if None)
  disabled: Option<bool>       (skip_serializing_if None)
  error: Option<String>        (skip_serializing_if None)
  ```

  **Step 3: Add SeparatorProps struct:**
  ```
  orientation: Option<Orientation>  (skip_serializing_if None, defaults to horizontal)
  ```
  Add `Orientation` enum: `Horizontal` (default), `Vertical`. Derives: `Debug, Clone, Default, PartialEq, Eq, Serialize, Deserialize` with `rename_all = "snake_case"`.

  **Step 4: Add DescriptionListProps and DescriptionItem structs:**
  ```
  DescriptionListProps:
    items: Vec<DescriptionItem>
    columns: Option<u8>  (1 or 2 column layout, skip_serializing_if None)

  DescriptionItem:
    label: String
    value: String
    format: Option<ColumnFormat>  (reuse existing ColumnFormat from Table, skip_serializing_if None)
  ```

  **Step 5: Add variants to Component enum:**
  Add `Checkbox(CheckboxProps)`, `Switch(SwitchProps)`, `Separator(SeparatorProps)`, `DescriptionList(DescriptionListProps)` to the Component enum.

  **Step 6: Update lib.rs re-exports:**
  Add to `pub use component::{ ... }`: `CheckboxProps`, `SwitchProps`, `SeparatorProps`, `DescriptionListProps`, `DescriptionItem`, `Orientation`.

  **Step 7: Update `all_ten_component_variants_serialize` test:**
  Rename to `all_component_variants_serialize`. Add the 4 new components to the list and update expected count from 10 to 14. Each new component must serialize with correct `type` field and round-trip successfully.

  **Step 8: Add dedicated tests for each new component:**
  - `checkbox_round_trips` — full props construction and round-trip
  - `switch_round_trips` — full props construction and round-trip
  - `separator_defaults_to_horizontal` — deserialize minimal JSON, verify default orientation
  - `description_list_with_format` — construct with ColumnFormat::Date on an item, round-trip
  - `checkbox_with_error` — verify error field serializes correctly
  </action>
  <verify>`cargo test -p ferro-json-ui` passes. `cargo check --workspace` passes.</verify>
  <done>4 new components added to Component enum. All round-trip tests pass. Lib.rs re-exports updated.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p ferro-json-ui` passes
- [ ] `cargo check --workspace` passes
- [ ] `cargo clippy -p ferro-json-ui` clean
- [ ] Component enum has 14 variants (10 original + 4 new)
- [ ] All new components serialize with correct `type` field
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Checkbox, Switch, Separator, DescriptionList added with full props
</success_criteria>

<output>
After completion, create `.planning/phases/24-component-catalog/24-02-SUMMARY.md`
</output>
