---
phase: 110-mcp-tool-accuracy
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-mcp/src/service.rs
autonomous: true
requirements: [CLIMCP-02]

must_haves:
  truths:
    - "Every 'Combine with' reference in tool descriptions points to a tool_name that exists in service.rs"
    - "Every 'Returns' section accurately describes what the tool function actually returns"
    - "No tool description references APIs, types, or patterns that have been removed or renamed"
    - "Newer tools (projection, AI, stripe, whatsapp) have complete 'Combine with' cross-references where relevant"
  artifacts:
    - path: "ferro-mcp/src/service.rs"
      provides: "65 MCP tool definitions with accurate description strings"
      contains: "#[tool("
  key_links:
    - from: "ferro-mcp/src/service.rs tool descriptions"
      to: "framework/src/lib.rs exports"
      via: "API references in description strings match actual exports"
      pattern: "description = "
---

<objective>
Audit all 65 MCP tool description strings in service.rs to ensure accuracy of "When to use", "Returns", "Combine with", and any API references.

Purpose: These description strings are the "generation hints" agents read before choosing which tool to call and how to use the results. Stale cross-references cause agents to call non-existent tools; inaccurate "Returns" descriptions cause agents to parse results incorrectly.

Output: service.rs with all 65 tool descriptions verified accurate or corrected.
</objective>

<execution_context>
@/Users/alberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-mcp-tool-accuracy/110-RESEARCH.md
</context>

<interfaces>
<!-- All 65 tool names that exist in service.rs (valid "Combine with" targets) -->
<!-- Extracted by: grep -oP 'name = "\K[^"]+' ferro-mcp/src/service.rs -->

Valid tool names (any "Combine with" backtick reference MUST be one of these):
```
application_info, db_query, db_schema, list_routes, list_commands,
list_migrations, list_events, list_jobs, list_lang_files, list_middleware,
list_services, request_metrics, queue_status, list_models, list_policies,
list_rate_limiters, list_broadcast_channels, get_handler, read_logs,
last_error, get_config, generate_types, list_props, list_resources,
inspect_props, search_docs, tinker, browser_logs, session_inspect,
relation_map, cache_inspect, job_history, get_middleware, test_route,
validate_contracts, create_project, domain_glossary, explain_route,
explain_model, diagnose_error, error_patterns, route_dependencies,
model_usages, dependency_graph, generation_context, code_templates,
json_ui_catalog, json_ui_inspect, json_ui_generate, crud_create,
crud_list, crud_update, crud_delete, list_projections, inspect_projection,
render_projection, validate_projection, projection_coverage,
stripe_config_status, stripe_webhook_events, stripe_subscription_info,
test_classifier, list_pending_confirmations, whatsapp_config_status,
whatsapp_webhook_events
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix "Combine with" cross-references across all 65 tool descriptions</name>
  <files>ferro-mcp/src/service.rs</files>
  <action>
Systematically verify every "Combine with" backtick reference in all 65 tool descriptions points to a tool name that exists in the valid tool names list above.

**Method:**
1. Extract every backtick-quoted tool name from description strings (pattern: `` `tool_name` ``)
2. Cross-reference each against the valid tool names list
3. Fix any that reference tools that do not exist (either correct the name or remove the reference)
4. For newer tools (projection_coverage, stripe_*, whatsapp_*, test_classifier, list_pending_confirmations), check if they should be adding "Combine with" references to related tools they were added alongside

**Known issues from research to verify:**
- Confirm newer tools (added after v9.0) have appropriate cross-references to each other
- Confirm no tool references deprecated/removed tool names

**Also audit:**
- Any API type references in descriptions (e.g., if a description mentions "ServiceDef", confirm that type exists)
- Any framework feature claims that may have changed (e.g., "20 built-in components" in json_ui_catalog — verify the count is still accurate or update)

After all fixes, verify the file compiles: `cargo check -p ferro-mcp`.
  </action>
  <verify>
    <automated>cd /Users/alberto/repositories/albertogferrario/ferro && cargo check -p ferro-mcp && echo "PASS"</automated>
  </verify>
  <done>Every "Combine with" backtick reference in service.rs points to a tool name that exists. No tool description references removed/renamed APIs. Newer tools have appropriate cross-references. cargo check passes.</done>
</task>

</tasks>

<verification>
```bash
# Extract all backtick-quoted tool names from descriptions and verify each exists
# This is a manual spot-check — the automated verify confirms compilation

# Full build passes
cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features
```
</verification>

<success_criteria>
- All 65 tool descriptions have been reviewed for accuracy
- Every "Combine with" cross-reference points to an existing tool
- No description references removed or renamed API types
- Component/tool counts in descriptions match reality
- cargo check, clippy, and tests all pass
</success_criteria>

<output>
After completion, create `.planning/phases/110-mcp-tool-accuracy/110-02-SUMMARY.md`
</output>
