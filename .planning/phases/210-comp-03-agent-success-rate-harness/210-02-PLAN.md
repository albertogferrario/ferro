---
phase: 210-comp-03-agent-success-rate-harness
plan: 02
type: execute
wave: 2
depends_on: [210-01]
files_modified:
  - ferro-mcp/tests/agent_harness.rs
  - ferro-mcp/tests/fixtures/agent_harness/transcripts/_fixture_valid.json
  - ferro-mcp/tests/fixtures/agent_harness/transcripts/_fixture_invalid.json
autonomous: true
requirements: [COMP-03]
must_haves:
  truths:
    - "The scorer returns a 4-field per-tier result (t1,t2,t3,t4), never a single boolean."
    - "A malformed ServiceDef scores t1=fail and does NOT panic/abort the test process."
    - "T4 materializes the ServiceDef into src/projections/<name>.rs in a tempdir before invoking checkpoint_projection."
    - "The replay path runs in default cargo test with no LLM, no network, no API key, and reproduces committed tier rates deterministically."
    - "Each tier is cumulative: tier N passes iff tiers 1..N pass."
  artifacts:
    - path: "ferro-mcp/tests/agent_harness.rs"
      provides: "TierResult struct + score(service, target_intent) -> TierResult + replay/tier-independence/T1-invalid tests"
      contains: "struct TierResult"
    - path: "ferro-mcp/tests/fixtures/agent_harness/transcripts/_fixture_valid.json"
      provides: "deterministic replay fixture: a ServiceDef that passes tiers"
      contains: '"service_def"'
    - path: "ferro-mcp/tests/fixtures/agent_harness/transcripts/_fixture_invalid.json"
      provides: "deterministic replay fixture: a malformed ServiceDef proving T1 fails without panic"
      contains: '"service_def"'
  key_links:
    - from: "ferro-mcp/tests/agent_harness.rs (T1 scorer)"
      to: "ferro_json_ui catalog validate"
      via: "catch_unwind around from_service_def OR global_catalog().validate(&spec)"
      pattern: "catch_unwind|global_catalog\\(\\)\\.validate"
    - from: "ferro-mcp/tests/agent_harness.rs (T4 scorer)"
      to: "ferro_mcp::tools::checkpoint_projection::execute"
      via: "tempfile::tempdir materialization of src/projections/<name>.rs"
      pattern: "tempfile::tempdir"
---

<objective>
Build the substantive deliverable: the deterministic T1–T4 scorer and the replay path that
runs CI-green WITHOUT any LLM. The scorer is a pure-ish function shared by both execution
paths; the replay path proves the scorer + tier logic on every `cargo test`.

Purpose: per scope guidance, the scorer + replay path is the substance of this phase and
MUST be built and CI-green BEFORE the live-agent loop (Wave 3) and BEFORE the gated baseline
run (Wave 4). The two RESEARCH pitfalls (T1 debug-build panic; T4 filesystem coupling) are
addressed here by explicit, acceptance-tested mechanisms.
Output: `TierResult` + `score()` + replay/tier-independence/T1-invalid tests + 2 hand-authored
deterministic fixture transcripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/210-comp-03-agent-success-rate-harness/210-CONTEXT.md
@.planning/phases/210-comp-03-agent-success-rate-harness/210-RESEARCH.md
@.planning/phases/210-comp-03-agent-success-rate-harness/210-PATTERNS.md
@.planning/phases/210-comp-03-agent-success-rate-harness/210-01-SUMMARY.md

<interfaces>
<!-- EXACT signatures the executor MUST match. Read the live source (read_first) — these
     are reproduced for orientation; verify against the file. -->

T1 deserialize + render (ferro-json-ui/src/projection/builder.rs lines 55-122):
  pub fn Spec::from_service_def(service: &ServiceDef, intents: &[IntentScore], ctx: &VisualContext)
      -> Result<Spec, ProjectionError>
  // ctx: VisualContext { intent_index: usize, current_state: Option<String>,
  //                      mode: RenderMode, templates: Option<ThemeTemplates> }
  // RenderMode::Display for Browse/Process/Summarize/Track/Focus/Analyze; RenderMode::Input for Collect.
  // PITFALL 3 (lines 112-122): on invalid spec, debug builds PANIC (#[cfg(debug_assertions)] panic!),
  //   release builds return Err(ProjectionError::CatalogValidation(errors)). cargo test IS a debug build.
  // Explicit error count: ferro_json_ui::global_catalog().validate(&spec) -> Result<(), Vec<CatalogError>>

T2 intent (ferro-projections/src/derive.rs lines 75-113):
  pub fn derive_intents(service: &ServiceDef) -> Vec<IntentScore>  // always non-empty, sorted desc
  // IntentScore { intent: Intent, confidence: f64, matching_signals: Vec<String> }
  // Intent derives PartialEq; intents[0].intent == task.target_intent

T4 checkpoint (ferro-mcp/src/tools/checkpoint_projection.rs lines 152-153, 64-73):
  pub async fn execute(project_root: &Path, name: &str) -> Result<Verdict, String>
  pub struct Verdict { status: SeamStatus, projection: String, seams: Vec<SeamResult>, next_steps: Vec<String> }
  // SeamStatus: Pass | Warn | Fail | NotChecked
  // T4 passes iff verdict.status != SeamStatus::Fail  (RESEARCH A3: "zero BLOCKING findings")
  // FILESYSTEM-COUPLED (Pitfall 4): reads src/projections/<name>.rs + src/models/<name>.rs from disk.

T3 binding bar (RESEARCH §T3 + 213-06 summary). Inspect rendered spec.elements (flat id→Element):
  | Intent          | Element type    | Bound iff (props present + non-empty)                 |
  | Browse / Track  | DataTable       | columns non-empty AND items_path present              |
  | Process         | KanbanBoard     | columns non-empty AND items_path present AND group_by |
  | Collect         | Form            | ≥1 field element                                       |
  | Summarize       | StatCard        | value_path present                                     |
  | Focus / Analyze | DescriptionList/chart | primary field bindings non-empty               |
  // 213 REMOVED `data_path` for kanban — use items_path + group_by. Pin exact serde prop keys by
  // reading ferro-json-ui/src/component.rs *Props structs (DataTableProps/KanbanBoardProps/
  // FormProps/StatCardProps/DescriptionListProps).

TierResult (no analog — new type, MUST be 4 fields, never bool):
  struct TierResult { t1: bool, t2: bool, t3: bool, t4: bool }
  // cumulative: t2 = t1 && <t2 check>; t3 = t2 && <t3 check>; t4 = t3 && <t4 check>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: T1/T2/T3 scorer (deterministic, panic-safe, no LLM)</name>
  <read_first>
    - ferro-json-ui/src/projection/builder.rs (lines 55-124 — from_service_def signature AND the debug-panic block; copy the real signature, do not guess)
    - ferro-json-ui/src/projection/mod.rs (lines 30-65 — VisualContext + RenderMode exact fields/variants)
    - ferro-json-ui/src/catalog.rs (~line 670 — global_catalog().validate signature returning Result<(), Vec<CatalogError>>)
    - ferro-json-ui/src/component.rs (DataTableProps / KanbanBoardProps / FormProps / StatCardProps / DescriptionListProps — pin the exact serde field names for T3 prop keys)
    - ferro-projections/src/derive.rs (lines 75-113 — derive_intents return type; IntentScore fields)
    - ferro-projections/src/service.rs (lines 62-93 — ServiceDef serde shape; it derives Deserialize)
    - .planning/phases/210-comp-03-agent-success-rate-harness/210-RESEARCH.md (§Scoring Pipeline T1/T2/T3 + Pitfall 3)
    - .planning/phases/210-comp-03-agent-success-rate-harness/210-PATTERNS.md (§4 T1/T2/T3 scorer patterns, lines 227-327)
  </read_first>
  <behavior>
    - A valid ServiceDef whose faithful authoring derives the declared intent → t1=true, t2=true.
    - A ServiceDef that deserializes but renders an INVALID spec → t1=false, NO panic (process survives).
    - A ServiceDef rendering a placeholder (empty columns / missing items_path) → t3=false even if t2=true.
    - A ServiceDef whose derived top intent != target_intent → t2=false (and t3,t4 false, cumulative).
    - `intent_hints` containing a Primary override → treated as a T2 DISQUALIFIER (t2=false) so T2 measures structural derivation, not a declared override (RESEARCH Q3/A4).
  </behavior>
  <action>
    In `ferro-mcp/tests/agent_harness.rs` add the scorer types and the T1/T2/T3 logic.

    1. `#[derive(Debug, Clone, Copy, PartialEq, serde::Serialize)] struct TierResult { t1: bool, t2: bool, t3: bool, t4: bool }`
       — 4 fields, never collapsed to bool (RESEARCH anti-pattern). Cumulative semantics.

    2. A `Task`/corpus struct that deserializes corpus.json entries: `id`, `target_intent`
       (deserialize the string into `ferro_projections::Intent` — map the 7 variant names),
       `description`, optional `expected_actions`/`expected_guards`.

    3. T1 (panic-safe — Pitfall 3): deserialize the agent JSON into `ferro_projections::ServiceDef`
       (failure → t1=false). Then `let intents = derive_intents(&service);`. Then render with
       PITFALL-3 protection — choose ONE and implement it so an invalid spec scores t1=false
       WITHOUT aborting the process:
         (a) wrap `Spec::from_service_def(&service, &intents, &ctx)` in `std::panic::catch_unwind`
             (it is sync; use `AssertUnwindSafe` as needed) and treat panic-or-Err as t1=false; OR
         (b) build the spec via the path that returns the spec, then call
             `ferro_json_ui::global_catalog().validate(&spec)` explicitly and treat
             `Err(errors)` (or any panic) as t1=false.
       Set a panic hook suppressor around the catch so the panic message does not pollute output
       (optional). DO NOT call `from_service_def` bare and rely on it to report T1 failure.
       Use `VisualContext { intent_index: 0, current_state: None, mode: <Input for Collect else Display>, templates: None }`.

    4. T2 (cumulative): `t2 = t1 && intents[0].intent == task.target_intent`. ALSO: if the
       ServiceDef contains any `intent_hints` Primary override, force `t2 = false` (RESEARCH Q3:
       intent_hints let the agent auto-pass T2; disqualify so T2 measures structural derivation).
       Document this in a code comment as a stated-before-runs tier rule.

    5. T3 (cumulative): from the rendered `Spec`, locate the primary element for the declared
       intent (per the table in <interfaces>) and assert its binding props are present and
       non-empty. Inspect the raw JSON `props` (robust to prop-struct churn). Use `items_path`
       + `group_by` for KanbanBoard (NOT `data_path` — removed in 213). `t3 = t2 && <bound>`.

    Expose the public-to-the-test entrypoint `fn score(agent_json: &serde_json::Value, target: ferro_projections::Intent) -> TierResult`
    that performs T1→T2→T3 (T4 is added in Task 2). For now `score` sets `t4: false` (or omits
    via a `score_t1_t3` helper) — Task 2 completes it.
  </action>
  <verify>
    <automated>cargo test -p ferro-mcp --test agent_harness 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'struct TierResult' ferro-mcp/tests/agent_harness.rs` ≥1, with fields t1,t2,t3,t4 (grep shows `t1`,`t2`,`t3`,`t4`)
    - `grep -E 'catch_unwind|global_catalog\(\)\.validate' ferro-mcp/tests/agent_harness.rs` matches (Pitfall 3 mitigation present)
    - `grep 'items_path' ferro-mcp/tests/agent_harness.rs` matches AND `grep -c 'data_path' ferro-mcp/tests/agent_harness.rs` returns 0 (213 binding bar honored)
    - `grep 'intent_hints' ferro-mcp/tests/agent_harness.rs` matches (T2 disqualifier rule present)
    - `cargo test -p ferro-mcp --test agent_harness` exits 0 with NO env vars set
    - A unit test in the file feeds a deliberately-invalid ServiceDef and asserts the returned TierResult has `t1==false` AND the test process does not abort (proves Pitfall 3 handled)
  </acceptance_criteria>
  <done>T1/T2/T3 scorer present, panic-safe, 213-binding-aware, intent_hints-disqualifying; replay-light tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: T4 checkpoint via tempdir materialization + complete the scorer</name>
  <read_first>
    - ferro-mcp/src/tools/checkpoint_projection.rs (lines 152-235 — execute signature + the filesystem coupling; AND lines ~829-846 + ~980-1038 — the project_with_projection/add_model tempdir test helpers to copy)
    - ferro-projections/src/service.rs (the ServiceDef builder API — to render ServiceDef → Rust source faithfully)
    - .planning/phases/210-comp-03-agent-success-rate-harness/210-RESEARCH.md (§T4 + Pitfall 4 + Open Question Q2)
    - .planning/phases/210-comp-03-agent-success-rate-harness/210-PATTERNS.md (§4 T4 patterns, lines 329-379)
  </read_first>
  <behavior>
    - A ServiceDef that passes T1–T3 and materializes to a checkpoint-clean source → t4=true.
    - A ServiceDef with a dangling field (no backing column in the stub model) → checkpoint returns Fail → t4=false.
    - T4 never runs unless T3 passed (cumulative): t4 = t3 && (verdict.status != Fail).
    - The materialization happens in a tempfile::tempdir and is cleaned up; no writes outside the tempdir.
  </behavior>
  <action>
    Implement T4 (Pitfall 4 — checkpoint_projection is filesystem-coupled; it does NOT take a
    ServiceDef in memory). In `ferro-mcp/tests/agent_harness.rs`:

    1. Copy the tempdir helpers from checkpoint_projection.rs's own test module
       (`project_with_projection(name, projection_src) -> tempfile::TempDir` and
       `add_model(&tmp, name, model_src)` — PATTERNS §4 lines 333-346). These create
       `src/projections/<name>.rs` and `src/models/<name>.rs` under a `tempfile::tempdir()`.

    2. Write `fn render_service_def_to_rust_source(service: &ServiceDef) -> String` that emits
       a faithful `src/projections/<name>.rs` source the checkpoint's `reconstruct_service_def`
       can parse (builder-call form OR the projection-file shape checkpoint expects — match what
       inspect_projection/reconstruct_service_def read; verify against checkpoint_projection.rs).
       Also emit a minimal matching `src/models/<name>.rs` stub so the field→column seam can run.

    3. T4: materialize → `let verdict = ferro_mcp::tools::checkpoint_projection::execute(tmp.path(), "<name>_service").await?;`
       → `t4 = t3 && (verdict.status != ferro_mcp::tools::checkpoint_projection::SeamStatus::Fail)`.
       State this "zero blocking findings = status != Fail" rule in a stated-before-runs comment (RESEARCH A3).
       Because execute is async, the function that runs T4 must be async (the replay test is
       `#[tokio::test]`). Keep T1–T3 callable sync; T4 wraps them.

    4. Complete `async fn score(agent_json, target) -> TierResult` to run T1→T2→T3→T4 cumulatively.

    Confine ALL T4 filesystem work to the tempdir; assert (in a test) no path outside the
    tempdir is written.
  </action>
  <verify>
    <automated>cargo test -p ferro-mcp --test agent_harness 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'tempfile::tempdir' ferro-mcp/tests/agent_harness.rs` matches (Pitfall 4 mitigation)
    - `grep 'checkpoint_projection::execute' ferro-mcp/tests/agent_harness.rs` matches
    - `grep -E 'status != .*Fail|SeamStatus::Fail' ferro-mcp/tests/agent_harness.rs` matches (T4 = not-Fail rule)
    - `grep 'render_service_def_to_rust_source\|fn .*rust_source' ferro-mcp/tests/agent_harness.rs` matches
    - `cargo test -p ferro-mcp --test agent_harness` exits 0 with NO env vars set
    - A test materializes a passing ServiceDef and asserts t4 reachable=true; another with a dangling field asserts t4=false (checkpoint Fail), and the process does not abort
  </acceptance_criteria>
  <done>T4 runs the real checkpoint_projection over a tempdir-materialized projection; score() is the full cumulative T1–T4 function.</done>
</task>

<task type="auto">
  <name>Task 3: Replay path + tier-independence test + deterministic fixture transcripts</name>
  <read_first>
    - ferro-mcp/tests/agent_harness.rs (the scorer from Tasks 1-2)
    - .planning/phases/210-comp-03-agent-success-rate-harness/210-RESEARCH.md (§Transcript & Baseline Format + §Gate Pattern + §Validation Architecture "Observable signals")
    - .planning/phases/210-comp-03-agent-success-rate-harness/210-PATTERNS.md (§2 gate idiom + §"Transcript and Baseline Format")
    - framework/tests/async_validation_pg_gate.rs (the #[ignore] + env-guard precedent — read the gate shape)
  </read_first>
  <action>
    Build the replay path — the CI-valuable half (runs in default `cargo test`, no LLM).

    1. Define `Transcript` / `TrialRecord` deserialize structs matching the RESEARCH shape:
       transcript = { task_id, target_intent, model, prompt_version, trials: [{ trial, service_def, tool_calls? }] }.

    2. Hand-author TWO deterministic fixture transcripts under
       `ferro-mcp/tests/fixtures/agent_harness/transcripts/`:
         - `_fixture_valid.json` — a ServiceDef that, when scored, passes through to a known
           tier (e.g. t1=t2=t3=true; t4 per materialization). Pick a synthetic domain consistent
           with the corpus contamination guard (no denylisted nouns).
         - `_fixture_invalid.json` — a deliberately-malformed ServiceDef that renders an invalid
           spec → t1=false (proves Pitfall 3 end-to-end through the replay path).
       These exist so the replay/tier tests are deterministically green BEFORE the Wave-4 gated
       run produces the real per-corpus transcripts. Name them with a leading underscore so the
       Wave-4 run's real transcripts (one per corpus task) sit alongside without collision.

    3. `#[tokio::test] async fn agent_eval_replay_scores_are_deterministic()` — NOT `#[ignore]`,
       no env, no network: load the two fixture transcripts via `include_str!`/dir read,
       run each trial's `service_def` through the IDENTICAL `score()` function, and assert the
       per-tier results equal the EXPECTED hand-computed results for the fixtures (determinism +
       same-scorer guard). Running it twice yields identical results (pure scorer).

    4. `#[test] fn tier_results_never_collapse_to_boolean()` — assert `TierResult` is a 4-field
       struct and that a ServiceDef passing T1 but failing T2 records `{t1:true, t2:false, t3:false, t4:false}`
       (cumulative independence; RESEARCH Observable signal "Tier independence").

    5. `#[test] fn t1_invalid_spec_scores_fail_without_panic()` — feed the `_fixture_invalid`
       ServiceDef to the T1 scorer, assert t1=false and the process survives (Pitfall 3 signal).

    NO `#[ignore]` on any test in this task — they all run in default CI. NO network, NO API key,
    NO writes outside `tempfile::tempdir`.
  </action>
  <verify>
    <automated>cargo test -p ferro-mcp --test agent_harness 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p ferro-mcp --test agent_harness` exits 0 with NO env vars set (replay path green)
    - The three test fns `agent_eval_replay_scores_are_deterministic`, `tier_results_never_collapse_to_boolean`, `t1_invalid_spec_scores_fail_without_panic` exist and are NOT `#[ignore]`
    - `_fixture_valid.json` and `_fixture_invalid.json` exist under tests/fixtures/agent_harness/transcripts/ and parse as transcripts
    - Running `cargo test -p ferro-mcp --test agent_harness` twice produces identical pass/fail (determinism)
    - The contamination guard (`corpus_contamination_guard`) STILL passes (no fixture noun leaked)
    - No test in agent_harness.rs other than the live one (Wave 3/4) is `#[ignore]`: `grep -c '#\[ignore' ferro-mcp/tests/agent_harness.rs` is 0 at end of this plan
  </acceptance_criteria>
  <done>Replay path is green in default cargo test; tier-independence + T1-invalid-without-panic proven; deterministic fixture transcripts committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixtures ↔ public repo | Fixture transcripts/baseline are public artifacts; must carry no secrets/PII |
| scorer ↔ filesystem (T4) | T4 writes a materialized projection; must stay inside a tempdir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-210-04 | Information disclosure | fixture transcripts | mitigate | Hand-authored fixtures use synthetic domains only; contamination guard test asserts no denylisted/app-identity noun leaks into committed JSON |
| T-210-05 | Denial of service | T1 scorer | mitigate | Pitfall-3 catch_unwind/validate prevents a malformed ServiceDef from aborting the whole test process (a panicking scorer would DoS the suite) |
| T-210-06 | Tampering | T4 materialization | mitigate | All T4 filesystem work confined to tempfile::tempdir; a test asserts no write outside the tempdir |
| T-210-07 | Information disclosure | scorer/replay path | accept | Replay path makes no network calls and reads no secrets — no API key path exists in this plan |
</threat_model>

<verification>
- `cargo test -p ferro-mcp --test agent_harness` exits 0 with NO env vars (replay path green — the always-green no-network invariant).
- `cargo clippy -p ferro-mcp --all-targets -- -D warnings` clean.
- Scorer returns 4-field TierResult; tier independence + cumulative semantics proven by test.
- Pitfall 3 (no panic on invalid spec) and Pitfall 4 (tempdir materialization for T4) each proven by an explicit test.
- Determinism: two consecutive runs identical.
</verification>

<success_criteria>
The deterministic T1–T4 scorer and the replay path are built and CI-green WITHOUT any LLM —
the substantive deliverable, built before the live loop. Addresses ROADMAP SC#2 (4 tiers
defined in source, reported separately, no boolean collapse) and SC#4's replay half (CI-safe,
no live key). Both pitfalls are mitigated and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/210-comp-03-agent-success-rate-harness/210-02-SUMMARY.md`
</output>
