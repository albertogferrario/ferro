---
phase: 210-comp-03-agent-success-rate-harness
plan: 03
type: execute
wave: 3
depends_on: [210-01, 210-02]
files_modified:
  - ferro-mcp/tests/agent_harness.rs
autonomous: true
requirements: [COMP-03]
user_setup:
  - service: anthropic
    why: "Live gated agent eval (run only in Wave 4 by the developer, not in CI)"
    env_vars:
      - name: FERRO_AI_API_KEY
        source: "Anthropic Console -> API Keys (fallback ANTHROPIC_API_KEY)"
must_haves:
  truths:
    - "An in-process rmcp client+server over tokio::io::duplex drives the 3 dev tools (generation_context, json_ui_catalog, checkpoint_projection) — not ferro-mcp-server, not a subprocess."
    - "The agent tool-use loop reuses ferro_ai::AnthropicClient::complete_with_tools; the loop driver maps ToolUseBlocks to rmcp call_tool and back."
    - "The live test is #[ignore] AND env-gated on FERRO_AGENT_EVAL=1 — default cargo test skips it with no API key, no network."
    - "generate_projection is NOT in the agent toolset (D-06); only the 3 introspection tools are exposed."
    - "All Wave 1 and Wave 2 no-LLM tests (corpus_contamination_guard, agent_eval_replay_scores_are_deterministic, tier_results_never_collapse_to_boolean, t1_invalid_spec_scores_fail_without_panic) remain passing after Wave 3 additions — `cargo test -p ferro-mcp --test agent_harness` exits 0 with NO env vars set."
  artifacts:
    - path: "ferro-mcp/tests/agent_harness.rs"
      provides: "in-process transport wiring + complete_with_tools loop + gated live test"
      contains: "tokio::io::duplex"
  key_links:
    - from: "agent tool-use loop"
      to: "FerroMcpService dev tools over duplex"
      via: "rmcp client.peer().call_tool(CallToolRequestParam{name, arguments})"
      pattern: "call_tool"
    - from: "agent loop"
      to: "ferro_ai::AnthropicClient"
      via: "complete_with_tools"
      pattern: "complete_with_tools"
---

<objective>
Wire the live (gated) half: an in-process rmcp client+server over `tokio::io::duplex`
exposing exactly the 3 D-06 dev tools, and the agentic tool-use loop driving
`claude-opus-4-8` via `ferro_ai::AnthropicClient::complete_with_tools`. The loop captures
each trial's final ServiceDef. The actual baseline-writing run is Wave 4 (requires a live
API key); this plan delivers the loop CODE, compile-verified, behind the gate.

Purpose: this is the apparatus that feeds the Wave-2 scorer with freshly-authored
ServiceDefs. It is autonomous to BUILD (compiles, gated test present) but does not RUN live
in CI — the live test is `#[ignore]` + `FERRO_AGENT_EVAL=1`-gated, preserving the always-green
no-network invariant.
Output: in-process transport helper, tool-use loop driver, the gated `#[ignore]` live test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/210-comp-03-agent-success-rate-harness/210-CONTEXT.md
@.planning/phases/210-comp-03-agent-success-rate-harness/210-RESEARCH.md
@.planning/phases/210-comp-03-agent-success-rate-harness/210-PATTERNS.md
@.planning/phases/210-comp-03-agent-success-rate-harness/210-02-SUMMARY.md

<interfaces>
<!-- AUTHORITATIVE: agent toolset = generation_context, json_ui_catalog, checkpoint_projection.
     generate_projection is EXCLUDED (D-06; model-derivation not NL authoring). ROADMAP SC#1's
     list_projections/generate_projection/validate_projection mention is STALE — follow D-06.
     Do NOT re-introduce generate_projection. -->

In-process transport (PATTERNS §1; server.rs lines 21-32 prove serve((stdin,stdout))):
  let (client_stream, server_stream) = tokio::io::duplex(64 * 1024);
  let service = FerroMcpService::new(project_root);          // service.rs lines 15-27; derives Clone
  let _server_handle = service.serve(server_stream).await?;  // server half
  let client: RunningService<rmcp::RoleClient, ()> = ().serve(client_stream).await?;
  let result = client.peer().call_tool(CallToolRequestParam {
      name: "json_ui_catalog".into(),
      arguments: Some(serde_json::json!({}).as_object().unwrap().clone()),
  }).await?;
  // extract text: result.content.iter().filter_map(|c| c.raw.as_text()).map(|t| t.text.as_str())...
  // A-rmcp ASSUMPTION: confirm serve() accepts a single DuplexStream vs a (read,write) tuple by
  //   reading the rmcp 0.12 ServiceExt::serve signature at execute time — server.rs uses a tuple.

ferro-ai client (client/mod.rs lines 33-191; anthropic.rs lines 35-45, 273-331):
  use ferro_ai::client::{AnthropicClient, CompletionRequest, CompletionResponse, LlmClient,
                         Message, Role, ToolChoice, ToolRequest, ToolUseBlock};
  enum CompletionResponse { Text(String), ToolUse { blocks: Vec<ToolUseBlock>, assistant_content: String } }
  struct ToolUseBlock { id: String, name: String, input: serde_json::Value }
  struct ToolRequest { name: String, description: String, parameters_schema: serde_json::Value }
  async fn LlmClient::complete_with_tools(&self, request: CompletionRequest) -> Result<CompletionResponse, Error>
  let client = AnthropicClient::new(api_key, Some("claude-opus-4-8".into()));  // D-02 pin
  // History reconstruction (mod.rs ToolUse doc-comment): push Assistant{content: assistant_content}
  //   FIRST, then one Role::Tool per block with tool_call_id = block.id.
  // API key: std::env::var("FERRO_AI_API_KEY").or_else(|_| std::env::var("ANTHROPIC_API_KEY"))

ServiceDef shape for the prompt (service.rs line 62 — derives JsonSchema):
  Inject schemars::schema_for!(ServiceDef) as the shape reference — teaches shape WITHOUT
  domain examples (generation_context has no ServiceDef guidance; defeats nothing in the guard).
  Prompt must FORBID intent_hints (Wave-2 scorer disqualifies T2 if present).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: In-process rmcp transport + tool dispatch over duplex</name>
  <read_first>
    - ferro-mcp/src/server.rs (lines 21-32 — proves service.serve(asyncrw) works; copy the serve shape)
    - ferro-mcp/src/service.rs (lines 15-27 — FerroMcpService::new + Clone)
    - ferro-api-mcp/tests/e2e.rs (lines 13-19 imports, 191-200 + 340-357 — call_tool shape + content extraction; 294 cancel)
    - rmcp 0.12 ServiceExt::serve signature (confirm DuplexStream vs (read,write) tuple — A-rmcp). Use Context7/ctx7 for rmcp 0.12 transport-async-rw if the local signature is ambiguous.
    - .planning/phases/210-comp-03-agent-success-rate-harness/210-PATTERNS.md (§1 in-process rmcp transport, lines 22-97)
  </read_first>
  <action>
    In `ferro-mcp/tests/agent_harness.rs` (all behind `#[cfg(test)]`, dev-deps only):

    1. `async fn spawn_in_process_client(project_root: PathBuf) -> RunningService<rmcp::RoleClient, ()>`:
       - `let (client_stream, server_stream) = tokio::io::duplex(64 * 1024);`
       - `let service = FerroMcpService::new(project_root);`
       - spawn the server half: `service.serve(server_stream)` (match the exact serve signature
         from server.rs — it currently serves `(stdin, stdout)`, a tuple; if serve needs a tuple,
         split the duplex accordingly or pass the DuplexStream per the rmcp 0.12 signature you read).
         Keep the server handle alive (tokio::spawn or hold it).
       - `let client = ().serve(client_stream).await?;` (the RoleClient handshake).
       - return the client.

    2. `async fn call_dev_tool(client, name: &str, args: serde_json::Value) -> String`:
       `client.peer().call_tool(CallToolRequestParam { name: name.into(),
       arguments: args.as_object().cloned() }).await?` then extract text via
       `result.content.iter().filter_map(|c| c.raw.as_text())...join("\n")`.

    3. A gated smoke test `#[tokio::test] #[ignore = "in-process rmcp roundtrip; run with FERRO_AGENT_EVAL=1"]`
       with the same `if env::var("FERRO_AGENT_EVAL").is_err() { return; }` early-return guard, that
       stands up the client over a tempdir project_root and calls `json_ui_catalog` and asserts a
       non-empty text result. This proves the transport WITHOUT an LLM (no API key needed) — but keep
       it gated so default CI does not depend on rmcp client runtime behavior. (The Wave-2 replay
       tests remain the unconditional CI signal.)

    Confirm: ONLY generation_context, json_ui_catalog, checkpoint_projection are ever called by name —
    never generate_projection (D-06).
  </action>
  <verify>
    <automated>cargo test -p ferro-mcp --test agent_harness 2>&1 | tail -12 && cargo clippy -p ferro-mcp --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'tokio::io::duplex' ferro-mcp/tests/agent_harness.rs` matches
    - `grep 'FerroMcpService::new' ferro-mcp/tests/agent_harness.rs` matches
    - `grep 'call_tool' ferro-mcp/tests/agent_harness.rs` matches
    - `grep -c 'generate_projection' ferro-mcp/tests/agent_harness.rs` returns 0 (D-06: excluded)
    - `cargo test -p ferro-mcp --test agent_harness` exits 0 with NO env vars (the duplex smoke test is `#[ignore]` + gated, so default run still skips/passes it)
    - `cargo clippy -p ferro-mcp --all-targets -- -D warnings` clean
  </acceptance_criteria>
  <done>In-process rmcp client over duplex drives the 3 dev tools; compile + clippy clean; default CI unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Agent tool-use loop + gated live eval test (no live run here)</name>
  <read_first>
    - ferro-ai/src/client/mod.rs (lines 33-191 — CompletionResponse/ToolUseBlock/ToolRequest/Message/Role; the ToolUse history-reconstruction doc-comment ~107-114)
    - ferro-ai/src/client/anthropic.rs (lines 35-45 new(); 273-331 complete_with_tools impl + stop_reason handling)
    - ferro-ai/src/config.rs (lines 48-56 — FERRO_AI_API_KEY / ANTHROPIC_API_KEY)
    - ferro-projections/src/service.rs (line 62 — ServiceDef derives JsonSchema; for schema_for! injection)
    - framework/tests/async_validation_pg_gate.rs (the #[ignore] + env-guard belt-and-suspenders precedent)
    - .planning/phases/210-comp-03-agent-success-rate-harness/210-PATTERNS.md (§3 ferro-ai loop + §2 gate idiom)
    - .planning/phases/210-comp-03-agent-success-rate-harness/210-RESEARCH.md (§Gate Pattern, §ferro-ai Client Surface, §Transcript & Baseline Format)
  </read_first>
  <action>
    In `ferro-mcp/tests/agent_harness.rs`:

    1. Build the prompt (Claude's discretion on exact text + a `prompt_version` string constant,
       e.g. `const PROMPT_VERSION: &str = "v1";`). The system/user prompt must:
       - give the task `description` (NL only),
       - supply the ServiceDef shape via `serde_json::to_string_pretty(&schemars::schema_for!(ferro_projections::ServiceDef))`,
       - INSTRUCT the agent to read the tools then emit ONLY a final ServiceDef JSON,
       - FORBID `intent_hints` (the Wave-2 scorer disqualifies T2 if present — keep the gate honest),
       - NOT name the target intent and NOT use ferro intent vocabulary (contamination discipline).

    2. Define `Vec<ToolRequest>` for EXACTLY the 3 allowed tools (D-06):
       - generation_context: `{"type":"object","properties":{}}`
       - json_ui_catalog: `{"type":"object","properties":{"component":{"type":"string"}}}`
       - checkpoint_projection: `{"type":"object","properties":{"name":{"type":"string"}}}`
       NEVER include generate_projection.

    3. `async fn run_agent_trial(client_llm: &AnthropicClient, rmcp_client, task, project_root) -> serde_json::Value`:
       the multi-turn loop —
         - call `complete_with_tools(CompletionRequest{ messages, tools: Some(tools), tool_choice: Some(ToolChoice::Auto), model_override: Some("claude-opus-4-8"), ...})`;
         - on `CompletionResponse::ToolUse{ blocks, assistant_content }`: push `Message{role:Assistant, content:assistant_content, tool_call_id:None}` FIRST, then for each block dispatch via `call_dev_tool(rmcp_client, &block.name, block.input.clone())` and push `Message{role:Tool, content:result, tool_call_id:Some(block.id)}`; loop;
         - on `CompletionResponse::Text(s)`: parse the final ServiceDef JSON out of `s` (strip code fences) and return it;
         - cap iterations (e.g. 8) to bound cost.

    4. The gated live test:
       ```rust
       #[tokio::test]
       #[ignore = "live LLM eval; run with FERRO_AGENT_EVAL=1 and FERRO_AI_API_KEY set"]
       async fn agent_eval_live_refresh_baseline() {
           if std::env::var("FERRO_AGENT_EVAL").is_err() {
               eprintln!("skipping: set FERRO_AGENT_EVAL=1 to run live eval");
               return;
           }
           // for each corpus task × ≥3 trials: run_agent_trial -> score() -> collect;
           // write per-task transcript JSON + baseline.json (Wave 4 actually runs this).
       }
       ```
       Implement the body fully (it must compile), including writing transcripts (one file per
       corpus task: `<task_id>.json`) and `baseline.json` (model, prompt_version, generated_at,
       tasks, trials_per_task, per-tier rates as integer pass-counts or fractions). Source the API
       key from FERRO_AI_API_KEY/ANTHROPIC_API_KEY ONLY; NEVER log it; NEVER write it into any
       transcript/baseline.

    DO NOT execute the live run in this plan (no API key in the autonomous env). The acceptance
    is COMPILE + gate-skips-cleanly. The real run is Wave 4 (autonomous: false).
  </action>
  <verify>
    <automated>cargo test -p ferro-mcp --test agent_harness 2>&1 | tail -15 && cargo clippy -p ferro-mcp --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'complete_with_tools' ferro-mcp/tests/agent_harness.rs` matches
    - `grep 'schema_for!' ferro-mcp/tests/agent_harness.rs` matches (ServiceDef shape injected, no domain examples)
    - `grep -E 'FERRO_AI_API_KEY|ANTHROPIC_API_KEY' ferro-mcp/tests/agent_harness.rs` matches AND the key is never passed to eprintln!/println!/any transcript write (manual read confirms no log of the key)
    - `grep 'FERRO_AGENT_EVAL' ferro-mcp/tests/agent_harness.rs` matches AND the live test carries `#[ignore`
    - `grep -c 'generate_projection' ferro-mcp/tests/agent_harness.rs` returns 0
    - `grep 'intent_hints' ferro-mcp/tests/agent_harness.rs` matches (prompt forbids it)
    - `cargo test -p ferro-mcp --test agent_harness` exits 0 with NO env vars (live test skipped) and the replay/contamination tests STILL pass
    - `cargo clippy -p ferro-mcp --all-targets -- -D warnings` clean
  </acceptance_criteria>
  <done>Tool-use loop + gated live test compile and are correctly gated; default CI green and unchanged; the live run is left for Wave 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process ↔ Anthropic API | API key crosses here (gated path only); untrusted model output returns here |
| LLM output ↔ scorer/filesystem | Agent-emitted ServiceDef + tool inputs are untrusted; must not exfiltrate repo beyond the prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-210-08 | Information disclosure | API key handling | mitigate | Key sourced from FERRO_AI_API_KEY/ANTHROPIC_API_KEY env ONLY; never logged (no println!/eprintln! of the key), never written into transcripts/baseline — acceptance criterion + threat_model both enforce |
| T-210-09 | Information disclosure | prompt construction | mitigate | Prompt carries only the task NL description + the ServiceDef JSON schema; it does NOT embed repo source, secrets, or tenant data — the gated path cannot exfiltrate beyond the intended prompt |
| T-210-10 | Elevation of privilege | agent tool exposure | mitigate | Only the 3 read-only introspection tools are exposed to the agent (D-06); generate_projection and any mutating tool are excluded — verified `grep -c generate_projection == 0` |
| T-210-11 | Tampering | untrusted LLM tool inputs | accept | block.input is passed to read-only dev tools over the in-process transport in a tempdir project_root; worst case is a failed tool call, scored as a tier failure — low risk |
</threat_model>

<verification>
- `cargo test -p ferro-mcp --test agent_harness` exits 0 with NO env vars (live test gated/skipped; replay path unchanged-green).
- `cargo clippy -p ferro-mcp --all-targets -- -D warnings` clean.
- In-process transport (duplex) drives exactly the 3 D-06 tools; generate_projection never referenced.
- Live test is `#[ignore]` + `FERRO_AGENT_EVAL=1`-gated; API key never logged or committed.
</verification>

<success_criteria>
The live (gated) apparatus is built and compiles: in-process rmcp duplex transport over the 3
D-06 tools, and the complete_with_tools agent loop. The always-green no-network invariant holds
(live test gated). Addresses ROADMAP SC#1 (in-process rmcp 0.12 duplex transport, the 3 tools,
not ferro-mcp-server) and the loop half of SC#4. The actual baseline-producing run is Wave 4.
</success_criteria>

<output>
After completion, create `.planning/phases/210-comp-03-agent-success-rate-harness/210-03-SUMMARY.md`
</output>
