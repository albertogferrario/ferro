---
phase: 210-comp-03-agent-success-rate-harness
plan: 04
type: execute
wave: 4
depends_on: [210-01, 210-02, 210-03]
files_modified:
  - ferro-mcp/tests/fixtures/agent_harness/transcripts/
  - ferro-mcp/tests/fixtures/agent_harness/baseline.json
  - .planning/phases/210-comp-03-agent-success-rate-harness/210-WEAKNESSES.md
autonomous: false
requirements: [COMP-03]
user_setup:
  - service: anthropic
    why: "First committed baseline requires a live claude-opus-4-8 run (~42 calls); no API key exists in the autonomous environment"
    env_vars:
      - name: FERRO_AI_API_KEY
        source: "Anthropic Console -> API Keys (fallback ANTHROPIC_API_KEY)"
must_haves:
  truths:
    - "A committed baseline.json records model=claude-opus-4-8, a prompt_version, and per-tier pass rates across 14 tasks × ≥3 trials."
    - "Per-task transcripts are committed; the replay path scores them through the SAME scorer and reproduces the committed baseline."
    - "The replay test asserts replayed tier rates == committed baseline (determinism guard) and is green in default cargo test."
    - "A 'discovered weaknesses' finding names ≥1 real thing the agent consistently got wrong (empty section fails the phase — SC#5)."
  artifacts:
    - path: "ferro-mcp/tests/fixtures/agent_harness/baseline.json"
      provides: "first committed baseline (model, prompt version, per-tier rates)"
      contains: '"claude-opus-4-8"'
    - path: ".planning/phases/210-comp-03-agent-success-rate-harness/210-WEAKNESSES.md"
      provides: "SC#5 discovered-weaknesses finding"
      min_lines: 8
  key_links:
    - from: "committed transcripts"
      to: "baseline.json"
      via: "replay test recomputes tier rates and asserts equality"
      pattern: "baseline"
---

<objective>
Produce and commit the FIRST real baseline: the developer runs the gated live eval locally
(`FERRO_AGENT_EVAL=1` + API key), which writes per-task transcripts + `baseline.json`. Then
wire the replay test to assert replayed rates == committed baseline (determinism guard), and
author the SC#5 "discovered weaknesses" finding.

Purpose: this is the only non-autonomous plan — the autonomous environment has no API key, so
the ~42-call live run must be performed by the developer. Everything around it (the replay
assertion, the weakness write-up) is verifiable afterward. SC#5 (a named real weakness) is a
hard phase-close gate: an empty section fails the phase.
Output: committed transcripts, baseline.json, replay-equals-baseline assertion, 210-WEAKNESSES.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/210-comp-03-agent-success-rate-harness/210-CONTEXT.md
@.planning/phases/210-comp-03-agent-success-rate-harness/210-RESEARCH.md
@.planning/phases/210-comp-03-agent-success-rate-harness/210-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Developer runs the gated live eval (no autonomous path — needs API key)</name>
  <read_first>
    - .planning/phases/210-comp-03-agent-success-rate-harness/210-RESEARCH.md (§Validation Architecture "Live (gated) command" + §Transcript & Baseline Format + Pitfall 5/6)
    - ferro-mcp/tests/agent_harness.rs (the gated test `agent_eval_live_refresh_baseline` from Wave 3)
  </read_first>
  <what-built>
    The Wave-3 gated test `agent_eval_live_refresh_baseline` runs 14 corpus tasks × ≥3 trials
    against `claude-opus-4-8` via the in-process rmcp transport, scores each trial through the
    Wave-2 scorer, and writes per-task transcripts + `baseline.json`. It cannot run in the
    autonomous environment (no API key, by design).
  </what-built>
  <action>
    MANUAL (checkpoint:human-action — no autonomous path). This step requires a live Anthropic
    API key that does not exist in the autonomous environment, so the executor cannot perform it.
    The developer runs the gated live eval locally per <how-to-verify>, then commits the produced
    transcripts + baseline.json after confirming no API key / PII leaked into any committed file.
    There is no CLI/API the executor can substitute — this is a genuinely human-gated step (live
    LLM credential + cost). Do not attempt to fabricate or stub the baseline.
  </action>
  <how-to-verify>
    Run locally (this is the manual step):
    ```
    df -h .            # Pitfall 6: ensure disk headroom before any cargo test
    export FERRO_AI_API_KEY=...      # or ANTHROPIC_API_KEY
    FERRO_AGENT_EVAL=1 cargo test -p ferro-mcp --test agent_harness \
        agent_eval_live_refresh_baseline -- --ignored --nocapture
    ```
    Expected outputs:
    1. `ferro-mcp/tests/fixtures/agent_harness/transcripts/<task_id>.json` for all 14 corpus tasks,
       each with ≥3 trials, each trial carrying the agent's final `service_def`.
    2. `ferro-mcp/tests/fixtures/agent_harness/baseline.json` with `"model": "claude-opus-4-8"`,
       a `prompt_version`, `tasks: 14`, `trials_per_task >= 3`, and per-tier pass rates (t1..t4).
    3. NO API key string in any committed file (`grep -r 'sk-' ferro-mcp/tests/fixtures/agent_harness/`
       returns nothing).
    4. Pitfall 5: `git checkout docs/protocol/schemas/*.json` if the full test run dirtied them —
       do NOT fold that churn into the phase commit.
    Then commit the transcripts + baseline.
  </how-to-verify>
  <verify>
    <automated>MISSING — manual gated live run (no API key in autonomous env). Verified by Task 2's replay-equals-baseline test once artifacts are committed.</automated>
  </verify>
  <done>Transcripts (14 tasks × ≥3 trials) + baseline.json committed; no API key/PII in any committed file.</done>
  <resume-signal>Type "baseline committed" once transcripts + baseline.json are written and committed, with no API key in any committed file.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Replay-equals-baseline determinism assertion</name>
  <read_first>
    - ferro-mcp/tests/agent_harness.rs (the replay test `agent_eval_replay_scores_are_deterministic` from Wave 2 + the score() fn)
    - ferro-mcp/tests/fixtures/agent_harness/baseline.json (committed in Task 1)
    - .planning/phases/210-comp-03-agent-success-rate-harness/210-RESEARCH.md (§Transcript & Baseline Format "Replay assertion")
  </read_first>
  <action>
    Extend the replay path so it scores ALL committed per-task transcripts (not just the Wave-2
    `_fixture_*` files) through the IDENTICAL `score()` function, recomputes per-tier rates, and
    asserts they equal the committed `baseline.json` rates.

    Add (or extend) `#[tokio::test] async fn agent_eval_replay_matches_baseline()` — NOT `#[ignore]`,
    no LLM, no network:
      - read every `transcripts/<task_id>.json` (skip the `_fixture_*` ones or include them — your
        choice, but be consistent),
      - for each trial, look up the task's `target_intent` and run `score(service_def, target)`,
      - aggregate per-(task×trial) cumulative pass fractions per tier,
      - assert they equal `baseline.json`'s `tier_rates`. To avoid float-equality fragility, store
        and compare INTEGER pass-counts (recommended by RESEARCH), or compare fractions with an
        exact rational/round. A mismatch means the scorer changed (regenerate baseline) or
        non-determinism leaked (bug) — the assertion message should say so.

    This is the determinism guard: same committed transcripts → same rates → CI-stable without an LLM.
  </action>
  <verify>
    <automated>cargo test -p ferro-mcp --test agent_harness 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p ferro-mcp --test agent_harness` exits 0 with NO env vars set (replay matches committed baseline deterministically)
    - The test `agent_eval_replay_matches_baseline` exists, is NOT `#[ignore]`, makes no network call
    - Running it twice yields identical results
    - `baseline.json` contains `"model": "claude-opus-4-8"`, a `prompt_version`, and `tier_rates` with t1..t4 reported separately (never one boolean)
    - `python3 -c "import json,glob; b=json.load(open('ferro-mcp/tests/fixtures/agent_harness/baseline.json')); assert b['model']=='claude-opus-4-8'; assert 'prompt_version' in b; assert set(b['tier_rates'])>={'t1','t2','t3','t4'}; print('OK')"` prints OK
  </acceptance_criteria>
  <done>Replay reproduces the committed baseline deterministically in default CI; the determinism guard is live.</done>
</task>

<task type="auto">
  <name>Task 3: SC#5 discovered-weaknesses finding</name>
  <read_first>
    - ferro-mcp/tests/fixtures/agent_harness/baseline.json (the per-tier + per-intent rates)
    - ferro-mcp/tests/fixtures/agent_harness/transcripts/ (inspect failing trials to name a concrete pattern)
    - .planning/phases/210-comp-03-agent-success-rate-harness/210-RESEARCH.md (§Open Questions Q1 "generation_context has no ServiceDef guidance" — a candidate finding)
    - .planning/ROADMAP.md (Phase 210 SC#5 — empty section FAILS the phase)
  </read_first>
  <action>
    Write `.planning/phases/210-comp-03-agent-success-rate-harness/210-WEAKNESSES.md` naming at
    least ONE real, concrete finding from the baseline — NOT a placeholder. Candidates to look for
    in the transcripts/rates:
      - a tier where the pass rate is lower than expected (e.g. T3 binding completeness, or the
        thin Analyze↔Summarize T2 margin causing Analyze tasks to derive Summarize),
      - a structural pattern the agent consistently gets wrong (e.g. omits `group_by` on KanbanBoard,
        emits `intent_hints` to cheat T2 — which the scorer disqualifies, leaves fields non-writable
        for Collect, adds a second numeric field to an Analyze task flipping it to Summarize),
      - the `generation_context` gap (Open Question Q1): the tool gives no ServiceDef authoring
        guidance, so the agent relies entirely on the prompt schema — a measurable surface weakness.

    For each finding: state the tier/task affected, the observed rate or failure pattern (cite the
    transcript), and a one-line hypothesis for the cause. This is the honest "the harness can FAIL"
    deliverable (REQUIREMENTS) and the SC#5 phase-close gate. Keep the voice neutral/architectural
    per CLAUDE.md (repo docs read as public).
  </action>
  <verify>
    <automated>test -s .planning/phases/210-comp-03-agent-success-rate-harness/210-WEAKNESSES.md && wc -l .planning/phases/210-comp-03-agent-success-rate-harness/210-WEAKNESSES.md</automated>
  </verify>
  <acceptance_criteria>
    - `210-WEAKNESSES.md` exists and is non-empty (≥8 lines)
    - It names at least one CONCRETE finding (a specific tier/task + observed rate or failure pattern + a cited transcript), not a placeholder
    - It does not contain TODO/placeholder text like "TBD" / "to be filled" / "<finding>"
    - The finding references actual baseline numbers or transcript content (verifiable against the committed files)
  </acceptance_criteria>
  <done>SC#5 satisfied: a real, concrete discovered-weakness finding is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer env ↔ committed artifacts | Live run writes transcripts/baseline that get committed (public) |
| API key ↔ committed files | Key must never leak into transcripts/baseline/logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-210-12 | Information disclosure | committed transcripts/baseline | mitigate | Task 1 how-to-verify includes `grep -r 'sk-' ... returns nothing`; transcripts store only ServiceDef JSON + tier results, never the key or PII |
| T-210-13 | Information disclosure | live run logs | mitigate | `--nocapture` output may print tool results but never the API key (Wave-3 acceptance forbade logging it); developer reviews before commit |
| T-210-14 | Tampering | baseline determinism | mitigate | Replay-equals-baseline assertion (Task 2) detects any drift between committed transcripts and the recorded rates |
</threat_model>

<verification>
- `baseline.json` committed with model=claude-opus-4-8, prompt_version, per-tier rates (14 tasks × ≥3 trials).
- Per-task transcripts committed; no API key/PII in any committed file.
- `cargo test -p ferro-mcp --test agent_harness` exits 0 with NO env vars (replay reproduces baseline).
- `210-WEAKNESSES.md` names ≥1 concrete finding (SC#5 gate).
- Full workspace gate green: `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` (check `df` first — Pitfall 6; `git checkout docs/protocol/schemas/*.json` — Pitfall 5).
</verification>

<success_criteria>
The first committed baseline exists (model + prompt version + per-tier rates), the replay path
reproduces it deterministically in default CI, and a real discovered-weakness finding is recorded.
Addresses ROADMAP SC#4 (committed baseline; ≥3 trials; tests `#[ignore]` in CI) and SC#5 (named
weakness — empty fails the phase). This closes COMP-03.
</success_criteria>

<output>
After completion, create `.planning/phases/210-comp-03-agent-success-rate-harness/210-04-SUMMARY.md`
</output>
