---
phase: 229-framework-benchmark-harness-foundation-1a-build-the-reproduc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - benchmark/README.md
  - benchmark/.gitignore
  - benchmark/results/.gitkeep
  - benchmark/contracts/micro-endpoints.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "The benchmark/ tree exists with harness, contracts, apps, results subdirs"
    - "benchmark/.gitignore excludes app build artifacts but NOT results/"
    - "The four micro-endpoints contract is documented and authoritative for both apps"
  artifacts:
    - path: "benchmark/README.md"
      provides: "Methodology skeleton, reproducibility + honesty note"
      min_lines: 20
    - path: "benchmark/.gitignore"
      provides: "Build-artifact ignore rules; results/ committed"
      contains: "apps/*/target/"
    - path: "benchmark/contracts/micro-endpoints.md"
      provides: "Shared 4-endpoint contract both apps implement identically"
      contains: "GET /json"
    - path: "benchmark/results/.gitkeep"
      provides: "Keeps results/ tracked before first run"
  key_links:
    - from: "benchmark/contracts/micro-endpoints.md"
      to: "benchmark/contracts/conformance/test_conformance.py"
      via: "contract is the source for conformance assertions (Plan 05)"
      pattern: "GET /json|GET /db|GET /queries|GET /updates"
---

<objective>
Scaffold the top-level `benchmark/` tree and write the shared four-endpoint contract that
both the Ferro and Laravel micro-apps will implement identically.

Purpose: Establish the directory skeleton, the reproducibility/honesty README, and the single
source-of-truth contract before any harness code or apps are built. This is pure file creation
— light, no thermal cost, parallel-safe with Plan 02.

Output: `benchmark/README.md`, `benchmark/.gitignore`, `benchmark/results/.gitkeep`,
`benchmark/contracts/micro-endpoints.md`.

Mirrors PRD Task 1 (scaffold tree) and Task 2 (contract).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/229-framework-benchmark-harness-foundation-1a-build-the-reproduc/229-CONTEXT.md
@docs/superpowers/plans/2026-06-15-benchmark-1a-harness-foundation.md

Workspace-isolation constraint (CLAUDE.md + CONTEXT.md): the benchmark apps are built in their
own Docker contexts and must NOT be added to the root `Cargo.toml` `members`, or the PHP/scratch
apps confuse cargo. This plan creates no Cargo manifests, but the directory layout it establishes
must keep `benchmark/apps/*` isolated.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the benchmark tree (README, gitignore, results placeholder)</name>
  <read_first>
    - docs/superpowers/plans/2026-06-15-benchmark-1a-harness-foundation.md (Task 1 — verbatim file contents)
    - .planning/phases/229-framework-benchmark-harness-foundation-1a-build-the-reproduc/229-CONTEXT.md (D-10, D-11 honesty/reproducibility guardrails)
  </read_first>
  <files>
    benchmark/README.md
    benchmark/.gitignore
    benchmark/results/.gitkeep
  </files>
  <action>
Create the directory skeleton and three files.

1. Create directories:
```bash
mkdir -p benchmark/harness/perf benchmark/harness/static benchmark/harness/report \
         benchmark/contracts/conformance benchmark/apps benchmark/results
touch benchmark/results/.gitkeep
```

2. Write `benchmark/.gitignore` exactly:
```gitignore
# build artifacts from the sample apps — never the harness or results
apps/*/target/
apps/*/vendor/
apps/*/node_modules/
apps/*/.env
**/__pycache__/
*.pyc
# results/ is intentionally committed (auditable raw data)
```

3. Write `benchmark/README.md` (methodology skeleton):
```markdown
# Ferro Framework Benchmark

A reproducible comparison of Ferro against mature batteries-included frameworks.
Design: `docs/superpowers/specs/2026-06-15-ferro-framework-benchmark-design.md`.

## What 1A measures
Four micro-endpoints (`/json`, `/db`, `/queries`, `/updates`) in Ferro and Laravel, on two
axes: raw performance (requests/sec, p50/p99 latency, memory) and static compression (LoC,
files, source tokens).

## Reproducibility
- Apps and tooling run in pinned containers (`compose.yaml`, `harness/Dockerfile.toolbox`).
- PostgreSQL 16.4, shared by both apps.
- Perf tool: `oha` (pinned in the toolbox). Static tool: `tokei` (pinned).
- Canonical perf numbers come from a fixed local machine; each results run records the
  hardware. CI runs conformance + a perf smoke only.

## Honesty note
Internal results (`results/<date>/internal.md`) report every number, including where Ferro is
slower or larger. The public table is a subset, never a contradiction, of the internal data.
Rust outperforming interpreted languages on raw throughput is expected and is reported as
such, not as a finding.

## Run it
See "Running the benchmark" below (added in Task 9).
```
  </action>
  <verify>
    <automated>test -f benchmark/README.md && test -f benchmark/.gitignore && test -f benchmark/results/.gitkeep && grep -q 'apps/\*/target/' benchmark/.gitignore && grep -q 'results/ is intentionally committed' benchmark/.gitignore && grep -q 'Honesty note' benchmark/README.md && test -d benchmark/harness/perf && test -d benchmark/harness/static && test -d benchmark/harness/report && test -d benchmark/contracts/conformance && test -d benchmark/apps</automated>
  </verify>
  <done>All directories exist; README has the honesty note; .gitignore ignores app build artifacts and explicitly keeps results/.</done>
</task>

<task type="auto">
  <name>Task 2: Write the shared micro-endpoints contract</name>
  <read_first>
    - docs/superpowers/plans/2026-06-15-benchmark-1a-harness-foundation.md (Task 2 — verbatim contract markdown)
  </read_first>
  <files>
    benchmark/contracts/micro-endpoints.md
  </files>
  <action>
Write `benchmark/contracts/micro-endpoints.md` exactly:
```markdown
# Micro-endpoints contract (all frameworks implement this identically)

All responses `Content-Type: application/json`. Errors are out of scope (happy path only).

## GET /json
200 → `{"message":"Hello, World!"}`

## GET /db
200 → `{"id":<int 1..10000>,"randomNumber":<int>}` for one random row of `world`.

## GET /queries?n=K
`n` clamped to [1,500] (missing/invalid → 1). 200 → JSON array of K `{"id","randomNumber"}`,
each from an independent random-id lookup.

## GET /updates?n=K
`n` clamped to [1,500]. For each of K: read a random row, set `randomNumber` to a new random
int, persist. 200 → JSON array of the K updated `{"id","randomNumber"}`.

## Schema
`world(id SERIAL PRIMARY KEY, randomNumber INT NOT NULL)`, seeded 10000 rows,
`randomNumber` initialized to a random int in [1,10000].
```

Note the JSON body key is `randomNumber` (camelCase) in HTTP responses; the database column is
`randomNumber INT NOT NULL`. The Ferro app (Plan 04) maps its `random_number` Rust field to the
`randomNumber` JSON key and the conformance test (Plan 05) asserts the `randomNumber` key.
  </action>
  <verify>
    <automated>test -f benchmark/contracts/micro-endpoints.md && grep -q 'GET /json' benchmark/contracts/micro-endpoints.md && grep -q 'GET /db' benchmark/contracts/micro-endpoints.md && grep -q 'GET /queries' benchmark/contracts/micro-endpoints.md && grep -q 'GET /updates' benchmark/contracts/micro-endpoints.md && grep -q 'clamped to \[1,500\]' benchmark/contracts/micro-endpoints.md && grep -q 'world(id SERIAL PRIMARY KEY' benchmark/contracts/micro-endpoints.md</automated>
  </verify>
  <done>Contract file documents all four endpoints with clamp behavior and the world schema; both apps and the conformance test derive from it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | Pure local file creation; no network service, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-229-01 | I (Info disclosure) | benchmark/.gitignore | accept | results/ is intentionally committed and contains only synthetic benchmark numbers + non-secret hardware metadata; no PII or credentials. .gitignore excludes `.env` files so app secrets never land in git. |

Scope note: this plan stands up local benchmark scaffolding only — no auth, no secrets, no
network service, no untrusted input. N/A-level threat surface.
</threat_model>

<verification>
- `benchmark/` tree exists with the expected subdirectories.
- `.gitignore` ignores `apps/*/target/`, `apps/*/vendor/`, `apps/*/node_modules/`, `.env`, `__pycache__`, `*.pyc` and explicitly does NOT ignore `results/`.
- README contains reproducibility + honesty sections.
- Contract documents all four endpoints + the `world` schema.
- No Cargo manifests created; nothing added to root workspace members (workspace build unchanged).
</verification>

<success_criteria>
Scaffolding and contract are committed; downstream plans (harness units, apps, conformance) have
a stable tree and an authoritative contract to build against.
</success_criteria>

<output>
After completion, create `.planning/phases/229-framework-benchmark-harness-foundation-1a-build-the-reproduc/229-01-SUMMARY.md`
</output>
