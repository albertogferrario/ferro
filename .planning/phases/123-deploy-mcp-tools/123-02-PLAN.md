---
phase: 123-deploy-mcp-tools
plan: 02
type: execute
wave: 2
depends_on: [123-01]
files_modified:
  - ferro-cli/src/lib.rs
  - ferro-mcp/Cargo.toml
autonomous: true
requirements: [D-12]
must_haves:
  truths:
    - "ferro-mcp crate compiles with a dependency on ferro-cli"
    - "ferro-mcp can call ferro_cli::deploy::{parse_env_example, is_secret, scan_runtime_deps_str, RUNTIME_DEP_REGISTRY}"
    - "ferro-mcp can call ferro_cli::deploy::ferro_deps helpers and ferro_cli::commands::deploy_check::check_ref"
    - "ferro-mcp can call ferro_cli::project::find_project_root"
  artifacts:
    - path: "ferro-cli/src/lib.rs"
      provides: "Public re-exports of deploy helpers for cross-crate consumption"
      contains: "pub mod deploy"
    - path: "ferro-mcp/Cargo.toml"
      provides: "Dependency on ferro-cli"
      contains: "ferro-cli"
  key_links:
    - from: "ferro-mcp/Cargo.toml"
      to: "ferro-cli/src/lib.rs"
      via: "path dependency"
      pattern: "ferro-cli\\s*=\\s*\\{\\s*path"
---

<objective>
Enable ferro-mcp to call into ferro-cli deploy helpers without going through the CLI binary. Adds the cross-crate dependency and verifies the public surface is accessible. Per D-12 (reuse, don't duplicate).

Purpose: Single source of truth for deploy primitives.
Output: ferro-mcp depends on ferro-cli; ferro-cli re-exports the helpers ferro-mcp needs; a compile-time smoke test proves the imports resolve.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-deploy-mcp-tools/SCOPE.md
@.planning/phases/123-deploy-mcp-tools/123-CONTEXT.md
@ferro-cli/src/lib.rs
@ferro-cli/src/deploy/mod.rs
@ferro-mcp/Cargo.toml

<interfaces>
Existing ferro-cli/src/lib.rs surface:
```rust
pub mod ai;
pub mod analyzer;
pub mod commands;
pub mod deploy;
pub mod project;
pub mod templates;
```

All of `deploy::*`, `project::*`, and `commands::deploy_check::*` are already `pub mod`. The only task is:
1. Make sure no `#![allow(...)]` or private item blocks prevent cross-crate use.
2. Add `ferro-cli` as a path dep in `ferro-mcp/Cargo.toml`.
3. Remove any `#![allow(dead_code)]` that would mask real breakage, OR verify the items ferro-mcp needs are explicitly `pub`.

Items ferro-mcp needs (from Plans 03/04/05):
- `ferro_cli::deploy::parse_env_example`
- `ferro_cli::deploy::EnvEntry`
- `ferro_cli::deploy::is_secret`
- `ferro_cli::deploy::ferro_deps` module (for path-dep detection — verify a public scanner fn exists; if only `render_rewrite_script` is public, add a pure `find_ferro_path_deps(content: &str) -> Vec<String>` helper extracted from the private `discover_ferro_path_deps`)
- `ferro_cli::deploy::scan_runtime_deps_str` / `RUNTIME_DEP_REGISTRY` (from Plan 01)
- `ferro_cli::project::find_project_root`
- `ferro_cli::commands::deploy_check::check_ref`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote required deploy helpers to public cross-crate surface</name>
  <files>ferro-cli/src/lib.rs, ferro-cli/src/deploy/ferro_deps.rs, ferro-cli/src/deploy/mod.rs</files>
  <read_first>
    - ferro-cli/src/lib.rs (already exposes deploy, project, commands)
    - ferro-cli/src/deploy/ferro_deps.rs (check if discover_ferro_path_deps is pub — currently private; extract public wrapper)
    - ferro-cli/src/commands/deploy_check.rs (confirm `pub fn check_ref` — already public)
  </read_first>
  <behavior>
    - `ferro_cli::deploy::parse_env_example` is callable from an external crate
    - `ferro_cli::deploy::is_secret` is callable
    - `ferro_cli::deploy::find_ferro_path_deps(content: &str) -> Vec<String>` exists and is public (extracted from private discover_ferro_path_deps)
    - `ferro_cli::deploy::scan_runtime_deps_str` and `RUNTIME_DEP_REGISTRY` are callable (from Plan 01)
    - `ferro_cli::project::find_project_root` is callable
    - `ferro_cli::commands::deploy_check::check_ref` is callable
  </behavior>
  <action>
    1. In `ferro-cli/src/deploy/ferro_deps.rs`:
       - Rename the existing private `fn discover_ferro_path_deps(content: &str) -> Vec<String>` to `pub fn find_ferro_path_deps(content: &str) -> Vec<String>`.
       - Update the one internal call site (inside `render_rewrite_script`) to use the new name.
       - Add a module-level test that `find_ferro_path_deps("[dependencies]\nferro = { path = \"..\" }")` returns `vec!["ferro"]`.
    2. In `ferro-cli/src/deploy/mod.rs` add the re-export line:
       ```rust
       pub use ferro_deps::{find_ferro_path_deps, render_rewrite_script};
       ```
       Also ensure `parse_env_example`, `EnvEntry`, `is_secret` are already re-exported (check the existing `pub use` block and add any missing).
    3. In `ferro-cli/src/lib.rs`: no structural change required — modules are already `pub`. Remove the top-level `#![allow(dead_code)]` ONLY if present at crate root (it is not; deploy/mod.rs has it locally, leave that alone).
    4. Confirm everything compiles: `cargo build -p ferro-cli`.

    Per D-12: do not duplicate this logic in ferro-mcp. All deploy-parse helpers come from ferro-cli.
  </action>
  <verify>
    <automated>cargo test -p ferro-cli --lib deploy::ferro_deps::tests::find_ferro_path_deps_public_wrapper 2>&1 | tail -20; cargo build -p ferro-cli</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub fn find_ferro_path_deps" ferro-cli/src/deploy/ferro_deps.rs` matches
    - `grep -n "find_ferro_path_deps" ferro-cli/src/deploy/mod.rs` matches (re-export)
    - `grep -n "pub use.*parse_env_example" ferro-cli/src/deploy/mod.rs` matches
    - `grep -n "pub use.*is_secret" ferro-cli/src/deploy/mod.rs` matches
    - `cargo build -p ferro-cli` exits 0
    - `cargo clippy -p ferro-cli --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>ferro-cli publicly exposes every deploy helper ferro-mcp needs; extraction test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add ferro-cli path dep to ferro-mcp + compile smoke test</name>
  <files>ferro-mcp/Cargo.toml, ferro-mcp/src/tools/mod.rs</files>
  <read_first>
    - ferro-mcp/Cargo.toml (existing deps list — mirror `ferro-projections = { path = "../ferro-projections", version = "0.1" }` style)
    - ferro-cli/Cargo.toml (confirm the package name and version for the path dep entry)
  </read_first>
  <action>
    1. In `ferro-mcp/Cargo.toml` `[dependencies]` block add:
       ```toml
       ferro-cli = { path = "../ferro-cli", version = "0.1" }
       ```
       (Use whatever version ferro-cli declares — read its `[package] version` field.)
    2. Add a compile-time smoke probe in a new file `ferro-mcp/src/tools/deploy_common.rs`:
       ```rust
       //! Shared imports used by deploy_* MCP tools (Plans 03/04/05).
       //! This module exists to prove the ferro-cli dependency resolves and
       //! to provide a single place for re-exports ferro-mcp tools consume.
       #![allow(unused_imports)]

       pub use ferro_cli::commands::deploy_check::check_ref;
       pub use ferro_cli::deploy::{
           find_ferro_path_deps, is_secret, parse_env_example,
           scan_runtime_dep_matches, scan_runtime_deps_str, EnvEntry,
           RuntimeDep, RUNTIME_DEP_REGISTRY,
       };
       pub use ferro_cli::project::find_project_root;
       ```
    3. Wire it in `ferro-mcp/src/tools/mod.rs`: add `pub mod deploy_common;` (alphabetical order).
    4. Run `cargo build -p ferro-mcp`. If any import fails → Plan 01 or Task 1 of this plan is incomplete; fix before proceeding.

    Plans 03, 04, 05 will import from `crate::tools::deploy_common::*`.
  </action>
  <verify>
    <automated>cargo build -p ferro-mcp 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ferro-cli" ferro-mcp/Cargo.toml` matches
    - `grep -n "pub mod deploy_common" ferro-mcp/src/tools/mod.rs` matches
    - `grep -n "pub use ferro_cli::deploy::" ferro-mcp/src/tools/deploy_common.rs` matches
    - `grep -n "scan_runtime_deps_str" ferro-mcp/src/tools/deploy_common.rs` matches
    - `cargo build -p ferro-mcp` exits 0
    - `cargo clippy -p ferro-mcp --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>ferro-mcp compiles against ferro-cli, deploy_common re-export module established as the single import point for deploy tools.</done>
</task>

</tasks>

<verification>
cargo build -p ferro-mcp
cargo clippy -p ferro-mcp -p ferro-cli --all-targets -- -D warnings
</verification>

<success_criteria>
A fresh `cargo build -p ferro-mcp` succeeds with the new ferro-cli dependency and the deploy_common module resolves all re-exports.
</success_criteria>

<output>
Create `.planning/phases/123-deploy-mcp-tools/123-02-SUMMARY.md` after completion.
</output>
