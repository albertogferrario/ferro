---
phase: 195-close-the-loop-by-default
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-mcp/src/tools/checkpoint_projection.rs
  - ferro-mcp/src/service.rs
autonomous: true
requirements: [CHK-07, CHK-08, CHK-09]
tags: [mcp, projection, checkpoint, async]

must_haves:
  truths:
    - "`run_for` and `execute` are async; the checkpoint MCP handler awaits them and still returns a Verdict"
    - "The five seam names in a fresh run use the canonical vocabulary (no Phase-194 names anywhere in source or docs)"
    - "A `VerdictSummary` exists that carries top-level `status` but never the raw `seams` array"
    - "`read_ambient_status` returns `clean`/`failing`/`unverified` by reading the cache file only, never recomputing"
  artifacts:
    - path: "ferro-mcp/src/tools/checkpoint_projection.rs"
      provides: "async run_for/execute, canonical seam names, VerdictSummary type + Verdict::summary(), read_ambient_status helper"
      contains: "pub async fn run_for"
    - path: "ferro-mcp/src/service.rs"
      provides: "awaited checkpoint_projection handler call"
      contains: "checkpoint_projection::execute(&self.project_root, &params.0.name).await"
  key_links:
    - from: "ferro-mcp/src/service.rs checkpoint_projection handler"
      to: "checkpoint_projection::execute"
      via: ".await"
      pattern: "checkpoint_projection::execute.*\\.await"
    - from: "Verdict::summary()"
      to: "VerdictSummary"
      via: "method returning compact summary, not seams array"
      pattern: "fn summary\\(&self\\) -> VerdictSummary"
---

<objective>
Establish the Phase 195 foundation inside `checkpoint_projection.rs`: convert `run_for`/`execute`
to async (load-bearing for seam 3 in Plan 02 and the inline hooks in Plan 03), reconcile the four
non-canonical Phase-194 seam names to the design-spec vocabulary across source, tests, and docs,
add the shared `VerdictSummary` type with `Verdict::summary()`, and add the `read_ambient_status`
cache-read helper consumed by the ambient tools in Plan 04. Wire the already-async checkpoint
MCP handler in `service.rs` to `.await` the now-async `execute`.

Purpose: Plans 02/03/04 all depend on these primitives existing. The async conversion cannot be
deferred — seam 3 (`action_to_route`) must load routes via async `list_routes::execute`, and the
inline generator hooks call the async `run_for`.
Output: An async, canonically-named checkpoint module with a public summary type and a `pub(crate)`
ambient-status reader. No new seam wiring yet (Plan 02), no generator/ambient consumers yet (Plans 03/04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/195-close-the-loop-by-default/195-CONTEXT.md
@.planning/phases/195-close-the-loop-by-default/195-RESEARCH.md
@.planning/phases/195-close-the-loop-by-default/195-PATTERNS.md
@.planning/phases/195-close-the-loop-by-default/195-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Current types in checkpoint_projection.rs (Phase 194 output). Use directly, no exploration. -->

```rust
// Output-contract types (KEEP verbatim — Plan 02 wrapper seams reuse them):
pub struct Finding { pub subject: String, pub detail: String, pub fix: String }

#[derive(..., PartialEq, Eq)]
#[serde(rename_all = "snake_case")]
pub enum SeamStatus { Pass, Warn, Fail, NotChecked }

pub struct SeamResult {
    pub seam: String,
    pub status: SeamStatus,
    pub source: String,
    pub findings: Vec<Finding>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub reason: Option<String>,
}

pub struct Verdict {
    pub status: SeamStatus,
    pub projection: String,
    pub seams: Vec<SeamResult>,
    pub next_steps: Vec<String>,
}

// Current async-target signatures (checkpoint_projection.rs:101,107):
pub fn execute(project_root: &Path, name: &str) -> Result<Verdict, String>          // → pub async fn
pub(crate) fn run_for(project_root: &Path, name: &str, now: DateTime<Utc>) -> Result<Verdict, String>  // → pub(crate) async fn

// write_cache derives ambient_status: Pass → "clean", else "failing" (checkpoint_projection.rs ~415).
// Cache path: project_root/.ferro/checkpoints/{name}.json
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Async conversion + seam-name reconciliation (source, tests, docs)</name>
  <files>ferro-mcp/src/tools/checkpoint_projection.rs, docs/src/agents/checkpoint-projection.md</files>
  <read_first>
    - ferro-mcp/src/tools/checkpoint_projection.rs (the whole file: signatures lines 101/107, stub block 144-171, tests ~849-933 and ~966-1096)
    - 195-PATTERNS.md "Stub block to replace (lines 139–171)", "`run_for` signature change (async)", "`execute` signature change (async)", "Test pattern for async conversion" (lines 29-185)
    - 195-PATTERNS.md "Seam-Name Reconciliation — Edit Sites (D-01)" (lines 591-614) — the three edit locations
    - 195-RESEARCH.md "Async Architecture Decision" (lines 540-558) and "Seam-Name Reconciliation (D-01)" (lines 517-538)
    - ferro-mcp/src/tools/json_ui_verify_action.rs line 146 (the existing `#[tokio::test]` proving tokio is a dev-dep)
    - docs/src/agents/checkpoint-projection.md (~line 42 seam example block; ~line 78 SeamResult source caveat)
  </read_first>
  <action>
    Make `run_for` and `execute` async, and reconcile the four non-canonical seam names in the SAME commit.

    1. Signatures:
       - `pub fn execute(...) -> Result<Verdict, String>` → `pub async fn execute(...) -> Result<Verdict, String>`; body becomes `run_for(project_root, name, chrono::Utc::now()).await`.
       - `pub(crate) fn run_for(...) -> Result<Verdict, String>` → `pub(crate) async fn run_for(...) -> Result<Verdict, String>`. Body unchanged for now EXCEPT the seam-name strings below (Plan 02 replaces the stub bodies with real dispatch — leave them as `NotChecked` stubs here, only renamed).

    2. Seam-name reconciliation — rename the four `seam: "..."` literals in the stub block (checkpoint_projection.rs lines 145/152/159/166) per D-01:
       - `"schema_load"` → `"projection_well_formed"`
       - `"field_type_compat"` → `"action_to_route"`
       - `"action_binding"` → `"rendered_view"`
       - `"render_target"` → `"props_to_contract"`
       Do NOT touch seam 2 (`"field_to_column"` — already correct). Leave the stub `source: "checkpoint"` and `reason: "not_implemented_phase_195"` AS-IS — Plan 02 replaces them with real validator sources. (The grep gate in Task 4 of Plan 02 enforces final source correctness; this task's gate only enforces the seam-name renames.)

    3. Update the six affected tests (checkpoint_projection.rs ~849-933) that hard-code old names via `make_seam("schema_load", ...)` / `make_seam("action_binding", ...)`:
       - `aggregate_status_fail_wins_over_not_checked`, `aggregate_status_warn_not_checked`, `aggregate_status_pass_not_checked`, `aggregate_status_all_not_checked_is_pass`, `next_steps_ranked_deduped`, `next_steps_dedup` — replace `"schema_load"`→`"projection_well_formed"`, `"action_binding"`→`"rendered_view"` (these tests only use those two old names; verify by grep).

    4. Async test conversion: every existing `#[test]` that calls `run_for(...)` or `execute(...)` directly (the cache/integration tests ~966-1096) becomes `#[tokio::test] async fn` with `.await` on the call. Tests that call ONLY pure helpers (`aggregate_status`, `aggregate_next_steps`, `write_cache`, `count_column_backed_builders`, `field_to_column_seam`, `make_seam`) STAY synchronous `#[test]` — do not convert these.

    5. Docs: in `docs/src/agents/checkpoint-projection.md`, update the seam example JSON block (~line 42) from `"schema_load"` to `"projection_well_formed"` (and its `source` from `"checkpoint"` to `"validate_projection"`, anticipating Plan 02 wiring). Update the SeamResult `source` table caveat (~line 78): remove the "always `checkpoint` in this version" wording; state that seams 1/3/4/5 name their delegating validator.

    Add a focused test `seam_names_canonical` (`#[tokio::test]`): run `run_for` against a fixture projection (reuse the existing Phase-194 test fixture setup, e.g. `booking_service`), collect `verdict.seams.iter().map(|s| &s.seam)`, and assert the set is exactly `{projection_well_formed, field_to_column, action_to_route, rendered_view, props_to_contract}` — no old names present.
  </action>
  <verify>
    <automated>grep -rn "schema_load\|field_type_compat\|action_binding\|render_target" ferro-mcp/src/ docs/ ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "schema_load\|field_type_compat\|action_binding\|render_target" ferro-mcp/src/ docs/` returns nothing (exit 1 / no matches).
    - `grep -n "pub async fn execute" ferro-mcp/src/tools/checkpoint_projection.rs` matches.
    - `grep -n "pub(crate) async fn run_for" ferro-mcp/src/tools/checkpoint_projection.rs` matches.
    - `cargo test -p ferro-mcp checkpoint_projection::tests::seam_names_canonical` exits 0.
    - `cargo test -p ferro-mcp checkpoint_projection` exits 0 (all Phase-194 tests still green after async + rename).
  </acceptance_criteria>
  <done>run_for/execute are async, all four seams use canonical names in source/tests/docs, the grep gate is clean, and seam_names_canonical passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VerdictSummary type + Verdict::summary() + read_ambient_status helper</name>
  <files>ferro-mcp/src/tools/checkpoint_projection.rs</files>
  <read_first>
    - ferro-mcp/src/tools/checkpoint_projection.rs (Verdict definition lines 59-69; SeamResult derives lines 45-56; write_cache + CacheEntry ~407-437)
    - 195-PATTERNS.md "`VerdictSummary` type placement" (lines 157-162), "`write_cache` / `CacheEntry` analog for `read_ambient_status`" (lines 131-155)
    - 195-RESEARCH.md "VerdictSummary type (D-08)" recommended shape (lines 339-381) and "Cache read helper" (lines 404-427)
    - 195-PATTERNS.md "`pub(crate)` helper consumed by sibling tool modules" (lines 570-574)
  </read_first>
  <action>
    Add the shared summary type and the ambient-status reader. Both are consumed by later plans (03 uses VerdictSummary; 04 uses read_ambient_status).

    1. Define `VerdictSummary` in the public output-contract block (after the `Verdict` struct, ~line 69). Derive `Debug, Clone, Serialize, JsonSchema` (same set as `Verdict`). Field layout (D-08: MUST have top-level `status`, MUST NOT contain the raw `seams` array):
       ```rust
       pub struct VerdictSummary {
           pub status: SeamStatus,
           #[serde(skip_serializing_if = "Vec::is_empty")]
           pub fail_seams: Vec<String>,   // names of Fail seams
           #[serde(skip_serializing_if = "Vec::is_empty")]
           pub warn_seams: Vec<String>,   // names of Warn seams
           #[serde(skip_serializing_if = "Vec::is_empty")]
           pub next_steps: Vec<String>,   // == Verdict.next_steps
       }
       ```
       Visibility `pub` (not `pub(crate)`) — generators in Plan 03 embed it in their result structs.

    2. `impl Verdict { pub fn summary(&self) -> VerdictSummary { ... } }`: `fail_seams` = seams with status `Fail` mapped to `.seam`; `warn_seams` = seams with status `Warn`; `status` cloned from `self.status`; `next_steps` cloned from `self.next_steps`. Do NOT include `not_checked` seams in either vec (SC-1 signal-to-noise: a wall of not_checked entries is forbidden).

    3. Add `pub(crate) fn read_ambient_status(project_root: &Path, name: &str) -> &'static str`. Construct the path exactly as `write_cache`: `project_root.join(".ferro").join("checkpoints").join(format!("{name}.json"))`. Behavior (D-09/D-11 — read only, NEVER call run_for/recompute):
       - path does not exist → return `"unverified"`
       - read error → `"unverified"`
       - JSON parse error → `"unverified"`
       - `ambient_status` field == `"clean"` → `"clean"`; == `"failing"` → `"failing"`; anything else/absent → `"unverified"`.
       Use `serde_json::from_str::<serde_json::Value>` + `.get("ambient_status").and_then(|v| v.as_str())` (do not require deserializing the full CacheEntry — tolerant read).

    Add two unit tests (pure helpers → plain `#[test]`, no tokio):
    - `verdict_summary_shape`: build a `Verdict` with a Fail seam, a Warn seam, a NotChecked seam; call `.summary()`; serialize to `serde_json::Value`; assert the serialized object has a `status` key and has NO `seams` key; assert `fail_seams`/`warn_seams` contain only the Fail/Warn seam names (not the not_checked one).
    - `ambient_missing_unverified`: `read_ambient_status(tempdir, "nope")` == `"unverified"`.
    - `ambient_read_clean`: write `.ferro/checkpoints/x.json` containing `{"ambient_status":"clean"}` into a tempdir, assert `read_ambient_status(tmp, "x") == "clean"`; repeat with `"failing"` → `"failing"`.
  </action>
  <verify>
    <automated>cargo test -p ferro-mcp checkpoint_projection::tests::verdict_summary_shape checkpoint_projection::tests::ambient_missing_unverified checkpoint_projection::tests::ambient_read_clean</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub struct VerdictSummary" ferro-mcp/src/tools/checkpoint_projection.rs` matches.
    - `grep -n "pub fn summary(&self) -> VerdictSummary" ferro-mcp/src/tools/checkpoint_projection.rs` matches.
    - `grep -n "pub(crate) fn read_ambient_status" ferro-mcp/src/tools/checkpoint_projection.rs` matches.
    - `cargo test -p ferro-mcp checkpoint_projection::tests::verdict_summary_shape` exits 0 (proves no `seams` key in serialized summary — SC-1).
    - `cargo test -p ferro-mcp checkpoint_projection::tests::ambient_missing_unverified checkpoint_projection::tests::ambient_read_clean` exits 0.
  </acceptance_criteria>
  <done>VerdictSummary + Verdict::summary() exist with top-level status and no seams array; read_ambient_status reads cache-only with unverified fallback; all three tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Await the now-async checkpoint handler in service.rs</name>
  <files>ferro-mcp/src/service.rs</files>
  <read_first>
    - ferro-mcp/src/service.rs checkpoint_projection handler (lines 1604-1613; the `pub async fn` handler is already async)
    - 195-PATTERNS.md "`checkpoint_projection` handler (lines 1604–1613)" (lines 466-476)
  </read_first>
  <action>
    The `checkpoint_projection` MCP handler in `service.rs` is already `pub async fn`. After Task 1 made `execute` async, add `.await` to its call:
    `match tools::checkpoint_projection::execute(&self.project_root, &params.0.name) {` →
    `match tools::checkpoint_projection::execute(&self.project_root, &params.0.name).await {`.
    Do not change the handler signature or the surrounding match arms.

    Do NOT touch the `generate_projection`, `json_ui_generate`, `projection_coverage`, or `application_info`
    handlers here — those are owned by Plans 03 and 04 (which also make their `execute` async). This keeps
    service.rs edits in this plan limited to the single checkpoint handler call site.
  </action>
  <verify>
    <automated>cargo test -p ferro-mcp checkpoint_projection</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "checkpoint_projection::execute(&self.project_root, &params.0.name).await" ferro-mcp/src/service.rs` matches.
    - `cargo build -p ferro-mcp` succeeds (the only proof the async handler compiles end-to-end — but note: per CLAUDE.md read-only/thermal policy, prefer `cargo test -p ferro-mcp` which compiles + runs).
    - `cargo test -p ferro-mcp checkpoint_projection` exits 0.
  </acceptance_criteria>
  <done>The checkpoint MCP handler awaits the async execute and the ferro-mcp suite compiles and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → ferro-mcp tool | Agent supplies `name` (projection function name) — already guarded by `validate_name` (Phase 194, T-194-01 path-traversal guard: only `[a-zA-Z0-9_-]`). |
| ferro-mcp → filesystem (cache read) | `read_ambient_status` joins `name` into `.ferro/checkpoints/{name}.json`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-195-01 | Tampering | `read_ambient_status` path join | mitigate | The `name` reaching `read_ambient_status` originates from the trusted projection scan (`list_projections`/`ModelCoverage.projection_name`), not raw user input; the checkpoint write path is already `validate_name`-guarded. Tolerant read (any parse/IO error → `"unverified"`) means a malformed/poisoned cache file cannot crash or mislead — worst case is an honest `"unverified"`. Note in code comment that the key is a derived/scanned name. |
| T-195-02 | Information Disclosure | tolerant JSON read | accept | Reads only the project's own `.ferro/checkpoints/*.json` (a cache the tool itself wrote). No new external surface; read-only, no PII. Low value. |
| T-195-03 | Denial of Service | async run_for / route load (Plan 02 territory) | accept | Read-only introspection over local source; no unbounded recursion introduced here. No cargo/compile invoked. |
</threat_model>

<verification>
- `grep -rn "schema_load\|field_type_compat\|action_binding\|render_target" ferro-mcp/src/ docs/` returns nothing.
- `cargo test -p ferro-mcp checkpoint_projection` passes (Phase-194 regression + new canonical/summary/ambient tests).
- Scoped gate before commit (per CLAUDE.md / thermal policy): `cargo fmt --all -- --check && cargo clippy -p ferro-mcp --all-targets -- -D warnings && cargo test -p ferro-mcp`.
</verification>

<success_criteria>
- `run_for`/`execute` are async; the checkpoint MCP handler awaits and still returns a Verdict.
- All four Phase-194 seam names reconciled to canonical vocabulary in source, tests, and docs; grep gate clean.
- `VerdictSummary` + `Verdict::summary()` carry top-level `status` and never the raw `seams` array (SC-1).
- `read_ambient_status` reads cache-only with `clean`/`failing`/`unverified` and a tolerant `"unverified"` fallback (D-11).
</success_criteria>

<output>
After completion, create `.planning/phases/195-close-the-loop-by-default/195-01-SUMMARY.md`.
</output>
