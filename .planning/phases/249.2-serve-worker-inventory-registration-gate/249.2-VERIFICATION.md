---
phase: 249.2-serve-worker-inventory-registration-gate
verified: 2026-08-15T00:00:00Z
status: passed
score: 4/4
overrides_applied: 0
---

# Phase 249.2: Serve Worker Inventory Registration Gate — Verification Report

**Phase Goal:** Restore OFFLOAD-05's "serve keeps its in-process worker" guarantee for apps that
register jobs exclusively through `#[offload]`. `has_registered_jobs()` must consult both
`JOB_REGISTRARS` and the `JobRegistrarEntry` inventory; the serve boot gate
(`framework/src/app.rs:488`) is the sole spawn caller using this predicate; `run_worker` remains
gated on `registered_queue_names()`. A regression test must lock the fix.

**Verified:** 2026-08-15  
**Status:** passed  
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `has_registered_jobs()` returns `true` when jobs are registered only via `#[offload]` inventory (no manual `Queue::register` call) | VERIFIED | `db.rs:79-85` body contains `\|\| inventory::iter::<JobRegistrarEntry>.into_iter().next().is_some()`; test binary confirms return value `true` from inventory alone |
| 2 | The serve-boot spawn decision (`!no_worker && has_registered_jobs()`) evaluates `true` for an inventory-only app on the default serve path, and `false` under `--no-worker` | VERIFIED | `app.rs:488` is the sole spawn gate; test asserts both `no_worker = false` (spawns) and `no_worker = true` (skips) |
| 3 | `run_worker` remains unaffected — it gates on `registered_queue_names()`, not `has_registered_jobs()` | VERIFIED | `app.rs:533` passes `no_worker = true` to `run_common_boot` (skipping the serve gate); `app.rs:535` and `app.rs:545` use `registered_queue_names()` exclusively; `has_registered_jobs()` does not appear in `run_worker` |
| 4 | A regression test registers a job via inventory only and asserts both the predicate and the gate boolean, guarding against reintroduction | VERIFIED | `ferro-queue/tests/serve_worker_inventory_gate.rs` exists; uses `inventory::submit!` and plain `#[test]`; asserts `has_registered_jobs() == true` and reproduces the exact gate boolean `!no_worker && has` for both `no_worker` states; test run: `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out` |

**Score:** 4/4 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `ferro-queue/src/db.rs` | `has_registered_jobs()` consulting both `JOB_REGISTRARS` and `JobRegistrarEntry` inventory | VERIFIED | Lines 79-85: OR-in of `inventory::iter::<JobRegistrarEntry>.into_iter().next().is_some()` alongside the `JOB_REGISTRARS` guard; docstring updated at lines 75-78 naming both paths |
| `ferro-queue/tests/serve_worker_inventory_gate.rs` | Inventory-only regression test binary | VERIFIED | File exists; contains `inventory::submit!` (line 34), `has_registered_jobs` (lines 51, 58), plain `#[test]` (line 45), no `#[tokio::test]`; passes with 1 test run |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `framework/src/app.rs:488` serve gate | `ferro_queue::Queue::has_registered_jobs()` | boot-time spawn-gate predicate | VERIFIED | `app.rs:488`: `if !no_worker && ferro_queue::Queue::has_registered_jobs()` — literal match |
| `ferro-queue/src/db.rs has_registered_jobs()` | `JobRegistrarEntry` inventory | `inventory::iter` consulted in the predicate | VERIFIED | `db.rs:81-84`: `inventory::iter::<JobRegistrarEntry>.into_iter().next().is_some()` — mirrors `registered_queue_names()` at `db.rs:99` |

---

## Data-Flow Trace (Level 4)

Not applicable. This phase modifies a boot-time boolean predicate and a regression test binary; no dynamic data rendering is involved.

---

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Test binary passes with >0 tests run (false-green guard) | `cargo test -p ferro-queue --test serve_worker_inventory_gate` | `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out` | PASS |
| `framework/src/app.rs` unchanged | `git diff --name-only framework/src/app.rs` | empty output | PASS |

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|---------|
| OFFLOAD-05 | 249.2-01-PLAN.md | Offloaded work runs on a deployable `ferro worker` process at N replicas; the default `serve` path auto-spawns its in-process worker for `#[offload]`-only apps | SATISFIED | `has_registered_jobs()` now consults the `JobRegistrarEntry` inventory; `app.rs:488` spawn gate fires for inventory-only apps; regression test locks the seam. REQUIREMENTS.md note: "Closed by Phase 249.2." |

---

## Anti-Patterns Found

No blockers or warnings. The modified files contain no TODOs, placeholders, stub returns, or hardcoded empty values in the relevant code paths. The `#[allow(clippy::nonminimal_bool)]` annotation at test line 72 is intentional and documented — it preserves the literal gate boolean form from `app.rs:488` for clarity.

---

## Human Verification Required

None. All acceptance criteria are verifiable programmatically and were confirmed above.

---

## Gaps Summary

No gaps. All four observable truths are verified, both required artifacts are substantive and wired, both key links are confirmed in the actual source, the regression test produces `1 passed` (not `0 passed; 0 filtered out`), `app.rs` is confirmed unchanged, and OFFLOAD-05 is satisfied.

---

_Verified: 2026-08-15_  
_Verifier: Claude (gsd-verifier)_
