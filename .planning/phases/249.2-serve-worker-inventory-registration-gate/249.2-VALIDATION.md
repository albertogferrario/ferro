---
phase: 249.2
slug: serve-worker-inventory-registration-gate
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-15
reconstructed_from: [249.2-01-PLAN.md, 249.2-01-SUMMARY.md, 249.2-VERIFICATION.md, 249.2-REVIEW.md]
---

# Phase 249.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Reconstructed retroactively from phase artifacts (State B — no prior VALIDATION.md).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust `cargo test` (integration test binary) |
| **Config file** | none — Cargo workspace; test is a standalone binary under `ferro-queue/tests/` |
| **Quick run command** | `cargo test -p ferro-queue --test serve_worker_inventory_gate` |
| **Full suite command** | `cargo test --all-features` |
| **Estimated runtime** | ~1s targeted (incremental); several minutes full-suite (ENOSPC-prone — see notes) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ferro-queue --test serve_worker_inventory_gate`
- **After every plan wave:** Run `cargo test -p ferro-queue` then `cargo test --all-features`
- **Before `/gsd-verify-work`:** Full suite green (or documented ENOSPC fallback to the changed crate)
- **Max feedback latency:** ~2 seconds (targeted); minutes (full)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 249.2-01-01 | 01 | 1 | OFFLOAD-05 | T-249.2-01 / T-249.2-02 | N/A — internal boot-time predicate, no boundary crossed | unit (predicate) | `cargo test -p ferro-queue --test serve_worker_inventory_gate` + `grep -A3 "pub fn has_registered_jobs" ferro-queue/src/db.rs \| grep inventory::iter` | ✅ | ✅ green |
| 249.2-01-02 | 01 | 1 | OFFLOAD-05 | — | N/A | integration (regression) | `cargo test -p ferro-queue --test serve_worker_inventory_gate` | ✅ | ✅ green |
| 249.2-01-03 | 01 | 1 | OFFLOAD-05 | — | N/A | static/structural | `grep -n "!no_worker && ferro_queue::Queue::has_registered_jobs()" framework/src/app.rs` + `grep -n registered_queue_names framework/src/app.rs` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

### Observable-Truth Coverage

| # | Observable Truth | Covered By | Method |
|---|------------------|-----------|--------|
| 1 | `has_registered_jobs()` returns `true` from `#[offload]` inventory alone (no manual `Queue::register`) | `serve_gate_spawns_worker_for_inventory_only_app` (assert `Queue::has_registered_jobs()`) | runtime assertion |
| 2 | Serve gate `!no_worker && has_registered_jobs()` is `true` on default serve, `false` under `--no-worker` | same test — both `no_worker` states asserted against the literal gate boolean | runtime assertion |
| 3 | `run_worker` unaffected — gates on `registered_queue_names()`, not `has_registered_jobs()` | Task 3 grep of `framework/src/app.rs` | static invariant (see Manual-Only note) |
| 4 | A regression test locks the fix via inventory-only registration | `ferro-queue/tests/serve_worker_inventory_gate.rs` exists and runs `1 passed` | test presence + green run |

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. The regression test binary
`ferro-queue/tests/serve_worker_inventory_gate.rs` was created during execution and runs
under the standard workspace `cargo test`; no framework install or fixture scaffolding was needed.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `run_worker` independence from `has_registered_jobs()` (Truth 3) | OFFLOAD-05 | Negative structural invariant ("X does not depend on Y"). A runtime assertion is awkward; the plan verifies it statically via grep of `framework/src/app.rs`. | Confirm `run_worker` (app.rs ~532-545) derives its queue list from `registered_queue_names()` and never calls `has_registered_jobs()`: `grep -n has_registered_jobs framework/src/app.rs` shows only the serve gate at :488, not `run_worker`. |

*All behavioral truths (1, 2, 4) have automated runtime verification. Truth 3 is a structural
invariant verified by automated grep rather than a runtime test — a legitimate sampling method
for a "does-not-depend-on" claim, recorded here for honesty about the sampling mode.*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (none — no MISSING gaps)
- [x] No watch-mode flags
- [x] Feedback latency < 2s (targeted)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-08-15

---

## Validation Audit 2026-08-15

| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |

Reconstructed from artifacts (State B). Every OFFLOAD-05 observable truth maps to an automated
verification; the regression test `serve_worker_inventory_gate` was re-run during this audit and
returned `test result: ok. 1 passed; 0 failed; 0 filtered out` (not a false-green 0-filtered exit).
No test generation or auditor escalation was required.
