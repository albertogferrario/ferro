---
phase: 22-publishing-announcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - framework/Cargo.toml
  - ferro-storage/Cargo.toml
  - ferro-cache/Cargo.toml
  - ferro-macros/Cargo.toml
  - ferro-mcp/Cargo.toml
  - ferro-notifications/Cargo.toml
autonomous: true
---

<objective>
Complete crates.io metadata and prepare dependency declarations for publishing.

Purpose: Ensure all crates have required and recommended metadata for crates.io, and update dependency declarations to use proper version specs instead of path-only aliases.
Output: All Cargo.toml files ready for crates.io publishing with complete metadata and correct dependency specifications.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-repository-ci/21-01-SUMMARY.md

# Current Cargo.toml files
@framework/Cargo.toml
@ferro-storage/Cargo.toml
@ferro-cache/Cargo.toml
@ferro-macros/Cargo.toml
@ferro-mcp/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete missing Cargo.toml metadata fields</name>
  <files>ferro-storage/Cargo.toml, ferro-cache/Cargo.toml, ferro-macros/Cargo.toml, ferro-mcp/Cargo.toml, ferro-notifications/Cargo.toml</files>
  <action>
Add missing optional metadata fields to crates that lack them:

**ferro-storage/Cargo.toml** - Add:
- repository = "https://github.com/ferroframework/ferro"
- keywords = ["storage", "files", "s3", "ferro", "web"]
- categories = ["web-programming", "filesystem"]
- readme = "README.md"

**ferro-cache/Cargo.toml** - Add:
- repository = "https://github.com/ferroframework/ferro"
- keywords = ["cache", "redis", "memory", "ferro", "web"]
- categories = ["caching", "web-programming"]
- readme = "README.md"

**ferro-macros/Cargo.toml** - Add:
- keywords = ["macros", "proc-macro", "ferro", "web", "derive"]
- categories = ["development-tools::procedural-macro-helpers"]
- readme = "README.md"

**ferro-mcp/Cargo.toml** - Add:
- repository = "https://github.com/ferroframework/ferro"
- keywords = ["mcp", "ai", "introspection", "ferro", "development"]
- categories = ["development-tools", "command-line-utilities"]
- readme = "README.md"

**ferro-notifications/Cargo.toml** - Add:
- readme = "README.md"

Do NOT modify fields that already exist and are correct.
  </action>
  <verify>grep -E "^(repository|keywords|categories|readme)" ferro-storage/Cargo.toml ferro-cache/Cargo.toml ferro-macros/Cargo.toml ferro-mcp/Cargo.toml ferro-notifications/Cargo.toml</verify>
  <done>All five crates have repository, keywords, categories, and readme fields</done>
</task>

<task type="auto">
  <name>Task 2: Update framework dependencies for crates.io publishing</name>
  <files>framework/Cargo.toml</files>
  <action>
Update the dependency declarations in framework/Cargo.toml to use proper crates.io-ready format.

Current format uses alias pattern:
```toml
cancer-macros = { path = "../ferro-macros", package = "ferro-macros", version = "0.1" }
```

Change to standard format ready for crates.io (keeping path for local development but with proper names):
```toml
ferro-macros = { path = "../ferro-macros", version = "0.1" }
```

Update ALL seven ferro-* dependencies:
- cancer-macros → ferro-macros
- cancer-events → ferro-events
- cancer-queue → ferro-queue
- cancer-notifications → ferro-notifications
- cancer-broadcast → ferro-broadcast
- cancer-storage → ferro-storage
- cancer-cache → ferro-cache

Keep inertia-rs unchanged (already correct format).

WHY: The alias pattern was a transitional approach during rebrand. For crates.io, dependencies must use their actual published names.
  </action>
  <verify>grep -E "^(ferro-|inertia)" framework/Cargo.toml | head -10</verify>
  <done>framework/Cargo.toml uses ferro-* dependency names instead of cancer-* aliases</done>
</task>

<task type="auto">
  <name>Task 3: Update framework source to use new dependency names</name>
  <files>framework/src/lib.rs, framework/src/*.rs</files>
  <action>
After changing Cargo.toml dependency names from cancer-* aliases to ferro-* names, update any imports in framework source code.

Search for:
- `use cancer_macros::`
- `use cancer_events::`
- `use cancer_queue::`
- `use cancer_notifications::`
- `use cancer_broadcast::`
- `use cancer_storage::`
- `use cancer_cache::`

Replace with:
- `use ferro_macros::`
- `use ferro_events::`
- `use ferro_queue::`
- `use ferro_notifications::`
- `use ferro_broadcast::`
- `use ferro_storage::`
- `use ferro_cache::`

Also update any `pub use cancer_*` re-exports.

Note: Rust uses underscores in crate names for imports (ferro-macros becomes ferro_macros).
  </action>
  <verify>cargo check -p ferro 2>&1 | head -20</verify>
  <done>Framework compiles with new dependency names, no cancer_* imports remain</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All five crates have complete metadata (repository, keywords, categories, readme)
- [ ] framework/Cargo.toml uses ferro-* names instead of cancer-* aliases
- [ ] `cargo check -p ferro` succeeds
- [ ] `grep -r "cancer_" framework/src/` returns no results
</verification>

<success_criteria>

- All crates have complete crates.io metadata
- No more cancer-* alias pattern in framework dependencies
- Framework compiles successfully with new import names
- Code is ready for crates.io publishing (path deps work locally, will be removed for actual publish)
</success_criteria>

<output>
After completion, create `.planning/phases/22-publishing-announcement/22-01-SUMMARY.md`
</output>
