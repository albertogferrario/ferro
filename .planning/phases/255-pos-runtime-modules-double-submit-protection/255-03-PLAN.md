---
phase: 255-pos-runtime-modules-double-submit-protection
plan: 03
type: execute
wave: 3
depends_on: [255-01, 255-02]
files_modified:
  - ferro-mcp/src/tools/json_ui_catalog.rs
  - app/src/views/cassa.json
  - docs/src/json-ui/components.md
  - docs/src/design-system/patterns.md
autonomous: true
requirements: [SC-0]
must_haves:
  truths:
    - "The SC-0 grep gate returns ZERO hits across ferro-json-ui/src, ferro-mcp/src, app/src, docs/src"
    - "The ferro-mcp catalog mirror lists Tile and register-* rule ids; component count stays 47"
    - "The /cassa sample spec references component type Tile with item_id (disable_on_submit added later, in Plan 255-05)"
    - "docs document the tile-builtin rename in a consumer migration note without re-introducing retired grep tokens"
  artifacts:
    - path: "ferro-mcp/src/tools/json_ui_catalog.rs"
      provides: "RULE_COMPONENTS register-* keys; expected component list contains Tile; count 47"
      contains: "register-fill-viewport"
    - path: "app/src/views/cassa.json"
      provides: "Tile component type with item_id prop"
      contains: "Tile"
    - path: "docs/src/json-ui/components.md"
      provides: "Tile section + v16.6 rename migration note"
      contains: "Tile"
  key_links:
    - from: "ferro-mcp/src/tools/json_ui_catalog.rs test_all_components_present"
      to: "ferro-json-ui catalog output"
      via: "expected list matches BUILTIN_TYPES names (Tile)"
      pattern: "\"Tile\""
---

<objective>
Reflect the ferro-json-ui vocabulary rename into its downstream mirrors — the
ferro-mcp catalog, the /cassa sample spec, and the docs — closing SC-0 to a global
zero-hits state. This plan runs after both ferro-json-ui rename plans because the
ferro-mcp catalog test compares against the renamed catalog output (Tile) and the
renamed rule ids (register-*).

Purpose: Complete SC-0 (vocabulary neutralization) so Part B (runtime modules,
Plans 255-04/05) is written entirely against the final vocabulary.

Output: ferro-mcp RULE_COMPONENTS + expected-component list updated (count stays 47),
cassa.json type/field renamed (Tile / item_id), docs components.md Tile section + a
v16.6 rename migration note, docs patterns.md register-* rule sections. Only the TYPE
and FIELD rename lands in cassa.json here; disable_on_submit:true is added in Plan 255-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/255-pos-runtime-modules-double-submit-protection/255-CONTEXT.md
@.planning/phases/255-pos-runtime-modules-double-submit-protection/255-RESEARCH.md
@.planning/phases/255-pos-runtime-modules-double-submit-protection/255-PATTERNS.md

Locked decisions implemented here: V-06 (SC-0 grep gate closure), V-07 (docs ship
in-phase + consumer migration note). Depends on 255-01 (catalog names) and 255-02
(rule ids). Note (255-RESEARCH.md Schema Export section): docs/protocol/schemas/*.json
contain ferro-projections protocol schemas, NOT component props — this rename does NOT
dirty them; do not expect or commit schema-file churn.

TENSION RESOLVED (see 255-03-SUMMARY guidance): SC-0 demands zero `ProductTile` /
`data-product-` hits in docs/src, while V-07 demands a migration note documenting the
break. Resolution: the migration note documents the rename descriptively (new names +
"formerly the product-prefixed ..." wording) WITHOUT re-spelling the retired compound
tokens, so both criteria hold literally.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ferro-mcp json_ui_catalog mirror (rule ids + Tile name + count message)</name>
  <files>ferro-mcp/src/tools/json_ui_catalog.rs</files>
  <read_first>
    - ferro-mcp/src/tools/json_ui_catalog.rs (RULE_COMPONENTS entries at lines 99-101; count assertion + message at 402-407; test_all_components_present expected array with "ProductTile" at line 450)
    - 255-RESEARCH.md section "ferro-mcp/src/tools/json_ui_catalog.rs [VERIFIED]"
  </read_first>
  <action>
ferro-mcp/src/tools/json_ui_catalog.rs:
  - RULE_COMPONENTS entries (99-101): `("pos-fill-viewport", &["Grid"])` -> `("register-fill-viewport", &["Grid"])`;
    `("pos-grid-fill", &["Grid"])` -> `("register-grid-fill", &["Grid"])`;
    `("pos-cart-present", &["Grid"])` -> `("register-selection-present", &["Grid"])`.
    (The `&["Grid"]` association is the 254 D-14 placeholder — do NOT change it; Phase 256
    extends it to TileGrid/SelectionPanel.)
  - `test_all_components_present` expected array (line 450): `"ProductTile"` -> `"Tile"`.
  - Count assertion (402-407): keep the number 47; update only the message text if it names
    "ProductTile". Do NOT change the count.

Compile+test unit for ferro-mcp only. Run `cargo test -p ferro-mcp --all-features` and confirm
`test_all_components_present` passes (it validates against the renamed ferro-json-ui catalog
output from Plan 255-01). Serialize cargo — no parallel runs.
  </action>
  <verify>
    <automated>test -z "$(grep -n 'pos-fill-viewport\|pos-grid-fill\|pos-cart-present\|ProductTile\|CartPanel\|CategoryNav\|ProductGrid' ferro-mcp/src/tools/json_ui_catalog.rs)" && cargo test -p ferro-mcp --all-features -- test_all_components_present</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'register-fill-viewport\|register-grid-fill\|register-selection-present' ferro-mcp/src/tools/json_ui_catalog.rs` is >= 3
    - `grep '"Tile"' ferro-mcp/src/tools/json_ui_catalog.rs` matches
    - `grep 'pos-fill-viewport\|pos-grid-fill\|pos-cart-present\|ProductTile\|CartPanel\|CategoryNav\|ProductGrid' ferro-mcp/src/tools/json_ui_catalog.rs` returns nothing
    - the count assertion still uses 47 (unchanged)
    - `cargo test -p ferro-mcp --all-features -- test_all_components_present` exits 0
  </acceptance_criteria>
  <done>ferro-mcp mirror lists Tile and register-* rule ids, count stays 47, test_all_components_present passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rename /cassa sample spec component type and field</name>
  <files>app/src/views/cassa.json</files>
  <read_first>
    - app/src/views/cassa.json (the "tile" element at lines 70-79: `"type": "ProductTile"` at 71, `"product_id"` at 74; the btn_confirm element at 57-62)
    - 255-RESEARCH.md section "app/src/views/cassa.json [VERIFIED]" and 255-PATTERNS.md section cassa.json
  </read_first>
  <action>
app/src/views/cassa.json — the tile element (lines ~70-79):
  - `"type": "ProductTile"` -> `"type": "Tile"`
  - `"product_id": { "$data": "/p/id" }` -> `"item_id": { "$data": "/p/id" }`
  - keep `name`, `price`, `field` bindings unchanged.
Do NOT add `disable_on_submit` to btn_confirm in this plan — that is Plan 255-05 (SC-4), which
depends on the ButtonProps.disable_on_submit field that does not exist yet. Leave btn_confirm as-is.

If the app has a catalog-validation test that checks cassa.json against BUILTIN_TYPES, run it:
`cargo test -p app --all-features` (or the app's spec-validation test). The renamed type "Tile"
must validate against the renamed catalog from Plan 255-01. Serialize cargo — no parallel runs.
  </action>
  <verify>
    <automated>test -z "$(grep -n 'ProductTile\|product_id' app/src/views/cassa.json)" && grep -q '"type": "Tile"' app/src/views/cassa.json && grep -q '"item_id"' app/src/views/cassa.json && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"type": "Tile"' app/src/views/cassa.json` matches
    - `grep '"item_id"' app/src/views/cassa.json` matches
    - `grep 'ProductTile\|product_id' app/src/views/cassa.json` returns nothing
    - `grep 'disable_on_submit' app/src/views/cassa.json` returns nothing (deferred to 255-05)
  </acceptance_criteria>
  <done>cassa.json tile element uses type Tile with item_id; no disable_on_submit yet; spec validates against the renamed catalog.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs (components.md Tile section + rename migration note, patterns.md register-* sections); close global SC-0 gate</name>
  <files>docs/src/json-ui/components.md, docs/src/design-system/patterns.md</files>
  <read_first>
    - docs/src/json-ui/components.md (component index row line 34; existing Phase 251 migration table at lines 72-93; ProductTile section at lines 1400-1424)
    - docs/src/design-system/patterns.md (three POS rule sections at lines 522-654)
    - 255-RESEARCH.md sections "docs/src/json-ui/components.md [VERIFIED]" and "docs/src/design-system/patterns.md [VERIFIED]"; 255-PATTERNS.md sections for both docs files
  </read_first>
  <action>
docs/src/json-ui/components.md:
  - component index row (line 34): `| **Commerce** | ProductTile |` -> `| **Commerce** | Tile |`
  - section heading `### ProductTile` (line 1400) -> `### Tile`; update the description to neutral
    touch-tile language; props table `product_id` -> `item_id`; example JSON `"type": "ProductTile"`
    / `"product_id"` -> `"type": "Tile"` / `"item_id"`; prose "Place ProductTile elements..." ->
    "Place Tile elements...".
  - Add a v16.6 rename migration note near the existing Phase 251 migration table (line ~72).
    CRITICAL: the SC-0 grep gate scans docs/src and forbids the literal substrings `ProductTile`
    and `data-product-` ANYWHERE in docs/src. Document the break DESCRIPTIVELY — do NOT re-spell
    the retired compound identifiers. Use this shape (adjust columns to the file's table style):
      | Renamed in v16.6 | New surface | Migration action |
      |------------------|-------------|------------------|
      | Commerce tile builtin (formerly the product-prefixed type string) | `Tile` | Change `"type"` in every spec to `"Tile"` |
      | Tile primary-id prop (formerly the product-prefixed prop name) | `item_id` | Rename the prop in every Tile element |
      | Tile category data attribute (formerly the product-prefixed data attribute) | `data-filter-tokens` | Update any custom runtime that reads the old attribute |
    A consumer reading this learns exactly what changed and what to do, while docs stay grep-clean.
    Do NOT write the literal `ProductTile` or `data-product-categories` anywhere.

docs/src/design-system/patterns.md — update the three rule sections (522-654):
  - `## \`pos-fill-viewport\`` -> `## \`register-fill-viewport\``
  - `## \`pos-grid-fill\`` -> `## \`register-grid-fill\``
  - `## \`pos-cart-present\`` -> `## \`register-selection-present\``
  - all prose + fixture JSON: `ProductGrid` -> `TileGrid`, `CartPanel` -> `SelectionPanel`
  - all allow-list strings: `"pos-fill-viewport"` -> `"register-fill-viewport"`, `"pos-grid-fill"`
    -> `"register-grid-fill"`, `"pos-cart-present"` -> `"register-selection-present"`
  - titles/rationale: "A ProductGrid register needs a CartPanel" -> "A TileGrid register needs a
    SelectionPanel", etc.

Then run the FINAL global SC-0 grep gate (this plan closes SC-0):
`grep -rn 'ProductTile\|product_tile\|setupProductTiles\|data-product-\|CartPanel\|CategoryNav\|ProductGrid' ferro-json-ui/src ferro-mcp/src app/src docs/src`
It MUST return zero hits. Also run `cargo doc --no-deps` clean (the renamed rustdoc must build)
and, if mdbook is available, `cd docs && mdbook build`.
  </action>
  <verify>
    <automated>test -z "$(grep -rn 'ProductTile\|product_tile\|setupProductTiles\|data-product-\|CartPanel\|CategoryNav\|ProductGrid' ferro-json-ui/src ferro-mcp/src app/src docs/src)" && echo SC0-ZERO-HITS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '### Tile' docs/src/json-ui/components.md` is >= 1
    - `grep 'ProductTile\|data-product-' docs/src/json-ui/components.md` returns nothing (the migration note describes the break without the retired substrings)
    - `grep -c 'register-fill-viewport\|register-grid-fill\|register-selection-present' docs/src/design-system/patterns.md` is >= 3
    - `grep 'pos-fill-viewport\|pos-grid-fill\|pos-cart-present\|ProductGrid\|CartPanel' docs/src/design-system/patterns.md` returns nothing
    - GLOBAL: `grep -rn 'ProductTile\|product_tile\|setupProductTiles\|data-product-\|CartPanel\|CategoryNav\|ProductGrid' ferro-json-ui/src ferro-mcp/src app/src docs/src` returns ZERO hits
    - `cargo doc --no-deps` exits 0
  </acceptance_criteria>
  <done>Docs renamed with a grep-clean v16.6 rename migration note; the global SC-0 grep gate returns zero hits; cargo doc builds clean. SC-0 is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ferro-mcp catalog -> agent | The catalog output feeds agent introspection; a stale name would misdirect an agent but is not a security boundary. |
| /cassa spec -> render | cassa.json is authored server-side (trusted); the rename does not change data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-255-05 | Spoofing / Info integrity | ferro-mcp catalog mirror name drift | mitigate | `test_all_components_present` asserts the mirror's expected list equals the live catalog names; a partial rename fails the test. Count assertion (47) guards accidental add/remove. |
| T-255-06 | Repudiation | docs migration note | accept | The note documents the published tile-builtin rename break for consumers (gestiscilo) descriptively; no live surface, no executable path. |
</threat_model>

<verification>
- GLOBAL SC-0: `grep -rn 'ProductTile\|product_tile\|setupProductTiles\|data-product-\|CartPanel\|CategoryNav\|ProductGrid' ferro-json-ui/src ferro-mcp/src app/src docs/src` returns ZERO hits. The migration note is written descriptively (no retired compound substrings), so zero-hits and "documents the break" both hold literally — no exception needed.
- `cargo test -p ferro-mcp --all-features` passes (Task 1).
- `cargo doc --no-deps` clean.
</verification>

<success_criteria>
- ferro-mcp mirror updated (Tile + register-* ids, count 47), test green.
- cassa.json uses Tile/item_id (no disable_on_submit).
- Docs: Tile section + grep-clean v16.6 rename migration note + register-* patterns sections.
- Global SC-0 grep gate returns zero hits.
</success_criteria>

<output>
After completion, create `.planning/phases/255-pos-runtime-modules-double-submit-protection/255-03-SUMMARY.md`.
</output>
