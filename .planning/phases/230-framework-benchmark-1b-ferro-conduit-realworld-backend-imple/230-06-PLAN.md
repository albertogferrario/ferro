---
phase: 230-framework-benchmark-1b-ferro-conduit
plan: 06
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - benchmark/apps/ferro-conduit/src/controllers/comments.rs
  - benchmark/apps/ferro-conduit/src/controllers/tags.rs
  - benchmark/apps/ferro-conduit/src/controllers/articles.rs
  - benchmark/apps/ferro-conduit/src/controllers/mod.rs
  - benchmark/apps/ferro-conduit/src/routes.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Comments add/list/delete work: POST/GET /api/articles/{slug}/comments and DELETE /api/articles/{slug}/comments/{id}, with author authorization on delete"
    - "Favorite/unfavorite work: POST/DELETE /api/articles/{slug}/favorite update the favorites junction and the article's favoritesCount/favorited flag"
    - "GET /api/tags returns the tag list; GET /api/articles/feed returns articles from followed authors (paginated), replacing the Plan 04 placeholder"
    - "All remaining Newman folders (Comments, Favorites, Tags, Feed) pass against the running Ferro Conduit"
  artifacts:
    - path: "benchmark/apps/ferro-conduit/src/controllers/comments.rs"
      provides: "index, store, destroy comment handlers"
      contains: "comment::"
    - path: "benchmark/apps/ferro-conduit/src/controllers/tags.rs"
      provides: "tags index handler"
      contains: "tag::"
  key_links:
    - from: "src/controllers/articles.rs (feed)"
      to: "follows + articles tables"
      via: "feed = articles where author_id in (followed by viewer)"
      pattern: "feed"
    - from: "src/controllers/articles.rs (favorite)"
      to: "favorites table"
      via: "insert/delete favorite then recompute favoritesCount"
      pattern: "favorite::"
---

<objective>
Complete the Conduit surface: comments (add/list/delete), favorites (favorite/unfavorite + favoritesCount), tags (list), and the real article feed (followed authors, paginated) — replacing Plan 04's `feed_placeholder`. Acceptance = the remaining Newman folders (Comments, Favorites, Tags, Feed) pass. After this, the full single-app collection is green against the Ferro Conduit.

Purpose: Closes the last endpoint groups. Favorites/feed reuse the follows/favorites junctions from Plans 02/05; tags reuse article_tags. All framework-provided.

Output: Remaining Newman folders green; the Ferro Conduit is feature-complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/230-framework-benchmark-1b-ferro-conduit-realworld-backend-imple/230-RESEARCH.md
@.planning/phases/230-framework-benchmark-1b-ferro-conduit-realworld-backend-imple/230-04-SUMMARY.md
@.planning/phases/230-framework-benchmark-1b-ferro-conduit-realworld-backend-imple/230-05-SUMMARY.md

<interfaces>
Entities (Plan 02): comment (body, article_id, author_id, created_at, updated_at), favorite (user_id, article_id), tag (id, name), article_tag, follow, article.
DTOs (Plan 03): `CommentDto{id,created_at,updated_at,body,author:ProfileDto}`, `CreateCommentEnvelope`, `ArticleDto`.
Helpers (Plan 04/05): `to_article_dto(db, article, viewer)`, `to_profile_dto(db, user, viewer)` — reuse, do not duplicate.
Middleware (Plan 01): JwtAuthMiddleware (comment add/delete, favorite, feed), OptionalJwtMiddleware (comment list), no auth on tags.
Feed pattern (RESEARCH endpoint list line 64): articles whose author_id is in (followed_id from follows where follower_id = viewer), order_by_desc(created_at).limit().offset() + articlesCount.
Plan 04 left `feed_placeholder` in articles.rs registered at `/api/articles/feed` — REPLACE it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comments (add/list/delete) + Tags index + wire routes</name>
  <files>benchmark/apps/ferro-conduit/src/controllers/comments.rs, benchmark/apps/ferro-conduit/src/controllers/tags.rs, benchmark/apps/ferro-conduit/src/controllers/mod.rs, benchmark/apps/ferro-conduit/src/routes.rs</files>
  <read_first>
    - RESEARCH.md "JSON response shapes / Comment(s)" (lines 93-98) + "Create comment" (line 128)
    - RESEARCH.md "Tags" envelope (line 101) — {"tags":[...]}
    - RESEARCH.md endpoint list comments/tags rows (lines 69-71, 74)
    - benchmark/apps/ferro-conduit/src/controllers/articles.rs (Plan 04) — reuse to_profile_dto for comment.author
    - benchmark/apps/ferro-conduit/src/routes.rs (Plan 04/05 final groups) — append comment/tags routes, do not recreate groups
  </read_first>
  <action>
    `controllers/comments.rs`:
    - `store(req)` (required auth): uid from req.get::<UserId>(). Resolve article by `req.param("slug")?` (404 if missing). Parse CreateCommentEnvelope; validate body non-empty (422 envelope). Insert comment (article_id, author_id=uid). Return `json_response!({"comment": CommentDto{ id, created_at (to_rfc3339), updated_at, body, author: to_profile_dto(db, author_user, Some(uid)) }})`.
    - `index(req)` (optional auth): resolve article by slug; load its comments ordered by created_at asc; map each to CommentDto (author.following relative to viewer). Return `{"comments":[...]}`.
    - `destroy(req)` (required auth): `id = req.param_as::<i64>("id")?`; load comment (404 if missing); assert `comment.author_id == uid` (403 otherwise); delete; return 200 (empty body or `{}` per collection).

    `controllers/tags.rs`:
    - `index()` (no auth): load tag names (all rows from tags). Return `{"tags": names}`.

    `controllers/mod.rs`: add `pub mod comments; pub mod tags;`.

    `routes.rs` — append to EXISTING groups (Plan 04/05 own the structure):
    - public: `post!`/`get!` not needed for tags beyond `get!("/api/tags", controllers::tags::index)`.
    - optional-auth group: `get!("/articles/{slug}/comments", controllers::comments::index)`.
    - required-auth group: `post!("/articles/{slug}/comments", controllers::comments::store)`, `delete!("/articles/{slug}/comments/{id}", controllers::comments::destroy)`.
  </action>
  <verify>
    <automated>cd benchmark/apps/ferro-conduit && cargo build --release 2>&1 | tail -3</automated>
  </verify>
  <done>Comments add/list/delete + tags index compile; delete enforces author ownership; routes appended to the correct groups; comment.author reuses to_profile_dto.</done>
</task>

<task type="auto">
  <name>Task 2: Favorite/unfavorite + real feed (replace placeholder) + wire routes; full single-app Newman green</name>
  <files>benchmark/apps/ferro-conduit/src/controllers/articles.rs, benchmark/apps/ferro-conduit/src/routes.rs</files>
  <read_first>
    - RESEARCH.md endpoint list favorite/unfavorite + feed (lines 64, 72-73)
    - RESEARCH.md "Single article" envelope — favorited + favoritesCount must update after favorite/unfavorite
    - benchmark/apps/ferro-conduit/src/controllers/articles.rs (Plan 04: to_article_dto + feed_placeholder to replace + index() to mirror)
    - RESEARCH.md Pitfall 2 — keep /articles/feed declared before /articles/{slug}
  </read_first>
  <action>
    In `articles.rs`:
    - `favorite(req)` (required auth): uid from req.get; resolve article by slug (404 if missing); insert favorite (user_id=uid, article_id) idempotently (composite PK no-op on dup, or check-then-insert); return `{"article": to_article_dto(db, article, Some(uid))}` (favorited true, favoritesCount incremented).
    - `unfavorite(req)` (required auth): delete the favorite row if present; return updated article envelope (favorited false).
    - `feed(req)` (required auth): REPLACE `feed_placeholder`. uid from req.get. followed_ids = `follow::Entity::find().filter(follow::Column::FollowerId.eq(uid)).all(db)` mapped to followed_id. Query `article::Entity::find().filter(article::Column::AuthorId.is_in(followed_ids))`; articlesCount = count before pagination; then order_by_desc(CreatedAt).limit(req.query_as_or("limit",20u64)).offset(req.query_as_or("offset",0u64)). Map each via to_article_dto(viewer=Some(uid)). Return `{"articles":[...], "articlesCount":N}`. Delete the `feed_placeholder` fn.

    `routes.rs`:
    - Repoint the required-auth `/api/articles/feed` route from `feed_placeholder` to `controllers::articles::feed` (keep it declared BEFORE the `{slug}` routes).
    - Add required-auth: `post!("/articles/{slug}/favorite", controllers::articles::favorite)`, `delete!("/articles/{slug}/favorite", controllers::articles::unfavorite)`.
  </action>
  <verify>
    <automated>cd benchmark/apps/ferro-conduit && cargo build --release 2>&1 | tail -3 && cargo test --release route_ordering 2>&1 | tail -5 && cargo clippy --release -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    Build + clippy + route_ordering green. With Postgres up + app served, each remaining Newman folder passes: `bash benchmark/contracts/conduit/run_newman.sh http://localhost:3000/api Comments`, `... Favorites`, `... Tags`, `... Feed` all exit 0; and the FULL collection (no --folder) exits 0 against the Ferro Conduit. Executor pastes the full-collection pass summary into SUMMARY. If a deferred-to-Plan-07-compose path was chosen in earlier plans, this plan MUST still run the full single-app Newman live (Postgres + serve) and record the result — the single-app green gate cannot itself be deferred past this plan.
  </acceptance_criteria>
  <done>favorite/unfavorite + real feed implemented; feed_placeholder removed; routes repointed/added with feed before {slug}. Full single-app RealWorld Newman collection green against the Ferro Conduit (recorded in SUMMARY).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → comment delete | Author-ownership enforced from the verified UserId |
| client → favorite/feed | user_id/follower scope taken only from the verified token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-230-17 | EoP | delete another user's comment | mitigate | destroy asserts comment.author_id == UserId; 403 otherwise |
| T-230-18 | Tampering | duplicate favorite rows | mitigate | Composite PK (user_id, article_id) makes repeat favorite a no-op; favoritesCount recomputed from the table |
| T-230-19 | Info Disclosure | feed leaking non-followed authors' drafts | accept | Conduit has no draft state; all articles are public; feed is just a follow-filtered view — no confidentiality boundary |
</threat_model>

<verification>
- `cargo build --release` + clippy + route_ordering green.
- Newman Comments/Favorites/Tags/Feed folders green AND full single-app collection green against the Ferro Conduit (live, recorded in SUMMARY).
</verification>

<success_criteria>
- Comments, favorites, tags, feed all conform.
- feed_placeholder replaced by the real feed; ordering preserved.
- Full single-app RealWorld Newman collection green against the Ferro Conduit.
</success_criteria>

<output>
After completion, create `.planning/phases/230-framework-benchmark-1b-ferro-conduit-realworld-backend-imple/230-06-SUMMARY.md`. Record the full single-app Newman pass summary (folder-by-folder counts). This is the gate that the Ferro Conduit is feature-complete before the Laravel + harness wave.

HEAVY/THERMAL: `cargo build --release` + a full live Newman run (Postgres + serve). Sequential only; never concurrent with other cargo/Docker work.
</output>
