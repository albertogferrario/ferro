---
phase: 230-framework-benchmark-1b-ferro-conduit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - benchmark/apps/ferro-conduit/Cargo.toml
  - benchmark/apps/ferro-conduit/.env.example
  - benchmark/apps/ferro-conduit/Dockerfile
  - benchmark/apps/ferro-conduit/src/main.rs
  - benchmark/apps/ferro-conduit/src/bootstrap.rs
  - benchmark/apps/ferro-conduit/src/config/mod.rs
  - benchmark/apps/ferro-conduit/src/routes.rs
  - benchmark/apps/ferro-conduit/src/controllers/mod.rs
  - benchmark/apps/ferro-conduit/src/controllers/health.rs
  - benchmark/apps/ferro-conduit/src/jwt.rs
  - benchmark/apps/ferro-conduit/src/middleware/mod.rs
  - benchmark/apps/ferro-conduit/src/middleware/jwt_auth.rs
  - benchmark/apps/ferro-conduit/src/middleware/optional_jwt.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "The ferro-conduit binary compiles in release and is OUTSIDE the root ferro workspace (its own [package], no [workspace] inheritance)"
    - "A JWT minted by mint_token round-trips through decode_token; an expired token and a bad-signature token are both rejected"
    - "JwtAuthMiddleware rejects a request with no/invalid Authorization: Token header with 401; OptionalJwtMiddleware never rejects"
    - "The hand-rolled JWT code is isolated in src/jwt.rs so the later static-compression report can count it separately"
  artifacts:
    - path: "benchmark/apps/ferro-conduit/Cargo.toml"
      provides: "Standalone [[bin]] package, NOT a workspace member"
      contains: "[[bin]]"
    - path: "benchmark/apps/ferro-conduit/src/jwt.rs"
      provides: "JwtClaims + mint_token + decode_token (hand-rolled, jsonwebtoken)"
      contains: "mint_token"
    - path: "benchmark/apps/ferro-conduit/src/middleware/jwt_auth.rs"
      provides: "JwtAuthMiddleware (401 on missing/invalid) + UserId extension type"
      contains: "impl Middleware"
    - path: "benchmark/apps/ferro-conduit/src/middleware/optional_jwt.rs"
      provides: "OptionalJwtMiddleware (inserts UserId if present, never rejects)"
      contains: "OptionalJwtMiddleware"
    - path: "benchmark/apps/ferro-conduit/Dockerfile"
      provides: "Multi-stage build serving the ferro-conduit binary"
      contains: "cargo build --release"
  key_links:
    - from: "src/middleware/jwt_auth.rs"
      to: "src/jwt.rs"
      via: "decode_token call in Middleware::handle"
      pattern: "decode_token"
    - from: "src/jwt.rs"
      to: "jsonwebtoken"
      via: "encode/decode with HS256 and a benchmark-only secret"
      pattern: "jsonwebtoken"
---

<objective>
Scaffold the standalone `benchmark/apps/ferro-conduit` Ferro app (mirroring the proven `ferro-micro` structure: own Cargo.toml + `[[bin]]`, clap subcommands, `DB::init` in bootstrap, Dockerfile, Postgres wiring) and hand-roll the ONE capability Ferro does not provide: JWT auth (`Authorization: Token <jwt>`). This is the foundation every later wave builds on.

Purpose: Conduit requires JWT auth; Ferro's auth is session-based only (RESEARCH §1, `framework/src/auth/*`). The JWT code is the only non-framework-provided piece and is deliberately isolated in `src/jwt.rs` so the static-compression report (Plan 07) can count it separately per D-10 honesty.

Output: A release-compilable, Docker-buildable skeleton with a health endpoint, the JWT module (unit-tested), and the two JWT middlewares (unit-tested).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/230-framework-benchmark-1b-ferro-conduit-realworld-backend-imple/230-RESEARCH.md
@.planning/phases/230-framework-benchmark-1b-ferro-conduit-realworld-backend-imple/230-VALIDATION.md

<interfaces>
<!-- Framework symbols this plan uses. All verified from framework/src/lib.rs in research. -->
Middleware trait (framework/src/middleware/mod.rs:104):
```rust
#[async_trait]
pub trait Middleware: Send + Sync {
    async fn handle(&self, request: Request, next: Next) -> Response;
}
pub type Next = Arc<dyn Fn(Request) -> MiddlewareFuture + Send + Sync>;
```
Request extension map + header (framework/src/http/request.rs):
```rust
pub fn insert<T: Send + Sync + 'static>(&mut self, value: T);     // line 87
pub fn get<T: Send + Sync + 'static>(&self) -> Option<&T>;        // line 92
pub fn header(&self, name: &str) -> Option<&str>;                 // line 309
```
Re-exports (framework/src/lib.rs): `ferro::{async_trait, Middleware, Next, Request, Response, HttpResponse, DB, Config, Server, serde_json, handler}`; `json_response!` macro.
Reference middleware (the proven extension-map pattern): `app/src/middleware/bearer_auth.rs` — `request.insert::<T>(...)` then `next(request).await`; reject path returns `Err(HttpResponse::new().status(401))`.
ferro-micro structural template: `benchmark/apps/ferro-micro/{Cargo.toml, Dockerfile, src/main.rs, src/bootstrap.rs, src/config/mod.rs, src/routes.rs}`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the standalone ferro-conduit app (Cargo.toml, main.rs, bootstrap, config, Dockerfile, health route)</name>
  <files>benchmark/apps/ferro-conduit/Cargo.toml, benchmark/apps/ferro-conduit/.env.example, benchmark/apps/ferro-conduit/Dockerfile, benchmark/apps/ferro-conduit/src/main.rs, benchmark/apps/ferro-conduit/src/bootstrap.rs, benchmark/apps/ferro-conduit/src/config/mod.rs, benchmark/apps/ferro-conduit/src/routes.rs, benchmark/apps/ferro-conduit/src/controllers/mod.rs, benchmark/apps/ferro-conduit/src/controllers/health.rs</files>
  <read_first>
    - benchmark/apps/ferro-micro/Cargo.toml (the dependency set + standalone [[bin]] — copy verbatim, add jsonwebtoken@9 + bcrypt via ferro::hashing + slug@0.1 later)
    - benchmark/apps/ferro-micro/src/main.rs (clap Cli with Serve/db:migrate/db:status/db:rollback/db:fresh/db:seed subcommands + Migrator::up pattern)
    - benchmark/apps/ferro-micro/src/bootstrap.rs (tracing init + DB::init)
    - benchmark/apps/ferro-micro/src/config/mod.rs (Config::register(DatabaseConfig::from_env()))
    - benchmark/apps/ferro-micro/Dockerfile (rust:1.88.0-slim multi-stage; binary name differs)
    - benchmark/apps/ferro-micro/src/routes.rs (routes! + get! macro usage)
    - RESEARCH.md "Standard Stack for ferro-conduit" table (line 320) for the exact crate versions
    - RESEARCH.md "must not be in the root ferro workspace" (line 344)
  </read_first>
  <action>
    Copy the ferro-micro skeleton, renamed to `ferro-conduit`. Concrete steps:

    1. `Cargo.toml`: package name `ferro-conduit`, version `0.1.0`, edition 2021, a `[[bin]]` named `ferro-conduit` path `src/main.rs`. Dependencies = ferro-micro's set PLUS `jsonwebtoken = "9"` and `slug = "0.1"`. Keep `ferro = { package = "ferro-rs", version = "0.2" }`, sea-orm/sea-orm-migration 1.0 with `sqlx-postgres` + `runtime-tokio-rustls` + `macros`, serde/serde_json, async-trait, dotenvy, clap derive, tracing, tracing-subscriber, rand. Add a `[dev-dependencies]` with nothing required yet (unit tests use std). CRITICAL: do NOT add a `[workspace]` table and do NOT place this under the root `ferro` workspace — it must be its own package (ferro-micro is already excluded; place ferro-conduit as a sibling so the root workspace does not absorb it). Unconditionally add `benchmark/apps/ferro-conduit` (and `benchmark/apps/ferro-micro`) to a `[workspace] exclude = [...]` array in the root `Cargo.toml` (create the `exclude` key if absent) as a defensive guard against future glob members.
    2. `src/main.rs`: copy ferro-micro main.rs verbatim, change `#[command(name = "ferro-micro")]` to `ferro-conduit`, keep all clap subcommands and the `Migrator` wiring (Migrator comes from `mod migrations` — for now declare `mod migrations;` referencing a stub that Plan 02 fills; to keep this plan compilable, include an empty `migrations/mod.rs` exporting an empty `Migrator` with no migrations). Declare modules: `mod bootstrap; mod config; mod controllers; mod jwt; mod middleware; mod migrations; mod routes;` (models/dto added in later waves).
    3. `src/bootstrap.rs`: copy verbatim from ferro-micro (tracing init + `DB::init()`). Additionally register `Cors` globally (RESEARCH Pitfall 6): add `ferro::register_global_middleware(...)` or include `Cors` in bootstrap per the framework's global-middleware API — use `ferro::Cors::permissive()` if available, otherwise default `Cors` (verify the constructor with `grep -n "impl Cors" framework/src/middleware/cors.rs`).
    4. `src/config/mod.rs`: copy verbatim from ferro-micro.
    5. `src/controllers/mod.rs`: `pub mod health;` (more modules added later).
    6. `src/controllers/health.rs`: one `#[handler] pub async fn show() -> Response` returning `Ok(HttpResponse::json(json!({"status":"ok"})))`. This gives Plan 06 a liveness probe and keeps the app runnable before endpoints land.
    7. `src/routes.rs`: `routes! { get!("/health", controllers::health::show) }` for now; later waves append.
    8. `src/migrations/mod.rs`: minimal `pub struct Migrator;` impl `MigratorTrait` with `fn migrations() -> Vec<Box<dyn MigrationTrait>> { vec![] }` (Plan 02 replaces the vec).
    9. `.env.example`: `DATABASE_URL=postgres://postgres:bench@localhost:5432/bench`, `SERVER_HOST=0.0.0.0`, `SERVER_PORT=3000`, `JWT_SECRET=benchmark-only-not-production-grade`, `APP_NAME=conduit`, `APP_URL=http://localhost:3000`.
    10. `Dockerfile`: copy ferro-micro's, change the binary path to `/app/target/release/ferro-conduit`. Keep `rust:1.88.0-slim-bookworm` build stage and `debian:bookworm-slim` runtime. CMD `["/usr/local/bin/app", "serve"]`.
  </action>
  <verify>
    <automated>cd benchmark/apps/ferro-conduit && cargo build --release 2>&1 | tail -5</automated>
  </verify>
  <done>`cargo build --release` succeeds in `benchmark/apps/ferro-conduit`. `grep -L "\[workspace\]" benchmark/apps/ferro-conduit/Cargo.toml` confirms no inherited workspace. The binary `target/release/ferro-conduit` exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hand-roll the JWT module (mint_token / decode_token / JwtClaims) with unit tests</name>
  <files>benchmark/apps/ferro-conduit/src/jwt.rs</files>
  <read_first>
    - RESEARCH.md §1 "Auth / JWT" (lines 135-173) — the gap finding + the illustrative pattern
    - RESEARCH.md "Standard Stack" line 327 — `jsonwebtoken = "9"`, HS256, same version already in workspace via ferro-mcp-oauth
    - ferro-mcp-oauth/src/jwt.rs — the proven mint_token/decode_token shape to mirror (HS256, claims with sub + exp)
  </read_first>
  <behavior>
    - Test 1 (round-trip): `let t = mint_token(42, "alice@x.com", &secret); let c = decode_token(&t, &secret).unwrap();` → `c.sub == 42`.
    - Test 2 (expiry): a token minted with a -1 second TTL fails `decode_token` (Err).
    - Test 3 (bad signature): a token minted with secret A fails `decode_token` with secret B (Err).
  </behavior>
  <action>
    Create `src/jwt.rs`. This file is the ONLY hand-rolled / non-framework-provided code and MUST be self-contained so the static counter can isolate it.

    1. `#[derive(Serialize, Deserialize)] pub struct JwtClaims { pub sub: i64, pub email: String, pub exp: usize }` (sub = user id; email is convenience for the user envelope).
    2. `pub fn jwt_secret() -> String` reading `std::env::var("JWT_SECRET").unwrap_or_else(|_| "benchmark-only-not-production-grade".into())`. Add a `//!` module doc line stating: "HAND-ROLLED — not framework-provided. Ferro auth is session-based (RESEARCH §1). Uses a benchmark-only HS256 secret; NOT production-grade."
    3. `pub fn mint_token(user_id: i64, email: &str, secret: &str) -> String` — build `JwtClaims` with `exp = now + 24h` (use `jsonwebtoken::get_current_timestamp()` + 86400), `jsonwebtoken::encode(&Header::new(Algorithm::HS256), &claims, &EncodingKey::from_secret(secret.as_bytes()))`.
    4. `pub fn decode_token(token: &str, secret: &str) -> Result<JwtClaims, jsonwebtoken::errors::Error>` — `jsonwebtoken::decode::<JwtClaims>(token, &DecodingKey::from_secret(secret.as_bytes()), &Validation::new(Algorithm::HS256)).map(|d| d.claims)`. Default `Validation` validates `exp`.
    5. For the expiry test, expose a test-only mint that accepts an explicit exp, OR have `mint_token` accept a TTL — simplest: add `pub fn mint_token_with_ttl(user_id: i64, email: &str, secret: &str, ttl_secs: i64) -> String` and have `mint_token` delegate with 86400. The expiry test mints with ttl -1.
    6. `#[cfg(test)] mod tests` with the three behaviors above.
  </action>
  <verify>
    <automated>cd benchmark/apps/ferro-conduit && cargo test jwt:: 2>&1 | tail -10</automated>
  </verify>
  <done>`cargo test jwt::` passes 3 tests (round-trip, expiry rejected, bad-signature rejected). `src/jwt.rs` has a module doc line labeling it hand-rolled / not-framework-provided.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: JwtAuthMiddleware + OptionalJwtMiddleware reading Authorization: Token header into UserId extension</name>
  <files>benchmark/apps/ferro-conduit/src/middleware/mod.rs, benchmark/apps/ferro-conduit/src/middleware/jwt_auth.rs, benchmark/apps/ferro-conduit/src/middleware/optional_jwt.rs</files>
  <read_first>
    - app/src/middleware/bearer_auth.rs — the exact `impl Middleware` pattern: `request.header("Authorization")`, `request.insert::<T>(principal)`, `next(request).await`, reject with `Err(HttpResponse::new().status(401))`
    - RESEARCH.md §1 illustrative pattern (lines 156-172) — `strip_prefix("Token ")` then `decode_jwt` then insert `UserId`
    - RESEARCH.md "Architecture Patterns / JWT Custom Middleware Pattern" (lines 398-406) — two variants; handlers read via `req.get::<UserId>()`, NOT `AuthUser<T>` (Pitfall 1)
    - framework/src/http/request.rs lines 87/92/309 — insert/get/header signatures
  </read_first>
  <behavior>
    - Test 1 (no header → 401): JwtAuthMiddleware on a request with no Authorization header returns a 401 response and does not call next.
    - Test 2 (valid token → UserId inserted): with `Authorization: Token <mint_token(7,...)>`, the request reaching `next` has `req.get::<UserId>() == Some(&UserId(7))`.
    - Test 3 (optional, no token → proceeds as guest): OptionalJwtMiddleware with no header calls next and `req.get::<UserId>()` is None (no rejection).
  </behavior>
  <action>
    1. `src/middleware/mod.rs`: `pub mod jwt_auth; pub mod optional_jwt;` and define `#[derive(Clone, Copy, Debug, PartialEq)] pub struct UserId(pub i64);` (shared extension type; later handlers `req.get::<UserId>()`).
    2. `src/middleware/jwt_auth.rs`: `pub struct JwtAuthMiddleware;` with `#[async_trait] impl Middleware`. In `handle`: read `request.header("Authorization")`, `.and_then(|v| v.strip_prefix("Token "))`, decode via `crate::jwt::decode_token(tok, &crate::jwt::jwt_secret())`. On `Ok(claims)`: `request.insert(UserId(claims.sub)); next(request).await`. On missing/Err: `Err(HttpResponse::json(json!({"errors":{"body":["Unauthorized"]}})).status(401))` (Conduit error envelope, RESEARCH line 104). Note: `request` must be `mut`.
    3. `src/middleware/optional_jwt.rs`: `pub struct OptionalJwtMiddleware;` — same header/decode logic, but on missing/invalid token DO NOT reject: just `next(request).await` without inserting. On valid token, insert `UserId` then proceed.
    4. Unit tests: constructing a `Next` closure for tests is non-trivial; the simplest reliable test asserts the header-parsing + decode logic via a small helper `fn extract_user_id(header: Option<&str>) -> Option<UserId>` that both middlewares call, and test that helper directly (no-header → None, valid Token → Some(UserId(7)), bad token → None). Then a thin integration assertion is deferred to the Newman Auth folder (Plan 03). This keeps the unit test deterministic without mocking the full middleware chain.
  </action>
  <verify>
    <automated>cd benchmark/apps/ferro-conduit && cargo test middleware:: 2>&1 | tail -10 && cargo clippy --release -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <done>`cargo test middleware::` passes (no-header→None, valid→Some(UserId(7)), bad→None). Both middlewares compile and clippy is clean. `extract_user_id` helper shared by both. Handlers will read `req.get::<UserId>()` (documented in mod.rs), never `AuthUser<T>`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (Authorization header) | Untrusted JWT crosses here; decoded + signature-verified before UserId is trusted |
| benchmark secret | JWT_SECRET is a benchmark-only constant, not a managed secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-230-01 | Spoofing | jwt_auth.rs token validation | mitigate | HS256 signature verified via jsonwebtoken `decode` with the shared secret; bad-signature unit-tested to reject |
| T-230-02 | Tampering | JWT claims (sub) | mitigate | `exp` enforced by `Validation::new`; expiry unit-tested |
| T-230-03 | Information Disclosure | JWT_SECRET weakness | accept | Benchmark-only HS256 secret, documented "not production-grade" in src/jwt.rs module doc; local benchmark app, not a deployment |
| T-230-04 | Elevation of Privilege | UserId trusted from a guest path | mitigate | OptionalJwtMiddleware inserts UserId ONLY on a valid decoded token; missing/invalid → no UserId, handler treats as guest |
</threat_model>

<verification>
- `cargo build --release` succeeds (standalone, no root-workspace inheritance).
- `cargo test` green: jwt:: (3) + middleware:: (3).
- `cargo clippy --release -- -D warnings` clean.
- `docker build benchmark/apps/ferro-conduit` produces an image (deferred full run to Plan 06; a local `docker build` smoke is sufficient here if Docker is available — otherwise the Dockerfile is reviewed against ferro-micro's).
</verification>

<success_criteria>
- ferro-conduit compiles in release, OUTSIDE the root workspace.
- JWT module (src/jwt.rs) is isolated, hand-rolled, labeled non-framework-provided, and unit-tested (round-trip, expiry, bad-sig).
- Both JWT middlewares compile and route UserId into the request extension map; required-variant rejects with a Conduit 401 envelope, optional-variant never rejects.
- Health endpoint returns `{"status":"ok"}`.
</success_criteria>

<output>
After completion, create `.planning/phases/230-framework-benchmark-1b-ferro-conduit-realworld-backend-imple/230-01-SUMMARY.md`. Note the JWT module path (src/jwt.rs) for Plan 07's separate static count, and confirm whether `docker build` ran or was deferred.

HEAVY/THERMAL: this plan runs `cargo build --release` + clippy. Run sequentially, never concurrent with another cargo build.
</output>
