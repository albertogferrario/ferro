---
phase: 230-framework-benchmark-1b-ferro-conduit
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - benchmark/apps/ferro-conduit/src/controllers/articles.rs
  - benchmark/apps/ferro-conduit/src/controllers/mod.rs
  - benchmark/apps/ferro-conduit/src/routes.rs
  - benchmark/apps/ferro-conduit/src/dto/responses.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "POST/GET/PUT/DELETE /api/articles/{slug} and GET /api/articles work: create generates a unique slug, list supports tag/author/favorited filters + limit/offset, and returns articlesCount"
    - "Article responses carry the full Conduit article envelope: tagList, author profile (with following), favorited, favoritesCount, createdAt/updatedAt"
    - "The Newman Articles folder passes against the running Ferro Conduit"
    - "/api/articles/feed is declared before /api/articles/{slug} so feed is not shadowed"
  artifacts:
    - path: "benchmark/apps/ferro-conduit/src/controllers/articles.rs"
      provides: "index, store, show, update, destroy handlers + article DTO assembly"
      contains: "generate_slug"
  key_links:
    - from: "src/controllers/articles.rs"
      to: "article_tags + favorites + follows tables"
      via: "tagList aggregation + favoritesCount + author.following assembly"
      pattern: "favorites"
    - from: "src/routes.rs"
      to: "OptionalJwtMiddleware / JwtAuthMiddleware"
      via: "GET articles (optional auth) vs POST/PUT/DELETE (required auth)"
      pattern: "OptionalJwtMiddleware"
---

<objective>
Implement Articles CRUD: create (with unique slug + tag association), list (tag/author/favorited filters + limit/offset + articlesCount), get-by-slug, update, delete. Assemble the full Conduit article envelope including the nested author profile, tagList, favorited flag, and favoritesCount. Acceptance = the Newman **Articles** folder passes.

Purpose: Articles are the core Conduit resource and exercise SeaORM's M:N (tags, favorites), 1:N (author), filtering and pagination (RESEARCH §6). All framework-provided — no hand-rolling beyond slug generation (Plan 02 helper).

Output: A passing Articles Newman folder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/230-framework-benchmark-1b-ferro-conduit-realworld-backend-imple/230-RESEARCH.md
@.planning/phases/230-framework-benchmark-1b-ferro-conduit-realworld-backend-imple/230-02-SUMMARY.md
@.planning/phases/230-framework-benchmark-1b-ferro-conduit-realworld-backend-imple/230-03-SUMMARY.md

<interfaces>
Entities (Plan 02): article (generate_slug, fields slug/title/description/body/author_id/created_at/updated_at), article_tag (article_id, tag_id), tag (id, name), favorite (user_id, article_id), follow (follower_id, followed_id), user.
DTOs (Plan 03): `ArticleDto{slug,title,description,body,tag_list,created_at,updated_at,favorited,favorites_count,author:ProfileDto}`, `ProfileDto{username,bio,image,following}`, `CreateArticleEnvelope/UpdateArticleEnvelope`.
Middleware (Plan 01): `JwtAuthMiddleware`, `OptionalJwtMiddleware`, `UserId`. Handlers read `req.get::<UserId>()` (Option — guest when None on optional routes).
Query/pagination (framework): `QueryBuilder::filter().order_by_desc().limit().offset()` (framework/src/database/query_builder.rs), `req.query_as_or("limit", 20u64)` / `req.query("tag")` (request.rs:201/243), `ferro::PaginatorTrait::count` (framework/src/lib.rs) for articlesCount. SeaORM `Column::Id.is_in(vec![...])` for tag/favorited filters (RESEARCH §6 line 314).
Param: `req.param("slug")?` (request.rs:156).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Article create/show/update/delete + envelope assembly helper</name>
  <files>benchmark/apps/ferro-conduit/src/controllers/articles.rs, benchmark/apps/ferro-conduit/src/controllers/mod.rs, benchmark/apps/ferro-conduit/src/dto/responses.rs</files>
  <read_first>
    - RESEARCH.md "JSON response shapes / Single article" (lines 85-88) + "Request shapes / Create article" (line 121-122)
    - RESEARCH.md "SeaORM Relations Pattern" (lines 410-422) — following check + favorites count
    - RESEARCH.md Anti-Pattern "N+1 queries for article lists" (line 438) — batch or two-pass for favoritesCount/following
    - RESEARCH.md "Slug Generation" + Anti-Pattern slug-conflict (line 439)
    - benchmark/apps/ferro-conduit/src/dto/responses.rs (Plan 03 ArticleDto/ProfileDto)
  </read_first>
  <action>
    `controllers/articles.rs`. First write a reusable assembly helper since list + show + create all need it:

    `async fn to_article_dto(db, article: article::Model, viewer: Option<i64>) -> ArticleDto`:
    - tagList: `SELECT name FROM tags JOIN article_tags ON tag_id=tags.id WHERE article_id=?` → Vec<String> (via SeaORM: `article_tag::Entity::find().filter(ArticleId.eq(a.id))` then load tag names, or a join).
    - favoritesCount: `favorite::Entity::find().filter(ArticleId.eq(a.id)).count(db)`.
    - favorited: if viewer Some(uid): `favorite::Entity::find().filter(ArticleId.eq(a.id)).filter(UserId.eq(uid)).count(db) > 0` else false.
    - author ProfileDto: load author user; following = if viewer Some(uid): `follow::Entity::find().filter(FollowerId.eq(uid)).filter(FollowedId.eq(author.id)).count > 0` else false.
    - created_at/updated_at: `a.created_at.to_rfc3339()`.

    Handlers:
    - `store(req)`: required auth (UserId from req.get). Parse CreateArticleEnvelope. Validate title/description/body required (422 envelope on fail). slug = `article::generate_slug(&r.title)`; on UNIQUE conflict, retry with a new suffix (loop max ~3). Insert article (author_id = uid). For each tag in tag_list: upsert into tags (find-or-create by name), insert article_tags row. Return `json_response!({"article": to_article_dto(...)})`.
    - `show(req)`: optional auth (viewer = req.get::<UserId>().map(|u| u.0)). Load by slug; 404 if missing. Return article envelope.
    - `update(req)`: required auth; load by slug; assert author == uid else 403; apply present title/description/body (if title changes, Conduit regenerates slug — match the collection's expectation; check whether the Articles folder asserts slug change, keep slug if unsure). Save, return envelope.
    - `destroy(req)`: required auth; load by slug; assert author; delete (cascade removes article_tags/favorites/comments). Return 200 (empty or `{}` per collection).

    `controllers/mod.rs`: add `pub mod articles;`. Add any missing DTO helper to responses.rs if needed (e.g. a `MultipleArticles { articles: Vec<ArticleDto>, articles_count: i64 }` with camelCase → articlesCount) — used by Task 2.
  </action>
  <verify>
    <automated>cd benchmark/apps/ferro-conduit && cargo build --release 2>&1 | tail -5</automated>
  </verify>
  <done>store/show/update/destroy compile. `to_article_dto` assembles tagList/favorited/favoritesCount/author.following without N+1 per-field in a loop (two-pass or count queries). Authorization (author check) enforced on update/destroy.</done>
</task>

<task type="auto">
  <name>Task 2: Article list with filters + pagination + articlesCount; wire all article routes (feed-first ordering) → Newman Articles green</name>
  <files>benchmark/apps/ferro-conduit/src/controllers/articles.rs, benchmark/apps/ferro-conduit/src/routes.rs</files>
  <read_first>
    - RESEARCH.md §6 "Pagination and Filtering" (lines 305-318) — query_as_or, limit/offset, is_in for M:N filters, PaginatorTrait::count for articlesCount
    - RESEARCH.md "Multiple articles" envelope (line 91) — `{"articles":[...], "articlesCount":N}`
    - RESEARCH.md Routing example (lines 234-263) — feed BEFORE {slug}, optional-auth group vs required-auth group
    - benchmark/apps/ferro-conduit/tests/route_ordering.rs (Plan 03) — the ordering this must satisfy
  </read_first>
  <action>
    `index(req)` in articles.rs (optional auth):
    - Read query params: `tag` (Option<String>), `author` (Option<String>), `favorited` (Option<String> username), `limit = req.query_as_or("limit", 20u64)`, `offset = req.query_as_or("offset", 0u64)`.
    - Build the article id filter set:
      - if `tag`: resolve tag id by name, get article_ids from article_tags.
      - if `author`: resolve user id by username, filter articles.author_id.
      - if `favorited`: resolve user id by username, get article_ids from favorites.
      - Intersect constraints (apply as chained `.filter(...)` / `Column::Id.is_in(ids)`).
    - articlesCount = the COUNT over the filtered query BEFORE limit/offset (`PaginatorTrait::count` or a count query). Then apply `.order_by_desc(article::Column::CreatedAt).limit(limit).offset(offset)` for the page.
    - Map each to `to_article_dto(db, m, viewer)`; return `json_response!({"articles": dtos, "articlesCount": count})`.

    Wire routes in `routes.rs` — REPLACE the Plan 03 stub group structure with the full optional-auth + required-auth groups, declaring the feed/articles routes. CRITICAL ordering (RESEARCH Pitfall 2): list `/api/articles/feed` (required-auth, handler stubbed `feed` — Plan 06 implements; for now register a placeholder feed handler returning empty `{"articles":[],"articlesCount":0}` so the route resolves and Newman feed assertions that only check shape pass minimally) BEFORE `/api/articles/{slug}`. Final routes (article portion):
    ```
    // optional auth
    group!("/api", {
        get!("/articles", controllers::articles::index),
        get!("/articles/{slug}", controllers::articles::show),
    }).middleware(OptionalJwtMiddleware),
    // required auth — feed declared before {slug} updates/deletes for clarity
    group!("/api", {
        get!("/articles/feed", controllers::articles::feed_placeholder),
        post!("/articles", controllers::articles::store),
        put!("/articles/{slug}", controllers::articles::update),
        delete!("/articles/{slug}", controllers::articles::destroy),
    }).middleware(JwtAuthMiddleware),
    ```
    Keep the Plan 03 auth routes intact. Add `feed_placeholder` to articles.rs (Plan 06 replaces it with the real feed).
  </action>
  <verify>
    <automated>cd benchmark/apps/ferro-conduit && cargo build --release 2>&1 | tail -3 && cargo test --release route_ordering 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    Build clean; route_ordering test still green. With Postgres up + app served, `bash benchmark/contracts/conduit/run_newman.sh http://localhost:3000/api Articles` exits 0 (Articles folder green). Executor runs the live Newman Articles folder and pastes the pass summary into SUMMARY; if deferred to Plan 06's compose, SUMMARY states so and build+route_ordering must be green.
  </acceptance_criteria>
  <done>index supports tag/author/favorited filters + limit/offset + correct articlesCount (pre-pagination count). Routes wired with optional vs required auth groups, feed before {slug}. Newman Articles green (live) OR deferred-to-Plan-06 noted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → article write (store/update/destroy) | Author-ownership enforced server-side from the verified UserId |
| client → list filters | Untrusted query params parameterized through SeaORM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-230-12 | EoP | update/destroy another user's article | mitigate | Handler asserts `article.author_id == UserId` before mutating; 403 otherwise |
| T-230-13 | Injection | tag/author/favorited filter params | mitigate | Resolved via SeaORM parameterized `.filter()` / `is_in`; no raw SQL |
| T-230-14 | DoS | unbounded limit/offset | mitigate | `limit` defaults to 20 via query_as_or; clamp to a max (e.g. 100) to bound page size |
</threat_model>

<verification>
- `cargo build --release` + clippy clean; route_ordering green.
- Newman Articles folder green (live or Plan 06 compose, stated).
</verification>

<success_criteria>
- Full Articles CRUD conforms; list filters + pagination + articlesCount correct.
- Article envelope complete (tagList, author.following, favorited, favoritesCount).
- feed declared before {slug}; ordering test green.
</success_criteria>

<output>
After completion, create `.planning/phases/230-framework-benchmark-1b-ferro-conduit-realworld-backend-imple/230-04-SUMMARY.md`. State Newman Articles live/deferred. Note the `feed_placeholder` that Plan 06 must replace.

HEAVY/THERMAL: `cargo build --release` + live Newman (Postgres). Sequential only.
</output>
