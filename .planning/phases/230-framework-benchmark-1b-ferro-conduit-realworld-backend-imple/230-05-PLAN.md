---
phase: 230-framework-benchmark-1b-ferro-conduit
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - benchmark/apps/ferro-conduit/src/controllers/profiles.rs
  - benchmark/apps/ferro-conduit/src/controllers/mod.rs
  - benchmark/apps/ferro-conduit/src/routes.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "GET /api/profiles/{username} returns the profile envelope with the correct following flag for the viewer (or false for a guest)"
    - "POST/DELETE /api/profiles/{username}/follow create/remove a follows junction row and return the updated profile (following true/false)"
    - "The Newman Profiles folder passes against the running Ferro Conduit"
  artifacts:
    - path: "benchmark/apps/ferro-conduit/src/controllers/profiles.rs"
      provides: "show, follow, unfollow handlers"
      contains: "follow::"
  key_links:
    - from: "src/controllers/profiles.rs"
      to: "follows table"
      via: "insert/delete follower_id+followed_id rows"
      pattern: "follower_id"
---

<objective>
Implement Profiles: get-profile (optional auth, viewer-relative following flag) and follow/unfollow (required auth, mutate the follows junction). Acceptance = the Newman **Profiles** folder passes. This also makes the `author.following` and feed logic (Plan 06) correct, since both read the same follows table.

Purpose: Follows are the self-referential M:N relation (RESEARCH §2) and the basis for the article feed (Plan 06). Direct junction queries (RESEARCH line 202).

Output: A passing Profiles Newman folder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/230-framework-benchmark-1b-ferro-conduit-realworld-backend-imple/230-RESEARCH.md
@.planning/phases/230-framework-benchmark-1b-ferro-conduit-realworld-backend-imple/230-02-SUMMARY.md
@.planning/phases/230-framework-benchmark-1b-ferro-conduit-realworld-backend-imple/230-04-SUMMARY.md

<interfaces>
Entities (Plan 02): follow (follower_id, followed_id), user (id, username, bio, image).
DTO (Plan 03): `ProfileDto{username, bio, image, following}`.
Middleware (Plan 01): `OptionalJwtMiddleware` (profile show), `JwtAuthMiddleware` (follow/unfollow), `UserId`.
Following-check pattern (RESEARCH lines 410-417): `follow::Entity::find().filter(FollowerId.eq(viewer)).filter(FollowedId.eq(target)).count(db) > 0`.
Param: `req.param("username")?`.
A shared `to_profile_dto(db, user, viewer: Option<i64>) -> ProfileDto` helper — consider extracting from articles.rs `to_article_dto`'s author assembly (Plan 04) to avoid duplication; if Plan 04 already exposes `pub async fn to_profile_dto`, reuse it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profiles show/follow/unfollow + wire routes → Newman Profiles green</name>
  <files>benchmark/apps/ferro-conduit/src/controllers/profiles.rs, benchmark/apps/ferro-conduit/src/controllers/mod.rs, benchmark/apps/ferro-conduit/src/routes.rs</files>
  <read_first>
    - RESEARCH.md "JSON response shapes / Profile" (lines 82-83) — `{"profile":{username,bio,image,following}}`
    - RESEARCH.md endpoint list rows for profiles (lines 60-62) — show optional, follow/unfollow required
    - RESEARCH.md "SeaORM Relations Pattern" following check (lines 411-417)
    - benchmark/apps/ferro-conduit/src/controllers/articles.rs (Plan 04) — reuse the author/following assembly if exposed as `to_profile_dto`
    - benchmark/apps/ferro-conduit/src/routes.rs (Plan 04 final route table) — append the profiles routes to the correct groups
  </read_first>
  <action>
    `controllers/profiles.rs`:
    - `show(req)` (optional auth): viewer = req.get::<UserId>().map(|u| u.0). Load target user by `req.param("username")?`; 404 if missing. following = viewer.map(|v| follows count > 0).unwrap_or(false). Return `json_response!({"profile": ProfileDto{...}})`.
    - `follow(req)` (required auth): uid = req.get::<UserId>() or 401. Load target by username (404 if missing). Insert a follow row (follower_id=uid, followed_id=target.id) — use `INSERT ... ON CONFLICT DO NOTHING` semantics (the composite PK from Plan 02 makes a duplicate a no-op; catch the unique-violation or check-then-insert). Return the profile envelope with following=true.
    - `unfollow(req)` (required auth): delete the follow row (follower_id=uid, followed_id=target.id) if present. Return profile envelope with following=false.

    Guard self-follow if the collection asserts it (Conduit generally allows or ignores; match the folder's assertions — if unsure, allow it, it is harmless).

    `controllers/mod.rs`: add `pub mod profiles;`.

    `routes.rs`: append to the EXISTING groups (do not recreate them — Plan 04 owns the final structure):
    - optional-auth group: `get!("/profiles/{username}", controllers::profiles::show)`.
    - required-auth group: `post!("/profiles/{username}/follow", controllers::profiles::follow)`, `delete!("/profiles/{username}/follow", controllers::profiles::unfollow)`.
  </action>
  <verify>
    <automated>cd benchmark/apps/ferro-conduit && cargo build --release 2>&1 | tail -3 && cargo clippy --release -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    Build + clippy clean. With Postgres up + app served, `bash benchmark/contracts/conduit/run_newman.sh http://localhost:3000/api Profiles` exits 0 (Profiles folder green). Executor runs the live Newman Profiles folder and pastes the summary into SUMMARY; if deferred to Plan 07's compose, SUMMARY states so and the build+clippy must be green here.
  </acceptance_criteria>
  <done>show/follow/unfollow implemented; following flag viewer-relative; follow is idempotent (composite PK no-op on dup); routes appended to the existing optional/required groups. Newman Profiles green (live) OR deferred-to-Plan-07 noted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → follow/unfollow | follower_id taken only from the verified UserId, never from the request body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-230-15 | Spoofing | follow on behalf of another user | mitigate | follower_id = req.get::<UserId>() (verified token), not a client-supplied field |
| T-230-16 | Tampering | duplicate follow rows | mitigate | Composite PK (follower_id, followed_id) makes a repeat follow a no-op |
</threat_model>

<verification>
- `cargo build --release` + clippy clean.
- Newman Profiles folder green (live or Plan 07 compose, stated).
</verification>

<success_criteria>
- Profile show returns viewer-relative following; follow/unfollow mutate the junction idempotently and return the updated profile.
- Newman Profiles folder green.
</success_criteria>

<output>
After completion, create `.planning/phases/230-framework-benchmark-1b-ferro-conduit-realworld-backend-imple/230-05-SUMMARY.md`. State Newman Profiles live/deferred.

HEAVY/THERMAL: `cargo build --release` + live Newman. Sequential only.
</output>
