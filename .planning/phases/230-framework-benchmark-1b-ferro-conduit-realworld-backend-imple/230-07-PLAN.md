---
phase: 230-framework-benchmark-1b-ferro-conduit
plan: 07
type: execute
wave: 7
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  - benchmark/apps/laravel-conduit/PINNED_COMMIT.md
  - benchmark/apps/laravel-conduit/Dockerfile
  - benchmark/apps/laravel-conduit/Dockerfile.octane
  - benchmark/conduit-compose.yaml
  - benchmark/harness/static/count_static.py
  - benchmark/harness/perf/run_perf.py
  - benchmark/results/conduit/RESULTS.md
  - benchmark/results/conduit/static-ferro-conduit.json
  - benchmark/results/conduit/static-laravel-conduit.json
  - benchmark/results/conduit/perf-ferro-conduit.json
  - benchmark/results/conduit/perf-laravel-conduit.json
  - benchmark/results/conduit/newman-ferro.json
  - benchmark/results/conduit/newman-laravel.json
autonomous: false
requirements: []

must_haves:
  truths:
    - "A community Laravel RealWorld backend is VENDORED (not authored) at a PINNED commit SHA recorded in PINNED_COMMIT.md, after evaluating f1amy vs gothinkster for conformance"
    - "The full RealWorld Newman collection is green against BOTH the Ferro Conduit AND the vendored Laravel Conduit (fair like-for-like baseline)"
    - "The static-compression report counts the hand-rolled JWT code (src/jwt.rs) SEPARATELY and labels it 'not framework-provided' (D-10 honesty)"
    - "The perf comparison runs the Conduit workload against Ferro and Laravel (php-fpm + octane) on shared Postgres, with honest caveats in RESULTS.md"
  artifacts:
    - path: "benchmark/apps/laravel-conduit/PINNED_COMMIT.md"
      provides: "Vendored repo URL + exact pinned SHA + evaluation rationale (f1amy vs gothinkster)"
      contains: "commit"
    - path: "benchmark/results/conduit/RESULTS.md"
      provides: "Static + perf + conformance report with JWT counted separately and honest framing"
      contains: "not framework-provided"
    - path: "benchmark/conduit-compose.yaml"
      provides: "Postgres + ferro-conduit + laravel-conduit + laravel-octane services"
      contains: "ferro-conduit"
  key_links:
    - from: "benchmark/harness/static/count_static.py"
      to: "src/jwt.rs"
      via: "separate count for the hand-rolled JWT module"
      pattern: "jwt"
    - from: "benchmark/conduit-compose.yaml"
      to: "benchmark/apps/laravel-conduit (pinned)"
      via: "build context for the vendored Laravel backend"
      pattern: "laravel-conduit"
---

<objective>
Vendor and pin a community Laravel RealWorld backend (evaluate f1amy vs gothinkster, pick the one that passes the Newman collection, pin its commit SHA — never author it), then run the Phase 229 harness on the real-app Conduit workload: full Newman against BOTH apps, static compression with the hand-rolled JWT counted separately (D-10), and perf (Ferro vs php-fpm vs octane on shared Postgres). Write RESULTS.md with honest caveats.

Purpose: This is the payoff — a like-for-like real-app comparison. The honesty hook (JWT counted separately, labeled non-framework-provided) is mandatory so the framework-provided line count is not overstated.

Output: A pinned Laravel backend, both apps green on Newman, and a RESULTS.md with static + perf + conformance evidence.

CHECKPOINTS: this plan vendors a third-party repo (license/version decision) and runs load tests (resource-heavy); both warrant a human-verify gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/230-framework-benchmark-1b-ferro-conduit-realworld-backend-imple/230-RESEARCH.md
@.planning/phases/230-framework-benchmark-1b-ferro-conduit-realworld-backend-imple/230-VALIDATION.md
@.planning/phases/230-framework-benchmark-1b-ferro-conduit-realworld-backend-imple/230-06-SUMMARY.md

<interfaces>
Phase 229 harness:
- Static: `benchmark/harness/static/count_static.py` — `run(app_dir)` → tokei code_lines/files + source_tokens, excludes vendor/target/docker.
- Perf: `benchmark/harness/perf/run_perf.py` — `run(base_url, framework, out_dir, endpoints=..., duration, concurrency)` drives `oha`, writes `perf-{framework}.json`. Default endpoints are the micro ones; OVERRIDE with Conduit endpoints.
- Compose template: `benchmark/compose.yaml` — Postgres 16.4 + ferro-micro + laravel-micro + laravel-octane services with shared `bench` DB. Mirror this for Conduit.
- Laravel micro patterns: `benchmark/apps/laravel-micro/{Dockerfile, Dockerfile.octane, docker/}` — php-fpm+nginx and octane Docker recipes to copy for the vendored Conduit Laravel app.
Newman runner (Plan 03): `benchmark/contracts/conduit/run_newman.sh APIURL [folder]`.
Ferro Conduit (Plans 01-06): standalone app at `benchmark/apps/ferro-conduit`, JWT isolated in `src/jwt.rs`.
RESEARCH Laravel recommendation (lines 523-535): gothinkster/laravel-realworld-example-app (archived, Laravel 5.x) vs f1amy/laravel-realworld-example-app (Laravel 10/11, maintained). Evaluate f1amy first; fall back to gothinkster. PIN a SHA, not a branch.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Select the Laravel RealWorld backend to vendor + pin</name>
  <action>Evaluate the candidate repos for Newman conformance, then pick one and pin its SHA (see options/resume-signal).</action>
  <decision>Which community Laravel RealWorld backend to vendor + pin, after a conformance check</decision>
  <context>The Laravel side must be vendored (not authored) and pinned to a commit SHA. RESEARCH recommends evaluating f1amy/laravel-realworld-example-app (Laravel 10/11, maintained) first, falling back to gothinkster/laravel-realworld-example-app (archived, Laravel 5.x). The executor should clone each candidate at HEAD, attempt to bring it up against Postgres, and run the vendored Newman collection to see which passes cleanly with the least config drift. Present the findings, then pick.</context>
  <options>
    <option id="f1amy">
      <name>f1amy/laravel-realworld-example-app (pin HEAD SHA)</name>
      <pros>Modern Laravel 10/11; maintained; likely builds with current PHP/composer; closer to the laravel-micro toolchain already vendored</pros>
      <cons>Must confirm it passes the full Newman collection without spec drift (RESEARCH Assumption A2 — medium risk)</cons>
    </option>
    <option id="gothinkster">
      <name>gothinkster/laravel-realworld-example-app (pin HEAD SHA)</name>
      <pros>Canonical reference the RealWorld project links to; known-conformant historically</pros>
      <cons>Archived; Laravel 5.x; old PHP — may need a legacy PHP image and more Docker work</cons>
    </option>
  </options>
  <resume-signal>Select: f1amy or gothinkster (or provide a third community repo). Executor records the chosen repo URL + exact SHA in PINNED_COMMIT.md with the conformance-check evidence.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Vendor + pin the chosen Laravel backend; Dockerize (php-fpm + octane); Conduit compose</name>
  <files>benchmark/apps/laravel-conduit/PINNED_COMMIT.md, benchmark/apps/laravel-conduit/Dockerfile, benchmark/apps/laravel-conduit/Dockerfile.octane, benchmark/conduit-compose.yaml</files>
  <read_first>
    - RESEARCH.md "Laravel RealWorld Backend Recommendation" (lines 523-535) — vendor not author, pin a SHA
    - benchmark/apps/laravel-micro/Dockerfile + Dockerfile.octane + docker/ (nginx.conf, php-fpm.conf, supervisord.conf) — the serving recipes to adapt
    - benchmark/compose.yaml — the Postgres + app + octane service shape to mirror (different ports for Conduit: db, ferro-conduit:3002, laravel-conduit:8011, laravel-octane:8012)
    - .planning/STATE.md "When adding a new crate ... wave" note is NOT relevant here (this is a benchmark app, not a workspace crate)
  </read_first>
  <action>
    1. Clone the chosen repo at the SHA selected in the checkpoint into `benchmark/apps/laravel-conduit/` (vendor the source tree). Remove its `.git` so it is a flat vendored copy. Record in `PINNED_COMMIT.md`: source URL, exact 40-char SHA, clone date, the conformance evidence from the checkpoint, and a one-line note that this is a vendored community implementation (do NOT modify its application logic — config-only changes for DB/env are acceptable and must be listed).
    2. `Dockerfile` (php-fpm+nginx) and `Dockerfile.octane`: adapt from `benchmark/apps/laravel-micro/Dockerfile*` for the vendored app's Laravel version (use the matching PHP image; f1amy → php 8.2+, gothinkster → legacy php 7.x). Wire APP_KEY + Postgres env. Run `php artisan migrate --force` on boot (entrypoint or supervisord), matching laravel-micro's pattern.
    3. `benchmark/conduit-compose.yaml`: mirror `benchmark/compose.yaml` with services `db` (postgres:16.4, DB `conduit`), `ferro-conduit` (build ./apps/ferro-conduit, DATABASE_URL → db/conduit, port 3002:3000), `laravel-conduit` (build ./apps/laravel-conduit, DB env → db/conduit, port 8011:8000), `laravel-octane` (Dockerfile.octane, port 8012:8080, APP_KEY set). All depend_on db healthy. Keep the JWT secret env for ferro-conduit (`JWT_SECRET`).
  </action>
  <verify>
    <automated>test -f benchmark/apps/laravel-conduit/PINNED_COMMIT.md && grep -Eiq "[0-9a-f]{40}" benchmark/apps/laravel-conduit/PINNED_COMMIT.md && python3 -c "import yaml;d=yaml.safe_load(open('benchmark/conduit-compose.yaml'));print(sorted(d['services']))"</automated>
  </verify>
  <done>laravel-conduit vendored at a pinned 40-char SHA recorded in PINNED_COMMIT.md (with conformance evidence + listed config-only changes). conduit-compose.yaml defines db + ferro-conduit + laravel-conduit + laravel-octane on the shared `conduit` Postgres DB. Dockerfiles adapted from laravel-micro.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the harness — static (JWT counted separately) + perf (Conduit workload)</name>
  <files>benchmark/harness/static/count_static.py, benchmark/harness/perf/run_perf.py</files>
  <read_first>
    - benchmark/harness/static/count_static.py — current `run(app_dir)` returns code_lines/files/source_tokens via tokei
    - benchmark/harness/static/test_count_static.py — keep its assertions passing; add one for the JWT-separate field
    - benchmark/harness/perf/run_perf.py — `run(base_url, framework, out_dir, endpoints=...)`; default endpoints are micro
    - VALIDATION.md "Honesty hook (D-10)" (lines 49-52) — JWT counted separately + labeled
    - .planning/STATE.md memory `project_v0260_brew_tap_live` note is unrelated; ignore
  </read_first>
  <action>
    1. Static counter — add a `run_with_carveout(app_dir, carveout_paths)` (or extend `run`) that, in addition to the total, reports a separate count for an explicit list of "not framework-provided" files and subtracts them from a `framework_provided_code_lines` figure. For ferro-conduit, `carveout_paths = ["src/jwt.rs"]` (the hand-rolled JWT module +, if the executor judges them clearly hand-rolled, the two JWT middleware files — but at minimum src/jwt.rs). Output JSON gains: `{"total": {...}, "hand_rolled": {"files":[...], "code_lines":N, "label":"not framework-provided (JWT auth — Ferro is session-based)"}, "framework_provided_code_lines": total-hand_rolled}`. Keep the Laravel run as total-only (Laravel's jwt-auth is a vendored package, not app code — note this asymmetry in RESULTS).
    2. Perf runner — when invoked for the Conduit workload, OVERRIDE the default endpoints with a representative Conduit set: e.g. `("/api/tags", "/api/articles?limit=20", "/api/profiles/celeb", "/api/articles/{a known slug}")` for read endpoints, plus optionally an authenticated GET /api/user with a pre-minted token header (oha supports `-H`). Keep duration/concurrency consistent with Phase 229 (30s, c=256) for comparability. The runner must accept the endpoint list + optional auth header as params (do not hardcode Conduit endpoints into the micro path — add a `conduit` preset or pass endpoints in).
    3. Update `test_count_static.py` with one assertion that the carveout field appears and `framework_provided_code_lines == total - hand_rolled` for a fixture. Keep existing tests green.
  </action>
  <verify>
    <automated>cd benchmark/harness/static && python3 -m pytest test_count_static.py -q 2>&1 | tail -5; cd ../perf && python3 -m pytest test_parse_perf.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>Static counter emits a separate hand-rolled (JWT) count + a framework_provided figure; tests green. Perf runner accepts a Conduit endpoint preset + auth header. Laravel/Ferro asymmetry (vendored jwt-auth package vs hand-rolled module) noted in code comments for RESULTS to cite.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify the Conduit compose stack is healthy before load tests</name>
  <action>Bring up the Conduit compose stack and confirm all services are healthy and migrated before the heavy load/Newman runs (see how-to-verify).</action>
  <what-built>The Conduit compose stack (Postgres + ferro-conduit + laravel-conduit + laravel-octane). The executor brought all services up via `docker compose -f benchmark/conduit-compose.yaml up -d --build`, ran migrations, and is ready to run the conformance + load tests.</what-built>
  <how-to-verify>
    1. `docker compose -f benchmark/conduit-compose.yaml ps` — all services healthy.
    2. `curl -s http://localhost:3002/health` returns `{"status":"ok"}` (Ferro); `curl -s http://localhost:8011/api/tags` returns a tags envelope (Laravel php-fpm).
    3. Confirm shared Postgres `conduit` DB is migrated for both apps.
    Approve to proceed to the heavy load tests + full Newman runs (resource-intensive — run sequentially, one app at a time).
  </how-to-verify>
  <resume-signal>Type "approved" to run the load tests + dual Newman, or report issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Full Newman against BOTH apps + static + perf runs; write RESULTS.md (JWT separate, honest caveats)</name>
  <files>benchmark/results/conduit/RESULTS.md, benchmark/results/conduit/static-ferro-conduit.json, benchmark/results/conduit/static-laravel-conduit.json, benchmark/results/conduit/perf-ferro-conduit.json, benchmark/results/conduit/perf-laravel-conduit.json, benchmark/results/conduit/newman-ferro.json, benchmark/results/conduit/newman-laravel.json</files>
  <read_first>
    - benchmark/results/2026-06-15/{public.md, internal.md, meta.json, NOTES.md} — the Phase 229 RESULTS format to mirror (public-voice + internal split, meta provenance)
    - VALIDATION.md "Validation Sign-Off" (lines 54-60) — the four gates this task closes
    - benchmark/harness/report/build_tables.py — if it builds comparison tables, reuse it for the Conduit numbers
    - ./CLAUDE.md "Repository documents must read as neutral" — RESULTS.md is committed; neutral architectural voice, honest caveats, no marketing
  </read_first>
  <action>
    Run these SEQUENTIALLY (each is resource-heavy; never concurrent):
    1. Full Newman against Ferro: `bash benchmark/contracts/conduit/run_newman.sh http://localhost:3002/api` → copy newman-result.json to `benchmark/results/conduit/newman-ferro.json`. Must be all-green.
    2. Full Newman against Laravel php-fpm: `... http://localhost:8011/api` → `newman-laravel.json`. Must be all-green (fair baseline — if it is not green, the Laravel pin/config is wrong; fix config-only or re-pin per the checkpoint, do NOT modify app logic).
    3. Static: `python3 benchmark/harness/static/count_static.py` (carveout mode) for `benchmark/apps/ferro-conduit` → `static-ferro-conduit.json`; total-only for `benchmark/apps/laravel-conduit` (exclude vendor) → `static-laravel-conduit.json`.
    4. Perf (one app at a time, after a warmup): Conduit endpoint preset against ferro-conduit (3002) → `perf-ferro-conduit.json`; against laravel-conduit php-fpm (8011) → `perf-laravel-conduit.json`; against laravel-octane (8012) → append as the octane variant. Keep 30s/c=256 for comparability with Phase 229.
    5. Write `RESULTS.md` (neutral voice): a conformance section (both apps green, folder counts), a static-compression section that REPORTS THE JWT CARVEOUT EXPLICITLY ("Ferro framework-provided: N lines; hand-rolled JWT auth (src/jwt.rs): M lines — not framework-provided, because Ferro auth is session-based; Laravel uses a vendored jwt-auth composer package, so its JWT lines are in vendor/ and excluded from app code — the comparison is framework-provided-app-code vs framework-provided-app-code, with Ferro's JWT carveout shown separately for honesty"), and a perf section (req/s + latency per endpoint, Ferro vs php-fpm vs octane) with caveats (cold-vs-warm, single-host, Docker overhead, N+1 query differences between the two implementations). End with the four VALIDATION sign-off boxes checked.
    6. Add a `meta.json` provenance (date, ferro version, pinned Laravel SHA, hardware note) mirroring the Phase 229 results meta.
  </action>
  <verify>
    <automated>test -f benchmark/results/conduit/RESULTS.md && grep -q "not framework-provided" benchmark/results/conduit/RESULTS.md && python3 -c "import json; j=json.load(open('benchmark/results/conduit/newman-ferro.json')); print('ferro failures:', j.get('run',{}).get('failures',j.get('failures','?')))" && python3 -c "import json; j=json.load(open('benchmark/results/conduit/newman-laravel.json')); print('laravel failures:', j.get('run',{}).get('failures',j.get('failures','?')))"</automated>
  </verify>
  <acceptance_criteria>
    newman-ferro.json AND newman-laravel.json both show zero failures (full collection green against BOTH apps). static-ferro-conduit.json contains the JWT carveout (`hand_rolled` + `framework_provided_code_lines`). perf-*.json exist for ferro + laravel-fpm + octane. RESULTS.md contains the phrase "not framework-provided" applied to the JWT count, reports static + perf with honest caveats, and checks the four VALIDATION sign-off boxes. The executor pastes the dual-Newman failure counts (both 0) into the SUMMARY.
  </acceptance_criteria>
  <done>Full Newman green against BOTH Ferro and Laravel; static (JWT counted separately + labeled) + perf (Ferro vs php-fpm vs octane) written to benchmark/results/conduit/; RESULTS.md neutral-voice with honest caveats; four VALIDATION gates signed off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored Laravel source | Third-party code run locally in Docker; not exposed publicly; benchmark-only |
| benchmark Postgres | Local container, throwaway `conduit` DB, benchmark-only credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-230-20 | Tampering | vendored Laravel integrity | mitigate | Pinned to an exact SHA in PINNED_COMMIT.md; .git removed; only config-only changes, all listed |
| T-230-21 | Info Disclosure | benchmark secrets in compose | accept | JWT_SECRET / APP_KEY / DB creds are benchmark-only throwaway values in a local compose file; documented not-production-grade |
| T-230-22 | Repudiation | unfair/overstated comparison | mitigate | JWT carveout reported separately; both apps must pass the same Newman collection; caveats documented in RESULTS (D-10 honesty) |
</threat_model>

<verification>
- PINNED_COMMIT.md has a 40-char SHA + conformance evidence.
- Full Newman green against BOTH Ferro and Laravel (zero failures each).
- Static carveout: JWT counted separately + labeled "not framework-provided".
- Perf: Ferro vs php-fpm vs octane on shared Postgres, honest caveats.
- All four VALIDATION sign-off boxes checked.
</verification>

<success_criteria>
- Community Laravel backend vendored + pinned (evaluated, not authored).
- Like-for-like conformance: both apps green on the same collection.
- Honest static + perf RESULTS with the JWT counted separately.
</success_criteria>

<output>
After completion, create `.planning/phases/230-framework-benchmark-1b-ferro-conduit-realworld-backend-imple/230-07-SUMMARY.md`. Record the pinned Laravel SHA, the dual-Newman failure counts (both 0), the JWT carveout line counts, and the headline perf numbers. This closes the phase.

HEAVY/THERMAL: Docker builds + load tests (oha) + dual Newman runs. Run every step SEQUENTIALLY, one app at a time; never concurrent Docker builds or load tests. Pace the chain and stop on any thermal-stress signal.
</output>
