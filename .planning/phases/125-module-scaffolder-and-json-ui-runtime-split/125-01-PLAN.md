---
phase: 125-module-scaffolder-and-json-ui-runtime-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-cli/src/commands/make_module.rs
  - ferro-cli/src/commands/mod.rs
  - ferro-cli/src/main.rs
  - ferro-cli/src/templates/module.rs
  - ferro-cli/src/templates/mod.rs
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05]
must_haves:
  truths:
    - "`ferro make:module orders` in a fresh project produces a compiling src/modules/orders/ skeleton"
    - "src/modules/mod.rs declares the new module (created if missing)"
    - "Generated routes.rs exports `pub fn register(router: Router) -> Router`"
    - "`--with-migration` drops a migration/src/m_{ts}_create_{name}.rs file"
    - "`--no-views` skips the views/ subtree"
    - "`--force` overwrites existing files; without it, existing files are preserved"
  artifacts:
    - path: ferro-cli/src/commands/make_module.rs
      provides: "make:module command implementation"
    - path: ferro-cli/src/templates/module.rs
      provides: "Stub templates for mod.rs/controller/model/views/routes"
  key_links:
    - from: ferro-cli/src/main.rs
      to: ferro-cli/src/commands/make_module.rs
      via: "Commands::MakeModule { name, with_migration, no_views, force } match arm"
      pattern: "MakeModule"
---

<objective>
Add `ferro make:module <name>` command to ferro-cli that scaffolds the
controller/model/views/routes feature-module convention used by gestiscilo and
mkmenu, with `--with-migration`, `--no-views`, `--force` flags. Honors D-01..D-05.

Purpose: Codify the unowned convention so it stops drifting.
Output: New CLI command + 6 stub templates + clap registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-module-scaffolder-and-json-ui-runtime-split/SCOPE.md
@.planning/phases/125-module-scaffolder-and-json-ui-runtime-split/125-CONTEXT.md
@ferro-cli/src/commands/make_controller.rs
@ferro-cli/src/templates/make.rs
@ferro-cli/src/project.rs

<interfaces>
Pattern from existing make:* commands:
- Each command = `ferro-cli/src/commands/<name>.rs` exposing `pub fn run(...)`.
- Registered in `ferro-cli/src/commands/mod.rs` (`pub mod <name>;`).
- Clap variant in `ferro-cli/src/main.rs` `Commands` enum + match arm.
- Templates in `ferro-cli/src/templates/<topic>.rs`, exported via `templates/mod.rs`.

Reusable helpers:
- `ferro_cli::project::find_project_root(None)` walks up to Cargo.toml.
- `console::style(...)` for colored output.
- to_snake_case + is_valid_identifier helpers exist in make_controller.rs (copy/extract).

make_controller.rs shows the canonical shape: validate name → check dir →
write file → update mod.rs → print success.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add module stub templates</name>
  <files>ferro-cli/src/templates/module.rs, ferro-cli/src/templates/mod.rs</files>
  <read_first>
    - ferro-cli/src/templates/make.rs (controller_template, json_view_template patterns)
    - ferro-cli/src/templates/mod.rs (how submodules are exported)
  </read_first>
  <behavior>
    - `module_mod_rs(name)` returns content declaring `pub mod controller; pub mod model; pub mod routes;` and `pub mod views;` (or no views when headless variant returned by separate fn).
    - `module_mod_rs_headless(name)` omits `pub mod views;`.
    - `module_controller_rs(name)` returns a compiling stub with one `#[handler] pub async fn index(_req: Request) -> Response` returning `json_response!({"module": "<name>"})`.
    - `module_model_rs(name)` returns an empty SeaORM entity stub: `use sea_orm::entity::prelude::*;` + `#[derive(Clone, Debug, PartialEq, DeriveEntityModel)] #[sea_orm(table_name = "<name>")] pub struct Model { #[sea_orm(primary_key)] pub id: i32 }` + empty Relation enum + `impl ActiveModelBehavior for ActiveModel {}`.
    - `module_views_mod_rs()` returns `pub mod index;`.
    - `module_view_index_rs(name)` returns a JsonUiView stub (mirror json_view_template in make.rs).
    - `module_routes_rs(name)` returns `use ferro::Router; pub fn register(router: Router) -> Router { router.get("/<name>", super::controller::index) }`.
    - `module_migration_rs(name, ts)` returns a SeaORM MigrationTrait stub creating a `<name>` table with id + created_at + updated_at columns.
    - All snippets must compile when dropped into a Ferro app (verified by Task 3 fixture).
  </behavior>
  <action>
    Create `ferro-cli/src/templates/module.rs` with the eight `pub fn` template functions listed in <behavior>. Each returns `String` interpolating `name` (snake_case) and where applicable `name` titlecased. Reference `make.rs::controller_template` and `make.rs::json_view_template` for tone — minimal but compiling. Add `pub mod module;` to `ferro-cli/src/templates/mod.rs`. Do NOT use `interface{}` patterns; use concrete types per CLAUDE.md.

    Add unit tests at the bottom of `module.rs` asserting:
    - `module_mod_rs("orders")` contains `pub mod controller;` and `pub mod views;`
    - `module_mod_rs_headless("orders")` does NOT contain `pub mod views;`
    - `module_routes_rs("orders")` contains `pub fn register(router: Router) -> Router` and `"/orders"`
    - `module_controller_rs("orders")` contains `#[handler]` and `"orders"`
    - `module_migration_rs("orders", "20260407120000")` contains `MigrationTrait` and `orders`
  </action>
  <verify>
    <automated>cargo test -p ferro-cli --lib templates::module -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - File ferro-cli/src/templates/module.rs exists with 8 template fns
    - `grep -q "pub mod module;" ferro-cli/src/templates/mod.rs` succeeds
    - `cargo test -p ferro-cli --lib templates::module` reports all 5 tests passing
    - `cargo build -p ferro-cli` succeeds with zero warnings
  </acceptance_criteria>
  <done>Templates module compiles, tests pass, exported from templates/mod.rs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement make:module command + clap wiring</name>
  <files>ferro-cli/src/commands/make_module.rs, ferro-cli/src/commands/mod.rs, ferro-cli/src/main.rs</files>
  <read_first>
    - ferro-cli/src/commands/make_controller.rs (canonical command shape)
    - ferro-cli/src/main.rs lines 73-90 (MakeController/MakeAction clap variants)
    - ferro-cli/src/main.rs lines 503-510 (MakeController match arm)
    - ferro-cli/src/project.rs (find_project_root)
  </read_first>
  <behavior>
    - `commands::make_module::run(name: String, with_migration: bool, no_views: bool, force: bool)` is the entry point.
    - Resolves project root via `ferro_cli::project::find_project_root(None)`; on error prints "Cargo.toml not found" and exits 1.
    - Validates name → snake_case → must be a valid Rust identifier; otherwise prints error and exits 1.
    - Creates `src/modules/<name>/` (and `src/modules/` if missing), then writes the 5 (or 4 with `--no-views`) stub files using templates from Task 1.
    - If any target file exists and `--force` is not set, prints "<path> already exists (use --force to overwrite)" and exits 1 WITHOUT touching anything.
    - Creates or updates `src/modules/mod.rs` to declare `pub mod <name>;` (idempotent: skip if already declared, like make_controller does).
    - With `--with-migration`: writes `migration/src/m_<unix_ts>_create_<name>.rs` using `module_migration_rs`. Uses `std::time::SystemTime::now().duration_since(UNIX_EPOCH)?.as_secs()` formatted as `YYYYMMDDHHMMSS` via chrono if available, else raw secs. Skip silently if `migration/src/` does not exist.
    - With `--no-views`: skips writing `views/mod.rs` and `views/index.rs`, and uses `module_mod_rs_headless`.
    - Prints `✓ Created <path>` per file, then a final "Module <name> created successfully!" line with usage hint: `crate::modules::<name>::routes::register(router)`.
  </behavior>
  <action>
    Create `ferro-cli/src/commands/make_module.rs` implementing `run(name, with_migration, no_views, force)` per <behavior>. Mirror the structure of `make_controller.rs` (validation, console::style, fs::write, mod.rs update). Reuse `to_snake_case` and `is_valid_identifier` — copy from make_controller.rs (do not refactor it now; this phase keeps changes localized).

    Register in `ferro-cli/src/commands/mod.rs`: add `pub mod make_module;` alphabetically near `make_middleware`.

    Wire into clap in `ferro-cli/src/main.rs`:
    1. Add Commands variant after `MakeMigration`:
       ```
       /// Generate a feature module skeleton (controller/model/views/routes)
       #[command(name = "make:module")]
       MakeModule {
           /// Name of the module (e.g., orders, user_profile)
           name: String,
           /// Also drop a migration stub under migration/src/
           #[arg(long)]
           with_migration: bool,
           /// Skip views/ subtree (headless module)
           #[arg(long)]
           no_views: bool,
           /// Overwrite existing files
           #[arg(long, short = 'f')]
           force: bool,
       },
       ```
    2. Add the matching match arm calling `commands::make_module::run(name, with_migration, no_views, force);`.

    Add `#[cfg(test)]` block with 4 unit tests inside `make_module.rs` (use `tempfile::TempDir`, set CWD via passing root explicitly OR refactor `run` to accept `root: &Path` and have a thin pub wrapper that resolves CWD — prefer the latter to keep tests deterministic):
    - `creates_default_skeleton`: orders module → 6 files exist, mod.rs declares `pub mod orders;`
    - `no_views_flag_skips_views`: views/ dir absent
    - `with_migration_flag_writes_migration`: migration file present when migration/src/ exists
    - `force_flag_overwrites`: pre-existing controller.rs gets replaced

    All tests must pass with `cargo test -p ferro-cli`.
  </action>
  <verify>
    <automated>cargo test -p ferro-cli --lib commands::make_module &amp;&amp; cargo build -p ferro-cli &amp;&amp; cargo clippy -p ferro-cli --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "MakeModule" ferro-cli/src/main.rs` succeeds (variant + match arm)
    - `grep -q "pub mod make_module;" ferro-cli/src/commands/mod.rs` succeeds
    - `cargo test -p ferro-cli --lib commands::make_module` shows 4 tests passing
    - `cargo clippy -p ferro-cli --all-targets -- -D warnings` is clean
    - `./target/debug/ferro make:module --help` lists `--with-migration`, `--no-views`, `--force`
  </acceptance_criteria>
  <done>Command is registered, all unit tests pass, clippy clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify make:module in a fresh ferro project (D-05)</name>
  <files>(none — manual UAT against generated project)</files>
  <action>Build CLI then run make:module against a freshly-scaffolded project; confirm flag matrix per how-to-verify steps. This is a human verification gate per D-05.</action>
  <verify>Manual UAT — see how-to-verify steps.</verify>
  <done>All 4 flag combinations behave as documented and the generated module compiles in a fresh project.</done>
  <what-built>Full make:module pipeline: command + templates + clap wiring.</what-built>
  <how-to-verify>
    1. Build CLI: `cargo build -p ferro-cli`
    2. In a scratch dir: `./target/debug/ferro new test-mod --no-interaction --no-git`
    3. `cd test-mod &amp;&amp; ../target/debug/ferro make:module orders`
    4. Verify `src/modules/orders/{mod.rs,controller.rs,model.rs,routes.rs,views/mod.rs,views/index.rs}` all exist
    5. Verify `src/modules/mod.rs` contains `pub mod orders;`
    6. `cargo build` inside test-mod must succeed (compiles with zero errors). If `register(router)` is not auto-wired, manually wire it once in main.rs to confirm types align.
    7. Re-run `make:module orders` → should error "already exists (use --force)"
    8. `make:module orders --force` → should overwrite without error
    9. `make:module accounts --no-views` → no views/ subtree
    10. `make:module invoices --with-migration` → migration file appears under migration/src/ (if migration crate exists)
  </how-to-verify>
  <acceptance_criteria>
    - Fresh project compiles cleanly with the generated module
    - All 4 flag combinations behave as documented
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cargo fmt --all -- --check` clean
- `cargo clippy -p ferro-cli --all-targets -- -D warnings` clean
- `cargo test -p ferro-cli` all green
- Manual fresh-project UAT (Task 3) approved
</verification>

<success_criteria>
- D-01..D-05 implemented and verified in a fresh project.
- `ferro make:module` is a first-class CLI command alongside other make:* commands.
</success_criteria>

<output>
Create `.planning/phases/125-module-scaffolder-and-json-ui-runtime-split/125-01-SUMMARY.md` after completion.
</output>
</content>
</invoke>