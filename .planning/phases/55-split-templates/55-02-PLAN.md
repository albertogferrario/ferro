---
phase: 55-split-templates
plan: 02
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - ferro-cli/src/templates/mod.rs
  - ferro-cli/src/templates/docker.rs
  - ferro-cli/src/templates/ai_boost.rs
  - ferro-cli/src/templates/scaffold.rs
  - ferro-cli/src/templates/auth.rs
autonomous: true
---

<objective>
Split the remaining large sections from templates/mod.rs into focused submodules: docker, AI boost, scaffold, and auth templates.

Purpose: Complete the split started in Plan 01 so that mod.rs contains only module declarations, re-exports, and tests.
Output: Four new submodules with mod.rs reduced to ~810 lines (module declarations + tests only).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-split-templates/55-01-SUMMARY.md

@ferro-cli/src/templates/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docker.rs, ai_boost.rs, scaffold.rs, and auth.rs submodules</name>
  <files>ferro-cli/src/templates/docker.rs, ferro-cli/src/templates/ai_boost.rs, ferro-cli/src/templates/scaffold.rs, ferro-cli/src/templates/auth.rs</files>
  <action>
Move remaining function groups from mod.rs into new submodules:

**docker.rs** — Container and deployment templates:
- `dockerfile_template`
- `dockerignore_template`
- `docker_compose_template`
- `do_app_yaml_template`

**ai_boost.rs** — AI development assistant configuration templates:
- `ferro_guidelines_template`
- `cursor_rules_template`
- `claude_md_template`
- `claude_md_ferro_section`
- `copilot_instructions_template`

**scaffold.rs** — Full-stack scaffold generation (the largest remaining section):
- `ScaffoldField` struct
- `ScaffoldForeignKey` struct
- `scaffold_factory_template`
- Helper functions: `scaffold_field_to_rust_type`, `scaffold_field_to_fake_value` (private helpers)
- `scaffold_test_template`
- `scaffold_test_with_factory_template`
- `ForeignKeyField` struct
- `scaffold_controller_with_fk_template`
- `scaffold_controller_template`
- `api_controller_template`
- `api_controller_with_fk_template`

**auth.rs** — Auth scaffolding templates (make:auth command):
- `auth_migration_template`
- `auth_controller_template`

Each file gets only the functions listed. Keep `pub` visibility on all public items. Private helpers remain private in their new home.
  </action>
  <verify>ls ferro-cli/src/templates/*.rs — should show mod.rs + 7 submodule files (3 from plan 01 + 4 new)</verify>
  <done>Four new files exist with all remaining non-test functions moved from mod.rs</done>
</task>

<task type="auto">
  <name>Task 2: Update mod.rs to only contain module declarations, re-exports, and tests</name>
  <files>ferro-cli/src/templates/mod.rs</files>
  <action>
Update mod.rs to declare and re-export the four new modules alongside the three from Plan 01:

```rust
mod project;
mod make;
mod entity;
mod docker;
mod ai_boost;
mod scaffold;
mod auth;

pub use project::*;
pub use make::*;
pub use entity::*;
pub use docker::*;
pub use ai_boost::*;
pub use scaffold::*;
pub use auth::*;

#[cfg(test)]
mod tests {
    // ... all existing tests stay here unchanged ...
}
```

mod.rs should now contain ONLY:
1. Module declarations (7 lines)
2. Re-exports (7 lines)
3. The `#[cfg(test)] mod tests` block (unchanged from Plan 01)

The tests block uses `use super::*` which pulls in all re-exported functions, so tests continue working without modification.
  </action>
  <verify>cargo build -p ferro-cli 2>&1 — should compile successfully</verify>
  <done>mod.rs contains only module wiring and tests, all functions live in submodules</done>
</task>

<task type="auto">
  <name>Task 3: Verify everything passes and check final structure</name>
  <files>ferro-cli/src/templates/mod.rs</files>
  <action>
Run full verification:

1. `cargo test -p ferro-cli` — all tests pass
2. `cargo clippy -p ferro-cli` — no warnings
3. `cargo fmt --check -p ferro-cli` — formatting clean
4. Verify line counts make sense:
   - `wc -l ferro-cli/src/templates/*.rs` to confirm split
   - mod.rs should be ~830 lines (14 lines of module wiring + ~815 lines of tests)
   - Total across all files should still be ~4,331 lines

If clippy warns about unused imports in any submodule, fix them. If tests reference moved private helpers, adjust visibility or move tests.
  </action>
  <verify>cargo test -p ferro-cli && cargo clippy -p ferro-cli -- -D warnings && cargo fmt --check -p ferro-cli</verify>
  <done>All tests pass, lint clean, mod.rs is minimal module hub with tests</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo build -p ferro-cli` succeeds
- [ ] `cargo test -p ferro-cli` passes all existing tests
- [ ] `cargo clippy -p ferro-cli` clean
- [ ] `cargo fmt --check -p ferro-cli` clean
- [ ] No changes to any files in `ferro-cli/src/commands/`
- [ ] mod.rs only contains module declarations, re-exports, and tests
- [ ] 7 submodule files exist: project.rs, make.rs, entity.rs, docker.rs, ai_boost.rs, scaffold.rs, auth.rs
- [ ] Total line count across all template files is approximately the same as original (4,331)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- mod.rs reduced to ~830 lines (module wiring + tests)
- 7 focused submodules with clear responsibilities
- Zero changes to command files
  </success_criteria>

<output>
After completion, create `.planning/phases/55-split-templates/55-02-SUMMARY.md`
</output>
