---
phase: 55-split-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-cli/src/templates/mod.rs
  - ferro-cli/src/templates/project.rs
  - ferro-cli/src/templates/make.rs
  - ferro-cli/src/templates/entity.rs
autonomous: true
---

<objective>
Split the first half of templates/mod.rs into focused submodules: project scaffolding, make command templates, and entity generation.

Purpose: Break down a 4,331-line monolith into focused modules for maintainability. This plan handles the foundational types and the project/make/entity templates that form the core of the CLI.
Output: Three new submodules (project.rs, make.rs, entity.rs) with mod.rs reduced and re-exporting everything.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@ferro-cli/src/templates/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project.rs, make.rs, and entity.rs submodules</name>
  <files>ferro-cli/src/templates/project.rs, ferro-cli/src/templates/make.rs, ferro-cli/src/templates/entity.rs</files>
  <action>
Split templates/mod.rs into three new files by moving functions (not copying — cut from mod.rs, paste into new file):

**project.rs** — Project scaffolding templates (used by `ferro new`):
- `cargo_toml`, `main_rs`, `routes_rs`, `controllers_mod`, `home_controller`
- `middleware_mod`, `middleware_logging`
- `models_mod`, `actions_mod`, `example_action`
- `config_mod`, `config_database`, `config_mail`, `bootstrap`
- `migrations_mod`
- `package_json`, `vite_config`, `tsconfig`, `index_html`, `main_tsx`, `home_page`, `inertia_props_types`
- `app_layout`, `auth_layout`, `layouts_index`, `globals_css`
- `login_page`, `register_page`, `forgot_password_page`, `reset_password_page`
- `dashboard_page`, `profile_page`, `settings_page`
- `auth_controller`, `dashboard_controller`, `profile_controller`, `settings_controller`
- `authenticate_middleware`, `user_model`, `password_reset_tokens_model`
- `create_users_migration`, `create_sessions_migration`, `create_password_reset_tokens_migration`
- `gitignore`, `env`, `env_example`
- `schedule_rs`, `tasks_mod`

**make.rs** — Make command templates (used by `make:*` commands):
- `middleware_template`
- `resource_template`
- `controller_template`
- `action_template`
- `inertia_page_template`
- `json_view_template`
- `error_template`
- `task_template`
- `event_template`, `events_mod`
- `listener_template`, `listeners_mod`
- `job_template`, `jobs_mod`
- `notification_template`, `notifications_mod`
- `seeder_template`, `seeders_mod`
- `factory_template`, `factories_mod`
- `policy_template`, `policies_mod`

**entity.rs** — Entity/model generation (used by `db:sync`):
- `to_snake_case` (private helper, move here)
- `ColumnInfo`, `TableInfo` structs
- All reserved keyword helpers: `RESERVED_KEYWORDS`, `is_reserved_keyword`, `escape_if_reserved`
- `entity_template`
- `user_model_template`
- `entities_mod_template`
- All SQL type conversion helpers (the private functions between entity_template and the helpers section)

Each new file starts with the appropriate `use` imports for `include_str!`. No use statements needed since `include_str!` is a compiler built-in and `format!` is in prelude.

Important: Keep `pub` visibility on all moved functions. The `to_snake_case` function is currently private — keep it private but move to entity.rs since that's its only consumer.
  </action>
  <verify>cargo build -p ferro-cli 2>&1 | head -5 — should show build starting (may fail until mod.rs is updated)</verify>
  <done>Three new files exist with all functions moved from mod.rs</done>
</task>

<task type="auto">
  <name>Task 2: Update mod.rs to re-export submodules</name>
  <files>ferro-cli/src/templates/mod.rs</files>
  <action>
Replace the moved content in mod.rs with module declarations and re-exports. The goal is that ALL existing `templates::function_name` and `templates::TypeName` call sites continue working without changes.

mod.rs should contain:
1. `mod project;` + `pub use project::*;`
2. `mod make;` + `pub use make::*;`
3. `mod entity;` + `pub use entity::*;`
4. Keep in mod.rs ONLY what doesn't fit cleanly elsewhere:
   - Docker templates (`dockerfile_template`, `dockerignore_template`, `docker_compose_template`)
   - DO template (`do_app_yaml_template`)
   - AI boost templates (`ferro_guidelines_template`, `cursor_rules_template`, `claude_md_template`, `claude_md_ferro_section`, `copilot_instructions_template`)
   - Scaffold types and templates (`ScaffoldField`, `ScaffoldForeignKey`, `ForeignKeyField`, and all scaffold_* / api_controller_* / auth_migration_template / auth_controller_template functions)
   - All `#[cfg(test)] mod tests` block — stays in mod.rs for now since tests reference functions from all submodules and re-exports make them available

This preserves the public API: every `crate::templates::*` import in commands/ continues working identically via `pub use *` glob re-exports.

Do NOT modify any files in commands/ — the re-exports handle backward compatibility.
  </action>
  <verify>cargo build -p ferro-cli 2>&1 — should compile successfully</verify>
  <done>mod.rs is reduced, re-exports submodules, all command files compile without changes</done>
</task>

<task type="auto">
  <name>Task 3: Verify tests pass and lint clean</name>
  <files>ferro-cli/src/templates/mod.rs</files>
  <action>
Run the full test suite and linting for ferro-cli to confirm the refactor is behavior-preserving.

1. Run `cargo test -p ferro-cli` — all existing template tests must pass
2. Run `cargo clippy -p ferro-cli` — no new warnings
3. Run `cargo fmt --check -p ferro-cli` — formatting clean

If any tests fail, fix by adjusting imports or visibility. The tests in mod.rs should work because they reference functions via `super::*` which includes the re-exported submodule items.
  </action>
  <verify>cargo test -p ferro-cli && cargo clippy -p ferro-cli -- -D warnings && cargo fmt --check -p ferro-cli</verify>
  <done>All tests pass, no clippy warnings, formatting clean</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo build -p ferro-cli` succeeds
- [ ] `cargo test -p ferro-cli` passes all existing tests
- [ ] `cargo clippy -p ferro-cli` clean
- [ ] `cargo fmt --check -p ferro-cli` clean
- [ ] No changes to any files in `ferro-cli/src/commands/` (re-exports preserve API)
- [ ] mod.rs line count significantly reduced (target: ~2,500 lines remaining — scaffold + docker + AI boost + tests)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- templates/mod.rs split into project.rs (~420 lines), make.rs (~700 lines), entity.rs (~300 lines)
- mod.rs reduced from 4,331 to ~2,500 lines
- Zero changes to command files
  </success_criteria>

<output>
After completion, create `.planning/phases/55-split-templates/55-01-SUMMARY.md`
</output>
