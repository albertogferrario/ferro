---
phase: 85.1-architecture-refinement
plan: 01
subsystem: api
tags: [schemars, json-schema, service-projections, protocol]

# Dependency graph
requires:
  - phase: 84-service-projections-foundation
    provides: ServiceDef, FieldDef, FieldMeaning, DataType, StateMachine, StateDef, Transition types
  - phase: 85-state-machines
    provides: StateMachine validation, state/transition definitions
provides:
  - JSON Schema derivation on all 7 public ferro-projections types
  - FieldMeaning description annotation for agent discovery of known variants
  - 4 schema generation tests validating schema structure
affects: [86-intent-graph, 87-renderers, 94-protocol]

# Tech tracking
tech-stack:
  added: [schemars 1.x]
  patterns: [JsonSchema derive on public protocol types, schemars description annotation for enum variant discovery]

key-files:
  modified:
    - ferro-projections/Cargo.toml
    - ferro-projections/src/field.rs
    - ferro-projections/src/service.rs
    - ferro-projections/src/state.rs

key-decisions:
  - "schemars 1.x chosen (latest stable, JSON Schema 2020-12 output)"
  - "Description annotation on FieldMeaning to mitigate anyOf shadowing by Custom(String)"
  - "JsonSchema NOT added to Error or Warning (internal types, not protocol contract)"

patterns-established:
  - "Public protocol types derive JsonSchema for agent-discoverable schemas"
  - "Enum types with untagged fallbacks use schemars description to document known variants"

# Metrics
duration: 5min
completed: 2026-02-28
---

# Phase 85.1 Plan 01: Schema Derivation Summary

**schemars 1.x JsonSchema derive on all 7 public ferro-projections types with FieldMeaning description annotation for agent variant discovery**

## Performance

- **Duration:** 5 min
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- All 7 public types (DataType, FieldMeaning, FieldDef, ServiceDef, StateMachine, StateDef, Transition) derive JsonSchema
- FieldMeaning annotated with schemars description listing all 18 known variants, mitigating the anyOf schema problem
- 4 new schema tests verify structure: DataType enum values, FieldMeaning description, FieldDef properties, ServiceDef properties
- Total test count: 53 unit + 2 doc = 55 tests, all passing

## Task Commits

Each task was committed atomically:

1. **Task 1: Add schemars dependency and derive JsonSchema on public types** - `b9025f7` (feat)
2. **Task 2: Add schema generation tests** - `8d723af` (test)

## Files Created/Modified
- `ferro-projections/Cargo.toml` - Added schemars 1.x dependency with derive feature
- `ferro-projections/src/field.rs` - JsonSchema derive on DataType, FieldMeaning (with description), FieldDef + 3 schema tests
- `ferro-projections/src/service.rs` - JsonSchema derive on ServiceDef + 1 schema test
- `ferro-projections/src/state.rs` - JsonSchema derive on StateMachine, StateDef, Transition

## Decisions Made
- Used schemars 1.x (latest stable, produces JSON Schema 2020-12)
- Added description annotation on FieldMeaning rather than restructuring the enum, since the Custom(String) untagged fallback is intentional design
- Did not add JsonSchema to Error or Warning as they are internal types

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All public types are schema-ready for protocol generation
- ServiceDef JSON Schema can be served to agents for capability discovery
- Ready for Plan 02 (roadmap refinement)

---
*Phase: 85.1-architecture-refinement*
*Completed: 2026-02-28*
