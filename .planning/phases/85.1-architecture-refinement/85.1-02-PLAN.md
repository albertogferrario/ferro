---
phase: 85.1-architecture-refinement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v9.0-ROADMAP.md
  - .planning/ROADMAP.md
autonomous: true
---

<objective>
Refine v9.0 phase descriptions to incorporate prior art insights from Phase 85.1 research, shifting the architectural direction from "map SAP floorplans" to "derive intent from structure."

Purpose: Future phases (86-93) need updated descriptions reflecting research findings so executors understand the architectural intent. The structural intent derivation principle — computing UI intent from ServiceDef shape rather than manual annotations — must be explicit in each phase's goal.
Output: Updated v9.0-ROADMAP.md and main ROADMAP.md with refined Phase 86-93 descriptions. Phase 94 already descoped to protocol documentation only.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/milestones/v9.0-ROADMAP.md
@.planning/STATE.md
@.planning/phases/85.1-architecture-refinement/85.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update v9.0-ROADMAP.md phase descriptions</name>
  <files>.planning/milestones/v9.0-ROADMAP.md</files>
  <action>
Update Phase 86-93 descriptions in v9.0-ROADMAP.md to reflect research insights:

**Phase 86: Actions & Preconditions** (update existing):
- Goal: Add action definitions with Siren-inspired ActionDef structure (name, display_name, inputs[], preconditions, side_effects). Add `readable`/`writable` booleans to FieldDef (from Hydra SupportedProperty) — needed for Collect vs Focus intent derivation. ActionInput reuses existing DataType and FieldMeaning types.
- Depends on: Phase 85.1 (was Phase 85)
- Research: Unlikely (Siren action format well-documented)

**Phase 87: Service Relationships** (refine existing):
- Goal: Declare service-to-service relationships with cardinality (one-to-one, one-to-many, many-to-many) and navigation hints. Cardinality is a structural signal for intent derivation — one-to-many suggests Browse sub-list, one-to-one suggests embedded detail.
- Depends on: Phase 84 (parallel with 85-86) — unchanged

**Phase 88: Intent Layer — Core Types** (significant update):
- Goal: Define Intent enum from structural derivation analysis, not SAP floorplan mapping. 7 structurally-derivable intents (Browse, Focus, Collect, Process, Summarize, Analyze, Track) + Custom(String) escape hatch. Each intent must answer: "What structural signal in ServiceDef triggers this?" Add IntentHint for optional per-ServiceDef override when automatic derivation is wrong.
- Note: Intent taxonomy should be validated during Phase 89. Intents that require manual annotation to select don't belong in the enum.
- Depends on: Phase 84, Phase 86

**Phase 89: Intent Graph Generation** (significant update — THE CORE PHASE):
- Goal: Structural analysis engine — input ServiceDef (fields, meanings, state machine, actions, relationships), analyze via pattern matching on structural signals, output ranked list of applicable intents with confidence scores. This is where Ferro's novelty lives: every other system requires manual specification or LLM reasoning. Must include validation suite — run derivation against representative ServiceDefs and measure accuracy. If <70% correct without hints, the approach needs rethinking.
- Depends on: Phase 88

**Phase 90: Renderer Trait & JSON-UI Renderer** (minor update):
- Add note: If intent→component mapping feels forced, the intent taxonomy in Phase 88 needs revision. Use A2UI widget catalog vocabulary for component naming where applicable.

**Phase 93: Field Test & Polish** (elevated importance):
- Goal: Build complete services (Order Management, User Management, Analytics Dashboard) exercising full stack. Primary validation: does the system derive the RIGHT intent for each service without manual IntentHint overrides? If >30% of cases need manual hints, heuristics need rework.
- Depends on: Phase 92

**Phase 94: Protocol Documentation** (already descoped — verify wording):
- Goal: Define standardized protocol specification. Formalize ServiceDef → IntentGraph → Renderer chain as documented, versioned protocol. NO patent preparation.

Also add an "Architecture Principles" section near the top of v9.0-ROADMAP.md (after Overview):
```
## Architecture Principles

1. **Structural intent derivation.** UI intent is DERIVED from ServiceDef structure (field meanings, state machine shape, relationship cardinality) — not manually specified, not deferred to LLM reasoning. This is Ferro's novel contribution.
2. **Schema as protocol contract.** JSON Schema generated from Rust types IS the protocol. Agents read it to discover capabilities.
3. **Every intent must be structurally derivable.** If an intent can't be selected without manual annotation, it doesn't belong in the enum.
4. **Confidence scores over hard selection.** A ServiceDef may match multiple intents. Return ranked list, let consumer pick.
```

Update the parallelization windows to account for Phase 85.1:
```
Wave 1: Phase 84 (foundation)
Wave 2: Phase 85 + 87 (parallel — state machines + relationships)
Wave 3: Phase 85.1 (architecture refinement, after 85)
Wave 4: Phase 86 (depends on 85.1)
Wave 5: Phase 88 (depends on 84 + 86)
Wave 6: Phase 89 (depends on 88 — THE CORE PHASE)
Wave 7: Phase 90 (depends on 89)
Wave 8: Phase 91 + 92 (integration + MCP can partially overlap)
Wave 9: Phase 93 (field test, sequential)
```
  </action>
  <verify>Verify v9.0-ROADMAP.md is valid markdown, all phase numbers are correct, dependency chains are consistent</verify>
  <done>v9.0-ROADMAP.md updated with: architecture principles section, refined Phase 86-93 descriptions reflecting structural intent derivation, updated parallelization windows, Phase 86 depends on 85.1</done>
</task>

<task type="auto">
  <name>Task 2: Update main ROADMAP.md phase descriptions</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Update the Phase 86-93 brief descriptions in .planning/ROADMAP.md to match v9.0-ROADMAP.md:

**Phase 86**: Change "Add action definitions — inputs, guards, preconditions, transition triggers" → "Add Siren-inspired ActionDef with inputs reusing DataType/FieldMeaning. Add readable/writable to FieldDef for intent derivation."
Change "Depends on: Phase 85" → "Depends on: Phase 85.1"

**Phase 87**: Change "Declare how services relate to each other with cardinality and navigation hints" → "Declare service relationships with cardinality as structural signal for intent derivation."

**Phase 88**: Change "Build the Intent vocabulary — universal UI operations for any medium" → "Define Intent enum from structural derivation analysis. 7 derivable intents + Custom escape hatch + IntentHint override."

**Phase 89**: Change "ServiceDef + current state/context → IntentGraph" → "Structural analysis engine — pattern matching on ServiceDef signals → ranked intents with confidence scores. Core innovation phase."

**Phase 93**: Change "Build complete Order Management service exercising full stack. Fix issues, polish API" → "Build representative services exercising full stack. Validate structural intent derivation accuracy (target: >70% correct without manual hints)."

Mark Phase 85.1 as complete in the progress section after both plans finish (this will be done during summary creation, not here).

Also update the v9.0 milestone summary line to note Phase 85.1 is inserted:
Under "v9.0 Service Projections" section, ensure Phase 85.1 appears with its current status.
  </action>
  <verify>Verify ROADMAP.md is valid markdown, phase numbers sequential, dependency references correct</verify>
  <done>Main ROADMAP.md Phase 86-93 descriptions updated to match v9.0-ROADMAP.md. Phase 86 dependency updated to 85.1. Phase 85.1 visible in roadmap.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] v9.0-ROADMAP.md has "Architecture Principles" section
- [ ] v9.0-ROADMAP.md Phase 86-93 descriptions reflect structural intent derivation
- [ ] v9.0-ROADMAP.md parallelization windows updated for 85.1
- [ ] Main ROADMAP.md Phase 86-93 descriptions match v9.0-ROADMAP.md
- [ ] Phase 86 depends on 85.1 in both files
- [ ] No broken markdown links or formatting
</verification>

<success_criteria>

- Phase 86-93 descriptions explicitly reference structural intent derivation where relevant
- Architecture principles documented in v9.0-ROADMAP.md
- Dependency chains updated (Phase 86 → 85.1)
- Parallelization windows account for Phase 85.1
- Both roadmap files consistent
</success_criteria>

<output>
After completion, create `.planning/phases/85.1-architecture-refinement/85.1-02-SUMMARY.md`
</output>
