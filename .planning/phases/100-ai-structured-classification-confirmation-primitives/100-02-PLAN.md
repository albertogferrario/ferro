---
phase: 100-ai-structured-classification-confirmation-primitives
plan: 02
type: execute
wave: 2
depends_on: [100-01]
files_modified:
  - ferro-ai/src/lib.rs
  - ferro-ai/src/confirmation/mod.rs
  - ferro-ai/src/confirmation/store.rs
  - ferro-ai/src/confirmation/events.rs
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03]

must_haves:
  truths:
    - "ConfirmationStore trait supports request_confirmation, confirm, reject, get, and list_pending operations"
    - "InMemoryConfirmationStore stores type-erased serde_json::Value payloads in DashMap"
    - "Pending actions expire after their TTL and dispatch ConfirmationExpired event via ferro-events"
    - "Confirming returns the stored payload for caller to deserialize back to original type"
    - "Rejecting removes the pending action without dispatching an expiry event"
    - "DashMap guards are never held across .await points"
  artifacts:
    - path: "ferro-ai/src/confirmation/mod.rs"
      provides: "ConfirmationStore trait, PendingAction struct, PendingActionInfo struct"
      exports: ["ConfirmationStore", "PendingActionInfo"]
    - path: "ferro-ai/src/confirmation/store.rs"
      provides: "InMemoryConfirmationStore with DashMap + tokio::spawn TTL expiry"
      exports: ["InMemoryConfirmationStore"]
    - path: "ferro-ai/src/confirmation/events.rs"
      provides: "ConfirmationExpired event implementing ferro_events::Event"
      exports: ["ConfirmationExpired"]
  key_links:
    - from: "ferro-ai/src/confirmation/store.rs"
      to: "dashmap::DashMap"
      via: "Arc<DashMap<String, StoredAction>> for concurrent access"
      pattern: "Arc<DashMap"
    - from: "ferro-ai/src/confirmation/store.rs"
      to: "ferro-ai/src/confirmation/events.rs"
      via: "dispatch_sync(ConfirmationExpired) on TTL expiry"
      pattern: "dispatch_sync.*ConfirmationExpired"
    - from: "ferro-ai/src/confirmation/store.rs"
      to: "tokio::time::sleep"
      via: "tokio::spawn + sleep for TTL expiry"
      pattern: "tokio::spawn.*sleep"
---

<objective>
Implement the confirmation state machine subsystem in the ferro-ai crate: ConfirmationStore trait, InMemoryConfirmationStore with DashMap, TTL expiry via tokio::spawn, and ConfirmationExpired ferro-events integration.

Purpose: Provides a reusable framework primitive for gating destructive actions behind explicit user confirmation with configurable TTL expiry and event-driven observability.
Output: Working confirmation module with all store operations and TTL lifecycle tested.
</objective>

<execution_context>
@/Users/alberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-ai-structured-classification-confirmation-primitives/100-CONTEXT.md
@.planning/phases/100-ai-structured-classification-confirmation-primitives/100-RESEARCH.md
@.planning/phases/100-ai-structured-classification-confirmation-primitives/100-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (ferro-ai crate already exists) -->

From ferro-ai/src/lib.rs:
```rust
pub mod classifier;
pub mod error;
pub use error::Error;
pub use classifier::{Classifier, ClassifierConfig, ClassificationResult, ClassificationProvider, AnthropicProvider};
```

From ferro-ai/src/error.rs:
```rust
#[derive(Debug, thiserror::Error)]
pub enum Error {
    Config(String),
    Provider(String),
    LowConfidence { best_guess: serde_json::Value, confidence: f64 },
    Deserialization(String),
    Timeout,
}
```

From ferro-events/src/traits.rs (Event trait to implement):
```rust
pub trait Event: Clone + Send + Sync + 'static {
    fn name(&self) -> &'static str;
}
```

From ferro-events/src/dispatcher.rs:
```rust
pub fn dispatch_sync<E: Event + 'static>(event: E);
```

<!-- DashMap guard-drop pattern from ferro-broadcast -->
// PITFALL: Do NOT hold DashMap guard across .await — causes deadlock in tokio
// Pattern: store.remove(&key) drops guard immediately, then dispatch event
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConfirmationStore trait, types, and InMemoryConfirmationStore</name>
  <files>ferro-ai/src/confirmation/mod.rs, ferro-ai/src/confirmation/store.rs, ferro-ai/src/confirmation/events.rs, ferro-ai/src/error.rs, ferro-ai/src/lib.rs</files>
  <behavior>
    - Test: request_confirmation stores a payload, get retrieves it
    - Test: confirm returns the stored payload and removes the entry
    - Test: confirm on nonexistent key returns None
    - Test: reject removes the entry and returns true, reject on nonexistent returns false
    - Test: list_pending returns all pending entries with key and created_at
    - Test: duplicate request_confirmation for same key overwrites (or returns error per design choice)
    - Test: ConfirmationExpired implements ferro_events::Event with name "ConfirmationExpired"
  </behavior>
  <action>
    1. Add confirmation Error variants to ferro-ai/src/error.rs:
       - StoreError(String) — generic store operation failure

    2. Create ferro-ai/src/confirmation/events.rs:
       - `ConfirmationExpired` struct: key (String), expired_at (chrono::DateTime<chrono::Utc>). Derive Clone, Debug.
       - Implement ferro_events::Event: name() returns "ConfirmationExpired"

    3. Create ferro-ai/src/confirmation/mod.rs:
       - pub mod store; pub mod events;
       - `PendingActionInfo` struct (for list_pending): key (String), created_at (chrono::DateTime<chrono::Utc>). Derive Clone, Debug, Serialize.
       - `#[async_trait] pub trait ConfirmationStore: Send + Sync`:
         - `async fn request_confirmation(&self, key: &str, payload: serde_json::Value, ttl: Duration) -> Result<(), Error>`
         - `async fn confirm(&self, key: &str) -> Result<Option<serde_json::Value>, Error>`
         - `async fn reject(&self, key: &str) -> Result<bool, Error>`
         - `async fn get(&self, key: &str) -> Result<Option<serde_json::Value>, Error>`
         - `async fn list_pending(&self) -> Result<Vec<PendingActionInfo>, Error>`
       - Re-export ConfirmationStore, InMemoryConfirmationStore, ConfirmationExpired, PendingActionInfo

    4. Create ferro-ai/src/confirmation/store.rs:
       - Internal `StoredAction` struct: payload (serde_json::Value), created_at (chrono::DateTime<chrono::Utc>), abort_handle (tokio::task::AbortHandle)
       - `InMemoryConfirmationStore` struct: inner (Arc<DashMap<String, StoredAction>>), default_ttl (Duration)
       - Constructor: `InMemoryConfirmationStore::new(default_ttl: Duration)`
       - Implement ConfirmationStore:
         - request_confirmation: insert into DashMap, spawn TTL expiry task (store Arc clone, key clone, ttl). Store AbortHandle in StoredAction.
         - confirm: remove from DashMap, abort the TTL task via AbortHandle, return payload
         - reject: remove from DashMap, abort TTL task, return whether entry existed
         - get: read from DashMap, clone payload (drop guard before return)
         - list_pending: iterate DashMap, collect PendingActionInfo (key, created_at)
       - spawn_ttl_expiry: tokio::spawn(async { sleep(ttl); store.remove(&key); if removed: dispatch_sync(ConfirmationExpired { key, expired_at: Utc::now() }) })
       - CRITICAL: Never hold DashMap guard across .await. In list_pending: collect into Vec inside the guard scope, then return.

    5. Update ferro-ai/src/lib.rs:
       - Add `pub mod confirmation;`
       - Re-export: ConfirmationStore, InMemoryConfirmationStore, ConfirmationExpired, PendingActionInfo

    6. Write tests in store.rs:
       - Basic CRUD: request, get, confirm, reject lifecycle
       - list_pending correctness
       - confirm returns None for missing key
       - reject returns false for missing key
       - ConfirmationExpired event name test

    7. `cargo test -p ferro-ai && cargo clippy -p ferro-ai -- -D warnings`
  </action>
  <verify>
    <automated>cd /Users/alberto/repositories/albertogferrario/ferro && cargo test -p ferro-ai -- confirmation && cargo clippy -p ferro-ai -- -D warnings</automated>
  </verify>
  <done>ConfirmationStore trait defined with 5 operations. InMemoryConfirmationStore stores payloads in DashMap with AbortHandle for TTL tasks. ConfirmationExpired event implements ferro_events::Event. All confirmation unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TTL expiry lifecycle and ferro-events integration</name>
  <files>ferro-ai/src/confirmation/store.rs</files>
  <behavior>
    - Test: entry is automatically removed after TTL expires (tokio::time::pause + advance)
    - Test: confirming before TTL prevents expiry event from firing
    - Test: rejecting before TTL prevents expiry event from firing
    - Test: multiple pending actions with different TTLs expire independently
    - Test: overwriting a pending action (same key, new request_confirmation) cancels the old TTL timer
  </behavior>
  <action>
    1. Add TTL lifecycle tests using tokio::time test utilities:
       - Use `#[tokio::test(start_paused = true)]` for deterministic time control
       - Test expired entry: create pending action with 100ms TTL, advance time by 150ms, verify get() returns None
       - Test confirm cancels TTL: create pending action, confirm it, advance time past TTL, verify no panic/no ConfirmationExpired dispatch (AbortHandle aborts the spawned task)
       - Test reject cancels TTL: same pattern as confirm but with reject()
       - Test independent TTLs: create two actions with 100ms and 200ms TTLs, advance 150ms, verify first gone and second still present, advance another 100ms, verify second gone
       - Test overwrite: request_confirmation with key "a", then request_confirmation again with key "a" and different payload. The old TTL task should be aborted (via stored AbortHandle). Verify new payload is returned by get(). If old AbortHandle not aborted before overwrite, fix: in request_confirmation, if key already exists, abort old handle before inserting new entry.

    2. Verify full ferro-ai test suite passes:
       - `cargo test -p ferro-ai` (all classification + confirmation tests)
       - `cargo fmt -p ferro-ai -- --check`
       - `cargo clippy -p ferro-ai -- -D warnings`
  </action>
  <verify>
    <automated>cd /Users/alberto/repositories/albertogferrario/ferro && cargo test -p ferro-ai && cargo fmt -p ferro-ai -- --check && cargo clippy -p ferro-ai -- -D warnings</automated>
  </verify>
  <done>TTL expiry removes entries and dispatches ConfirmationExpired event. Confirm/reject abort the TTL timer. Overwriting a key cancels the previous TTL. All tests use deterministic time control (start_paused). Full ferro-ai test suite green.</done>
</task>

</tasks>

<verification>
- `cargo test -p ferro-ai` — all classification + confirmation tests pass
- `cargo clippy -p ferro-ai -- -D warnings` — no warnings
- `cargo fmt -p ferro-ai -- --check` — formatted
- DashMap guards never held across .await (verified by code review in store.rs)
- ConfirmationExpired event dispatches on TTL expiry (verified by tokio time tests)
</verification>

<success_criteria>
- ConfirmationStore trait with 5 async operations
- InMemoryConfirmationStore using DashMap with AbortHandle-managed TTL
- ConfirmationExpired event fires on expiry via ferro_events::dispatch_sync
- Confirm and reject abort pending TTL timers
- All tests pass with deterministic time control, no flaky sleeps
</success_criteria>

<output>
After completion, create `.planning/phases/100-ai-structured-classification-confirmation-primitives/100-02-SUMMARY.md`
</output>
