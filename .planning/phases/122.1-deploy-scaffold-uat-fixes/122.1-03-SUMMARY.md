---
phase: 122.1-deploy-scaffold-uat-fixes
plan: 03
subsystem: infra
tags: [digitalocean, ferro-cli, scaffolding, do-init, env-substitution]
requires:
  - phase: 122-deploy-scaffold-core-rewrite
    provides: build_envs_block in ferro-cli/src/templates/do.rs
provides:
  - should_placeholder heuristic for dev-default detection
  - build_envs_block emits ${KEY} placeholders with override comments for dev defaults
affects: [122.1-deploy-scaffold-uat-fixes]
tech-stack:
  added: []
  patterns:
    - "Heuristic substitution at render time; no input mutation"
key-files:
  created: []
  modified:
    - ferro-cli/src/templates/do.rs
key-decisions:
  - "Any *_PORT key keeps literal value (ports are non-secret scalars)"
  - "Relative path heuristic: leading ./, ../, or bare segment containing / without scheme"
  - "DATABASE_URL exempted in both build_envs_block branch and should_placeholder guard (defense in depth)"
patterns-established:
  - "Dev-default detection via should_placeholder(key, value) -> bool"
requirements-completed: [D-09, D-10, D-11, D-12, D-13]
duration: 3min
completed: 2026-04-07
---

# Phase 122.1 Plan 03: Dev-default placeholder substitution Summary

**`build_envs_block` now replaces dev-default env values (localhost, empty, relative paths, `file:`) with `${KEY}` placeholders annotated with an override comment, while leaving `*_PORT`, `DATABASE_URL`, and production URLs as literals.**

## Performance

- **Duration:** ~3 min
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- `should_placeholder(key, value)` heuristic covering localhost/127.0.0.1/0.0.0.0, empty/whitespace, `file:`, `./`, `../`, bare relative paths
- `build_envs_block` restructured to emit `# Override in DO App Platform secrets (was: <orig>)` comment + `value: ${KEY}` for placeholdered entries
- `*_PORT` keys exempted (ports stay literal per D-10)
- `DATABASE_URL` double-guarded (branch + helper guard)
- 4 new unit tests: `should_placeholder_covers_all_branches`, `envs_block_substitutes_localhost_app_url`, `envs_block_keeps_production_url_literal`, `envs_block_database_url_unchanged_by_placeholder_logic`

## Task Commits

1. **Task 1: Add should_placeholder heuristic and rewire build_envs_block** - `24d68b84` (feat)

## Files Created/Modified
- `ferro-cli/src/templates/do.rs` - Added `should_placeholder` helper, restructured `build_envs_block`, added 4 tests

## Decisions Made
- Double-guard DATABASE_URL: both the `is_db` branch in `build_envs_block` and a guard in `should_placeholder`. Cheap safety against future refactors.
- `*_PORT` check before value inspection — any PORT key stays literal regardless of value.

## Deviations from Plan
None — plan executed exactly as written.

## Issues Encountered
- None.

## Verification
- `cargo fmt --all -- --check`: clean
- `cargo clippy -p ferro-cli --all-targets --no-deps -- -D warnings`: clean
- `cargo test -p ferro-cli --lib templates::do_spec`: 9/9 passed (5 pre-existing + 4 new)

## Self-Check: PASSED
- `ferro-cli/src/templates/do.rs`: FOUND
- Commit `24d68b84`: FOUND

## Next Phase Readiness
- Bug 3 resolved. Ready for plan 122.1-04 (worker filtering for test/dev binaries).

---
*Phase: 122.1-deploy-scaffold-uat-fixes*
*Completed: 2026-04-07*
