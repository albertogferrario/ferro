---
phase: 122.1-deploy-scaffold-uat-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-cli/src/templates/do.rs
  - ferro-cli/src/commands/do_init.rs
autonomous: true
requirements: [D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "sanitize_do_app_name(`mkmenu_ferro`) == `mkmenu-ferro`"
    - "AppYamlContext uses sanitized name so `app.yaml` emits `name: mkmenu-ferro` for mkmenu_ferro package"
    - "Helper handles edge cases: uppercase, dots, double underscores, illegal chars"
  artifacts:
    - path: "ferro-cli/src/templates/do.rs"
      provides: "sanitize_do_app_name helper + unit tests"
      contains: "fn sanitize_do_app_name"
    - path: "ferro-cli/src/commands/do_init.rs"
      provides: "call sanitize_do_app_name on package_name before rendering"
      contains: "sanitize_do_app_name"
  key_links:
    - from: "commands/do_init.rs run()"
      to: "templates::do_spec::sanitize_do_app_name"
      via: "let app_name = sanitize_do_app_name(&pkg); ctx.package_name = &app_name"
      pattern: "sanitize_do_app_name\\(&pkg\\)"
---

<objective>
Bug 2 fix: introduce `sanitize_do_app_name` helper and wire it into `do_init` so DO `app.yaml` uses hyphenated, DO-compliant names (e.g. `mkmenu_ferro` → `mkmenu-ferro`).

Purpose: DO App Platform convention is kebab-case names; underscores and dots are either illegal or unidiomatic.
Output: Helper + call site + unit tests. No template file changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122.1-deploy-scaffold-uat-fixes/SCOPE.md
@.planning/phases/122.1-deploy-scaffold-uat-fixes/122.1-CONTEXT.md
@ferro-cli/src/templates/do.rs
@ferro-cli/src/commands/do_init.rs

<interfaces>
Existing:
```rust
// ferro-cli/src/templates/do.rs (real module path: ferro_cli::templates::do_spec)
pub struct AppYamlContext<'a> {
    pub package_name: &'a str,
    pub github_repo: &'a str,
    pub region: &'a str,
    pub bins: &'a [BinEntry],
    pub env_entries: &'a [EnvEntry],
}
pub fn render_app_yaml(ctx: &AppYamlContext) -> String;
```
New public helper:
```rust
pub fn sanitize_do_app_name(pkg: &str) -> String;
```
AppYamlContext is unchanged — the caller (`do_init`) passes the sanitized string.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sanitize_do_app_name helper with unit tests</name>
  <files>ferro-cli/src/templates/do.rs</files>
  <read_first>
    - ferro-cli/src/templates/do.rs (entire file, 216 lines)
    - SCOPE.md section "2. DO app name hyphenation"
    - 122.1-CONTEXT.md decisions D-05, D-06, D-08
  </read_first>
  <behavior>
    Per D-08, these exact transformations:
    - `mkmenu_ferro` → `mkmenu-ferro`
    - `MyApp_v2` → `myapp-v2`
    - `foo__bar` → `foo-bar`  (collapse consecutive dashes)
    - `app.name` → `appname`  (drop illegal chars, no replacement dash)
    Additional sensible cases:
    - `gestiscilo` → `gestiscilo` (unchanged)
    - `Foo_Bar_Baz` → `foo-bar-baz`
    - `--foo--` → `-foo-` then collapsed → `-foo-` (keep, do not trim — SCOPE doesn't specify trimming, keep conservative)
    Actually per D-06 we: lowercase → `_` to `-` → strip non `[a-z0-9-]` → collapse consecutive `-`. No trim.
  </behavior>
  <action>
    Add, at the top of `ferro-cli/src/templates/do.rs` (near other `pub fn`), a public function:

    ```rust
    /// Sanitize a Cargo package name into a DO App Platform-compliant app name.
    /// Lowercase, `_` → `-`, drop any char not in `[a-z0-9-]`, collapse consecutive `-`.
    pub fn sanitize_do_app_name(pkg: &str) -> String {
        let mut out = String::with_capacity(pkg.len());
        let mut prev_dash = false;
        for c in pkg.chars() {
            let c = c.to_ascii_lowercase();
            let mapped = if c == '_' { Some('-') }
                else if c.is_ascii_alphanumeric() || c == '-' { Some(c) }
                else { None };
            if let Some(ch) = mapped {
                if ch == '-' {
                    if prev_dash { continue; }
                    prev_dash = true;
                } else {
                    prev_dash = false;
                }
                out.push(ch);
            }
        }
        out
    }
    ```

    Add unit tests in the existing `#[cfg(test)] mod tests` block (D-08):
    ```rust
    #[test]
    fn sanitize_do_app_name_cases() {
        assert_eq!(sanitize_do_app_name("mkmenu_ferro"), "mkmenu-ferro");
        assert_eq!(sanitize_do_app_name("MyApp_v2"), "myapp-v2");
        assert_eq!(sanitize_do_app_name("foo__bar"), "foo-bar");
        assert_eq!(sanitize_do_app_name("app.name"), "appname");
        assert_eq!(sanitize_do_app_name("gestiscilo"), "gestiscilo");
    }
    ```

    Do NOT change `render_app_yaml`, `AppYamlContext`, or any existing tests.
  </action>
  <acceptance_criteria>
    - `grep -n "pub fn sanitize_do_app_name" ferro-cli/src/templates/do.rs` returns 1 match
    - `grep -n "sanitize_do_app_name_cases" ferro-cli/src/templates/do.rs` returns 1 match
    - `cargo test -p ferro-cli --lib templates::do_spec::tests::sanitize_do_app_name_cases` passes
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p ferro-cli --lib templates::do_spec</automated>
  </verify>
  <done>Helper exists, is public, all unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire sanitize_do_app_name into do_init command and test helper</name>
  <files>ferro-cli/src/commands/do_init.rs</files>
  <read_first>
    - ferro-cli/src/commands/do_init.rs (entire file, 201 lines)
    - Task 1 output (helper signature from templates/do.rs)
  </read_first>
  <action>
    In `ferro-cli/src/commands/do_init.rs`:

    1. Extend the import line `use crate::templates::do_spec::{render_app_yaml, AppYamlContext};` to also import `sanitize_do_app_name`:
       ```rust
       use crate::templates::do_spec::{render_app_yaml, sanitize_do_app_name, AppYamlContext};
       ```

    2. In `pub fn run(...)`, after `let pkg = package_name(&root);`, insert:
       ```rust
       let app_name = sanitize_do_app_name(&pkg);
       ```
       Update the `AppYamlContext { package_name: &pkg, ... }` to `package_name: &app_name`.

       Note: `pkg` is still used for `render_ci_workflow(&CiWorkflowContext { package_name: &pkg })` and `docker_init::generate` — leave those unchanged (they need the real Cargo package name for build commands).

    3. In the test helper `fn run_for_test`, mirror the change: after `let pkg = package_name(root);`, compute `let app_name = sanitize_do_app_name(&pkg);` and pass `&app_name` as `package_name`.

    4. Add a new test (below `writes_app_yaml_with_region_and_envs`) that asserts sanitization end-to-end:
       ```rust
       #[test]
       fn writes_app_yaml_sanitizes_package_name() {
           let td = TempDir::new().unwrap();
           fs::write(
               td.path().join("Cargo.toml"),
               "[package]\nname = \"mkmenu_ferro\"\nversion = \"0.1.0\"\nedition = \"2021\"\n",
           ).unwrap();
           fs::write(td.path().join(".env.example"), "APP_URL=https://x.io\n").unwrap();
           run_for_test(td.path(), "owner/repo", "fra1", true);
           let yaml = fs::read_to_string(td.path().join(".do/app.yaml")).unwrap();
           assert!(yaml.contains("name: mkmenu-ferro"), "expected sanitized name, got:\n{yaml}");
           assert!(!yaml.contains("name: mkmenu_ferro"));
       }
       ```

    Do NOT touch the Dockerfile/CI generation or any unrelated logic.
  </action>
  <acceptance_criteria>
    - `grep -n "sanitize_do_app_name(&pkg)" ferro-cli/src/commands/do_init.rs` returns at least 2 matches (run + run_for_test)
    - `grep -n "writes_app_yaml_sanitizes_package_name" ferro-cli/src/commands/do_init.rs` returns 1 match
    - `cargo test -p ferro-cli --lib commands::do_init::tests::writes_app_yaml_sanitizes_package_name` passes
    - `cargo test -p ferro-cli --lib commands::do_init` all tests pass
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p ferro-cli --lib commands::do_init</automated>
  </verify>
  <done>do_init uses sanitized name for the app.yaml only; Dockerfile/CI still use raw package name; new test passes.</done>
</task>

</tasks>

<verification>
- cargo fmt --all -- --check
- cargo clippy -p ferro-cli --all-targets -- -D warnings
- cargo test -p ferro-cli --lib templates::do_spec commands::do_init
</verification>

<success_criteria>
- Bug 2 resolved: mkmenu_ferro generates `name: mkmenu-ferro` in app.yaml.
- Helper covered by 5 transformation test cases.
- No change to Dockerfile rendering.
</success_criteria>

<output>
After completion, create `.planning/phases/122.1-deploy-scaffold-uat-fixes/122.1-02-SUMMARY.md`
</output>
