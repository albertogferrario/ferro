---
phase: 122.1-deploy-scaffold-uat-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-cli/src/deploy/env_example.rs
autonomous: true
requirements: [D-01, D-02, D-03, D-04]
must_haves:
  truths:
    - "parse_env_example strips trailing `\\s+#comment` from unquoted values"
    - "`#` characters inside quoted values are preserved verbatim"
    - "`#` as first non-whitespace char after `=` with no leading space is preserved as literal value"
    - "Unit tests cover all four comment scenarios and still pass existing tests"
  artifacts:
    - path: "ferro-cli/src/deploy/env_example.rs"
      provides: "parse_env_example with inline comment stripping"
      contains: "fn strip_inline_comment"
  key_links:
    - from: "parse_env_example"
      to: "strip_inline_comment"
      via: "called on unquoted trimmed value before push"
      pattern: "strip_inline_comment"
---

<objective>
Bug 1 fix: strip trailing inline `# comment` from unquoted `.env.example` values while preserving `#` inside quoted values and leading `#` with no whitespace.

Purpose: Real `.env.example` files commonly annotate defaults (`APP_ENV=local # local, staging, production`). Current parser captures the comment into the value, polluting downstream `app.yaml` and Dockerfile generation.
Output: Updated `env_example.rs` with comment stripper + unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122.1-deploy-scaffold-uat-fixes/SCOPE.md
@.planning/phases/122.1-deploy-scaffold-uat-fixes/122.1-CONTEXT.md
@ferro-cli/src/deploy/env_example.rs

<interfaces>
Current public API (unchanged):
```rust
pub struct EnvEntry { pub key: String, pub value: String }
pub fn parse_env_example(content: &str) -> Vec<EnvEntry>
```
Only the value-parsing path changes. No signature changes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement inline comment stripping in parse_env_example</name>
  <files>ferro-cli/src/deploy/env_example.rs</files>
  <read_first>
    - ferro-cli/src/deploy/env_example.rs (entire file, 80 lines)
    - SCOPE.md section "1. `.env.example` inline-comment stripping"
  </read_first>
  <behavior>
    Per D-01..D-04:
    - `APP_ENV=local          # local, staging` → value `"local"` (D-01)
    - `KEY="foo#bar"` → value `"\"foo#bar\""` (D-02, quoted preserved)
    - `KEY='a#b'` → value `"'a#b'"` (D-02, single quoted preserved)
    - `KEY=#onlyhash` → value `"#onlyhash"` (D-03, no leading space before `#`)
    - `KEY=val# nospace` → value `"val# nospace"` kept (no leading whitespace before `#`, treated as part of value — conservative; matches D-03 spirit)
    - `KEY=val  # comment` → value `"val"` (D-01)
    - Existing tests: `skips_comments_and_blank_lines`, `preserves_quotes_verbatim`, `splits_on_first_equals_only`, `skips_lines_without_equals`, `trims_key_and_value` MUST still pass.
  </behavior>
  <action>
    Add a private helper `fn strip_inline_comment(value: &str) -> &str` that:
    1. If `value` starts with `"` or `'`: return as-is (quoted — D-02).
    2. Otherwise scan for the first `#` that is preceded by at least one whitespace char (space or tab). If found, truncate at that position and `trim_end` the result. If not found, return `value` as-is (covers D-03: leading `#` and `#` without leading whitespace).

    Wire in `parse_env_example`:
    - After `let Some((key, value)) = raw.split_once('=')`, trim the value as before, then pass through `strip_inline_comment` before `.to_string()`.

    Implementation sketch (per D-01..D-04):
    ```rust
    fn strip_inline_comment(value: &str) -> &str {
        let bytes = value.as_bytes();
        if bytes.first() == Some(&b'"') || bytes.first() == Some(&b'\'') {
            return value; // D-02
        }
        let mut prev_ws = false;
        for (i, b) in bytes.iter().enumerate() {
            if *b == b'#' && prev_ws {
                return value[..i].trim_end();
            }
            prev_ws = *b == b' ' || *b == b'\t';
        }
        value
    }
    ```

    Then in parse_env_example:
    ```rust
    let trimmed_val = value.trim();
    let stripped = strip_inline_comment(trimmed_val);
    out.push(EnvEntry { key, value: stripped.to_string() });
    ```

    Add 4 new unit tests in the existing `#[cfg(test)] mod tests` block (D-04):
    - `strips_trailing_inline_comment`: `"APP_ENV=local          # local, staging"` → `("APP_ENV", "local")`
    - `preserves_hash_inside_quoted_value`: `"KEY=\"foo#bar\""` → `("KEY", "\"foo#bar\"")`
    - `preserves_hash_with_no_leading_space`: `"KEY=#literal"` → `("KEY", "#literal")`
    - `strips_multiple_spaces_before_comment`: `"APP_DEBUG=true      # Set false in production"` → `("APP_DEBUG", "true")`

    Do NOT touch unrelated logic. Do NOT refactor the existing parser loop beyond inserting the strip call.
  </action>
  <acceptance_criteria>
    - `grep -n "fn strip_inline_comment" ferro-cli/src/deploy/env_example.rs` returns 1 match
    - `grep -n "strips_trailing_inline_comment\|preserves_hash_inside_quoted_value\|preserves_hash_with_no_leading_space\|strips_multiple_spaces_before_comment" ferro-cli/src/deploy/env_example.rs` returns 4 matches
    - `cargo test -p ferro-cli --lib deploy::env_example` passes all tests (old + 4 new)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p ferro-cli --lib deploy::env_example -- --nocapture</automated>
  </verify>
  <done>All parse_env_example tests pass including 4 new comment-handling tests; existing tests still green.</done>
</task>

</tasks>

<verification>
- cargo fmt --all -- --check
- cargo clippy -p ferro-cli --all-targets -- -D warnings
- cargo test -p ferro-cli --lib deploy::env_example
</verification>

<success_criteria>
- Bug 1 resolved: inline comments stripped from unquoted values, quoted `#` preserved, bare `#` preserved.
- No regression in existing env_example tests.
- No changes outside `ferro-cli/src/deploy/env_example.rs`.
</success_criteria>

<output>
After completion, create `.planning/phases/122.1-deploy-scaffold-uat-fixes/122.1-01-SUMMARY.md`
</output>
