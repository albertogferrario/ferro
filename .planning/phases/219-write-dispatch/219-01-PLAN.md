---
phase: 219-write-dispatch
plan: 01
type: execute
wave: 1
depends_on: ["219-00"]
files_modified:
  - ferro-mcp-server/src/write_dispatch.rs
  - ferro-mcp-server/src/jsonrpc.rs
  - ferro-mcp-server/src/lib.rs
autonomous: true
requirements: [AMCP-04]
must_haves:
  truths:
    - "dispatch_write re-evaluates every action precondition via the live GuardEvaluator before execution; a failing guard returns Err(GuardFailed) and the executor never runs"
    - "A second call with the same (tenant_id, idempotency_key) replays the stored result without re-executing the action"
    - "handle_write_call routes non-list_ tool calls behind the 217 scope gate, validates inputs, dispatches, and audits via ferro-audit"
    - "Every write result (success and guard-denied/error) parses as rmcp::model::CallToolResult with the correct is_error flag"
    - "dispatch_write contains a documented Phase-220 confirmation seam at transition_trigger.is_some() with no ferro-ai dependency"
  artifacts:
    - path: "ferro-mcp-server/src/write_dispatch.rs"
      provides: "dispatch_write (guard loop, idempotency, audit, seam) + handle_write_call routing + write_tool_error_result helper"
      contains: "pub async fn dispatch_write"
    - path: "ferro-mcp-server/src/jsonrpc.rs"
      provides: "handle_tools_call routes is_write_tool to handle_write_call behind the scope gate"
      contains: "handle_write_call"
  key_links:
    - from: "ferro-mcp-server/src/jsonrpc.rs"
      to: "ferro-mcp-server/src/write_dispatch.rs"
      via: "handle_tools_call routes is_write_tool to handle_write_call"
      pattern: "handle_write_call"
    - from: "ferro-mcp-server/src/write_dispatch.rs"
      to: "ferro-audit"
      via: "AuditEntry::record builder chain after execution"
      pattern: "AuditEntry::record"
    - from: "dispatch_write guard loop"
      to: "WriteDispatcher.guard_evaluator (live DB)"
      via: "per-precondition call, never ctx.evaluated_guards"
      pattern: "guard_evaluator"
---

<objective>
Implement the framework write-dispatch machinery. Fill the Wave 0 stubs: the `dispatch_write` pipeline (live guard re-evaluation → idempotency check → seam → execute → store → audit), the `handle_write_call` router (scope gate stays in front, ActionDef resolution, input validation, structured result envelopes), and route write-tool calls from `handle_tools_call`. Turn the Wave 0 RED tests (SC#1, SC#3, SC#5) GREEN.

Purpose: This is the security spine of v15.0 — server-side guard re-evaluation at call time is the only structural prevention of the guard-bypass privilege-escalation class (PITFALLS §2). The agent is never trusted.
Output: `ferro-mcp-server` write path complete and unit/integration tested; SC#1, SC#3, SC#5 GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/219-write-dispatch/219-CONTEXT.md
@.planning/phases/219-write-dispatch/219-RESEARCH.md
@.planning/phases/219-write-dispatch/219-PATTERNS.md
@.planning/phases/219-write-dispatch/219-VALIDATION.md
@.planning/phases/219-write-dispatch/219-00-SUMMARY.md

<interfaces>
<!-- Contracts defined in Wave 0 (219-00). Implement against these exactly. -->

dispatch_write / handle_write_call / WriteDispatcher: see 219-00-PLAN.md <interfaces> block (already in write_dispatch.rs).

ferro-audit builder chain (ferro-audit/src/entry.rs, re-exported from ferro_audit root):
```rust
AuditEntry::record(action_str)            // e.g. format!("mcp.action.{}", action.name)
    .tenant(tenant_id.to_string())
    .actor(AuditActor::User(tenant_id.to_string()))
    .target(AuditTarget::new(kind_str, id_str))  // kind=action.name, id=record id
    .after(result_json)
    .reason(reason_str)
    .write(db).await?;                     // returns Result<_, ferro_audit::Error>
```
before() is optional — omit it (call-level audit, not a delta).

CallToolResult::structured (ferro-mcp-server, used in jsonrpc.rs:115-123): always sets is_error: Some(false). Use for SUCCESS only.

Existing handle_tools_call (jsonrpc.rs:54) signature — a new `dispatcher: &WriteDispatcher` param must be threaded:
```rust
pub async fn handle_tools_call(
    call_params: Value, services: &[ServiceDef], db: &DatabaseConnection,
    tenant_id: Option<i64>, ctx: &McpContext, dispatcher: &WriteDispatcher,
) -> Value
```
Scope gate to KEEP in front (jsonrpc.rs:68-82): is_write_tool && key_scope=="read" → -32603 BEFORE any dispatch.

ActionDef (ferro-projections/src/action.rs): name, inputs: Vec<InputDef>, preconditions: Vec<String>, transition_trigger: Option<String>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dispatch_write pipeline — live guard re-eval, idempotency, audit, 220 seam</name>
  <files>ferro-mcp-server/src/write_dispatch.rs</files>
  <read_first>
    - ferro-mcp-server/src/write_dispatch.rs (Wave 0 stubs + RED tests — fill the bodies)
    - ferro-mcp-server/src/dispatch.rs lines 99-167 (tenant fail-closed discipline to mirror; tenant NEVER from payload)
    - ferro-audit/src/entry.rs lines 25-57 (AuditEntry builder), ferro-audit/src/lib.rs (re-exports: AuditEntry, AuditActor, AuditTarget)
    - ferro-mcp-oauth/src/migration.rs (MigrationMcpIdempotencyKeys — the table dispatch_write reads/writes)
    - 219-PATTERNS.md §"write_dispatch.rs" (guard loop, idempotency, audit, seam — verbatim)
    - 219-RESEARCH.md §"Research Finding 2" (guard pipeline; contradiction note: ARCHITECTURE.md diagram shows ctx.evaluated_guards at call time — IGNORE it, D-02 mandates the live GuardEvaluator), §"Research Finding 4" (idempotency SQL: INSERT OR IGNORE / ON CONFLICT DO NOTHING), §"Research Finding 5" (audit reuse), §"Research Finding 8" (220 seam)
  </read_first>
  <action>
    Implement `dispatch_write` in `ferro-mcp-server/src/write_dispatch.rs` with the pipeline in EXACTLY this order (D-07):
    1. **Guard re-evaluation (D-02, T-219-02 — load-bearing):** loop over `action.preconditions` (Vec<String>); for each, call `(dispatcher.guard_evaluator)(guard_name, tenant_id, inputs, db).await`. Map any error to `Error::GuardFailed`. If it returns `Ok(false)`, return `Err(Error::GuardFailed(format!("precondition '{}' not met", guard_name)))`. CRITICAL: read ONLY from the guard_evaluator callback (live DB). NEVER consult `ctx.evaluated_guards` — that is the 218 list-time visibility cache, not the authorization gate. Add a comment stating this explicitly.
    2. **Idempotency check (D-04, T-219-03):** `let key = inputs.get("idempotency_key").and_then(|v| v.as_str());` If `Some(key)`, call `lookup_idempotency(tenant_id, key, db)` (SELECT result FROM mcp_idempotency_keys WHERE tenant_id=? AND idempotency_key=? — composite scope, never key alone). If a stored result exists, return it (replay — skip execute, skip audit, skip store).
    3. **D-08 confirmation seam:** insert a comment block exactly: `// D-08 SEAM: Phase 220 inserts confirmation gating here for destructive actions (transition_trigger.is_some()). In 219: pass through. Do NOT wire ferro-ai / ConfirmationStore here.` Pass through.
    4. **Execute (D-01):** `let result = (dispatcher.executor)(&action.name, inputs, tenant_id, db).await?;` (executor Err propagates → handle_write_call maps to isError result; audit on failure handled per §6).
    5. **Store idempotency (D-04):** if `Some(key)`, `store_idempotency(tenant_id, key, &result, db)` — use `INSERT OR IGNORE` (SQLite) / `ON CONFLICT (tenant_id, idempotency_key) DO NOTHING` (Postgres) for concurrency safety (PITFALLS §5). Detect backend via `db.get_database_backend()`.
    6. **Audit (D-05, SC#4):** `let record_id = inputs.get("id").map(|v| v.to_string()).unwrap_or_default();` then the `AuditEntry::record(format!("mcp.action.{}", &action.name))` builder chain (tenant, actor=User(tenant_id), target=AuditTarget::new(&action.name, record_id), after=result.clone(), write(db)). Map ferro_audit error to `Error::Database`.
    7. Return `Ok(result)`.

    Implement the private helpers `lookup_idempotency(tenant_id: i64, key: &str, db) -> Result<Option<Value>>` (SELECT, deserialize TEXT→Value) and `store_idempotency(tenant_id, key, &Value, db) -> Result<()>` (INSERT OR IGNORE). Use raw `sea_orm::Statement` like `dispatch.rs` does, OR a small SeaORM entity — your discretion; raw Statement matches the dispatch.rs analog. Store result as JSON TEXT.
  </action>
  <verify>
    <automated>cargo test -p ferro-mcp-server guard_denied_at_call_time idempotent_replay_does_not_re_execute</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "guard_evaluator" ferro-mcp-server/src/write_dispatch.rs` AND the guard loop precedes the executor call (verify by reading order)
    - dispatch_write does NOT reference `ctx.evaluated_guards` or `evaluated_guards` (`grep -L "evaluated_guards" ferro-mcp-server/src/write_dispatch.rs` — must NOT match)
    - `grep -q "D-08 SEAM" ferro-mcp-server/src/write_dispatch.rs` AND `grep -q "transition_trigger" ferro-mcp-server/src/write_dispatch.rs`
    - `grep -q "AuditEntry::record" ferro-mcp-server/src/write_dispatch.rs`
    - idempotency lookup scopes by BOTH tenant_id AND idempotency_key (verify the SQL/filter references both)
    - `grep -qE "INSERT OR IGNORE|ON CONFLICT" ferro-mcp-server/src/write_dispatch.rs`
    - NO ferro-ai dependency: `grep -L "ferro_ai\|ferro-ai\|ConfirmationStore" ferro-mcp-server/src/write_dispatch.rs` (must NOT match)
    - `cargo test -p ferro-mcp-server guard_denied_at_call_time` GREEN; `cargo test -p ferro-mcp-server idempotent_replay_does_not_re_execute` GREEN
  </acceptance_criteria>
  <done>dispatch_write re-evaluates guards live before execution (SC#1 GREEN), replays on idempotency-key match without re-executing (SC#3 GREEN), audits via ferro-audit, and leaves a clean 220 seam with no ferro-ai dep. ctx.evaluated_guards is never consulted at call time.</done>
</task>

<task type="auto">
  <name>Task 2: handle_write_call router + structured result envelopes + jsonrpc routing</name>
  <files>ferro-mcp-server/src/write_dispatch.rs, ferro-mcp-server/src/jsonrpc.rs, ferro-mcp-server/src/lib.rs</files>
  <read_first>
    - ferro-mcp-server/src/jsonrpc.rs lines 54-133 (handle_tools_call: scope gate 68-82, -32601 placeholder 84-92, CallToolResult::structured 115-123, regression test 193-235)
    - app/src/controllers/mcp.rs lines 35-47 (make_tool_deny_response — the isError:true content[] envelope precedent)
    - 219-PATTERNS.md §"jsonrpc.rs" (routing insertion point, isError result helper) and §"Shared Patterns → Structured Result Construction"
    - 219-RESEARCH.md §"Research Finding 6" (result shapes for all 5 outcomes; CallToolResult::structured hard-codes is_error:false so errors need a separate helper) and §"Research Finding 7" (envelope mapping table: ActionNotFound→-32601 error key; GuardFailed/Validation/Executor→result key isError:true)
  </read_first>
  <action>
    1. Implement `handle_write_call` in `write_dispatch.rs` per the `<interfaces>` signature:
       - Read `tool_name = call_params["name"]`. Unwrap `tenant_id` from `Option<i64>`: `None` → return `json!({ "error": { "code": -32603, "message": "auth: tenant required" } })` (fail-closed — writes always need an authenticated tenant, mirroring dispatch.rs).
       - Resolve the ActionDef: `find_action(services, tool_name)` searching `services.iter()` (mcp_exposed only) `.flat_map(|s| &s.actions)` for `action.name == tool_name`. `None` → `json!({ "error": { "code": -32601, "message": "Method not found" } })` (ActionNotFound → protocol error, error key).
       - `let args = call_params.get("arguments").cloned().unwrap_or(json!({}));`
       - Validate inputs against `action.inputs` (required InputDefs present; basic type check). On failure → `write_tool_error_result` with `error_kind:"validation"` (isError:true, result key).
       - Call `dispatch_write(action, &args, tenant_id, db, dispatcher).await` and map:
         - `Ok(result)` → `CallToolResult::structured(json!({ "status": "ok", "action": action.name, "result": result }))` wrapped as `json!({ "result": tool_result })` (is_error:false).
         - `Err(Error::GuardFailed(msg))` → `write_tool_error_result(json!({ "error_kind": "guard_denied", "message": msg }))`.
         - `Err(e)` (executor/Validation/etc.) → `write_tool_error_result(json!({ "error_kind": "execution_error", "message": e.to_string() }))`.
       - On guard-denied AND executor-error outcomes, fire an audit entry with `after = json!({ "denied": true, ... })` so denials leave a forensic trail (PITFALLS §2 / D-05 scope). (Success audit already fires inside dispatch_write.) Use your judgment to avoid double-auditing success.
    2. Implement `write_tool_error_result(payload: Value) -> Value` helper: returns `json!({ "result": { "content": [{"type":"text","text": <message-from-payload>}], "isError": true, "structuredContent": payload } })`. This is the ONLY error-result constructor — NO bare hand-built content[] elsewhere (D-06). CallToolResult::structured cannot be used (it hard-codes is_error:false).
    3. In `jsonrpc.rs` `handle_tools_call`: add `dispatcher: &WriteDispatcher` as the new last parameter. After the scope gate (keep lines 68-82 EXACTLY in front), BEFORE the service lookup, insert:
       ```rust
       if is_write_tool {
           return handle_write_call(call_params, services, db, tenant_id, ctx, dispatcher).await;
       }
       ```
       The read path (list_ tools) below is unchanged.
    4. Re-export `handle_write_call` from lib.rs if not already.
    5. Update any in-crate test/call site of `handle_tools_call` to pass a dispatcher (a no-op test dispatcher).
  </action>
  <verify>
    <automated>cargo test -p ferro-mcp-server write_tool_result_parses_as_valid_mcp_content tools_call_result_parses_as_valid_mcp_content</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "write_tool_error_result" ferro-mcp-server/src/write_dispatch.rs`
    - `grep -q "handle_write_call" ferro-mcp-server/src/jsonrpc.rs` (routing present)
    - scope gate (`key_scope == "read"`) still appears BEFORE the `is_write_tool` route in jsonrpc.rs (verify order by reading)
    - success path uses `CallToolResult::structured`; NO hand-built `"content"` array exists outside `write_tool_error_result` (`grep -n '"content"' ferro-mcp-server/src/write_dispatch.rs` shows only the helper)
    - `cargo test -p ferro-mcp-server write_tool_result_parses_as_valid_mcp_content` GREEN (SC#5: success is_error:false, guard-denied is_error:true both parse as CallToolResult)
    - existing `tools_call_result_parses_as_valid_mcp_content` still GREEN (read path unbroken)
  </acceptance_criteria>
  <done>handle_write_call routes behind the scope gate, validates inputs, dispatches, and produces structured results for all outcomes; every result parses as CallToolResult (SC#5 GREEN); read path unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 1 gate — ferro-mcp-server + ferro-mcp-oauth tests + clippy green</name>
  <files>ferro-mcp-server/src/write_dispatch.rs, ferro-mcp-server/src/jsonrpc.rs</files>
  <read_first>
    - 219-VALIDATION.md §"Sampling Rate" (per-wave: cargo test --all-features + clippy)
    - ferro CLAUDE.md Testing & Linting (exact commands, -D warnings)
  </read_first>
  <action>
    Run the wave-boundary gate for the framework crates and fix any failure or warning:
    `cargo test -p ferro-mcp-server -p ferro-mcp-oauth` and `cargo clippy -p ferro-mcp-server -p ferro-mcp-oauth --all-targets -- -D warnings`.
    Confirm the three Wave 0 RED tests (guard_denied_at_call_time, idempotent_replay_does_not_re_execute, write_tool_result_parses_as_valid_mcp_content) are now GREEN. Fix any clippy lint (no `#[allow]` suppression of real issues). Keep the workspace compiling — `handle_tools_call`'s new `dispatcher` param means the app call site (app/src/controllers/mcp.rs:177) will NOT compile yet; that is Wave 2's job. If `cargo test --all-features` is needed to confirm no other crate broke, run it, but the app-crate break is expected and resolved in Plan 02 — scope this gate to `-p ferro-mcp-server -p ferro-mcp-oauth`.
  </action>
  <verify>
    <automated>cargo test -p ferro-mcp-server -p ferro-mcp-oauth && cargo clippy -p ferro-mcp-server -p ferro-mcp-oauth --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p ferro-mcp-server -p ferro-mcp-oauth` exits 0 (all framework write-dispatch tests GREEN)
    - `cargo clippy -p ferro-mcp-server -p ferro-mcp-oauth --all-targets -- -D warnings` exits 0
    - SC#1, SC#3, SC#5 tests GREEN
  </acceptance_criteria>
  <done>ferro-mcp-server and ferro-mcp-oauth tests and clippy are green; SC#1/#3/#5 GREEN. Framework machinery complete; app wiring (Plan 02) remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP agent → tools/call → handle_write_call | Untrusted agent input; tenant_id from authenticated principal only (never from arguments payload) |
| handle_write_call → dispatch_write guard loop → live GuardEvaluator | The authorization gate; reads live DB state, independent of the 218 tools/list visibility filter |
| dispatch_write → idempotency table | Replay boundary scoped by (tenant_id, idempotency_key) composite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-219-02 | Elevation of Privilege (guard bypass) | dispatch_write guard loop (Task 1) | mitigate | Every `action.precondition` re-evaluated via the live `GuardEvaluator` BEFORE the executor; fail-closed on false/error; `ctx.evaluated_guards` NEVER consulted. Test `guard_denied_at_call_time` proves a direct tools/call on a guarded action is rejected even though tools/list was never invoked (SC#1). |
| T-219-03 | Tampering (retry double-write) | idempotency check/store (Task 1) | mitigate | `(tenant_id, idempotency_key)` lookup before execute + `INSERT OR IGNORE`/`ON CONFLICT DO NOTHING` store; replay returns stored result without re-executing. Test `idempotent_replay_does_not_re_execute` asserts exactly one execution (SC#3). |
| T-219-01 | Elevation of Privilege (cross-tenant) | tenant_id sourcing + idempotency scope (Task 1/2) | mitigate | `tenant_id` is the unwrapped authenticated principal, never the payload; `None` tenant → fail-closed -32603; idempotency lookup scoped by tenant_id so tenant B cannot replay tenant A's result. The executor-level `find_for_tenant` denial is wired+tested in Plan 02 (SC#2). |
| Info Disclosure (prompt injection) | structured result echoing record text (Task 2) | mitigate | Results use `CallToolResult::structured`/`write_tool_error_result` (typed structuredContent), never interpolated record text as instructions (PITFALLS §3). |
</threat_model>

<verification>
- SC#1: `cargo test -p ferro-mcp-server guard_denied_at_call_time` GREEN — failing guard → Err(GuardFailed), executor never runs.
- SC#3: `cargo test -p ferro-mcp-server idempotent_replay_does_not_re_execute` GREEN — two identical calls, one execution.
- SC#5: `cargo test -p ferro-mcp-server write_tool_result_parses_as_valid_mcp_content` GREEN — every result parses as CallToolResult with correct is_error.
- `cargo clippy -p ferro-mcp-server -p ferro-mcp-oauth --all-targets -- -D warnings` clean.
- dispatch_write does not reference ctx.evaluated_guards and has no ferro-ai dependency.
</verification>

<success_criteria>
- dispatch_write pipeline complete: live guard re-eval → idempotency → 220 seam → execute → store → audit.
- handle_write_call routes behind the scope gate with ActionDef resolution, input validation, and structured envelopes for all outcomes.
- write_tool_error_result is the sole error-result constructor; no bare content[] arrays (D-06).
- SC#1, SC#3, SC#5 GREEN. Framework crates' clippy clean.
</success_criteria>

<output>
After completion, create `.planning/phases/219-write-dispatch/219-01-SUMMARY.md`
</output>
