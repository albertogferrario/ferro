---
phase: 211
plan: 02
type: execute
wave: 2
depends_on: [211-01]
files_modified:
  - ferro-cli/tests/fixtures/benchmark/RESULTS.md
  - .planning/phases/211-comp-04-time-to-working-app-benchmark/211-WEAKNESSES.md
autonomous: false
requirements: [COMP-04]
user_setup:
  - service: docker
    why: "The cold-cache run (SC#3) requires a Docker daemon + a clean container with no pre-installed Rust toolchain and no warmed cargo cache; the autonomous environment has no Docker daemon and the run is disk/CPU/time-heavy and needs network to crates.io"
must_haves:
  truths:
    - "RESULTS.md contains at least one row labeled cache: cold, populated from a real Docker run of the committed Dockerfile (SC#3)."
    - "RESULTS.md carries the full SC#4 environment specification with real values: Rust toolchain version, cache state, host machine class, agent-assistance level (manual commands), per-step wall-clock breakdown, total."
    - "A Discovered Weaknesses finding names at least one real thing (empty fails the phase — SC#5)."
  artifacts:
    - path: "ferro-cli/tests/fixtures/benchmark/RESULTS.md"
      provides: "committed result doc with real cold-cache numbers + full env spec"
      contains: "cold"
    - path: ".planning/phases/211-comp-04-time-to-working-app-benchmark/211-WEAKNESSES.md"
      provides: "SC#5 discovered-weaknesses finding (phase-close gate)"
      min_lines: 8
  key_links:
    - from: "the cold-cache Docker run"
      to: "RESULTS.md"
      via: "developer copies per-step + total numbers into the cache: cold row"
      pattern: "cold"
---

<objective>
Produce and commit the FIRST real cold-cache number: the developer builds the committed Dockerfile
(211-01) and runs the five-step benchmark inside a clean container (no toolchain, no cargo cache),
then copies the per-step + total wall-clock into RESULTS.md as a row labeled cache: cold and fills
the full SC#4 environment specification with real values. Finally, author/finalize the SC#5
Discovered Weaknesses finding.

Purpose: this is the only non-autonomous plan — the autonomous environment has no Docker daemon and
the cold run is disk/CPU/time-heavy and needs network to crates.io (mirrors 210-04's human-action
checkpoint). The cold number is the honest first-time-experience figure (SC#3) and the only one
external docs should quote. SC#5 (a named real weakness) is a hard phase-close gate: an empty
section fails the phase.

Output: RESULTS.md with the real cold-cache row + full env spec; 211-WEAKNESSES.md with >=1 concrete finding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/211-comp-04-time-to-working-app-benchmark/211-CONTEXT.md
@.planning/phases/211-comp-04-time-to-working-app-benchmark/211-RESEARCH.md
@.planning/phases/211-comp-04-time-to-working-app-benchmark/211-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Developer runs the cold-cache Docker benchmark (no autonomous path — needs Docker daemon)</name>
  <read_first>
    - ferro-cli/tests/fixtures/benchmark/Dockerfile (committed by 211-01 — the cold-cache build)
    - ferro-cli/tests/fixtures/benchmark/RESULTS.md (committed by 211-01 — the env-spec template to fill)
    - .planning/phases/211-comp-04-time-to-working-app-benchmark/211-RESEARCH.md (Docker Cold-Cache Strategy L414-472 — run commands + cargo install caveat; Pitfall 3 disk; Pitfall 6 --network none must NOT be used)
  </read_first>
  <files>ferro-cli/tests/fixtures/benchmark/Dockerfile (read), ferro-cli/tests/fixtures/benchmark/RESULTS.md (read)</files>
  <what-built>
    211-01 committed a Dockerfile (debian:bookworm-slim + rustup, no warm toolchain, no cargo cache)
    whose CMD runs the same five steps as the Rust benchmark, printing per-step seconds. It cannot run
    in the autonomous environment (no Docker daemon, by design — D-02).
  </what-built>
  <action>
    MANUAL (checkpoint:human-action — no autonomous path). This step requires a Docker daemon that
    does not exist in the autonomous environment, and the run is disk/CPU/time-heavy (builds a full
    scaffolded app + ~200 crates) and needs network to crates.io. The executor cannot perform it.
    The developer runs the cold-cache benchmark locally per <how-to-verify>, then records the numbers
    into RESULTS.md. There is no CLI/API the executor can substitute. Do NOT fabricate or stub the
    cold numbers.
  </action>
  <how-to-verify>
    Run locally (this is the manual step):
    ```
    df -h .            # Pitfall 3: ensure disk headroom; the cold build needs GBs
    docker build -t ferro-bench ferro-cli/tests/fixtures/benchmark/
    docker run --rm ferro-bench 2>&1 | tee cold-cache-run.txt
    ```
    Notes:
    - Do NOT pass --network none (Pitfall 6): cargo build resolves ferro-rs from crates.io.
    - cargo install ferro-cli is in a build LAYER (not timed) — the printed steps measure scaffolding,
      not CLI install (RESEARCH.md A3). Record the resolved ferro-rs version (printed during cargo build,
      or check `cargo search ferro-rs`).
    Expected output: the container prints "Step 1 ferro new: Ns" ... "Step 5 cargo build: Ns" and exits 0.
    Then proceed to Task 2 to copy the numbers into RESULTS.md. Keep cold-cache-run.txt locally
    (do NOT commit it; the numbers go into RESULTS.md).
  </how-to-verify>
  <verify>
    <automated>MISSING — manual cold-cache Docker run (no Docker daemon in autonomous env). Verified by Task 2's grep checks on RESULTS.md once the numbers are committed.</automated>
  </verify>
  <done>The cold-cache container builds and the five-step run completes (exit 0), printing per-step + total seconds; numbers captured for Task 2.</done>
  <resume-signal>Type "cold run complete" once the Docker run finished (exit 0) and you have the per-step + total numbers + the resolved ferro-rs version.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Fill RESULTS.md with the real cold-cache row + full env spec</name>
  <read_first>
    - ferro-cli/tests/fixtures/benchmark/RESULTS.md (the template committed by 211-01 — replace TBD cells)
    - .planning/phases/211-comp-04-time-to-working-app-benchmark/211-RESEARCH.md (RESULTS.md Schema L476-520 — the exact env-spec field set)
  </read_first>
  <files>ferro-cli/tests/fixtures/benchmark/RESULTS.md</files>
  <action>
    Replace the TBD placeholder cells in ferro-cli/tests/fixtures/benchmark/RESULTS.md with the real
    values from Task 1's cold run. Specifically:
    - Add/complete a row or labeled block with cache state = cold (literal token "cold" present), carrying
      the per-step (1, 2, 3a, 3b, 3c, 4) + Total wall-clock from the Docker run.
    - Fill the environment-spec table with real values: Rust toolchain version (from the container's
      rustc), ferro-rs version (resolved during the cold build), Host machine class (the dev host class),
      CPU cores, Memory, Disk free at run time, Agent-assistance level = manual commands (D-06), Date.
    - Keep the Notes section (cold via Docker; warm via FERRO_BENCH=1 gated test; CI threshold deferred D-07).
    - Optionally also add a warm row if the developer ran the gated warm benchmark
      (FERRO_BENCH=1 cargo test -p ferro-cli --test benchmark_new_project -- --ignored --nocapture);
      the cold row is the mandatory committed one (SC#3).
    Do NOT remove the seeded Discovered Weaknesses content from 211-01; Task 3 extends it.
  </action>
  <verify>
    <automated>grep -qi 'cold' ferro-cli/tests/fixtures/benchmark/RESULTS.md && grep -q 'Rust toolchain' ferro-cli/tests/fixtures/benchmark/RESULTS.md && grep -q 'Agent-assistance' ferro-cli/tests/fixtures/benchmark/RESULTS.md && test "$(grep -c 'TBD' ferro-cli/tests/fixtures/benchmark/RESULTS.md)" = 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - RESULTS.md contains a row/block labeled cache: cold (grep -i 'cold' matches) with real per-step + total numbers
    - All env-spec fields have real values (no remaining "TBD"): grep -c 'TBD' RESULTS.md returns 0
    - grep matches each of: Rust toolchain, ferro-rs version, Cache state, Host machine class, CPU cores, Memory, Disk free, Agent-assistance, Date
    - Agent-assistance level reads manual commands (D-06)
    - The Per-Step breakdown has numeric values for steps 1, 2, 3a, 3b, 3c, 4, Total
  </acceptance_criteria>
  <done>RESULTS.md carries the real cold-cache row and the complete SC#4 environment specification; no TBD cells remain.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize the SC#5 Discovered Weaknesses finding</name>
  <read_first>
    - ferro-cli/tests/fixtures/benchmark/RESULTS.md (the cold numbers + the seeded weakness from 211-01)
    - .planning/phases/211-comp-04-time-to-working-app-benchmark/211-RESEARCH.md (Critical Findings L301-336: make:model naming, crates.io dep; Open Questions; RESULTS.md weakness candidates L510-513)
    - .planning/ROADMAP.md (Phase 211 SC#5 — empty section FAILS the phase)
    - .planning/phases/210-comp-03-agent-success-rate-harness/210-WEAKNESSES.md (sibling format reference)
  </read_first>
  <files>.planning/phases/211-comp-04-time-to-working-app-benchmark/211-WEAKNESSES.md, ferro-cli/tests/fixtures/benchmark/RESULTS.md</files>
  <action>
    Write .planning/phases/211-comp-04-time-to-working-app-benchmark/211-WEAKNESSES.md naming at least
    ONE real, concrete finding — NOT a placeholder. The 211-01 seed (make:model vs make:scaffold naming
    mismatch) already qualifies; strengthen it and add findings the cold run surfaced. Candidates:
      - cargo build dominates total time by an order of magnitude over the CLI steps (cite the real
        cold numbers: step 5 vs steps 1-4),
      - the scaffold's crates.io ferro-rs dependency means the cold "first-time" number is gated on
        crates.io network + a published-version match (RESEARCH Critical Finding L319-336),
      - unhappy paths not measured (scaffold into an existing dir, duplicate entity name, make:scaffold
        before make:auth, no ferro db:migrate timing),
      - the make:model -> make:scaffold spec/impl naming mismatch (SC#2 wording vs reality).
    For each finding: state what was observed (cite the cold numbers or the verified fact), and a
    one-line hypothesis/implication. Keep the voice neutral/architectural (CLAUDE.md repo-voice).
    This is the SC#5 phase-close gate. Also append a one-line pointer in RESULTS.md's Discovered
    Weaknesses section to 211-WEAKNESSES.md so the apparatus doc and the phase finding stay linked.
  </action>
  <verify>
    <automated>test -s .planning/phases/211-comp-04-time-to-working-app-benchmark/211-WEAKNESSES.md && test "$(wc -l < .planning/phases/211-comp-04-time-to-working-app-benchmark/211-WEAKNESSES.md)" -ge 8 && ! grep -qiE 'TBD|to be filled|<finding>' .planning/phases/211-comp-04-time-to-working-app-benchmark/211-WEAKNESSES.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 211-WEAKNESSES.md exists and is >= 8 lines
    - It names at least one CONCRETE finding (a specific observed fact or cited cold number + a one-line implication), not a placeholder
    - It contains no TODO/placeholder text (TBD / to be filled / <finding>)
    - At least one finding references actual cold-run numbers or a verified fact (make:model absence, crates.io dep) checkable against the committed files
    - RESULTS.md Discovered Weaknesses section links to 211-WEAKNESSES.md
  </acceptance_criteria>
  <done>SC#5 satisfied: a real, concrete discovered-weakness finding is committed and linked from RESULTS.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker daemon -> host | The cold run builds + runs a container on the developer host |
| crates.io -> cold container build | cargo build inside the cold container resolves ferro-rs + transitive deps over the network |
| developer env -> committed RESULTS.md | The developer copies run output into a committed (public) file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-211-06 | Information disclosure | committed RESULTS.md / cold-cache-run.txt | mitigate | RESULTS.md carries only timing numbers + env spec (no secrets this phase); cold-cache-run.txt is NOT committed (Task 1 how-to-verify) |
| T-211-07 | Tampering | cold container pulls crates.io deps | accept | Deps are ferro's own published crates + their resolved transitive deps; standard crates.io supply-chain; the run is reproducible from the committed Dockerfile |
| T-211-08 | Denial of service | cold build disk/time on dev host | accept | Developer-invoked with a df precheck (Task 1); operator-controlled, not a CI path |
</threat_model>

<verification>
- RESULTS.md has a cache: cold row with real per-step + total numbers from a Docker run of the committed Dockerfile (SC#3).
- RESULTS.md env spec is fully populated (no TBD): Rust toolchain, ferro-rs version, cache state, host class, CPU/memory/disk, Agent-assistance = manual commands, date (SC#4).
- 211-WEAKNESSES.md names >=1 concrete finding; no placeholder text (SC#5 gate); linked from RESULTS.md.
- No secret/PII in any committed file; cold-cache-run.txt not committed.
- Workspace gate green: cargo fmt --all -- --check && cargo clippy -p ferro-cli --all-targets -- -D warnings (check df first; git checkout docs/protocol/schemas/*.json if a full test run dirtied them).
</verification>

<success_criteria>
The first real cold-cache number is committed (SC#3), RESULTS.md carries the complete environment
specification with real values (SC#4), and a concrete discovered-weakness finding is recorded and
linked (SC#5). With 211-01's apparatus (SC#1 + SC#2 wiring), this closes COMP-04.
</success_criteria>

<output>
After completion, create `.planning/phases/211-comp-04-time-to-working-app-benchmark/211-02-SUMMARY.md`
</output>
