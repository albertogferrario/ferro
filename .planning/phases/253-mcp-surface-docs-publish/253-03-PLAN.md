---
phase: 253-mcp-surface-docs-publish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/design-system/principles.md
  - docs/src/design-system/tokens.md
  - docs/src/design-system/variants.md
  - docs/src/design-system/patterns.md
  - docs/src/design-system/linting.md
  - docs/src/SUMMARY.md
  - ferro-json-ui/src/design/mod.rs
autonomous: true
requirements: [DS-08]
must_haves:
  truths:
    - "A reader finds a Design System chapter in the docs with principles, token reference, variant vocabulary, pattern catalog, and linting guide"
    - "The pattern catalog documents every rule id from design::rules() with rationale, example, and how-to-allow"
    - "patterns.md cannot silently drift from the rule registry (a test asserts every rule id appears and every documented rule id exists)"
    - "The chapter is registered in SUMMARY.md so mdBook renders it"
  artifacts:
    - path: "docs/src/design-system/patterns.md"
      provides: "Per-rule catalog covering all 10 rule ids, rationale copied from the registry"
      contains: "page-header"
    - path: "docs/src/SUMMARY.md"
      provides: "Design System chapter registration"
      contains: "design-system/principles.md"
    - path: "ferro-json-ui/src/design/mod.rs"
      provides: "D-09 docs drift test (patterns.md <-> rule registry, both directions)"
      contains: "patterns_md"
  key_links:
    - from: "ferro-json-ui/src/design/mod.rs drift test"
      to: "docs/src/design-system/patterns.md"
      via: "CARGO_MANIFEST_DIR/../docs/src/design-system/patterns.md read at test time"
      pattern: "design-system/patterns.md"
    - from: "docs/src/SUMMARY.md"
      to: "docs/src/design-system/*.md"
      via: "mdBook chapter links"
      pattern: "design-system/"
---

<objective>
Author the `docs/src/design-system/` chapter (five pages) documenting the whole design system, and
register it in `SUMMARY.md` (D-08). The pattern catalog (`patterns.md`) is hand-written prose but
drift-guarded (D-09): a test asserts every rule id from `design::rules()` appears in the file and
every documented rule id exists in the registry, with rationale text copied verbatim from the
registry's `rationale` field so prose and machine metadata cannot diverge. Voice is neutral product
documentation (D-10) — no "v2 vs legacy" framing, no internal strategy language. Cross-link
`features/themes.md` (token authoring recipe) and `json-ui/components.md` (the Phase 251 migration
table) — link, never duplicate.

Purpose: DS-08 (part 1 of 3). The reference an agent (and human) reads to understand the system.
Output: 5 markdown pages, SUMMARY.md chapter block, D-09 drift test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/253-mcp-surface-docs-publish/253-CONTEXT.md
@.planning/phases/253-mcp-surface-docs-publish/253-PATTERNS.md

<interfaces>
<!-- Concrete content the pages must carry. -->

The 10 rule ids the drift test asserts (ferro-json-ui/src/design/rules.rs, in registry order) —
each MUST appear as a plain string in patterns.md:
```
page-header
prefer-data-table
list-empty-state
row-actions-grouped
breadcrumb-on-subpages
process-kanban
card-actions-in-menu
create-separate-page
form-default-values
destructive-confirmation
```
Read each rule's `title` and `rationale` fields from rules.rs and copy the `rationale` VERBATIM
into the matching patterns.md section (D-09 keeps prose and metadata in sync).

The 30 token names for tokens.md (ferro-theme/src/token.rs:86, ALL_TOKENS order):
--color-background, --color-surface, --color-card, --color-border, --color-text,
--color-text-muted, --color-primary, --color-primary-foreground, --color-secondary,
--color-secondary-foreground, --color-accent, --color-destructive, --color-success,
--color-warning, --radius-sm, --radius-md, --radius-lg, --radius-full, --shadow-sm,
--shadow-md, --shadow-lg, --font-sans, --font-mono, --spacing, --motion-duration-fast,
--motion-duration-base, --motion-duration-slow, --motion-ease, --color-ring, --font-display.

Canonical vocabulary for variants.md:
variant = primary | secondary | outline | ghost | destructive
tone    = neutral | success | warning | destructive
size    = sm | md | lg

Cross-link targets (link, DO NOT duplicate):
- docs/src/features/themes.md — token authoring recipe + v2 token table (lines 52-132)
- docs/src/json-ui/components.md — Shared Enum Values (lines 40-49) + migration table (72-99)

SUMMARY.md insertion point: after the JSON-UI chapter block (docs/src/SUMMARY.md lines 61-73),
before the next chapter.

D-09 drift test analog: ferro-json-ui/src/design/mod.rs:284-314 (the existing D-08 intent drift
test — same `#[cfg(test)] mod ... { read external state; assert coverage }` structure). Append the
new test module after it; it does NOT need the projections feature (patterns.md is a plain file).
CARGO_MANIFEST_DIR for ferro-json-ui resolves patterns.md at `../docs/src/design-system/patterns.md`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the five design-system docs pages + register in SUMMARY.md</name>
  <files>docs/src/design-system/principles.md, docs/src/design-system/tokens.md, docs/src/design-system/variants.md, docs/src/design-system/patterns.md, docs/src/design-system/linting.md, docs/src/SUMMARY.md</files>
  <read_first>
    - ferro-json-ui/src/design/rules.rs (all 10 rules — read each title + rationale to copy into patterns.md)
    - docs/src/features/themes.md:52-132 (token table format + the authoring recipe to cross-link, not copy)
    - docs/src/json-ui/components.md:40-99 (Shared Enum Values + migration table to cross-link, not copy)
    - docs/src/json-ui/getting-started.md (neutral product voice reference)
    - docs/src/SUMMARY.md:61-73 (JSON-UI chapter block — copy its format for the new chapter)
    - .planning/phases/253-mcp-surface-docs-publish/253-PATTERNS.md (each docs page section: structure + cross-link one-liners)
  </read_first>
  <action>
Create `docs/src/design-system/` with five pages, neutral product voice (D-10):

**principles.md** — "Design System Principles". Three sections: Semantic Tokens (components emit
semantic classes like `bg-primary`, themes supply the values), Intent-Keyed Patterns (page
archetypes ARE the seven projection intents; link to patterns.md), Lint as Diagnostics (design
lint never rejects a spec or affects rendering — it advises; link to linting.md). End with links
to the other four pages.

**tokens.md** — "Token Reference". A table of the 30 tokens (`| Token | Purpose |`) using the
30 names above with one-line purposes (reuse the purposes from 253-PATTERNS.md's
DESIGN_TOKEN_DESCRIPTIONS). Group by category (surface/role/shape/shadow/typography/density/motion/
focus/display). Open with one sentence: "For how to write and activate a theme that customizes
these tokens, see [Themes](../features/themes.md)." — do NOT duplicate the authoring recipe.

**variants.md** — "Variant Vocabulary". Document the canonical variant/tone/size sets (values +
one-line meaning each). Open with: "These canonical values are enforced at catalog validation time
and checked by the design lint engine. For the full component rename/migration table from earlier
versions, see [Components](../json-ui/components.md#component-specific-enum-values)." — do NOT copy
the migration table.

**patterns.md** — "Pattern Catalog". One `## \`<rule-id>\`` section per rule, ALL 10 ids present
as plain strings (the drift test greps for them). Each section: **Title** (from the rule's title),
**Rationale** (copied VERBATIM from the rule's `rationale` field in rules.rs), **Intents** (from
`rule.intents`, or "all"), a Conforming example (small JSON snippet), a Violating example, and a
"How to allow" line showing `"design": { "allow": ["<rule-id>"] }`. Order the sections in registry
order.

**linting.md** — "Linting Guide". CLI usage (`ferro design:lint`, `[path]`, `--json`, `--deny`
with a one-line meaning each), the `design_lint` MCP tool (provide exactly one of `spec_json`
inline or `path`), the output shape (`{ file, rule, element_id, severity, message, suggestion }`;
empty array = clean; Warning trips `--deny`, Info advisory), and how to allow a rule.

Then register the chapter in `docs/src/SUMMARY.md`, inserting after the JSON-UI block (after line 73):
```markdown
# Design System

- [Principles](design-system/principles.md)
- [Token Reference](design-system/tokens.md)
- [Variant Vocabulary](design-system/variants.md)
- [Pattern Catalog](design-system/patterns.md)
- [Linting Guide](design-system/linting.md)
```

No "v2 vs legacy" comparisons, no "we"/personal pronouns, no strategy language (D-10).
  </action>
  <verify>
    <automated>ls docs/src/design-system/ && for id in page-header prefer-data-table list-empty-state row-actions-grouped breadcrumb-on-subpages process-kanban card-actions-in-menu create-separate-page form-default-values destructive-confirmation; do grep -q "$id" docs/src/design-system/patterns.md || echo "MISSING $id"; done</automated>
  </verify>
  <done>Five pages exist; patterns.md contains all 10 rule ids; SUMMARY.md registers the chapter; cross-links point at themes.md and components.md without duplicating them.</done>
  <acceptance_criteria>
    - `ls docs/src/design-system/` lists exactly: principles.md, tokens.md, variants.md, patterns.md, linting.md
    - The verify loop prints NO "MISSING" lines (all 10 rule ids present in patterns.md)
    - `grep -c "^| \`--" docs/src/design-system/tokens.md` (or equivalent token rows) reflects 30 token entries
    - `grep -n "design-system/principles.md" docs/src/SUMMARY.md` returns a hit
    - `grep -n "features/themes.md" docs/src/design-system/tokens.md` returns a hit (cross-link, not copy)
    - `grep -ni "legacy\|v2 vs" docs/src/design-system/*.md` returns no framing violations (token-version labels excepted)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add the D-09 patterns.md <-> registry drift test</name>
  <files>ferro-json-ui/src/design/mod.rs</files>
  <read_first>
    - ferro-json-ui/src/design/mod.rs:284-314 (existing D-08 drift test — copy its module structure)
    - docs/src/design-system/patterns.md (must exist from Task 1 before this test is added — RESEARCH Pitfall 4)
    - .planning/phases/253-mcp-surface-docs-publish/253-PATTERNS.md (mod.rs section: the drift test code)
  </read_first>
  <action>
Append a new test module after the existing D-08 drift test block (~mod.rs:314). It runs under
plain `#[cfg(test)]` (patterns.md is a file, no projections feature needed):
```rust
#[cfg(test)]
mod docs_drift_tests {
    use super::rules;

    /// D-09: every rule id in the registry has a section in patterns.md,
    /// and every rule id documented in patterns.md exists in the registry.
    #[test]
    fn patterns_md_matches_rule_registry() {
        let manifest_dir = std::env::var("CARGO_MANIFEST_DIR").expect("CARGO_MANIFEST_DIR");
        let path = std::path::Path::new(&manifest_dir).join("../docs/src/design-system/patterns.md");
        let content = std::fs::read_to_string(&path)
            .unwrap_or_else(|e| panic!("patterns.md not found at {}: {e} (D-09 drift guard)", path.display()));

        // Forward: registry -> docs
        for rule in rules() {
            assert!(
                content.contains(rule.id),
                "patterns.md is missing rule id `{}` — add a section for it (D-09)",
                rule.id
            );
        }
        // Reverse: docs -> registry. Documented ids appear as `## `<id>`` headers.
        let known: std::collections::HashSet<&str> = rules().iter().map(|r| r.id).collect();
        for line in content.lines() {
            let t = line.trim();
            if let Some(rest) = t.strip_prefix("## `") {
                if let Some(id) = rest.strip_suffix('`') {
                    assert!(
                        known.contains(id),
                        "patterns.md documents unknown rule id `{id}` — not in design::rules() (D-09)"
                    );
                }
            }
        }
    }
}
```
If Task 1 used a different header format than `` ## `id` `` for rule sections, align the reverse
check's parsing to the actual header format used in patterns.md (the forward check is format-agnostic).
  </action>
  <verify>
    <automated>cargo test -p ferro-json-ui patterns_md_matches_rule_registry</automated>
  </verify>
  <done>Drift test passes — all 10 registry rule ids appear in patterns.md and no unknown id is documented.</done>
  <acceptance_criteria>
    - `grep -n "patterns_md_matches_rule_registry" ferro-json-ui/src/design/mod.rs` returns a hit
    - `grep -n "design-system/patterns.md" ferro-json-ui/src/design/mod.rs` returns a hit
    - `cargo test -p ferro-json-ui patterns_md_matches_rule_registry` reports 1 passed
    - `cargo test -p ferro-json-ui design` still reports all prior design tests passing
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation pages and a test that reads a repo-local file; no runtime component, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-253-06 | Tampering | Docs prose drifting from the rule registry | mitigate | The D-09 drift test fails CI (both directions) if any rule id is undocumented or any documented id is unknown; rationale copied verbatim from the registry. |
| T-253-07 | Information Disclosure | Repository docs voice leaking internal strategy | mitigate | D-10 neutral product voice enforced; acceptance criteria greps for "legacy"/"v2 vs" framing; content limited to public design vocabulary. |
</threat_model>

<verification>
- `ls docs/src/design-system/` — five pages present.
- `cargo test -p ferro-json-ui patterns_md_matches_rule_registry` — drift test green (requires Task 1 committed first).
- `cargo doc --no-deps -p ferro-json-ui` — no new rustdoc warnings from the test module.
</verification>

<success_criteria>
DS-08 (part 1): `docs/src/design-system/` covers principles, token v2 reference, variant
vocabulary, pattern catalog (rationale + example + `allow` per rule), and the lint guide (CLI +
MCP), registered in SUMMARY.md, drift-guarded against the rule registry, neutral voice.
</success_criteria>

<output>
After completion, create `.planning/phases/253-mcp-surface-docs-publish/253-03-SUMMARY.md`.
</output>
