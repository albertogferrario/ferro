---
phase: 253-mcp-surface-docs-publish
plan: 05
subsystem: infra
tags: [crates-io, publish, ci, gestiscilo, design-system, v16.5]

requires:
  - phase: 253-04
    provides: "IN-01/IN-02 cleanup; doc HTML tag fix; workspace bumped to 0.2.85 locally"
  - phase: 253-01
    provides: "design_lint MCP tool"
  - phase: 253-02
    provides: "json_ui_catalog + generation_context design-system extensions"
  - phase: 253-03
    provides: "docs/src/design-system/ chapter + patterns drift test"

provides:
  - "ferro-rs 0.2.85 published to crates.io (v16.5 JSON-UI Design System milestone)"
  - "CI-exact gate verified on the full gate-review scope (18 post-gate commits)"
  - "gestiscilo Phase 232 handoff brief (.planning/phases/253-mcp-surface-docs-publish/253-GESTISCILO-BRIEF.md)"
  - "Exported protocol/service-def schemas updated with Track A CRUD fields"

affects:
  - gestiscilo-phase-232

tech-stack:
  added: []
  patterns:
    - "Publish gate: full CI-exact gate (fmt/clippy/test/doc) on the final tree before push, not on a prior snapshot"
    - "Schema drift: cargo test regenerates docs/protocol/schemas/*.json — commit when CRUD fields change"
    - "Remote divergence: gh HTTPS credential helper fetch+merge before push when remote has CI auto-bumps"

key-files:
  created:
    - .planning/phases/253-mcp-surface-docs-publish/253-GESTISCILO-BRIEF.md
  modified:
    - Cargo.toml (workspace version 0.2.83→0.2.85; conflict-resolved with remote 0.2.84)
    - Cargo.lock
    - docs/protocol/schemas/protocol.json (Track A CRUD fields: creatable/updatable/deletable/mcp_write_ability/table)
    - docs/protocol/schemas/service-def.json (same Track A fields)

key-decisions:
  - "D-commit: schema JSONs regenerated by test — committed as docs(253) rather than discarded, since Track A CRUD fields are genuine schema additions"
  - "D-merge: remote had a CI auto-bump to 0.2.84 (independent gestiscilo payments + json-ui fixes pushed while the publish gate was open); resolved by fetch+merge, keeping local 0.2.85 as the higher version"
  - "D-disk: first test run hit ENOSPC (target/debug/deps = 6.3G; data volume 98%); resolved by deleting target/debug/deps (freeing 6.3G) and re-running"

patterns-established:
  - "Gate-review scope: any commits that land after the initial gate run require a fresh gate before push — stale evidence is invalid"

requirements-completed: [DS-08]

duration: ~90min (continuation; includes re-gate, merge resolution, CI wait)
completed: 2026-07-04
---

# Phase 253 Plan 05: Publish Summary

**ferro-rs 0.2.85 published to crates.io — v16.5 JSON-UI Design System milestone exits; CI-exact gate re-run covering 18 gate-review commits; gestiscilo Phase 232 unblocked via handoff brief.**

## Performance

- **Duration:** ~90 min (continuation from checkpoint)
- **Started:** 2026-07-04T13:30:00Z (continuation)
- **Completed:** 2026-07-04T14:30:00Z (approx; pending CI confirm)
- **Tasks:** 3 (Task 2 evidence captured, Task 3 push+verify+brief)
- **Files modified:** 4 (Cargo.toml, Cargo.lock, 2 schema JSONs)

## Accomplishments

- Full CI-exact gate (fmt / clippy --all-features / test --all-features / doc -D warnings) green on the complete tree including all 18 gate-review commits.
- Resolved disk-full (ENOSPC) during test run — cleaned target/debug/deps (6.3G freed), tests passed on retry with 15G available.
- Resolved remote divergence — remote had a CI auto-bump to 0.2.84 (independent json-ui + payments commits pushed during the gate pause); fetched, merged, pushed 0.2.85 cleanly.
- Committed Track A CRUD fields regenerated into protocol/service-def schemas.
- Pushed master; CI publish workflow triggered; gestiscilo handoff brief written.

## Pre-publish UAT Evidence (Task 2)

Performed with operator during gate-review:

| Check | Result |
|---|---|
| `ferro design:lint app/src/views` | "No findings — all specs are clean." (7 views incl. new cassa/ordini/prodotti/prodotto_nuovo) |
| Visual pass (light+dark) | login/pagamenti/prodotti/ordini/cassa verified in browser — focus ring, motion tokens, mobile emulation |
| Version sanity | crates.io max_version = 0.2.84 (API-verified), local = 0.2.85 |
| Operator approval | "Approved" |

## Task Commits (continuation session)

Gate-review commits (pre-continuation, included in re-gate scope):

- `848fcd33` fix(253): destructive-confirmation conformance is action.confirm, not entry-level confirm
- `18a00afe` feat(253): default theme — white surfaces and cards, monochrome primary buttons
- `180135d8` feat(253): app example pages — ordini kanban, prodotti list, prodotto create form
- `9f1c8e0b` feat(253): export register_layout through the ferro facade
- `d28f01b7` feat(253): app dashboard layout shell + spec composition fixes
- `44e15465` fix(253): max_width wrappers anchor left
- `21e41731` feat(253): bounded max-w-7xl content column in dashboard-family layouts
- `008c1d6b` revert(253): drop Grid focused-column max_width — rejected pattern
- `988d3f61` feat(253): two-column create page — form card + info card fill the content row
- `db5337ec` feat(253): Grid per-child spans + 2/3-1/3 create-page split
- `a7012b5d` feat(253): viewport-fill workspaces — Spec.fill_viewport, Grid fill mode, ferro-fill base CSS
- `69fd7c62` feat(253): cassa POS demo page — fill workspace, cart pane + ProductTile grid
- `37952fbe` docs(253): cassa consumer-audit findings in friction report
- `ef0ea8e5` docs(253): dogfood friction report from pre-publish review pass
- `f675be6b` feat(253): prefer-components lint rule
- `e15edfa3` docs(253): cassa/booking consolidation audit in friction report

Continuation session commits:

1. **Schema commit** - `54253135` docs(253): refresh exported schemas — CRUD write verbs (Track A fields)
2. **Config cleanup** - `eb8991d8` chore: clear auto-chain flag after operator publish-gate review
3. **Merge** - `a28e935e` chore: merge remote 0.2.84 auto-bump — local advances to 0.2.85
4. **Gestiscilo brief** - `f16b829f` docs(253): gestiscilo Phase 232 handoff brief

Original Task 1 commit (prior session):
- `674217ef` chore(253-05): bump workspace to 0.2.85; fix doc HTML tag in design_lint

## Files Created/Modified

- `Cargo.toml` — workspace version 0.2.83→0.2.85 (conflict-merged with remote 0.2.84)
- `docs/protocol/schemas/protocol.json` — Track A CRUD fields added (72 lines)
- `docs/protocol/schemas/service-def.json` — Track A CRUD fields added (72 lines)
- `.planning/phases/253-mcp-surface-docs-publish/253-GESTISCILO-BRIEF.md` — gestiscilo Phase 232 handoff brief

## Decisions Made

- **Schema commit over discard:** `cargo test --all-features` regenerated `docs/protocol/schemas/*.json` with Track A CRUD fields (`creatable`, `updatable`, `deletable`, `mcp_write_ability`, `table`) — these are genuine schema additions from prior phases (241-243) that hadn't been committed yet. Committed rather than discarded.

- **Remote merge before push:** The remote had advanced to 0.2.84 via CI auto-bump (gestiscilo-facing payments + json-ui fixes pushed while the gate pause was open). Correct approach was fetch+merge, keeping 0.2.85 as the higher version, not force-push.

- **target/debug/deps cleanup for ENOSPC:** Deleted 6.3G of debug deps to free disk space for the scaffold test. This forced recompilation for subsequent steps (`cargo doc`) but was the documented recovery path for the known disk-full issue.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Disk-full during first test run**
- **Found during:** Task 3 gate re-run
- **Issue:** `scaffold_builds_against_workspace_ferro` failed with ENOSPC — data volume 98% full (414/460 GB), target/debug/deps = 6.3G, leaving only 8.8G for the benchmark's scratch compilation of a new project.
- **Fix:** Deleted `target/debug/deps` (6.3G freed; 15G then available). Re-ran `cargo test --all-features` — passed including the scaffold test.
- **Files modified:** None (build artifact cleanup, not source)
- **Committed in:** N/A (build artifact deletion; schema regen committed in 54253135)

**2. [Rule 3 - Blocking] Remote divergence prevented push**
- **Found during:** Task 3 push
- **Issue:** Remote master had 1 commit ahead (CI auto-bump to 0.2.84 from independent payments/json-ui changes pushed while the gate-review pause was open). Push rejected with "fetch first".
- **Fix:** Fetched remote, merged, resolved Cargo.toml conflict (keep 0.2.85), committed as `a28e935e`.
- **Files modified:** Cargo.toml (conflict resolution)
- **Committed in:** a28e935e

---

**Total deviations:** 2 auto-fixed (both blocking — disk and remote divergence)
**Impact on plan:** Both required for task completion; neither changed the publish artifact.

## Publish Verification

- **crates.io version:** 0.2.85 confirmed via API (`curl .../ferro-rs | jq -r .crate.max_version`)
- **GitHub Release tag:** v0.2.85 exists (points to `6e6e98a9`)
- **CI Publish run:** 28708390679 — Check Version ✓, Test ✓ (15m3s), Publish to crates.io ✓ (7m44s, all waves), Post-publish scaffold smoke (in-progress at SUMMARY write)
- **Publish waves completed:** 1a → 1b → 1c → 2 (framework + mcp) → 3 (framework-consumers) → Tag + binary build trigger ✓
- **origin/master ref:** corrected to `6e6e98a9aaf4b5928cb77430361ce37ffe8ae2ad`
- **CI Security Check (cargo-deny):** FAILED — pre-existing advisories (RUSTSEC-2026-0190 anyhow, RUSTSEC-2026-0189 rmcp 0.12.0); same failure present on the prior push (CI run 28486489730); not introduced by this phase. Logged to deferred-items.md.

## gestiscilo Phase 232 Handoff

See `.planning/phases/253-mcp-surface-docs-publish/253-GESTISCILO-BRIEF.md`.

Key adoption requirements for gestiscilo:
1. Apply enum migration table (retired values rejected at parse) — at least `"variant": "default"` in `views/cassa/orders_nuovo.json`
2. Run `ferro design:lint app/src/views` and resolve/allow all findings
3. Pin `ferro-rs = "0.2.85"` in Cargo.toml
4. Produce FRICTION.md at end of sweep

## Known Stubs

None in files created/modified by this plan.

## Issues Encountered

- ENOSPC during test run — documented disk-full issue, resolved by cleanup (see Deviations).
- Remote divergence — CI auto-bumps on independent changes during the gate-review pause; resolved by fetch+merge.
- `cargo doc` run used recompiled artifacts after target cleanup — this is expected behavior and did not affect gate correctness.

## Next Phase Readiness

- v16.5 milestone is published (pending CI confirmation).
- gestiscilo Phase 232 is unblocked — handoff brief written.
- `/gsd-complete-milestone v16.5` deferred (same as v16.0/v16.1/v16.2/v16.3 archival — not blocking).
- v16.4 Work Distribution (Phases 244-249) and v16.3 archive remain queued.

---
*Phase: 253-mcp-surface-docs-publish*
*Completed: 2026-07-04*
