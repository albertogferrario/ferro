---
phase: 253-mcp-surface-docs-publish
plan: 05
type: execute
wave: 2
depends_on: ["253-01", "253-02", "253-03", "253-04"]
files_modified:
  - Cargo.toml
autonomous: false
requirements: [DS-08]
must_haves:
  truths:
    - "The full CI-exact gate passes on the whole workspace before any push"
    - "The workspace version is bumped to the next available crates.io patch (verified via API, not stale refs)"
    - "master is pushed and CI publishes the new version to crates.io"
    - "The published version is confirmed live via crates.io / gh API"
    - "gestiscilo Phase 232 receives a handoff brief (no consumer-tree edits from this session)"
  artifacts:
    - path: "Cargo.toml"
      provides: "workspace version bump = the publish trigger"
      contains: "version ="
  key_links:
    - from: "Cargo.toml version bump commit"
      to: "CI publish.yml"
      via: "push to master triggers the publish waves"
      pattern: "version ="
---

<objective>
Ship the v16.5 milestone: run the full CI-exact gate, bump the workspace version to the next
available crates.io patch, and push master so CI publishes — the milestone exit (success criterion 4)
that unblocks the consumer adoption phase (gestiscilo Phase 232). The publish step is operator-gated
(Phase 236 practice, D-14): present the pre-publish UAT checklist and wait for approval before the
irreversible push. Version selection is verified against crates.io at gate time (D-11, D-14 OQ-2) —
local refs and memory are known to lie; a `docs/*`-only push would be skipped by the publish gate, so
the version bump commit is the mandatory trigger (RESEARCH Pitfall 6).

Purpose: DS-08 (part 3 of 3) — the single crates.io publish.
Output: version bump commit, pushed master, verified publish, gestiscilo handoff brief.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/253-mcp-surface-docs-publish/253-CONTEXT.md
@.planning/phases/253-mcp-surface-docs-publish/253-RESEARCH.md

<interfaces>
<!-- Publish mechanics (verified). -->

Workspace version lives at Cargo.toml:46 (`[workspace.package] version = "0.2.83"` at plan time —
re-read at gate time; local unpushed bumps may differ from crates.io).

Gate commands (CI-exact, D-12; CPU-serialization — run ONE at a time, never chained/parallel):
```
cargo fmt --all -- --check
cargo clippy --all --all-targets --all-features -- -D warnings
cargo test --all-features
cargo doc --no-deps -D warnings        # CI matrix is wider than the 3-command gate
```
Re-run `cargo fmt --all -- --check` after ANY hand-edit (memory: a hand-Write file has broken a
prior publish at the fmt gate).

Version check (D-11, do not trust origin/master or memory):
```
curl -s https://crates.io/api/v1/crates/ferro-rs | jq -r .crate.max_version
git tag | grep -E "^v0\.2\.(8[0-9]|9[0-9])$"
```
Next version = crates.io max_version patch + 1 (unless a higher unpushed local bump is already the
correct next — reconcile against the tag list; RESEARCH Pitfall 7).

Push (SSH is denied; use the gh HTTPS credential helper — memory):
```
git -c credential.helper='!gh auth git-credential' push https://github.com/albertogferrario/ferro.git master
```
After a verified push, correct the local ref: `git update-ref refs/remotes/origin/master HEAD`.

Constraints: ferro-payments is independently versioned (0.1.3) and untouched — no ferro-payments bump
(D-13). No new crates → no publish.yml wave changes, no publish-new token bootstrap (D-13). Cross-repo
handoff to gestiscilo Phase 232 is a BRIEF ONLY — never edit the consumer tree or its planning (D-16).

Pre-existing flaky test to NOT chase at the gate: `commands::serve::tests::spawn_child_with_prefix_uses_new_process_group`
(252 deferred-items.md — timing race, unrelated to this phase).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full CI-exact gate + version bump commit (no push)</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (workspace [workspace.package] version — re-read current value)
    - CLAUDE.md (Testing & Linting section — the exact CI commands)
    - .planning/phases/253-mcp-surface-docs-publish/253-RESEARCH.md (Pitfalls 5,6,7 + Common Pitfalls)
  </read_first>
  <action>
Run the CI-exact gate on the whole workspace, ONE command at a time (CPU-serialization convention —
never chain or parallelize cargo runs; reuse prior evidence over re-running):
1. `cargo fmt --all -- --check` — if it fails, `cargo fmt --all`, re-inspect the diff, re-run --check.
2. `cargo clippy --all --all-targets --all-features -- -D warnings` — fix every warning (the new public
   ferro-mcp types need `///` docs or `cargo doc -D warnings` will fail; see step 4).
3. `cargo test --all-features` — all pass EXCEPT the known-flaky `spawn_child_with_prefix_uses_new_process_group`
   (do not chase it; note it if it appears). If disk-full errors appear (ENOSPC), `df -h`, clean `target/`, retry.
4. `cargo doc --no-deps -D warnings` — resolve any missing-docs on the new public types
   (`DesignLintParams`, `DesignVocabulary`, `DesignRuleRef`, `DesignSystemSummary`, `TokenInfo`,
   `IntentPattern`, `CanonicalVariants`).
Also confirm the ferro-mcp documented mirror count is intact: `grep -rn "39 built-in" ferro-mcp/src`
returns nothing (Plan 01 fixed it); the 47-count assertion in json_ui_catalog.rs is untouched.

Then determine the publish version WITHOUT trusting stale refs:
```
curl -s https://crates.io/api/v1/crates/ferro-rs | jq -r .crate.max_version
git tag | grep -E "^v0\.2\."
```
Set the next version = highest of (crates.io max_version, current Cargo.toml version) with the patch
incremented so it does not collide with an existing tag. Edit `Cargo.toml` `[workspace.package]
version` to that value. Run `cargo fmt --all -- --check` once more (Cargo.toml is not fmt-checked, but
re-confirm nothing else drifted). Update Cargo.lock (`cargo build -p ferro-mcp` or `cargo update -w`
as appropriate) so the lockfile matches.

Commit the bump: `docs(253): v16.5 design system publish — bump workspace to <version>` (no co-author
lines). DO NOT PUSH — the push is gated by Task 2's operator approval.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && grep -n "^version" Cargo.toml | head -1</automated>
  </verify>
  <done>Full CI-exact gate green (fmt/clippy --all-features/test --all-features/doc); workspace version bumped to the verified next patch and committed locally; not pushed.</done>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy --all --all-targets --all-features -- -D warnings` exits 0
    - `cargo test --all-features` passes (flaky serve PGID test excepted/noted)
    - `cargo doc --no-deps -D warnings` exits 0
    - `grep -rc "39 built-in" ferro-mcp/src` totals `0`
    - `git log --oneline -1` shows the version-bump commit; `git status` shows nothing to push blocked by the gate
    - The chosen version does not already exist in `git tag` output
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Pre-publish UAT checklist + operator publish approval</name>
  <what-built>
    All 253 code has landed and the CI-exact gate is green: the `design_lint` MCP tool, the
    `json_ui_catalog` + `generation_context` design-system extensions, the `docs/src/design-system/`
    chapter with the patterns drift test, the IN-01/IN-02 cleanup, and a local (unpushed) workspace
    version bump commit ready to trigger CI publish.
  </what-built>
  <how-to-verify>
    1. Human CLI output check (folded 252 item, D-14): from the repo root run
       `cargo run --bin ferro -- design:lint app/src/views` (or the installed `ferro design:lint app/src/views`).
       Expected: findings grouped by file with severity labels, rule id, message, and suggestion, plus a
       summary; or the "No findings — all specs are clean." / "No JSON-UI spec files found." message.
       Confirm color, indentation, and readability are acceptable.
    2. Pixel-level visual pass (folded 251 item, D-14, advisory): spot-check the refreshed default theme
       renders as intended (focus ring, motion tiers, cool-tinted neutrals) on a sample view.
    3. Confirm the publish version shown in the bump commit is the correct next crates.io patch
       (re-run `curl -s https://crates.io/api/v1/crates/ferro-rs | jq -r .crate.max_version` if unsure).
    4. Approve the irreversible push to master (CI will publish to crates.io + tag + build binaries + brew tap).
  </how-to-verify>
  <action>
    Pause here (blocking checkpoint). Present the pre-publish UAT checklist above to the operator and wait
    for explicit approval before Task 3 pushes. Do NOT push, publish, or run any irreversible git/network
    command until the operator responds with the resume signal. If the operator reports an issue, stop and
    surface it rather than proceeding to Task 3.
  </action>
  <resume-signal>Type "approved" to authorize the publish push, or describe issues to fix first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Push, verify publish, hand off gestiscilo brief</name>
  <files>Cargo.toml</files>
  <read_first>
    - .planning/phases/253-mcp-surface-docs-publish/253-RESEARCH.md (publish flow + Open Question 2)
    - .planning/STATE.md (Operator actions pending — for the handoff brief context)
  </read_first>
  <action>
After operator approval:
1. Push master via the gh HTTPS credential helper (SSH is denied):
```
git -c credential.helper='!gh auth git-credential' push https://github.com/albertogferrario/ferro.git master
```
2. Correct the local remote ref (it recurrently lies): `git update-ref refs/remotes/origin/master HEAD`.
3. Watch CI publish and verify the result via API (NOT stale refs):
```
curl -s https://crates.io/api/v1/crates/ferro-rs | jq -r .crate.max_version   # == the bumped version
gh api repos/albertogferrario/ferro/releases/latest --jq .tag_name             # == v<version>
```
   Confirm `max_version` equals the version bumped in Task 1 and the GitHub Release/tag exists.
   If CI's auto-bump path produced a different final version than the local bump (RESEARCH Pitfall 7),
   record the actual published version.
4. Write the gestiscilo Phase 232 handoff BRIEF (D-16 — brief only, never edit the consumer tree): a
   short note in the 253-05-SUMMARY.md (or a dedicated handoff file under this phase dir) stating the
   published ferro-rs version to pin, and the public contracts now available — the `design_lint` MCP
   tool (`spec_json` XOR `path`, `FileFinding[]` output), the CLI `--json`/`--deny` gate, the
   `json_ui_catalog` design_system vocabulary, the `generation_context` design-system summary, and the
   migration table location in `docs/src/json-ui/components.md`.
Do not run `/gsd-complete-milestone` here — archival is deferred bookkeeping (CONTEXT deferred list).
  </action>
  <verify>
    <automated>curl -s https://crates.io/api/v1/crates/ferro-rs | jq -r .crate.max_version</automated>
  </verify>
  <done>master pushed; crates.io max_version equals the published version; GitHub Release tag exists; gestiscilo handoff brief written; local origin/master ref corrected.</done>
  <acceptance_criteria>
    - `curl -s https://crates.io/api/v1/crates/ferro-rs | jq -r .crate.max_version` equals the version bumped in Task 1
    - `gh api repos/albertogferrario/ferro/releases/latest --jq .tag_name` returns `v<version>`
    - `git rev-parse refs/remotes/origin/master` equals `git rev-parse HEAD`
    - A gestiscilo Phase 232 handoff brief exists naming the pinned version and the public contracts
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local workspace → crates.io / GitHub (CI publish) | The push exposes code to public registries; irreversible once published |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-253-10 | Tampering | Publishing a broken build to crates.io | mitigate | Full CI-exact gate (fmt/clippy --all-features/test --all-features/doc) green before the push; publish is blocked behind the gate and the operator checkpoint. |
| T-253-11 | Repudiation | Publishing the wrong version (stale local refs) | mitigate | Version verified against the crates.io API and `git tag` at gate time, not local refs/memory; post-publish verification asserts `max_version` equals the intended version. |
| T-253-12 | Elevation of Privilege | Cross-repo edits to the consumer tree | mitigate | D-16: gestiscilo handoff is a brief only; no edits to the consumer repo or its planning from this session. |
| T-253-13 | Information Disclosure | cargo-deny / license or advisory regressions shipped | accept | CI runs cargo-deny; no new external crates added this phase (only workspace-internal ferro-theme + already-present strum), so the advisory/license surface is unchanged. |
</threat_model>

<verification>
- CI-exact gate green (all four commands) before push.
- Post-push: crates.io `max_version` == bumped version; GitHub Release tag `v<version>` exists.
- `origin/master` local ref corrected to HEAD.
</verification>

<success_criteria>
DS-08 (part 3): workspace version bumped and published to crates.io after the full CI-exact gate;
the consumer adoption phase (gestiscilo Phase 232) is unblocked with a handoff brief.
</success_criteria>

<output>
After completion, create `.planning/phases/253-mcp-surface-docs-publish/253-05-SUMMARY.md`
(include the gestiscilo Phase 232 handoff brief).
</output>
