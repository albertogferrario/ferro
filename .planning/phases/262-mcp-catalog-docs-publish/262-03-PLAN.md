---
phase: 262-mcp-catalog-docs-publish
plan: 03
type: execute
wave: 2
depends_on: [262-01, 262-02]
files_modified:
  - Cargo.toml
autonomous: false
requirements: [LIVE-04]
nyquist_compliant: true

must_haves:
  truths:
    - "The full CI-exact gate is green (fmt + clippy --all-features + test --all-features + cargo doc -D warnings)"
    - "cargo publish -p ferro-rs --dry-run exits 0"
    - "After the operator-gated push, the published ferro-rs version exceeds 0.2.89"
  artifacts:
    - path: "Cargo.toml"
      provides: "workspace version for the single publish commit (bumped only if crates.io >= 0.2.91)"
      contains: "version ="
  key_links:
    - from: "local master (HEAD)"
      to: "crates.io ferro-rs published version"
      via: "gh HTTPS push -> publish.yml auto-publish"
      pattern: "credential.helper"
---

<objective>
Run the full CI-exact gate, then perform the single operator-gated crates.io publish that ships all three v17.0 capabilities (SC-4). Resolve the workspace version against crates.io at gate time (D-11), verify the ferro-payments rider (D-12) and publish.yml wave order (D-13), and push master via the gh HTTPS credential helper (D-14). Commit hygiene per D-15.

Purpose: make LiveFragment, #[memoize], and asset!() pinnable for consumers in one publish — no mid-milestone publishes.

Output: a green CI-exact gate, an operator-approved publish push, and post-publish verification via crates.io / gh API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/262-mcp-catalog-docs-publish/262-CONTEXT.md
@.planning/phases/262-mcp-catalog-docs-publish/262-RESEARCH.md

<gate_commands>
<!-- CI-exact gate (D-09). Run SERIALIZED, one CPU-heavy op at a time (feedback_one_cpu_op_at_a_time). -->
cargo fmt --all -- --check
cargo clippy --all --all-targets --all-features -- -D warnings
cargo test --all-features
RUSTDOCFLAGS="-Dwarnings" cargo doc --no-deps --all-features
# cargo-deny runs as a separate CI job (note deny.toml); not local-gate-blocking.
</gate_commands>

<publish_facts>
<!-- Verified world-state (RESEARCH.md + this session) -->
- Branch: master; HEAD == master (0 ahead / 0 behind); all 259/260/261 work is already on local master. No feat-branch merge.
- Cargo.toml:47 — workspace version = "0.2.91" (already bumped, unpublished).
- ferro-payments/Cargo.toml:3 — version = "0.1.6" (shipped in the 258 close; no v17.0 code changes).
- publish.yml:217 — ferro-bundle already in WAVE1A_CRATES (261 D-06 move landed). ferro-a2ui absent (intentional, publish = false).
- SSH is denied for this repo; push via: git -c credential.helper='!gh auth git-credential' push https://github.com/albertogferrario/ferro.git master
- NEVER trust local origin/* refs; verify remote via gh api / curl crates.io. After a verified push: git update-ref refs/remotes/origin/master HEAD
</publish_facts>

<gate_gotchas>
- Re-run `cargo fmt --all -- --check` after ANY hand-edit (recurrent publish-blocker; clippy/test do not catch fmt drift).
- `cargo test --all-features` regenerates docs/protocol/schemas/*.json (Phase 94 export test). If only that path is dirty after tests, `git checkout docs/protocol/schemas/` to discard (project_schema_export_test_dirties_tree).
- Check disk + clean target/ before the full --all-features test gate (recurrent ENOSPC).
- If `git tag | grep v0.2.91` returns a result, CI will auto-bump on push — bump the workspace to crates.io_max+1 manually so cargo publishes directly (D-11 manual-bump pattern).
- Stage SPECIFIC files only. NEVER `git add -A` / `git add .` — excludes the 36 app/frontend/node_modules/.vite/deps_temp_* deletions, .planning/config.json churn, phantom planning/phases/158-... deletion (D-15).
</gate_gotchas>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full CI-exact gate + version resolution + commit staging (pre-publish preparation)</name>
  <files>Cargo.toml (conditional single-field version bump only)</files>
  <read_first>
    - Cargo.toml (THE FILE — line 47 workspace version = "0.2.91")
    - ferro-payments/Cargo.toml (line 3 version = "0.1.6")
    - .github/workflows/publish.yml (confirm ferro-bundle in Wave 1a ~line 217; ferro-a2ui absent)
    - .github/workflows/ci.yml (confirm the docs gate: cargo doc --no-deps --all-features + RUSTDOCFLAGS=-Dwarnings)
    - .planning/phases/262-mcp-catalog-docs-publish/262-CONTEXT.md D-09 through D-15
  </read_first>
  <action>
Prepare the publish. Run every CPU-heavy step SERIALIZED (one at a time — do not chain or parallelize cargo/clippy/test).

STEP A — Disk + tree hygiene: check `df -h .` and, if space is tight, clean `target/`. Confirm branch: `git -C <repo-root> branch --show-current` == master and `git rev-parse HEAD` matches (assert HEAD=master from the repo root before any ref move, per feedback_worktree_merge_cwd_trap).

STEP B — Run the CI-exact gate IN ORDER, serialized:
1. `cargo fmt --all -- --check`
2. `cargo clippy --all --all-targets --all-features -- -D warnings`
3. `cargo test --all-features`
4. `RUSTDOCFLAGS="-Dwarnings" cargo doc --no-deps --all-features`
After the test run, `git status` — if `docs/protocol/schemas/*.json` is the only dirty path, `git checkout docs/protocol/schemas/` (schema-export churn, discard per D-09). Only keep a schema diff if it results from this phase's code changes (not expected).

STEP C — Verify publish.yml wave order (D-13): confirm `ferro-bundle` appears in the Wave 1a crate list (WAVE1A_CRATES ~line 217) and `ferro-a2ui` is absent. This is verify-only (261 D-06 shipped the move); if the wave ordering is somehow wrong, fix it here as a publish-correctness touch and re-run fmt if any manifest changed.

STEP D — Version resolution (D-11) — read crates.io, do NOT trust local refs:
```
curl -s https://crates.io/api/v1/crates/ferro-rs | python3 -c "import sys,json; print(json.load(sys.stdin)['crate']['newest_version'])"
```
- If crates.io newest < 0.2.91 → keep workspace at 0.2.91 (no Cargo.toml change).
- If crates.io newest >= 0.2.91 → edit Cargo.toml:47 `version = "0.2.91"` to `version = "0.2.{max_patch+1}"` where max_patch is the crates.io newest patch. Single-field edit only; re-run `cargo fmt --all -- --check`.
Also check `git tag | grep -c v0.2.91` — if the tag exists, treat 0.2.91 as taken and bump per the >= branch.
Record the resolved version and the crates.io reading for the operator checklist.

STEP E — ferro-payments rider check (D-12) — read crates.io:
```
curl -s https://crates.io/api/v1/crates/ferro-payments | python3 -c "import sys,json; print(json.load(sys.stdin)['crate']['newest_version'])"
```
- If crates.io ferro-payments == 0.1.6 → it will NOT re-publish (cargo skips already-published); note this.
- If crates.io is behind 0.1.6 → it rides this publish; note that verification must confirm both.
No ferro-payments code changes; no forced bump.

STEP F — Dry-run the package build:
```
cargo publish -p ferro-rs --dry-run
```
Must exit 0.

STEP G — Stage SPECIFIC files only (D-15). The staged set is exactly the phase's real changes:
```
git add ferro-mcp/src/tools/generation_context.rs \
        docs/src/json-ui/components.md docs/src/json-ui/runtime-primitives.md \
        docs/src/features/ferro-assets.md docs/src/features/projections.md
# add Cargo.toml ONLY if STEP D bumped it; add docs/src/SUMMARY.md ONLY if a new page was created;
# add ferro-json-ui/src/catalog.rs ONLY if Plan 01 Task 2 added a LiveFragment prop description.
git add <those files if applicable>
```
NEVER `git add -A` / `git add .`. Run `git status` and confirm NO `app/frontend/node_modules/.vite/deps_temp_*`, `.planning/config.json`, or phantom `planning/phases/158-...` deletion is staged.

STEP H — Do NOT commit-push yet. Task 2 is the operator gate. Assemble the pre-publish checklist: gate results (fmt/clippy/test/doc all green), resolved ferro-rs version, ferro-payments rider status, publish.yml wave confirmation, exact staged file list, and dry-run result.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --all --all-targets --all-features -- -D warnings && cargo test --all-features && RUSTDOCFLAGS="-Dwarnings" cargo doc --no-deps --all-features && cargo publish -p ferro-rs --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy --all --all-targets --all-features -- -D warnings` exits 0
    - `cargo test --all-features` exits 0
    - `RUSTDOCFLAGS="-Dwarnings" cargo doc --no-deps --all-features` exits 0
    - `cargo publish -p ferro-rs --dry-run` exits 0
    - `grep -q 'ferro-bundle' .github/workflows/publish.yml` (Wave 1a verified) AND `grep -c 'ferro-a2ui' .github/workflows/publish.yml` returns 0
    - `git status --porcelain` shows NO staged path matching `node_modules/.vite/deps_temp_` or `.planning/config.json` or `planning/phases/158-`
    - Resolved version recorded; crates.io ferro-rs and ferro-payments readings recorded
  </acceptance_criteria>
  <done>CI-exact gate green, dry-run passes, version resolved against crates.io, ferro-payments rider determined, publish.yml verified, only the phase's real files staged, pre-publish checklist assembled. No push yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Pre-publish checklist + operator publish approval</name>
  <what-built>
    A green CI-exact gate (fmt + clippy --all-features + test --all-features + cargo doc -D warnings all exit 0), a passing `cargo publish -p ferro-rs --dry-run`, the resolved workspace version (Task 1 STEP D), the ferro-payments rider determination (Task 1 STEP E), the publish.yml wave confirmation (Task 1 STEP C), and the phase's real changes staged (Task 1 STEP G): the generation_context guidance, the four doc sections, and (conditionally) the Cargo.toml version bump.
  </what-built>
  <how-to-verify>
    Present this pre-publish checklist to the operator and wait for explicit approval (D-10):
    1. Gate results: fmt / clippy --all-features / test --all-features / cargo doc -D warnings — all green (paste the exit-0 confirmations).
    2. Resolved ferro-rs version: 0.2.91 as-is (if crates.io < 0.2.91) OR crates.io_max+1 (if crates.io >= 0.2.91). Re-confirm the crates.io reading (`curl … | python3 -c "…newest_version…"`).
    3. ferro-payments rider: re-publishes 0.1.6 (if crates.io behind) or skips (if crates.io already 0.1.6). No v17.0 code changes.
    4. publish.yml wave order confirmed: ferro-bundle in Wave 1a; ferro-a2ui absent; no new crates → no wave changes.
    5. Exact staged file list (`git status --porcelain`): only generation_context.rs + the four docs pages (+ Cargo.toml / SUMMARY.md / catalog.rs if applicable). NO .vite / .planning/config.json / phantom-158 pollution.
    6. What ships: LiveFragment + #[memoize] + asset!() surfaced through generation_context + docs. CI will publish to crates.io + tag + build binaries + brew tap.
    Approve the irreversible push to master.
  </how-to-verify>
  <action>
    Pause here (blocking checkpoint). Present the pre-publish checklist above and wait for explicit
    operator approval before Task 3 pushes. Do NOT commit, push, publish, or run any irreversible
    git/network command until the operator responds with the resume signal. If the operator reports an
    issue, stop and surface it rather than proceeding.
  </action>
  <resume-signal>Type "approved" to authorize the publish push, or describe issues to fix first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit, push, and post-publish verification</name>
  <files>(git operations only — no file edits)</files>
  <read_first>
    - .planning/phases/262-mcp-catalog-docs-publish/262-CONTEXT.md D-10, D-14, D-15
  </read_first>
  <action>
Runs ONLY after the Task 2 operator approval. Publishes the single v17.0 release.

STEP A — Assert topology from the repo root (feedback_worktree_merge_cwd_trap): `git -C <repo-root> branch --show-current` must be `master`.

STEP B — Commit with a clean message (NO co-author line, NO "Generated with" attribution — per CLAUDE.md git rules):
```
git commit -m "docs(262): surface LiveFragment/#[memoize]/asset!() via generation_context + docs; publish v17.0"
```

STEP C — Push via the HTTPS credential helper (SSH is denied for this repo):
```
git -c credential.helper='!gh auth git-credential' push https://github.com/albertogferrario/ferro.git master
```

STEP D — Re-sync the stale local ref (local origin/* refs recurrently lie):
```
git update-ref refs/remotes/origin/master HEAD
```

STEP E — Post-publish verification via crates.io / gh API ONLY (never local origin/* refs). Poll until the Publish workflow completes:
```
gh run list --workflow publish.yml --limit 1
curl -s https://crates.io/api/v1/crates/ferro-rs/versions | python3 -c "import sys,json; print([v['num'] for v in json.load(sys.stdin)['versions'][:3]])"
```
Confirm the published ferro-rs version exceeds 0.2.89 (SC-4). If ferro-payments rode (Task 1 STEP E), confirm its published version too:
```
curl -s https://crates.io/api/v1/crates/ferro-payments | python3 -c "import sys,json; print(json.load(sys.stdin)['crate']['newest_version'])"
```
Record the published versions and the workflow conclusion in the SUMMARY.
  </action>
  <verify>
    <automated>curl -s https://crates.io/api/v1/crates/ferro-rs | python3 -c "import sys,json; v=json.load(sys.stdin)['crate']['newest_version']; parts=[int(x) for x in v.split('.')]; assert parts >= [0,2,90], f'{v} not > 0.2.89'; print('OK', v)"</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` is `master` at commit time
    - Commit message contains no "Co-Authored-By" / "Generated with" line
    - Push succeeds via the gh HTTPS credential helper
    - `git update-ref refs/remotes/origin/master HEAD` run after the verified push
    - crates.io ferro-rs newest version > 0.2.89 (verified via crates.io API, not local refs)
    - ferro-payments version confirmed on crates.io (rode or already-current)
    - Published versions + Publish workflow conclusion recorded in the SUMMARY
  </acceptance_criteria>
  <done>The single v17.0 publish landed: ferro-rs (> 0.2.89) and any ferro-payments rider confirmed on crates.io; local origin/master re-synced; verification via crates.io/gh API only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local tree → crates.io (public, irreversible) | Publishing pushes an immutable artifact to a public registry; wrong version or leaked secret is unrecoverable |
| operator → publish authorization | The irreversible action requires explicit human go — not run autonomously |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-262-06 | Elevation of privilege / Tampering (supply-chain integrity) | crates.io publish of ferro-rs | mitigate | Publish is operator-gated (checkpoint:human-verify, blocking) with a pre-publish checklist; version resolved against crates.io at gate time so the artifact is a genuine advance, not a clobber; dry-run proves the package builds before push |
| T-262-07 | Information disclosure (accidental secret/artifact inclusion) | the publish commit staging | mitigate | Specific-file staging only (D-15) — no `git add -A`; `git status` audited so no .vite cache, .planning/config.json, or phantom path enters the commit; the crate manifests carry no secrets and this phase adds only guidance/docs |
| T-262-08 | Repudiation (untraceable version state) | post-publish version verification | mitigate | Verify via crates.io / gh API only (never local origin/* refs, which recurrently lie); `git update-ref` re-syncs the local ref after a verified push |
| T-262-09 | Tampering (double-bump / tag collision) | version resolution vs CI auto-bump | mitigate | If `v0.2.91` tag exists, manually bump to crates.io_max+1 so cargo publishes directly with no CI double-bump (D-11); the resolved version is recorded and shown in the checklist |
</threat_model>

<verification>
- Full CI-exact gate green (fmt + clippy --all-features + test --all-features + cargo doc -D warnings)
- `cargo publish -p ferro-rs --dry-run` exits 0
- publish.yml wave order confirmed (ferro-bundle Wave 1a; ferro-a2ui absent)
- Only the phase's real files staged (D-15 hygiene)
- Post-publish: crates.io ferro-rs version > 0.2.89 (verified via crates.io/gh API, not local refs)
- Operator approval recorded before the push
</verification>

<success_criteria>
- The full CI-exact gate is green (SC-4)
- `cargo publish -p ferro-rs` exits 0 and the published version exceeds 0.2.89 (SC-4)
- Single operator-gated publish; no mid-milestone publishes
- Post-publish verification via crates.io / gh API; local origin/master re-synced
</success_criteria>

<output>
After completion, create `.planning/phases/262-mcp-catalog-docs-publish/262-03-SUMMARY.md`
</output>
