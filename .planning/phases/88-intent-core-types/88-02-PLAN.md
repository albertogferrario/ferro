---
phase: 88-intent-core-types
plan: 02
type: execute
wave: 2
depends_on: ["88-01"]
files_modified: [ferro-projections/src/intent.rs, ferro-projections/src/service.rs]
autonomous: true
---

<objective>
Comprehensive test suite validating Intent types, serde behavior, JSON Schema, and ServiceDef integration with intent hints.

Purpose: Prove all Phase 88 types work correctly — serialization, deserialization, builder chains, validation warnings, and JSON Schema generation.
Output: ~20-25 new tests bringing ferro-projections total to ~140+.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/88-intent-core-types/88-01-SUMMARY.md

@ferro-projections/src/intent.rs
@ferro-projections/src/service.rs
@ferro-projections/src/state.rs
@ferro-projections/src/field.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Intent type tests — serde, schema, construction</name>
  <files>ferro-projections/src/intent.rs</files>
  <action>
Add `#[cfg(test)] mod tests { ... }` to intent.rs with comprehensive tests:

**Intent serde tests:**
- `intent_serde_round_trip` — round-trip all 7 known variants (Browse through Track)
- `intent_custom_fallback` — deserialize unknown string (e.g., `"configure"`) → Custom("configure")
- `intent_custom_round_trip` — Custom("my_intent") → serialize → deserialize → same value
- `intent_known_not_custom` — deserialize `"browse"` matches Browse, not Custom("browse"). Same pattern as FieldMeaning test.
- `intent_snake_case_serialization` — verify specific serialized values (e.g., Browse → `"browse"`, FreeText-style naming not needed here but verify consistency)

**IntentScore tests:**
- `intent_score_construction` — create IntentScore with intent, confidence, matching_signals, verify fields
- `intent_score_serde_round_trip` — round-trip with non-trivial matching_signals vec
- `intent_score_empty_signals` — round-trip with empty matching_signals

**IntentHint tests:**
- `intent_hint_primary_serde` — Primary(Intent::Browse) → `{"primary":"browse"}` → round-trip
- `intent_hint_exclude_serde` — Exclude(Intent::Process) → `{"exclude":"process"}` → round-trip
- `intent_hint_primary_custom` — Primary(Intent::Custom("special".into())) → serde round-trip
- `intent_hint_exclude_custom` — Exclude(Intent::Custom("niche".into())) → serde round-trip

**JSON Schema tests:**
- `intent_json_schema_has_description` — Intent schema has description annotation listing known variants (matches FieldMeaning pattern)
- `intent_score_json_schema` — IntentScore schema has properties: intent, confidence, matching_signals
- `intent_hint_json_schema` — IntentHint schema exists and has structure

**Trait tests:**
- `intent_hash_consistency` — same Intent value produces same hash (needed for HashMap keys in Phase 89)
- `intent_eq_known_vs_custom` — Browse != Custom("browse") (critical correctness test)
  </action>
  <verify>cargo test -p ferro-projections -- intent::tests</verify>
  <done>All intent type tests pass, covering serde round-trip, Custom fallback, JSON Schema, Hash consistency</done>
</task>

<task type="auto">
  <name>Task 2: ServiceDef + Intent integration tests</name>
  <files>ferro-projections/src/service.rs</files>
  <action>
Add tests to the existing `#[cfg(test)] mod tests` in service.rs:

**Builder chain tests:**
- `service_def_intent_hint_builder` — ServiceDef::new("svc").intent_hint(IntentHint::Primary(Intent::Process)) → verify intent_hints has 1 entry
- `service_def_multiple_intent_hints` — chain .intent_hint() twice (Primary + Exclude) → verify intent_hints has 2 entries
- `service_def_no_intent_hints_default` — ServiceDef::new("svc") → intent_hints is empty

**Serde tests:**
- `service_def_intent_hints_omitted_when_empty` — JSON output for ServiceDef with no intent hints should not contain "intent_hints" key
- `service_def_intent_hints_serde_round_trip` — ServiceDef with 2 intent hints → serialize → deserialize → equal

**Validation tests:**
- `validate_conflicting_intent_hints_warning` — ServiceDef with Primary(Browse) + Exclude(Browse) → validate returns ConflictingIntentHints warning
- `validate_multiple_primary_intent_hints_warning` — ServiceDef with Primary(Browse) + Primary(Process) → validate returns MultiplePrimaryIntentHints warning
- `validate_non_conflicting_intent_hints_no_warning` — ServiceDef with Primary(Browse) + Exclude(Process) → validate returns no intent-related warnings
- `validate_single_primary_no_warning` — ServiceDef with just Primary(Process) → no MultiplePrimaryIntentHints warning

**Full integration test:**
- `full_service_with_intent_hints` — Build a complete ServiceDef with fields, actions, guards, state machine, relationships, AND intent hints. Verify serde round-trip and validate() passes clean (no warnings).
  </action>
  <verify>cargo test -p ferro-projections && cargo clippy -p ferro-projections --all-targets -- -D warnings</verify>
  <done>All integration tests pass, ~140+ total ferro-projections tests, zero clippy warnings</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo fmt --all -- --check` passes
- [ ] `cargo clippy -p ferro-projections --all-targets -- -D warnings` passes
- [ ] `cargo test -p ferro-projections` — all tests pass (existing 118 + ~20-25 new)
- [ ] Total test count ≥ 135
- [ ] Intent Custom fallback tested (unknown string → Custom)
- [ ] Conflicting hints validation tested
- [ ] Full ServiceDef integration test with all features
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Test coverage matches Phase 84-87 thoroughness: serde round-trip, builder chain, validation, JSON Schema, integration
- Every Phase 88 type has at least serde round-trip + schema test
  </success_criteria>

<output>
After completion, create `.planning/phases/88-intent-core-types/88-02-SUMMARY.md`
</output>
