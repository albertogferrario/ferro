---
phase: 205-fix-ferro-mcp-server-tools-call-result-content-blocks-wrap-p
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified: []
autonomous: false
requirements:
  - AMCP-03
  - AMCP-10
nyquist_compliant: true

must_haves:
  truths:
    - "A real MCP client (Claude Code's SDK) parses the list_order result with NO Zod errors"
    - "alice@acme.test sees exactly 2 of 4 orders (Acme tenant scoping intact end-to-end)"
    - "The browser-OAuth login → consent → token → tools/call chain completes against the live :8090 binary"
  artifacts:
    - path: ".planning/phases/205-fix-ferro-mcp-server-tools-call-result-content-blocks-wrap-p/205-ACCEPTANCE.md"
      provides: "Recorded GO/NO-GO verdict of the live dogfood with observed row count and parse result"
  key_links:
    - from: "Claude Code MCP client"
      to: "http://127.0.0.1:8090/mcp list_order"
      via: "browser-OAuth (chrome-devtools-3) → bearer token → tools/call"
      pattern: "list_order"
---

<objective>
Re-run the live `:8090` browser-OAuth dogfood end-to-end (D-06) — the phase's GO/NO-GO acceptance
gate, not just a unit pass. A real MCP client (Claude Code's SDK) must parse the `list_order` result
with no Zod errors (proving Plan 01's content-block fix works against a strict client), and tenant
scoping must still return only Acme's 2 of 4 orders (proving Plan 02's non-regression holds live).

This wave runs ONLY after Plans 01 and 02 are green and the full fmt/clippy/test gate passes. It is
`autonomous: false` — a human must observe "no Zod errors" in Claude Code's MCP output and confirm
the row count, because the flow requires a live browser-OAuth session and a real MCP client SDK that
cannot be driven from `cargo test`.

Purpose: confirm the fix resolves the originally-reported client-side defect end to end.
Output: `205-ACCEPTANCE.md` with the GO/NO-GO verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/205-fix-ferro-mcp-server-tools-call-result-content-blocks-wrap-p/205-CONTEXT.md
@.planning/phases/205-fix-ferro-mcp-server-tools-call-result-content-blocks-wrap-p/205-VALIDATION.md
@.planning/phases/205-fix-ferro-mcp-server-tools-call-result-content-blocks-wrap-p/205-01-SUMMARY.md
@.planning/phases/205-fix-ferro-mcp-server-tools-call-result-content-blocks-wrap-p/205-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the app binary with the fix</name>
  <files>(none — produces target/debug/app)</files>
  <read_first>
    - .planning/phases/205-fix-ferro-mcp-server-tools-call-result-content-blocks-wrap-p/205-01-SUMMARY.md (confirm the jsonrpc.rs fix landed)
    - .planning/phases/205-fix-ferro-mcp-server-tools-call-result-content-blocks-wrap-p/205-02-SUMMARY.md (confirm full gate green)
    - .planning/phases/205-fix-ferro-mcp-server-tools-call-result-content-blocks-wrap-p/205-RESEARCH.md (§Environment Availability — binary may be stale)
  </read_first>
  <action>
The live binary at `target/debug/app` may predate the fix. Rebuild it so the dogfood exercises the
patched envelope:

```bash
cargo build --bin app
```

Check `df -h` first; if disk is tight, clean stale artifacts before building (ENOSPC is a known
tooling issue on this machine, not a defect). Do NOT start the server — the operator runs it
(`cd app && ../target/debug/app`, port 8090) per project convention (servers are user-run).
  </action>
  <verify>
    <automated>cargo build --bin app 2>&1 | tail -3 && test -x target/debug/app && echo BINARY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --bin app` exits 0
    - `test -x target/debug/app` exits 0 (binary present and executable)
  </acceptance_criteria>
  <done>target/debug/app rebuilt with the Plan 01 fix; ready for the operator to run on :8090.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live :8090 browser-OAuth dogfood (D-06 acceptance gate)</name>
  <files>.planning/phases/205-fix-ferro-mcp-server-tools-call-result-content-blocks-wrap-p/205-ACCEPTANCE.md (verdict captured by Task 3)</files>
  <read_first>
    - .planning/phases/205-fix-ferro-mcp-server-tools-call-result-content-blocks-wrap-p/205-CONTEXT.md (D-06 definition)
    - .planning/phases/205-fix-ferro-mcp-server-tools-call-result-content-blocks-wrap-p/205-VALIDATION.md (§Manual-Only Verifications)
  </read_first>
  <what-built>
The full fix is implemented and unit/integration green: jsonrpc.rs emits a valid CallToolResult
(`type:text` content block + structuredContent), the interop regression test parses it with the
client's own type, and both tenant-isolation tests pass against the new envelope. The app binary is
rebuilt at target/debug/app.
  </what-built>
  <action>
HUMAN-VERIFY checkpoint (autonomous: false). The agent does not perform this — the operator runs the
live dogfood and reports the verdict. See &lt;how-to-verify&gt; for the exact steps and &lt;resume-signal&gt;
for the expected response. After the operator responds, Task 3 records the verdict.
  </action>
  <how-to-verify>
Run the live `:8090` browser-OAuth dogfood (harness recorded in operator memory
`project_ferro_mcp_toolcall_content_bug.md`). Steps:

1. Start the server (operator): `cd app && ../target/debug/app` — listens on port 8090,
   uses `app/.env`, seeded `app/database.db` (alice@acme.test, tenant Acme).
2. Ensure Claude Code has MCP server `ferro-sample-app` → `http://127.0.0.1:8090/mcp`.
3. Clear stale chrome session: remove `/tmp/chrome-mcp-3/Singleton{Lock,Socket,Cookie}` before
   driving OAuth via chrome-devtools-3.
4. Drive the OAuth login: the dev `/auth/verify?token=…` link is in the DOM (the button href is `#`)
   — grab it from the page, not the button.
5. Approve the consent screen; let the token exchange complete.
6. From the MCP client, call `list_order` for alice@acme.test.

Expected (GO):
- (a) Claude Code's MCP client parses the result with NO Zod errors (the original failure mode is gone).
- (b) The result contains exactly 2 orders (Acme's 2 of the 4 seeded) — tenant scoping intact.

If either fails → NO-GO: capture the exact client error / observed row count and stop; do not mark
the phase verified.
  </how-to-verify>
  <verify>
    <automated>MANUAL — operator-driven live browser-OAuth dogfood; verdict recorded by Task 3 in 205-ACCEPTANCE.md (no cargo command can drive a real MCP client SDK + browser session)</automated>
  </verify>
  <resume-signal>
Type "GO — 2 orders, no Zod errors" to record acceptance, or "NO-GO — <observed error / row count>" to record a failure.
  </resume-signal>
  <done>Operator has reported GO (no Zod errors AND exactly 2 Acme orders) or NO-GO with the observed failure.</done>
</task>

<task type="auto">
  <name>Task 3: Record the dogfood verdict in 205-ACCEPTANCE.md</name>
  <files>.planning/phases/205-fix-ferro-mcp-server-tools-call-result-content-blocks-wrap-p/205-ACCEPTANCE.md</files>
  <read_first>
    - .planning/phases/205-fix-ferro-mcp-server-tools-call-result-content-blocks-wrap-p/205-CONTEXT.md (D-06 acceptance definition)
    - .planning/phases/200-per-tenant-scoping-policy-authorization-dogfood-acceptance/200-ACCEPTANCE.md (prior dogfood acceptance record format, if present, for structure parity)
  </read_first>
  <action>
Write `205-ACCEPTANCE.md` recording the human verdict from the Task 2 checkpoint: GO or NO-GO, the
observed order count for alice@acme.test, whether the MCP client reported any Zod/parse errors, the
date, and the harness used (`:8090`, chrome-devtools-3). Keep it neutral and factual (this file is
committed — no internal-strategy voice). If NO-GO, record the exact error and mark the phase blocked.
  </action>
  <verify>
    <automated>test -f .planning/phases/205-fix-ferro-mcp-server-tools-call-result-content-blocks-wrap-p/205-ACCEPTANCE.md && grep -Eq "GO|NO-GO" .planning/phases/205-fix-ferro-mcp-server-tools-call-result-content-blocks-wrap-p/205-ACCEPTANCE.md && echo ACCEPTANCE_RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - `205-ACCEPTANCE.md` exists in the phase directory
    - It contains a "GO" or "NO-GO" verdict and the observed order count
  </acceptance_criteria>
  <done>205-ACCEPTANCE.md records the dogfood GO/NO-GO verdict, observed row count, and parse-error status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser-OAuth client → live :8090 MCP endpoint | Full untrusted-client path. This dogfood is the end-to-end confirmation that the authorized, tenant-scoped row set reaches a strict client correctly and without cross-tenant leakage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-205-06 | Information Disclosure (cross-tenant) | live list_order under alice@acme.test | mitigate | The dogfood asserts alice sees exactly 2 of 4 orders — live evidence that the Plan 01 envelope change did not widen the row set or leak the other tenant's 2 orders. GO requires both no-Zod-error AND row-count==2. |
| T-205-07 | Spoofing/Elevation | OAuth login + token exchange | accept | The OAuth/consent/token chain is unchanged from v12.6 (Phases 197-200, already verified). This phase only re-exercises it to confirm the result-formatting fix; no auth surface is modified here. |
</threat_model>

<verification>
- Task 1: `cargo build --bin app` clean; binary present.
- Checkpoint: human confirms (a) no Zod errors and (b) exactly 2 orders for alice@acme.test.
- Task 3: `205-ACCEPTANCE.md` written with the GO/NO-GO verdict.
- Phase gate: GO verdict is required before `/gsd-verify-work`. A NO-GO blocks the phase and triggers
  gap analysis (capture the exact client error in the acceptance file).
</verification>

<success_criteria>
- Live MCP client parses `list_order` with no Zod errors (AMCP-03 confirmed end-to-end).
- alice@acme.test sees exactly 2 of 4 orders (AMCP-10 confirmed end-to-end).
- `205-ACCEPTANCE.md` records GO with observed row count and parse status.
</success_criteria>

<output>
After completion, create `.planning/phases/205-fix-ferro-mcp-server-tools-call-result-content-blocks-wrap-p/205-03-SUMMARY.md`
and ensure `205-ACCEPTANCE.md` exists with the recorded verdict.
</output>
