# Phase 249.3: Offload result-path terminal-state completeness - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-16
**Phase:** 249.3-offload-result-path-terminal-state-completeness
**Mode:** `--auto` (recommended defaults; interactive AskUserQuestion declined by user, then "Auto" chosen)
**Areas discussed:** Sync-mode fix scope, Sync `.dispatch()` return contract, Reaper timeout envelope, Reaper write seam

---

## Sync-mode fix scope

| Option | Description | Selected |
|--------|-------------|----------|
| Full hook reuse | Route sync mode through `persist_offload_outcome` → snapshot + redacted delta, full async parity | ✓ |
| Snapshot-only | Persist snapshot only for sync (SC#1 needs read-back only), skip the delta | |

**User's choice:** Full hook reuse (recommended default, auto-selected).
**Notes:** The hook already does persist + best-effort broadcast, so parity is the minimal-code path and avoids a sync/async divergence.

---

## Sync `.dispatch()` return contract

| Option | Description | Selected |
|--------|-------------|----------|
| Keep `Err` + persist | On failure, return `Err(e)` inline AND persist the failed envelope (dual signal, non-breaking) | ✓ |
| Match async | `dispatch()` returns `Ok(())`; failure observable only via the handle | |

**User's choice:** Keep `Err` + persist (recommended default, auto-selected).
**Notes:** SC#1 is satisfied by the persisted envelope; the raw `Result` return stays a lower-level dev/test convenience.

---

## Reaper timeout envelope

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse existing message | Route parked handle-key rows through the hook; envelope error = `"visibility timeout exceeded"` (matches `jobs.error`) | ✓ |
| Distinct message | Route through the hook but with a more descriptive/distinguishable error string | |

**User's choice:** Reuse existing message (recommended default, auto-selected).
**Notes:** SC#2 requires the subscriber to wake, so the reaper must emit a delta (full hook), not just a snapshot. Client-facing delta stays redacted (247 D-05); raw string lives only in the snapshot.

---

## Reaper write seam

| Option | Description | Selected |
|--------|-------------|----------|
| Leave to planning | Lock the contract (reaper surfaces parked `handle_key`s); defer the exact seam to planning | ✓ |
| Lock now | Decide the exact restructuring of `db::reaper()` in this discussion | |

**User's choice:** Leave to planning (recommended default, auto-selected).
**Notes:** Mirrors 246 D-05 / 247 D-03, which deferred propagation mechanics to planning. Recommended home: the worker's reaper call-site, which already has the hook and the `&'static` connection.

## Claude's Discretion

- Sync-mode connection source for the hook (`Queue::connection()` vs threaded connection), subject to the research flag on sync-mode connection availability.
- Exact reaper restructuring to surface parked `(id, handle_key)` rows.

## Deferred Ideas

None — discussion stayed within the two-edge hardening scope.
