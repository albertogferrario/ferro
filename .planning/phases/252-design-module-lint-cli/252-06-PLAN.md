---
phase: 252
plan: 06
type: execute
wave: 4
depends_on: [2, 4, 5]
files_modified:
  - app/src/views/login.json
  - app/src/views/login_confirm.json
  - app/src/views/pagamenti.json
  - app/Cargo.toml
  - app/src/tests/design_lint.rs
  - app/src/tests/mod.rs
autonomous: true
requirements: [DS-05, DS-06]

must_haves:
  truths:
    - "All three app views declare `design.intent` and lint with ZERO findings"
    - "`pagamenti.json` gains a PageHeader element so the dashboard-layout page-header rule passes by conformance (not by allow)"
    - "A test in the app crate walks `app/src/views/*.json`, lints each, and asserts zero findings"
    - "The full CI-exact gate (fmt, clippy --all-targets --all-features -D warnings, test --all-features) and the docs build are green"
  artifacts:
    - path: "app/src/tests/design_lint.rs"
      provides: "D-17 app-views lint-clean gate"
      contains: "app_views_lint_clean"
    - path: "app/src/views/pagamenti.json"
      provides: "design.intent + PageHeader element"
      contains: "PageHeader"
    - path: "app/Cargo.toml"
      provides: "ferro-json-ui dev-dependency"
      contains: "ferro-json-ui"
  key_links:
    - from: "app/src/tests/design_lint.rs"
      to: "ferro_json_ui::design::lint"
      via: "walk src/views + assert empty"
      pattern: "design::lint"
    - from: "app/src/tests/mod.rs"
      to: "design_lint module"
      via: "pub mod design_lint;"
      pattern: "pub mod design_lint"
---

<objective>
Dogfood the design field and close the phase: declare `design.intent` on all three
sample `app/` views, add a PageHeader to `pagamenti.json` so it conforms by
construction, add the D-17 test that asserts the app views lint clean (zero findings),
and run the full CI-exact gate + docs build (D-20).

Purpose: the zero-findings gate proves the rule set is coherent against real specs, and
`pagamenti.json` gaining a PageHeader (conformance over the `allow` escape hatch, per
D-17) demonstrates the intended remediation path. This is the phase's final green gate.

Output: app views declare intent and lint clean; the whole workspace passes the CI-exact
gate and the docs build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/252-design-module-lint-cli/252-CONTEXT.md
@.planning/phases/252-design-module-lint-cli/252-RESEARCH.md
@.planning/phases/252-design-module-lint-cli/252-PATTERNS.md

<interfaces>
Public API (Plans 01/03/04): `ferro_json_ui::design::lint(&spec) -> Vec<Finding>`,
`ferro_json_ui::spec::Spec::from_json(&str)`. The design module compiles WITHOUT the
`projections` feature (D-07), so the app dev-dep needs no features.

Rule behavior relevant to the three views (from Plans 03/04):
- `page-header` (all intents) fires only on layout `"dashboard"`/`"app"` → needs a
  PageHeader with a title.
- `breadcrumb-on-subpages` (collect/focus) fires only on layout `"dashboard"`/`"app"` →
  auth pages are exempt (login/login_confirm stay clean).
- `form-default-values` (collect) fires only when a field binds `default_value` via
  `$data` (edit form). login's email Input has `data_path` but no `default_value` →
  pure create form → no finding.
- `destructive-confirmation` (all) fires on `variant:"destructive"` / `destructive:true`
  actions lacking a confirm → none of the three views has one.

App test/dev-dep precedents: `app/src/tests/mod.rs` (module list), `app/Cargo.toml`
`[dev-dependencies]` with `ferro-projections = { path = "../ferro-projections" }`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare design.intent on app views, add PageHeader to pagamenti, add D-17 gate</name>
  <files>app/src/views/login.json, app/src/views/login_confirm.json, app/src/views/pagamenti.json, app/Cargo.toml, app/src/tests/design_lint.rs, app/src/tests/mod.rs</files>
  <read_first>
    - app/src/views/login.json, app/src/views/login_confirm.json, app/src/views/pagamenti.json (current content — exact structure to edit)
    - app/Cargo.toml (the `[dev-dependencies]` block; ferro-projections path-dep precedent)
    - app/src/tests/mod.rs (module registration list)
    - .planning/phases/252-design-module-lint-cli/252-PATTERNS.md (§ app/src/tests/design_lint.rs — verbatim test shape; § app/src/views/*.json — the exact field additions)
    - .planning/phases/252-design-module-lint-cli/252-RESEARCH.md (§ Sample App Views Analysis — predicted findings + PageHeader recommendation)
  </read_first>
  <behavior>
    - `app_views_lint_clean` walks `app/src/views/*.json`, parses each with `Spec::from_json`, calls `lint`, and asserts `findings.is_empty()` for every file (panics with the file + findings on failure).
  </behavior>
  <action>
Edit the three views (declared intent must yield ZERO findings against the full rule set):

**`app/src/views/login.json`** — add `"design": {"intent": "collect"}` immediately after `"layout": "auth",` (line 4). (auth layout → page-header + breadcrumb exempt; email Input has no `$data` default_value → pure create form → no form-default-values finding; no destructive action → clean.)

**`app/src/views/login_confirm.json`** — add `"design": {"intent": "focus"}` immediately after `"layout": "auth",`. (auth layout → exempt; dev_link Button is `variant: "outline"` (not destructive) → clean.)

**`app/src/views/pagamenti.json`** — add `"design": {"intent": "summarize"}` immediately after `"layout": "dashboard",` AND add a PageHeader element so the (all-intents, dashboard-layout) page-header rule passes by conformance:
  - Add to `elements`:
    ```json
    "page_header": {
      "type": "PageHeader",
      "props": { "title": "Pagamenti" }
    }
    ```
  - Change `root.children` from `["stats_row", "payments_table"]` to `["page_header", "stats_row", "payments_table"]`.
  (summarize intent → no browse/collect/process/focus rules run; PageHeader present with title → page-header passes; no destructive action → clean.)

Add `ferro-json-ui = { path = "../ferro-json-ui" }` to `app/Cargo.toml` under `[dev-dependencies]` (Pitfall 3 — the transitive dep through `ferro` is not enough to `use ferro_json_ui` in a test).

Create `app/src/tests/design_lint.rs` (PATTERNS.md § app test — verbatim):
```rust
//! D-17 lint-clean gate: every view under `app/src/views/*.json` must lint clean
//! (zero findings) with `design.intent` declared.

#[cfg(test)]
mod tests {
    use ferro_json_ui::design::lint;
    use ferro_json_ui::spec::Spec;

    #[test]
    fn app_views_lint_clean() {
        let views_dir = concat!(env!("CARGO_MANIFEST_DIR"), "/src/views");
        let entries = std::fs::read_dir(views_dir).expect("app/src/views must exist");
        for entry in entries.flatten() {
            let path = entry.path();
            if path.extension().and_then(|e| e.to_str()) != Some("json") {
                continue;
            }
            let content = std::fs::read_to_string(&path)
                .unwrap_or_else(|e| panic!("read {}: {e}", path.display()));
            let spec = Spec::from_json(&content)
                .unwrap_or_else(|e| panic!("parse {}: {e:?}", path.display()));
            let findings = lint(&spec);
            assert!(
                findings.is_empty(),
                "{}: {} finding(s)\n{:#?}",
                path.display(),
                findings.len(),
                findings
            );
        }
    }
}
```

Register it: add `pub mod design_lint;` to `app/src/tests/mod.rs` in alphabetical order.

If any view still produces a finding, prefer fixing the spec toward conformance (as with pagamenti's PageHeader); use `"allow": ["<rule-id>"]` only as a last resort per D-17 — record any `allow` and its reason in the SUMMARY.
  </action>
  <verify>
    <automated>cargo test -p app design_lint 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"intent": "collect"' app/src/views/login.json`
    - `grep -q '"intent": "focus"' app/src/views/login_confirm.json`
    - `grep -q '"intent": "summarize"' app/src/views/pagamenti.json` and `grep -q '"PageHeader"' app/src/views/pagamenti.json` and `grep -q '"page_header"' app/src/views/pagamenti.json`
    - `grep -q 'ferro-json-ui = { path = "../ferro-json-ui" }' app/Cargo.toml`
    - `grep -q "pub mod design_lint;" app/src/tests/mod.rs`
    - `cargo test -p app design_lint` runs `app_views_lint_clean` and it PASSES (zero findings across all three views)
    - All three views still parse as valid `ferro-json-ui/v2` specs (the test asserts this)
  </acceptance_criteria>
  <done>All three app views declare intent, pagamenti conforms via a PageHeader, and the D-17 zero-findings gate passes.</done>
</task>

<task type="auto">
  <name>Task 2: Full CI-exact gate + docs build</name>
  <files>(gate only — no file modifications; runs fmt/clippy/test/doc)</files>
  <read_first>
    - CLAUDE.md (Testing & Linting section — exact CI commands)
    - .planning/phases/252-design-module-lint-cli/252-CONTEXT.md (D-20)
  </read_first>
  <action>
Run the CI-exact gate ONE COMMAND AT A TIME (per the CPU-serialization operating rule —
never chain/parallelize cargo/clippy/test). Before the `--all-features` test, verify
disk headroom (`df -h .`) and, if low, `cargo clean` stale artifacts — the
`--all-features` test is known to overflow small disks.

1. `cargo fmt --all -- --check` (exit 0 — run this AFTER any hand-authored file, non-canonical formatting fails the gate).
2. `cargo clippy --all --all-targets --all-features -- -D warnings` (exit 0 — this is the exact CI clippy; `--all-features` surfaces the drift test + feature-gated code the plain command misses).
3. `cargo test --all-features` (exit 0 — enables ferro-json-ui `projections` so the D-08 drift test runs; runs the design, catalog, CLI, and app-views tests).
4. Docs build: `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --all-features` (exit 0 — the new public `design` module ships rustdoc; missing/malformed `///` docs fail here).

Expected benign churn: `cargo test` regenerates `docs/protocol/schemas/*.json` (Phase 94
export test). Schema changes that reflect the NEW `Spec.design` field / `Finding` /
`Severity` types are LEGITIMATE (keep them). Any unrelated pre-existing CRUD-export drift
should be reverted (`git checkout -- <path>`), not folded into this phase — note which is
which in the SUMMARY. Do not commit here; the orchestrator handles the commit.

If a gate step fails, fix the underlying issue (formatting, a clippy lint incl.
`--all-features`-only ones, a missing rustdoc comment) and re-run only the failed step
plus a final confirming pass. Do not weaken the commands.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && echo FMT_OK; cargo clippy --all --all-targets --all-features -- -D warnings 2>&1 | tail -5; cargo test --all-features 2>&1 | tail -15; RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --all-features 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0 (no output)
    - `cargo clippy --all --all-targets --all-features -- -D warnings` exits 0 (no warnings)
    - `cargo test --all-features` exits 0 — includes the design drift test, catalog Stage 2b tests, CLI design_lint tests, and the app `app_views_lint_clean` test
    - `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --all-features` exits 0
    - `git status` shows only expected changes (design source + app views + schema regen reflecting the new design field); no unrelated churn folded in
  </acceptance_criteria>
  <done>The full CI-exact gate and the docs build are green across the workspace; benign schema regeneration identified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app view JSON → Spec::from_json → lint (in test) | Fixed, in-repo fixtures; no untrusted input at this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-252-07 | Tampering (silent regression) | app-views lint-clean gate | mitigate | `app_views_lint_clean` runs under `cargo test --all-features` in CI — any future view or rule change that reintroduces a finding fails the build |
| T-252-01 | Elevation of Privilege | — | accept | This plan edits in-repo fixtures + runs the gate; no auth/network/persistence surface |
</threat_model>

<verification>
- `cargo test -p app design_lint` green (app views lint clean).
- Full CI-exact gate green: `cargo fmt --all -- --check`, `cargo clippy --all --all-targets --all-features -- -D warnings`, `cargo test --all-features`.
- `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --all-features` green.
</verification>

<success_criteria>
- The sample `app/` views lint clean, enforced by a test (DS-05; ROADMAP success criterion 5).
- `pagamenti.json` conforms via a PageHeader rather than an `allow` escape hatch (D-17).
- The design field, lint engine, and CLI are fully wired and pass the CI-exact gate + docs build (DS-05, DS-06; D-20).
</success_criteria>

<output>
After completion, create `.planning/phases/252-design-module-lint-cli/252-06-SUMMARY.md`.
</output>
