---
phase: 84-service-identity-field-semantics
plan: 02
type: execute
wave: 2
depends_on: ["84-01"]
files_modified:
  - ferro-projections/src/field.rs
  - ferro-projections/src/service.rs
autonomous: true
---

<objective>
Add comprehensive test suite for all ferro-projections types, verifying serde round-trips, builder chains, inference rules, and type properties.

Purpose: Ensure all Phase 84 types serialize correctly and behave as documented. Tests serve as living documentation for subsequent phases (85-93).
Output: Passing test suite covering all types and edge cases.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-service-identity-field-semantics/84-RESEARCH.md
@.planning/phases/84-service-identity-field-semantics/84-01-SUMMARY.md
@ferro-projections/src/field.rs
@ferro-projections/src/service.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Field type tests — DataType, FieldMeaning, FieldDef, infer_meaning</name>
  <files>ferro-projections/src/field.rs</files>
  <action>
Add `#[cfg(test)] mod tests { ... }` at the bottom of `ferro-projections/src/field.rs` with these test cases:

**DataType tests:**
- `data_type_serde_round_trip` — serialize each of the 10 variants to JSON string, deserialize back, assert equality. E.g., `DataType::DateTime` → `"date_time"` → `DataType::DateTime`.
- `data_type_is_copy` — verify Copy semantics: `let a = DataType::Float; let b = a; assert_eq!(a, b);`

**FieldMeaning tests:**
- `field_meaning_known_variants_round_trip` — test all 18 known variants serialize to snake_case and deserialize back correctly. E.g., `FieldMeaning::ForeignKey` → `"foreign_key"` → `FieldMeaning::ForeignKey`.
- `field_meaning_custom_fallback` — deserialize an unknown string `"tax_rate"` → `FieldMeaning::Custom("tax_rate".to_string())`.
- `field_meaning_custom_round_trip` — `FieldMeaning::Custom("my_thing".into())` → `"my_thing"` → `FieldMeaning::Custom("my_thing".into())`.
- `field_meaning_known_not_custom` — deserialize `"money"` and assert it matches `FieldMeaning::Money`, NOT `FieldMeaning::Custom("money".into())`. This validates variant ordering.

**FieldDef tests:**
- `field_def_serde_round_trip` — create a FieldDef with all fields set, serialize to JSON, deserialize back, assert equality.
- `field_def_defaults` — verify a FieldDef with required=true and is_list=false serializes correctly.

**infer_meaning tests:**
- `infer_id` — `infer_meaning("id")` == `FieldMeaning::Identifier`
- `infer_foreign_key` — `infer_meaning("user_id")` == `FieldMeaning::ForeignKey`
- `infer_email` — `infer_meaning("email")` == `FieldMeaning::Email`
- `infer_created_at` — `infer_meaning("created_at")` == `FieldMeaning::CreatedAt`
- `infer_updated_at` — `infer_meaning("updated_at")` == `FieldMeaning::UpdatedAt`
- `infer_timestamp_suffix` — `infer_meaning("deleted_at")` == `FieldMeaning::DateTime`
- `infer_boolean_prefix` — `infer_meaning("is_active")` == `FieldMeaning::Boolean` and `infer_meaning("has_verified")` == `FieldMeaning::Boolean`
- `infer_sensitive` — `infer_meaning("password_hash")` == `FieldMeaning::Sensitive` and `infer_meaning("api_key")` == `FieldMeaning::Sensitive`
- `infer_fallback` — `infer_meaning("title")` == `FieldMeaning::Custom("title".into())`
  </action>
  <verify>cargo test -p ferro-projections -- field::tests passes all tests</verify>
  <done>All DataType, FieldMeaning, FieldDef, and infer_meaning tests pass</done>
</task>

<task type="auto">
  <name>Task 2: ServiceDef tests + full verification</name>
  <files>ferro-projections/src/service.rs</files>
  <action>
Add `#[cfg(test)] mod tests { ... }` at the bottom of `ferro-projections/src/service.rs` with these test cases:

**Builder tests:**
- `service_def_minimal` — `ServiceDef::new("order")` creates service with name="order", display_name=None, description=None, fields=empty vec.
- `service_def_full_builder` — chain all builder methods: new → display_name → description → field → optional_field → list_field. Assert all fields set correctly, required/is_list flags correct on each field.
- `service_def_multiple_fields` — add 5+ fields via .field() chain, assert fields.len() == 5 and order preserved.

**Serde tests:**
- `service_def_serde_round_trip` — build a ServiceDef with display_name, description, and 3 fields (one required, one optional, one list). Serialize to JSON string, deserialize back, assert full equality.
- `service_def_json_structure` — serialize a ServiceDef and parse with serde_json::Value. Assert JSON has "name", "display_name", "description", "fields" keys. Assert fields array has correct length. This validates the external JSON contract.

**Integration test:**
- `order_service_example` — build the full order service from the research code example:
  ```rust
  ServiceDef::new("order")
      .display_name("Order")
      .description("Manages customer orders and fulfillment")
      .field("id", DataType::Integer, FieldMeaning::Identifier)
      .field("customer_id", DataType::Integer, FieldMeaning::ForeignKey)
      .field("total", DataType::Float, FieldMeaning::Money)
      .field("status", DataType::String, FieldMeaning::Status)
      .field("email", DataType::String, FieldMeaning::Email)
      .field("notes", DataType::String, FieldMeaning::FreeText)
      .field("created_at", DataType::DateTime, FieldMeaning::CreatedAt)
      .field("updated_at", DataType::DateTime, FieldMeaning::UpdatedAt)
  ```
  Assert 8 fields, serde round-trip, specific field properties (e.g., fields[2].meaning == FieldMeaning::Money).

After all tests written, run full verification:
- `cargo fmt --all -- --check`
- `cargo clippy --all --all-targets -- -D warnings`
- `cargo test --all-features`
  </action>
  <verify>cargo test -p ferro-projections passes ALL tests. cargo fmt + clippy + test --all-features all pass.</verify>
  <done>All tests pass, no warnings, no formatting issues, complete test coverage for Phase 84 types</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p ferro-projections` — all tests pass
- [ ] `cargo fmt --all -- --check` — no formatting issues
- [ ] `cargo clippy --all --all-targets -- -D warnings` — no warnings
- [ ] `cargo test --all-features` — full workspace still passes
- [ ] Test count: minimum 15 tests covering all types and edge cases
</verification>

<success_criteria>

- All tests pass (0 failures)
- Minimum 15 test cases covering: DataType serde + Copy, FieldMeaning serde + Custom fallback + ordering, FieldDef serde, infer_meaning rules, ServiceDef builder + serde + integration
- No clippy warnings
- No formatting issues
- Full workspace compiles and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/84-service-identity-field-semantics/84-02-SUMMARY.md`
</output>
