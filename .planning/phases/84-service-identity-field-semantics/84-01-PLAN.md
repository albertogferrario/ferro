---
phase: 84-service-identity-field-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - ferro-projections/Cargo.toml
  - ferro-projections/CLAUDE.md
  - ferro-projections/src/lib.rs
  - ferro-projections/src/error.rs
  - ferro-projections/src/field.rs
  - ferro-projections/src/service.rs
autonomous: true
---

<objective>
Create the ferro-projections workspace crate with all Phase 84 types: ServiceDef, FieldDef, FieldMeaning, DataType, infer_meaning(), and Error.

Purpose: Establish the foundation crate for v9.0 Service Projections. All subsequent phases (85-93) build on these types.
Output: Compiling ferro-projections crate with complete type definitions and builder API.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-service-identity-field-semantics/84-RESEARCH.md
@.planning/phases/v9.0-RESEARCH.md
@.planning/phases/v9.0-STRATEGY.md
@.planning/phases/84-service-identity-field-semantics/CLAUDE.md
@ferro-cache/Cargo.toml
@ferro-cache/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create crate structure + field types</name>
  <files>Cargo.toml, ferro-projections/Cargo.toml, ferro-projections/src/lib.rs, ferro-projections/src/error.rs, ferro-projections/src/field.rs</files>
  <action>
1. Create `ferro-projections/` directory with `Cargo.toml`:
   - name: "ferro-projections"
   - version.workspace, edition.workspace, license.workspace
   - description: "Service projection definitions for the Ferro framework"
   - repository: "https://github.com/albertogferrario/ferro"
   - keywords: ["projections", "service", "schema", "ferro", "web"]
   - dependencies: serde = { version = "1", features = ["derive"] }, serde_json = "1", thiserror = "1.0"
   - No dev-dependencies needed yet (tests use only std + serde_json already in deps)

2. Add "ferro-projections" to workspace members in root `Cargo.toml` (after "ferro-lang", before closing bracket).

3. Create `ferro-projections/src/error.rs`:
   ```rust
   use thiserror::Error;

   #[derive(Error, Debug)]
   pub enum Error {
       #[error("service definition error: {0}")]
       Definition(String),
       #[error("validation error: {0}")]
       Validation(String),
       #[error("serialization error: {0}")]
       Serialization(#[from] serde_json::Error),
   }
   ```

4. Create `ferro-projections/src/field.rs` with:

   **DataType enum** — 10 variants, all Copy:
   ```
   String, Integer, Float, Boolean, DateTime, Date, Json, Binary, Uuid, Enum
   ```
   Derive: `Debug, Clone, Copy, Serialize, Deserialize, PartialEq, Eq`
   Attribute: `#[serde(rename_all = "snake_case")]`

   **FieldMeaning enum** — 18 known variants + Custom(String) fallback:
   ```
   Identifier, ForeignKey, EntityName, Email, Phone, Url, ImageUrl,
   Money, Percentage, Quantity,
   Status, Category, Boolean,
   FreeText, CreatedAt, UpdatedAt, DateTime, Sensitive,
   #[serde(untagged)] Custom(String)  // MUST be last variant
   ```
   Derive: `Debug, Clone, Serialize, Deserialize, PartialEq, Eq`
   Attribute: `#[serde(rename_all = "snake_case")]`
   CRITICAL: `Custom(String)` must be the LAST variant with `#[serde(untagged)]` per-variant attribute. Serde tries variants in order; if Custom is first, it matches everything.

   **FieldDef struct**:
   ```rust
   pub struct FieldDef {
       pub name: String,
       pub data_type: DataType,
       pub meaning: FieldMeaning,
       pub required: bool,  // default: true
       pub is_list: bool,   // default: false
   }
   ```
   Derive: `Debug, Clone, Serialize, Deserialize, PartialEq, Eq`

   **infer_meaning() function** — public utility function that infers FieldMeaning from field name:
   - Exact matches: "id" → Identifier, "email" → Email, "created_at" → CreatedAt, "updated_at" → UpdatedAt
   - Suffix: `_id` → ForeignKey, `_at` → DateTime
   - Prefix: `is_` / `has_` → Boolean
   - Contains: "password", "secret", "token", "api_key", "hashed_key" → Sensitive
   - Fallback: Custom(field_name.to_string())
   This reuses the 7 inference rules already in ferro-cli/make_scaffold.rs and make_api.rs.

5. Create `ferro-projections/src/lib.rs` with crate doc comment and re-exports (placeholder — completed in Task 2).
  </action>
  <verify>cargo check -p ferro-projections compiles without errors</verify>
  <done>ferro-projections crate exists in workspace, DataType/FieldMeaning/FieldDef/infer_meaning defined and compiling</done>
</task>

<task type="auto">
  <name>Task 2: ServiceDef builder + re-exports + CLAUDE.md</name>
  <files>ferro-projections/src/service.rs, ferro-projections/src/lib.rs, ferro-projections/CLAUDE.md</files>
  <action>
1. Create `ferro-projections/src/service.rs` with:

   **ServiceDef struct**:
   ```rust
   pub struct ServiceDef {
       pub name: String,
       pub display_name: Option<String>,
       pub description: Option<String>,
       pub fields: Vec<FieldDef>,
   }
   ```
   Derive: `Debug, Clone, Serialize, Deserialize, PartialEq, Eq`

   **Builder methods** on ServiceDef (consuming `mut self -> Self` pattern, NOT `&mut self -> &mut Self`):
   - `new(name: impl Into<String>) -> Self` — required name, all else None/empty
   - `display_name(mut self, name: impl Into<String>) -> Self`
   - `description(mut self, desc: impl Into<String>) -> Self`
   - `field(mut self, name: impl Into<String>, data_type: DataType, meaning: FieldMeaning) -> Self` — adds required field (required=true, is_list=false)
   - `optional_field(mut self, name: impl Into<String>, data_type: DataType, meaning: FieldMeaning) -> Self` — adds optional field (required=false, is_list=false)
   - `list_field(mut self, name: impl Into<String>, data_type: DataType, meaning: FieldMeaning) -> Self` — adds list field (is_list=true, required=true)

   Do NOT implement Default for ServiceDef — name is always required.

2. Complete `ferro-projections/src/lib.rs`:
   - Crate-level doc comment: "Service projection definitions for the Ferro framework."
   - `mod error;` `mod field;` `mod service;`
   - Re-export all public types: `pub use error::Error;` `pub use field::{DataType, FieldDef, FieldMeaning, infer_meaning};` `pub use service::ServiceDef;`

3. Copy the CLAUDE.md from `.planning/phases/84-service-identity-field-semantics/CLAUDE.md` to `ferro-projections/CLAUDE.md`. Update builder pattern note: builders use `mut self -> Self` (not `&mut Self`).
  </action>
  <verify>cargo check -p ferro-projections compiles. ServiceDef::new("order").display_name("Order").field("id", DataType::Integer, FieldMeaning::Identifier) chain compiles.</verify>
  <done>ServiceDef builder works with chaining, all types re-exported from lib.rs, CLAUDE.md present in crate root</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check -p ferro-projections` succeeds
- [ ] `cargo fmt --all -- --check` passes
- [ ] `cargo clippy -p ferro-projections --all-targets -- -D warnings` passes
- [ ] All types importable via `use ferro_projections::{ServiceDef, FieldDef, FieldMeaning, DataType, infer_meaning, Error}`
</verification>

<success_criteria>

- ferro-projections crate compiles in workspace
- DataType: 10 variants, Copy
- FieldMeaning: 18 known variants + Custom(String) with serde untagged
- FieldDef: name, data_type, meaning, required, is_list
- ServiceDef: builder with new/display_name/description/field/optional_field/list_field
- infer_meaning(): 7+ rules matching existing CLI patterns
- Error type with thiserror
- CLAUDE.md in crate root
- No warnings, formatted code
</success_criteria>

<output>
After completion, create `.planning/phases/84-service-identity-field-semantics/84-01-SUMMARY.md`
</output>
