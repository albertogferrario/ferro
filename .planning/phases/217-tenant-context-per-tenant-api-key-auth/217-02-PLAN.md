---
phase: 217-tenant-context-per-tenant-api-key-auth
plan: 02
type: execute
wave: 2
depends_on: ["00", "01"]
files_modified:
  - ferro-mcp-server/src/jsonrpc.rs
  - ferro-mcp-server/src/renderer.rs
  - ferro-mcp-server/tests/mcp_tenant_isolation.rs
autonomous: true
requirements: [AMCP-01, AMCP-02]
user_setup: []

must_haves:
  truths:
    - "Every tools/list and tools/call path reads tenant_id and scope from McpContext, not from tool-call arguments"
    - "A read-scoped key is rejected with a JSON-RPC -32603 error on a write-tool call and allowed on a read-tool call"
    - "Authenticating as tenant A via API key surfaces only tenant A data in dispatch — tenant B rows never appear"
  artifacts:
    - path: "ferro-mcp-server/src/jsonrpc.rs"
      provides: "ctx-threaded tools/list + tools/call with server-side scope gate"
      contains: "scope insufficient"
    - path: "ferro-mcp-server/tests/mcp_tenant_isolation.rs"
      provides: "GREEN cross-tenant + scope + auth-parity integration tests"
      contains: "api_key_cross_tenant_isolation"
  key_links:
    - from: "ferro-mcp-server/src/jsonrpc.rs handle_tools_call"
      to: "dispatch(... ctx.tenant_id)"
      via: "tenant_id sourced from McpContext, never payload"
      pattern: "tenant_id"
    - from: "McpContext.scope"
      to: "write-tool rejection"
      via: "is_write_tool && scope == read"
      pattern: "is_write_tool"
---

<objective>
Complete the `ferro-mcp-server` integration: thread `McpContext` through the
JSON-RPC handlers so every tool listing and call is tenant- and scope-scoped
(AMCP-01), enforce the server-side scope gate independently of the listing
filter (D-06 / SC#3), and turn the Plan 00 cross-tenant + scope + parity
integration tests GREEN against the real `validate_api_key` from Plan 01
(AMCP-02 / SC#5).

The scope gate is already wired (Plan 00 Task 2). This plan finalizes the
context threading, verifies the gate against real seeded keys, and proves
cross-tenant isolation end-to-end through `dispatch`.

Key correctness invariants honored:
- `tenant_id` always sourced from the resolved auth principal (via McpContext),
  NEVER from tool-call arguments (RESEARCH §Anti-Patterns; dispatch.rs:104 invariant).
- Scope re-checked at `tools/call`, not merely filtered at `tools/list` (PITFALLS §4).
- Absent `scope` (OAuth JWT principal) = `read_write` (full access) — preserves the
  existing OAuth path (RESEARCH Pitfall 6 / Assumption A3).

Purpose: tenant- and permission-scoped MCP surface with a unified API-key path (AMCP-01, AMCP-02).
Output: ctx-threaded handlers + GREEN SC#3 and SC#5 integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/217-tenant-context-per-tenant-api-key-auth/217-RESEARCH.md
@.planning/phases/217-tenant-context-per-tenant-api-key-auth/217-PATTERNS.md

<interfaces>
<!-- Contracts. Extracted from codebase. Use directly. -->

From ferro-mcp-server/src/renderer.rs (Plan 00 — already extended):
```rust
#[derive(Debug, Clone, Default)]
pub struct McpContext {
    pub tenant_id: Option<i64>,
    pub evaluated_guards: HashMap<String, bool>,
    pub scope: Option<String>,
}
```

From ferro-mcp-server/src/jsonrpc.rs (Plan 00 — signatures already changed):
```rust
pub async fn handle_tools_list(services: &[ServiceDef], ctx: &McpContext, _config: &McpServerConfig) -> Value
pub async fn handle_tools_call(call_params: Value, services: &[ServiceDef], db: &DatabaseConnection, tenant_id: Option<i64>, ctx: &McpContext) -> Value
// scope gate already inserted: is_write_tool && key_scope == "read" → -32603
```

From ferro-mcp-server/src/dispatch.rs lines 108-115 (unchanged — already tenant-scoped, fail-closed):
```rust
pub async fn dispatch(service: &ServiceDef, filters: Value, limit: u64, offset: u64,
                      db: &DatabaseConnection, tenant_id: Option<i64>) -> Result<DispatchResult>
```

From ferro-mcp-oauth (Plan 01 — now real):
```rust
pub async fn validate_api_key(header: Option<&str>, db: &DatabaseConnection, expected_tenant: Option<i64>) -> BearerCheck
pub fn generate_mcp_api_key() -> (String, String)  // (raw_key, key_hash)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize McpContext threading through tools/list and tools/call</name>
  <files>ferro-mcp-server/src/jsonrpc.rs, ferro-mcp-server/src/renderer.rs</files>
  <read_first>
    - ferro-mcp-server/src/jsonrpc.rs (Plan 00 state: handle_tools_list ctx param, handle_tools_call ctx param + scope gate, dispatch call site line ~84)
    - ferro-mcp-server/src/renderer.rs (render_exposed_tools signature line ~56-58; McpContext consumers)
    - ferro-mcp-server/src/dispatch.rs (lines 108-115 signature; lines 152-166 fail-closed guarantee — DO NOT modify dispatch)
  </read_first>
  <action>
    Plan 00 already changed the signatures and wired the scope gate. This task
    confirms the context is the single source of tenant_id and scope and removes
    any redundancy.

    1. In `handle_tools_call`, ensure the `tenant_id` passed to `dispatch` is the
       authenticated tenant (from `ctx.tenant_id` or the existing `tenant_id`
       parameter — they must be the same value). Decide the minimal-diff shape:
       KEEP the `tenant_id: Option<i64>` parameter (caller already passes the
       resolved tenant) AND `ctx: &McpContext`. Assert in a debug context that the
       caller passes `ctx.tenant_id == tenant_id`; if you instead drop the standalone
       `tenant_id` param and read `ctx.tenant_id`, update the test call sites accordingly.
       Whichever you choose: `dispatch(service, filters, limit, offset, db, <tenant from auth>)`.
       NEVER read tenant from `call_params` / tool arguments (preserve dispatch.rs:104 invariant).

    2. Confirm `handle_tools_list` passes `ctx` into `render_exposed_tools(services, ctx)` and
       returns the standard `{ "result": { "tools": [...] } }` envelope. In Phase 217 all tools are
       `list_*` (read), so a `read`-scoped ctx lists everything (the scope filter on listing is a
       no-op until Phase 218 adds write tools). Add a code comment marking this no-op-now / active-218 boundary.

    3. Confirm `render_exposed_tools` (renderer.rs) reads from `ctx` and does not require a signature
       change (RESEARCH §3 says line 56-58 already takes `ctx: &McpContext`). If it ignores `ctx`,
       leave it — `evaluated_guards`-based filtering is Phase 218; in 217 the field exists but is unused
       for filtering. Do not add guard filtering here.

    4. Do NOT modify `dispatch.rs` — the fail-closed tenant predicate injection already exists.
  </action>
  <verify>
    <automated>cargo build -p ferro-mcp-server --tests 2>&1 | tail -3; grep -q 'ctx: &McpContext' ferro-mcp-server/src/jsonrpc.rs && grep -q 'render_exposed_tools(services, ctx)' ferro-mcp-server/src/jsonrpc.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p ferro-mcp-server --tests` exits 0
    - `jsonrpc.rs` `handle_tools_list` passes `ctx` to `render_exposed_tools`
    - `handle_tools_call` passes the auth-derived `tenant_id` to `dispatch` and does NOT read tenant from `call_params` (`grep -n 'call_params' ferro-mcp-server/src/jsonrpc.rs` shows no tenant extraction from params)
    - `dispatch.rs` is unmodified (`git diff --stat ferro-mcp-server/src/dispatch.rs` shows no changes)
  </acceptance_criteria>
  <done>Context threaded; tenant_id sourced only from auth; scope read from ctx; dispatch untouched; workspace builds with tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Turn SC#3 (scope gate) and SC#5 (cross-tenant isolation) integration tests GREEN</name>
  <files>ferro-mcp-server/tests/mcp_tenant_isolation.rs</files>
  <read_first>
    - ferro-mcp-server/tests/mcp_tenant_isolation.rs (Plan 00 RED tests + setup_isolation_db/seed_api_key/order_service helpers)
    - ferro-mcp-server/src/dispatch.rs (lines 295-363 dispatch test assertions style; lines 152-166 fail-closed)
    - ferro-mcp-oauth/src/validate.rs (Plan 01 real validate_api_key — principal["tenant_id"], principal["scope"])
    - .planning/phases/217-tenant-context-per-tenant-api-key-auth/217-RESEARCH.md (§Test Plan SC#3, SC#5)
  </read_first>
  <behavior>
    - `read_scope_key_rejected_on_write_tool_name`: handle_tools_call with synthetic tool name "create_order" + McpContext{scope: Some("read"), ..} → response has error.code == -32603 (GREEN — gate is real from Plan 00)
    - `read_scope_key_allowed_on_read_tool`: tool name "list_order" + scope "read" → response has a "result" key, no scope error
    - `api_key_cross_tenant_isolation`: seed a tenant-1 read key; validate_api_key resolves tenant_id=1; dispatch(order_service, .., tenant_id=1) returns exactly the 2 tenant-1 rows; assert no row has tenant_id==2 and count==2
    - `api_key_and_jwt_produce_same_tenant_id`: validate_api_key for a tenant-1 key → Authenticated principal tenant_id==1 (the JWT half may live in the oauth unit test per Plan 00's comment; if asserted here, both equal 1)
  </behavior>
  <action>
    1. The integration tests were authored in Plan 00 (RED). With Plan 01's real
       `validate_api_key` and Task 1's threading in place, RUN them and confirm GREEN:
       `cargo test -p ferro-mcp-server --test mcp_tenant_isolation 2>&1 | tail -25`

    2. For any still-RED test, fix the TEST WIRING only (not the assertions, not production logic):
       - Ensure `seed_api_key` INSERTs the hash from `generate_mcp_api_key()` and the test passes the
         matching raw key (`Bearer ferro_...`) to `validate_api_key`.
       - Ensure `api_key_cross_tenant_isolation` extracts `tenant_id` from the `BearerCheck::Authenticated`
         principal (`principal["tenant_id"].as_i64()`), then dispatches with that exact value.
       - Ensure the SC#3 tests construct `McpContext { scope: Some("read".to_string()), ..Default::default() }`
         and call `handle_tools_call` with the synthetic write tool name. If `handle_tools_call` requires a
         `db` arg even for the scope-reject path (gate fires before dispatch), pass the in-memory db; the
         gate returns before any query so the empty `mcp_api_keys`/`orders` tables are fine.

    3. Confirm the cross-tenant assertion is strict: it must assert BOTH `count == 2` AND
       `every returned row.tenant_id == 1` AND `no returned row.tenant_id == 2`. If the Plan 00 draft
       only checked the count, strengthen it to also assert the per-row tenant_id (this is the SC#5
       security assertion — do not weaken).

    4. Run the full server-crate suite to confirm no regression in the existing
       `dispatch_integration` / `jsonrpc_integration` tests after the signature changes:
       `cargo test -p ferro-mcp-server 2>&1 | tail -15`
  </action>
  <verify>
    <automated>cargo test -p ferro-mcp-server --test mcp_tenant_isolation 2>&1 | grep -q 'test result: ok' && cargo test -p ferro-mcp-server 2>&1 | tail -8 | grep -q 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p ferro-mcp-server --test mcp_tenant_isolation` prints `test result: ok` (all SC#3/SC#5 tests GREEN)
    - `mcp_tenant_isolation.rs` `api_key_cross_tenant_isolation` asserts `count == 2` AND per-row `tenant_id == 1` AND absence of `tenant_id == 2` (grep for both `== 1` and `!= 2` / `== 2` assertions)
    - `cargo test -p ferro-mcp-server` (whole crate, incl. jsonrpc_integration + dispatch_integration) prints `test result: ok` — no regression from the signature changes
  </acceptance_criteria>
  <done>SC#3 scope-gate tests and SC#5 cross-tenant isolation test are GREEN against real validate_api_key; existing server tests still pass; cross-tenant assertion is strict (per-row tenant_id).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool-call arguments → dispatch | Untrusted; tenant_id must NOT be read from here |
| Resolved principal (ctx) → list/call | Trusted source of tenant_id and scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217-01 | Information Disclosure (cross-tenant) | dispatch tenant predicate + ctx.tenant_id | mitigate | tenant_id flows only from the auth principal via McpContext into dispatch's fail-closed `AND tenant_id=?` injection (dispatch.rs:152-166). `api_key_cross_tenant_isolation` strictly asserts only tenant-A rows surface. dispatch.rs unmodified — the existing fail-closed guarantee is reused, not re-implemented. |
| T-217-04 | Elevation (scope creep) | scope gate in handle_tools_call | mitigate | Server-side re-check at call time: `is_write_tool && scope=="read" → -32603`, independent of the tools/list filter (PITFALLS §4). `read_scope_key_rejected_on_write_tool_name` (synthetic write tool) proves it before Phase 218 adds real write tools. Absent scope = read_write preserves OAuth path (Assumption A3). |
| T-217-03 | Spoofing | tenant from payload | mitigate | `handle_tools_call` never reads tenant from `call_params`; acceptance criterion greps to confirm. |
</threat_model>

<verification>
- `cargo test -p ferro-mcp-server` → `test result: ok` (isolation + scope + existing integration tests)
- tenant_id never sourced from tool-call arguments
- scope gate rejects write tools for read keys at call time
- cross-tenant isolation assertion is strict (per-row tenant_id == A, none == B)
</verification>

<success_criteria>
- McpContext threaded through tools/list and tools/call; tenant_id from auth only
- read-scoped key rejected on write-tool call, allowed on read-tool call (SC#3)
- tenant A API key surfaces only tenant A data (SC#5)
- No regression in existing ferro-mcp-server tests
</success_criteria>

<output>
After completion, create `.planning/phases/217-tenant-context-per-tenant-api-key-auth/217-02-SUMMARY.md`
</output>
