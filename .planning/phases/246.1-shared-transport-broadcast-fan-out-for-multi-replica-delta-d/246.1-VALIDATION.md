---
phase: 246.1
slug: shared-transport-broadcast-fan-out-for-multi-replica-delta-d
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-14
---

# Phase 246.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` (tokio runtime; `#[tokio::test]`) |
| **Config file** | none — existing workspace test setup |
| **Quick run command** | `cargo test -p ferro-broadcast` |
| **Full suite command** | `cargo test --all-features -p ferro-broadcast` |
| **Estimated runtime** | ~5–15 seconds (no live Redis on the default path) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p ferro-broadcast` (fast — in-memory transport, no live Redis)
- **After every plan wave:** Run `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

Task IDs are assigned by the planner; rows below map each success criterion / key decision to the
automated check that samples it. The planner MUST attach each row to the task that delivers it.

| Behaviour | Requirement | Threat Ref | Test Type | Automated Command | File Exists | Status |
|-----------|-------------|------------|-----------|-------------------|-------------|--------|
| Delta published on process A reaches subscriber on process B (in-memory shared bus) | SC1 | T-246.1-01 | unit | `cargo test -p ferro-broadcast sc1_cross_process_delivery` | ❌ W0 | ⬜ pending |
| Own echo suppressed — publisher receives exactly one copy (origin filter, D-03) | SC1 | T-246.1-01 | unit | `cargo test -p ferro-broadcast sc1_own_echo_suppressed` | ❌ W0 | ⬜ pending |
| No-transport-configured `Broadcaster` behaves identically to today | SC2 | — | unit (existing suite unchanged) | `cargo test -p ferro-broadcast` | ✅ (existing) | ⬜ pending |
| Public API unchanged (`broadcast` / `broadcast_except` / `whisper` / `Broadcast` builder) | SC3 | — | unit (existing suite unchanged) | `cargo test -p ferro-broadcast` | ✅ (existing) | ⬜ pending |
| Presence membership (`MemberAdded`/`MemberRemoved`) stays per-process, not fanned out (D-05) | SC3 | — | unit | `cargo test -p ferro-broadcast presence_stays_per_process` | ❌ W0 | ⬜ pending |
| Bus publish error does not fail the caller; local delivery still happens (D-06) | D-06 | T-246.1-02 | unit (transport stub returns error) | `cargo test -p ferro-broadcast publish_error_does_not_propagate` | ❌ W0 | ⬜ pending |
| Cross-process delivery over live Redis pub/sub | SC1 (Redis) | T-246.1-03 | integration (feature + env gated) | `REDIS_URL=redis://127.0.0.1/ cargo test -p ferro-broadcast --features redis-transport redis_integration` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `ferro-broadcast/tests/shared_transport.rs` — SC1 cross-process delivery, own-echo suppression, presence-stays-per-process, publish-error-graceful (covers SC1, SC2 subset, D-03, D-04, D-06 via the in-memory transport)
- [ ] `ferro-broadcast/src/transport/mod.rs` — `BroadcastTransport` trait + `BusEnvelope` (compilation target the tests bind to)
- [ ] `ferro-broadcast/src/transport/memory.rs` — `InMemoryTransport` (deterministic two-`Broadcaster`s-share-one-`tokio::sync::broadcast` bus)
- [ ] `ferro-broadcast/tests/redis_integration.rs` — `#![cfg(feature = "redis-transport")]` + `REDIS_URL` env guard (live-Redis path; skipped when Redis absent)

*Existing `ferro-broadcast` unit tests cover SC2/SC3 parity — they must continue to pass unchanged.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Cross-**OS-process** delivery over a real Redis instance (two separately-launched binaries, not two in-process `Broadcaster`s) | SC1 (deployment proof) | CI proves the logic deterministically via the in-memory bus and the env-gated single-process Redis test; the true multi-replica topology needs two running processes + a Redis server, which CI does not provision | Start Redis; launch two app replicas with `BROADCAST_TRANSPORT=redis` sharing `REDIS_URL`; subscribe a socket on replica B; publish a delta on replica A; confirm B receives it exactly once |

*All in-process behaviours (SC1 logic, SC2 parity, SC3 API stability, D-03 echo suppression, D-06 failure semantics) have automated verification.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (the four Wave 0 files above)
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
