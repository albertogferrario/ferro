---
phase: 248-deployable-ferro-worker-runtime
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - ferro-queue/tests/worker_runtime.rs
  - framework/tests/worker_boot.rs
  - ferro-macros/tests/ui/offload/pass/queue_arg.rs
  - ferro-macros/tests/ui/offload/fail/queue_unknown_arg.rs
  - ferro-macros/tests/ui/offload/fail/queue_unknown_arg.stderr
autonomous: true
requirements: [OFFLOAD-05]

must_haves:
  truths:
    - "A single #[tokio::test] in ferro-queue/tests/worker_runtime.rs exists and resolves to ≥1 test via --list"
    - "framework/tests/worker_boot.rs exists with a feature-gated WR-01 scenario and a D-07 warning scenario"
    - "ferro-macros trybuild fixtures for #[offload(queue = \"…\")] exist (one pass, one fail with .stderr)"
    - "The three SC#1–SC#3 test bodies fail RED (the runtime API they exercise does not exist yet) or are gated so RED is provable"
  artifacts:
    - path: "ferro-queue/tests/worker_runtime.rs"
      provides: "SC#1/SC#2/SC#3 as sub-functions of one #[tokio::test] worker_runtime_suite"
      contains: "worker_runtime_suite"
      min_lines: 90
    - path: "framework/tests/worker_boot.rs"
      provides: "WR-01 bootstrap + D-07 warning test scaffold"
      contains: "worker_boot"
    - path: "ferro-macros/tests/ui/offload/pass/queue_arg.rs"
      provides: "trybuild pass fixture for #[offload(queue = \"reports\")]"
      contains: "offload(queue"
    - path: "ferro-macros/tests/ui/offload/fail/queue_unknown_arg.rs"
      provides: "trybuild fail fixture for an unknown #[offload] argument"
      contains: "offload("
  key_links:
    - from: "ferro-queue/tests/worker_runtime.rs"
      to: "ferro_queue::WorkerConfig / claim / enqueue"
      via: "in-process WorkerLoop drain over NamedTempFile SQLite"
      pattern: "NamedTempFile"
    - from: "ferro-macros/tests/offload_macro.rs"
      to: "ferro-macros/tests/ui/offload/{pass,fail}"
      via: "trybuild harness picks up new fixtures by directory convention"
      pattern: "ui/offload"
---

<objective>
Create the Wave 0 test scaffolds that all subsequent Phase 248 plans satisfy: the
`ferro-queue` worker-runtime integration suite (SC#1–SC#3), the `framework` worker-boot
suite (WR-01 + D-07), and the `ferro-macros` trybuild fixtures for
`#[offload(queue = "…")]`. These files establish the Nyquist test contract so later plans
turn RED tests GREEN rather than inventing verification after the fact.

Purpose: pin the observable behaviours (queue-scoped consumption, exactly-once split,
fault-domain isolation, transport attach, feature-off warning, macro attribute acceptance)
as executable tests before any production code changes.
Output: three new test files + two macro fixtures (+ one `.stderr`), all committed. The
SC#1–SC#3 suite and the macro fixtures are RED until Plans 01/02/03 land.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/248-deployable-ferro-worker-runtime/248-VALIDATION.md
@.planning/phases/248-deployable-ferro-worker-runtime/248-PATTERNS.md
@.planning/phases/248-deployable-ferro-worker-runtime/248-RESEARCH.md

<interfaces>
<!-- Signatures the test scaffolds bind against. Extracted from the codebase — do not re-explore. -->

From ferro-queue (public API used by the tests):
```rust
// ferro-queue/src/worker.rs
pub struct WorkerConfig { /* queues: Vec<String>, ... */ }
impl WorkerConfig { pub fn new(queues: Vec<String>) -> Self; }        // worker.rs:80
pub struct WorkerLoop;
impl WorkerLoop { pub fn from_registry(config: WorkerConfig) -> Self; pub async fn run(self) -> Result<(), Error>; }

// ferro-queue/src/db.rs — existing low-level helpers used by race_claim_sqlite.rs
pub async fn enqueue(conn, queue: &str, kind: &str, payload: &str, max_attempts: i32,
                     available_at: Option<..>, /* ... */, now) -> Result<..>;
pub async fn claim(conn, queue: &str, worker_id: &str) -> Result<Option<Row>, Error>;
pub async fn delete_job(conn, id: i64) -> Result<(), Error>;
pub use CreateJobsTable; // migration for the jobs table
```

From framework/ferro-broadcast (WR-01 boot test):
```rust
// ferro-broadcast/src/broadcaster.rs
impl Broadcaster {
    pub fn new() -> Self;
    pub fn with_config(config: BroadcastConfig) -> Self;
    pub fn with_transport(self, transport: Arc<dyn BroadcastTransport + Send + Sync>) -> Self; // :90
    pub fn config(&self) -> &BroadcastConfig;
}
// ferro-broadcast/src/config.rs
impl BroadcastConfig { pub fn from_env() -> Self; /* transport_redis_url: Option<String> */ }
// ferro-broadcast/src/transport/memory.rs
pub struct InMemoryTransport; impl InMemoryTransport { pub fn new(cap: usize) -> Self; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold ferro-queue/tests/worker_runtime.rs (SC#1–SC#3 in one #[tokio::test])</name>
  <read_first>
    - ferro-queue/tests/race_claim_sqlite.rs (EXACT analog: NamedTempFile fixture lines 36–43, inline TestMigrator lines 19–26, enqueue+concurrent-drain lines 47–99, imports)
    - ferro-queue/src/worker.rs:80 (WorkerConfig::new signature)
    - .planning/phases/248-deployable-ferro-worker-runtime/248-PATTERNS.md § "ferro-queue/tests/worker_runtime.rs" (test structure, SC#1/SC#3 adaptation notes, imports block)
    - .planning/phases/248-deployable-ferro-worker-runtime/248-VALIDATION.md § "Test-Collapse Guard" and § "Per-Task Verification Map"
  </read_first>
  <files>ferro-queue/tests/worker_runtime.rs</files>
  <action>
    Create `ferro-queue/tests/worker_runtime.rs` following `race_claim_sqlite.rs` exactly.
    Structure: ONE outer `#[tokio::test(flavor = "multi_thread", worker_threads = 4)]
    async fn worker_runtime_suite()` that sequentially awaits three plain `async fn`
    sub-functions (NOT separate `#[tokio::test]` fns — this dodges Pitfall 2 OnceLock and
    Pitfall 3 test-collapse):
      1. `async fn worker_consumes_only_selected_queue()` (SC#1): create a NamedTempFile
         SQLite DB (`sqlite://{path}?mode=rwc`, keep the `NamedTempFile` binding alive for the
         whole fn), run `TestMigrator::up`, enqueue N jobs on queue `"reports"` and M jobs on
         queue `"default"`, then run an in-process consumer scoped to `vec!["reports".to_string()]`
         via the low-level `claim(&conn, "reports", worker_id)` drain loop from the analog. Assert
         only the `"reports"` jobs were claimed and the `"default"` jobs remain (claim on
         `"default"` still returns rows afterward). Use the `claim`/`delete_job`/`enqueue`
         helpers, not the not-yet-built framework entry point.
      2. `async fn two_loops_split_work_no_duplicates()` (SC#2): NamedTempFile DB; enqueue N=20
         jobs on `"default"`; two concurrent `drain(conn, worker_id, out)` tasks over the shared
         file DB (same pattern as the analog's `drain` fn), `tokio::join!` them; assert
         `unique.len() == all.len()` (no duplicate claim) and `unique.len() == N` (all claimed).
      3. `async fn queue_fault_isolation()` (SC#3): NamedTempFile DB with two distinct queues
         `"media"` and `"reports"`; enqueue several slow-simulated jobs on `"media"` and fast
         jobs on `"reports"`; drain `"media"` and `"reports"` on two concurrent tasks whose start
         is coordinated by a `tokio::sync::Barrier::new(2)` (NO `tokio::time::sleep` for
         synchronization). Assert every `"reports"` job is claimed even while `"media"` still has
         unclaimed backlog — i.e. a saturated `"media"` queue does not prevent `"reports"` drain.
    Copy the imports block from PATTERNS.md verbatim:
    `use std::collections::HashSet; use std::sync::{Arc, Mutex}; use sea_orm::Database;
     use sea_orm_migration::MigratorTrait; use ferro_queue::{claim, delete_job, enqueue,
     CreateJobsTable, WorkerConfig, WorkerLoop};` (drop any that end unused; keep `WorkerConfig`
    only if referenced). Tests `unwrap()`/`expect()` on Ok, `panic!` on Err — no `?`.
    This file is allowed to be GREEN already if it exercises only existing `claim`/`enqueue`
    primitives; the SC#1 scoped-consumption assertion is the load-bearing new behaviour and
    must be written so it FAILS if a consumer scoped to `"reports"` ever claims a `"default"`
    job (i.e. do not weaken the assertion to always pass).
  </action>
  <verify>
    <automated>cargo test -p ferro-queue --test worker_runtime -- --list 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ferro-queue/tests/worker_runtime.rs` exists.
    - `grep -q "worker_runtime_suite" ferro-queue/tests/worker_runtime.rs` succeeds.
    - `grep -q "worker_consumes_only_selected_queue" ferro-queue/tests/worker_runtime.rs` succeeds.
    - `grep -q "two_loops_split_work_no_duplicates" ferro-queue/tests/worker_runtime.rs` succeeds.
    - `grep -q "queue_fault_isolation" ferro-queue/tests/worker_runtime.rs` succeeds.
    - `grep -q "NamedTempFile" ferro-queue/tests/worker_runtime.rs` succeeds (NOT `sqlite::memory:`).
    - `grep -q "Barrier" ferro-queue/tests/worker_runtime.rs` succeeds AND `! grep -q "time::sleep" ferro-queue/tests/worker_runtime.rs`.
    - `cargo test -p ferro-queue --test worker_runtime -- --list` lists exactly one test (`worker_runtime_suite`).
    - Exactly one `#[tokio::test]` attribute in the file: `grep -c "#\[tokio::test" ferro-queue/tests/worker_runtime.rs` returns `1`.
  </acceptance_criteria>
  <done>File compiles as a test target and `-- --list` resolves to the single `worker_runtime_suite` test; the SC#1 scoped-consumption assertion is present and non-vacuous.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold framework/tests/worker_boot.rs (WR-01 attach + D-07 warning)</name>
  <read_first>
    - framework/tests/offload_delta_broadcast.rs:364-385 (single-#[tokio::test] suite structure, `extern crate ferro_rs as ferro;`, `#[serial_test::serial]`)
    - framework/tests/offload_delta_broadcast.rs:370-378 (Broadcaster + InMemoryTransport construction)
    - framework/tests/offload_delta_broadcast.rs:391-432 (feature-gated `#[cfg(feature = "redis-transport")]` block + REDIS_URL skip pattern)
    - .planning/phases/248-deployable-ferro-worker-runtime/248-PATTERNS.md § "framework/tests/worker_boot.rs" (imports block, D-07 warning test note)
    - ferro-broadcast/src/broadcaster.rs:90 (with_transport), ferro-broadcast/src/config.rs (from_env, transport_redis_url)
  </read_first>
  <files>framework/tests/worker_boot.rs</files>
  <action>
    Create `framework/tests/worker_boot.rs` modeled on `offload_delta_broadcast.rs`.
    Header: `extern crate ferro_rs as ferro;` then the imports block from PATTERNS.md
    (`use ferro_broadcast::{BroadcastConfig, Broadcaster};` etc.). Structure as ONE outer
    `#[tokio::test(flavor = "multi_thread", worker_threads = 2)] #[serial_test::serial]
    async fn worker_boot_suite()` calling two plain `async fn` scenarios:
      1. `async fn transport_url_no_feature_warns()` (D-07): set an env `BroadcastConfig` with
         `transport_redis_url = Some("redis://127.0.0.1:6379")` (via `from_env` after setting
         `BROADCAST_REDIS_URL`, or by constructing a config with the field set), construct a
         `Broadcaster::with_config(config)`, register it via `ferro::App::singleton(...)`, then
         invoke the framework boot step under test. Under `#[cfg(not(feature = "redis-transport"))]`
         assert the boot completes WITHOUT panic (in-process fallback). Capture the warning by
         initializing a `tracing_subscriber::fmt().with_test_writer()` guarded against double-init
         (wrap in `once_cell::sync::Lazy` or `std::sync::Once`). This scenario is the RED target
         for Plan 01's D-07 branch — until `run_common_boot` exists, call whatever boot entry the
         plan will expose; if that symbol does not exist yet, gate this scenario body behind a
         `// TODO(plan-01): call framework::run_common_boot` and a trivial `assert!(true)` so the
         file compiles, and record in the plan SUMMARY that Plan 01 must un-stub it.
      2. Under `#[cfg(feature = "redis-transport")] mod redis_tests { ... }`: an
         `async fn transport_url_attaches_redis_transport()` that skips when `REDIS_URL` is unset
         (`let Some(url) = std::env::var("REDIS_URL").ok().filter(|s| !s.is_empty()) else { return; }`),
         otherwise drives the boot step and asserts the `App`-registered `Broadcaster` reports a
         transport attached (assert via observable behaviour — e.g. a delta published reaches the
         transport; reuse the `InMemoryTransport`/`RedisTransport` assertion shape from the analog).
    Keep the file compiling with the `redis-transport` feature OFF (the default) — the redis
    scenario lives entirely inside `#[cfg(feature = "redis-transport")]`.
    Do NOT introduce new detached spawns (WR-02 tech debt is out of scope — see RESEARCH).
  </action>
  <verify>
    <automated>cargo test -p framework --test worker_boot -- --list 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `framework/tests/worker_boot.rs` exists.
    - `grep -q "worker_boot_suite" framework/tests/worker_boot.rs` succeeds.
    - `grep -q "transport_url_no_feature_warns" framework/tests/worker_boot.rs` succeeds.
    - `grep -q "redis-transport" framework/tests/worker_boot.rs` succeeds (feature gate present).
    - `grep -q "REDIS_URL" framework/tests/worker_boot.rs` succeeds (skip-when-unset pattern).
    - `grep -q "extern crate ferro_rs as ferro" framework/tests/worker_boot.rs` succeeds.
    - `cargo test -p framework --test worker_boot -- --list` resolves to ≥1 test (default features).
  </acceptance_criteria>
  <done>Test target compiles with default features; `-- --list` resolves to ≥1 test; the redis scenario is confined to `#[cfg(feature = "redis-transport")]` and skips without `REDIS_URL`.</done>
</task>

<task type="auto">
  <name>Task 3: Add ferro-macros trybuild fixtures for #[offload(queue = "…")]</name>
  <read_first>
    - ferro-macros/tests/offload_macro.rs (trybuild harness wiring; how pass/fail dirs are globbed)
    - ferro-macros/tests/ui/offload/pass/basic.rs (canonical passing #[offload] fixture to clone)
    - ferro-macros/tests/ui/offload/fail/non_serializable_param.rs + .stderr (fail fixture shape and stderr snapshot convention)
    - .planning/phases/248-deployable-ferro-worker-runtime/248-RESEARCH.md § "Pattern 3" (attribute parsing; unknown-arg error message)
  </read_first>
  <files>ferro-macros/tests/ui/offload/pass/queue_arg.rs, ferro-macros/tests/ui/offload/fail/queue_unknown_arg.rs, ferro-macros/tests/ui/offload/fail/queue_unknown_arg.stderr</files>
  <action>
    Clone `ui/offload/pass/basic.rs` to `ui/offload/pass/queue_arg.rs` and change the offloaded
    method's attribute to `#[offload(queue = "reports")]` (keep the rest identical — a serializable
    param + return so it passes serializable enforcement). This is the RED→GREEN target: it will
    FAIL to compile until Plan 02 teaches the macro to accept the `queue = "…"` argument.
    Create `ui/offload/fail/queue_unknown_arg.rs`: same shape but with an invalid argument
    `#[offload(qeue = "reports")]` (typo) or `#[offload(retries = 3)]` — an argument the macro
    must reject. Create the matching `queue_unknown_arg.stderr` with the expected compile error.
    Do NOT hand-guess the exact stderr text now: write a placeholder `.stderr` containing a single
    line comment noting it must be regenerated, and record in the SUMMARY that Plan 02 regenerates
    it via `TRYBUILD=overwrite cargo test -p ferro-macros --test offload_macro`. The error message
    Plan 02 emits is `unknown #[offload] argument; expected \`queue = "name"\`` (per RESEARCH
    Pattern 3), so the fail fixture's argument must be one the macro does not recognize.
    If `offload_macro.rs` does not auto-glob the `pass`/`fail` directories (i.e. it lists fixtures
    explicitly), add the two new fixtures to the appropriate `t.pass(...)` / `t.compile_fail(...)`
    calls.
  </action>
  <verify>
    <automated>test -f ferro-macros/tests/ui/offload/pass/queue_arg.rs && test -f ferro-macros/tests/ui/offload/fail/queue_unknown_arg.rs && echo FIXTURES_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `test -f ferro-macros/tests/ui/offload/pass/queue_arg.rs` succeeds.
    - `grep -q 'offload(queue = "reports")' ferro-macros/tests/ui/offload/pass/queue_arg.rs` succeeds.
    - `test -f ferro-macros/tests/ui/offload/fail/queue_unknown_arg.rs` succeeds.
    - `test -f ferro-macros/tests/ui/offload/fail/queue_unknown_arg.stderr` succeeds.
    - The fail fixture uses an argument the macro will reject (NOT `queue = "…"`): `! grep -q 'offload(queue = ' ferro-macros/tests/ui/offload/fail/queue_unknown_arg.rs`.
    - If `offload_macro.rs` lists fixtures explicitly, both new fixtures are referenced there: (glob harness → this check is N/A).
  </acceptance_criteria>
  <done>Both fixtures and the placeholder `.stderr` exist; the pass fixture carries `#[offload(queue = "reports")]` and is the RED target for Plan 02; the fail fixture carries a rejectable argument.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Wave 0 creates test-only artifacts. No runtime input path, no network, no persistence outside per-test NamedTempFile SQLite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-248-00-01 | Tampering | Test fixture DB (`NamedTempFile`) | accept | Ephemeral per-test file, deleted on drop; no shared state, no secrets. |
| T-248-00-02 | Information Disclosure | `tracing_subscriber` test writer capturing warnings | accept | Captures only the D-07 warning string (no secrets); scoped to the test process. |

No HIGH threats: this plan adds only test scaffolds. `--queue` and Redis-URL handling are asserted, not introduced, here.
</threat_model>

<verification>
- `cargo test -p ferro-queue --test worker_runtime -- --list` resolves to exactly one test (`worker_runtime_suite`).
- `cargo test -p framework --test worker_boot -- --list` resolves to ≥1 test under default features.
- Both new trybuild fixtures exist; the pass fixture is the RED target for Plan 02.
- `cargo fmt --all -- --check` clean on the new files (run last; serialize CPU ops — one at a time).
</verification>

<success_criteria>
- `ferro-queue/tests/worker_runtime.rs`, `framework/tests/worker_boot.rs`, and the two macro
  fixtures (+ placeholder `.stderr`) exist and compile as targets.
- The SC#1–SC#3 suite is a single `#[tokio::test]` using `NamedTempFile` (not `:memory:`) and a
  `Barrier` (not `sleep`) for SC#3 synchronization.
- The macro pass fixture uses `#[offload(queue = "reports")]`; the fail fixture uses a rejectable
  argument.
- No production code changed in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/248-deployable-ferro-worker-runtime/248-00-SUMMARY.md`.
Record explicitly: (a) which SC#1–SC#3 assertions are RED vs already-GREEN, (b) that Plan 01 must
un-stub the D-07 scenario body and Plan 02 must regenerate `queue_unknown_arg.stderr` via
`TRYBUILD=overwrite`, and (c) whether `offload_macro.rs` globs or lists fixtures.
</output>
