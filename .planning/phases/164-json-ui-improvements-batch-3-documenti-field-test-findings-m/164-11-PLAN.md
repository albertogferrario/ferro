---
phase: 164
plan: 11
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09, 10]
files_modified:
  - .planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/V1-DELETION-AUDIT.md
  - .planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/PLUGIN-SURFACE-AUDIT.md
autonomous: false
requirements: [D-01, D-02, D-03, D-06, D-07]
must_haves:
  truths:
    - "V1-DELETION-AUDIT.md exists with a row for every v1 surface element (JsonUiView, Component enum, all *Props removed in v2, ComponentNode, PluginProps)"
    - "Each row has a Resolution column populated with one of: MIGRATED, INTENTIONAL_DROP, BLOCKER"
    - "Zero rows are marked BLOCKER (Phase 160 gate condition)"
    - "Plugin surface audit (D-06) is conducted; outcome captured (clean OR list of doc-gaps OR BLOCKER escalation)"
    - "Grep proof of v1 surface absence: no `JsonUiView`, `ComponentNode`, `Component::` enum-variant patterns remain in ferro-json-ui/src or framework/src"
  artifacts:
    - path: ".planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/V1-DELETION-AUDIT.md"
      provides: "Per-row v1 surface audit with Resolution column"
    - path: ".planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/PLUGIN-SURFACE-AUDIT.md"
      provides: "Plugin surface audit outcome (D-06/D-07); only created if audit surfaces gaps"
  key_links:
    - from: "V1-DELETION-AUDIT.md"
      to: "COMPLETED.md (Plan 12)"
      via: "the audit table is one of COMPLETED.md's required sections"
---

<objective>
Two responsibilities, both audit-focused:

1. **D-01..D-03 (v1-deletion-readiness audit)** — Produce `V1-DELETION-AUDIT.md` listing every v1 public surface element with its v2 equivalent, its gestiscilo usage status, and a Resolution (MIGRATED, INTENTIONAL_DROP, or BLOCKER). Phase 160 (v1 deletion) gates on **zero BLOCKER rows**.

2. **D-06..D-07 (plugin surface audit)** — Paper exercise: walk the Phase 162 D-19 plugin author guide (`docs/src/json-ui/plugins.md`) and verify it suffices to implement: (a) a Stripe payment widget, (b) a WhatsApp connection flow, (c) a chart renderer. Any blocker gaps escalate to a BLOCKER row in D-01's audit.

This plan depends on Plans 01-10 because the audit is informed by EVERY ferro-side change in this phase. Several v1 capabilities that would have been BLOCKERs are closed by Plans 03 (RawHtml — closes the Component::Plugin gap) and 05 (CardVariant — closes the auth-layout regression). The audit must run AFTER those plans have landed.

This plan is **NOT autonomous** — it ends with a `checkpoint:human-verify` step because the BLOCKER classification is a judgement call and the user must approve the final table before Phase 160 (v1 deletion) is unblocked.

Per CONTEXT D-03: "If Phase 164 cannot ship a fix for a BLOCKER, it must be reclassified as INTENTIONAL_DROP with explicit rationale, or Phase 160 is blocked until a follow-up phase."

Output: One required audit document; one conditional plugin-audit document; checkpoint for user approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/164-CONTEXT.md
@.planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/164-PATTERNS.md
@.planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/164-RESEARCH.md
@.planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-CONTEXT.md
@.planning/phases/163-json-ui-improvements-batch-2-cassa-and-calendario-field-test/163-CONTEXT.md
@framework/src/lib.rs
@ferro-json-ui/src/lib.rs
@docs/src/json-ui/plugins.md
</context>

<interfaces>
<!-- The canonical audit table shape — RESEARCH §Example 4 ships a 16-row starter -->

RESEARCH §Example 4 (lines 532-555) provides a starter table covering: JsonUiView, Component enum, ComponentNode, PluginProps, CardProps.children, FormProps.fields, GridProps.children, CollapsibleProps.children, FormSectionProps.children, ButtonGroupProps.buttons, SwitchProps.compact (re-added 162 D-16), ImageProps.inline_svg (re-added 162 D-17), RichTextEditorProps (re-added 162 D-18 as plugin), DetailFormProps/DetailField/EditMode, make_node helpers, view.rs.

RESEARCH §State of the Art table (lines 561-575) and Phase 115's `.planning/phases/115-spec-v2-data-structures/115-CONTEXT.md` are additional input for verifying what was deleted.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Grep-verify current state of v1 surface absence</name>
  <read_first>
    - RESEARCH §Assumption A5 (HIGH confidence — v1 surface fully deleted)
    - framework/src/lib.rs (re-export block)
    - ferro-json-ui/src/lib.rs (re-export block)
  </read_first>
  <action>
    Run the following grep commands and capture results for the audit:

    ```bash
    # v1 type-name absence (must return 0 matches)
    grep -rE '\b(JsonUiView|ComponentNode|PluginProps)\b' ferro-json-ui/src framework/src 2>&1 | grep -v '^//\|^ *//' | grep -v test | wc -l

    # Component enum-variant pattern (must return 0)
    grep -rE 'Component::(Card|Plugin|Text|Form|Button|Grid)' ferro-json-ui/src framework/src 2>&1 | grep -v test | wc -l

    # v1 view.rs file (must not exist)
    ls ferro-json-ui/src/view.rs 2>&1

    # framework re-exports (audit for v1 leakage)
    grep -E 'JsonUiView|ComponentNode|PluginProps|Component::' framework/src/lib.rs

    # ferro-json-ui re-exports
    grep -E 'JsonUiView|ComponentNode|PluginProps' ferro-json-ui/src/lib.rs
    ```

    Record each command's output (count or first 10 lines) for inclusion in the audit document. If any command returns NON-ZERO matches that are not in test code or comments, escalate to a BLOCKER row.
  </action>
  <verify>
    <automated>cd /Users/alberto/repositories/albertogferrario/ferro && (grep -rE '\b(JsonUiView|ComponentNode|PluginProps)\b' ferro-json-ui/src framework/src 2>/dev/null | grep -v test | grep -v '^//\|^ *//' | head -5; echo '---'; ls ferro-json-ui/src/view.rs 2>&1 | head -1)</automated>
  </verify>
  <acceptance_criteria>
    - The grep for `JsonUiView|ComponentNode|PluginProps` in production source (excluding test code and comments) returns 0 non-trivial matches
    - `ls ferro-json-ui/src/view.rs` reports "No such file or directory"
    - The framework/lib.rs and ferro-json-ui/lib.rs re-exports contain NO v1 type names
    - Grep results captured for inclusion in V1-DELETION-AUDIT.md
  </acceptance_criteria>
  <done>
    Grep evidence of v1-surface absence captured; any residual matches surfaced as BLOCKER candidates.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author V1-DELETION-AUDIT.md</name>
  <read_first>
    - .planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/164-RESEARCH.md (Example 4 — starter table at lines 532-555)
    - .planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/164-CONTEXT.md (D-01..D-03 spec)
    - .planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-CONTEXT.md (D-15, D-16, D-17, D-18, D-19 — what was re-added in Phase 162)
    - Output from Task 1 (grep results)
    - SUMMARYs from Plans 03 (RawHtml), 05 (CardVariant), 08 (Visibility/PageHeader fixes) — features that close potential BLOCKERs
  </read_first>
  <action>
    Create `.planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/V1-DELETION-AUDIT.md`:

    ```markdown
    # V1-Deletion Readiness Audit (Phase 164 D-01..D-03)

    **Audited:** 2026-05-17 (or actual date of execution)
    **Status:** [draft / approved]
    **Input to:** Phase 160 (v1 JSON-UI API deletion), Phase 161 (v12.0 merge + publish)

    ## Purpose

    Phase 160 deletes the v1 JSON-UI API. Phase 164 ensures that deletion is safe by walking every v1 public surface element and confirming a v2 equivalent exists, OR documenting the gap as intentional, OR escalating to a BLOCKER (which must be closed before Phase 160 can run).

    ## Methodology

    1. Enumerate v1 surface elements: types previously re-exported from `ferro-json-ui` and `framework` crates; consult Phase 115's `115-CONTEXT.md` for the full list of what was deleted in commit `dbe5adaf`.
    2. For each element: check v2 equivalent (via ferro-json-ui catalog, plugin guide, or docs), gestiscilo migration status (per Phase 138 + V7-RUNTIME-FRICTION.md), and Resolution.
    3. Resolution column values:
       - **MIGRATED** — v2 has a direct equivalent; all consumers ported successfully.
       - **INTENTIONAL_DROP** — no v2 equivalent; documented in COMPLETED.md as a future-milestone gap; no current consumer blocked.
       - **BLOCKER** — no v2 equivalent; real consumer still needs it; Phase 164 ships a fix.

    ## Grep evidence (v1 absence)

    [Embed the grep output from Task 1 here.]

    | Check | Command | Result |
    |-------|---------|--------|
    | JsonUiView / ComponentNode / PluginProps in source | (see Task 1) | [N matches in test/comments only — pass] |
    | Component::* enum variants in source | (see Task 1) | [0 — pass] |
    | view.rs file existence | ls ferro-json-ui/src/view.rs | [No such file — pass] |
    | framework/src/lib.rs v1 re-exports | grep | [none — pass] |
    | ferro-json-ui/src/lib.rs v1 re-exports | grep | [none — pass] |

    ## Surface audit

    | v1 surface | v2 equivalent | gestiscilo usage | Resolution | Notes |
    |------------|---------------|------------------|------------|-------|
    | `JsonUiView` | `Spec { schema_version, root, elements }` | Migrated in all controllers | MIGRATED | Deleted in commit `dbe5adaf`. |
    | `Component` enum | `Element.type_name: String` + catalog | Every Element since Phase 115 | MIGRATED | Type-erased dispatch via catalog. |
    | `ComponentNode` | `Element` in flat `Spec.elements` HashMap | Codemod `ferro json-ui:migrate-v1` for v1 controllers | MIGRATED | v2 is flat; nesting by ID. |
    | `PluginProps { plugin_type, props }` | First-class plugin type names (e.g. `"StripeConnectStatus"`) via `JsonUiPlugin`; one-off islands via `RawHtml` (Phase 164 D-17a) | gestiscilo settings → `"type": "Plugin"` — closed by D-17a RawHtml | MIGRATED via D-17a + plugin docs | Phase 115 D-01 killed the generic dispatch; D-17a + plugins.md is the migration path. |
    | `CardProps.children` (typed) | `Element.children: Vec<String>` ID refs | All Card uses | MIGRATED | Same for FormProps.fields, GridProps.children, CollapsibleProps.children, FormSectionProps.children, ButtonGroupProps.buttons. |
    | `SwitchProps.compact` | Re-added in Phase 162 D-16 | 6 gestiscilo settings sites | MIGRATED | |
    | `ImageProps::inline_svg` | Re-added in Phase 162 D-17 | gestiscilo statistiche bar charts | MIGRATED | |
    | `RichTextEditorProps` | Re-added in Phase 162 D-18 (as plugin or first-class — verify with 162 SUMMARY) | 2 gestiscilo documenti templates | MIGRATED | |
    | `DetailFormProps` / `DetailField` / `EditMode` | Documented v2 pattern (Phase 162 D-15) — `Form` with explicit `Input` children pre-populated via `data_path` | gestiscilo documenti edit flows | INTENTIONAL_DROP | Pattern documented in Phase 162; no consumer blocked. |
    | `make_node` / `make_node_with_action` builder helpers | `JsonUi::render_file` + JSON spec files; codemod for legacy controllers | Phase 138 controllers all migrated | INTENTIONAL_DROP | Consumer-side helpers; never part of ferro public API. |
    | `view.rs` / `JsonUiView::new` builder chain | `Spec::builder()` / `Spec::from_json` | All controllers | MIGRATED | File deleted in commit `dbe5adaf`. |
    | Spec.title literal-only | `Option<TitleBinding>` (literal or {$data}) via Phase 164 D-12 | 23 gestiscilo specs unblocked | MIGRATED via D-12 | |
    | KanbanBoard with hand-coded columns | `data_path` override via Phase 164 D-13a | gestiscilo dashboard kanban | MIGRATED via D-13a | |
    | MAX_NESTING_DEPTH = 3 ceiling | Raised to 5 via Phase 164 D-14 | gestiscilo dashboard depth-4 page | MIGRATED via D-14 | |
    | Image.src static-only / DescList.items static-only | `data_path` overrides via Phase 164 D-15 | gestiscilo statistiche dynamic data | MIGRATED via D-15 | |
    | Parse-time enum validation against raw spec | Validation after expand_directives via Phase 164 D-16 | 2 gestiscilo pages with $if-gated bad-variant | MIGRATED via D-16 | |
    | Card chrome hard-coded for dashboard | `CardVariant` enum via Phase 164 D-18 | gestiscilo auth/login + errors pages | MIGRATED via D-18 | |
    | Visibility deserializer error opaque | Hand-rolled deserialiser names all shapes via Phase 164 D-19/F5 | gestiscilo clienti/flotta debugging | MIGRATED via D-19/F5 | |
    | PageHeader.actions strict `Vec<String>` | Lax deserializer accepting null/empty-string via Phase 164 D-19/F6 | gestiscilo pages with optional actions | MIGRATED via D-19/F6 | |

    ## BLOCKER summary

    Total BLOCKER rows: **0**

    Phase 160 (v1 deletion) is **UNBLOCKED**.

    ## Plugin surface audit cross-reference

    See `PLUGIN-SURFACE-AUDIT.md` for the D-06 paper-exercise outcome (or noted "clean — verbal checkpoint" if no gaps surfaced).

    ## Sign-off

    [ ] Audit reviewed by user
    [ ] BLOCKER count confirmed = 0
    [ ] Phase 160 (v1 deletion) cleared to proceed
    ```

    1. Replace the embedded grep result placeholders with actual Task 1 output.
    2. Verify each row by `grep`-ing the actual ferro-json-ui / framework source for the v2 equivalent.
    3. If any row would be BLOCKER, STOP and surface to the user before writing the file — per CONTEXT D-03, a BLOCKER row means Phase 164 must ship a fix OR reclassify as INTENTIONAL_DROP with rationale.
  </action>
  <verify>
    <automated>cd /Users/alberto/repositories/albertogferrario/ferro && grep -c '| MIGRATED\|| INTENTIONAL_DROP\|| BLOCKER' .planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/V1-DELETION-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/V1-DELETION-AUDIT.md` exits 0
    - `grep -c '| BLOCKER' .planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/V1-DELETION-AUDIT.md` returns `0` (no BLOCKER rows in the table)
    - `grep -c '| MIGRATED\|| INTENTIONAL_DROP\|| BLOCKER' .planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/V1-DELETION-AUDIT.md` returns at least `15` (i.e. the table is populated with at least 15 rows)
    - The "BLOCKER summary" section explicitly states "Total BLOCKER rows: 0"
    - Grep evidence section is populated (no placeholder text)
  </acceptance_criteria>
  <done>
    V1-DELETION-AUDIT.md exists; table is populated; zero BLOCKERs; ready for user review.
  </done>
</task>

<task type="auto">
  <name>Task 3: D-06/D-07 plugin surface paper-audit</name>
  <read_first>
    - docs/src/json-ui/plugins.md (the Phase 162 D-19 plugin author guide; updated by Plan 10 Task 4)
    - .planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/164-CONTEXT.md (D-06..D-07)
    - .planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/164-RESEARCH.md (Open Questions §4)
  </read_first>
  <action>
    Conduct three paper-implementation exercises against `docs/src/json-ui/plugins.md`. For each scenario, walk the plugin guide as if implementing the widget from scratch. Note any step where the documentation is insufficient to proceed.

    **Scenario A — Stripe payment widget**

    Goal: A plugin that renders a "Connect Stripe" button when no Stripe account is linked, and a "Stripe Connected (acct_xxx)" status pill when one is. Needs to: receive `stripe_account_id: Option<String>` from handler data; render either of two HTML shapes.

    Walk plugins.md. Note doc-gaps if any (e.g. "how do I get handler data into the plugin's render fn?", "how do I declare CSS assets?", etc.).

    **Scenario B — WhatsApp connection flow**

    Goal: A plugin that orchestrates a WhatsApp Business API connection: shows a "Link WhatsApp" button → opens a QR code modal → polls a status endpoint → flips to "Connected" state. Needs: assets injection (QR-rendering JS), client-side state, fetch polling.

    Walk plugins.md. Note doc-gaps.

    **Scenario C — Chart renderer**

    Goal: A plugin that renders a bar chart from `data_path`-resolved data. Needs: typed props shape; rendering at the server side (SVG) AND/OR client-side library hook.

    Walk plugins.md. Note doc-gaps.

    **Outcome dispatch:**

    - **Outcome A — Zero gaps** for all three scenarios: write a one-paragraph note in SUMMARY ("D-06 audit clean — verbal checkpoint complete") and do NOT create PLUGIN-SURFACE-AUDIT.md.

    - **Outcome B — Minor gaps (1-2 docs fixes per scenario)**: Fix in plugins.md directly (under Plan 10 Task 4's mandate; if Plan 10 already shipped, ship the fixes here). Create `PLUGIN-SURFACE-AUDIT.md` with: scenarios attempted, gaps surfaced, fixes shipped. Mark as clean.

    - **Outcome C — Major gaps (≥3 per scenario OR a load-bearing missing primitive like asset-injection ordering)**: Per CONTEXT D-07, escalate to a BLOCKER row in V1-DELETION-AUDIT.md AND ship the fix in this phase. If the fix would require new code in ferro-json-ui/src/plugin.rs, surface to the user as a checkpoint:decision (this plan cannot ship plugin.rs changes — it would conflict with Plans 03/08 file ownership).

    Per CONTEXT D-06: this is a paper exercise, NOT actual plugin implementation. The audit is the deliverable.

    Capture the outcome in SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/alberto/repositories/albertogferrario/ferro && ls .planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/PLUGIN-SURFACE-AUDIT.md 2>&1; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - Outcome A: SUMMARY contains "D-06 audit clean" note; PLUGIN-SURFACE-AUDIT.md may be absent
    - Outcome B: PLUGIN-SURFACE-AUDIT.md exists with gap list + fixes shipped; clean
    - Outcome C: V1-DELETION-AUDIT.md table has a BLOCKER row referencing the audit + a checkpoint:decision was raised
    - Audit outcome explicitly captured (one of A/B/C) in SUMMARY
  </acceptance_criteria>
  <done>
    Plugin surface audit completed; outcome captured; no silent gap-coverage.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: User reviews V1-DELETION-AUDIT.md and approves BLOCKER classification</name>
  <what-built>
    Plans 01-10 shipped 17 ferro-side decisions (D-01..D-19); Plan 11 produced V1-DELETION-AUDIT.md (and conditionally PLUGIN-SURFACE-AUDIT.md). The audit table classifies every v1 surface element as MIGRATED, INTENTIONAL_DROP, or BLOCKER. The expected outcome is zero BLOCKER rows — Phase 160 (v1 deletion) gates on this.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/V1-DELETION-AUDIT.md`.
    2. Read every row of the surface-audit table. For each MIGRATED row, spot-check the v2 equivalent exists (e.g. for "RawHtml closes Plugin gap", confirm `grep -c "pub struct RawHtmlProps" ferro-json-ui/src/component.rs` returns 1).
    3. For every INTENTIONAL_DROP row, confirm you (the user) agree the gap is acceptable for v12.0. If any feel BLOCKER-worthy, request the planner add a new plan to close them — or accept the drop with full understanding.
    4. Confirm "BLOCKER summary" reads "Total BLOCKER rows: 0".
    5. If a PLUGIN-SURFACE-AUDIT.md exists, read its outcome (clean / N gaps fixed / escalated).
    6. Approve: type "approved" to unblock Plan 12 (COMPLETED.md) and proceed to Phase 160 readiness. OR: describe issues — the planner will replan.
  </how-to-verify>
  <action>
    Stop execution and surface the V1-DELETION-AUDIT.md to the user for review per the steps in &lt;how-to-verify&gt;. Do not auto-advance. The user explicitly approves the BLOCKER classification (must be 0) and any INTENTIONAL_DROP rows. If the user rejects any row or asks for changes, surface the requested change as a follow-up plan request rather than silently editing the audit.
  </action>
  <resume-signal>Type "approved" to proceed; OR describe what should change.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit artefact → Phase 160 / Phase 161 readiness gate | The V1-DELETION-AUDIT.md is the gating document for Phase 160. A silent BLOCKER (mis-classified as INTENTIONAL_DROP) would let Phase 160 ship and break a real consumer. The user-checkpoint mitigates this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-11-01 | T (Tampering) | a row mis-classified as INTENTIONAL_DROP when it should be BLOCKER | mitigate | (a) Plans 01-10's SUMMARYs are loaded as evidence — the audit cross-references concrete fixes shipped in this phase. (b) User checkpoint is BLOCKING — the audit cannot ship without explicit user approval per CONTEXT D-03. |
| T-164-11-02 | I (Information disclosure) | audit lists internal classification labels | accept | MIGRATED / INTENTIONAL_DROP / BLOCKER are documentation-internal labels; no security-sensitive content. |
</threat_model>

<verification>
- V1-DELETION-AUDIT.md exists, populated, zero BLOCKERs
- Plugin surface audit (D-06) outcome captured
- User reviewed and approved the audit
- Phase 160 readiness condition met
</verification>

<success_criteria>
- D-01: audit table covers every v1 surface element
- D-02: every row has a Resolution
- D-03: zero BLOCKER rows (per Phase 160 gate)
- D-06: plugin paper-audit completed; outcome captured
- D-07: any gaps surfaced are either fixed in this phase or escalated to BLOCKER
- User approval recorded
</success_criteria>

<output>
After completion, create `.planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/164-11-SUMMARY.md` listing: (a) the audit row count by Resolution category (X MIGRATED, Y INTENTIONAL_DROP, Z BLOCKER — Z must be 0), (b) the D-06 outcome (A/B/C), (c) the user-approval timestamp (or note "pending user approval" if the checkpoint blocks), (d) handoff statement for Plan 12 (COMPLETED.md): the audit table can be embedded as the "v1 → v2 surface migration table" section.
</output>
