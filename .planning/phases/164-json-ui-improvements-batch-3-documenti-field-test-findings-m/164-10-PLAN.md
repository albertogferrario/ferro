---
phase: 164
plan: 10
type: execute
wave: 5
depends_on: [01, 03, 04, 05, 06, 07, 08, 09]
files_modified:
  - docs/src/json-ui/components.md
  - docs/src/json-ui/expressions.md
  - docs/src/json-ui/migration-v1-to-v2.md
  - docs/src/json-ui/plugins.md
  - docs/src/json-ui/spec-construction.md
autonomous: true
requirements: [D-08, D-09, D-13b]
must_haves:
  truths:
    - "components.md has new sub-sections for: CardVariant (D-18), Image.data_path (D-15), DescriptionList.data_path (D-15), KanbanBoard.data_path (D-13a), RawHtml (D-17a), PageHeader.actions lax acceptance (D-19/F6)"
    - "expressions.md has a worked $each example for kanban columns (D-13b) — distinct from D-13a's data_path pattern"
    - "migration-v1-to-v2.md has a 10-row v1 → v2 cheat sheet at the top of the file (D-09)"
    - "spec-construction.md mentions the new D-12 Spec.title binding shape (literal or {$data})"
    - "plugins.md cross-references RawHtml as a narrow alternative; reaffirms Phase 115 D-01 ban on generic Plugin dispatch"
    - "mdbook build is clean"
  artifacts:
    - path: "docs/src/json-ui/components.md"
      provides: "Doc sections for every new/modified component prop from Phase 164"
    - path: "docs/src/json-ui/expressions.md"
      provides: "$each-for-kanban worked example"
    - path: "docs/src/json-ui/migration-v1-to-v2.md"
      provides: "Top-of-page cheat-sheet table (10 rows)"
    - path: "docs/src/json-ui/spec-construction.md"
      provides: "Spec.title binding documented; MAX_NESTING_DEPTH=5 already by Plan 01"
    - path: "docs/src/json-ui/plugins.md"
      provides: "Cross-link to RawHtml for HTML-island use; reaffirms generic-Plugin ban"
  key_links:
    - from: "components.md RawHtml section"
      to: "plugins.md richer-widget recommendation"
      via: "cross-link 'see plugins.md'"
      pattern: "see.*plugins\\.md|plugins\\.md.*see"
    - from: "expressions.md $each-kanban example"
      to: "components.md KanbanBoard.data_path section"
      via: "cross-link distinguishing the two patterns"
---

<objective>
Documentation pass closing out the v12.0 ferro-side JSON-UI surface. Three deliverables per CONTEXT D-08, D-09, D-13b:

1. **D-08** — Final sweep over `docs/src/json-ui/`: add doc sections for every new field/variant/behaviour from Plans 01-09. Confirm every component in the catalog has a matching doc section.

2. **D-09** — Add a "v1 → v2 cheat sheet" table to the top of `migration-v1-to-v2.md`. 10 rows drawn from the gestiscilo migration's actual code rewrites.

3. **D-13b** — Add a worked `$each` example for kanban columns in `expressions.md`. Per Assumption A7: position this as "one element type per card row inside a column" (different from D-13a's `data_path` which is "one element type per column").

This plan depends on Plans 01, 03, 04, 05, 06, 07, 08, 09 because each ships a feature requiring documentation. Plan 02 (codemod regression test) does not touch user-facing docs.

Voice constraint per CLAUDE.md "Repository documents must read as neutral": no marketing copy, no "killer feature" framing, no first-person, no strategic positioning. Sections describe the feature, the JSON shape, and one short rationale paragraph. Code examples in JSON form (matching the existing components.md style).

Output: Updated component-by-component sections; new cheat-sheet table; one new $each example; cross-references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/164-CONTEXT.md
@.planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/164-PATTERNS.md
@.planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/164-RESEARCH.md
@docs/src/json-ui/components.md
@docs/src/json-ui/expressions.md
@docs/src/json-ui/migration-v1-to-v2.md
@docs/src/json-ui/spec-construction.md
@docs/src/json-ui/plugins.md
</context>

<interfaces>
docs/src/json-ui/components.md follows a per-component pattern: heading, brief description, JSON example, props table. The executor MUST read 3-5 existing sections (Button, Alert, Card, ActionCard, DataTable) before writing new sections to match the established style. The plan touches FIVE doc files — read each before editing.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Document new component fields and variants in components.md and spec-construction.md (D-08)</name>
  <read_first>
    - docs/src/json-ui/components.md (entire file; focus on Button, Alert, Card, ActionCard, Image, DescriptionList, KanbanBoard, PageHeader, DataTable sections as templates)
    - docs/src/json-ui/spec-construction.md (entire file; for the Spec.title binding addition)
    - ferro-json-ui/src/component.rs (current state AFTER Plans 03, 04, 05, 06, 08 land — confirm field names and shapes)
    - .planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/164-CONTEXT.md (D-08 list of features needing docs)
  </read_first>
  <action>
    Edit `docs/src/json-ui/components.md`. For each of the seven items below, ADD or UPDATE a sub-section under the relevant component heading. Match the existing per-component voice (sober, technical, no marketing).

    **A. Card variant (D-18, Plan 05).** Under the `Card` section, add a "Variant" subsection covering `bordered` (default; `border + bg-card + shadow-sm` with `p-4`) and `elevated` (`bg-card + shadow-md`, no border, with `p-8`). Include a JSON example with `"variant": "elevated"`.

    **B. Image data_path (D-15, Plan 03).** Under the `Image` section, add a "Dynamic source via data_path" subsection: `data_path` (optional, takes precedence over `src`) resolves a JSON Pointer against handler data; missing or non-string paths fall back to `src`. Include a JSON example.

    **C. DescriptionList data_path (D-15, Plan 03).** Under `DescriptionList`, add the parallel subsection: `data_path` resolves to a JSON array of DescriptionItem-shaped objects; falls back to `items` when missing.

    **D. KanbanBoard data_path (D-13a, Plan 06).** Under `KanbanBoard`, add a "Dynamic columns via data_path" subsection: `data_path` resolves to a JSON array decoded as `Vec<KanbanColumnProps>`. Add a "For finer-grained templating (one element type per card row inside a column), see the $each directive in expressions.md" cross-link.

    **E. RawHtml component (D-17a, Plan 03).** Add a NEW component section under appropriate alphabetical/grouping placement. Include: brief description (server-injected HTML island for narrow HTML-fragment use cases); JSON example; a "Trust boundary" call-out stating `html` is emitted verbatim with no sanitization, consumer is responsible for sanitisation (mirror RichTextEditor's discipline, suggest `ammonia` for untrusted input); cross-link "For richer widgets (interactive forms, charts, OAuth flows), use the first-class plugin system — see plugins.md".

    **F. PageHeader.actions lax acceptance (D-19/F6, Plan 08).** Under `PageHeader`, add an "actions" subsection noting acceptance of: omission, `null`, empty string `""`, or array of element IDs. The lax forms deserialize to an empty list.

    Edit `docs/src/json-ui/spec-construction.md`:

    **G. Spec.title binding (D-12, Plan 04).** Add a "title binding" subsection near the existing `title` reference: `title` accepts either a literal string or `{"$data": "/path"}` binding; bindings resolve against `spec.data` at render time via JSON Pointer; missing/non-string falls back to "Ferro". Include both literal and binding JSON examples.

    Confirm `spec-construction.md`'s `MAX_NESTING_DEPTH` reference already says `5` (Plan 01 Task 2 handled this).
  </action>
  <verify>
    <automated>cd /Users/alberto/repositories/albertogferrario/ferro && (cd docs && mdbook build 2>&1 | tail -10)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '#### Variant\|### CardVariant' docs/src/json-ui/components.md` returns at least `1`
    - `grep -c 'data_path' docs/src/json-ui/components.md` returns at least `3`
    - `grep -c '### RawHtml' docs/src/json-ui/components.md` returns `1`
    - `grep -c 'Trust boundary\|verbatim with no sanitization' docs/src/json-ui/components.md` returns at least `1`
    - `grep -c '\$data\|title binding' docs/src/json-ui/spec-construction.md` returns at least `1`
    - `(cd docs && mdbook build)` exits 0
  </acceptance_criteria>
  <done>
    Seven doc additions in place; mdbook build clean; voice neutral.
  </done>
</task>

<task type="auto">
  <name>Task 2: v1→v2 cheat sheet in migration-v1-to-v2.md (D-09)</name>
  <read_first>
    - docs/src/json-ui/migration-v1-to-v2.md (entire file — may have an existing cheat-sheet stub from Phase 162 D-20)
    - .planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-CONTEXT.md (D-20 — original cheat-sheet decision)
    - Phase 138 + Phase 162-163 FRICTION files for examples of common v1 patterns
  </read_first>
  <action>
    Edit `docs/src/json-ui/migration-v1-to-v2.md`:

    1. At the top (after opening heading + brief intro), insert (or replace any existing stub) a "## Cheat sheet" section with a 10-row table mapping v1 patterns to v2 equivalents. Cover at minimum:
    - `JsonUiView::new("name").card(...)` → `Spec::builder().element(...)` (or JSON spec + JsonUi::render_file)
    - `Component::Card { children: vec![Component::Text(...)] }` → `Element::new("Card").with_children(vec!["text_id".into()])` + sibling Text element (v2 is flat; nesting by ID)
    - `Component::Plugin { plugin_type: "Stripe", props: {...} }` → register a `JsonUiPlugin` with explicit type name (e.g. `"StripeConnectStatus"`); for one-off HTML islands use `RawHtml`. Note: generic Plugin dispatch removed (Phase 115 D-01).
    - `Button::new("Submit").action(Action::post("users.store"))` → `{"type": "Button", "props": {"label": "Submit", "on_click": {"method": "POST", "handler": "users.store"}}}`. Note: HTTP method MUST be uppercase.
    - `DetailForm { fields, mode: EditMode::Edit }` → `Form` component with explicit `Input` children pre-populated via `data_path`
    - `view.with_validation_errors(errs)` → errors via handler data referenced by `data_path` on relevant elements
    - `make_node_with_action(...)` helper → hand-author or `Spec::builder`; codemod `ferro json-ui:migrate-v1` for simple cases
    - `auth/layout.rs` implicit Card wrapper → each auth spec declares own `Card` root with `"variant": "elevated"` (Phase 162 D-05 + Phase 164 D-18)
    - Conditional rendering via Rust `if/else` in controller → `visible: {path, operator}` or `$if: {path}` (which removes element before catalog validation)
    - `KanbanBoard` with hand-coded columns → `{"type": "KanbanBoard", "props": {"data_path": "/order_columns"}}` driven by handler-emitted data (Phase 164 D-13a)

    Each row: v1 pattern, v2 equivalent, brief notes.

    2. If an earlier-phase cheat sheet exists, MERGE rather than duplicate. Drop rows that became inaccurate; keep accurate ones; add Phase 164's rows.
  </action>
  <verify>
    <automated>grep -c '## Cheat sheet\|## v1 → v2 cheat sheet' /Users/alberto/repositories/albertogferrario/ferro/docs/src/json-ui/migration-v1-to-v2.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## Cheat sheet\|## v1 → v2 cheat sheet' docs/src/json-ui/migration-v1-to-v2.md` returns at least `1`
    - The cheat-sheet table contains at least 10 rows
    - `grep -c 'KanbanBoard.*data_path\|variant.*elevated\|RawHtml' docs/src/json-ui/migration-v1-to-v2.md` returns at least `3` (Phase 164 features represented)
    - `(cd docs && mdbook build)` exits 0
  </acceptance_criteria>
  <done>
    Cheat-sheet table in place with 10+ rows; Phase 164 surface represented; mdbook build clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: $each-for-kanban example in expressions.md (D-13b)</name>
  <read_first>
    - docs/src/json-ui/expressions.md (entire file — find the existing `$each` directive section from Phase 163 D-08)
    - .planning/phases/163-json-ui-improvements-batch-2-cassa-and-calendario-field-test/163-CONTEXT.md (D-08 — original $each documentation decision)
    - .planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/164-CONTEXT.md (D-13b)
  </read_first>
  <action>
    Edit `docs/src/json-ui/expressions.md`:

    Locate the existing `$each` directive section. Append a worked-example sub-section "Example: kanban cards from a data array" that:

    1. Frames the tradeoff: `$each` templates element STRUCTURE (one element-type per array item); use it inside `KanbanColumn.cards` for the "fixed columns, varying card content" case.

    2. Distinguishes from `KanbanBoard.data_path` (D-13a, documented in components.md). Include a small comparison table:

    ```
    | Pattern | Use when |
    |---------|----------|
    | KanbanBoard.data_path (D-13a) | Column set varies (one column per status); each column has the same KanbanColumnProps shape |
    | $each inside KanbanColumn (D-13b) | Column set is fixed but card content varies per item |
    ```

    3. Includes a JSON spec snippet using `$each` to template Card elements inside a fixed `KanbanColumn`. Read the existing `$each` section first to discover the canonical key names (likely `path`, `as`, `template`); use those exact keys.

    4. Add a corresponding cross-link from `components.md`'s KanbanBoard section back to this expressions.md sub-section (a single "See also: $each-for-kanban-cards in expressions.md" line).
  </action>
  <verify>
    <automated>grep -c 'kanban\|KanbanColumn' /Users/alberto/repositories/albertogferrario/ferro/docs/src/json-ui/expressions.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'kanban\|KanbanColumn' docs/src/json-ui/expressions.md` returns at least `2`
    - `grep -c '\$each' docs/src/json-ui/expressions.md` returns at least `3` (existing section + new example references)
    - `grep -c 'expressions.md' docs/src/json-ui/components.md` returns at least `1` (back-reference from KanbanBoard section)
    - `(cd docs && mdbook build)` exits 0
  </acceptance_criteria>
  <done>
    $each-for-kanban example documented; comparison table distinguishes from D-13a; cross-link added.
  </done>
</task>

<task type="auto">
  <name>Task 4: plugins.md cross-reference to RawHtml + reaffirm Plugin-dispatch ban</name>
  <read_first>
    - docs/src/json-ui/plugins.md (entire file — from Phase 162 D-19)
    - .planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/164-CONTEXT.md (D-06..D-07 — plugin surface audit; D-17a default rationale)
  </read_first>
  <action>
    Edit `docs/src/json-ui/plugins.md`:

    1. Near the top (after the intro paragraph), add a "When to use RawHtml instead" subsection:

    ```markdown
    ## When to use RawHtml instead

    For a one-off HTML fragment (status pill, badge, link decoration), the `RawHtml` component is the lowest-friction option — see components.md. RawHtml is a single-field primitive (`html: String`) emitted verbatim into the response.

    Choose a first-class `JsonUiPlugin` (the rest of this guide) when:
    - The widget is interactive (forms, OAuth flows, dynamic state)
    - The widget needs asset injection (CSS/JS bundles)
    - The widget is reused across multiple pages and benefits from explicit registration with a type name

    The generic v1 `Component::Plugin { plugin_type, props }` dispatch was removed in Phase 115 D-01. Every plugin in v2 has its own type name (e.g. `"StripeConnectStatus"`, `"Map"`); see the type-name registration section below.
    ```

    2. If there's an existing section that mentions Plugin-style escape hatches without naming the prohibition, reword to align with Phase 115 D-01 + Phase 164 D-17a's framing (RawHtml = narrow primitive; richer widgets = JsonUiPlugin).

    3. **D-06..D-07 audit checkpoint (paper exercise):** As you read plugins.md, do a thought-experiment of authoring three plugins:
       - (a) a Stripe payment status widget,
       - (b) a WhatsApp connection-status flow,
       - (c) a chart renderer.

       For each: identify a step where the documentation doesn't tell you what to do. If you surface a doc-gap, ADD a short clarifying paragraph in plugins.md (don't write a whole new section). If no gap surfaces, note "D-06 paper audit clean" in the SUMMARY. The audit table for D-01..D-03 (Plan 11) records the audit outcome.

       Per CONTEXT D-06: "audit produces a written artefact or is a verbal checkpoint with the user — depends on how many gaps it surfaces." If your audit surfaces 3+ gaps, escalate to Plan 11 as a `BLOCKER` row. If 0-2 gaps, fix them inline here and note in SUMMARY.
  </action>
  <verify>
    <automated>grep -c 'RawHtml\|JsonUiPlugin' /Users/alberto/repositories/albertogferrario/ferro/docs/src/json-ui/plugins.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'RawHtml' docs/src/json-ui/plugins.md` returns at least `1`
    - `grep -c 'Phase 115 D-01\|generic.*Plugin.*dispatch\|removed.*Plugin' docs/src/json-ui/plugins.md` returns at least `1`
    - `(cd docs && mdbook build)` exits 0
    - The audit conclusion is captured in SUMMARY (clean OR list of gaps fixed inline OR escalation to Plan 11 BLOCKER)
  </acceptance_criteria>
  <done>
    plugins.md cross-references RawHtml; reaffirms generic-Plugin ban; D-06 audit conclusion captured.
  </done>
</task>

<task type="auto">
  <name>Task 5: Per-component doc coverage grep + mdbook build (D-08 coverage check)</name>
  <read_first>
    - ferro-json-ui/src/render/mod.rs (BUILTIN_TYPES array — 41 components after Plan 03)
    - docs/src/json-ui/components.md
  </read_first>
  <action>
    Final coverage sweep:

    1. List the 41 built-in component names:

    ```bash
    grep -oE '"[A-Z][A-Za-z]+"' ferro-json-ui/src/render/mod.rs | sed 's/"//g' | sort -u > /tmp/component_names.txt
    ```

    2. For each component name in `/tmp/component_names.txt`, grep `docs/src/json-ui/components.md` (and adjacent doc files) for a matching section heading:

    ```bash
    while read name; do
        if ! grep -q "### $name\b\|## $name\b" docs/src/json-ui/components.md docs/src/json-ui/*.md 2>/dev/null; then
            echo "MISSING DOC: $name"
        fi
    done < /tmp/component_names.txt
    ```

    3. For each `MISSING DOC` line, add a stub section to components.md with at minimum: heading, one-sentence description (read the catalog entry in `ferro-json-ui/src/catalog.rs` for the canonical description), one JSON example.

    4. Re-run the coverage script and confirm zero MISSING DOC lines.

    5. Run `(cd docs && mdbook build)` — must exit 0.
  </action>
  <verify>
    <automated>cd /Users/alberto/repositories/albertogferrario/ferro && (cd docs && mdbook build 2>&1 | tail -10) && grep -oE '"[A-Z][A-Za-z]+"' ferro-json-ui/src/render/mod.rs | sed 's/"//g' | sort -u | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All 41 components in BUILTIN_TYPES have a matching section heading in components.md or another file under docs/src/json-ui/
    - `(cd docs && mdbook build)` exits 0
    - The coverage grep (above bash snippet) prints zero `MISSING DOC` lines
  </acceptance_criteria>
  <done>
    Per-component doc coverage proven complete; mdbook build clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo reader (incl. external) → documentation | Repository documents are potentially public per CLAUDE.md. Documentation describes the surface neutrally; no internal strategic framing, no Trigger-Phrase voice. |
| RawHtml documentation → consumer trust-boundary discipline | The Trust-boundary call-out in components.md is the primary place a consumer reads about sanitisation responsibility. The wording must be unambiguous. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-10-01 | I (Information disclosure) | docs may reveal strategic positioning or internal voice | mitigate | Per CLAUDE.md: review every new section for Trigger Phrases ("killer feature", "the bet", "load-bearing weakness", "no stop-loss", etc.); softening or removal is a real fix. Doc voice is technical and neutral throughout. |
| T-164-10-02 | I | RawHtml docs could understate the trust boundary | mitigate | The "Trust boundary" call-out uses bold emphasis and explicit language ("verbatim with no sanitization"); mirrors RichTextEditor's discipline statement. A consumer skimming the section cannot miss the responsibility transfer. |
| T-164-10-03 | T (Tampering) | inaccurate cheat-sheet rows could mislead migrators | mitigate | Task 2 specifies: drop inaccurate rows from earlier-phase stubs; verify each v2 equivalent against the current source (`grep` for the cited APIs). |
</threat_model>

<verification>
- Five doc files updated (components.md, spec-construction.md, migration-v1-to-v2.md, expressions.md, plugins.md)
- Every catalog component (41 after Plan 03) has a doc section
- Cheat sheet (10+ rows) present at top of migration-v1-to-v2.md
- $each-for-kanban example present in expressions.md
- RawHtml trust-boundary call-out present and unambiguous
- mdbook build clean
- D-06 plugin-audit conclusion captured in SUMMARY
</verification>

<success_criteria>
- D-08: every Phase 164 feature has a doc section; coverage grep clean
- D-09: cheat-sheet table present with 10+ rows
- D-13b: $each-for-kanban example documented; distinguished from D-13a's data_path pattern
- D-06: plugin paper-audit completed; outcome recorded
- mdbook build green
</success_criteria>

<output>
After completion, create `.planning/phases/164-json-ui-improvements-batch-3-documenti-field-test-findings-m/164-10-SUMMARY.md` listing: (a) the list of doc files modified and the new sections added (with anchor headings), (b) the D-06 plugin-audit outcome (clean / N gaps fixed inline / escalated to Plan 11 BLOCKER), (c) the per-component coverage grep result (must read "0 MISSING DOC"), (d) confirmation that mdbook build is clean.
</output>
