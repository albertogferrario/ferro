---
phase: 122-deploy-scaffold-core-rewrite
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - ferro-cli/src/templates/files/docker/dockerignore.tpl
autonomous: true
requirements: [D-20, D-21]
must_haves:
  truths:
    - ".dockerignore template ignores database.db, *.sqlite*, .planning/, storage/, and data/"
    - "The drift-vs-gitignore audit is captured as a top-of-file comment referencing Phase 124"
  artifacts:
    - path: "ferro-cli/src/templates/files/docker/dockerignore.tpl"
      provides: "Updated .dockerignore with D-20 entries"
      contains: "database.db"
  key_links: []
---

<objective>
Append the five entries from D-20 to `dockerignore.tpl` and note the Phase 124 follow-up for drift sync.

Purpose: Prevent committed SQLite databases, planning notes, user uploads, and data dumps from being copied into Docker build contexts.
Output: Updated `dockerignore.tpl`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-deploy-scaffold-core-rewrite/SCOPE.md
@ferro-cli/src/templates/files/docker/dockerignore.tpl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add D-20 entries to dockerignore.tpl</name>
  <read_first>
    - ferro-cli/src/templates/files/docker/dockerignore.tpl (current full content)
  </read_first>
  <files>ferro-cli/src/templates/files/docker/dockerignore.tpl</files>
  <action>
    Append a new section to the END of `ferro-cli/src/templates/files/docker/dockerignore.tpl`:

    ```
    # Local databases and SQLite files
    database.db
    *.sqlite*

    # Planning and workspace notes (never ship to Docker context)
    .planning/

    # User-uploaded files and runtime data
    storage/
    data/

    # NOTE: .gitignore/.dockerignore drift audit deferred to Phase 124.
    ```

    Do not remove or reorder any pre-existing lines. The new block goes after the current last section.
  </action>
  <verify>
    <automated>grep -qx 'database.db' ferro-cli/src/templates/files/docker/dockerignore.tpl && grep -qx '\*.sqlite\*' ferro-cli/src/templates/files/docker/dockerignore.tpl && grep -qx '.planning/' ferro-cli/src/templates/files/docker/dockerignore.tpl && grep -qx 'storage/' ferro-cli/src/templates/files/docker/dockerignore.tpl && grep -qx 'data/' ferro-cli/src/templates/files/docker/dockerignore.tpl && grep -q 'Phase 124' ferro-cli/src/templates/files/docker/dockerignore.tpl</automated>
  </verify>
  <acceptance_criteria>
    - All 5 D-20 entries appear as exact lines in dockerignore.tpl (grep -qx confirms).
    - The comment referencing Phase 124 is present.
    - Pre-existing entries (target/, .env, etc.) are still present (`grep -qx 'target/' ferro-cli/src/templates/files/docker/dockerignore.tpl` succeeds).
    - `cargo build -p ferro-cli` exits 0 (no Rust file touched, but include_str! consumers must still compile).
  </acceptance_criteria>
  <done>dockerignore.tpl contains all five D-20 entries and a Phase 124 deferral note.</done>
</task>

</tasks>

<verification>
- Grep checks pass (see verify block)
- cargo build -p ferro-cli
</verification>

<success_criteria>
Fresh Ferro projects scaffolded via `ferro docker:init` have `database.db`, `*.sqlite*`, `.planning/`, `storage/`, and `data/` in their `.dockerignore` by default.
</success_criteria>

<output>
After completion, create `.planning/phases/122-deploy-scaffold-core-rewrite/122-06-SUMMARY.md`
</output>
