---
phase: 45-dx-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-mcp/src/tools/list_commands.rs
  - docs/src/reference/cli.md
autonomous: true
---

<objective>
Update MCP list_commands tool and CLI reference documentation to include all commands added since v2.2.

Purpose: AI agents using MCP cannot discover 7 missing commands, and developers reading CLI docs miss 2 commands. The framework appears incomplete when it isn't.
Output: Complete MCP command list and complete CLI reference documentation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-dx-polish/CONTEXT.md

@ferro-mcp/src/tools/list_commands.rs
@ferro-cli/src/main.rs
@docs/src/reference/cli.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 7 missing commands to MCP list_commands</name>
  <files>ferro-mcp/src/tools/list_commands.rs</files>
  <action>
Add the following 7 commands to the static command list in `execute()`, placing them in logical order among existing entries:

1. `make:auth` — "Scaffold complete authentication system (migration, controller, routes, middleware)"
   Place after `make:action` (alphabetical within make: group)

2. `make:json-view` — "Generate a JSON-UI view using AI (requires ANTHROPIC_API_KEY)"
   Place after `make:inertia`

3. `make:resource` — "Generate an API resource for model-to-response transformation"
   Place after `make:policy`

4. `db:seed` — "Run database seeders"
   Place after `db:rollback`

5. `do:init` — "Initialize DigitalOcean App Platform deployment configuration"
   Place after `docker:init`

6. `claude:install` — "Install Claude Code skill files for AI-assisted development"
   Place after `boost:install`

7. `validate:contracts` — "Validate API contracts between Rust types and TypeScript"
   Place after `storage:link`

Maintain the existing code style (same struct construction pattern). Keep descriptions concise and action-oriented.
  </action>
  <verify>cargo build -p ferro-mcp && cargo clippy -p ferro-mcp -- -D warnings</verify>
  <done>MCP list_commands returns all 39 CLI commands. No command is missing from the static list.</done>
</task>

<task type="auto">
  <name>Task 2: Add make:auth and make:resource to CLI reference docs</name>
  <files>docs/src/reference/cli.md</files>
  <action>
Add documentation sections for the 2 missing make: commands. Follow the existing documentation pattern in cli.md (heading, description, usage example, options table, generated files description).

**make:auth** — Add after the `make:action` section (or wherever alphabetical order places it in the make: commands):
- Description: Scaffolds a complete authentication system
- Usage: `ferro make:auth`
- What it generates: migration adding auth fields to users table, auth controller with register/login/logout, route registration instructions, middleware setup instructions
- Note: Uses ALTER TABLE approach (users table likely exists), outputs instructional text for routes/providers

**make:resource** — Add after `make:policy` section:
- Description: Generate an API resource for transforming models to JSON responses
- Usage: `ferro make:resource UserResource` or `ferro make:resource UserResource --model users`
- Options: `--model <name>` to auto-generate From<Model> implementation
- What it generates: Resource struct with #[derive(ApiResource)] and field attributes
- Note: Auto-appends "Resource" suffix if not present

Read the existing make:controller and make:scaffold sections for format reference. Match their level of detail.
  </action>
  <verify>Check that docs/src/reference/cli.md contains sections for both `make:auth` and `make:resource`. Verify markdown formatting is valid.</verify>
  <done>CLI reference docs cover all make: commands. make:auth and make:resource documented with usage, options, and generated file descriptions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo build -p ferro-mcp` succeeds
- [ ] `cargo clippy -p ferro-mcp -- -D warnings` passes
- [ ] `cargo fmt --check` passes
- [ ] MCP list_commands includes all 39 CLI commands
- [ ] docs/src/reference/cli.md covers make:auth and make:resource
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- AI agents can discover all CLI commands via MCP
- Developers find all make: commands in CLI reference
</success_criteria>

<output>
After completion, create `.planning/phases/45-dx-polish/45-02-SUMMARY.md`
</output>
