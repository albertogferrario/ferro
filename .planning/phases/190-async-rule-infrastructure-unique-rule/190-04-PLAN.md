---
phase: 190-async-rule-infrastructure-unique-rule
plan: 04
type: execute
wave: 4
depends_on: [190-01, 190-02, 190-03]
files_modified:
  - framework/src/validation/mod.rs
  - framework/src/lib.rs
  - framework/tests/async_validation_integration.rs
autonomous: true
requirements: [VALID-01, VALID-02, VALID-03]
must_haves:
  truths:
    - "AsyncRule, AsyncValidator, AsyncValidationError, and unique are public on the crate root (ferro_rs::...)."
    - "An end-to-end test exercises the full handler path: req-style data → AsyncValidator → unique → Validation error → with_old_input flow."
    - "The whole workspace passes fmt + clippy (-D warnings) + tests."
  artifacts:
    - path: "framework/src/lib.rs"
      provides: "crate-root re-exports of the async validation surface"
      contains: "AsyncValidator"
    - path: "framework/tests/async_validation_integration.rs"
      provides: "end-to-end async validation test (unique + exclude-self + redirect-back shape)"
      contains: "AsyncValidator"
  key_links:
    - from: "framework/src/lib.rs"
      to: "validation::{AsyncRule, AsyncValidator, AsyncValidationError, unique}"
      via: "pub use re-export"
      pattern: "AsyncValidator"
    - from: "framework/src/validation/mod.rs"
      to: "async_rule / async_validator / rules_async"
      via: "pub use re-exports"
      pattern: "pub use async_validator::"
---

<objective>
Wire the public surface and prove the whole thing works end-to-end. Add the
`pub use` re-exports for the three new modules to `validation/mod.rs` and the
crate-root re-exports to `lib.rs`, then add an integration test that drives the
full proactive-uniqueness path the way a handler would, and run the complete
workspace quality gate.

Purpose: makes VALID-01/02/03 reachable by consumers as `ferro_rs::unique`,
`ferro_rs::AsyncValidator`, etc., and verifies the assembled behavior against a
real in-memory SQLite DB.
Output: updated `mod.rs` + `lib.rs` re-exports;
`framework/tests/async_validation_integration.rs`; green fmt/clippy/test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/190-async-rule-infrastructure-unique-rule/190-RESEARCH.md
@.planning/phases/190-async-rule-infrastructure-unique-rule/190-PATTERNS.md

<interfaces>
<!-- mod.rs existing pub-use block (framework/src/validation/mod.rs:60-66). Append the new lines here. -->
```rust
pub use rule::Rule;
pub use rules::*;
pub use validator::{validate, Validator};
// ADD:
pub use async_rule::AsyncRule;
pub use async_validator::{AsyncValidationError, AsyncValidator};
pub use rules_async::unique;
```

<!-- lib.rs validation re-export block (framework/src/lib.rs:286-320). Insert alphabetically inside the existing `pub use validation::{ ... }`. -->
```rust
pub use validation::{
    accepted, alpha, /* ... */ required, required_if, same, string, url, validate,
    Rule, TranslatorFn, Validatable, ValidationError, Validator,
};
// ADD (alphabetical): AsyncRule, AsyncValidationError, AsyncValidator before Rule; unique after url.
```

<!-- Public API now available: -->
ferro_rs::{AsyncRule, AsyncValidator, AsyncValidationError, unique, ValidationError}
AsyncValidator::new(&data).rules(...).async_rule("slug", unique("widgets","slug").ignore(id)).validate_async().await
-> Result<(), AsyncValidationError>  // Validation(ValidationError) | Infra(FrameworkError)

<!-- Shared fixture (Plan 01). -->
init_test_db().await; seed_widget(id, slug).await;  // widgets(id, slug)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Public re-exports (mod.rs + lib.rs)</name>
  <files>framework/src/validation/mod.rs, framework/src/lib.rs</files>
  <read_first>
    - framework/src/validation/mod.rs (lines 53-66 — mod declarations already added by Plans 01/02/03; the pub-use block to extend)
    - framework/src/lib.rs (lines 285-320 — the `pub use validation::{ ... }` block)
    - 190-RESEARCH.md § "Module Export Checklist"
    - 190-PATTERNS.md § "framework/src/validation/mod.rs" and § "framework/src/lib.rs"
  </read_first>
  <action>
In `framework/src/validation/mod.rs`, the three `mod async_rule;`,
`mod rules_async;`, `mod async_validator;` lines already exist (added by Plans
01-03). Append to the `pub use` block (after `pub use validator::{validate, Validator};`):

```rust
pub use async_rule::AsyncRule;
pub use async_validator::{AsyncValidationError, AsyncValidator};
pub use rules_async::unique;
```

In `framework/src/lib.rs`, inside the existing `pub use validation::{ ... }` block
(lines 286-320), add the four new names in alphabetical position to match the
block's existing ordering:
- `AsyncRule,`
- `AsyncValidationError,`
- `AsyncValidator,`
  (these three go before the existing `Rule,` line)
- `unique,`
  (after the existing `url,` line)

Do not reorder existing entries; just insert the four new ones in their
alphabetical slots.
  </action>
  <verify>
    <automated>grep -q 'pub use async_validator::{AsyncValidationError, AsyncValidator};' framework/src/validation/mod.rs && grep -q 'pub use rules_async::unique;' framework/src/validation/mod.rs && grep -q 'AsyncValidator,' framework/src/lib.rs && grep -q 'unique,' framework/src/lib.rs && cargo check -p ferro-rs --lib</automated>
  </verify>
  <acceptance_criteria>
    - `framework/src/validation/mod.rs` contains `pub use async_rule::AsyncRule;`, `pub use async_validator::{AsyncValidationError, AsyncValidator};`, `pub use rules_async::unique;`
    - `framework/src/lib.rs` `pub use validation::{...}` block contains `AsyncRule`, `AsyncValidationError`, `AsyncValidator`, and `unique`
    - `cargo check -p ferro-rs --lib` exits 0
  </acceptance_criteria>
  <done>The async validation surface is public at the crate root; `ferro_rs::{AsyncRule, AsyncValidator, AsyncValidationError, unique}` all resolve.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end integration test + full quality gate</name>
  <files>framework/tests/async_validation_integration.rs</files>
  <read_first>
    - framework/tests/async_rule_fixture.rs (Plan 01 — init_test_db / seed_widget)
    - framework/src/validation/error.rs (with_old_input / into_action_error / redirect_back — to assert the redirect-back shape)
    - 190-VALIDATION.md § "Sampling Rate" and § "Validation Sign-Off"
    - 190-RESEARCH.md § "Pattern 4" usage example (the #[action] handler shape)
  </read_first>
  <action>
Create `framework/tests/async_validation_integration.rs` driving the full path as
a handler would. Use the crate-root public API only (`ferro_rs::{AsyncValidator,
AsyncValidationError, unique, required, ...}`) to prove the public surface is
sufficient. Include the shared fixture (`#[path = "async_rule_fixture.rs"] mod
fixture;` or the project's `tests/` sharing convention confirmed in Plan 01).

Tests (all `#[tokio::test] #[serial]` since they use the DB singleton):

1. `duplicate_value_is_validation_error` — init_test_db, seed_widget(1,"taken");
   build `AsyncValidator::new(&json!({"slug":"taken"}))`
   `.rules("slug", rules![required(), string()])`
   `.async_rule("slug", unique("widgets","slug"))`
   `.validate_async().await`; assert `Err(AsyncValidationError::Validation(e))`
   and that `e` has an error for "slug" (SC1: field error, not a panic/500).

2. `free_value_passes` — same setup, value "free" → `Ok(())`.

3. `exclude_self_passes_on_edit` — seed_widget(1,"taken");
   `.async_rule("slug", unique("widgets","slug").ignore(1_i64))` with value
   "taken" → `Ok(())` (SC2: unchanged own value passes).

4. `sync_failure_skips_async` — value "" (empty) with `required()` sync +
   `unique(...)` async; assert `Err(Validation)` and that the error for "slug" is
   the required message (async did not overwrite / run). (SC3 at the public-API
   level.)

5. `redirect_back_shape` — take the `Validation(e)` from test 1 and call
   `e.with_old_input(&data).into_action_error("/widgets/create")` (or
   `redirect_back`); assert it produces the expected ActionError/redirect type
   (reuses the existing flow — VALID-03). Keep this assertion at the type/shape
   level that the existing error API supports; do not spin up an HTTP server.

Then run the full workspace quality gate and make it green:
- `cargo fmt --all -- --check`
- `cargo clippy --all --all-targets -- -D warnings`
- `cargo test --all-features`

If the disk-full ENOSPC issue appears during `cargo test --all-features` (known
recurring flake — see memory `project_ferro_disk_full_test_gate`), check `df` and
clean `target/` before re-running; it is not a real defect.
  </action>
  <verify>
    <automated>test -f framework/tests/async_validation_integration.rs && grep -q 'AsyncValidator' framework/tests/async_validation_integration.rs && grep -q 'with_old_input' framework/tests/async_validation_integration.rs && cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test -p ferro-rs --test async_validation_integration 2>&1 | grep -qE 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - `framework/tests/async_validation_integration.rs` exists, uses only crate-root public API, and contains tests for: duplicate→Validation, free→Ok, exclude-self→Ok, sync-failure-skips-async, redirect-back shape
    - `cargo fmt --all -- --check` passes
    - `cargo clippy --all --all-targets -- -D warnings` passes
    - `cargo test -p ferro-rs --test async_validation_integration` passes; `cargo test --all-features` green (modulo known ENOSPC flake)
  </acceptance_criteria>
  <done>The full proactive-uniqueness path works through the public API end-to-end (duplicate, free, exclude-self, sync-first, redirect-back), and the entire workspace passes fmt + clippy(-D warnings) + tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer code → public async validation API | The newly public surface is the contract consumers build against; it must expose the safe path (bound params, infra/validation split) and nothing that bypasses the guards. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-190-01 | Tampering (SQL identifier injection) | public `unique` re-export | mitigate (verified) | The public `unique` carries the Plan 02 identifier guard; this plan adds no bypass. The integration test exercises only the guarded path. |
| T-190-05 | Elevation / API misuse (consumer accidentally treats Infra as Validation) | public `AsyncValidationError` | mitigate | The enum is public with documented variants and a `From<AsyncValidationError> for ActionError` that routes Infra → 500; the integration test asserts the redirect-back path uses `Validation` only. |
</threat_model>

<verification>
- `cargo check -p ferro-rs --lib` green after re-exports.
- `cargo fmt --all -- --check` + `cargo clippy --all --all-targets -- -D warnings` + `cargo test --all-features` all green (the project commit gate).
- Public names resolve: `ferro_rs::AsyncValidator`, `ferro_rs::unique`, `ferro_rs::AsyncValidationError`, `ferro_rs::AsyncRule`.
</verification>

<success_criteria>
- VALID-01/02/03 reachable via the crate-root public API and proven end-to-end.
- SC1 (duplicate→field error), SC2 (exclude-self passes), SC3 (sync-first), SC4 (sync API unchanged — no sync file modified across the phase), SC5 (DB via singleton) all demonstrated.
- Workspace quality gate green.
</success_criteria>

<output>
After completion, create `.planning/phases/190-async-rule-infrastructure-unique-rule/190-04-SUMMARY.md`
</output>
