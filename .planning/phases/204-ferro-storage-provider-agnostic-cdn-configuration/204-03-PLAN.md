---
phase: 204-ferro-storage-provider-agnostic-cdn-configuration
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - Cargo.toml
  - ferro-storage/CHANGELOG.md
  - app/.env.example
  - docs/src/features/storage.md
autonomous: true
requirements: [SC-6, SC-7]
must_haves:
  truths:
    - "The ferro-storage (workspace) version is bumped one minor to 0.2.53"
    - "ferro-storage/CHANGELOG.md exists with a ## [0.2.53] entry documenting the quartet, the per-var deprecation mapping, and the one-release removal policy"
    - "app/.env.example CDN section uses the quartet as primary with a comment marking the old names as deprecated fallbacks"
    - "docs/src/features/storage.md CDN section documents the quartet as primary with the legacy vars listed as deprecated"
    - "bunny/cloudflare remain non-default features (not added to default)"
    - "cargo test --all-features and cargo clippy --all --all-targets -- -D warnings both pass on the whole workspace"
  artifacts:
    - path: "ferro-storage/CHANGELOG.md"
      provides: "0.2.53 changelog entry with deprecation table"
      contains: "## [0.2.53]"
    - path: "Cargo.toml"
      provides: "workspace version 0.2.53"
      contains: "0.2.53"
    - path: "app/.env.example"
      provides: "quartet CDN section with deprecation comment"
      contains: "CDN_PROVIDER"
    - path: "docs/src/features/storage.md"
      provides: "updated CDN env-var documentation"
      contains: "CDN_PROVIDER"
  key_links:
    - from: "workspace Cargo.toml version"
      to: "ferro-storage/Cargo.toml version.workspace = true"
      via: "minor bump 0.2.52 → 0.2.53"
      pattern: "0.2.53"
    - from: "CHANGELOG deprecation table"
      to: "the eight legacy env vars"
      via: "deprecated→replacement mapping"
      pattern: "AWS_CDN_URL"
---

<objective>
Document and version the unified CDN surface, then run the BLOCKING full-workspace gate.

Purpose: ship the refactor as a clean minor release with operator-facing migration guidance
(CHANGELOG deprecation table, quartet `.env.example`, updated docs) and a green
`--all-features` + `-D warnings` workspace gate. Per D-06 and SC-6/SC-7.

Output: workspace `Cargo.toml` at `0.2.53`; new `ferro-storage/CHANGELOG.md`; migrated
`app/.env.example`; updated `docs/src/features/storage.md`; the SC-7 gate green.

This plan depends on Plans 01 and 02 (the code + parity tests must exist for the gate to pass).
It touches docs/manifest/env files only — no overlap with the source files of Plans 01/02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/204-ferro-storage-provider-agnostic-cdn-configuration/204-CONTEXT.md
@.planning/phases/204-ferro-storage-provider-agnostic-cdn-configuration/204-PATTERNS.md
@.planning/phases/204-ferro-storage-provider-agnostic-cdn-configuration/204-RESEARCH.md
@.planning/phases/204-ferro-storage-provider-agnostic-cdn-configuration/204-01-SUMMARY.md
@.planning/phases/204-ferro-storage-provider-agnostic-cdn-configuration/204-02-SUMMARY.md

<interfaces>
<!-- Workspace version source of truth: root Cargo.toml line ~38. ferro-storage uses version.workspace = true. -->
Current: `version = "0.2.52"`  →  Target: `version = "0.2.53"`

<!-- ferro-storage features (ferro-storage/Cargo.toml) — MUST stay non-default: -->
```toml
[features]
default = []
s3 = ["aws-sdk-s3", "aws-config", "aws-credential-types"]
s3-tests = ["s3"]
cdn-bunny = []
cdn-cloudflare = []
```

<!-- CHANGELOG header/entry structure to model: ferro-stripe/CHANGELOG.md. -->
<!-- app/.env.example current CDN section: lines 79-91 (replace). -->
<!-- docs/src/features/storage.md CDN section: starts at "## CDN" (~line 377); env-var listing ~384-386. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump workspace version to 0.2.53 and create ferro-storage/CHANGELOG.md</name>
  <read_first>
    - Cargo.toml (root) line ~38 — confirm `version = "0.2.52"`
    - ferro-storage/Cargo.toml lines 1-32 — confirm `version.workspace = true` and the feature block (do NOT add bunny/cloudflare to default)
    - ferro-stripe/CHANGELOG.md lines 1-32 — copy the header + entry structure
    - 204-PATTERNS.md §"ferro-storage/CHANGELOG.md — create new file" (the full 0.2.53 content to write)
  </read_first>
  <files>Cargo.toml, ferro-storage/CHANGELOG.md</files>
  <action>
    1. In the root workspace `Cargo.toml`, change `version = "0.2.52"` to `version = "0.2.53"` (the single `[workspace.package]` version line ~38). Do not touch any other version. ferro-storage inherits via `version.workspace = true`. Do NOT add `cdn-bunny`/`cdn-cloudflare` to `default` — they must stay opt-in (D-04; preserves the Phase 188 `cargo tree` zero-default-graph-impact proof).

    2. Create `ferro-storage/CHANGELOG.md` with the exact content from 204-PATTERNS.md §"ferro-storage/CHANGELOG.md — create new file". It must contain:
       - the `# Changelog` header + Keep-a-Changelog line referencing `ferro-storage`,
       - a `## [0.2.53] - 2026-06-11` entry,
       - `### Added` listing `cdn::Config`, `CdnProvider`, `Config::from_env()`, `Config::build_purge_api()`,
       - `### Changed` noting `StorageConfig::from_env` now sources the CDN URL from `cdn::Config`,
       - `### Deprecated` with the full deprecated→replacement table (all eight legacy vars: `AWS_CDN_URL`, `BUNNY_CDN_URL`, `CF_CDN_URL`, `DO_SPACES_CDN_ID`, `CF_ZONE_ID`, `DIGITALOCEAN_ACCESS_TOKEN`, `CF_API_TOKEN`, `BUNNY_ACCESS_KEY`) and a one-release removal policy sentence,
       - `### Notes` recording SC-3 and SC-4 parity.
  </action>
  <verify>
    <automated>grep -q '0.2.53' Cargo.toml && grep -q '## \[0.2.53\]' ferro-storage/CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'version = "0.2.53"' Cargo.toml` exits 0
    - `! grep -q 'version = "0.2.52"' Cargo.toml` (old version gone)
    - `test -f ferro-storage/CHANGELOG.md` exits 0
    - `grep -q '## \[0.2.53\]' ferro-storage/CHANGELOG.md` exits 0
    - `grep -q 'AWS_CDN_URL' ferro-storage/CHANGELOG.md` and `grep -q 'BUNNY_ACCESS_KEY' ferro-storage/CHANGELOG.md` both exit 0
    - `grep -q 'CDN_PURGE_TOKEN' ferro-storage/CHANGELOG.md` exits 0
    - `! grep -E 'default = \[.*cdn-(bunny|cloudflare)' ferro-storage/Cargo.toml` (features stay non-default)
    - `cargo build -p ferro-storage` exits 0 (version change compiles)
  </acceptance_criteria>
  <done>Workspace version is 0.2.53; CHANGELOG.md documents the quartet + deprecation table + removal policy; bunny/cloudflare stay opt-in.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate app/.env.example and docs/src/features/storage.md to the quartet</name>
  <read_first>
    - app/.env.example lines 79-91 (the current `# CDN Settings (ferro-storage — optional)` block to replace)
    - docs/src/features/storage.md lines 377-474 (the `## CDN` section; env-var listing ~384-386)
    - 204-RESEARCH.md §"app/.env.example current CDN section" + §"Fallback mapping (D-02) — complete table"
  </read_first>
  <files>app/.env.example, docs/src/features/storage.md</files>
  <action>
    1. **app/.env.example** — replace the current CDN block (lines ~79-91) with the quartet as primary plus a deprecation comment (D-06; supersedes the interim grouping from commit 77d360cf):
    ```env
    #-------------------------------------------------
    # CDN Settings (ferro-storage — optional)
    #-------------------------------------------------
    # Provider-agnostic CDN quartet (preferred).
    # CDN_PROVIDER: none | digitalocean | bunny | cloudflare
    CDN_URL=
    CDN_PROVIDER=none
    CDN_PURGE_TOKEN=
    CDN_PURGE_ZONE=
    #
    # Deprecated fallbacks (read for one release, emit a warning; migrate to the quartet above):
    #   AWS_CDN_URL / BUNNY_CDN_URL / CF_CDN_URL          → CDN_URL
    #   DO_SPACES_CDN_ID / CF_ZONE_ID                     → CDN_PURGE_ZONE
    #   DIGITALOCEAN_ACCESS_TOKEN / CF_API_TOKEN / BUNNY_ACCESS_KEY → CDN_PURGE_TOKEN
    ```
    `CDN_PROVIDER=none` is the sensible default (no purge unless configured). Remove the old `AWS_CDN_URL=`, `DIGITALOCEAN_ACCESS_TOKEN=`, `DO_SPACES_CDN_ID=`, `BUNNY_CDN_URL=`, `BUNNY_ACCESS_KEY=`, `CF_CDN_URL=`, `CF_API_TOKEN=`, `CF_ZONE_ID=` lines (they live only in the deprecation comment now).

    2. **docs/src/features/storage.md** — in the `## CDN` section (~line 377), update the env-var listing so the quartet (`CDN_URL`, `CDN_PROVIDER`, `CDN_PURGE_TOKEN`, `CDN_PURGE_ZONE`) is documented as the primary surface, with the eight legacy vars listed as deprecated fallbacks (one-release window). Keep the existing purge-adapter prose unchanged — only the env-var naming changes. Match the existing doc style (neutral architectural voice, no marketing language).
  </action>
  <verify>
    <automated>grep -q 'CDN_PROVIDER' app/.env.example && grep -q 'CDN_PROVIDER' docs/src/features/storage.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'CDN_URL=' app/.env.example` exits 0
    - `grep -q 'CDN_PROVIDER=none' app/.env.example` exits 0
    - `grep -q 'CDN_PURGE_TOKEN=' app/.env.example` and `grep -q 'CDN_PURGE_ZONE=' app/.env.example` both exit 0
    - `grep -q 'Deprecated fallbacks' app/.env.example` exits 0
    - `! grep -qE '^AWS_CDN_URL=' app/.env.example` (old bare assignment removed; only in the comment)
    - `grep -q 'CDN_PROVIDER' docs/src/features/storage.md` exits 0
    - `grep -q 'CDN_PURGE_ZONE' docs/src/features/storage.md` exits 0
  </acceptance_criteria>
  <done>`.env.example` and storage docs present the quartet as primary with the legacy names marked deprecated.</done>
</task>

<task type="auto">
  <name>Task 3: BLOCKING — full-workspace gate (SC-7)</name>
  <read_first>
    - CLAUDE.md §"Testing & Linting (MUST run before every commit)" (the exact gate command)
    - 204-VALIDATION.md §"Test Infrastructure" (env-touching tests run serial)
    - MEMORY: `project_ferro_disk_full_test_gate` (check `df` / clean `target/` if ENOSPC) and `project_schema_export_test_dirties_tree` (`git checkout` regenerated schema files after the run; do not fold into this phase's commit)
  </read_first>
  <files></files>
  <action>
    Run the mandatory CLAUDE.md pre-commit gate, in this order, and ensure each is green before proceeding:

    1. `cargo fmt --all -- --check`
    2. `cargo clippy --all --all-targets -- -D warnings`
    3. `cargo test --all-features`

    Notes:
    - This is the SC-7 acceptance gate (`cargo test --all-features` + `cargo clippy --all -- -D warnings`). `--all-features` exercises the `cdn-bunny` + `cdn-cloudflare` arms of `build_purge_api`, so the `#[cfg(feature = ...)]` blocks AND their `#[cfg(not(...))]` counterparts must both compile clean (Pitfall 3 — `unreachable_code`).
    - Env-var-touching tests (the parity/fallback tests from Plans 01-02) can collide under the default parallel harness. If `cargo test --all-features` shows flaky CDN test failures, re-run the ferro-storage portion serially: `cargo test -p ferro-storage --all-features -- --test-threads=1`. The whole-workspace `--all-features` run should still pass; investigate any genuine (non-env-collision) failure.
    - Do NOT chain the three CPU-heavy commands in one shell line — run them one at a time (serialize CPU ops per project convention) and reuse the green result of each.
    - If `cargo test --all-features` fails with ENOSPC / link-fingerprint errors (not real defects), check `df` and clean `target/`, then re-run.
    - After the test run, if `docs/protocol/schemas/*.json` are regenerated and dirty, `git checkout` them — they are not part of this phase's change set.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy --all --all-targets -- -D warnings` exits 0
    - `cargo test --all-features` exits 0 (re-run ferro-storage with `--test-threads=1` only if env-collision flakiness appears; the underlying assertions must pass)
    - `cargo tree --all-features -e features` shows `cdn-bunny`/`cdn-cloudflare` are not pulled into the default graph (they are opt-in; confirm `default = []` still holds in ferro-storage/Cargo.toml)
  </acceptance_criteria>
  <done>fmt + clippy(-D warnings) + test(--all-features) all green on the full workspace; bunny/cloudflare remain opt-in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → public | `.env.example`, CHANGELOG, and docs are public artifacts; must not leak real credentials or app identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-204-TOKEN-REDACT | Information Disclosure | `.env.example`, CHANGELOG, docs | mitigate | env files use empty assignments (`CDN_PURGE_TOKEN=`) and the docs/CHANGELOG name vars only — no real token values committed |
| T-204-MISCONFIG | Tampering / Availability | SC-7 gate | mitigate | `--all-features` gate compiles and runs both the feature-on and feature-off `build_purge_api` arms, catching a fail-loud regression before release |
| T-204-DEPRECATION-LEAK | Information Disclosure | CHANGELOG deprecation table | accept | the table names deprecated vars only (no values) — informational, no secret surface |
</threat_model>

<verification>
- Workspace builds at 0.2.53; `ferro-storage/CHANGELOG.md` has the `## [0.2.53]` entry with the deprecation table.
- `.env.example` and `docs/src/features/storage.md` lead with the quartet.
- `cargo fmt --all -- --check`, `cargo clippy --all --all-targets -- -D warnings`, `cargo test --all-features` all green.
- `cdn-bunny`/`cdn-cloudflare` remain non-default.
</verification>

<success_criteria>
- SC-6: minor version bump (0.2.53) + CHANGELOG `## [0.2.53]` documenting the quartet + deprecation policy — file checks green.
- SC-7: `cargo test --all-features` + `cargo clippy --all --all-targets -- -D warnings` pass on the workspace.
</success_criteria>

<output>
After completion, create `.planning/phases/204-ferro-storage-provider-agnostic-cdn-configuration/204-03-SUMMARY.md`
</output>
