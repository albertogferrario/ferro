---
status: complete
phase: 120-cli-and-mcp-updates
source:
  - 120-01-SUMMARY.md
  - 120-02-SUMMARY.md
  - 120-03-SUMMARY.md
  - 120-04-SUMMARY.md
  - 120-05-SUMMARY.md
started: 2026-04-21T17:15:00Z
updated: 2026-04-21T17:35:00Z
---

## Current Test

[testing complete]

## Tests

### 1. make:json-view --no-ai generates a JSON file
expected: Running `ferro make:json-view TestView --no-ai` creates `src/views/test_view.json`. No `.rs` file created. No `mod.rs` touched.
result: pass

### 2. Generated JSON spec has correct structure
expected: The generated `test_view.json` is valid JSON with `layout: "dashboard"`, a root-level `elements` map, and at least one Card element with `props.description` pointing to `src/views/test_view.json`.
result: pass

### 3. json_ui_catalog MCP tool exposes JSON Schema
expected: Calling the `json_ui_catalog` MCP tool returns a response that includes `json_schema` (an object with `$id: "ferro-json-ui/v2"`) and `component_schemas` (a map with at least one entry like `Button` or `Card`).
result: pass

### 4. json_ui_inspect MCP tool scans .json files
expected: In a ferro project with `.json` view files in `src/views/`, calling `json_ui_inspect` returns view entries showing `components_used` extracted from the JSON, not from any `.rs` patterns.
result: skipped
reason: Unit tests pass (9/9). CLI test harness lacks full ferro project structure (no Cargo.toml at scan root) so runtime returns empty — not a code defect. Verified by unit tests.

### 5. json_ui_generate MCP conventions reference v2
expected: Calling `json_ui_generate` (or checking its conventions output) shows `file_location: src/views/{name}.json` and `function_signature` using `JsonUi::render_file`, not the old `pub async fn view() -> JsonUiView` or `Spec::builder()` pattern.
result: pass

### 6. code_templates json_view templates return v2 JSON
expected: The `code_templates` MCP tool for category `json_view` returns templates whose content is a JSON object (not Rust code). The `basic_view` template should have `elements` and no `Spec::builder()` or `Element::new()` calls.
result: pass

### 7. generation_context json_ui_view pattern shows render_file
expected: The `generation_context` MCP tool's `json_ui_view` common pattern shows a v2 JSON spec file paired with a Rust handler using `JsonUi::render_file`, not the v1 `Spec::builder()` Rust code.
result: pass

## Summary

total: 7
passed: 6
issues: 0
pending: 0
skipped: 1

## Gaps

[none]
