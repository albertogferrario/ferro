---
phase: 182-ferro-json-ui-data-lazy-hero-runtime-primitive
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/json-ui/runtime-primitives.md
  - docs/src/SUMMARY.md
autonomous: true
requirements: [LAZYHERO-02, LAZYHERO-03]
must_haves:
  truths:
    - "docs/src/json-ui/runtime-primitives.md exists and documents the data-lazy-hero family (data-lazy-hero, data-lazy-hero-margin, data-lazy-hero-promoted)"
    - "docs/src/SUMMARY.md registers the new page in the JSON-UI section so mdbook renders it"
    - "The docs page reads as neutral architectural documentation — no internal-strategy voice, no phase references, no tenant names"
    - "The docs page states the browser-support / no-op behavior for browsers without IntersectionObserver"
    - "The docs page states the one-shot at DOMContentLoaded behavior (forward-compat note)"
  artifacts:
    - path: "docs/src/json-ui/runtime-primitives.md"
      provides: "Public-contract documentation for ferro-json-ui consumer-set DOM attributes"
      min_lines: 40
      contains: "data-lazy-hero"
    - path: "docs/src/SUMMARY.md"
      provides: "mdbook TOC entry for runtime-primitives.md"
      contains: "json-ui/runtime-primitives.md"
  key_links:
    - from: "docs/src/SUMMARY.md"
      to: "docs/src/json-ui/runtime-primitives.md"
      via: "mdbook link inside the JSON-UI section"
      pattern: "json-ui/runtime-primitives\\.md"
---

<objective>
Create the new docs page `docs/src/json-ui/runtime-primitives.md` documenting the public DOM-attribute contract for ferro-json-ui runtime primitives, and register it in `docs/src/SUMMARY.md` so mdbook includes it in the rendered book. This is the first ferro-json-ui docs page that documents the JS-runtime side as a public surface — `data-lazy-hero` is the first runtime attribute that tenant HTML sets directly (not output from a Rust component).

Purpose: Per project CLAUDE.md "Always update docs when framework changes" and CONTEXT.md D-09, the new attribute family is a public ferro contract and must be documented at the same time the runtime primitive ships. Without the SUMMARY.md edit (RESEARCH Pitfall 7), the page will not surface in the rendered mdbook output even if the file exists.

Output: One new markdown page + one one-line addition to SUMMARY.md. No code changes; no test impact. The page is voiced as steady-state public-contract documentation — no internal-strategy framing, no references to Phase 182, no references to gestiscilo or jetskiadriatic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/182-ferro-json-ui-data-lazy-hero-runtime-primitive/182-CONTEXT.md
@.planning/phases/182-ferro-json-ui-data-lazy-hero-runtime-primitive/182-RESEARCH.md
@docs/src/SUMMARY.md
@docs/src/json-ui/data-binding.md
@docs/src/json-ui/plugins.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/src/json-ui/runtime-primitives.md</name>
  <files>docs/src/json-ui/runtime-primitives.md</files>
  <read_first>
    - docs/src/json-ui/data-binding.md (voice/structure reference — scientific, minimalistic, neutral)
    - docs/src/json-ui/plugins.md (closest-topic sibling page — public-contract framing)
    - .planning/phases/182-ferro-json-ui-data-lazy-hero-runtime-primitive/182-CONTEXT.md D-09 (page scope and section list)
    - .planning/phases/182-ferro-json-ui-data-lazy-hero-runtime-primitive/182-RESEARCH.md §"Security Domain" (the "performance primitive, not a security primitive" framing the docs page must honor)
  </read_first>
  <action>
Create `docs/src/json-ui/runtime-primitives.md` with EXACTLY this content. The page is voiced as neutral architectural documentation — no phase references, no internal-strategy framing, no consumer/tenant names, no marketing language.

```markdown
# Runtime Primitives

ferro-json-ui ships a small JavaScript runtime that is inlined into every page rendered by `DefaultLayout` and `DashboardLayout`. Most of the runtime is internal: it powers behaviors emitted by components (popover menus, tabs, dismissible toasts, sidebar, modals) and the attributes those components use are implementation details.

A small subset of the runtime is a public contract: DOM attributes the runtime recognizes on hand-authored or component-output HTML. This page documents that subset.

## `data-lazy-hero`

Opts a `<video>` element into intersection-driven `preload` promotion. When the element approaches the viewport, the runtime sets `preload="auto"` and calls `<video>.load()` so the first frame is ready by the time the user reaches the element.

### Contract

| Attribute | Required | Default | Description |
|-----------|----------|---------|-------------|
| `data-lazy-hero` | yes | — | Opt-in marker. The element must also have `preload="none"`. |
| `data-lazy-hero-margin` | no | `200px 0px` | Per-element `rootMargin` for the IntersectionObserver. Any CSS-margin shorthand the IntersectionObserver constructor accepts. |
| `data-lazy-hero-promoted` | no (runtime sets it) | absent | Idempotency marker. The runtime sets this to `"1"` after promotion; re-running the primitive on the same element is a no-op. |

### Selector

The runtime matches `video[preload="none"][data-lazy-hero]:not([data-lazy-hero-promoted])`. Three consequences:

- `<video>` without `preload="none"` is ignored. The runtime does not override an author-tuned `preload` value.
- Non-`<video>` elements with `data-lazy-hero` are ignored. The promote action (flip `preload`, call `.load()`) is video-specific.
- Already-promoted elements are excluded by the `:not(...)` clause.

### Usage

```html
<!-- Above-the-fold: load eagerly -->
<video preload="auto" poster="/posters/hero-0.jpg" muted playsinline>
  <source src="/assets/hero-0.mp4" type="video/mp4">
</video>

<!-- Below-the-fold: lazy-promote on viewport approach (default 200px lead) -->
<video preload="none" data-lazy-hero poster="/posters/hero-1.jpg" muted playsinline>
  <source src="/assets/hero-1.mp4" type="video/mp4">
</video>

<!-- Below-the-fold with a larger lead time (slower-loading hero) -->
<video preload="none" data-lazy-hero data-lazy-hero-margin="400px 0px"
       poster="/posters/hero-2.jpg" muted playsinline>
  <source src="/assets/hero-2.mp4" type="video/mp4">
</video>
```

### Observer cardinality

Elements are grouped by their resolved `rootMargin` value. The runtime constructs one IntersectionObserver per distinct margin value, with all elements sharing that value fanned out to it. A page where every hero uses the default has exactly one observer; a page mixing defaults with one override has two observers.

### Browser support

The primitive depends on the `IntersectionObserver` API. On environments without it (rare; some legacy embedded WebViews, certain test harnesses with minimal DOM polyfills), the runtime silently no-ops and the videos behave exactly as authored.

### Lifecycle

The runtime scans the DOM once, when `DOMContentLoaded` fires. Elements inserted into the DOM after that point are not observed. Pages that render heroes server-side as part of the initial HTML — the intended use case — are unaffected.

### Performance, not access control

`data-lazy-hero` defers a fetch the browser would otherwise issue at page load. It does not prevent a fetch. Once the element approaches the viewport, the runtime initiates the fetch. Do not use this attribute to gate paid content or otherwise restrict resource access — that is an access-control concern, not a performance concern.
```

Voice rules followed:
- No mention of Phase 182, gestiscilo, jetskiadriatic, or any specific consumer.
- No "killer feature" framing, no marketing voice.
- Scientific and minimalistic per CLAUDE.md.
- Framed as steady-state contract, not as a new addition.
- The "Performance, not access control" subsection translates RESEARCH §"Security Domain" into docs-page voice (the warning a tenant author needs).
  </action>
  <verify>
    <automated>test -f docs/src/json-ui/runtime-primitives.md && wc -l docs/src/json-ui/runtime-primitives.md | awk '{ if ($1 < 40) exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/src/json-ui/runtime-primitives.md` exits 0
    - `wc -l docs/src/json-ui/runtime-primitives.md` reports at least 40 lines
    - `grep -c 'data-lazy-hero' docs/src/json-ui/runtime-primitives.md` returns at least 6 (mentioned across multiple sections + table + example)
    - `grep -c 'data-lazy-hero-margin' docs/src/json-ui/runtime-primitives.md` returns at least 3
    - `grep -c 'data-lazy-hero-promoted' docs/src/json-ui/runtime-primitives.md` returns at least 2
    - `grep -c 'IntersectionObserver' docs/src/json-ui/runtime-primitives.md` returns at least 2
    - `grep -c '200px 0px' docs/src/json-ui/runtime-primitives.md` returns at least 1
    - `grep '# Runtime Primitives' docs/src/json-ui/runtime-primitives.md` matches (top-level heading)
    - The file does NOT contain `Phase 182`, `gestiscilo`, `jetskiadriatic`, or `killer feature` (neutral voice — no internal-strategy framing)
    - `grep -iE 'performance, not access control|do not use.*to gate' docs/src/json-ui/runtime-primitives.md` matches (the performance-not-security framing required by RESEARCH §Security Domain)
  </acceptance_criteria>
  <done>
File exists at the correct path with all required sections (heading, contract table, selector, usage, observer cardinality, browser support, lifecycle, performance-not-access-control). Voice is neutral; no phase or tenant references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register runtime-primitives.md in docs/src/SUMMARY.md</name>
  <files>docs/src/SUMMARY.md</files>
  <read_first>
    - docs/src/SUMMARY.md (the JSON-UI section at lines 52-63 — insertion point is between Plugins and Spec construction)
    - .planning/phases/182-ferro-json-ui-data-lazy-hero-runtime-primitive/182-RESEARCH.md §"Pitfall 7" (SUMMARY.md miss → invisible page; this task closes that pitfall)
  </read_first>
  <action>
Apply one Edit to `docs/src/SUMMARY.md`. Use the Edit tool — DO NOT use Write.

Current text (lines 59-61):
```
- [Plugins](json-ui/plugins.md)
- [Spec construction](./json-ui/spec-construction.md)
- [Expressions](json-ui/expressions.md)
```

Replace with:
```
- [Plugins](json-ui/plugins.md)
- [Runtime Primitives](json-ui/runtime-primitives.md)
- [Spec construction](./json-ui/spec-construction.md)
- [Expressions](json-ui/expressions.md)
```

This places "Runtime Primitives" after "Plugins" and before "Spec construction" — keeps browser-side concerns grouped (Plugins are also browser-side rendering plumbing), and matches the placement recommended in RESEARCH.md Example 6.

After the edit, run a grep to confirm the registration is exactly one line:
```bash
grep -c "json-ui/runtime-primitives.md" docs/src/SUMMARY.md
```
The expected output is `1`.
  </action>
  <verify>
    <automated>grep -q "json-ui/runtime-primitives.md" docs/src/SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "json-ui/runtime-primitives.md" docs/src/SUMMARY.md` returns exactly 1
    - `grep -c "Runtime Primitives" docs/src/SUMMARY.md` returns exactly 1
    - The line `- [Runtime Primitives](json-ui/runtime-primitives.md)` appears immediately after the `- [Plugins](json-ui/plugins.md)` line and immediately before the `- [Spec construction](./json-ui/spec-construction.md)` line — verify with `grep -A 2 'Plugins.*json-ui/plugins.md' docs/src/SUMMARY.md` showing Plugins → Runtime Primitives → Spec construction in that order
    - `wc -l docs/src/SUMMARY.md` reports exactly 1 more line than the pre-edit count (i.e., 77 lines if the original was 76)
  </acceptance_criteria>
  <done>
The TOC includes the new page in the JSON-UI section between Plugins and Spec construction. mdbook will render the page; RESEARCH Pitfall 7 (page invisible due to missing registration) is closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Documentation files only. No code, no input parsing, no runtime execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-04 | Information Disclosure | docs page voice | mitigate | A docs page written in internal-strategy voice (referencing Phase numbers, tenant names, discovery context, killer-feature framing) would leak project-internal context to anyone reading the public mdbook output. The page is voiced as neutral architectural documentation per CLAUDE.md "Repository documents must read as neutral"; the acceptance criteria includes negative greps for `Phase 182`, `gestiscilo`, `jetskiadriatic`, `killer feature`. |
| T-182-05 | Tampering | docs guidance on `data-lazy-hero` semantics | mitigate | A docs page that implied `data-lazy-hero` prevents fetches (rather than defers them) could mislead a tenant author into using it for access control of paid video content. The "Performance, not access control" subsection (RESEARCH §Security Domain) explicitly disclaims this and is required by acceptance criteria. |

No auth, no authz, no user-supplied input, no executable code paths. The only "threat" is documentation voice — addressed by acceptance criteria.
</threat_model>

<verification>
- `docs/src/json-ui/runtime-primitives.md` exists with all required sections.
- `docs/src/SUMMARY.md` registers the new page in the JSON-UI section between Plugins and Spec construction.
- No internal-strategy voice; negative greps for `Phase 182`, `gestiscilo`, `jetskiadriatic`, `killer feature` all return zero.
- The "performance, not access control" framing is present (RESEARCH §Security Domain compliance).
- Local `mdbook build docs/` (if mdbook is available) emits no warning about an orphaned page. This is informational — CI runs mdbook on master.
</verification>

<success_criteria>
LAZYHERO-02 (per-element override documented) — the docs page explicitly documents `data-lazy-hero-margin` in the contract table and the usage example.
LAZYHERO-03 (idempotency contract documented) — the docs page documents `data-lazy-hero-promoted` as the runtime-set idempotency marker.
Page is reachable via the rendered mdbook output (SUMMARY.md registration closes RESEARCH Pitfall 7).
</success_criteria>

<output>
After completion, create `.planning/phases/182-ferro-json-ui-data-lazy-hero-runtime-primitive/182-02-SUMMARY.md` capturing: line count of the new page, grep counts confirming the attribute coverage, and the exact insertion point in SUMMARY.md.
</output>
