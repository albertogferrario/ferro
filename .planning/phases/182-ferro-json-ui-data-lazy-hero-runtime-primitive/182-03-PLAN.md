---
phase: 182-ferro-json-ui-data-lazy-hero-runtime-primitive
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - Cargo.toml
  - Cargo.lock
autonomous: true
requirements: [LAZYHERO-05]
must_haves:
  truths:
    - "Workspace package version in Cargo.toml is 0.2.42 (bumped from 0.2.41)"
    - "Cargo.lock reflects the new workspace version across all internal ferro-* crate entries"
    - "cargo build --workspace succeeds with the new version (no dependency-resolution drift)"
    - "cargo test --all-features remains green after the bump"
    - "The change is structurally ready to trigger Wave1A publish to crates.io on master push (ferro-json-ui is already in WAVE1A_CRATES; no workflow edit needed)"
  artifacts:
    - path: "Cargo.toml"
      provides: "workspace.package.version bump to 0.2.42"
      contains: "version = \"0.2.42\""
    - path: "Cargo.lock"
      provides: "lockfile sync to the new workspace version"
      contains: "version = \"0.2.42\""
  key_links:
    - from: "Cargo.toml workspace.package.version"
      to: "every ferro-* crate's Cargo.toml (version.workspace = true)"
      via: "Cargo workspace version inheritance"
      pattern: "version\\.workspace = true"
    - from: "Cargo.lock"
      to: "Cargo.toml workspace version"
      via: "cargo build --workspace updates Cargo.lock"
      pattern: "version = \"0\\.2\\.42\""
---

<objective>
Bump the workspace package version from `0.2.41` to `0.2.42` in `Cargo.toml` and sync `Cargo.lock` to match. This is the publish-trigger commit for Phase 182: when merged to master, the existing `.github/workflows/publish.yml` Wave1A job picks up the version bump and publishes `ferro-json-ui` (and every other Wave1A crate that has changes since the previous release) to crates.io.

Purpose: Phase 182's deliverable is "publish a runtime primitive." The runtime primitive itself lands in Plan 01; the docs page lands in Plan 02; this plan ships the version bump that the CI workflow keys on to push 0.2.42 to crates.io. Per CONTEXT.md D-08 and memory `feedback_friction_loop_release_cadence.md`, this is a single end-of-phase publish — gestiscilo Phase 186 consumes via Cargo.toml bump after this lands.

Output: Two-file diff (`Cargo.toml` workspace.package.version + `Cargo.lock` regenerated). No code changes, no docs changes. The commit that lands these two files together is the precedent established by commit `474f4490 chore: sync Cargo.lock to workspace version 0.2.41` (RESEARCH Pitfall 6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/182-ferro-json-ui-data-lazy-hero-runtime-primitive/182-CONTEXT.md
@.planning/phases/182-ferro-json-ui-data-lazy-hero-runtime-primitive/182-RESEARCH.md
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump workspace.package.version to 0.2.42 and sync Cargo.lock</name>
  <files>Cargo.toml, Cargo.lock</files>
  <read_first>
    - Cargo.toml (current workspace.package.version is `"0.2.41"` on line 33 — see the file head loaded above)
    - .planning/phases/182-ferro-json-ui-data-lazy-hero-runtime-primitive/182-CONTEXT.md D-08 (single-publish-at-end cadence)
    - .planning/phases/182-ferro-json-ui-data-lazy-hero-runtime-primitive/182-RESEARCH.md §"Pitfall 6" (Cargo.lock drift on version bump — precedent commit `474f4490`)
  </read_first>
  <action>
Two-step process. Use the Edit tool for the `Cargo.toml` change; use the Bash tool to regenerate `Cargo.lock`.

**Step 1 — Bump `Cargo.toml` workspace.package.version.**

Apply an Edit to `Cargo.toml`.

Current line 33:
```
version = "0.2.41"
```

Replace with:
```
version = "0.2.42"
```

Context (verify in the file the line you replace is inside the `[workspace.package]` block, not inside any individual crate's manifest — the workspace-root Cargo.toml has only one `version = "0.2.41"` line, at line 33, under `[workspace.package]`).

**Step 2 — Sync `Cargo.lock` via cargo build.**

Run:
```bash
cargo build --workspace
```

This causes cargo to update every `ferro-*` crate entry in `Cargo.lock` from `version = "0.2.41"` to `version = "0.2.42"` because each ferro-* crate's Cargo.toml uses `version.workspace = true` (inherits from the workspace). The pre-bump `Cargo.lock` contains 26 lines reading `version = "0.2.41"`; the post-bump `Cargo.lock` should contain at least 26 lines reading `version = "0.2.42"` (one per ferro-* workspace crate that publishes its workspace version), and zero `version = "0.2.41"` lines on any ferro-* crate name.

Do NOT manually edit `Cargo.lock` — cargo regenerates it from `Cargo.toml`. The role of the build is to invoke cargo's lockfile-update logic.

After the build completes, both files must be committed together so the publish workflow sees a coherent dependency graph (RESEARCH Pitfall 6 — partial commits cause CI lockfile-drift errors).
  </action>
  <verify>
    <automated>grep -c '^version = "0.2.42"$' Cargo.toml | grep -qx 1 && grep -c '^version = "0.2.41"$' Cargo.toml | grep -qx 0 && cargo build --workspace && cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'version = "0.2.42"' Cargo.toml` matches exactly 1 line, and that line is line 33 (the `[workspace.package]` version field)
    - `grep -c 'version = "0.2.41"' Cargo.toml` returns exactly 0
    - `grep -c 'version = "0.2.42"' Cargo.lock` returns at least 26 (matches the count of `0.2.41` lines in the pre-bump lockfile per RESEARCH-confirmed grep)
    - `grep -c 'version = "0.2.41"' Cargo.lock` returns 0 for ferro-* crate stanzas — specifically, `awk '/^name = "ferro/,/^$/' Cargo.lock | grep -c 'version = "0.2.41"'` returns 0 (defensive: external deps may legitimately use `0.2.41` and must not match)
    - `cargo build --workspace` exits 0
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy --all --all-targets -- -D warnings` exits 0
    - `cargo test --all-features` exits 0 (full workspace regression after the bump)
    - `cargo test -p ferro-json-ui --lib runtime::tests` exits 0 (Plan 01 deliverables still green after the bump)
    - `git status --porcelain Cargo.toml Cargo.lock` shows both files as modified (both must commit together per RESEARCH Pitfall 6 + commit 474f4490 precedent)
  </acceptance_criteria>
  <done>
Workspace version is 0.2.42 in both Cargo.toml and Cargo.lock; full workspace builds, lints, and tests cleanly. On merge to master the existing Wave1A publish workflow will detect ferro-json-ui changes (the runtime/hero_lazy.rs file added in Plan 01 + the version bump here) and push ferro-json-ui 0.2.42 to crates.io. No CI workflow file edit needed — `ferro-json-ui` is already in `WAVE1A_CRATES`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → crates.io | The version bump is the trigger for CI to publish to crates.io after master merge. The publish itself runs in a GH Actions environment with the publish token; no token handling in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-06 | Tampering | Cargo.lock drift | mitigate | Bumping `Cargo.toml` without regenerating `Cargo.lock` produces a CI failure when the publish workflow rebuilds the dependency graph. Task action mandates `cargo build --workspace` immediately after the Cargo.toml edit, and the acceptance criteria requires both files to appear in `git status` together. Precedent: commit `474f4490`. |
| T-182-07 | Elevation of Privilege | publish workflow trigger | accept | The version bump triggers a publish to crates.io. This is the intended effect; no privilege escalation in the sense of STRIDE. The publish token is held by CI, not the local environment; the local executor cannot directly publish — only by merging to master can the publish workflow run, and master push is a separate authenticated action outside this plan. |

No new auth surface, no new input parsing, no new untrusted data flow. The only effect of this plan is a lockfile sync and a version-string flip — the publish itself is a downstream CI artifact.
</threat_model>

<verification>
- `Cargo.toml` line 33 reads `version = "0.2.42"`.
- `Cargo.lock` has zero references to `0.2.41` in any ferro-* crate stanza; at least 26 references to `0.2.42` in ferro-* crate stanzas.
- `cargo build --workspace`, `cargo fmt --all -- --check`, `cargo clippy --all --all-targets -- -D warnings`, `cargo test --all-features` all pass.
- The combined two-file diff is structurally identical to commit `474f4490` shape (Cargo.toml + Cargo.lock + nothing else).
- LAZYHERO-05 (publish via Wave1A) is verified post-merge by checking the GH Actions run and `cargo search ferro-json-ui`. Out of this plan's local scope.
</verification>

<success_criteria>
LAZYHERO-05 (ferro-json-ui publishes 0.2.42 via existing Wave1A workflow) — the version bump is in place; the publish itself is a CI artifact that runs on master push. No workflow edit needed because `ferro-json-ui` is already in `WAVE1A_CRATES` and the library-change gate in `publish.yml` lists `ferro-json-ui/**` as a publishable path; the runtime/hero_lazy.rs file added in Plan 01 qualifies the change as publishable.
</success_criteria>

<output>
After completion, create `.planning/phases/182-ferro-json-ui-data-lazy-hero-runtime-primitive/182-03-SUMMARY.md` capturing: the exact byte-diff in Cargo.toml (one-line change at line 33), the line-count delta in Cargo.lock (number of `0.2.41` → `0.2.42` replacements actually applied), `cargo build --workspace` time, `cargo test --all-features` pass output summary, and a note that on master push the existing Wave1A workflow handles the publish.
</output>
