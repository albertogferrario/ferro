---
phase: 182-ferro-json-ui-data-lazy-hero-runtime-primitive
plan: 03
subsystem: workspace-release
tags: [chore, version-bump, cargo-lock-sync, wave1a-publish-trigger]
requires:
  - "Cargo.toml workspace.package.version at 0.2.41 (post-Phase 181 baseline)"
  - "Plan 01 + 02 merged into the worktree base (b623746ed7c161cd0dc9bd281d17e10e728840d1)"
provides:
  - "Cargo.toml workspace.package.version = 0.2.42 (single bump)"
  - "Cargo.lock fully synced to 0.2.42 across every workspace-inheriting ferro-* stanza"
  - "Structural readiness for Wave1A publish of ferro-json-ui 0.2.42 on master push"
affects:
  - "Every ferro-* crate that uses `version.workspace = true` (all crates except ferro-stripe which independently pins 0.5.0)"
  - ".github/workflows/publish.yml WAVE1A_CRATES path-filter pickup on master merge"
tech-stack:
  patterns:
    - "Workspace version inheritance via `version.workspace = true`"
    - "Single-publish-at-end-of-phase release cadence (D-08, memory feedback_friction_loop_release_cadence.md)"
    - "Two-file Cargo.toml + Cargo.lock atomic commit shape (precedent: commit 474f4490)"
key-files:
  modified:
    - path: "Cargo.toml"
      change: "Line 33 workspace.package.version flipped from \"0.2.41\" to \"0.2.42\""
    - path: "Cargo.lock"
      change: "25 ferro-* + workspace-inheriting stanzas regenerated by `cargo build --workspace`"
decisions:
  - "Verification ran the full plan-mandated gate (cargo fmt --check + cargo clippy --all --all-targets -D warnings + cargo test --all-features) sequentially; not collapsed to lighter form despite version-only change, because the plan's <verify> block explicitly required it."
  - "Acceptance-criterion line-count expectation of '>=26' for 0.2.42 references in Cargo.lock was met as exactly 25 (perfect 1:1 swap with the pre-bump 25 references to 0.2.41). The plan's '26' figure was a minor research estimate (the ferro-stripe crate independently pins 0.5.0 and does not participate in workspace-version inheritance, so the count is 25 of 26 publishable workspace crates). Substantive contract — zero remaining ferro-* references to the old version — is fully satisfied."
metrics:
  duration: "~11 minutes (657s)"
  completed: "2026-06-06T13:34:09Z"
  tasks: 1
  files_modified: 2
  commit: "7c64996e"
---

# Phase 182 Plan 03: Workspace Version Bump and Cargo.lock Sync — Summary

## One-liner

Workspace bumped to 0.2.42 (Cargo.toml + Cargo.lock) — Wave1A publish trigger for ferro-json-ui's data-lazy-hero runtime primitive.

## Outcome

Cargo.toml workspace.package.version is now `0.2.42`. Cargo.lock has zero remaining references to `0.2.41` and 25 references to `0.2.42` (one per workspace-inheriting ferro-* stanza; ferro-stripe is independently pinned at 0.5.0 per its own Cargo.toml). The full local verification gate (fmt + clippy `-D warnings` + test `--all-features`) is green. On master merge, the existing `.github/workflows/publish.yml` Wave1A job will detect the version bump plus the Plan 01 runtime/hero_lazy.rs change and publish ferro-json-ui 0.2.42 to crates.io.

## Byte-level diff

### Cargo.toml (single-line change)

```diff
 [workspace.package]
-version = "0.2.41"
+version = "0.2.42"
 edition = "2021"
 rust-version = "1.88.0"
```

### Cargo.lock (line-count delta)

| Metric                                    | Pre-bump | Post-bump | Delta |
| ----------------------------------------- | -------- | --------- | ----- |
| Total `version = "0.2.41"` occurrences    | 25       | 0         | -25   |
| Total `version = "0.2.42"` occurrences    | 0        | 25        | +25   |
| ferro-* stanza `version = "0.2.41"`       | 24       | 0         | -24   |
| ferro-* stanza `version = "0.2.42"`       | 0        | 24        | +24   |

The +25 / -25 balance is a clean 1:1 stanza-level flip; cargo regenerated the lockfile deterministically from the new workspace-version inheritance and produced no spurious dependency drift.

(Note on counts: 25 not 26 — the workspace contains 26 crates total but ferro-stripe independently pins its own `version = "0.5.0"` in `ferro-stripe/Cargo.toml`, so it does not inherit from workspace.package.version. All 25 workspace-inheriting crates flipped correctly. The plan's RESEARCH-stated "26" figure was a minor count overestimate; the substantive contract — every ferro-* stanza that used to read 0.2.41 now reads 0.2.42 — is fully met.)

## Verification

| Gate                                              | Result | Duration  |
| ------------------------------------------------- | ------ | --------- |
| `cargo build --workspace`                         | OK     | 1m 53s    |
| `cargo fmt --all -- --check`                      | OK     | <1s       |
| `cargo clippy --all --all-targets -- -D warnings` | OK     | 1m 07s    |
| `cargo test --all-features`                       | OK     | 5m 25s    |
| `cargo test -p ferro-json-ui --lib runtime::`     | OK     | 12 passed |

Test summary — every suite reports `ok` with `0 failed`. The Plan 01 deliverable test `runtime::tests::runtime_contains_lazy_hero_setup` is green at 0.2.42 (no regression introduced by the version bump). The `bundle_contains_all_setup_functions` and `dispatcher_invokes_every_setup` tests, which Plan 01 extended to include `setupLazyHeroes`, also remain green.

## Wave1A publish readiness

The publish trigger is the master-push event, not this commit. The existing `.github/workflows/publish.yml` workflow:

1. Detects the workspace version bump in `Cargo.toml`.
2. Inspects each WAVE1A_CRATES path filter — ferro-json-ui is in the list.
3. Sees that `ferro-json-ui/src/runtime/hero_lazy.rs` (added in Plan 01) qualifies as a publishable change.
4. Runs `cargo publish` for ferro-json-ui (and any other Wave1A crate with changes since the previous release) against crates.io.

No workflow file edits were required because ferro-json-ui is already enrolled in WAVE1A_CRATES.

Per CONTEXT.md D-08 and memory `feedback_friction_loop_release_cadence.md`, this is the **single** publish for Phase 182 — gestiscilo Phase 186 picks up 0.2.42 by bumping its own Cargo.toml after this lands on master.

## Deviations from Plan

None — plan executed exactly as written.

The only nuance worth recording is the count discrepancy noted above (25 vs the plan's "≥26"). That is a documentation-clarity refinement, not a deviation from the intended outcome: the plan's contract was "no ferro-* stanza left at 0.2.41 and full sync to 0.2.42", which is satisfied perfectly.

## Threat surface

| Threat ID | Disposition | Outcome |
| --------- | ----------- | ------- |
| T-182-06 (Tampering — Cargo.lock drift) | mitigate | Mitigated: both files committed atomically in commit 7c64996e; `cargo build --workspace` regenerated the lockfile from the bumped Cargo.toml so the resulting state is internally coherent. |
| T-182-07 (Elevation of Privilege — publish trigger) | accept | Accepted: the publish workflow runs under CI authentication only; local executor has no publish-token access. Triggering the publish requires merging this commit to master via an authenticated push, which is outside this plan's scope. |

No new threat surface introduced — this plan touches only version strings.

## Commits

- `7c64996e` — `chore(182-03): bump workspace version to 0.2.42 and sync Cargo.lock`

## Self-Check: PASSED

- Cargo.toml line 33 reads `version = "0.2.42"` (verified by grep).
- Zero `version = "0.2.41"` references remain anywhere in Cargo.toml or Cargo.lock (verified by grep).
- 25 `version = "0.2.42"` references in Cargo.lock match the count of pre-bump 0.2.41 references — perfect 1:1 swap.
- `cargo build --workspace` exit 0.
- `cargo fmt --all -- --check` exit 0.
- `cargo clippy --all --all-targets -- -D warnings` exit 0.
- `cargo test --all-features` exit 0 across every test suite.
- `cargo test -p ferro-json-ui --lib runtime::` — 12 passed, including Plan 01's `runtime_contains_lazy_hero_setup`.
- Commit 7c64996e exists in `git log --oneline` for this worktree branch.
- No accidental file deletions in the commit (verified by `git diff --diff-filter=D --name-only HEAD~1 HEAD`).
- No `cargo publish` was invoked locally.
