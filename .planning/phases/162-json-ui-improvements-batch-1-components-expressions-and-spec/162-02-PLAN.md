---
phase: 162-json-ui-improvements-batch-1-components-expressions-and-spec
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-json-ui/src/render/data.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "DataTable row_actions URL placeholders {row_key} and {id} continue to substitute (regression-guarded)"
    - "DataTable row_actions URL supports interpolation of any column key bound at render time ({label}, {slug_path}, {status}, etc.)"
    - "Placeholders that do not match any column key are left literally in the URL (no panic, no silent removal)"
  artifacts:
    - path: "ferro-json-ui/src/render/data.rs"
      provides: "Extended template_actions function + 3 new unit tests"
      contains: "fn template_actions"
  key_links:
    - from: "template_actions per-row substitution loop"
      to: "row JSON object column keys"
      via: "iteration over row.as_object()"
      pattern: "row.as_object.*for.*placeholder"
---

<objective>
Generalize DataTable row_actions URL placeholder interpolation (D-03, D-04). Today `template_actions` only substitutes `{row_key}` and `{id}`; this plan extends it to substitute any column key bound at render time (`{label}`, `{slug_path}`, `{status}`, …) while preserving legacy `{row_key}` / `{id}` semantics. Missing placeholders are left unsubstituted (no panic, no silent removal).

Purpose: Unblocks per-row navigation in gestiscilo `/dashboard/pagine` (publish/delete/QR-download), closing FRICTION entry "DataTable row_actions URL templates."
Output: Extended `template_actions` function in `ferro-json-ui/src/render/data.rs` + three new unit tests + one regression-guard test for existing placeholders.

Bidirectional-adaptation note (CONTEXT planning note + RESEARCH D-03/D-04 planner gate): The decision to ship per-row-action interpolation rather than redesigning to "detail page navigation" is recorded in CONTEXT.md D-03 as locked. The executor does NOT need to revisit this; the gestiscilo confirmation has been treated as obtained at CONTEXT time. The manual confirmation flag is preserved in 162-VALIDATION.md for traceability only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-CONTEXT.md
@.planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-RESEARCH.md
@.planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-PATTERNS.md

<interfaces>
From ferro-json-ui/src/render/data.rs (lines 285-316, current state):
```rust
fn template_actions(
    actions: &[DropdownMenuAction],
    row: &Value,
    row_key_value: &str,
) -> Vec<DropdownMenuAction>
```
Function clones each action and substitutes `{row_key}` and `{id}` in `action.url`. `DropdownMenuAction` and the inner `action.url: Option<String>` / `action.handler: String` shape are already defined in component.rs.

`mk_element` / `mk_spec` helpers exist in data.rs tests (lines 356-376) — reuse for new tests.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| row column values → action.url string | User-supplied data fields are substituted into URLs that the browser later follows; URL-injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-02-01 | Tampering (URL injection) | template_actions substitution of column values | accept | Same trust model as existing `{row_key}` / `{id}` substitution. Values come from server-controlled data (the handler's response); they are not user input from the request. The substitution is a literal `String::replace` (not template parsing), so braces inside values cannot inject new placeholders. URL escaping is the consumer's responsibility — same contract as the existing function. |
| T-162-02-02 | Information Disclosure | Missing-key placeholders leaked into URLs | accept | Documented behavior: unsubstituted `{name}` text appears verbatim in URL, surfaces fast in field testing. Test `data_table_url_template_missing_key_leaves_placeholder` pins this contract. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend template_actions to substitute all row column keys + add 4 tests</name>
  <files>ferro-json-ui/src/render/data.rs</files>
  <read_first>
    - ferro-json-ui/src/render/data.rs lines 285-316 (current `template_actions` implementation), lines 86, 199, 233 (call sites where row_actions are rendered), and lines 356-376 (test helpers `mk_element` + `mk_spec`).
    - Any existing test named `*row_actions*` or `*row_href*` in data.rs — use it as the canonical row-driven test pattern.
    - .planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-PATTERNS.md "template_actions extension (D-03/D-04)" section (full diff).
    - .planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-RESEARCH.md "D-03/D-04 DataTable Row_Actions Placeholder Interpolation" (subtleties around ordering).
  </read_first>
  <behavior>
    - Test 1: `data_table_url_template_replaces_column_key` — row `{"id":"7","label":"Home","slug_path":"/home"}`, action URL `"/p/{slug_path}/edit"` → substituted to `"/p//home/edit"`.
    - Test 2: `data_table_url_template_replaces_multiple_keys` — action URL `"/p/{slug_path}/{status}"` with `slug_path="/home"`, `status="draft"` → `"/p//home/draft"`.
    - Test 3: `data_table_url_template_missing_key_leaves_placeholder` — action URL `"/p/{nonexistent}/edit"`, row without `nonexistent` column → unchanged: `"/p/{nonexistent}/edit"`. No panic.
    - Test 4: `data_table_row_href_legacy_placeholders` (regression guard) — action URL `"/p/{row_key}/{id}"` with `row_key_value="row-3"` and row `id="7"` → `"/p/row-3/7"`. Confirms legacy substitution still works after generalization.
  </behavior>
  <action>
Modify `template_actions` in `ferro-json-ui/src/render/data.rs` (lines 285-316) so that the per-row substitution loop iterates over all row columns BEFORE applying the existing `{row_key}` and `{id}` substitutions. Column-key substitution happens first; legacy `{row_key}` / `{id}` then runs to preserve precedence when a column happens to be named `row_key` or `id`.

Replace the substitution body (where `url` is mutated) with:

```rust
if let Some(mut url) = base_url {
    // D-04: iterate all row columns first.
    if let Some(obj) = row.as_object() {
        for (col_key, col_val) in obj {
            let placeholder = format!("{{{col_key}}}");
            let val_str = match col_val {
                Value::String(s) => s.clone(),
                Value::Number(n) => n.to_string(),
                _ => continue,
            };
            url = url.replace(&placeholder, &val_str);
        }
    }
    // Legacy substitutions (D-03 compatibility).
    url = url.replace("{row_key}", row_key_value);
    if let Some(ref id) = id_value {
        url = url.replace("{id}", id);
    }
    cloned.action.url = Some(url);
}
```

Important notes:
- Do not change the function signature.
- Only `Value::String` and `Value::Number` are substituted. Booleans, nulls, arrays, objects are skipped (continue) — they cannot meaningfully appear in URLs.
- `String::replace` with no match is already a no-op; missing-key passthrough requires no special handling.

Add the four tests above to the `#[cfg(test)] mod tests` block, using the existing `mk_element` / `mk_spec` helpers. Each test constructs a small spec with a `DataTable` element whose `data_path` resolves to a single-row array, calls the top-level render entry, and asserts substrings of the returned HTML.
  </action>
  <verify>
    <automated>cd /Users/alberto/repositories/albertogferrario/ferro &amp;&amp; cargo test -p ferro-json-ui data_table_url_template --all-features 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn data_table_url_template_replaces_column_key\|fn data_table_url_template_replaces_multiple_keys\|fn data_table_url_template_missing_key_leaves_placeholder\|fn data_table_row_href_legacy_placeholders" ferro-json-ui/src/render/data.rs` returns 4
    - `grep -n "row.as_object()" ferro-json-ui/src/render/data.rs` returns at least one line inside `template_actions`
    - `cargo test -p ferro-json-ui data_table_url_template --all-features` exits 0 with 4 tests passing
    - `cargo test -p ferro-json-ui data_table_row_href_legacy_placeholders --all-features` exits 0 (regression guard)
    - `cargo test -p ferro-json-ui --all-features` exits 0 (no other DataTable test regresses)
  </acceptance_criteria>
  <done>template_actions handles arbitrary column-key placeholders, legacy `{row_key}` and `{id}` still resolve, missing keys are left literal, all four new tests pass, no panic paths introduced.</done>
</task>

</tasks>

<verification>
- `cargo test -p ferro-json-ui --all-features` exits 0 (existing + 4 new DataTable tests pass).
- `cargo build -p ferro-json-ui` exits 0.
- No new clippy warnings.
</verification>

<success_criteria>
- DataTable row_actions URL supports any column key as a placeholder.
- Missing placeholders pass through unchanged.
- Wire format unchanged (only render-time substitution).
</success_criteria>

<output>
After completion, create `.planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-02-SUMMARY.md` per the template.
</output>
