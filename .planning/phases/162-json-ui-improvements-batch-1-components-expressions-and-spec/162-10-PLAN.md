---
phase: 162-json-ui-improvements-batch-1-components-expressions-and-spec
plan: 10
type: execute
wave: 4
depends_on: ["07"]
files_modified:
  - docs/src/json-ui/migration-v1-to-v2.md
  - docs/src/json-ui/components.md
  - docs/src/json-ui/plugins.md
  - docs/src/SUMMARY.md
  - ferro-mcp/src/tools/code_templates.rs
  - .planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-DEFERRED.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "docs/src/json-ui/migration-v1-to-v2.md exists with 7 worked-example sections matching D-20"
    - "docs/src/json-ui/components.md has a migration banner at the top linking to migration-v1-to-v2.md (D-13)"
    - "docs/src/json-ui/components.md has a worked Card+children example (D-14) and an Inline view/edit section (D-15)"
    - "docs/src/json-ui/plugins.md exists describing the JsonUiPlugin authoring surface (D-19)"
    - "docs/src/SUMMARY.md lists both migration-v1-to-v2.md and plugins.md under the JSON-UI nav"
    - "mdbook build docs/ exits 0 and both new pages are present in the generated book"
    - "ferro-mcp code_templates returns 7 migration_v1_to_v2 templates (D-22)"
    - "162-DEFERRED.md exists, extracted from CONTEXT.md <deferred> section, satisfying ROADMAP phase goal"
  artifacts:
    - path: "docs/src/json-ui/migration-v1-to-v2.md"
      provides: "Migration guide with 7 worked-example sections"
      min_lines: 300
    - path: "docs/src/json-ui/plugins.md"
      provides: "Plugin authoring guide describing JsonUiPlugin trait + Asset system"
      min_lines: 80
    - path: "ferro-mcp/src/tools/code_templates.rs"
      provides: "migration_v1_to_v2_templates() function returning 7 CodeTemplate entries"
      contains: "migration_v1_to_v2_templates"
    - path: ".planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-DEFERRED.md"
      provides: "Standalone deferred-items artifact extracted from CONTEXT.md <deferred> section"
      contains: "Phase 162: Deferred Items"
  key_links:
    - from: "docs/src/SUMMARY.md"
      to: "migration-v1-to-v2.md and plugins.md"
      via: "nav entries under JSON-UI section"
      pattern: "migration-v1-to-v2|plugins.md"
    - from: "ferro-mcp code_templates::execute"
      to: "migration_v1_to_v2_templates()"
      via: "build_templates() concatenation"
      pattern: "templates.extend.*migration_v1_to_v2"
---

<objective>
Land all Phase 162 documentation and MCP code_templates surface (D-13, D-14, D-15, D-19, D-20, D-22):
- New `docs/src/json-ui/migration-v1-to-v2.md` with 7 worked-example sections.
- New `docs/src/json-ui/plugins.md` describing the JsonUiPlugin authoring surface.
- `docs/src/json-ui/components.md` updates: migration banner at top (D-13), worked Card+children example (D-14), Inline view/edit section (D-15).
- `docs/src/SUMMARY.md` nav entries for the two new pages.
- `ferro-mcp/src/tools/code_templates.rs` adds `migration_v1_to_v2_templates()` exposing the 7 D-20 sections as `CodeTemplate` entries (D-22).

Purpose: Make Phase 162 surface discoverable to consumer migrations (gestiscilo Phases 139-143) via both human docs and agent-introspectable code templates.
Output: Two new doc pages, three sections added to components.md, one SUMMARY.md update, one new function in code_templates.rs + a test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-CONTEXT.md
@.planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-RESEARCH.md
@.planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-PATTERNS.md

<interfaces>
Documentation:
- `docs/` is an mdbook project; build via `mdbook build docs/`.
- `docs/src/SUMMARY.md` controls the nav; new pages must be listed here.
- `docs/src/json-ui/components.md` is the existing component reference. D-13/D-14/D-15 add sections, do not replace the file.
- Canonical v2 reference cited throughout migration guide: `app/static/pagamenti.json` + `app/src/controllers/pagamenti.rs` (Phase 121).

MCP code_templates:
- `ferro-mcp/src/tools/code_templates.rs` defines `CodeTemplate { name, category, description, code, imports, placeholders }` and `Placeholder { name, description, example }`.
- `build_templates()` concatenates per-category template lists.
- `execute(category: Option<&str>)` filters by category.
- Existing categories: `handler_templates()`, `json_view_templates()`, `api_templates()`, etc. — `migration_v1_to_v2_templates()` follows the same shape.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc content → rendered HTML in mdbook | Static markdown; no XSS surface |
| code_templates output → MCP client (agent context) | Server-controlled template strings; same trust model as existing templates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-10-01 | None | Documentation files | n/a | Static markdown only. No new attack surface. |
| T-162-10-02 | Tampering (agent confusion) | Misleading code templates | mitigate | Templates are tagged with category `"migration_v1_to_v2"` so agents can disambiguate from other categories. Each template's `description` field clearly states the v1→v2 context. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/src/json-ui/migration-v1-to-v2.md with 7 worked-example sections + update SUMMARY.md</name>
  <files>docs/src/json-ui/migration-v1-to-v2.md, docs/src/SUMMARY.md</files>
  <read_first>
    - docs/src/SUMMARY.md — locate the existing JSON-UI section (around the `json-schema.md` entry).
    - docs/src/json-ui/components.md — copy the file's tone, code-block style, and worked-example formatting.
    - app/static/pagamenti.json AND app/src/controllers/pagamenti.rs (the canonical v2 reference cited throughout).
    - .planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-CONTEXT.md D-20 (section ordering and length target: 300-500 lines).
    - .planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-RESEARCH.md "D-20 migration-v1-to-v2.md" section.
  </read_first>
  <action>
**Create `docs/src/json-ui/migration-v1-to-v2.md`** with the following 7 sections in this order (per D-20):

1. **`JsonUi::render_file` vs `Spec::builder()`** — explain when to use each; cite `app/static/pagamenti.json` + `app/src/controllers/pagamenti.rs` as the canonical reference. Show a v1 builder snippet, the equivalent v2 JSON file, and the controller using `JsonUi::render_file`.

2. **`Card + Form + Alert` depth-flattening** — the account.rs case from FRICTION. Show v1 nested-children code, then v2 flat `elements` map with `children: ["heading", "form_login", "error_alert"]`.

3. **Per-row action interpolation in DataTable** — D-03/D-04 worked example. Show a DataTable spec with `row_actions: [{ ... url: "/p/{slug_path}/edit" }]` and the row data shape that drives it.

4. **Read+edit detail pattern (D-15 replacement for DetailForm)** — show `Form` element with `DescriptionList`-style read-only items and `Input` elements toggled by `visible: { eq: ["query.mode", "edit"] }` or equivalent.

5. **Data-driven options with `CheckboxList` (D-01)** — show static-options spec, then data-driven spec with `options_path: "/services"` and `selected_path: "/user.selected_services"`.

6. **Variant string round-trip with strum derives (D-11)** — show consumer Rust code using `AlertVariant::Success.as_ref()` in a spec-builder context vs hand-typed `"success"` strings.

7. **Handler-name verification with `json_ui_verify_action` MCP tool (D-09)** — show the agent workflow: `mcp__ferro__json_ui_verify_action({ handler: "dashboard.show", method: "GET" })` and what the response looks like; include the Levenshtein-candidate response shape.

Each section: 30-70 lines (introduction + v1 snippet + v2 snippet + commentary). Total file: ~300-500 lines.

Tone: neutral architectural documentation. No "killer feature" or strategy language. Voice matches existing docs/src/json-ui/components.md.

**Update `docs/src/SUMMARY.md`:**

Add the new entry under the `# JSON-UI` section (alphabetically or in a logical position near `json-schema.md`):

```markdown
  - [Migration v1 → v2](./json-ui/migration-v1-to-v2.md)
```

Use the exact indentation convention SUMMARY.md already uses.
  </action>
  <verify>
    <automated>cd /Users/alberto/repositories/albertogferrario/ferro &amp;&amp; mdbook build docs/ 2>&amp;1 | tail -10 &amp;&amp; test -f docs/book/json-ui/migration-v1-to-v2/index.html &amp;&amp; echo "built OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/src/json-ui/migration-v1-to-v2.md`
    - `wc -l docs/src/json-ui/migration-v1-to-v2.md` reports >= 300 lines
    - All 7 section headers present: `grep -c "^## " docs/src/json-ui/migration-v1-to-v2.md` >= 7
    - `grep -c "pagamenti" docs/src/json-ui/migration-v1-to-v2.md` >= 1 (canonical reference cited)
    - `grep -c "migration-v1-to-v2" docs/src/SUMMARY.md` >= 1
    - `mdbook build docs/` exits 0
    - `test -f docs/book/json-ui/migration-v1-to-v2/index.html` (mdbook output produced)
  </acceptance_criteria>
  <done>Migration guide ships at 300+ lines covering 7 sections, mdbook builds clean, new page reachable from SUMMARY.md.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/src/json-ui/plugins.md (D-19) + update components.md (D-13, D-14, D-15) + SUMMARY.md</name>
  <files>docs/src/json-ui/plugins.md, docs/src/json-ui/components.md, docs/src/SUMMARY.md</files>
  <read_first>
    - ferro-json-ui/src/plugin.rs — read the `JsonUiPlugin` trait definition, `Asset` struct, `register_plugin` / `global_plugin_registry` signatures. These are the consumer-facing primitives the page documents.
    - ferro-json-ui/src/plugins/map.rs — the canonical worked example to cite.
    - docs/src/json-ui/components.md — current structure (Component Overview table around line 20, individual component sections below).
  </read_first>
  <action>
**Create `docs/src/json-ui/plugins.md` (D-19):**

Sections (80-150 lines total):

1. **Overview** — when to write a plugin (third-party widget, server-emitted CSS/JS assets, init scripts).
2. **The `JsonUiPlugin` trait** — show the trait signature, document each method.
3. **Asset system** — `Asset::new(url).integrity(hash).crossorigin(value)` with SRI explanation.
4. **Init script convention** — IIFE pattern, data-attribute binding (`data-ferro-*`), idempotency.
5. **Registration** — `register_plugin(MyPlugin)` and `global_plugin_registry()` patterns.
6. **Worked example** — point at `ferro-json-ui/src/plugins/map.rs` (and `rich_text_editor.rs` after Phase 162 ships) as in-tree examples; include a 30-40 line custom plugin snippet (e.g. a hypothetical Stripe widget plugin).
7. **Catalog discoverability** — explain how `BUILTIN_SPECS` registration in `ferro-json-ui/src/catalog.rs` makes the plugin discoverable via the `json_ui_catalog` MCP tool.

**Update `docs/src/json-ui/components.md` (D-13, D-14, D-15):**

1. **D-13 migration banner** — at the very top of components.md (before the Component Overview table, around line 20), add a callout box:

   ```markdown
   > **Migrating from v1?** See the [Migration v1 → v2](./migration-v1-to-v2.md) guide for the
   > correct path. v1 types (`JsonUiView`, `Component`, `ComponentNode`) are removed in v2;
   > the migration pattern is `JsonUi::render_file("src/views/.../*.json", data)`.
   ```

2. **D-14 worked Card+children example** — in (or near) the Card component section, add a worked example showing the flat `elements` map pattern:

   ```json
   {
     "$schema": "ferro-json-ui/v2",
     "root": "card_main",
     "elements": {
       "card_main": {
         "type": "Card",
         "props": { "title": "Welcome" },
         "children": ["heading", "form_login"]
       },
       "heading": { "type": "Text", "props": { "content": "Sign in" } },
       "form_login": { "type": "Form", "props": { ... }, "children": [...] }
     }
   }
   ```

   Explain that v1's nested `CardProps.children: Vec<Component>` is replaced by a `children: Vec<String>` of element IDs that index into the flat `elements` map.

3. **D-15 Inline view/edit section** — new H2 section "Inline view/edit pattern". Show a `Form` element whose children mix `DescriptionList`-style read-only items (when `?mode=view`) with `Input` elements (when `?mode=edit`), driven by `visible` conditions on the query param.

**Update `docs/src/SUMMARY.md`:**

Add the plugins.md entry:
```markdown
  - [Plugins](./json-ui/plugins.md)
```
  </action>
  <verify>
    <automated>cd /Users/alberto/repositories/albertogferrario/ferro &amp;&amp; mdbook build docs/ 2>&amp;1 | tail -10 &amp;&amp; test -f docs/book/json-ui/plugins/index.html &amp;&amp; grep -q "Migrating from v1" docs/src/json-ui/components.md &amp;&amp; echo "built OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/src/json-ui/plugins.md`
    - `wc -l docs/src/json-ui/plugins.md` reports >= 80 lines
    - `grep -c "JsonUiPlugin" docs/src/json-ui/plugins.md` >= 1
    - `grep -c "Migrating from v1" docs/src/json-ui/components.md` >= 1 (D-13 banner)
    - `grep -c "Inline view/edit" docs/src/json-ui/components.md` >= 1 (D-15 section)
    - `grep -c "plugins.md" docs/src/SUMMARY.md` >= 1
    - `mdbook build docs/` exits 0
    - `test -f docs/book/json-ui/plugins/index.html`
  </acceptance_criteria>
  <done>plugins.md ships, components.md has migration banner + worked Card+children + inline-view/edit, SUMMARY.md links plugins.md, mdbook builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add migration_v1_to_v2_templates() to ferro-mcp code_templates.rs + 1 test</name>
  <files>ferro-mcp/src/tools/code_templates.rs</files>
  <read_first>
    - ferro-mcp/src/tools/code_templates.rs full file — `CodeTemplate` struct, `Placeholder` struct, `build_templates()` aggregator, existing per-category functions (`handler_templates()`, `json_view_templates()`, `api_templates()`).
    - .planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-PATTERNS.md "code_templates.rs — migration templates (D-22)" section.
  </read_first>
  <behavior>
    - Test 1: `code_templates_returns_migration_patterns` — `execute(Some("migration_v1_to_v2"))` returns >= 7 templates, each with `category == "migration_v1_to_v2"`, matching the 7 sections of D-20.
  </behavior>
  <action>
**Add a new per-category function to `ferro-mcp/src/tools/code_templates.rs`:**

```rust
fn migration_v1_to_v2_templates() -> Vec<CodeTemplate> {
    vec![
        CodeTemplate {
            name: "render_file_migration".to_string(),
            category: "migration_v1_to_v2".to_string(),
            description: "Replace v1 JsonUiView builder with v2 JsonUi::render_file. \
                          v1 types (JsonUiView, Component, ComponentNode) are removed.".to_string(),
            code: r#"// v2: load spec from JSON file and merge handler data
JsonUi::render_file("src/views/{{module}}/{{page}}.json", json!({
    "data": { /* your handler data */ }
}))"#.to_string(),
            imports: vec!["use ferro_json_ui::JsonUi;".to_string()],
            placeholders: vec![
                Placeholder {
                    name: "{{module}}".to_string(),
                    description: "Controller module name".to_string(),
                    example: "account".to_string(),
                },
                Placeholder {
                    name: "{{page}}".to_string(),
                    description: "Page name".to_string(),
                    example: "settings".to_string(),
                },
            ],
        },
        CodeTemplate {
            name: "card_children_flat_map".to_string(),
            category: "migration_v1_to_v2".to_string(),
            description: "v2 Card with children as IDs into the flat elements map (was nested in v1).".to_string(),
            code: r#"{
  "root": "card_main",
  "elements": {
    "card_main": { "type": "Card", "props": {"title": "{{title}}"}, "children": ["heading", "body"] },
    "heading":  { "type": "Text", "props": {"content": "{{heading}}"} },
    "body":     { "type": "Text", "props": {"content": "{{body}}"} }
  }
}"#.to_string(),
            imports: vec![],
            placeholders: vec![
                Placeholder { name: "{{title}}".to_string(), description: "Card title".to_string(), example: "Welcome".to_string() },
                Placeholder { name: "{{heading}}".to_string(), description: "Heading text".to_string(), example: "Sign in".to_string() },
                Placeholder { name: "{{body}}".to_string(), description: "Body text".to_string(), example: "Enter your credentials.".to_string() },
            ],
        },
        CodeTemplate {
            name: "datatable_row_actions_interpolation".to_string(),
            category: "migration_v1_to_v2".to_string(),
            description: "DataTable per-row action URL with column-key interpolation (D-03/D-04). Any row column key like {slug_path}, {label}, {status} is substituted at render time.".to_string(),
            code: r#"{
  "type": "DataTable",
  "props": {
    "data_path": "/pages",
    "row_key": "slug_path",
    "row_actions": [
      { "label": "Edit", "action": { "url": "/p/{slug_path}/edit" } },
      { "label": "Delete", "action": { "url": "/p/{slug_path}/delete" } }
    ]
  }
}"#.to_string(),
            imports: vec![],
            placeholders: vec![],
        },
        CodeTemplate {
            name: "inline_view_edit_pattern".to_string(),
            category: "migration_v1_to_v2".to_string(),
            description: "Read+edit detail pattern using Form + visible conditions on a ?mode= query param (replaces v1's DetailForm).".to_string(),
            code: r#"{
  "type": "Form",
  "props": { "method": "POST", "action": "/account/update" },
  "children": ["name_view", "name_edit"],
  "elements": {
    "name_view": { "type": "Text",  "props": {"content": "{{name}}"},
                   "visible": { "ne": ["query.mode", "edit"] } },
    "name_edit": { "type": "Input", "props": {"field": "name", "label": "Name", "data_path": "/name"},
                   "visible": { "eq": ["query.mode", "edit"] } }
  }
}"#.to_string(),
            imports: vec![],
            placeholders: vec![
                Placeholder { name: "{{name}}".to_string(), description: "Current name value".to_string(), example: "Jane Doe".to_string() },
            ],
        },
        CodeTemplate {
            name: "checkbox_list_data_driven".to_string(),
            category: "migration_v1_to_v2".to_string(),
            description: "Data-driven CheckboxList (D-01). Options resolved from options_path; pre-selected values from selected_path.".to_string(),
            code: r#"{
  "type": "CheckboxList",
  "props": {
    "field": "services",
    "options_path": "/available_services",
    "selected_path": "/user/selected_services",
    "label": "Choose services"
  }
}"#.to_string(),
            imports: vec![],
            placeholders: vec![],
        },
        CodeTemplate {
            name: "variant_strum_round_trip".to_string(),
            category: "migration_v1_to_v2".to_string(),
            description: "Use typed variant enums via AsRefStr (D-11) instead of hand-typed strings in Rust spec-builder code.".to_string(),
            code: r#"use ferro_json_ui::{AlertVariant, AlertProps};

let props = AlertProps {
    variant: AlertVariant::Success,
    // ...
};
// At serialization time, AlertVariant::Success -> "success" (snake_case).
// At consumer call sites, AlertVariant::Success.as_ref() == "success"."#.to_string(),
            imports: vec!["use ferro_json_ui::{AlertVariant, AlertProps};".to_string()],
            placeholders: vec![],
        },
        CodeTemplate {
            name: "verify_action_mcp".to_string(),
            category: "migration_v1_to_v2".to_string(),
            description: "Verify a handler name via the json_ui_verify_action MCP tool (D-09). Returns route info on hit; Levenshtein candidate on miss.".to_string(),
            code: r#"// MCP tool call (agent context):
// mcp__ferro__json_ui_verify_action({ "handler": "{{handler}}", "method": "{{method}}" })
//
// Hit:  { "found": true,  "route": { ... },  "candidate": null }
// Miss: { "found": false, "route": null,     "candidate": "dashboard.show" }"#.to_string(),
            imports: vec![],
            placeholders: vec![
                Placeholder { name: "{{handler}}".to_string(), description: "Registered route name".to_string(), example: "dashboard.show".to_string() },
                Placeholder { name: "{{method}}".to_string(), description: "HTTP method".to_string(), example: "GET".to_string() },
            ],
        },
    ]
}
```

**Register in `build_templates()`** (after `api_templates()` or wherever existing categories are concatenated):

```rust
templates.extend(migration_v1_to_v2_templates());
```

**Add the test** to the inline test block:

```rust
#[test]
fn code_templates_returns_migration_patterns() {
    let templates = execute(Some("migration_v1_to_v2"));
    assert!(templates.len() >= 7, "expected at least 7 migration_v1_to_v2 templates, got {}", templates.len());
    for t in &templates {
        assert_eq!(t.category, "migration_v1_to_v2");
    }
    let names: Vec<&str> = templates.iter().map(|t| t.name.as_str()).collect();
    for required in [
        "render_file_migration",
        "card_children_flat_map",
        "datatable_row_actions_interpolation",
        "inline_view_edit_pattern",
        "checkbox_list_data_driven",
        "variant_strum_round_trip",
        "verify_action_mcp",
    ] {
        assert!(names.contains(&required), "missing migration template: {required}");
    }
}
```

If `execute()` returns a `Catalog` or wrapper type rather than `Vec<CodeTemplate>` directly, adjust the test's access pattern accordingly — read `execute()`'s signature first.
  </action>
  <verify>
    <automated>cd /Users/alberto/repositories/albertogferrario/ferro &amp;&amp; cargo test -p ferro-mcp code_templates_returns_migration_patterns --all-features 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn migration_v1_to_v2_templates" ferro-mcp/src/tools/code_templates.rs` returns 1
    - `grep -c '"migration_v1_to_v2"' ferro-mcp/src/tools/code_templates.rs` >= 8 (7 categories + 1 in test or aggregator call)
    - `grep -c "templates.extend(migration_v1_to_v2_templates())" ferro-mcp/src/tools/code_templates.rs` returns 1
    - `cargo test -p ferro-mcp code_templates_returns_migration_patterns --all-features` exits 0
  </acceptance_criteria>
  <done>migration_v1_to_v2_templates returns 7 templates each tagged correctly; test confirms presence by name.</done>
</task>

<task type="auto">
  <name>Task 4: Extract 162-DEFERRED.md from CONTEXT.md &lt;deferred&gt; section (ROADMAP goal artifact)</name>
  <files>.planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-DEFERRED.md</files>
  <read_first>
    - .planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-CONTEXT.md (entire `<deferred>` section — copy verbatim with each item's rationale)
    - .planning/ROADMAP.md Phase 162 entry — the goal states "Dropped items are documented with rationale in a DEFERRED.md"
  </read_first>
  <action>
**Create `.planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-DEFERRED.md`** with this content (extracted verbatim from CONTEXT.md `<deferred>` section, plus a heading + date):

```markdown
# Phase 162: Deferred Items

**Deferred:** 2026-05-16
**Source:** Extracted from `162-CONTEXT.md` `<deferred>` section.

Phase 162 deliberately stays narrow — only items justified by the four gestiscilo Phase 138 controllers AND the blast-radius API decisions. Items observed but explicitly deferred:

- `$each` / `$if` / `$template` spec-level iteration directives — deferred to Phase 163 (gestiscilo Phase 140 cassa, where heterogeneous iteration provides the forcing function).
- `SpecBuilder` ergonomic nested DSL — deferred to Phase 163.
- `ferro json-ui:migrate-v1` codemod — deferred to Phase 163.
- Multi-step form patterns, `visible` rule expressiveness at depth, PDF preview routing — deferred to Phase 164 (gestiscilo Phase 142 documenti).
- Host-based tenancy gap — out of JSON-UI scope; tracked in `.planning/backlog/host-based-tenancy.md` for a dedicated tenancy-layer phase. The `PreRouteMiddleware.rewrite` → `handle` rename surfaced in the blast radius is a one-line gestiscilo-side fix and does not bring the tenancy work forward.
- `Fragment` / `Group` borderless container (D-06) — explicitly rejected for Phase 162. Revisit only if a future phase finds a use case that D-05 + existing containers (Grid 1-col, FormSection without title) cannot express.
- `#[handler(name = "...")]` attribute (D-10) — explicitly rejected for Phase 162. Revisit only if a future use case justifies a second source of truth for route names.

Detailed next-phase inputs are tracked in `162-CONTEXT.md` `<followups>`.
```

This task is a literal file write — no logic. The content is the verbatim extraction.
  </action>
  <verify>
    <automated>cd /Users/alberto/repositories/albertogferrario/ferro &amp;&amp; test -f .planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-DEFERRED.md &amp;&amp; grep -q "Phase 162: Deferred Items" .planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-DEFERRED.md &amp;&amp; grep -q "host-based-tenancy" .planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-DEFERRED.md &amp;&amp; echo "DEFERRED.md OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-DEFERRED.md`
    - `grep -c "Phase 162: Deferred Items" .planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-DEFERRED.md` >= 1
    - `grep -c "host-based-tenancy" .planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-DEFERRED.md` >= 1
    - `grep -c "Phase 163" .planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-DEFERRED.md` >= 1
    - `grep -c "Phase 164" .planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-DEFERRED.md` >= 1
  </acceptance_criteria>
  <done>162-DEFERRED.md exists at the phase directory with deferred items + rationale, satisfying the ROADMAP phase goal "Dropped items are documented with rationale in a DEFERRED.md".</done>
</task>

</tasks>

<verification>
- `mdbook build docs/` exits 0; both new pages are in the generated book.
- `cargo test -p ferro-mcp code_templates_returns_migration_patterns --all-features` exits 0.
- Components.md, plugins.md, and migration-v1-to-v2.md cross-reference each other coherently.
- `162-DEFERRED.md` exists and contains the verbatim deferred items from CONTEXT.md.
</verification>

<success_criteria>
- 7 D-20 sections shipped in migration-v1-to-v2.md.
- Plugin authoring guide shipped (D-19).
- components.md has migration banner, Card+children example, Inline view/edit section.
- code_templates exposes 7 migration templates via MCP (D-22).
- 162-DEFERRED.md ships satisfying the ROADMAP phase goal artifact requirement.
</success_criteria>

<output>
After completion, create `.planning/phases/162-json-ui-improvements-batch-1-components-expressions-and-spec/162-10-SUMMARY.md` per the template.
</output>
