---
phase: 259-request-scoped-memoization
plan: 03
type: execute
wave: 3
depends_on: ["259-02"]
files_modified:
  - framework/src/memo/mod.rs
autonomous: true
requirements: [LIVE-01]

must_haves:
  truths:
    - "A projection deriving multiple intents (Browse + Summarize) over one key issues exactly ONE underlying fetch through the memo store — SC-3, proven by a constructed honest harness"
    - "The harness invokes the REAL render pass (derive_intents + JsonUiRenderer::render) around a #[memoize]d loader, asserting a single underlying call via an Arc<AtomicUsize> counter"
    - "Every exported memo symbol carries neutral /// rustdoc (cargo doc clean)"
    - "The CI-exact gate passes: fmt --check, clippy --all --all-targets -D warnings, test --all-features"
  artifacts:
    - path: "framework/src/memo/mod.rs"
      provides: "SC-3 render-path proof as an in-crate #[cfg(all(test, feature = \"projections\"))] mod render_path_tests; submodule (in-crate so the pub(crate) MEMO_STORE / with_memo_scope are reachable, consistent with Plan 02's in-crate macro tests)"
      contains: "derive_intents"
      min_lines: 60
  key_links:
    - from: "framework/src/memo/mod.rs (mod render_path_tests)"
      to: "ferro-projections::derive_intents / ferro-json-ui JsonUiRenderer"
      via: "a #[memoize]d loader called around a multi-intent render, asserting counter == 1"
      pattern: "derive_intents|render"
    - from: "framework/src/memo/mod.rs (mod render_path_tests)"
      to: "ferro::memo::MemoStore"
      via: "MEMO_STORE.scope wrapping the two intent renders (in-crate, so MEMO_STORE is reachable)"
      pattern: "scope"
---

<objective>
Prove Success Criterion #3 honestly: a projection render deriving multiple intents over one
key issues a single underlying fetch through the memo store. Per the locked D-05 finding and
RESEARCH Pattern 8, the `ServiceDef → JsonUiRenderer` pass is SCHEMA-ONLY (no I/O). Therefore
SC-3 is proven by a CONSTRUCTED harness: a `#[memoize]`d loader invoked around a genuine
multi-intent render (`derive_intents` + `JsonUiRenderer::render` twice, once per intent), with
an `Arc<AtomicUsize>` counter asserting the loader ran exactly once. Do NOT fabricate a fetch
inside the renderer that does not exist.

Purpose: SC-3 is the concrete payoff that justifies the whole primitive ("N intents over one
key issue one fetch"). Plans 01-02 proved the store and macro in isolation; this plan proves the
render-path deduplication that the milestone exists for.

Output: an in-crate `#[cfg(all(test, feature = "projections"))] mod render_path_tests;` submodule
of `framework/src/memo/mod.rs` (projections-gated; in-crate so the `pub(crate)` `MEMO_STORE` /
`with_memo_scope` are reachable, matching Plan 02's in-crate macro tests), a final `///` rustdoc
audit on the memo API, and the CI-exact gate green — the terminal wave gate for the phase.

Depends on Plan 02: the harness uses the `#[memoize]` macro over the store.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/259-request-scoped-memoization/259-CONTEXT.md
@.planning/phases/259-request-scoped-memoization/259-RESEARCH.md
@.planning/phases/259-request-scoped-memoization/259-01-SUMMARY.md
@.planning/phases/259-request-scoped-memoization/259-02-SUMMARY.md

<interfaces>
<!-- The schema-only render pass this harness drives (VERIFIED schema-only in RESEARCH Pattern 8). -->
<!-- Read the real signatures before writing the test — extract them in Task 1. -->
- `ferro_projections::derive_intents(service_def: &ServiceDef) -> ...` (schema signals only, no I/O)
  — ferro-projections/src/derive.rs:75-80
- `ferro_json_ui` `JsonUiRenderer` / `Spec::from_service_def(&ServiceDef, &[IntentScore], &VisualContext)`
  — ferro-json-ui/src/projection/builder.rs:56-124 (synchronous, pure)
- `ferro::memo::MemoStore` + `#[ferro::memoize]` (Plans 01/02)
</interfaces>

<analogs>
- 259-RESEARCH.md Pattern 8 — the EXACT harness design (constructed memoized loader called
  around the render pass; assert counter == 1 across Browse + Summarize over one key).
- 259-RESEARCH.md Example 5 — the `Arc<AtomicUsize>` counter test pattern.
- Any existing `#[cfg(feature = "projections")]` integration test under framework/tests/ or the
  ferro-json-ui tests, for the ServiceDef fixture shape (find one that builds a multi-intent
  ServiceDef — Browse + Summarize).
- framework/Cargo.toml:18 — the `projections` feature that enables ferro-projections + ferro-json-ui.
</analogs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC-3 render-path integration harness (single fetch across multi-intent render)</name>
  <read_first>
    - ferro-projections/src/derive.rs (lines ~75-80 — derive_intents signature; confirm it takes &ServiceDef and returns intent scores with NO async/DB)
    - ferro-json-ui/src/projection/builder.rs (lines ~56-124 — Spec::from_service_def signature; confirm synchronous/pure)
    - .planning/phases/259-request-scoped-memoization/259-RESEARCH.md (Pattern 8 — the harness design; Pitfall 3 — --all-features requirement)
    - framework/Cargo.toml (line 18 — the projections feature deps)
    - an existing ServiceDef test fixture (grep ferro-projections / ferro-json-ui tests for a Browse+Summarize ServiceDef to copy the builder calls) 
  </read_first>
  <files>framework/src/memo/mod.rs</files>
  <action>
    Write the SC-3 proof as an IN-CRATE submodule of `framework/src/memo/mod.rs` (NOT an integration
    test under `framework/tests/`). Rationale: `MEMO_STORE` is `pub(crate)` (Plan 01), so only an
    in-crate module can enter its scope directly; this also keeps the public API narrow and is
    consistent with Plan 02's in-crate `macro_tests` module. In `framework/src/memo/mod.rs` add:

    ```rust
    #[cfg(all(test, feature = "projections"))]
    mod render_path_tests;
    ```

    and create the module inline (or as `framework/src/memo/render_path_tests.rs` if you prefer a
    separate file — either is in-crate; if a separate file, add it to files_modified in the SUMMARY).
    The `feature = "projections"` gate ensures it only compiles/runs under `--all-features`, matching
    CI (RESEARCH Pitfall 3). Do NOT create `framework/tests/memoize_render_path.rs`.

    First, EXTRACT the real signatures of `derive_intents` and `Spec::from_service_def` /
    `JsonUiRenderer::render` from the two source files above — the harness must call the ACTUAL render
    pass, not a stub renderer. Build a `ServiceDef` fixture whose field signals derive at least two
    intents (Browse + Summarize) over one underlying model set (copy a fixture from the projections
    tests; do not hand-roll intent scores).

    The harness (RESEARCH Pattern 8):

    1. A module-level `static FETCH_COUNTER: AtomicUsize = AtomicUsize::new(0);`.
    2. A `#[ferro::memoize] async fn load_model_set(key: u32) -> Vec<String>` that does
       `FETCH_COUNTER.fetch_add(1, Ordering::SeqCst)` and returns representative data — this is the
       "underlying fetch" a real multi-intent handler would issue once and reuse.
    3. `#[tokio::test] async fn render_path_single_fetch()`:
       - Reset `FETCH_COUNTER` to 0.
       - Enter the memo scope in-crate — use `super::MEMO_STORE.scope(super::memo_scope(), async { ... })`
         (or `super::with_memo_scope(super::memo_scope(), async { ... }).await`); both are reachable
         because this module is in-crate.
       - Inside the scope: call `derive_intents(&service_def)` ONCE to get the intent list, then for
         EACH derived intent (Browse, Summarize) render via the real `JsonUiRenderer` /
         `Spec::from_service_def`, and BEFORE/AROUND each render call `load_model_set(THE_SAME_KEY)`
         to fetch the data that intent needs. Both intents fetch the SAME key.
       - Assert both renders produced a non-empty `Spec`/HTML (the render pass ran for both intents).
       - Assert `FETCH_COUNTER.load(Ordering::SeqCst) == 1` — N intents, one fetch. THIS is SC-3.

    Add a `//` comment block at the top of the module citing RESEARCH Pattern 8 / D-05: the render
    pass is schema-only, so the memoized fetch is the loader a real multi-intent handler calls, not a
    fabricated fetch inside the renderer. This documents the honesty of the proof.
  </action>
  <verify>
    <automated>cargo test -p ferro-rs render_path_single_fetch --all-features 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The SC-3 test exists as the in-crate `render_path_tests` module of `framework/src/memo/mod.rs`
      (registered via `#[cfg(all(test, feature = "projections"))] mod render_path_tests;`) and calls
      the REAL `derive_intents` (grep `derive_intents`) and the REAL renderer (grep `from_service_def`
      or `render`)
    - `framework/tests/memoize_render_path.rs` is NOT created
    - The test uses a `static AtomicUsize` fetch counter and a `#[ferro::memoize]` / `#[memoize]` loader
    - `cargo test -p ferro-rs render_path_single_fetch --all-features` exits 0
    - The assertion is `== 1` (single fetch across ≥2 intents over one key) — grep for the counter assertion
    - No stub/fake renderer is introduced — the test drives the actual ServiceDef→intent→render path
  </acceptance_criteria>
  <done>
    SC-3 proven honestly: a genuine multi-intent render over one key issues exactly one underlying fetch through the memo store.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final rustdoc audit + CI-exact gate (phase terminal gate)</name>
  <read_first>
    - framework/src/memo/mod.rs (confirm /// on every exported symbol)
    - ferro-macros/src/memoize.rs (confirm /// on memoize_impl + the pub macro)
    - CLAUDE.md (Testing & Linting section — the exact CI gate commands)
    - .planning/phases/259-request-scoped-memoization/259-RESEARCH.md (Pitfall 3 — clippy must run --all-features)
  </read_first>
  <files>framework/src/memo/mod.rs, ferro-macros/src/memoize.rs</files>
  <action>
    Audit `///` rustdoc coverage on ALL exported memo symbols across Plans 01-03: `MemoStore`,
    `MemoKey`, `MemoKey::new`, `MemoSlot`, `MemoStore::new`, `MemoStore::get_or_insert`,
    `current_memo_store`, and the `#[memoize]` macro. Every one must have neutral, minimal,
    scientific-voice `///` docs (project convention: godoc-style on every exported symbol; no
    marketing language). Fix any gaps. Ensure the `#[memoize]` doc block includes a
    `rust,ignore`/`compile_fail` usage example and documents the three constraints: value args must
    be `Hash`; return type must be `Clone + Send + Sync + 'static`; applies to impl methods
    (`&self` excluded), not trait declarations; and the D-02 graceful-no-op / D-04 Err-cached
    semantics.

    Then run the CI-EXACT gate, in order, and fix anything it flags (the phase does NOT close until
    all three are green — note the disk-full caveat in project memory: check `df` and clean `target/`
    if `test --all-features` fails with ENOSPC link/fingerprint errors, which are NOT real defects):

    ```bash
    cargo fmt --all -- --check
    cargo clippy --all --all-targets --all-features -- -D warnings
    cargo test --all-features
    ```

    (Clippy MUST include `--all-features` — the projections-gated SC-3 test only compiles under it;
    RESEARCH Pitfall 3.) Any warning is a build failure. Do NOT commit until all three exit 0.

    If `cargo test --all-features` regenerates `docs/protocol/schemas/*.json` (Phase 94 export test,
    per project memory) or leaves the tree dirty with churn unrelated to this phase, `git checkout`
    those files — do NOT fold them into the phase commit.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --all --all-targets --all-features -- -D warnings && cargo test --all-features 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy --all --all-targets --all-features -- -D warnings` exits 0
    - `cargo test --all-features` exits 0 (all memo/memoize/render-path tests green)
    - `cargo doc -p ferro-rs --no-deps` exits 0 with no missing-docs warnings on the memo API
    - Every exported memo symbol has a `///` doc comment (grep-verify: no `pub fn`/`pub struct`/`pub type` in memo/mod.rs without a preceding `///`)
    - No unrelated schema/tree churn is included in the working tree (git status clean except this phase's files)
  </acceptance_criteria>
  <done>
    All exported memo symbols documented; the full CI-exact gate (fmt + clippy --all-features + test --all-features) is green; phase 259 is ready for /gsd-verify-work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request → request | Same per-request store boundary as Plans 01-02. This plan adds only a test harness and a docs/gate pass; it introduces NO new runtime code path or trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-259-07 | Information Disclosure | SC-3 render-path memoized fetch | mitigate | The render-path harness enters a FRESH `MEMO_STORE.scope` per test; the memoized loader reads only the per-request store (inherited T-259-01 mitigation). The single-fetch assertion also transitively confirms no cross-scope reuse leaks a prior fetch. No new disclosure path. |
| T-259-08 | Tampering | render pass reading a wrongly-keyed memoized value | accept | Both intents fetch the SAME explicit key by construction; the callsite marker + arg hash (Plan 02) guarantees correct keying. Within-request `DefaultHasher` collision remains the accepted T-259-02 risk. |

No HIGH-severity open threats. This plan is test + docs + gate; the sole runtime risk
(cross-request leakage) is structurally mitigated by the Plan 01 per-request store and is
re-exercised by the SC-3 fresh-scope harness.
</threat_model>

<verification>
- `cargo test -p ferro-rs render_path_single_fetch --all-features` — SC-3 passes (counter == 1).
- `cargo fmt --all -- --check` — formatting clean.
- `cargo clippy --all --all-targets --all-features -- -D warnings` — no warnings.
- `cargo test --all-features` — full suite green.
- `cargo doc -p ferro-rs --no-deps` — memo API fully documented.
</verification>

<success_criteria>
- SC-3 proven: N intents over one key issue exactly one underlying fetch through the memo store,
  via a genuine multi-intent render (not a fabricated in-renderer fetch).
- All exported memo symbols carry neutral `///` rustdoc.
- The CI-exact gate (fmt + clippy `--all-features` + test `--all-features`) is green.
- No new crate; no unrelated tree churn.
</success_criteria>

<output>
After completion, create `.planning/phases/259-request-scoped-memoization/259-03-SUMMARY.md`
</output>
