---
phase: 258-mcp-surface-docs-publish
plan: 03
type: execute
wave: 2
depends_on: ["258-01", "258-02"]
files_modified:
  - Cargo.toml
autonomous: false
requirements: [POS-13]
must_haves:
  truths:
    - "The projection-derived /cassa flip stands (verified pre-satisfied from Phase 257 — evidence recorded)"
    - "The full CI-exact gate (fmt, clippy --all --all-targets --all-features, test --all-features, cargo doc -D warnings) is green before any push"
    - "The workspace version is bumped 0.2.88 → 0.2.89 (verified 0.2.88 is crates.io max first)"
    - "local master is fast-forwarded from the branch head (from main repo root, HEAD=master asserted) and pushed; CI publishes"
    - "BOTH ferro-rs 0.2.89 AND ferro-payments 0.1.6 are confirmed live on crates.io (via API, not stale refs)"
    - "gestiscilo receives a handoff brief only — no consumer-tree edits"
  artifacts:
    - path: "Cargo.toml"
      provides: "workspace version bump 0.2.89 = the CI publish trigger"
      contains: "version ="
  key_links:
    - from: "Cargo.toml 0.2.89 bump commit"
      to: "CI publish.yml waves"
      via: "push to master triggers the publish waves (ferro-rs + ferro-payments 0.1.6 rider)"
      pattern: "version ="
---

<objective>
Close the v16.6 POS Component Suite milestone: verify the Phase 257 `/cassa` projection-derived flip
still stands, run the full CI-exact gate green, bump the workspace version 0.2.88 → 0.2.89, and — after
an operator checkpoint — fast-forward local master and push so CI performs the single crates.io
publish. The push carries the already-committed ferro-payments 0.1.6 return-URL-seam rider (D-13), so
verification must confirm BOTH ferro-rs 0.2.89 and ferro-payments 0.1.6. gestiscilo's register phase
pins the published crate via a brief-only handoff (D-17).

Purpose: POS-13 — the milestone exit: gate + single publish.
Output: version bump commit, pushed master, verified dual-crate publish, gestiscilo handoff brief.
Guards: operator-gated publish (D-16); stage specific files only, stray planning artifacts excluded
(D-18); verify via crates.io/gh API, never local origin refs; run
`git update-ref refs/remotes/origin/master HEAD` after a verified push.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/258-mcp-surface-docs-publish/258-CONTEXT.md
@.planning/phases/258-mcp-surface-docs-publish/258-RESEARCH.md
@.planning/phases/258-mcp-surface-docs-publish/258-PATTERNS.md
@.planning/phases/253-mcp-surface-docs-publish/253-05-PLAN.md

<interfaces>
<!-- Publish mechanics (verified this branch). Re-read live values at gate time — refs/memory lie. -->

Branch topology (D-12): work sits on `feat/billable-return-url-seam`; remote master is an ANCESTOR of
HEAD (clean fast-forward); local master is behind HEAD and holds nothing unique.
  Sequence: land 258-01/258-02 + the bump on this branch → from the MAIN REPO ROOT, `git checkout master`
  and ASSERT `git rev-parse --abbrev-ref HEAD == master` (feedback_worktree_merge_cwd_trap) → fast-forward
  master to the branch head → push master.

Versions (verified in-tree): Cargo.toml `[workspace.package] version = "0.2.88"` (line 47);
  `ferro-payments/Cargo.toml version = "0.1.6"` (line 3, already committed in 4477e394 — do NOT bump again).
  crates.io currently carries ferro-rs 0.2.88 and ferro-payments 0.1.5 → this push publishes 0.2.89 + 0.1.6.

Gate commands (CI-exact, D-15; CPU-serialized — ONE at a time, never chained/parallel):
```
cargo fmt --all -- --check
cargo clippy --all --all-targets --all-features -- -D warnings
cargo test --all-features
cargo doc --no-deps --all-features -D warnings      # CI matrix is wider than the 3-command gate
```
Re-run `cargo fmt --all -- --check` after ANY hand-edit (a hand-Write file has broken a prior publish).
Before `cargo test --all-features`: `df -h`; if < 10G free, `rm -rf target/debug/deps` (safe, rebuilt).
Schema-export churn: `cargo test` regenerates docs/protocol/schemas/*.json — `git checkout` it unless a
real content diff appears (no ServiceDef field changes this phase).

Version check (do not trust origin/master or memory):
```
curl -s https://crates.io/api/v1/crates/ferro-rs | jq -r .crate.max_version        # expect 0.2.88
curl -s https://crates.io/api/v1/crates/ferro-payments | jq -r .crate.max_version   # expect 0.1.5
git tag | grep -E "^v0\.2\."
```

Remote-divergence check immediately before push (RESEARCH Pitfall 5):
```
git fetch https://github.com/albertogferrario/ferro.git master
git log HEAD..FETCH_HEAD --oneline      # if non-empty: git merge FETCH_HEAD, keep higher Cargo.toml version
```

Push (SSH denied — gh HTTPS credential helper) + ref correction:
```
git -c credential.helper='!gh auth git-credential' push https://github.com/albertogferrario/ferro.git master
git update-ref refs/remotes/origin/master HEAD
```

Post-publish verification (BOTH crates + release tag):
```
curl -s https://crates.io/api/v1/crates/ferro-rs | jq -r .crate.max_version        # == 0.2.89
curl -s https://crates.io/api/v1/crates/ferro-payments | jq -r .crate.max_version   # == 0.1.6
gh api repos/albertogferrario/ferro/releases/latest --jq .tag_name                  # == v0.2.89
```

publish.yml: NO changes needed. Waves: 1a ferro-json-ui; 1b ferro-stripe/ferro-projections/…; 1c
ferro-payments; 2 ferro-rs/ferro-mcp/ferro-mcp-oauth/ferro-mcp-server; 3 ferro-cli. `ferro-a2ui` stays
`publish = false` and out of publish.yml (D-14) — no new crates, no wave changes, no token bootstrap.

/cassa flip (world-state correction 3): already shipped in Phase 257 — `app/src/views/cassa.json` is
DELETED and `app/src/controllers/cassa.rs` renders via `register_template()` + `JsonUiRenderer`. This
phase VERIFIES it stands (records evidence), it does not re-do it.

Pre-existing flaky test to NOT chase at the gate:
`commands::serve::tests::spawn_child_with_prefix_uses_new_process_group` (timing race, unrelated).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify /cassa flip stands + full CI-exact gate + 0.2.89 version bump (no push)</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (re-read the current `[workspace.package] version` value)
    - ferro-payments/Cargo.toml (confirm it is already 0.1.6 — do not touch)
    - app/src/controllers/cassa.rs (confirm the register_template()/JsonUiRenderer flip is in place)
    - CLAUDE.md (Testing & Linting — the exact CI commands)
    - .planning/phases/258-mcp-surface-docs-publish/258-RESEARCH.md (Publish Mechanics + Common Pitfalls 1,2,6,7)
  </read_first>
  <action>
Step A — verify /cassa flip stands (D-01 evidence, world-state correction 3). Confirm
`app/src/views/cassa.json` is absent (`ls app/src/views/cassa.json` → no such file) and
`app/src/controllers/cassa.rs` calls `register_template()` + `derive_intents` + `JsonUiRenderer`
(`grep -n "register_template\|JsonUiRenderer" app/src/controllers/cassa.rs`). Run the cassa
integration/e2e test if one exists (`cargo test -p app -- cassa` or the app harness test). Record as
"pre-satisfied — shipped Phase 257, UAT passed". Do NOT re-implement.

Step B — full CI-exact gate, ONE command at a time (CPU-serialization — never chain/parallelize cargo
runs; reuse Wave-1 per-crate evidence rather than re-running it needlessly):
  1. `cargo fmt --all -- --check` — if it fails, `cargo fmt --all`, inspect the diff, re-run --check.
  2. `cargo clippy --all --all-targets --all-features -- -D warnings` — fix every warning (the two new
     ferro-mcp public types from Plan 01 need `///` docs or step 4 fails).
  3. `df -h` first; if < 10G free `rm -rf target/debug/deps`; then `cargo test --all-features` — all
     pass EXCEPT the known-flaky `spawn_child_with_prefix_uses_new_process_group` (note, do not chase).
     After it, `git checkout -- docs/protocol/schemas/` unless `git diff docs/protocol/schemas/` shows a
     real content change (none expected).
  4. `cargo doc --no-deps --all-features -D warnings` — resolve any missing-docs on the new public types.

Step C — determine + apply the version bump (D-11). Verify crates.io max is 0.2.88 BEFORE bumping
(`curl -s https://crates.io/api/v1/crates/ferro-rs | jq -r .crate.max_version`). Edit
`Cargo.toml` `[workspace.package] version` from `0.2.88` to `0.2.89`. Confirm the chosen version does
not already exist in `git tag`. Do NOT touch `ferro-payments/Cargo.toml` (already 0.1.6). Update the
lockfile (`cargo update -w` or a targeted `cargo build`) so Cargo.lock matches. Re-run
`cargo fmt --all -- --check` once more.

Step D — commit the bump on `feat/billable-return-url-seam`, staging SPECIFIC files only (D-18): stage
`Cargo.toml` and `Cargo.lock` (and only the Wave-1 source/docs files if not already committed). Do NOT
`git add -A` — stray untracked planning artifacts (phases 209/212/214/231/232/238/251/252/253 files,
`app/tmp/`) and the phantom `planning/phases/158-…` deletion stay OUT. Commit message:
`docs(258): v16.6 POS component suite publish — bump workspace to 0.2.89` (no co-author lines).
DO NOT PUSH — the push is gated by Task 2's operator approval.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && grep -n '^version = "0.2.89"' Cargo.toml</automated>
  </verify>
  <done>/cassa flip verified standing (evidence recorded); full CI-exact gate green (fmt/clippy --all-features/test --all-features/doc); workspace at 0.2.89 committed locally on the feat branch; ferro-payments untouched at 0.1.6; not pushed; no stray files staged.</done>
  <acceptance_criteria>
    - `ls app/src/views/cassa.json` reports no such file (flip stands)
    - `grep -c "register_template" app/src/controllers/cassa.rs` is ≥ 1
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy --all --all-targets --all-features -- -D warnings` exits 0
    - `cargo test --all-features` passes (flaky serve PGID test excepted/noted)
    - `cargo doc --no-deps --all-features -D warnings` exits 0
    - `grep -n '^version = "0.2.89"' Cargo.toml` returns a match
    - `grep -n '^version = "0.1.6"' ferro-payments/Cargo.toml` returns a match (unchanged)
    - `0.2.89` does not already exist in `git tag` output
    - `git show --stat HEAD` lists only Cargo.toml/Cargo.lock (+ any Wave-1 files), NO stray planning artifacts
    - `git log --oneline -1` shows the bump commit; nothing pushed
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Pre-publish checklist + operator publish approval</name>
  <what-built>
    All 258 code/docs have landed and the full CI-exact gate is green: the json_ui_catalog BUILDER_API +
    RULE_COMPONENTS additions and the generation_context register_composition guidance (Plan 01), the
    five component sections + register projection docs with a clean mdBook build (Plan 02), and a local
    (unpushed) workspace bump to 0.2.89 on `feat/billable-return-url-seam` ready to trigger CI publish.
    The branch base also carries the already-committed ferro-payments 0.1.6 return-URL-seam rider.
  </what-built>
  <how-to-verify>
    Present this pre-publish checklist to the operator and wait for explicit approval:
    1. Gate results: fmt / clippy --all-features / test --all-features / doc -D warnings all green
       (paste the four exit-0 confirmations; note the excepted known-flaky serve PGID test).
    2. Versions shipping: ferro-rs 0.2.88 → 0.2.89 (this phase); ferro-payments 0.1.5 → 0.1.6 (RIDER,
       already committed in 4477e394 — return-URL seam). Re-confirm crates.io currently shows ferro-rs
       0.2.88 / ferro-payments 0.1.5 (`curl … | jq -r .crate.max_version` for both).
    3. What ships: json_ui_catalog + generation_context register guidance; five component docs +
       register projection docs; NO renderer/component/lint/projection code changes (verify-only phase
       for those). `ferro-a2ui` stays `publish = false` / out of publish.yml (D-14); no new crates → no
       publish.yml wave changes.
    4. Staged files: `git show --stat HEAD` lists ONLY Cargo.toml/Cargo.lock (+ Wave-1 source/docs);
       no stray planning artifacts (D-18).
    5. Approve the irreversible push to master (CI will publish both crates to crates.io + tag v0.2.89 +
       build binaries + brew tap).
  </how-to-verify>
  <action>
    Pause here (blocking checkpoint). Present the pre-publish checklist above and wait for explicit
    operator approval before Task 3 pushes. Do NOT push, publish, or run any irreversible git/network
    command until the operator responds with the resume signal. If the operator reports an issue, stop
    and surface it rather than proceeding.
  </action>
  <resume-signal>Type "approved" to authorize the publish push, or describe issues to fix first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Fast-forward master, push, verify both crates, hand off gestiscilo brief</name>
  <files>Cargo.toml</files>
  <read_first>
    - .planning/phases/258-mcp-surface-docs-publish/258-RESEARCH.md (Publish Mechanics — push flow, divergence check, dual-crate verification; Pitfalls 5,7,8)
    - .planning/phases/258-mcp-surface-docs-publish/258-CONTEXT.md (D-12 topology, D-13 rider, D-17 handoff)
    - .planning/STATE.md (Operator actions pending — handoff context)
  </read_first>
  <action>
After operator approval:
1. From the MAIN REPO ROOT (not a worktree), fast-forward local master to the branch head
   (feedback_worktree_merge_cwd_trap):
   ```
   git checkout master
   git rev-parse --abbrev-ref HEAD    # MUST print `master` — abort if not
   git merge --ff-only feat/billable-return-url-seam
   ```
   `--ff-only` guarantees no merge commit; if it refuses, the branch is not a clean descendant — stop
   and surface it (do not force).
2. Remote-divergence check immediately before the push (RESEARCH Pitfall 5):
   ```
   git fetch https://github.com/albertogferrario/ferro.git master
   git log HEAD..FETCH_HEAD --oneline
   ```
   If non-empty: `git merge FETCH_HEAD`, resolve any Cargo.toml version conflict keeping the HIGHER
   version, re-run `cargo fmt --all -- --check`.
3. Push master via the gh HTTPS credential helper (SSH denied):
   ```
   git -c credential.helper='!gh auth git-credential' push https://github.com/albertogferrario/ferro.git master
   ```
4. Correct the recurrently-lying local ref: `git update-ref refs/remotes/origin/master HEAD`.
5. Watch CI publish, then verify BOTH crates + the release tag via API (NOT stale refs; D-13 requires
   both — RESEARCH Pitfall 8):
   ```
   curl -s https://crates.io/api/v1/crates/ferro-rs | jq -r .crate.max_version        # == 0.2.89
   curl -s https://crates.io/api/v1/crates/ferro-payments | jq -r .crate.max_version   # == 0.1.6
   gh api repos/albertogferrario/ferro/releases/latest --jq .tag_name                  # == v0.2.89
   ```
   If CI's auto-bump path produced a different final ferro-rs version, record the ACTUAL published value.
6. Write the gestiscilo register-phase handoff BRIEF (D-17 — brief only, never edit the consumer tree):
   a short note in 258-03-SUMMARY.md (or a dedicated handoff file under this phase dir) stating the
   ferro-rs version to pin (0.2.89) and the public contracts now available for the register: the five
   builtins (TileGrid/SelectionPanel/FilterTabs/QuantityStepper/Numpad), `register_template()`, the
   `fill_viewport`/`.each()` builder additions, the four `register-*` design-lint rules, the
   generation_context register_composition guidance, and the docs/src component + layout sections.
Do not run `/gsd-complete-milestone` here — archival is deferred bookkeeping (CONTEXT deferred list).
  </action>
  <verify>
    <automated>curl -s https://crates.io/api/v1/crates/ferro-rs | jq -r .crate.max_version</automated>
  </verify>
  <done>master fast-forwarded from the branch head (HEAD=master asserted) and pushed; crates.io shows ferro-rs 0.2.89 AND ferro-payments 0.1.6; GitHub release tag v0.2.89 exists; local origin/master ref corrected; gestiscilo handoff brief written.</done>
  <acceptance_criteria>
    - `curl -s https://crates.io/api/v1/crates/ferro-rs | jq -r .crate.max_version` equals `0.2.89`
    - `curl -s https://crates.io/api/v1/crates/ferro-payments | jq -r .crate.max_version` equals `0.1.6`
    - `gh api repos/albertogferrario/ferro/releases/latest --jq .tag_name` returns `v0.2.89`
    - `git rev-parse refs/remotes/origin/master` equals `git rev-parse HEAD`
    - `git rev-parse --abbrev-ref HEAD` is `master` (fast-forward landed on the right branch)
    - A gestiscilo handoff brief exists naming the pinned version (0.2.89) and the register public contracts
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local workspace → crates.io / GitHub (CI publish) | The push exposes code to public registries and is irreversible once published; a supply-chain boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-258-07 | Tampering | Publishing a broken build to crates.io | mitigate | Full CI-exact gate (fmt/clippy --all-features/test --all-features/doc -D warnings) green before the push; publish is blocked behind the gate AND the operator checkpoint (Task 2) |
| T-258-08 | Repudiation | Publishing the wrong tree or version — stale local refs, unreviewed rider commits swept in | mitigate | Version verified against the crates.io API + `git tag` at gate time (not local refs); master is fast-forwarded `--ff-only` from the known branch head with `HEAD=master` asserted from the main repo root; the pre-publish checklist enumerates exactly what ships incl. the ferro-payments 0.1.6 rider; post-publish asserts BOTH crate versions |
| T-258-09 | Elevation of Privilege | Cross-repo edits to the gestiscilo consumer tree from this session | mitigate | D-17: the handoff is a brief only; no edits to the consumer repo or its planning |
| T-258-10 | Information Disclosure | cargo-deny / license or advisory regressions shipped in the publish | accept | CI runs cargo-deny; no new external crates this phase (docs + MCP output additions + version bump only), so the advisory/license surface is unchanged; `ferro-a2ui` stays `publish = false` |
| T-258-11 | Tampering | Stray untracked planning artifacts committed into the publish commit | mitigate | D-18: stage specific files only (`Cargo.toml`/`Cargo.lock` + Wave-1 files); acceptance criterion asserts `git show --stat` lists no stray artifacts; never `git add -A` |
</threat_model>

<verification>
- CI-exact gate green (all four commands) before push; disk checked before the full test run.
- /cassa flip verified standing (pre-satisfied, evidence recorded — not re-done).
- Post-push: crates.io shows ferro-rs 0.2.89 AND ferro-payments 0.1.6; gh release tag v0.2.89 exists.
- `origin/master` local ref corrected to HEAD; gestiscilo handoff brief written; no consumer-tree edits.
</verification>

<success_criteria>
POS-13: the projection-derived `/cassa` stands, the full CI-exact gate is green, and a single operator-
approved crates.io publish ships ferro-rs 0.2.89 (with the ferro-payments 0.1.6 rider) — closing the
v16.6 POS Component Suite milestone and unblocking gestiscilo's register phase.
</success_criteria>

<output>
After completion, create `.planning/phases/258-mcp-surface-docs-publish/258-03-SUMMARY.md`
(include the /cassa pre-satisfied evidence and the gestiscilo Phase handoff brief).
</output>
