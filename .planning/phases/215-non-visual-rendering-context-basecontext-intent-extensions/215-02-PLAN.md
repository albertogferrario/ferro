---
phase: 215-non-visual-rendering-context-basecontext-intent-extensions
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - ferro-json-ui/src/projection/mod.rs
  - ferro-json-ui/src/projection/builder.rs
  - ferro-mcp/src/tools/render_projection.rs
  - ferro-mcp/src/tools/generate_projection.rs
  - ferro-mcp/src/tools/projection_coverage.rs
  - ferro-ai/tests/projection_roundtrip.rs
  - ferro-mcp/tests/agent_harness.rs
autonomous: true
requirements: [CHAN-01, CHAN-02]

must_haves:
  truths:
    - "VisualContext embeds base: BaseContext (single source of truth for intent_index + current_state + the two new fields)"
    - "VisualContext::default() still produces intent_index 0, current_state None, mode Display, templates None"
    - "JsonUiRenderer and the ferro-json-ui builder compile and all existing tests pass"
    - "ferro-mcp tools emit lowercase intent labels via Intent::label() (no format!(\"{:?}\", intent) label sites remain)"
    - "ferro-mcp-server still compiles and its tests pass unchanged"
  artifacts:
    - path: "ferro-json-ui/src/projection/mod.rs"
      provides: "VisualContext { base: BaseContext, mode, templates }"
      contains: "pub base: BaseContext"
    - path: "ferro-json-ui/src/projection/builder.rs"
      provides: "ctx.base.intent_index / ctx.base.current_state access"
      contains: "ctx.base."
    - path: "ferro-mcp/src/tools/render_projection.rs"
      provides: "label()-based intent strings + updated test expectation (browse)"
      contains: ".label().to_string()"
  key_links:
    - from: "ferro-mcp IntentInfo.intent"
      to: "Intent::label()"
      via: ".label().to_string()"
      pattern: "\\.label\\(\\)\\.to_string\\(\\)"
    - from: "VisualContext.base"
      to: "BaseContext"
      via: "embedded field"
      pattern: "pub base: BaseContext"
---

<objective>
Adopt the Plan 01 `ferro-projections` surface across downstream consumers,
completing CHAN-01 (VisualContext single-source-of-truth) and CHAN-02 (label
migration) at the consumer level:

1. Refactor `VisualContext` (`ferro-json-ui/src/projection/mod.rs`) to EMBED
   `base: BaseContext` instead of re-declaring `intent_index` / `current_state`,
   collapsing the two parallel sources of truth into one. (D-02)
2. Migrate `builder.rs` field access: `ctx.intent_index` → `ctx.base.intent_index`,
   `ctx.current_state` → `ctx.base.current_state`. Migrate all `VisualContext`
   struct-literal sites (in builder.rs tests + external test/tool sites) to the
   embedded shape. (D-02, Pitfall 1/2)
3. Migrate the FOUR `format!("{:?}", *.intent)` LABEL sites in `ferro-mcp` to
   `.label().to_string()`, and update the one test that asserts the PascalCase
   value ("Browse" → "browse"). (D-07)

Purpose: prove the new surface is source-compatible with the existing visual + MCP
renderers, with the only behavioral change being the intended PascalCase→lowercase
label normalization.

Output: embedded `VisualContext`, migrated `builder.rs`, four migrated mcp label
sites, one updated test expectation. `ferro-json-ui` + `ferro-mcp` + `ferro-mcp-server`
build and test green.

Boundary: do NOT change `ProjectionError::EmptyIntents` or reroute the visual
renderer through `Error::NoIntents` (D-09). Do NOT migrate the internal
`{:?}` debug uses that are NOT labels — leave `catalog.rs` (lines ~660, ~1090),
the `pub(crate)` sketch renderers (`render/sketch/cli.rs`, `mobile.rs`), and
`intent_layout.rs:163/167` assert messages untouched. Do NOT keep the flat-field
fallback unless embedding genuinely cascades out of budget (document the reason in
the SUMMARY if you fall back).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/215-non-visual-rendering-context-basecontext-intent-extensions/215-CONTEXT.md
@.planning/phases/215-non-visual-rendering-context-basecontext-intent-extensions/215-RESEARCH.md
@.planning/phases/215-non-visual-rendering-context-basecontext-intent-extensions/215-PATTERNS.md
@.planning/phases/215-non-visual-rendering-context-basecontext-intent-extensions/215-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (Wave 1): BaseContext now carries two extra fields. -->
ferro-projections BaseContext (post Plan 01):
```rust
#[derive(Debug, Clone, Default)]
pub struct BaseContext {
    pub intent_index: usize,
    pub current_state: Option<String>,
    pub evaluated_guards: std::collections::HashMap<String, bool>,
    pub verbosity: Verbosity, // Verbosity::Full default
}
```
`Intent::label() -> &str` is available (returns "browse".."track", or Custom inner string).

<!-- Current VisualContext (ferro-json-ui/src/projection/mod.rs ~lines 44-68) — to be embedded. -->
```rust
#[derive(Debug, Clone)]
pub struct VisualContext {
    pub intent_index: usize,
    pub current_state: Option<String>,
    pub mode: RenderMode,
    pub templates: Option<ThemeTemplates>,
}
impl Default for VisualContext {
    fn default() -> Self {
        Self { intent_index: 0, current_state: None, mode: RenderMode::Display, templates: None }
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Embed base: BaseContext in VisualContext and migrate builder.rs access + struct-literal sites</name>
  <files>ferro-json-ui/src/projection/mod.rs, ferro-json-ui/src/projection/builder.rs, ferro-ai/tests/projection_roundtrip.rs, ferro-mcp/tests/agent_harness.rs, ferro-mcp/src/tools/render_projection.rs</files>
  <read_first>
    - ferro-json-ui/src/projection/mod.rs — `VisualContext` (lines ~44-68) and its
      hand-written `impl Default` and the `use ferro_projections::render::{...}` import;
      the `visual_context_default_has_sensible_values` test (~lines 162-168).
    - ferro-json-ui/src/projection/builder.rs — production access sites at lines 67, 94,
      485 (`ctx.intent_index` / `ctx.current_state`) and the 8 test struct-literal sites
      (lines 846, 870, 891, 944, 983, 1011, 1093, 1133). Note the file is
      `#![cfg(feature = "projections")]` — tests need `--all-features`.
    - External struct-literal sites: ferro-ai/tests/projection_roundtrip.rs:33,
      ferro-mcp/tests/agent_harness.rs:275, and ferro-mcp/src/tools/render_projection.rs:72
      (the render_projection.rs:72 site is a PRODUCTION struct literal constructing
      VisualContext — it MUST be migrated in THIS task so ferro-mcp keeps compiling.
      This is distinct from the label sites at render_projection.rs:94/102 handled in Task 2).
    - 215-PATTERNS.md §"ferro-json-ui/src/projection/mod.rs" and §"...builder.rs" — exact
      before/after for the struct, the Default impl, the import, and Pattern A / Pattern B
      struct-literal migrations.
    - 215-01-SUMMARY.md — confirms the two new BaseContext fields and that Verbosity has no serde.
  </read_first>
  <action>
    EMBED approach (D-02, preferred). In `ferro-json-ui/src/projection/mod.rs`:

    1. Ensure `BaseContext` is imported. Change the existing use to:
       `use ferro_projections::render::{BaseContext, Renderer};`
       (add `BaseContext` to whatever is already imported from that path).

    2. Replace `VisualContext`'s flat fields with the embedded base (keep `#[derive(Debug, Clone)]`,
       keep mode + templates as visual-only):
       ```rust
       #[derive(Debug, Clone)]
       pub struct VisualContext {
           /// Modality-agnostic context (intent index, state, guards, verbosity).
           pub base: BaseContext,
           /// Display or Input mode.
           pub mode: RenderMode,
           /// Optional theme template overrides.
           pub templates: Option<ThemeTemplates>,
       }
       ```

    3. Keep the HAND-WRITTEN `impl Default` (do NOT switch to `#[derive(Default)]` —
       `RenderMode` has no Default):
       ```rust
       impl Default for VisualContext {
           fn default() -> Self {
               Self {
                   base: BaseContext::default(),
                   mode: RenderMode::Display,
                   templates: None,
               }
           }
       }
       ```

    4. Update the `visual_context_default_has_sensible_values` test field paths:
       `ctx.intent_index` → `ctx.base.intent_index`,
       `ctx.current_state` → `ctx.base.current_state`. Leave the mode/templates assertions.

    In `ferro-json-ui/src/projection/builder.rs`, migrate production access:
    - line ~67: `ctx.intent_index` → `ctx.base.intent_index`
    - line ~94: `ctx.intent_index` → `ctx.base.intent_index`
    - line ~485: `ctx.current_state.clone()` → `ctx.base.current_state.clone()`
    Leave `ctx.mode` and `ctx.templates` unchanged (visual-only). Keep
    `emit_kanban_root`'s signature `&VisualContext` (just change the field access).

    Migrate the 8 builder.rs test struct-literal sites and the 3 external sites
    (ferro-ai/tests/projection_roundtrip.rs:33, ferro-mcp/tests/agent_harness.rs:275,
    ferro-mcp/src/tools/render_projection.rs:72) to the embedded shape. The
    render_projection.rs:72 site is the PRODUCTION VisualContext struct literal that
    feeds the render tool — it MUST gain the embedded `base` field in THIS task, e.g.:
    ```rust
    // BEFORE (render_projection.rs:72):
    VisualContext { intent_index, current_state, mode: RenderMode::Display, ..Default::default() }
    // AFTER:
    VisualContext {
        base: BaseContext { intent_index, current_state, ..Default::default() },
        mode: RenderMode::Display,
        ..Default::default()
    }
    ```
    General pattern for all struct-literal sites:
    ```rust
    // BEFORE:
    VisualContext { intent_index: <expr>, mode: RenderMode::Display, ..Default::default() }
    // AFTER:
    VisualContext {
        base: BaseContext { intent_index: <expr>, ..Default::default() },
        mode: RenderMode::Display,
        ..Default::default()
    }
    ```
    Sites that set `current_state` move it inside the `base { .. }` block too. Sites
    that only use `..Default::default()` with no flat field need no inner base block.
    Add `use ferro_projections::render::BaseContext;` (or the crate's existing path)
    to any test/external/production file that now names `BaseContext` (including
    render_projection.rs).

    Let the compiler enumerate missed sites — "no field `intent_index` on type
    `VisualContext`" errors are the checklist. Resolve every one. This task MUST
    leave ferro-mcp compiling — verify with `cargo build -p ferro-mcp --tests`
    BEFORE declaring the task done, so the line-72 site cannot be silently skipped.

    Do NOT touch `ProjectionError::EmptyIntents` (D-09). Do NOT touch the
    render_projection.rs:94/102 LABEL sites here — those are Task 2's scope.
  </action>
  <verify>
    <automated>cargo test -p ferro-json-ui --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub base: BaseContext" ferro-json-ui/src/projection/mod.rs` matches.
    - `! grep -n "pub intent_index: usize" ferro-json-ui/src/projection/mod.rs` (the flat field is gone from VisualContext).
    - `grep -c "ctx.base.intent_index" ferro-json-ui/src/projection/builder.rs` ≥ 2.
    - `grep -n "ctx.base.current_state" ferro-json-ui/src/projection/builder.rs` matches.
    - `grep -n "impl Default for VisualContext" ferro-json-ui/src/projection/mod.rs` matches (hand-written Default retained; no `#[derive(Default)]` on VisualContext).
    - `grep -n "base: BaseContext" ferro-mcp/src/tools/render_projection.rs` matches (the line-72 production struct literal was migrated to the embedded shape).
    - `cargo test -p ferro-json-ui --all-features` exits 0.
    - `cargo build -p ferro-ai --tests` compiles (external struct-literal site migrated).
    - `cargo build -p ferro-mcp --tests` compiles (the render_projection.rs:72 struct literal + agent_harness.rs:275 site migrated; this catches the line-72 site within the task, before Task 2 runs).
  </acceptance_criteria>
  <done>VisualContext embeds base: BaseContext; builder.rs production + all struct-literal sites (builder tests + ferro-ai + ferro-mcp, including the render_projection.rs:72 production literal) migrated; VisualContext::default() preserves the old values; ferro-json-ui tests pass with --all-features; ferro-mcp builds with --tests.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the four ferro-mcp {:?} label sites to Intent::label() and update the test expectation</name>
  <files>ferro-mcp/src/tools/render_projection.rs, ferro-mcp/src/tools/generate_projection.rs, ferro-mcp/src/tools/projection_coverage.rs</files>
  <read_first>
    - ferro-mcp/src/tools/render_projection.rs — the `all_intents` mapping (~line 94) and
      the `RenderResult.intent` field (~line 102); the test at ~line 488 that asserts
      `intent: "Browse".to_string()`. (The line-72 VisualContext struct literal was already
      migrated in Task 1 — do not touch it again here.)
    - ferro-mcp/src/tools/generate_projection.rs — the `intent_infos` mapping (~line 89).
    - ferro-mcp/src/tools/projection_coverage.rs — `derive_primary_intent` (~line 173),
      the user-facing `ModelCoverage.primary_intent: Option<String>` field.
    - 215-PATTERNS.md §"ferro-mcp/src/tools/*" — exact before/after for each of the 4 sites
      and the test-expectation change (the PascalCase→lowercase catch).
    - 215-RESEARCH.md Pitfall 5/6 — confirms exactly 4 mandatory sites; catalog.rs,
      sketch renderers, and intent_layout.rs:163/167 are NOT labels and stay as-is.
  </read_first>
  <action>
    Migrate each label site from `format!("{:?}", <expr>.intent)` to
    `<expr>.intent.label().to_string()` (`.to_string()` because the target fields are
    `String`; `label()` returns `&str`):

    1. ferro-mcp/src/tools/render_projection.rs ~line 94 (`all_intents` map):
       `intent: format!("{:?}", is.intent),` → `intent: is.intent.label().to_string(),`
    2. ferro-mcp/src/tools/render_projection.rs ~line 102 (`RenderResult.intent`):
       `intent: format!("{:?}", selected.intent),` → `intent: selected.intent.label().to_string(),`
    3. ferro-mcp/src/tools/generate_projection.rs ~line 89 (`intent_infos` map):
       `intent: format!("{:?}", score.intent),` → `intent: score.intent.label().to_string(),`
    4. ferro-mcp/src/tools/projection_coverage.rs ~line 173 (`derive_primary_intent`):
       `Some(format!("{:?}", primary.intent)),` → `Some(primary.intent.label().to_string()),`

    Update the one test that pins the old Debug/PascalCase value in
    ferro-mcp/src/tools/render_projection.rs (~line 488):
    `intent: "Browse".to_string(),` → `intent: "browse".to_string(),`
    Scan that test (and any sibling assertion in the same module) for other PascalCase
    intent string literals being compared against the migrated output and lowercase them
    to match `label()` (e.g. any `"Process"`/`"Summarize"` expectation tied to these fields).

    Leave the NON-label `{:?}` uses untouched: `ferro-projections/tests/catalog.rs`
    (~660, ~1090), `ferro-projections/src/render/sketch/cli.rs` + `mobile.rs` (pub(crate)
    sketches), and `ferro-json-ui/.../intent_layout.rs:163/167` (assert! messages).
  </action>
  <verify>
    <automated>cargo test -p ferro-mcp</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.label().to_string()" ferro-mcp/src/tools/render_projection.rs` ≥ 2.
    - `grep -n "\.label().to_string()" ferro-mcp/src/tools/generate_projection.rs` matches.
    - `grep -n "\.label().to_string()" ferro-mcp/src/tools/projection_coverage.rs` matches.
    - No label-site `{:?}` remains in ferro-mcp tool source: `! grep -rn 'format!("{:?}", [a-z_]*\.intent)' ferro-mcp/src/tools` returns no matches.
    - The test expectation is lowercase: `grep -n 'intent: "browse".to_string()' ferro-mcp/src/tools/render_projection.rs` matches and `! grep -n 'intent: "Browse".to_string()' ferro-mcp/src/tools/render_projection.rs`.
    - `cargo test -p ferro-mcp` exits 0.
  </acceptance_criteria>
  <done>All four ferro-mcp label sites emit Intent::label() lowercase strings; the render_projection test asserts "browse"; no label-deriving {:?} remains in ferro-mcp tools; ferro-mcp tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ferro-mcp tool output → MCP client | `IntentInfo.intent` / `ModelCoverage.primary_intent` strings cross to the consuming agent. This plan changes their VALUE (PascalCase→lowercase) but not their nature; both pre- and post-migration values are non-sensitive, derived from the frozen intent vocabulary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-215-03 | Tampering | label-value change breaks a downstream consumer that parsed "Browse" | mitigate | The only emitted-value change is PascalCase→lowercase, and lowercase already matches the serde-canonical strings every other surface emits; the one in-tree test asserting "Browse" is updated to "browse". No external consumer contract documents PascalCase labels (they were an accidental Debug artifact). |
| T-215-04 | Information disclosure | VisualContext now carries evaluated_guards | accept | The visual renderer ignores `evaluated_guards` and `verbosity` (Plan 01 default = render-all / Full); no new data is emitted by the visual path. |
</threat_model>

<verification>
Run AFTER both tasks, in this order (serialize CPU-heavy ops — one cargo invocation
at a time; reuse each task's crate-scoped evidence rather than re-running per task):

1. Downstream crates green together (the phase's full-suite command):
   `cargo test -p ferro-json-ui --all-features -p ferro-mcp -p ferro-mcp-server`
   (or run them sequentially if a combined invocation is awkward).
2. ferro-mcp-server unchanged and still compiling:
   `cargo build -p ferro-mcp-server` exits 0 (no edits to renderer.rs).
3. No label-deriving Debug format remains in renderer/tool source:
   `! grep -rn 'format!("{:?}", [a-z_]*\.intent)' ferro-mcp/src ferro-json-ui/src`
   returns no matches (catalog.rs tests + sketch renderers + intent_layout asserts are
   not under these paths / are not label sites — confirm none reappear here).

Phase wave-close gate (run once, after both plans complete — NOT per task):
`cargo fmt --all -- --check`, then `cargo clippy --all --all-targets -- -D warnings`,
then `cargo test --all-features`. One at a time; if disk-pressure surfaces (known
ENOSPC pattern), `df` + clean `target/` first. Note: a full `cargo test` may dirty
`docs/protocol/schemas/*.json` (Phase 94 export test) — `git checkout` those, do not
fold them into the phase commit.
</verification>

<success_criteria>
- CHAN-01: `VisualContext` embeds `base: BaseContext`; `VisualContext::default()`
  preserves intent_index 0 / current_state None / mode Display / templates None;
  `ferro-json-ui` + `ferro-mcp` + `ferro-mcp-server` build and test green (the visual
  path's behavior is unchanged — guards/verbosity ignored).
- CHAN-02: the four `ferro-mcp` label sites emit `Intent::label()` lowercase strings;
  no label-deriving `format!("{:?}", *.intent)` remains in renderer/tool source; the
  one render_projection test expectation is updated Browse→browse.
- `ProjectionError::EmptyIntents` and the visual render path are unchanged (D-09).
</success_criteria>

<output>
After completion, create
`.planning/phases/215-non-visual-rendering-context-basecontext-intent-extensions/215-02-SUMMARY.md`.
Record: whether the embed (preferred) or flat fallback was used (and why if fallback);
the count of struct-literal sites migrated; the four label sites + the test-expectation
change; and the results of the wave-close gate (`fmt` / `clippy --all --all-targets
-D warnings` / `test --all-features`). Confirm ferro-mcp-server had zero source edits.
</output>
</task>
</output>
