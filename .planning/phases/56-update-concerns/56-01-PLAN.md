---
phase: 56-update-concerns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/codebase/CONCERNS.md]
autonomous: true
---

<objective>
Update CONCERNS.md to reflect current codebase state after v4.0, v5.0, and v5.1 phases.

Purpose: CONCERNS.md (dated 2026-01-15) is stale — 6 of 8 concerns have been resolved by subsequent phases. Updating it ensures accurate technical debt tracking for future milestone planning.
Output: Accurate CONCERNS.md reflecting what's resolved, what's still open, and any new findings.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all concern statuses against current codebase</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
Systematically verify each concern category in CONCERNS.md against the current codebase state. For each item, confirm resolution or document remaining issues.

**Concerns to verify:**

1. **Error Handling Risks (Unhandled Panics)**
   - `app/src/main.rs` — RESOLVED: no .expect() calls remain
   - `app/src/bootstrap.rs` — RESOLVED: no .expect() calls remain
   - `framework/src/schedule/expression.rs` — STILL OPEN: 18 .unwrap() calls in cron builder methods. Document exact count and nature (builder methods returning known-valid values vs actual risk).

2. **Missing Configuration (.env.example)** — RESOLVED by Phase 54: `ferro-cli/src/templates/files/root/env.example.tpl` exists with all framework env vars documented.

3. **Incomplete Features (TODOs)**
   - `app/src/middleware/share_inertia.rs` — STILL OPEN: 2 TODOs (auth user sharing, flash messages)
   - `framework/src/testing/http.rs` — STILL OPEN: 1 TODO at line ~211 (route through actual handlers)
   - TODOs in template files (scaffold.rs, make.rs) are INTENTIONAL user-facing placeholders, not incomplete features. Remove these from the concern.

4. **Code Quality (Large Files)**
   - `ferro-cli/src/templates/mod.rs` — RESOLVED by Phase 55: reduced from 2,713 to 831 lines, split into 7 submodules
   - `framework/src/testing/factory.rs` (1,281 lines) — Acceptable for test infrastructure, not actionable
   - `framework/src/testing/http.rs` (736 lines) — Acceptable

5. **Security Considerations (Session)**
   - Session rotation on auth — RESOLVED in v4.0: `regenerate_session_id()` called on login, CSRF token regenerated on login/logout
   - `logout_and_invalidate()` provides complete session destruction

6. **COMPONENT_CATALOG duplication** (from PROJECT.md key decisions)
   - STILL OPEN: duplicated in `ferro-cli/src/ai.rs` and `ferro-mcp/src/tools/json_ui_generate.rs` with minor drift (div/section in Text, step param in Input)

7. **Performance, Testing Gaps, Monitoring** — Review these sections for accuracy against v4.0/v5.0 additions (rate limiting, broadcasting, auth all added infrastructure).

Also scan for any NEW concerns introduced by v4.0/v5.0 work that should be documented (search for new .unwrap()/.expect() patterns in recently modified files, large new files, etc.).
  </action>
  <verify>All concern items have been verified with specific file evidence</verify>
  <done>Complete status map of every concern item: RESOLVED (with evidence) or STILL OPEN (with current state)</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite CONCERNS.md with current state</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
Rewrite CONCERNS.md with updated analysis date (2026-02-13). Structure:

**Format for resolved items:** Move to a "Resolved" section at the bottom with brief evidence of resolution (phase that fixed it, current state). Keep this section collapsed with `<details>` tag.

**Format for open items:** Keep in main body with updated status, line counts, and priority. Update the Priority Matrix to reflect only remaining actionable items.

**Specific updates:**

1. **Error Handling Risks** — Remove app/main.rs and app/bootstrap.rs (resolved). Keep schedule/expression.rs with updated unwrap count (18). Note these are mostly in builder convenience methods returning known-valid cron expressions.

2. **Missing Configuration** — Move entirely to Resolved (Phase 54).

3. **Incomplete Features** — Update TODO list. Remove template file TODOs (intentional placeholders). Keep app/middleware/share_inertia.rs (2 TODOs) and framework/testing/http.rs (1 TODO). Lower priority since these are non-blocking.

4. **Code Quality** — Move templates/mod.rs to Resolved (Phase 55, now 831 lines across 7 submodules). Remove factory.rs and http.rs from concerns (acceptable sizes). Note new large file if any found in verification.

5. **Security Considerations** — Update session security to Resolved (v4.0 auth). Keep input validation and dependency security as ongoing practices.

6. **COMPONENT_CATALOG duplication** — Add as new concern item. Document the two locations, the drift (div/section missing in MCP, step param missing in CLI), and recommendation to extract to shared constant or build script.

7. **Performance** — Update with v4.0 additions (rate limiting infrastructure, WebSocket broadcasting). Note connection pool config is now more documented via .env.example.

8. **Testing Gaps** — Update: router integration exists via `TestClient::with_router()`. E2E and load testing still gaps. Note this is acceptable for current project stage.

9. **Monitoring** — Keep as-is, these are future recommendations.

10. **Priority Matrix** — Rebuild with only remaining actionable items:
    - COMPONENT_CATALOG consolidation (Medium effort, Medium impact, P2)
    - Schedule expression unwraps (Low effort, Low impact, P3 — builder methods, not user-facing)
    - Share inertia TODOs (Low effort, Low impact, P3 — sample app only)

Keep the writing scientific and minimalistic (per project style). No marketing language.
  </action>
  <verify>
    - CONCERNS.md has updated analysis date (2026-02-13)
    - All resolved items are in collapsed "Resolved" section
    - Open items have current evidence (line counts, file paths)
    - Priority matrix contains only remaining actionable items
    - No stale references to pre-Phase-54/55 state
  </verify>
  <done>CONCERNS.md accurately reflects current codebase state with clear resolved/open distinction and updated priority matrix</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] CONCERNS.md analysis date is 2026-02-13
- [ ] Every original concern has been addressed (resolved or updated)
- [ ] No references to stale state (2,713 line mod.rs, missing .env.example, etc.)
- [ ] COMPONENT_CATALOG duplication documented as concern
- [ ] Priority matrix reflects only remaining actionable items
- [ ] File reads cleanly with proper markdown formatting
</verification>

<success_criteria>
- All tasks completed
- CONCERNS.md is accurate to current codebase state
- Resolved items clearly separated from open items
- No new stale information introduced
</success_criteria>

<output>
After completion, create `.planning/phases/56-update-concerns/56-01-SUMMARY.md`
</output>
