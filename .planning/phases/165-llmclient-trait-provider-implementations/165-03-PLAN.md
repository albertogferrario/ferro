---
phase: 165-llmclient-trait-provider-implementations
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - ferro-ai/src/client/ollama.rs
autonomous: true
requirements: [AISDK-01]
user_setup: []

must_haves:
  truths:
    - "OllamaClient implements LlmClient and is instantiable as Box<dyn LlmClient>"
    - "OllamaClient::default_model() returns \"llama3.1\""
    - "OllamaClient requires no API key; base_url defaults to http://localhost:11434"
    - "OllamaClient::complete posts to {base_url}/api/chat and extracts message.content"
    - "OllamaClient::complete_stream parses NDJSON line-by-line (NOT SSE) and yields message.content tokens until done==true"
    - "OllamaClient::embed posts to {base_url}/api/embed and returns embeddings[0] as Vec<f32>"
  artifacts:
    - path: "ferro-ai/src/client/ollama.rs"
      provides: "OllamaClient struct + impl LlmClient (complete/complete_stream/embed/default_model)"
      contains: "impl LlmClient for OllamaClient"
  key_links:
    - from: "ferro-ai/src/client/ollama.rs"
      to: "{base_url}/api/chat"
      via: "reqwest POST + bytes_stream NDJSON parse"
      pattern: "/api/chat"
    - from: "ferro-ai/src/client/ollama.rs"
      to: "async_stream::try_stream"
      via: "NDJSON token stream construction"
      pattern: "try_stream"
---

<objective>
Implement `OllamaClient` into the empty `client/ollama.rs` stub Plan 01 created. Ollama is the no-auth,
local-default provider; its streaming path is NDJSON (newline-delimited JSON) over `bytes_stream()` —
explicitly NOT SSE — so it uses `async_stream::try_stream!` rather than `reqwest-eventsource`.

Purpose: completes the three-provider set for `Box<dyn LlmClient>` dispatch (SC#2) and the Wave 3
`AiConfig` factory. This plan edits ONLY `client/ollama.rs` (Plan 01 declared `pub mod ollama;` and left the
stub), so it runs in parallel with the Anthropic/OpenAI plan with zero file contention. Output: filled-in
`client/ollama.rs` with complete/complete_stream/embed/default_model + unit tests for default_model, NDJSON
line parsing, and embedding parse.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/165-llmclient-trait-provider-implementations/165-RESEARCH.md
@.planning/phases/165-llmclient-trait-provider-implementations/165-PATTERNS.md
@.planning/phases/165-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (ferro-ai/src/client/mod.rs) — already in place, do NOT redefine: -->
pub type TokenStream = BoxStream<'static, Result<String, Error>>;
pub enum Role { User, Assistant }
pub struct Message { pub role: Role, pub content: String }
pub struct CompletionRequest {
    pub system: Option<String>, pub messages: Vec<Message>, pub max_tokens: u32,
    pub model_override: Option<String>, pub schema: Option<serde_json::Value>,
}
#[async_trait] pub trait LlmClient: Send + Sync {
    fn default_model(&self) -> &str;
    async fn complete(&self, request: CompletionRequest) -> Result<String, Error>;
    async fn complete_stream(&self, request: CompletionRequest) -> Result<TokenStream, Error>;
    async fn embed(&self, text: &str) -> Result<Vec<f32>, Error>;
}
<!-- Import via: use crate::client::{LlmClient, CompletionRequest, Message, Role, TokenStream}; use crate::error::Error; -->

<!-- From Plan 01 (ferro-ai/src/error.rs): -->
Error::Provider { status: Option<u16>, message: String }, Error::Unsupported, Error::Timeout,
Error::Deserialization(String).

<!-- async-stream 0.3 try_stream! pattern (RESEARCH lines 313-345): -->
use async_stream::try_stream;
Box::pin(try_stream! { /* yield String tokens; ? propagates Error */ })
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OllamaClient — struct (no auth), complete, complete_stream (NDJSON), embed</name>
  <files>ferro-ai/src/client/ollama.rs</files>
  <read_first>
    - ferro-ai/src/client/ollama.rs (the empty stub from Plan 01 — file being filled in)
    - ferro-ai/src/classifier/anthropic.rs (analog: struct+constructor+60s timeout lines 15-37, status+parse pattern lines 111-142)
    - 165-RESEARCH.md §"Ollama API" lines 649-681, §"Pattern 2 — async-stream try_stream!" lines 312-345, §"Ollama Embeddings" lines 866-887, Pitfall 2 (NDJSON not SSE) lines 745-750
    - 165-PATTERNS.md §"ferro-ai/src/client/ollama.rs" lines 233-263
  </read_first>
  <behavior>
    - OllamaClient::new(None, None).default_model() == "llama3.1"
    - OllamaClient::new(Some("mistral".into()), None).default_model() == "mistral"
    - default base_url when None == "http://localhost:11434"
    - parse_ollama_line on `{"message":{"content":"The"},"done":false}` yields token "The", done=false
    - parse_ollama_line on `{"message":{"content":""},"done":true}` yields no token, done=true
    - parse_ollama_embedding on `{"embeddings":[[0.1,-0.2]]}` yields vec![0.1, -0.2]
    - let _: Box<dyn LlmClient> = Box::new(OllamaClient::new(None, None)); compiles
  </behavior>
  <action>
Fill in `ferro-ai/src/client/ollama.rs` (replace the Plan 01 placeholder comment).

Struct per PATTERNS lines 246-251 (no api_key field):
```rust
pub struct OllamaClient { client: reqwest::Client, model: Option<String>, base_url: String }
```
`new(model: Option<String>, base_url: Option<String>) -> Self`: base_url defaults to
`"http://localhost:11434".to_string()` when None; 60s timeout client (T-165-04). `default_model()` →
`self.model.as_deref().unwrap_or("llama3.1")`.

`build_body(&self, request, stream)`: `{model: override-or-default, messages: map(Role→"user"/"assistant"),
stream: bool}`. Ollama ignores `max_tokens`/`schema` for chat — do NOT send `output_config`/`response_format`
(those are provider-specific to Anthropic/OpenAI). No auth header.

`complete` per RESEARCH lines 651-672: POST `format!("{}/api/chat", self.base_url)` with `stream:false`,
`.json(&body)`; send-error → `Error::Provider { status: None, message: e.to_string() }` (or `Error::Timeout`
on `e.is_timeout()`); non-success → `Error::Provider { status: Some(status), message: text }`; extract
`json["message"]["content"]`, else `Error::Deserialization`.

`complete_stream` per RESEARCH Pattern 2 NDJSON (lines 313-345) — **Pitfall 2: NOT SSE, never call
`.eventsource()`**: POST with `stream:true`, take `response.bytes_stream()`, wrap in `Box::pin(try_stream! { ... })`:
accumulate bytes into a String buffer, split on `\n`, for each complete line parse JSON; yield
`message.content` if non-empty; `return` when `done == true`. Map chunk errors to
`Error::Provider { status: None, message }` and JSON errors to `Error::Deserialization`.

`embed(&self, text: &str)` per RESEARCH lines 866-887: POST `format!("{}/api/embed", self.base_url)`, body
`{model: model-or-default, input: text}`, extract `json["embeddings"][0]` as `Vec<f32>`, else
`Error::Deserialization`.

Extract two `pub(crate)` parse helpers so streaming + embedding are unit-testable without a server:
- `parse_ollama_line(line: &str) -> Result<(Option<String>, bool), Error>` → (token, done)
- `parse_ollama_embedding(json: &serde_json::Value) -> Result<Vec<f32>, Error>`

`#[async_trait] impl LlmClient for OllamaClient`. Tests (`#[cfg(test)] mod tests`):
`test_ollama_default_model`, `test_ollama_model_override`, `test_ollama_default_base_url`,
`test_parse_ollama_line_token`, `test_parse_ollama_line_done`, `test_parse_ollama_embedding`,
`test_ollama_is_object_safe`.
  </action>
  <verify>
    <automated>cargo test -p ferro-ai client::ollama 2>&amp;1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'impl LlmClient for OllamaClient' ferro-ai/src/client/ollama.rs`
    - `grep -q 'llama3.1' ferro-ai/src/client/ollama.rs`
    - `grep -q '/api/chat' ferro-ai/src/client/ollama.rs` AND `grep -q '/api/embed' ferro-ai/src/client/ollama.rs`
    - `grep -q 'try_stream' ferro-ai/src/client/ollama.rs` AND `grep -q 'bytes_stream' ferro-ai/src/client/ollama.rs`
    - `! grep -q 'eventsource' ferro-ai/src/client/ollama.rs` (Pitfall 2 — Ollama is NDJSON, never SSE)
    - `cargo test -p ferro-ai client::ollama` exits 0
  </acceptance_criteria>
  <done>OllamaClient implements LlmClient with NDJSON streaming (no SSE), no-auth local default, embeddings via /api/embed, default_model "llama3.1"; all ollama unit tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ferro-ai → Ollama HTTP | Local-default `http://localhost:11434`; no auth. base_url operator-configured via `FERRO_AI_BASE_URL` (Plan 03/Wave 3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-03 | Tampering / SSRF | base_url-derived URLs in OllamaClient | accept | base_url is operator-configured (server-side `FERRO_AI_BASE_URL`), not end-user-supplied at request time. Default is loopback. No SSRF surface from user input. |
| T-165-04 | Denial of Service | NDJSON stream + non-streaming reads | mitigate | reqwest client retains 60s timeout; bounds connection time for chat/embed/stream. |
| T-165-06 | Information Disclosure | Error paths | mitigate | Ollama has no API key; error messages carry only `e.to_string()` and provider response text — no secret to leak. |
</threat_model>

<verification>
- `cargo test -p ferro-ai client::ollama` green.
- `cargo clippy -p ferro-ai --all-targets -- -D warnings` clean for ollama.rs.
- Per-wave merge gate: `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features -p ferro-ai`.
</verification>

<success_criteria>
- OllamaClient implements LlmClient; instantiable as Box<dyn LlmClient>.
- default_model() == "llama3.1"; base_url defaults to http://localhost:11434; no api_key.
- complete → /api/chat message.content; complete_stream parses NDJSON line-by-line (no SSE); embed → /api/embed embeddings[0].
- NDJSON parse helpers unit-tested without a live server.
</success_criteria>

<output>
After completion, create `.planning/phases/165-llmclient-trait-provider-implementations/165-03-SUMMARY.md`
</output>
