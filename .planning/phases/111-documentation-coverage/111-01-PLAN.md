---
phase: 111-documentation-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/features/projections.md
  - docs/src/SUMMARY.md
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "docs/src/features/projections.md exists and is non-empty"
    - "SUMMARY.md contains a link to features/projections.md under the Features section"
    - "The page explains the ServiceDef -> derive_intents -> Renderer pipeline"
    - "The page contains a complete worked example with code blocks"
  artifacts:
    - path: "docs/src/features/projections.md"
      provides: "Service Projections user documentation"
      min_lines: 80
      contains: "ServiceDef"
    - path: "docs/src/SUMMARY.md"
      provides: "Navigation entry for projections page"
      contains: "projections.md"
  key_links:
    - from: "docs/src/SUMMARY.md"
      to: "docs/src/features/projections.md"
      via: "mdBook link entry"
      pattern: "\\[.*Projections.*\\]\\(features/projections\\.md\\)"
---

<objective>
Create the Service Projections user documentation page and link it in SUMMARY.md.

Purpose: DOC-01 requires a user-facing documentation page for Service Projections — the system that derives UI intent from data structure and auto-renders JSON-UI. Currently this shipped feature has zero user docs.

Output: `docs/src/features/projections.md` linked in SUMMARY.md.
</objective>

<execution_context>
@/Users/alberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-documentation-coverage/111-RESEARCH.md

@docs/src/SUMMARY.md
@docs/src/features/themes.md (style reference — follow same structure)

<interfaces>
<!-- Source types the documentation must describe. All imported from ferro:: crate root. -->

From framework/src/lib.rs (re-exports):
```rust
// Service Projections (behind `projections` feature)
pub use ferro_projections::{
    ServiceDef, DataType, FieldMeaning, Intent, IntentScore, IntentHint,
    StateMachine, StateDef, Transition, ActionDef, GuardDef,
    derive_intents, JsonUiRenderer, Renderer, RenderContext, RenderMode,
};
```

Pipeline: ServiceDef -> derive_intents(&service_def) -> IntentScore[] -> JsonUiRenderer.render(&service_def, &intents, &ctx) -> serde_json::Value
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/src/features/projections.md</name>
  <files>docs/src/features/projections.md</files>
  <action>
Create the Service Projections documentation page following the same structure as themes.md:
1. Title + 1-2 sentence description of what Service Projections does (derives UI layout from data structure semantics)
2. Overview section: explain the three-stage pipeline (ServiceDef -> derive_intents -> Renderer) with a plain-text flow diagram
3. Quick Start section: minimal working example showing a simple ServiceDef, deriving intents, and rendering to JSON-UI. Use the product example from the RESEARCH.md Pattern 4.
4. Core Concepts sections:
   - ServiceDef Builder: document field(), display_name(), state_machine(), guard(), action() methods. Use the order example from RESEARCH.md Pattern 3.
   - Fields & Meaning: DataType enum (Integer, String, Float, Boolean, Date, DateTime, Text, Enum) and FieldMeaning enum (Identifier, EntityName, Money, Description, Status, Email, Phone, Url, Image, Timestamp, Count, Location, Generic). Brief table for each.
   - Intent Derivation: explain the 5 signal analyzers (field count, field meanings, state machines, guards/actions, naming patterns) and how they rank intents. List the 7 intents: Browse, Focus, Collect, Process, Summarize, Analyze, Track.
   - Intent Overrides: IntentHint::Primary and IntentHint::Exclude examples from RESEARCH.md.
   - Rendering: RenderContext struct (intent_index, current_state, mode, templates), RenderMode::Display vs RenderMode::Input. Show the JsonUiRenderer usage.
5. Reference table at end: all key types with one-line descriptions.

IMPORTANT import rules (from STATE.md Phase 110 decision):
- ALL imports use `ferro::` crate root — never `ferro_projections::` or `ferro::projections::`
- Do NOT mention the `projections` Cargo feature flag — it is an infrastructure detail

Style rules:
- Match themes.md tone: concise prose, code blocks with comments, no marketing language
- Use ```rust code blocks (not doctests)
- Keep examples self-contained and realistic
  </action>
  <verify>
    <automated>test -f docs/src/features/projections.md && grep -q "ServiceDef" docs/src/features/projections.md && grep -q "derive_intents" docs/src/features/projections.md && grep -q "JsonUiRenderer" docs/src/features/projections.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>docs/src/features/projections.md exists with 80+ lines, covers ServiceDef builder API, intent derivation, rendering pipeline, and contains a complete worked example</done>
</task>

<task type="auto">
  <name>Task 2: Add projections.md to SUMMARY.md</name>
  <files>docs/src/SUMMARY.md</files>
  <action>
Add a link to the new projections page in docs/src/SUMMARY.md under the "# Features" section.

Insert it after the "Themes" entry (line 41) and before "AI & Confirmation" (line 42):
```
- [Service Projections](features/projections.md)
```

This places it logically near Themes and JSON-UI since projections are part of the JSON-UI rendering pipeline.
  </action>
  <verify>
    <automated>grep -q "projections.md" docs/src/SUMMARY.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>SUMMARY.md contains a link to features/projections.md in the Features section</done>
</task>

</tasks>

<verification>
- `docs/src/features/projections.md` exists and contains sections: Quick Start, ServiceDef, Intent Derivation, Rendering
- `docs/src/SUMMARY.md` has a `[Service Projections](features/projections.md)` entry
- All code examples use `ferro::` imports (not `ferro_projections::`)
- No mention of `projections` Cargo feature flag
</verification>

<success_criteria>
- DOC-01 satisfied: projections.md exists, linked in SUMMARY.md, covers pipeline with worked example
</success_criteria>

<output>
After completion, create `.planning/phases/111-documentation-coverage/111-01-SUMMARY.md`
</output>
