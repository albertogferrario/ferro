---
phase: 113-pattern-coherence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-json-ui/src/lib.rs
  - ferro-cli/Cargo.toml
  - ferro-cli/src/ai.rs
  - ferro-mcp/src/tools/json_ui_generate.rs
  - .planning/PROJECT.md
autonomous: true
requirements:
  - COH-04

must_haves:
  truths:
    - "COMPONENT_CATALOG is defined exactly once in the workspace — in ferro-json-ui"
    - "Both ferro-cli and ferro-mcp import COMPONENT_CATALOG from ferro-json-ui"
    - "The project compiles with cargo build --all-features"
    - "PROJECT.md COMPONENT_CATALOG decision status is updated from Revisit to Good"
  artifacts:
    - path: "ferro-json-ui/src/lib.rs"
      provides: "Single source of truth for COMPONENT_CATALOG"
      contains: "pub const COMPONENT_CATALOG"
    - path: "ferro-cli/src/ai.rs"
      provides: "References COMPONENT_CATALOG from ferro-json-ui"
      contains: "use ferro_json_ui::COMPONENT_CATALOG"
    - path: "ferro-mcp/src/tools/json_ui_generate.rs"
      provides: "References COMPONENT_CATALOG from ferro-json-ui"
      contains: "use ferro_json_ui::COMPONENT_CATALOG"
    - path: "ferro-cli/Cargo.toml"
      provides: "Direct dependency on ferro-json-ui"
      contains: "ferro-json-ui"
    - path: ".planning/PROJECT.md"
      provides: "Design decision resolved"
      contains: "COMPONENT_CATALOG"
  key_links:
    - from: "ferro-cli/src/ai.rs"
      to: "ferro-json-ui/src/lib.rs"
      via: "use ferro_json_ui::COMPONENT_CATALOG"
      pattern: "ferro_json_ui::COMPONENT_CATALOG"
    - from: "ferro-mcp/src/tools/json_ui_generate.rs"
      to: "ferro-json-ui/src/lib.rs"
      via: "use ferro_json_ui::COMPONENT_CATALOG"
      pattern: "ferro_json_ui::COMPONENT_CATALOG"
---

<objective>
Deduplicate COMPONENT_CATALOG by moving it to ferro-json-ui as the single source of truth, updating both consumers (ferro-cli and ferro-mcp) to import from there, and recording the design decision resolution in PROJECT.md.

Purpose: Eliminate copy-paste drift risk between two identical 100+ line constants. Close the "Revisit" marker in PROJECT.md Key Decisions table.

Output: Single COMPONENT_CATALOG definition, two importers, clean compilation, design decision documented.
</objective>

<execution_context>
@/Users/alberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-pattern-coherence/113-CONTEXT.md
@.planning/phases/113-pattern-coherence/113-RESEARCH.md

<interfaces>
<!-- Current COMPONENT_CATALOG locations (to be consolidated) -->

ferro-cli/src/ai.rs:
- Contains `const COMPONENT_CATALOG: &str = r#"..."#;` (local, not pub)
- Used in `build_view_context()` function

ferro-mcp/src/tools/json_ui_generate.rs:
- Contains `const COMPONENT_CATALOG: &str = r#"..."#;` (local, not pub)
- Used in tool response construction

ferro-json-ui/src/lib.rs:
- Does NOT currently export COMPONENT_CATALOG
- Target location for the shared constant

ferro-cli/Cargo.toml:
- Does NOT currently list ferro-json-ui as a dependency
- Must be added: ferro-json-ui = { path = "../ferro-json-ui", version = "0.1" }

ferro-mcp/Cargo.toml:
- Already has ferro-json-ui dependency (line 24)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move COMPONENT_CATALOG to ferro-json-ui and update consumers</name>
  <files>
    ferro-json-ui/src/lib.rs
    ferro-cli/Cargo.toml
    ferro-cli/src/ai.rs
    ferro-mcp/src/tools/json_ui_generate.rs
  </files>
  <action>
**Step 1 — Add COMPONENT_CATALOG to ferro-json-ui/src/lib.rs:**
1. Read `ferro-cli/src/ai.rs` to get the current COMPONENT_CATALOG constant value
2. Read `ferro-mcp/src/tools/json_ui_generate.rs` to verify it's identical (they should be — Phase 67 synced them)
3. Add to `ferro-json-ui/src/lib.rs` as a public constant:
   ```rust
   /// Concise reference of all JSON-UI components for AI generation prompts.
   ///
   /// Used by ferro-cli (AI view generation) and ferro-mcp (json_ui_generate tool)
   /// as shared context for LLM-based code generation.
   pub const COMPONENT_CATALOG: &str = r#"..."#;
   ```
   Place it after the existing exports, before any `mod` declarations if applicable.

**Step 2 — Add ferro-json-ui dependency to ferro-cli/Cargo.toml:**
1. Read `ferro-cli/Cargo.toml`
2. In the `[dependencies]` section, add: `ferro-json-ui = { path = "../ferro-json-ui", version = "0.1" }`
3. Place alphabetically among the other ferro-* dependencies

**Step 3 — Update ferro-cli/src/ai.rs:**
1. Read the file
2. Remove the local `const COMPONENT_CATALOG: &str = r#"..."#;` definition (the entire multi-line raw string)
3. Add `use ferro_json_ui::COMPONENT_CATALOG;` in the imports section
4. Verify all usages of COMPONENT_CATALOG in the file still reference the same identifier (should be seamless)

**Step 4 — Update ferro-mcp/src/tools/json_ui_generate.rs:**
1. Read the file
2. Remove the local `const COMPONENT_CATALOG: &str = r#"..."#;` definition
3. Add `use ferro_json_ui::COMPONENT_CATALOG;` in the imports section
4. Verify all usages still reference the same identifier

**Step 5 — Verify compilation:**
Run `cargo build --all-features` to confirm no compile errors.
Run `cargo clippy --all --all-targets -- -D warnings` to confirm no new warnings.
  </action>
  <verify>
    <automated>cargo build --all-features 2>&1 | tail -5</automated>
  </verify>
  <done>COMPONENT_CATALOG defined once in ferro-json-ui/src/lib.rs. ferro-cli/src/ai.rs and ferro-mcp/src/tools/json_ui_generate.rs import it from ferro-json-ui. No local definitions remain. Workspace compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Record COMPONENT_CATALOG design decision in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <action>
1. Read `.planning/PROJECT.md`
2. Find the Key Decisions table row containing "COMPONENT_CATALOG duplicated"
3. Update it from:
   `| COMPONENT_CATALOG duplicated | Cannot share code across workspace crates | ⚠️ Revisit |`
   to:
   `| COMPONENT_CATALOG in ferro-json-ui | Single pub const shared by ferro-cli and ferro-mcp via direct dependency | ✓ Good |`
4. This closes the "Revisit" marker — the duplication is resolved.
  </action>
  <verify>
    <automated>grep "COMPONENT_CATALOG" .planning/PROJECT.md</automated>
  </verify>
  <done>PROJECT.md Key Decisions table shows COMPONENT_CATALOG with "Good" status, not "Revisit". Decision rationale documented.</done>
</task>

</tasks>

<verification>
Run after both tasks complete:

1. `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` — full suite passes
2. `grep -rn "COMPONENT_CATALOG" ferro-cli/src/ ferro-mcp/src/ ferro-json-ui/src/` — ferro-json-ui has the definition, others have only `use` imports
3. `grep "COMPONENT_CATALOG" .planning/PROJECT.md` — shows "Good" not "Revisit"
</verification>

<success_criteria>
- COH-04: COMPONENT_CATALOG exists in exactly one location (ferro-json-ui/src/lib.rs), both consumers import it, workspace compiles, design decision documented in PROJECT.md
</success_criteria>

<output>
After completion, create `.planning/phases/113-pattern-coherence/113-02-SUMMARY.md`
</output>
