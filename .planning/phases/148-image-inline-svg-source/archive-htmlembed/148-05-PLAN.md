---
phase: 148
plan: 05
type: execute
wave: 2
depends_on: [01, 02, 03, 04]
files_modified:
  - ferro-mcp/src/tools/json_ui_catalog.rs
  - ferro-json-ui/src/lib.rs
  - docs/src/json-ui/components.md
autonomous: true
requirements: [EMBED-04, EMBED-05]
must_haves:
  truths:
    - "ferro-mcp/src/tools/json_ui_catalog.rs contains a CatalogComponent entry with name \"HtmlEmbed\" and a safety-first description foregrounding the `html_escape` bypass + caller-owned XSS responsibility"
    - "The MCP CatalogComponent's single `prop(\"html\", \"String\", true, ...)` description also names the bypass (D-18)"
    - "no_required allowlist at json_ui_catalog.rs:1375 is UNCHANGED — HtmlEmbed has one required prop (D-20)"
    - "test_all_components_present passes (42 components, HtmlEmbed in expected array) — flipping Plan 01 Task 3 Step B RED → GREEN"
    - "test_components_have_props passes (HtmlEmbed has at least one required prop)"
    - "ferro-json-ui/src/lib.rs COMPONENT_CATALOG const contains a `### HtmlEmbed` section with safety messaging (D-16)"
    - "docs/src/json-ui/components.md contains a `### HtmlEmbed` section with opening paragraph, safety callout (blockquote/warning), props table, Rust example, JSON example, use-case list, and pointer to Component::Text"
    - "cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features all succeed (EMBED-05 CI gate green)"
  artifacts:
    - path: "ferro-mcp/src/tools/json_ui_catalog.rs"
      provides: "CatalogComponent entry for HtmlEmbed with safety-first description"
      contains: "name: \"HtmlEmbed\""
    - path: "ferro-json-ui/src/lib.rs"
      provides: "### HtmlEmbed section in COMPONENT_CATALOG string (agent context)"
      contains: "### HtmlEmbed"
    - path: "docs/src/json-ui/components.md"
      provides: "### HtmlEmbed user documentation section with safety callout"
      contains: "### HtmlEmbed"
  key_links:
    - from: "ferro-mcp CatalogComponent"
      to: "HtmlEmbedProps single required prop"
      via: "prop(\"html\", \"String\", true, ...)"
      pattern: "prop\\(\\s*\"html\",\\s*\"String\",\\s*true"
    - from: "COMPONENT_CATALOG const"
      to: "### HtmlEmbed safety contract"
      via: "markdown-like string section with 'verbatim' / 'caller' / 'XSS' language"
      pattern: "### HtmlEmbed"
    - from: "docs/src/json-ui/components.md"
      to: "### HtmlEmbed"
      via: "markdown section with blockquote-style safety callout"
      pattern: "^### HtmlEmbed$"
---

<objective>
Close the phase by adding the three documentation surfaces that round out the safety
messaging contract, then run the full CI gate to verify EMBED-05.

Four discrete deliverables:
1. `CatalogComponent { name: "HtmlEmbed", ... }` entry in `ferro-mcp/src/tools/json_ui_catalog.rs`
   with a safety-first description — flips Plan 01 Task 3 Step B exhaustive-list RED → GREEN.
2. `### HtmlEmbed` section in the `COMPONENT_CATALOG` const in `ferro-json-ui/src/lib.rs`
   with safety language (D-16) — agent-readable context when LLMs introspect the crate.
3. `### HtmlEmbed` section in `docs/src/json-ui/components.md` with opening paragraph,
   safety callout (blockquote/warning block matching existing convention), props
   table, Rust example, JSON example, use-case list, and a pointer to `Component::Text`
   for escaped output (D-21).
4. Run the full CI gate:
   `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features`

Purpose: Complete the five-surface safety-messaging contract (rustdoc on
HtmlEmbedProps in Plan 02; function rustdoc + inline SAFETY CONTRACT comment in Plan 03;
this plan adds COMPONENT_CATALOG + MCP CatalogComponent + docs callout). An author
who introspects the component via rust-analyzer, `ferro-mcp`, the shared context string,
or the docs site encounters the safety contract at every turn.

Output: Three file edits + final CI gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-htmlembed-component-ferro-json-ui/148-CONTEXT.md
@.planning/phases/148-htmlembed-component-ferro-json-ui/148-RESEARCH.md
@.planning/phases/148-htmlembed-component-ferro-json-ui/148-PATTERNS.md
@.planning/phases/148-htmlembed-component-ferro-json-ui/148-UI-SPEC.md
@.planning/phases/148-htmlembed-component-ferro-json-ui/148-01-PLAN.md
@.planning/phases/148-htmlembed-component-ferro-json-ui/148-02-PLAN.md
@.planning/phases/148-htmlembed-component-ferro-json-ui/148-03-PLAN.md
@.planning/phases/148-htmlembed-component-ferro-json-ui/148-04-PLAN.md
@CLAUDE.md

<interfaces>
<!-- ferro-mcp CatalogComponent (json_ui_catalog.rs:597-607 Separator precedent): -->
```rust
CatalogComponent {
    name: "Separator".to_string(),
    description: "Visual divider between content sections.".to_string(),
    props: vec![prop(
        "orientation",
        "Option<Orientation>",
        false,
        "Direction: horizontal (default) or vertical",
    )],
    variants: None,
},
```

<!-- prop helper (json_ui_catalog.rs:1193-1201) — used as-is: -->
```rust
fn prop(name: &str, ty: &str, required: bool, description: &str) -> PropInfo;
```

<!-- Existing test assertions this plan flips GREEN: -->
- `test_all_components_present` (L1207-1263) — asserts catalog.len()==42 and "HtmlEmbed" in expected; Plan 01 Task 3 bumped expectation; this plan adds the catalog entry to satisfy it
- `test_components_have_props` (L1364-1384) — HtmlEmbed has one required prop (`html`), so the "at least one required prop" assertion passes

<!-- ferro-json-ui/src/lib.rs COMPONENT_CATALOG const (L103+) — existing ### DetailForm section at L120-122 is the safety-density precedent -->

<!-- docs/src/json-ui/components.md: -->
- L233-258 `### Separator` — minimal density precedent
- L473+ `### DetailForm` — richer-example density with safety-adjacent content (method spoofing callout)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CatalogComponent entry for HtmlEmbed in ferro-mcp + add ### HtmlEmbed to COMPONENT_CATALOG in ferro-json-ui/src/lib.rs</name>
  <files>
    - ferro-mcp/src/tools/json_ui_catalog.rs
    - ferro-json-ui/src/lib.rs
  </files>
  <read_first>
    - ferro-mcp/src/tools/json_ui_catalog.rs (lines 597-607 — Separator CatalogComponent entry; canonical minimal single-prop shape)
    - ferro-mcp/src/tools/json_ui_catalog.rs (lines 1193-1201 — `prop(...)` helper used to build PropInfo entries)
    - ferro-mcp/src/tools/json_ui_catalog.rs (adjacent DetailForm / KeyValueEditor CatalogComponent entries — grep for `name: "DetailForm"` to find the insertion point and the safety-adjacent description density)
    - ferro-mcp/src/tools/json_ui_catalog.rs (lines 1207-1263 — test_all_components_present; already bumped to 42 in Plan 01 Task 3 — this task's CatalogComponent entry makes it GREEN)
    - ferro-mcp/src/tools/json_ui_catalog.rs (line 1375 — `no_required` allowlist; MUST NOT be modified per D-20)
    - ferro-json-ui/src/lib.rs (lines 103+ — COMPONENT_CATALOG const; grep for `### DetailForm` to find the safety-density precedent at L120-122)
    - ferro-json-ui/src/lib.rs (lines 59-72 — the `pub use component::{...}` block; DO NOT add further re-exports — Plan 02 owns that)
    - .planning/phases/148-htmlembed-component-ferro-json-ui/148-CONTEXT.md (D-16, D-17, D-18, D-19, D-20)
    - .planning/phases/148-htmlembed-component-ferro-json-ui/148-RESEARCH.md (§Pattern 8 COMPONENT_CATALOG body; §Pattern 10 MCP CatalogComponent body; §Pitfall 3 three atomic sub-edits; §Pitfall 4 no_required must not include HtmlEmbed)
    - .planning/phases/148-htmlembed-component-ferro-json-ui/148-PATTERNS.md (§4b COMPONENT_CATALOG; §5a CatalogComponent; §5b reconfirm no `no_required` edit)
  </read_first>
  <behavior>
    Two file edits in this task because they share the same safety-messaging contract
    and flip the same red tests.

    ### A. ferro-mcp/src/tools/json_ui_catalog.rs — add CatalogComponent entry

    Per D-17, D-18, 148-RESEARCH.md §Pattern 10, 148-PATTERNS.md §5a: add a single
    `CatalogComponent { name: "HtmlEmbed", ... }` to the `execute(None)` catalog
    builder. Placement: adjacent to the DetailForm / KeyValueEditor entries (the recent
    additions). The catalog builder is NOT alphabetical — it uses loose family grouping.

    The entry MUST include:
    - `name: "HtmlEmbed".to_string()`
    - Safety-first description foregrounding the `html_escape` bypass, caller-owned
      safety, intended-use-cases, "do NOT pass user input", and a pointer to
      Component::Text for escaped output.
    - `props: vec![prop("html", "String", true, "...")]` — REQUIRED prop (third
      argument `true`); description also names the bypass.
    - `variants: None`

    Do NOT modify the `no_required` allowlist at L1375 (D-20, 148-RESEARCH.md §Pitfall 4).
    HtmlEmbed has a required prop and does NOT join that allowlist.

    After this edit, `cargo test -p ferro-mcp json_ui_catalog::tests::test_all_components_present`
    turns GREEN (Plan 01 Task 3 Step B bumped the assertion; this plan satisfies it).
    `test_components_have_props` also passes (HtmlEmbed has one required prop).

    ### B. ferro-json-ui/src/lib.rs — add ### HtmlEmbed section to COMPONENT_CATALOG

    Per D-16, 148-RESEARCH.md §Pattern 8, 148-PATTERNS.md §4b: add a `### HtmlEmbed`
    section to the `COMPONENT_CATALOG` const string. Placement: adjacent to the recent
    `### DetailForm` / `### KeyValueEditor` entries (L120-122 area) OR alphabetical —
    match the existing ordering convention at write time.

    The section MUST include:
    - `### HtmlEmbed` heading
    - `Props: html (String)` line
    - Safety-first function statement foregrounding the `html_escape` bypass
    - Intended-use-cases: inline SVG charts, pre-rendered markdown, static HTML
      widgets, third-party embed snippets
    - Explicit non-target: "Do NOT pass user input"
    - Pointer to `Component::Text` for escaped output
    - `Example JSON: {"type": "HtmlEmbed", "html": "<svg>...</svg>"}`

    Do NOT add a public re-export in this task — Plan 02 already did that. Do NOT
    touch `pub use component::{...}` block.
  </behavior>
  <action>
    ### Step A: Add CatalogComponent entry in ferro-mcp/src/tools/json_ui_catalog.rs

    Open `ferro-mcp/src/tools/json_ui_catalog.rs`. Locate the DetailForm / KeyValueEditor
    CatalogComponent entries (grep `name: "DetailForm"` to find them). Insert the new
    entry adjacent to them (the builder is not alphabetical — loose family grouping;
    place near these recent additions).

    Insert verbatim:

    ```rust
    CatalogComponent {
        name: "HtmlEmbed".to_string(),
        description: "Injects pre-rendered HTML verbatim into a wrapping <div>. \
                      The renderer does NOT html-escape the `html` prop — this is \
                      the only component in ferro-json-ui that bypasses escaping. \
                      Caller is responsible for content safety (XSS). Intended for \
                      server-generated content: inline SVG charts, pre-rendered \
                      markdown, static HTML widgets, third-party embed snippets. \
                      Do NOT pass user input. For escaped text output, use \
                      Component::Text.".to_string(),
        props: vec![prop(
            "html",
            "String",
            true,
            "Raw HTML emitted unescaped inside a wrapping <div>. Caller is \
             responsible for content safety.",
        )],
        variants: None,
    },
    ```

    Notes:
    - `true` for the `required` arg is load-bearing (D-18, D-20): `html` is required;
      HtmlEmbed does NOT belong in the `no_required` allowlist.
    - Description text is author-facing (agents reading the MCP catalog will see this
      before any other source). Every word in the safety framing is load-bearing per
      148-UI-SPEC §8 and 148-RESEARCH.md §S-3 (signal 2).

    **Do NOT** touch the `no_required` allowlist at L1375 — HtmlEmbed has a required
    prop. The `test_components_have_props` test at L1364-1384 enforces this.

    Save.

    ### Step B: Add ### HtmlEmbed to COMPONENT_CATALOG in ferro-json-ui/src/lib.rs

    Open `ferro-json-ui/src/lib.rs`. Locate the `COMPONENT_CATALOG` const (L103+).
    Grep for `### DetailForm` to find the safety-density precedent at L120-122.
    Place the `### HtmlEmbed` section adjacent to the recent additions (DetailForm /
    KeyValueEditor) OR alphabetical — match whatever ordering the file currently uses.

    Insert verbatim:

    ```
    ### HtmlEmbed
    Props: html (String)
    Injects pre-rendered HTML verbatim into a wrapping <div>. Renderer does NOT html-escape the html prop — callers are responsible for content safety (XSS). Intended for server-generated content: inline SVG charts, pre-rendered markdown, static HTML widgets, third-party embed snippets. Do NOT pass user input. For escaped text output, use Component::Text.
    Example JSON: {"type": "HtmlEmbed", "html": "<svg width=\"100\" height=\"100\"><circle cx=\"50\" cy=\"50\" r=\"40\" fill=\"red\"/></svg>"}
    ```

    (The exact formatting — line breaks, spacing — follows the existing DetailForm /
    KeyValueEditor entries in this const string; adjust spacing to match that local
    style without changing the content requirements above.)

    Do NOT modify the `pub use component::{...}` block — Plan 02 already re-exported
    `HtmlEmbedProps`.

    Save.
  </action>
  <verify>
    <automated>cargo test -p ferro-mcp json_ui_catalog 2>&amp;1 | tail -20 &amp;&amp; cargo build -p ferro-json-ui 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'name: "HtmlEmbed"' ferro-mcp/src/tools/json_ui_catalog.rs` succeeds (CatalogComponent entry present)
    - `grep -q 'prop(' ferro-mcp/src/tools/json_ui_catalog.rs && grep -A3 'name: "HtmlEmbed"' ferro-mcp/src/tools/json_ui_catalog.rs | grep -q '"html"'` succeeds (html prop entry found in the HtmlEmbed entry)
    - `grep -A10 'name: "HtmlEmbed"' ferro-mcp/src/tools/json_ui_catalog.rs | grep -q 'true'` succeeds (required=true present in the entry)
    - `grep -A10 'name: "HtmlEmbed"' ferro-mcp/src/tools/json_ui_catalog.rs | grep -E 'html-escape|unescape|verbatim|Caller is responsible|XSS'` returns at least one match (safety language present in description, D-17)
    - `grep -A10 'name: "HtmlEmbed"' ferro-mcp/src/tools/json_ui_catalog.rs | grep -q 'Component::Text'` succeeds (pointer to Text for escaped output)
    - no_required allowlist untouched: `git diff ferro-mcp/src/tools/json_ui_catalog.rs | grep -E '^[+-].*no_required' | wc -l` returns 0 (D-20)
    - `cargo test -p ferro-mcp json_ui_catalog::tests::test_all_components_present` exits 0 (42 components; HtmlEmbed in names)
    - `cargo test -p ferro-mcp json_ui_catalog::tests::test_components_have_props` exits 0 (HtmlEmbed has required html prop)
    - `grep -q '^### HtmlEmbed$' ferro-json-ui/src/lib.rs` succeeds (COMPONENT_CATALOG section heading present)
    - `grep -A5 '^### HtmlEmbed$' ferro-json-ui/src/lib.rs | grep -E 'html-escape|unescape|verbatim|caller|XSS|Caller'` returns at least one match (D-16 safety language)
    - `grep -A5 '^### HtmlEmbed$' ferro-json-ui/src/lib.rs | grep -q 'Component::Text'` succeeds (pointer to Text)
    - `grep -A5 '^### HtmlEmbed$' ferro-json-ui/src/lib.rs | grep -q 'Do NOT pass user input'` succeeds (D-16 clause)
    - `cargo build -p ferro-json-ui` exits 0
  </acceptance_criteria>
  <done>
    CatalogComponent entry for HtmlEmbed lands in ferro-mcp with safety-first
    description and a required `html` prop. The exhaustive-list test flips RED → GREEN
    (42 components; HtmlEmbed in expected array). `### HtmlEmbed` section added to
    COMPONENT_CATALOG const in ferro-json-ui/src/lib.rs with the full safety contract
    language. no_required allowlist deliberately untouched (D-20).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ### HtmlEmbed section to docs/src/json-ui/components.md with safety callout</name>
  <files>docs/src/json-ui/components.md</files>
  <read_first>
    - docs/src/json-ui/components.md (lines 233-258 — ### Separator section; canonical minimal single-prop docs section with props table, Rust example, JSON output example)
    - docs/src/json-ui/components.md (lines 473+ — ### DetailForm section; richer-example shape for more-involved components; inspect for existing safety-callout styling conventions, if any)
    - docs/src/json-ui/components.md (grep for `^> ⚠️` or `> **Warning`** or `> **Note**` blocks across the file — identify the docs site's existing warning-block convention and match it)
    - .planning/phases/148-htmlembed-component-ferro-json-ui/148-CONTEXT.md (D-21)
    - .planning/phases/148-htmlembed-component-ferro-json-ui/148-RESEARCH.md (§Example 1 Rust example; §Example 2 JSON example; §Example 3 rendered HTML for use-case description)
    - .planning/phases/148-htmlembed-component-ferro-json-ui/148-PATTERNS.md (§6 docs section content requirements)
    - .planning/phases/148-htmlembed-component-ferro-json-ui/148-UI-SPEC.md (§8 surface 4 — safety-callout content requirements)
  </read_first>
  <behavior>
    Add a new `### HtmlEmbed` section to `docs/src/json-ui/components.md`. Placement:
    adjacent to `### DetailForm` at L473+ (recent additions) OR alphabetical — match
    whatever ordering convention sibling sections already use.

    Per D-21 and 148-PATTERNS.md §6, the section MUST contain all eight content blocks
    in this order:

    1. Section heading: `### HtmlEmbed`
    2. Opening paragraph (1-2 sentences): what the component does and its intended
       content types ("Injects pre-rendered HTML verbatim into a wrapping `<div>`.
       Intended for server-generated content like inline SVG charts or pre-rendered
       markdown.").
    3. **Safety callout** as a blockquote / warning block. Match the existing docs-site
       convention (`> ⚠️ Warning` or `> **Warning**` — inspect the file and match).
       Content: unescaped emission; caller XSS responsibility; NOT for user input;
       pointer to Component::Text for escaped output.
    4. Props table with one row: `html` / `String` / Yes / - / Raw HTML emitted unescaped.
    5. Rust example using `ComponentNode::html_embed(...)` with `HtmlEmbedProps::new(...)`
       — shape from 148-RESEARCH.md §Example 1.
    6. JSON output example: `{"key": "chart", "type": "HtmlEmbed", "html": "<svg>...</svg>"}`.
    7. Use-case bullet list: inline SVG, pre-rendered markdown, static HTML widgets,
       third-party embed snippets.
    8. Closing pointer: "For escaped text output, use [`Text`](#text)."
  </behavior>
  <action>
    Open `docs/src/json-ui/components.md`. Inspect existing warning/callout convention
    (grep for `> ⚠` and `> **Warning**` and similar patterns — match whichever the
    file already uses).

    Locate `### DetailForm` section (L473+) OR alphabetical position near `### Header`
    / `### Image`. Insert the new section before the next `### ` heading in the chosen
    location.

    Insert a markdown section with the following structure (adapt the blockquote
    style — `> ⚠️` or `> **Warning**` — to match the existing docs convention):

    ```markdown
    ### HtmlEmbed

    Injects pre-rendered HTML verbatim into a wrapping `<div>`. Intended for
    server-generated content like inline SVG charts, pre-rendered markdown, or static
    HTML widgets.

    > ⚠️ **Safety contract — HTML is emitted verbatim.**
    >
    > The `html` prop is **not** HTML-escaped. The caller is responsible for ensuring
    > the content is safe against XSS. This component exists for server-generated
    > content (SVG charts, pre-rendered markdown, static widgets) — **never pass user
    > input directly into this component.** For escaped text output, use
    > [`Text`](#text) instead.

    | Prop | Type | Required | Default | Description |
    |------|------|----------|---------|-------------|
    | `html` | `String` | Yes | — | Raw HTML emitted unescaped inside a wrapping `<div>`. Caller is responsible for content safety. |

    ```rust
    use ferro::{ComponentNode, HtmlEmbedProps};

    // Server-generated SVG chart (from plotters, charming, or a custom renderer).
    let chart_svg: String = generate_chart(&data);
    ComponentNode::html_embed(
        "revenue-chart",
        HtmlEmbedProps::new(chart_svg),
    )
    ```

    JSON output:

    ```json
    {
      "key": "revenue-chart",
      "type": "HtmlEmbed",
      "html": "<svg viewBox=\"0 0 200 100\"><polyline points=\"0,80 50,40 100,60 150,20 200,50\" stroke=\"blue\" fill=\"none\"/></svg>"
    }
    ```

    **Use cases:**

    - Inline SVG charts (plotters, charming, custom renderers)
    - Pre-rendered markdown (e.g. via `pulldown-cmark::html::push_html`)
    - Static HTML widgets
    - Third-party embed snippets (tweet embeds, etc.)

    **Do not use for:** user input, any string not fully controlled by the server.
    For escaped text output, use [`Text`](#text).
    ```

    (Adjust the warning blockquote style — `> ⚠️`, `> **Warning**`, `> [!WARNING]` —
    to match the existing docs convention. Content requirements are invariant.)

    Save.
  </action>
  <verify>
    <automated>grep -c '^### HtmlEmbed$' docs/src/json-ui/components.md &amp;&amp; grep -A20 '^### HtmlEmbed$' docs/src/json-ui/components.md | head -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^### HtmlEmbed$' docs/src/json-ui/components.md` succeeds
    - `grep -A40 '^### HtmlEmbed$' docs/src/json-ui/components.md | grep -E '^>' | head -1` returns a blockquote line (safety callout present)
    - `grep -A40 '^### HtmlEmbed$' docs/src/json-ui/components.md | grep -E 'verbatim|html-escape|unescape'` returns at least one match (safety language in callout or opening paragraph)
    - `grep -A40 '^### HtmlEmbed$' docs/src/json-ui/components.md | grep -q 'XSS'` succeeds (XSS named explicitly)
    - `grep -A40 '^### HtmlEmbed$' docs/src/json-ui/components.md | grep -q 'user input'` succeeds (user input explicitly named as non-target)
    - `grep -A40 '^### HtmlEmbed$' docs/src/json-ui/components.md | grep -E '\\[.*Text.*\\]\\(#text\\)|Component::Text'` returns at least one match (pointer to Text for escaped output, D-21)
    - `grep -A40 '^### HtmlEmbed$' docs/src/json-ui/components.md | grep -q '^| \`html\` |'` succeeds (props table row present)
    - `grep -A40 '^### HtmlEmbed$' docs/src/json-ui/components.md | grep -q 'ComponentNode::html_embed'` succeeds (Rust example)
    - `grep -A60 '^### HtmlEmbed$' docs/src/json-ui/components.md | grep -q '"type": "HtmlEmbed"'` succeeds (JSON example)
    - `grep -A60 '^### HtmlEmbed$' docs/src/json-ui/components.md | grep -iE 'inline svg|pre-rendered markdown|static html|embed'` returns at least two matches (use-case list)
  </acceptance_criteria>
  <done>
    New `### HtmlEmbed` section added to docs/src/json-ui/components.md with all eight
    required content blocks per D-21: heading, opening paragraph, safety callout
    (blockquote matching existing convention), props table, Rust example, JSON
    example, use-case list, and pointer to Text. The safety-messaging surface #4 (of
    five per 148-UI-SPEC §8) is now live.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run full CI gate — EMBED-05 completion</name>
  <files></files>
  <read_first>
    - .planning/phases/148-htmlembed-component-ferro-json-ui/148-VALIDATION.md (row 148-05-04 — EMBED-05 CI gate)
    - .planning/phases/148-htmlembed-component-ferro-json-ui/148-CONTEXT.md (Canonical References §Project principles — Testing & Linting section)
    - /Users/alberto/.claude/CLAUDE.md §Testing & Linting
  </read_first>
  <behavior>
    Run the full workspace CI gate as the completion criterion for EMBED-05. This is
    the same gate used on every prior ferro phase and mirrors the CI workflow.

    Three commands in sequence — all three must pass:

    1. `cargo fmt --all -- --check`
    2. `cargo clippy --all --all-targets -- -D warnings`
    3. `cargo test --all-features`

    If any command fails, fix the root cause. Do NOT use `--no-verify` or skip the
    gate. Do NOT run `cargo fmt --all` (without `--check`) to mutate code on this
    task — that would silence formatting issues rather than surface them.

    If clippy flags anything in the phase's new code (the `render_html_embed` function,
    the catalog entries, the COMPONENT_CATALOG string additions), fix the lint and
    re-run. Common issues to watch for:
    - `clippy::format_push_string` or similar on the `format!("<div>{}</div>", ...)`
      body — unlikely since the same pattern is used in render_separator.
    - Unused imports introduced by test modules.
    - `clippy::needless_borrow` on the `&HtmlEmbedProps` parameter — unlikely since
      render_separator takes `&SeparatorProps`.

    If `cargo test --all-features` fails:
    - If a Plan 01..04 test fails, the upstream plan is incomplete and must be
      revisited before this plan can complete. Do NOT paper over.
    - If a doctest fails (e.g. HtmlEmbedProps::new example), check the re-export
      in Plan 02 — the doctest uses `use ferro_json_ui::HtmlEmbedProps` which depends
      on the re-export.
    - If `test_all_components_present` fails with "Catalog should contain all 42 built-in
      components, got 41", Task 1 Step A's CatalogComponent entry didn't land.

    Successful exit of all three commands marks EMBED-05 GREEN and closes the phase.
  </behavior>
  <action>
    Run the three CI-gate commands in sequence. Capture exit codes.

    ```bash
    cargo fmt --all -- --check
    cargo clippy --all --all-targets -- -D warnings
    cargo test --all-features
    ```

    If the first command fails, run `cargo fmt --all` ONCE to fix formatting, then
    re-run `cargo fmt --all -- --check` to confirm it now passes. If it still fails,
    inspect which file has non-idempotent formatting.

    If clippy fails, read the diagnostic, fix the code at the identified location
    (do not `#[allow]` unless clippy is wrong for a domain-specific reason — rare for
    this phase's code), re-run.

    If tests fail, triage per the behavior section above. Fixes belong in the plan
    that owns the failing code, not in this task — revisit 01/02/03/04 as needed.

    After all three pass, report the exit codes in the final SUMMARY.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check &amp;&amp; cargo clippy --all --all-targets -- -D warnings &amp;&amp; cargo test --all-features 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy --all --all-targets -- -D warnings` exits 0 (NO warnings across the workspace)
    - `cargo test --all-features` exits 0
    - All Plan 01..04 tests green under `cargo test --all-features`
    - `test_all_components_present` in ferro-mcp passes (42 components, HtmlEmbed in expected)
    - `test_components_have_props` in ferro-mcp passes (HtmlEmbed has ≥1 required prop)
    - Load-bearing `render_html_embed_emits_html_verbatim_without_escaping` passes
    - All five html_embed_tests in component.rs pass
    - All three resolver no-op tests pass
  </acceptance_criteria>
  <done>
    `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features`
    exits 0. EMBED-05 satisfied. Phase 148 ships.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → MCP catalog | Agents introspect ferro components via `ferro-mcp json_ui_catalog`. The description field is the first safety signal an LLM sees when suggesting HtmlEmbed for a flow. Per 148-UI-SPEC §8 this is surface #3 of the five-surface safety-messaging contract. |
| Author → docs site | Human authors browse `docs/src/json-ui/components.md`. The safety callout is surface #4 of the five-surface contract; its prominent blockquote styling is load-bearing. |
| LLM → COMPONENT_CATALOG string | `ferro-json-ui/src/lib.rs` embeds a shared-context string that LLMs absorb when reasoning about the crate. Surface #2 of the five-surface contract; present wherever LLM context is seeded from crate source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-01 | Tampering | XSS via HtmlEmbed input (author-facing messaging) | accept (documented contract) | This plan adds three of the five safety-messaging surfaces: MCP CatalogComponent description (D-17/D-18), COMPONENT_CATALOG string entry (D-16), and docs section with blockquote safety callout (D-21). Combined with the HtmlEmbedProps rustdoc (Plan 02) and the inline SAFETY CONTRACT comment + function rustdoc (Plan 03), an author or agent encounters the bypass contract at every introspection surface. Severity: low — author-facing documentation; caller-owned responsibility. |
| T-148-02 | Tampering | Reflexive "fix" adding html_escape in future refactor | mitigate | The MCP catalog description explicitly names "the only component in ferro-json-ui that bypasses escaping" — an agent suggesting a "fix" would see the description flag it as intentional. The COMPONENT_CATALOG string and docs callout reinforce. Combined with the load-bearing XSS passthrough test (Plan 01) and the inline comment (Plan 03), a reflexive fix must override five concurrent signals to land. Severity: low individually, medium combined (block threshold high). |
</threat_model>

<verification>
- Full CI gate green: `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features`
- `test_all_components_present` in ferro-mcp passes (42 components including HtmlEmbed)
- `test_components_have_props` in ferro-mcp passes (HtmlEmbed has required html prop)
- no_required allowlist unchanged (D-20, Pitfall 4)
- `### HtmlEmbed` section exists in `ferro-json-ui/src/lib.rs` COMPONENT_CATALOG const with safety language
- `### HtmlEmbed` section exists in `docs/src/json-ui/components.md` with blockquote-style safety callout, props table, Rust example, JSON example, use-case list, and pointer to Text
- All five safety-messaging surfaces live (per 148-UI-SPEC §8):
  1. HtmlEmbedProps rustdoc (Plan 02)
  2. COMPONENT_CATALOG string (this plan)
  3. MCP CatalogComponent description (this plan)
  4. docs/src/json-ui/components.md safety callout (this plan)
  5. Inline SAFETY CONTRACT comment in render_html_embed body (Plan 03)
</verification>

<success_criteria>
- CatalogComponent entry for HtmlEmbed lands in ferro-mcp with safety-first description and required html prop
- no_required allowlist untouched (D-20)
- `test_all_components_present` flips RED → GREEN
- `test_components_have_props` passes for HtmlEmbed
- `### HtmlEmbed` section added to COMPONENT_CATALOG const in ferro-json-ui/src/lib.rs with safety language
- `### HtmlEmbed` section added to docs/src/json-ui/components.md with all eight required content blocks per D-21
- Full CI gate green (`cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features`)
- EMBED-04 and EMBED-05 satisfied
- All five safety-messaging surfaces live
- Phase 148 closes
</success_criteria>

<output>
After completion, create `.planning/phases/148-htmlembed-component-ferro-json-ui/148-05-SUMMARY.md`
capturing: exact text of CatalogComponent description committed, exact text of
COMPONENT_CATALOG entry committed, blockquote style chosen for the docs callout
(which existing docs convention was matched), confirmation no_required is unchanged,
the exit codes of the three CI-gate commands, and a phase-close checklist verifying all
five safety-messaging surfaces are live.
</output>
