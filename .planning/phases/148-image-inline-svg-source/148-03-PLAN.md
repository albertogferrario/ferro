---
phase: 148
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - ferro-json-ui/src/lib.rs
  - ferro-mcp/src/tools/json_ui_catalog.rs
  - docs/src/json-ui/components.md
autonomous: true
requirements: [IMG-SRC-04, IMG-SRC-05]
must_haves:
  truths:
    - "COMPONENT_CATALOG in ferro-json-ui/src/lib.rs has a ### Image section documenting both source variants with a one-line safety note on the SVG branch (D-13 — pre-existing catalog gap closed)"
    - "ferro-mcp CatalogComponent for Image widened: description describes dual-source shape, props list contains both src (optional) and svg (optional), with a safety note on the svg prop (D-14)"
    - "ferro-mcp catalog exhaustive-list assertion stays at 41 components — Image is one component with two variants, NOT two components (D-08)"
    - "docs/src/json-ui/components.md has a ### Image section (pre-existing gap closed per D-15) with props table covering the flattened shape, safety callout blockquote on svg, Rust examples for both constructors, JSON examples for both variants, use-case list for inline SVG, and an explicit 'no generic HTML escape hatch' pointer"
    - "Full CI gate green workspace-wide: cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features — the IMG-SRC-05 deliverable"
    - "ferro-mcp test_all_components_present continues to pass asserting 41 components (no count bump)"
  artifacts:
    - path: "ferro-json-ui/src/lib.rs"
      provides: "### Image section added to COMPONENT_CATALOG string constant, documenting both source variants and the SVG-branch safety note"
      contains: "### Image"
    - path: "ferro-mcp/src/tools/json_ui_catalog.rs"
      provides: "Updated Image CatalogComponent entry with dual-source description, src + svg props (both optional), safety note on svg prop; count stays 41"
      contains: "Bounded visual asset"
    - path: "docs/src/json-ui/components.md"
      provides: "### Image section added with props table + safety callout + Rust + JSON examples + use-case list + no-generic-HTML-escape-hatch pointer"
      contains: "### Image"
  key_links:
    - from: "COMPONENT_CATALOG ### Image block"
      to: "D-13 substance"
      via: "narrative that names both variants and the SVG safety scope"
      pattern: "both source variants|src OR svg"
    - from: "MCP CatalogComponent Image entry"
      to: "D-14 substance"
      via: "prop() entries for src + svg + safety-scoped description text"
      pattern: "emits the svg string verbatim|server-constructed"
    - from: "docs/src/json-ui/components.md ### Image safety callout"
      to: "D-15 substance"
      via: "blockquote scoped to the svg variant"
      pattern: "Safety note|emitted verbatim"
---

<objective>
Close the three documentation surfaces that must reflect the dual-source shape and the
SVG-branch safety scope (D-13, D-14, D-15) and run the full CI gate as the phase's
final completion criterion (IMG-SRC-05). Two of the three edits (COMPONENT_CATALOG,
docs/json-ui/components.md) close PRE-EXISTING documentation gaps — the Image component
was never documented in either catalog despite being shipped in the component set.
The third edit (ferro-mcp CatalogComponent) widens an existing entry.

Purpose: The InlineSvg branch is the one deliberate html_escape bypass in the crate.
The safety framing is load-bearing at five sites (rustdoc on the enum variant + rustdoc
on the constructor + inline render comment + the three documentation surfaces in this
plan). Plan 02 shipped the first three sites (the Rust-language sites); this plan ships
the three agent/human-reader surfaces. Together they satisfy D-12 through D-15.

Output: Three file edits. No new code. No test changes (except verifying the existing
test_all_components_present assertion at 41 remains green). Final full-workspace CI
gate invocation as the phase-level ship-off check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-image-inline-svg-source/148-CONTEXT.md
@.planning/phases/148-image-inline-svg-source/148-RESEARCH.md
@.planning/phases/148-image-inline-svg-source/148-PATTERNS.md
@.planning/phases/148-image-inline-svg-source/148-VALIDATION.md
@.planning/phases/148-image-inline-svg-source/148-02-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Existing COMPONENT_CATALOG shape (ferro-json-ui/src/lib.rs:103-179, VERIFIED) — narrative markdown inside a const &str. Image is currently ABSENT. -->

<!-- ### Avatar entry density (lib.rs:167-168, VERIFIED) — target density to mirror: -->
```
### Avatar
Props: src (Option<String>), alt (String), fallback (Option<String>), size (Option<xs|sm|default|lg>)
```

<!-- ### Separator entry (lib.rs:149, VERIFIED) — minimal density: -->
```
### Separator
Props: orientation (Option<horizontal|vertical>)
```

<!-- ### StatCard / ### DescriptionList density (two-line entries when the component warrants it) — used as template for Image (dual-source needs two lines). -->

<!-- Existing ferro-mcp Image CatalogComponent (ferro-mcp/src/tools/json_ui_catalog.rs:1113-1134, VERIFIED) — pre-refactor: -->
```rust
CatalogComponent {
    name: "Image".to_string(),
    description: "Image element with optional aspect ratio and skeleton placeholder fallback on load error.".to_string(),
    props: vec![
        prop("src", "String", true, "Image source URL"),
        prop("alt", "String", true, "Alt text for accessibility"),
        prop("aspect_ratio", "Option<String>", false, "CSS aspect ratio (e.g., \"16/9\")"),
        prop("placeholder_label", "Option<String>", false, "Label shown in the skeleton placeholder behind the image"),
    ],
    variants: None,
},
```

<!-- prop() helper signature (json_ui_catalog.rs:1194-1201, VERIFIED): prop(name, type_name, required, description) — both required: false for src and svg (the invariant "exactly one" is expressed in the description, not the individual prop required-flags). -->

<!-- test_all_components_present (json_ui_catalog.rs:1208-1264, VERIFIED) — currently asserts 41 components. DO NOT bump. This plan verifies it stays 41. -->

<!-- docs/src/json-ui/components.md — ### Avatar section at L623-654 (VERIFIED) — exact density template. ### StatCard at L795-843 (VERIFIED) — icon-prop safety prose template. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ### Image section to COMPONENT_CATALOG in ferro-json-ui/src/lib.rs</name>
  <files>ferro-json-ui/src/lib.rs</files>
  <read_first>
    - ferro-json-ui/src/lib.rs (lines 100-186 — COMPONENT_CATALOG const with all existing ### sections; locate the insertion point between ### Separator / ### Avatar / ### Skeleton, wherever is closest to a natural "visual leaf" grouping)
    - ferro-json-ui/src/lib.rs (lines 149 — ### Separator entry for density reference)
    - ferro-json-ui/src/lib.rs (lines 167-168 — ### Avatar entry for density reference)
    - .planning/phases/148-image-inline-svg-source/148-CONTEXT.md (D-13 — what Image entry MUST contain)
    - .planning/phases/148-image-inline-svg-source/148-PATTERNS.md (§3 — exact target text)
  </read_first>
  <action>
    Open `ferro-json-ui/src/lib.rs`.

    Inspect the `pub const COMPONENT_CATALOG: &str = r#"…"#;` block (starts near L103).
    Find a natural neighbor for Image — candidates (in order of preference):
      1. Between `### Avatar` (L167-168) and the next `### …` heading — both are
         visual-leaf bounded assets.
      2. Near `### Skeleton` if it groups placeholder-style components.
      3. Near `### Separator` if it groups structural leaves.

    Choose the location closest to `### Avatar` since both are bounded visual assets.

    Insert a new section. The section must contain: (a) both source variants by name,
    (b) flattened wire-shape (src OR svg, plus alt, aspect_ratio, placeholder_label),
    (c) one-line safety note scoped to the svg variant per D-13. Target text:

    ```
    ### Image
    Props: src (Option<String>) OR svg (Option<String>) — exactly one required; alt (String, required for accessibility); aspect_ratio (Option<String>, e.g. "16/9"); placeholder_label (Option<String>, URL variant only)
    Bounded visual asset rendered into a box. URL variant (src): the attribute is HTML-escaped; placeholder_label drives the skeleton fallback. SVG variant (svg): the string is emitted verbatim, intended for server-constructed SVG (charts, sparklines, icons) — not user input. alt is required on both variants (compile-enforced accessibility).
    ```

    Preserve a blank line before and after the new section, matching the existing
    inter-section spacing in the catalog string.

    Save the file. Do NOT alter any other section. Do NOT add the `src` and `svg`
    parameters to two separate rows — they share one row because they are mutually
    exclusive in the wire format.
  </action>
  <verify>
    <automated>grep -c '### Image' ferro-json-ui/src/lib.rs &amp;&amp; grep -q 'src (Option&lt;String&gt;) OR svg' ferro-json-ui/src/lib.rs &amp;&amp; grep -q 'emitted verbatim' ferro-json-ui/src/lib.rs &amp;&amp; cargo check -p ferro-json-ui 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '### Image' ferro-json-ui/src/lib.rs` returns exactly 1 (new section added; no duplicate)
    - `grep -q 'src (Option<String>) OR svg (Option<String>)' ferro-json-ui/src/lib.rs` succeeds (dual-source row)
    - `grep -q 'exactly one required' ferro-json-ui/src/lib.rs` succeeds (invariant named)
    - `grep -q 'emitted verbatim' ferro-json-ui/src/lib.rs` succeeds (safety substance on svg variant — D-13 required content)
    - `grep -q 'not user input' ferro-json-ui/src/lib.rs` succeeds (safety intent named)
    - `grep -q 'server-constructed SVG' ferro-json-ui/src/lib.rs` succeeds (use-case named)
    - `grep -q 'compile-enforced accessibility' ferro-json-ui/src/lib.rs` succeeds (alt-required posture surfaced)
    - No existing catalog section was modified — `git diff ferro-json-ui/src/lib.rs | grep -E '^-### ' | wc -l` returns 0
    - `cargo check -p ferro-json-ui` exits 0 (catalog is a &str — no compile impact from content, but guards against stray edits)
    - `cargo fmt --all -- --check` exits 0
  </acceptance_criteria>
  <done>
    COMPONENT_CATALOG now has a `### Image` section documenting both source variants
    with the one-line safety note on the SVG branch. Pre-existing catalog gap (Image
    was never documented despite shipping in the component set) closed per D-13.
  </done>
</task>

<task type="auto">
  <name>Task 2: Widen ferro-mcp CatalogComponent for Image to describe both source variants</name>
  <files>ferro-mcp/src/tools/json_ui_catalog.rs</files>
  <read_first>
    - ferro-mcp/src/tools/json_ui_catalog.rs (lines 1113-1134 — current Image CatalogComponent entry; exact rewrite target)
    - ferro-mcp/src/tools/json_ui_catalog.rs (lines 1194-1201 — prop() helper signature)
    - ferro-mcp/src/tools/json_ui_catalog.rs (lines 1208-1264 — test_all_components_present asserting 41 components; DO NOT bump the count)
    - .planning/phases/148-image-inline-svg-source/148-CONTEXT.md (D-14)
    - .planning/phases/148-image-inline-svg-source/148-PATTERNS.md (§4 — exact target CatalogComponent shape)
  </read_first>
  <action>
    Open `ferro-mcp/src/tools/json_ui_catalog.rs`.

    Locate the existing Image entry at L1113-1134. Replace the whole CatalogComponent
    struct-literal block for Image VERBATIM with the following widened entry. The
    narrative description surfaces the dual-source shape, the "exactly one" invariant,
    the URL-branch escape posture, the SVG-branch verbatim-emission scope, and the
    compile-enforced alt requirement. The props list now carries both src and svg,
    each with required=false because the "exactly one of" constraint lives at the
    untagged-enum level and the description carries the invariant:

    ```rust
    CatalogComponent {
        name: "Image".to_string(),
        description: "Bounded visual asset rendered into a box. Accepts either an \
                      external URL (src) or a server-constructed inline SVG string \
                      (svg) — exactly one of the two must be set. The URL variant \
                      HTML-escapes the src attribute; the SVG variant emits the svg \
                      string verbatim (intended for server-constructed SVG — charts, \
                      sparklines, icons — not user input). alt is required on both \
                      variants (compile-enforced accessibility). placeholder_label \
                      applies to the URL variant only."
            .to_string(),
        props: vec![
            prop(
                "src",
                "String",
                false,
                "Image source URL (URL variant — use when svg is absent). \
                 HTML-escaped as an attribute value.",
            ),
            prop(
                "svg",
                "String",
                false,
                "Inline SVG string emitted verbatim (SVG variant — use when src is absent). \
                 Server-constructed SVG only; not suitable for user input.",
            ),
            prop(
                "alt",
                "String",
                true,
                "Alt text for accessibility — required on both source variants.",
            ),
            prop(
                "aspect_ratio",
                "Option<String>",
                false,
                "CSS aspect ratio (e.g., \"16/9\").",
            ),
            prop(
                "placeholder_label",
                "Option<String>",
                false,
                "Label shown in the skeleton placeholder behind the image (URL variant only).",
            ),
        ],
        variants: None,
    },
    ```

    Save. Run `cargo fmt --all`.

    Do NOT touch `test_all_components_present` at L1208-1264 — the count stays at 41.
    Image remains one entry; Phase 148 extends its schema, not the catalog cardinality.
  </action>
  <verify>
    <automated>cargo test -p ferro-mcp test_all_components_present 2&gt;&amp;1 | tail -5 &amp;&amp; grep -q 'Bounded visual asset rendered into a box' ferro-mcp/src/tools/json_ui_catalog.rs &amp;&amp; grep -q 'SVG variant' ferro-mcp/src/tools/json_ui_catalog.rs &amp;&amp; cargo clippy -p ferro-mcp --all-targets -- -D warnings 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Bounded visual asset rendered into a box' ferro-mcp/src/tools/json_ui_catalog.rs` succeeds (new narrative present)
    - `grep -q 'exactly one of the two must be set' ferro-mcp/src/tools/json_ui_catalog.rs` succeeds (invariant named in description)
    - `grep -q 'emits the svg string verbatim' ferro-mcp/src/tools/json_ui_catalog.rs` succeeds (safety note in description)
    - `grep -q 'not user input' ferro-mcp/src/tools/json_ui_catalog.rs` succeeds
    - `grep -q '"svg",' ferro-mcp/src/tools/json_ui_catalog.rs` succeeds (svg prop added)
    - `grep -q 'use when svg is absent' ferro-mcp/src/tools/json_ui_catalog.rs` succeeds (src prop description updated)
    - `grep -q 'use when src is absent' ferro-mcp/src/tools/json_ui_catalog.rs` succeeds (svg prop description)
    - `grep -q 'URL variant only' ferro-mcp/src/tools/json_ui_catalog.rs` succeeds (placeholder_label scope clarified)
    - The old one-line description is gone — `grep -q 'Image element with optional aspect ratio and skeleton placeholder fallback on load error' ferro-mcp/src/tools/json_ui_catalog.rs` returns non-zero (string no longer present)
    - `cargo test -p ferro-mcp test_all_components_present` passes (count stays at 41 — Image still counts as one entry)
    - `cargo clippy -p ferro-mcp --all-targets -- -D warnings` exits 0
    - `cargo fmt --all -- --check` exits 0
  </acceptance_criteria>
  <done>
    The existing Image CatalogComponent entry in ferro-mcp is widened per D-14: the
    description communicates the dual-source shape and the SVG safety scope; the props
    list carries both src and svg with scoped descriptions. Catalog count stays 41
    (Image is one component with two variants, not two components) per D-08. The
    test_all_components_present assertion stays green without a count bump.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add ### Image section to docs/src/json-ui/components.md with props table, safety callout, examples, and CI gate</name>
  <files>docs/src/json-ui/components.md</files>
  <read_first>
    - docs/src/json-ui/components.md (lines 623-654 — ### Avatar section; density template to mirror)
    - docs/src/json-ui/components.md (lines 795-843 — ### StatCard section; icon-prop safety prose template)
    - docs/src/json-ui/components.md (full TOC scan — find where Image belongs alphabetically / semantically; component docs tend to be alphabetical with exceptions)
    - .planning/phases/148-image-inline-svg-source/148-CONTEXT.md (D-15 — what the Image doc section MUST contain)
    - .planning/phases/148-image-inline-svg-source/148-PATTERNS.md (§5 — section structure + blockquote style + example templates)
  </read_first>
  <action>
    Open `docs/src/json-ui/components.md`.

    Scan the file for the correct insertion point for `### Image`. The components
    chapter is organized primarily alphabetically — Image belongs between whatever
    component sorts just above "Image" (likely `### Header`, `### IconPosition`, or
    an HTML element section) and whatever sorts just after (likely `### Input` or
    `### KanbanBoard`). If the file deviates from alphabetical for any reason, match
    the existing convention of the surrounding sections.

    Insert a new `### Image` section with the following structure. Use the `### Avatar`
    section at L623-654 as the density template.

    ```markdown
    ### Image

    Renders a bounded visual asset — either an external image via URL, or a
    server-constructed inline SVG.

    **Props**

    | Name                | Type              | Required  | Description                                                                             |
    |---------------------|-------------------|-----------|-----------------------------------------------------------------------------------------|
    | `src`               | `String`          | one-of    | Image source URL (URL variant). Attribute is HTML-escaped.                              |
    | `svg`               | `String`          | one-of    | Inline SVG emitted verbatim (SVG variant). See **Safety note** below.                   |
    | `alt`               | `String`          | yes       | Alt text for accessibility — required on both variants (compile-enforced).              |
    | `aspect_ratio`      | `Option<String>`  | no        | CSS aspect ratio (e.g., `"16/9"`).                                                      |
    | `placeholder_label` | `Option<String>`  | no        | Label shown in the skeleton placeholder (URL variant only).                             |

    Exactly one of `src` or `svg` must be set. Backward-compatibility note: existing
    JSON sending `{"type":"Image","src":"…","alt":"…"}` continues to work unchanged.

    > **Safety note — `svg` variant:** The `svg` value is emitted verbatim without
    > HTML escaping. Intended for server-constructed SVG (charts, sparklines, icons).
    > **Not suitable for user-supplied strings.** Callers that incorporate user data
    > into the SVG output are responsible for sanitization before constructing the
    > `svg` variant. The `alt` attribute is HTML-escaped on both variants.

    **Rust**

    ```rust
    use ferro_json_ui::{ComponentNode, ImageProps};

    // URL variant
    let url_node = ComponentNode::image(
        "hero",
        ImageProps::url("/img/hero.png", "Hero image"),
    );

    // SVG variant — server-constructed chart (e.g. from a Rust helper)
    let svg = bar_chart_svg(&weekly_data, 800, 300);
    let chart_node = ComponentNode::image(
        "revenue-chart",
        ImageProps::inline_svg(svg, "Incassi settimanali: 150€ lun, 320€ mar, …"),
    );
    ```

    **JSON**

    URL variant:

    ```json
    {
      "type": "Image",
      "src": "/img/hero.png",
      "alt": "Hero image",
      "aspect_ratio": "16/9"
    }
    ```

    SVG variant:

    ```json
    {
      "type": "Image",
      "svg": "<svg viewBox=\"0 0 800 300\">…</svg>",
      "alt": "Incassi settimanali: 150€ lun, 320€ mar, …"
    }
    ```

    **Use cases for the SVG variant**

    - Server-rendered charts (bar, line, sparkline)
    - Diagrams assembled from typed data on the server
    - Decorative vector assets constructed by Rust code
    - Server-rendered icon sets

    **No generic HTML escape hatch.** For rendering HTML (not SVG), no generic
    `HtmlEmbed`-style component exists — by design. If a real use-case demands
    HTML embedding, author a narrower component scoped to the specific content
    shape rather than a generic string-to-HTML escape hatch.
    ```

    Save the file.

    After saving all three files from this plan, run the phase-level CI gate. This is
    the IMG-SRC-05 completion criterion:

    ```
    cargo fmt --all -- --check
    cargo clippy --all --all-targets -- -D warnings
    cargo test --all-features
    ```

    All three must exit 0. If clippy flags a doc-comment formatting warning caused by
    the new catalog text, fix it in-place. If a test fails, diagnose whether it is a
    regression from Plan 02 (in which case fix there) or a real issue surfaced by the
    new surface (in which case report in the summary).

    Do NOT alter Avatar's docs (the known `src: Option<String>` drift at L629 — out
    of scope per CONTEXT.md, tracked for a later docs-cleanup phase). Do NOT modify
    any other `###` section in components.md.
  </action>
  <verify>
    <automated>grep -c '^### Image$' docs/src/json-ui/components.md &amp;&amp; grep -q 'Safety note — `svg` variant' docs/src/json-ui/components.md &amp;&amp; grep -q 'ImageProps::inline_svg' docs/src/json-ui/components.md &amp;&amp; grep -q 'No generic HTML escape hatch' docs/src/json-ui/components.md &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo clippy --all --all-targets -- -D warnings 2&gt;&amp;1 | tail -5 &amp;&amp; cargo test --all-features 2&gt;&amp;1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### Image$' docs/src/json-ui/components.md` returns exactly 1 (new section added; no duplicate heading)
    - `grep -q 'Renders a bounded visual asset' docs/src/json-ui/components.md` succeeds (opening paragraph present)
    - `grep -q '| `src`' docs/src/json-ui/components.md` succeeds (props table row for src)
    - `grep -q '| `svg`' docs/src/json-ui/components.md` succeeds (props table row for svg)
    - `grep -q '| `alt`' docs/src/json-ui/components.md` succeeds (props table row for alt)
    - `grep -q 'Exactly one of `src` or `svg` must be set' docs/src/json-ui/components.md` succeeds (invariant named)
    - `grep -q 'Safety note — `svg` variant' docs/src/json-ui/components.md` succeeds (blockquote present)
    - `grep -q 'emitted verbatim without' docs/src/json-ui/components.md` succeeds (safety substance)
    - `grep -q 'Not suitable for user-supplied strings' docs/src/json-ui/components.md` succeeds (safety intent)
    - `grep -q 'ImageProps::url(' docs/src/json-ui/components.md` succeeds (Rust example for URL variant)
    - `grep -q 'ImageProps::inline_svg(' docs/src/json-ui/components.md` succeeds (Rust example for SVG variant)
    - `grep -q '"type": "Image"' docs/src/json-ui/components.md` succeeds (JSON examples present)
    - `grep -q '"svg":' docs/src/json-ui/components.md` succeeds (JSON SVG example present)
    - `grep -q 'Use cases for the SVG variant' docs/src/json-ui/components.md` succeeds (use-case list header)
    - `grep -q 'No generic HTML escape hatch' docs/src/json-ui/components.md` succeeds (explicit pointer present per D-15)
    - No existing `### …` section was modified — `git diff docs/src/json-ui/components.md | grep -E '^-### [A-Z]' | wc -l` returns 0
    - **Phase-level CI gate (IMG-SRC-05):**
      - `cargo fmt --all -- --check` exits 0
      - `cargo clippy --all --all-targets -- -D warnings` exits 0
      - `cargo test --all-features` exits 0 across the workspace
    - ferro-mcp test still green: `cargo test -p ferro-mcp test_all_components_present` passes at 41 components
    - No regression: all Plan 01 + Plan 02 tests remain GREEN (10 Phase-148 tests, plus unchanged suites in phases 146/147)
  </acceptance_criteria>
  <done>
    docs/src/json-ui/components.md has a full `### Image` section with props table,
    safety callout blockquote, Rust examples for both constructors, JSON examples for
    both variants, use-case list, and the explicit "no generic HTML escape hatch"
    pointer — the pre-existing docs gap closed per D-15. The phase-level CI gate
    (`cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings
    && cargo test --all-features`) is green workspace-wide. Phase 148 is ready to
    ship — all five IMG-SRC-0N requirements are satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP catalog consumer → agent | Agents reading the catalog via MCP rely on the description and prop-list to understand what is safe to put in `svg` vs `src`. The widened description makes the dual-source shape and SVG safety scope discoverable. |
| Docs reader → human developer | Developers reading docs/src/json-ui/components.md reach a decision point about SVG construction. The safety callout blockquote is the load-bearing surface element — it must be visible, not buried. |
| COMPONENT_CATALOG runtime string → consumer | The runtime catalog string is queried by agents during JSON-UI authoring. The `### Image` section is the agent-facing surface for the dual-source shape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-05 | Information Disclosure | MCP / catalog / docs fail to surface the svg-variant safety scope; future callers assume symmetry with `src` and ship user-supplied SVG unescaped | mitigate | D-13 / D-14 / D-15 require the safety note at all three agent/human-reader surfaces. This plan's acceptance criteria include grep-level checks that each surface contains the required substance ("emitted verbatim", "not user input", "server-constructed"). The rustdoc surface (Plan 02) documents the Rust-caller contract; this plan documents the JSON-caller and agent-reader contracts. Residual risk: a caller who ignores the safety note constructs from user data; accepted — the bypass is the phase's deliberate design decision. |
| T-148-06 | Tampering | ferro-mcp catalog count drift (bump to 42 accidentally) — would create an invalid JsonUiProducer contract since there are still only 41 components | mitigate | This plan's acceptance criteria explicitly require the count to stay at 41 (D-08). The `test_all_components_present` assertion is the regression guard and is included in the Task 2 and Task 3 verify commands. |
</threat_model>

<verification>
- `grep -c '### Image' ferro-json-ui/src/lib.rs` == 1
- `grep -q 'Bounded visual asset' ferro-mcp/src/tools/json_ui_catalog.rs` succeeds
- `grep -c '^### Image$' docs/src/json-ui/components.md` == 1
- `cargo test -p ferro-mcp test_all_components_present` — GREEN (count stays at 41)
- `cargo fmt --all -- --check` — GREEN
- `cargo clippy --all --all-targets -- -D warnings` — GREEN workspace-wide
- `cargo test --all-features` — GREEN workspace-wide (phase-level CI gate, IMG-SRC-05 deliverable)
- Plan 01 + Plan 02 tests remain GREEN (no regression)
</verification>

<success_criteria>
- COMPONENT_CATALOG has a new `### Image` section with dual-source props and SVG safety note (D-13)
- ferro-mcp CatalogComponent for Image widened: description covers both variants, props list carries src + svg, count stays 41 (D-14, D-08)
- docs/src/json-ui/components.md has a full `### Image` section with props table, safety blockquote, Rust + JSON examples, use-case list, and "no generic HTML escape hatch" pointer (D-15)
- Phase-level CI gate green: `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` (IMG-SRC-05)
- No regression in Plan 01 or Plan 02 tests
- Pre-1.0 repository instruction honored: no co-author line in any commit this plan produces
</success_criteria>

<output>
After completion, create `.planning/phases/148-image-inline-svg-source/148-03-SUMMARY.md`
capturing:
- Exact line where the `### Image` section landed in `ferro-json-ui/src/lib.rs` (neighbor
  section it was inserted next to)
- Exact line where the `### Image` section landed in `docs/src/json-ui/components.md`
  (neighbors it was inserted between)
- Confirmation that ferro-mcp `test_all_components_present` stayed green at 41
- CI gate output summary: fmt OK, clippy OK (workspace), test OK (workspace) — explicit
  confirmation per IMG-SRC-05
- Total number of Phase-148 tests flipped RED→GREEN across the three plans (expected: 10 —
  5 image_source_tests + 1 image_round_trips (extended) + 1 all_known_types_round_trip
  (extended) + 3 inline_svg_ render tests)
- Any deviations from the planned edits (e.g. if clippy surfaced a warning in the new
  docs code block that required a formatting tweak)
</output>
</content>
</invoke>