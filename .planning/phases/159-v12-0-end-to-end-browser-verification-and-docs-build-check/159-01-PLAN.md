---
phase: 159-v12-0-end-to-end-browser-verification-and-docs-build-check
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/mdbook-build.log
  - .planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/DOCS-CHECK.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "mdbook build docs/ exits with code 0 on the current branch"
    - "No internal broken links (links targeting files inside docs/src/) are reported by mdbook"
    - "No SUMMARY.md entry references a file missing from disk (create-missing = false in book.toml)"
    - "External URL warnings, if any, are documented as non-blocking and listed verbatim"
    - "The full mdbook build stdout+stderr is captured to a log file in the phase directory as evidence"
  artifacts:
    - path: ".planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/mdbook-build.log"
      provides: "Verbatim stdout+stderr from mdbook build docs/ (evidence per D-10)"
      min_lines: 1
    - path: ".planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/DOCS-CHECK.md"
      provides: "Pass/fail report for the docs build check, with exit code, internal-link verdict, and external-link list"
      contains: "Exit code: 0"
  key_links:
    - from: "docs/book.toml"
      to: "docs/src/SUMMARY.md"
      via: "mdbook resolves every SUMMARY entry against the filesystem"
      pattern: "create-missing\\s*=\\s*false"
    - from: "DOCS-CHECK.md"
      to: "mdbook-build.log"
      via: "report cites the log as evidence"
      pattern: "mdbook-build\\.log"
---

<objective>
Run the mdbook docs build check and produce the pass/fail evidence record for the first half of the Phase 159 gate.

Purpose: Verify the rewritten JSON-UI documentation (Phase 121 output) builds clean with no internal broken links and no missing SUMMARY.md entries, before Phase 160 (v1 API removal) begins. This is the no-server half of the phase gate — it runs first per D-12 (Claude's Discretion: docs build first, then browser test).

Output: A captured `mdbook-build.log` and a `DOCS-CHECK.md` report stating PASS or FAIL with verbatim evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/159-CONTEXT.md
@.planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/159-RESEARCH.md

# Source of truth for the docs build
@docs/book.toml
@docs/src/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run mdbook build and capture full output</name>
  <files>
    .planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/mdbook-build.log
  </files>
  <read_first>
    - docs/book.toml (confirm build-dir is `book` and create-missing = false)
    - docs/src/SUMMARY.md (orient on what pages mdbook will resolve)
    - .planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/159-RESEARCH.md (verified during research that mdbook build exits 0 on current branch)
  </read_first>
  <action>
    From the workspace root (/Users/alberto/repositories/albertogferrario/ferro), run the docs build exactly once and capture every byte of stdout AND stderr to a single log file in the phase directory. Use this command exactly:

    `mdbook build docs/ > .planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/mdbook-build.log 2>&1; echo "EXIT_CODE=$?" >> .planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/mdbook-build.log`

    Do NOT use any flags other than `build docs/` — per D-07 / research, mdbook v0.5.2 has no `--no-follow-web-links` flag, do not invent one. Do NOT clean or wipe `docs/book/` before the run; mdbook handles its own incremental state.

    After the command completes, read the log back and confirm the final line is `EXIT_CODE=0`. If it is not 0, the task fails — do NOT attempt any fix in this task; record the failure for Task 2 to report.
  </action>
  <verify>
    <automated>tail -1 .planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/mdbook-build.log | grep -q '^EXIT_CODE=0$'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/mdbook-build.log` exists
    - Last line of the log matches the regex `^EXIT_CODE=0$` (grep-verifiable)
    - The log contains stdout and stderr from a single `mdbook build docs/` invocation (no second build appended)
    - `docs/book/index.html` exists after the build (the build wrote its output)
    - No second log file was created; only `mdbook-build.log` is produced by this task
  </acceptance_criteria>
  <done>
    `mdbook-build.log` exists with `EXIT_CODE=0` on the final line and the mdbook output is preserved verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 2: Classify mdbook output and write DOCS-CHECK.md verdict</name>
  <files>
    .planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/DOCS-CHECK.md
  </files>
  <read_first>
    - .planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/mdbook-build.log (the captured output from Task 1)
    - docs/src/SUMMARY.md (the list of every linked page mdbook will resolve)
    - .planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/159-CONTEXT.md (decisions D-05 through D-08 define pass/fail rules)
  </read_first>
  <action>
    Classify every line in `mdbook-build.log` against Phase 159 decisions:

    - **D-05 (blocking):** Exit code must be 0. Confirm by reading the final `EXIT_CODE=` line.
    - **D-06 (blocking):** Any error or warning naming an internal link (a path inside `docs/src/`, e.g. `./foo.md`, `../bar.md`, anchor-only links) is a blocking failure. Search the log for `ERROR`, `error:`, `WARN`, `warning:` and inspect each.
    - **D-07 (non-blocking):** Any error/warning naming an external URL (starts with `http://` or `https://`) is non-blocking. List them in the report; do NOT mark the phase as failed for these.
    - **D-08 (blocking):** Any message about a missing file referenced from `SUMMARY.md` is a blocking failure. `create-missing = false` in `docs/book.toml` means mdbook will surface missing files as errors, not auto-create them.

    Write `.planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/DOCS-CHECK.md` with EXACTLY this structure:

    ```
    # Phase 159 — Docs Build Check

    **Verdict:** PASS  (or FAIL)
    **Command:** `mdbook build docs/`
    **Exit code:** 0
    **Log:** [mdbook-build.log](./mdbook-build.log)
    **Run at:** <ISO 8601 timestamp>

    ## Blocking issues (D-05, D-06, D-08)

    - None

    ## Non-blocking external link warnings (D-07)

    - <list verbatim, or "None">

    ## Verbatim mdbook output

    ```
    <paste the entire content of mdbook-build.log here, fenced as a code block>
    ```
    ```

    Replace `0` with the actual exit code from the log. If `Verdict` is `FAIL`, the "Blocking issues" section must list every blocking line with its exact text from the log (no paraphrasing). Use real ISO 8601 (`date -u +%Y-%m-%dT%H:%M:%SZ`).

    Do NOT attempt to fix any blocking issue in this task — the plan deliverable is the verdict + evidence. Any fix happens in a follow-up plan or a revision pass, per D-11 "minimal fix if trivial". This plan stops at reporting.
  </action>
  <verify>
    <automated>grep -E '^\*\*Verdict:\*\* (PASS|FAIL)' .planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/DOCS-CHECK.md && grep -E '^\*\*Exit code:\*\* [0-9]+' .planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/DOCS-CHECK.md && grep -F 'mdbook-build.log' .planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/DOCS-CHECK.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/DOCS-CHECK.md` exists
    - Line matching `^\*\*Verdict:\*\* (PASS|FAIL)$` is present
    - Line matching `^\*\*Exit code:\*\* [0-9]+$` is present and reflects the actual exit code from `mdbook-build.log`
    - The report cites `mdbook-build.log` by name (grep `mdbook-build.log` returns at least one hit)
    - The report contains three required sections by exact heading: `## Blocking issues (D-05, D-06, D-08)`, `## Non-blocking external link warnings (D-07)`, `## Verbatim mdbook output`
    - If Verdict is PASS: `grep -E 'ERROR|error:' .planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/mdbook-build.log` returns no lines describing internal links (paths inside docs/src/) OR returns nothing at all
    - The verbatim mdbook output section contains a fenced code block with the full log content (not truncated)
  </acceptance_criteria>
  <done>
    `DOCS-CHECK.md` exists with a clear PASS/FAIL verdict, exit-code line, and verbatim evidence. The docs half of the phase gate is decided.
  </done>
</task>

</tasks>

<verification>
The plan is complete when:

1. `mdbook-build.log` exists with `EXIT_CODE=0` on its last line (Task 1).
2. `DOCS-CHECK.md` exists with a PASS verdict and cites the log (Task 2).
3. No new files outside the phase directory have been created or modified.
4. `docs/book/` may have been (re)written by mdbook — that is expected and not a concern.

Full automated check (run from workspace root):

```
tail -1 .planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/mdbook-build.log | grep -q '^EXIT_CODE=0$' && grep -q '^\*\*Verdict:\*\* PASS$' .planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/DOCS-CHECK.md
```
</verification>

<success_criteria>
- mdbook build evidence captured to log file (D-10)
- Verdict report classifies output against D-05, D-06, D-07, D-08
- If FAIL: blocking lines are listed verbatim from the log
- If PASS: the docs half of the Phase 159 gate is closed; Plan 02 (browser test) can proceed
</success_criteria>

<output>
After completion, create `.planning/phases/159-v12-0-end-to-end-browser-verification-and-docs-build-check/159-01-SUMMARY.md` recording:
- Exit code and verdict
- Whether any blocking issues were found, and if so, what they were
- The path to `DOCS-CHECK.md` and `mdbook-build.log`
- Confirmation that Plan 02 may proceed (verdict = PASS) or must not proceed (verdict = FAIL)
</output>
