---
phase: 129-publish-workflow-refinement
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - PUBLISHING.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "PUBLISHING.md contains a `## Version Model` section explaining lockstep versioning, the single global `ferro_version` field, and the reserved `ferro_versions` override hook."
    - "PUBLISHING.md contains a `## Publish Gating` section explaining the library-change rule from plan 01 and listing the excluded paths verbatim."
    - "The excluded-paths list in PUBLISHING.md matches the case-statement globs in .github/workflows/publish.yml character-for-character."
    - "Both sections are written in neutral, scientific, minimalistic voice per CLAUDE.md — no marketing language, no internal-strategy framing."
  artifacts:
    - path: "PUBLISHING.md"
      provides: "Version Model + Publish Gating sections"
      contains: "## Version Model"
  key_links:
    - from: "PUBLISHING.md Publish Gating section"
      to: ".github/workflows/publish.yml gate step"
      via: "excluded-paths list must match workflow case statement"
      pattern: "ferro-cli/\\*|app/\\*"
---

<objective>
Document the lockstep version model and the library-change publish gate in `PUBLISHING.md`. Closes the documentation half of REPORT §§8 and 14.

Purpose: make the release semantics and the gate rule discoverable without reading YAML or source. Reserve the forward-compatibility note for `ferro_versions` so future contributors know the hook exists.
Output: two new sections in `PUBLISHING.md`, written in neutral architectural voice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/129-publish-workflow-refinement/129-CONTEXT.md
@.planning/phases/129-publish-workflow-refinement/129-RESEARCH.md
@.planning/phases/126-deploy-experience-feedback/REPORT.md
@PUBLISHING.md
@.github/workflows/publish.yml
@.planning/phases/129-publish-workflow-refinement/129-01-SUMMARY.md
@.planning/phases/129-publish-workflow-refinement/129-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Version Model and Publish Gating sections to PUBLISHING.md</name>
  <files>PUBLISHING.md</files>
  <read_first>
    - PUBLISHING.md (full file — note existing `## Version Coordination` section around line 94)
    - .github/workflows/publish.yml (post-plan-01 — to copy the exclusion list verbatim)
    - .planning/phases/129-publish-workflow-refinement/129-CONTEXT.md (D-10, D-11)
    - .planning/phases/129-publish-workflow-refinement/129-RESEARCH.md (section "`PUBLISHING.md` Structure")
    - CLAUDE.md (global + project) — "repository documents must read as neutral"; trigger phrases to avoid
  </read_first>
  <action>
Edit `PUBLISHING.md` as follows.

**Step A — Replace the existing `## Version Coordination` section** (currently at line ~94: four-line lockstep checklist) with an expanded `## Version Model` section. The replacement must read:

```markdown
## Version Model

Ferro ships in lockstep: every library crate in the workspace is published at
the same version on every release. The authoritative version lives in the
workspace-root `Cargo.toml` `version` field and is propagated to each member
crate on publish.

Consumer projects pin ferro by setting a single `ferro_version` field in
`[package.metadata.ferro.deploy]` inside their own `Cargo.toml`. The
`ferro-cli` `docker:init` command reads this field and rewrites every ferro
path dependency to that version when emitting `Cargo.docker.toml`. One field,
one version, every ferro crate.

Release checklist:

1. Bump the workspace-root `Cargo.toml` `version` field.
2. Commit the version bump as a single atomic change.
3. Push to `master`. The Publish workflow handles tagging and crates.io
   upload per the gating rule documented below.

### Per-crate override reservation

The `[package.metadata.ferro.deploy]` schema also accepts an optional
`ferro_versions` table keyed by crate name:

```toml
[package.metadata.ferro.deploy]
ferro_version = "0.2.0"

# Reserved. Parsed and round-tripped today; not yet consulted by the
# rewrite pipeline. See Phase 129 in .planning/ for the rationale.
# [package.metadata.ferro.deploy.ferro_versions]
# ferro-json-ui = "0.2.1"
```

This field is a schema reservation for the day library crates desync from
the current lockstep cadence. The parser in `ferro-cli` accepts and
round-trips it, but every rewrite still uses the global `ferro_version`
above. When a desync occurs, the rewriter will resolve per-crate overrides
from this table without requiring a breaking schema change in downstream
projects.
```

**Step B — Add a new `## Publish Gating` section** immediately after `## Publishing Order` and before the `### Wave 1` heading. Content must be:

```markdown
## Publish Gating

The Publish workflow (`.github/workflows/publish.yml`) only runs a patch
bump and crates.io release when at least one file under a library crate
path has changed since the last `v*` tag. Pushes that touch only non-library
paths leave the workspace version untouched and exit the workflow after the
`check-version` job.

### Excluded paths

The following paths do NOT count as library changes:

- `ferro-cli/*` — installable binary, not a published library
- `app/*` — sample application, not published
- `docs/*` — user documentation
- `.github/*` — CI configuration
- `.planning/*` — planning notes
- `scripts/*` — developer scripts
- Top-level `*.md` files (`README.md`, `PUBLISHING.md`, `CLAUDE.md`, etc.)
- `LICENSE`
- `Cargo.lock` — regenerated automatically
- Top-level config files: `.gitignore`, `.editorconfig`, `rustfmt.toml`,
  `bacon.toml`, `deny.toml`, `dev.sh`, `llms.txt`, `rust-toolchain.toml`

All other paths — including the workspace-root `Cargo.toml` and every
library crate directory (`framework/`, `ferro-macros/`, `ferro-events/`,
`ferro-queue/`, `ferro-notifications/`, `ferro-broadcast/`, `ferro-storage/`,
`ferro-cache/`, `ferro-mcp/`, `ferro-inertia/`, `ferro-json-ui/`,
`ferro-lang/`, `ferro-api-mcp/`, `ferro-projections/`, `ferro-stripe/`,
`ferro-theme/`, `ferro-ai/`, `ferro-whatsapp/`) — count as library changes.

`ferro-cli` is excluded because it is the only workspace binary consumed
directly by end users via `cargo install`; its own commits must not churn
the versions of the libraries it embeds. `app/` is excluded because it is
a reference application, not a published crate. If future binary-only
crates are added, extend the exclusion list in `publish.yml` accordingly.

### First run

When no `v*` tag exists yet, the gate treats every file as a library
change and the workflow publishes normally. This preserves bootstrap
behavior on an empty tag history.

### Scenarios

| Changed paths since last `v*` tag | Gate output | Result |
|-----------------------------------|-------------|--------|
| Only `docs/` | `should_publish=none` | No bump, no publish |
| Only `ferro-cli/src/...` | `should_publish=none` | No bump, no publish |
| Only `.github/workflows/...` | `should_publish=none` | No bump, no publish |
| Only top-level `*.md` | `should_publish=none` | No bump, no publish |
| `framework/src/lib.rs` | `should_publish=yes` | Bump + publish waves run |
| Workspace-root `Cargo.toml` | `should_publish=yes` | Bump + publish waves run |
| Mix of `docs/` and `framework/` | `should_publish=yes` | Bump + publish waves run |
| No `v*` tag exists yet | `should_publish=yes` | Publish (first run) |
```

**Constraints:**

- Copy the excluded-paths list character-for-character against the case statement in `.github/workflows/publish.yml`. If the workflow lists `bacon.toml`, the doc lists `bacon.toml`. If the workflow excludes `Cargo.lock`, the doc excludes `Cargo.lock`. No drift.
- The workspace-member list in the "All other paths" paragraph must match the current `[workspace] members` in the root `Cargo.toml` (minus `ferro-cli` and `app`). Verify by reading the root `Cargo.toml` before writing.
- Voice: neutral, scientific, minimalistic. No trigger phrases ("killer feature", "the bet", "load-bearing", "forcing function", dated session provenance). Do not reference internal memory files or personal strategy. The text should read as architectural documentation.
- Do NOT add a co-author line to the commit (per `CLAUDE.md`).
- Do NOT touch the existing Wave tables, Path Dependency Handling, Troubleshooting, or Crate Summary sections — even though RESEARCH.md notes they are stale, updating them is out of scope for this phase.
- Preserve the existing `## Prerequisites`, `## Pre-publish Verification`, `## Publishing Order` headings and their content except for the insertion point of the new `## Publish Gating` section.
  </action>
  <verify>
    <automated>grep -q '^## Version Model$' PUBLISHING.md && grep -q '^## Publish Gating$' PUBLISHING.md && grep -q 'ferro_versions' PUBLISHING.md && grep -q 'ferro-cli/\*' PUBLISHING.md && grep -q 'bacon.toml' PUBLISHING.md && grep -q 'rust-toolchain.toml' PUBLISHING.md && grep -q 'Cargo.lock' PUBLISHING.md && grep -q 'should_publish=none' PUBLISHING.md && grep -q 'should_publish=yes' PUBLISHING.md && ! grep -qi 'killer feature\|load-bearing\|forcing function' PUBLISHING.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^## Version Model$' PUBLISHING.md` succeeds.
    - `grep -q '^## Publish Gating$' PUBLISHING.md` succeeds.
    - `grep -q 'ferro_versions' PUBLISHING.md` succeeds (reservation is documented).
    - `grep -q '^## Version Coordination$' PUBLISHING.md` returns NO match (old section was replaced, not duplicated).
    - `grep -q 'should_publish=none' PUBLISHING.md` succeeds (scenario table documents the gate output literal).
    - `grep -q 'should_publish=yes' PUBLISHING.md` succeeds (first-run / library-change case documented with literal).
    - All workflow exclusion tokens appear in `PUBLISHING.md`: `ferro-cli/*`, `app/*`, `docs/*`, `.github/*`, `.planning/*`, `scripts/*`, `LICENSE`, `Cargo.lock`, `.gitignore`, `.editorconfig`, `rustfmt.toml`, `bacon.toml`, `deny.toml`, `dev.sh`, `llms.txt`, `rust-toolchain.toml`.
    - No CLAUDE.md trigger phrases appear in the added text: `grep -qi 'killer feature\|load-bearing\|forcing function\|the bet\|no stop-loss'` returns NO match in `PUBLISHING.md`.
    - File still parses as Markdown (no broken code fences): count of ` ``` ` fences is even.
  </acceptance_criteria>
  <done>
    Two new sections are present, worded neutrally, with the exclusion list matching the workflow verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cross-check exclusion list against workflow</name>
  <files>PUBLISHING.md, .github/workflows/publish.yml</files>
  <read_first>
    - PUBLISHING.md (post-edit)
    - .github/workflows/publish.yml (post plan 01)
  </read_first>
  <action>
Manually verify that every token in the case-statement exclusion list in `.github/workflows/publish.yml` also appears in the `## Publish Gating` section of `PUBLISHING.md`, and vice versa. The two lists are the spec — any drift is a bug.

Run the following shell check:

```bash
for token in ferro-cli/ app/ docs/ .github/ .planning/ scripts/ LICENSE Cargo.lock .gitignore .editorconfig rustfmt.toml bacon.toml deny.toml dev.sh llms.txt rust-toolchain.toml; do
  grep -q "$token" PUBLISHING.md || { echo "MISSING in PUBLISHING.md: $token"; exit 1; }
  grep -q "$token" .github/workflows/publish.yml || { echo "MISSING in publish.yml: $token"; exit 1; }
done
echo "ok"
```

Expect `ok`. If either file is missing a token, add it and re-run.
  </action>
  <verify>
    <automated>for token in ferro-cli/ app/ docs/ .github/ .planning/ scripts/ LICENSE Cargo.lock .gitignore .editorconfig rustfmt.toml bacon.toml deny.toml dev.sh llms.txt rust-toolchain.toml; do grep -q "$token" PUBLISHING.md || exit 1; grep -q "$token" .github/workflows/publish.yml || exit 1; done; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Every exclusion token listed in Task 1's action appears in both `PUBLISHING.md` and `.github/workflows/publish.yml`.
    - The shell loop above prints `ok` and exits 0.
  </acceptance_criteria>
  <done>
    Exclusion list is consistent between doc and workflow.
  </done>
</task>

</tasks>

<verification>
- `grep -q '^## Version Model$' PUBLISHING.md`
- `grep -q '^## Publish Gating$' PUBLISHING.md`
- `grep -q 'ferro_versions' PUBLISHING.md`
- Cross-check loop from Task 2 exits 0
- No CLAUDE.md trigger phrases in the added text
- Phase-level gate before sign-off: `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` still passes (no code changed, but sanity-run before final commit)
</verification>

<success_criteria>
- `PUBLISHING.md` documents the lockstep version model, the single global `ferro_version` field, and the reserved `ferro_versions` override hook.
- `PUBLISHING.md` documents the library-change gate and lists the excluded paths in a form that mirrors the workflow case statement.
- Voice is neutral and minimalistic; no internal-strategy language.
- Both files agree on the exclusion list.
</success_criteria>

<output>
After completion, create `.planning/phases/129-publish-workflow-refinement/129-03-SUMMARY.md`.
</output>
