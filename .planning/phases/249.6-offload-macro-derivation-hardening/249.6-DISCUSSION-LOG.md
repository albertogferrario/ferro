# Phase 249.6: Offload macro derivation hardening - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-16
**Phase:** 249.6-offload-macro-derivation-hardening
**Mode:** --auto (recommended defaults auto-selected; no interactive questions)
**Areas discussed:** WR-01 receiver rejection, WR-02 double-registration, WR-03 panic→Err, 245-WR-01 handle bounds, 245-IN-01 doc label, cross-cutting gate/scope discipline

---

## WR-01 — Reject unsupported receiver forms in `owned_type`

| Option | Description | Selected |
|--------|-------------|----------|
| Spanned `syn::Error` at macro eval, scoped to inner references | Reject `&&T`/`&Ref<'a>` at the `#[offload]` annotation; leave `&str`/`&[T]` arms intact | ✓ |
| General ownability/lifetime analysis of arbitrary inner types | Broader macro rework | |
| Doc-only limitation note | Leave the miscompile, document it | |

**Choice:** Spanned error scoped narrowly (SC#1 mandates a spanned compile error, not silent verbatim `T`). New trybuild `fail/double_ref_param.rs` fixture pins the message.

## WR-02 — No double-registration across successive `from_registry`

| Option | Description | Selected |
|--------|-------------|----------|
| Lock invariant, prefer non-destructive per-loop idempotency | Assert exactly-once by test; mechanism chosen after call-ordering analysis | ✓ |
| Apply review's `registrars.clear()` verbatim | Clears the Vec in `apply_registrars` | (flagged — side-effect) |
| Doc-only caveat on `from_registry` | Not test-assertable | |

**Choice:** Invariant locked (SC#2 requires a test). Review's `clear()` snippet flagged as a discrepancy: the same Vec feeds `has_registered_jobs`/`registered_queue_names` via `is_empty()`, so a naïve clear risks a regression. Mechanism deferred to planning (D-04/D-05).

## WR-03 — Panic → `Err` on missing service binding

| Option | Description | Selected |
|--------|-------------|----------|
| `.ok_or_else(|| Error::job_failed(...))?` at BOTH generated sites | Records a failed job with the real message | ✓ |
| Keep `.expect()` | Panic caught by worker, message lost, retries exhaust | |

**Choice:** Fallible resolution at both `handle()` (:383) and `handle_with_value()` (:400). Discrepancy flagged: the review recorded one site; Phase 246 added the second. `Error::job_failed` confirmed at error.rs:82.

## 245-WR-01 — Drop over-broad generic bounds on `OffloadHandle<T>`

| Option | Description | Selected |
|--------|-------------|----------|
| Hand-written unbounded impls (Clone/Debug/PartialEq/Eq) | Delegate to `self.key`, no `T` bound; keep serde derive | ✓ |
| Add `derive_more`/`educe` dependency | Derives without bounds via macro | |
| Leave std derives | Rejects serde-only success types | |

**Choice:** Hand-written impls — no new dependency for four trivial impls (SC#4). Serde-only regression test added.

## 245-IN-01 — Correct the mislabeled re-export doc comment

| Option | Description | Selected |
|--------|-------------|----------|
| Replace with an accurate offload-surface label | e.g. "Typed offload handle and serializable-contract primitives." | ✓ |
| Leave as-is | Inaccurate copy of the `async_trait` comment | |

**Choice:** Fix the label at lib.rs:72 (SC#5).

## Cross-cutting — gate & scope discipline

- External contract unchanged; OFFLOAD-01/02 not reopened.
- Macro-emission changes require trybuild UI gate + full changed-crates test; emit only `::ferro::*` paths; run CI's exact clippy/doc/deny commands; watch ENOSPC on `--all-features`.
- No new dependencies.
- The three 244 INFO items, 245 WR-02, and 245 IN-02/IN-03 are out of scope.

## Claude's Discretion

- WR-02 fix mechanism (within the D-04 invariant / D-05 constraint).
- WR-01 `.stderr` fixture wording.
- D-12 corrected doc-comment phrasing.

## Deferred Ideas

None new — stayed within the five-defect boundary. Reviewed-but-deferred items listed in CONTEXT.md `<deferred>`.
