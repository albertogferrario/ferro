---
phase: 249.6-offload-macro-derivation-hardening
verified: 2026-08-17T00:00:00Z
status: passed
score: 5/5
overrides_applied: 0
overrides:
  - must_have: "An unsupported receiver form (`&&T`) produces a spanned compile error, not silent verbatim `T` — scoped to the detectable `&&T` form"
    reason: "The PLAN frontmatter phrased the artifact's `contains` check as 'must be fully owned', but the committed message reads 'may not be double references (&&T) — the job payload must be fully owned and serializable'. The substring 'must be fully owned' is present. The SC#1 note in the phase prompt explicitly records that `&Ref<'a>` is not detectable at macro-expansion time and that the diagnostic was deliberately scoped to the detectable `&&T` form in commit d2a53861. The .stderr snapshot captures exactly the emitted span at the parameter token. This is the correct, honest resolution."
    accepted_by: "alberto"
    accepted_at: "2026-08-17T00:00:00Z"
---

# Phase 249.6: Offload Macro Derivation Hardening — Verification Report

**Phase Goal:** Repair the five latent macro/handle defects recorded in the 244 and 245 REVIEW.md files (244 WR-01/02/03, 245 WR-01/IN-01) without altering the derivation's external contract, so the `#[offload]` write-boundary surface fails loudly and constrains correctly. HARDENING — OFFLOAD-01/02 remain satisfied and are NOT reopened.
**Verified:** 2026-08-17T00:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC#1 — An unsupported `&&T` receiver form produces a spanned compile error, not silent verbatim `T` | VERIFIED | `ferro-macros/src/offload.rs:101` — `matches!(other, Type::Reference(_))` guard returns `syn::Error::new_spanned(ty, "…may not be double references (&&T)…")`. Fixture `double_ref_param.rs` exists with `data: &&Inner`. Blessed `.stderr` at line 1 confirms the span at column 35 / `^^Inner` token. CI-parity gate (including trybuild) ran green. |
| 2 | SC#2 — Two successive `from_registry` calls register each job exactly once; asserted by test | VERIFIED | `ferro-queue/tests/offload_round_trip.rs` contains `double_from_registry_does_not_double_register` (line 141); calls `from_registry` twice, asserts `registered_job_count()` equality; asserts `count1 >= 1`. `db.rs` is untouched (no destructive `.clear()`) — `has_registered_jobs` / `registered_queue_names` signals preserved by construction. |
| 3 | SC#3 — Generated `handle()` / `handle_with_value()` return `Err` on a missing service binding rather than panicking | VERIFIED | `ferro-macros/src/offload.rs:397` and `:414` — both sites use `.ok_or_else(|| ::ferro::queue::Error::job_failed(#job_ident_str, #expect_msg))?`. Zero `.expect(#expect_msg)` remain (grep confirms 0 matches on `.expect(`). The `?` operator is the compile-time no-panic guarantee; both return types are `Result<_, ::ferro::queue::Error>`. |
| 4 | SC#4 — `OffloadHandle<T>` compiles for a result type that is `OffloadSerializable` but not `Clone + Debug + Eq` | VERIFIED | `ferro-queue/src/offload.rs:71` — `#[derive(Serialize, Deserialize)]` only. Four hand-written impls (lines 78–101) bound nothing on `T`: `impl<T> Clone`, `impl<T> std::fmt::Debug`, `impl<T> PartialEq`, `impl<T> Eq`. Pass fixture `offload_handle.rs` contains `SerdeOnly` (serde-only, no Clone/Debug/Eq) and `_check_serde_only` inner fn that calls `.clone()`, `format!("{:?}", h)`, and `==` on `OffloadHandle<SerdeOnly>` — compile-pass proof. |
| 5 | SC#5 — The `ferro-queue` re-export doc comment names the offload surface correctly; full offload suite passes | VERIFIED | `ferro-queue/src/lib.rs:72` reads `/// Typed offload handle and serializable-contract primitives.`. Exactly one `async_trait for convenience` comment remains (line 75, above the genuine async_trait re-export). Incremental macro-gate sequence (offload_macro UI + full ferro-macros + ferro-queue) ran green per SUMMARY.md. Full CI-parity gate (fmt + clippy --all-targets -D warnings + test --all-features + cargo doc) ran green (orchestrator-confirmed per prompt). |

**Score:** 5/5 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `ferro-macros/src/offload.rs` | `matches!(other, Type::Reference(_))` guard in `owned_type`; two `.ok_or_else` service-resolution sites | VERIFIED | Guard at line 101; `.ok_or_else` at lines 397 and 414; zero `.expect(#expect_msg)` |
| `ferro-macros/tests/ui/offload/fail/double_ref_param.rs` | Compile-fail fixture with `data: &&Inner` | VERIFIED | File exists; `data: &&Inner` present |
| `ferro-macros/tests/ui/offload/fail/double_ref_param.stderr` | Blessed trybuild snapshot containing "must be fully owned" / double-reference wording | VERIFIED | Exists; line 1 reads "may not be double references (&&T) — the job payload must be fully owned and serializable"; substring "must be fully owned" present |
| `ferro-queue/src/offload.rs` | `#[derive(Serialize, Deserialize)]` only on `OffloadHandle<T>`; four hand-written T-free impls | VERIFIED | derive at line 71; four impls at lines 78–101 |
| `ferro-queue/tests/offload_round_trip.rs` | `double_from_registry_does_not_double_register` test asserting `registered_job_count()` equality | VERIFIED | Test exists at line 141; body asserts count1==count2 and count1>=1 |
| `ferro-queue/src/lib.rs` | "Typed offload handle and serializable-contract primitives" doc comment | VERIFIED | Line 72 contains exact string; one `async_trait for convenience` remains |
| `ferro-macros/tests/ui/offload/pass/offload_handle.rs` | `SerdeOnly` type + `_check_serde_only` fn proving T-free handle | VERIFIED | `SerdeOnly` at line 18; `_check_serde_only` at line 53 using `OffloadHandle<SerdeOnly>` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `owned_type` inner `other =>` arm | `syn::Error::new_spanned(ty, …)` | `matches!(other, Type::Reference(_))` guard | VERIFIED | Lines 101–106 in offload.rs |
| generated `handle()` | `::ferro::queue::Error::job_failed` | `.ok_or_else(…)?` at line 397 | VERIFIED | Exact pattern confirmed |
| generated `handle_with_value()` | `::ferro::queue::Error::job_failed` | `.ok_or_else(…)?` at line 414 | VERIFIED | Exact pattern confirmed |
| `double_from_registry_does_not_double_register` | `WorkerLoop::registered_job_count()` | two `from_registry` calls, `assert_eq!(count1, count2)` | VERIFIED | Lines 147 and 150 in offload_round_trip.rs |
| `_check_serde_only` pass fixture | `OffloadHandle<T>` hand-written impls (Plan 02) | compile-pass trybuild exercising Clone/Debug/PartialEq on serde-only T | VERIFIED | Fixture compiles under trybuild; gate ran 1/1 |
| `double_ref_param.rs` fixture | `double_ref_param.stderr` | `TRYBUILD=overwrite` bless + inspect workflow | VERIFIED | .stderr generated and confirmed before commit; snapshot stable |

### Data-Flow Trace (Level 4)

Not applicable — this phase produces no data-rendering components or API endpoints. All artifacts are proc-macro source, test fixtures, and documentation.

### Behavioral Spot-Checks

| Behavior | Evidence | Status |
|----------|----------|--------|
| `&&T` fixture emits spanned error | `cargo test -p ferro-macros --test offload_macro` PASS (1/1) per SUMMARY.md | PASS |
| `double_from_registry_does_not_double_register` passes | `cargo test -p ferro-queue` exit 0 per SUMMARY.md | PASS |
| Full CI-parity gate (fmt + clippy + test --all-features + doc) | Orchestrator-confirmed green per prompt `<verification_evidence_already_established>` | PASS |

Per the prompt's `<verification_evidence_already_established>` directive, cargo is not re-run; the CI-parity gate evidence is treated as established.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| OFFLOAD-01 | Plans 01, 03 | `#[offload]` macro correctness on the write-boundary | SATISFIED | SC#1 (spanned diagnostic), SC#3 (fallible service resolution) — both verified |
| OFFLOAD-02 | Plans 02, 03 | `OffloadHandle<T>` serializable-contract correctness | SATISFIED | SC#4 (T-free impls + pass fixture) — verified |

No REQUIREMENTS.md phase mapping for this hardening sub-phase; requirements coverage assessed directly from the five SCs above.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | — | — | — |

No TODO/FIXME, placeholder, `return null`, or hardcoded empty data patterns in the modified files. The only intentional temporary absence (`.stderr` snapshot deferred to Plan 03) was resolved before phase completion.

### Human Verification Required

None. All five success criteria are verifiable programmatically via source inspection and the committed test/fixture evidence. The CI-parity gate result is established by the orchestrator.

---

## Gaps Summary

No gaps. All five roadmap success criteria are satisfied:

- SC#1: `owned_type` rejects `&&T` at compile time with a spanned diagnostic; fixture and blessed snapshot committed.
- SC#2: Registration idempotency locked by an asserting test; `db.rs` untouched; consumer signals preserved.
- SC#3: Both generated handle sites use `.ok_or_else(…)?`; zero `.expect` remain; the `?` operator is the no-panic guarantee.
- SC#4: `OffloadHandle<T>` derives only `Serialize, Deserialize`; four T-free hand-written impls proven by a compile-pass fixture.
- SC#5: Corrected doc comment at `ferro-queue/src/lib.rs:72`; full offload suite green under the macro-gate blindspot sequence and workspace gate.

OFFLOAD-01 and OFFLOAD-02 are not reopened; no public API surface was altered.

---

_Verified: 2026-08-17T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
