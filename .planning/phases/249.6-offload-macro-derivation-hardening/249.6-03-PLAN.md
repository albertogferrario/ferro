---
phase: 249.6-offload-macro-derivation-hardening
plan: 03
type: execute
wave: 2
depends_on: ["249.6-01", "249.6-02"]
files_modified:
  - ferro-macros/tests/ui/offload/fail/double_ref_param.stderr
  - ferro-macros/tests/ui/offload/pass/offload_handle.rs
autonomous: true
requirements: [OFFLOAD-01, OFFLOAD-02]

must_haves:
  truths:
    - "The `&&T` compile-fail fixture emits its spanned error and the blessed `.stderr` snapshot matches (SC#1, end-to-end)"
    - "A serde-only success type (Serialize+DeserializeOwned, not Clone+Debug+Eq) yields an `OffloadHandle<T>` that clones, debugs, and compares — proven by a compile-PASS fixture (SC#4)"
    - "The generated `handle()`/`handle_with_value()` cannot panic on a missing binding — the `?` operator is a compile-time guarantee, confirmed by inspection (SC#3)"
    - "The full offload suite passes under the macro-gate command sequence and the workspace gate is green (SC#5)"
  artifacts:
    - path: "ferro-macros/tests/ui/offload/fail/double_ref_param.stderr"
      provides: "blessed trybuild snapshot for the &&T spanned error"
      contains: "must be fully owned"
    - path: "ferro-macros/tests/ui/offload/pass/offload_handle.rs"
      provides: "SerdeOnly type + _check_serde_only fn proving the handle clones/debugs/compares"
      contains: "OffloadHandle<SerdeOnly>"
  key_links:
    - from: "double_ref_param.rs (Plan 01 fixture)"
      to: "double_ref_param.stderr"
      via: "TRYBUILD=overwrite cargo test -p ferro-macros --test offload_macro"
      pattern: "TRYBUILD=overwrite"
    - from: "offload_handle.rs SerdeOnly extension"
      to: "ferro-queue OffloadHandle<T> hand-written impls (Plan 02)"
      via: "compile-pass trybuild exercising Clone/Debug/PartialEq on a serde-only T"
      pattern: "OffloadHandle<SerdeOnly>"
---

<objective>
Complete the two cross-crate test surfaces that could only land after the Wave-1 source fixes, then run the
single thermally-serialized macro-gate + workspace gate for the whole phase:
- **SC#1 bless:** generate `double_ref_param.stderr` from the Plan 01 fixture via `TRYBUILD=overwrite` (the
  snapshot must reflect the REAL emitted message, so it is blessed here, after the `owned_type` fix landed).
- **SC#4 fixture:** extend `ferro-macros/tests/ui/offload/pass/offload_handle.rs` with a serde-only success
  type. This fixture's correctness depends on Plan 02's `ferro-queue` `OffloadHandle<T>` hand-written impls —
  hence Wave 2 (the handle impl must land before the fixture is blessed).
- **SC#3 verify:** confirm the code-property (the `?` operator cannot panic on `None`) by inspection — no
  runtime behavioral test is added to `ferro-queue/tests/` (it cannot depend on `framework`; per RESEARCH the
  `?` property is the primary acceptance criterion).
- **Phase gate:** run the macro-gate blindspot command sequence + the queue tests + the workspace gate ONCE.

Purpose: this plan owns every `ferro-macros/tests/` fixture edit that depends on Wave-1 output, and the one
expensive full-workspace gate — keeping cargo invocations minimal and serialized (thermal constraint / D-14).
Output: the blessed `.stderr`, the extended pass fixture, and a green phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/249.6-offload-macro-derivation-hardening/249.6-CONTEXT.md
@.planning/phases/249.6-offload-macro-derivation-hardening/249.6-RESEARCH.md
@.planning/phases/249.6-offload-macro-derivation-hardening/249.6-PATTERNS.md
@.planning/phases/249.6-offload-macro-derivation-hardening/249.6-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the current tree (2026-08-16). -->

Trybuild runner (ferro-macros/tests/offload_macro.rs) — globs both dirs, no edit needed:
```rust
#[test]
fn offload_macro_ui() {
    let t = trybuild::TestCases::new();
    t.pass("tests/ui/offload/pass/*.rs");
    t.compile_fail("tests/ui/offload/fail/*.rs");
}
```

Existing pass fixture to extend (ferro-macros/tests/ui/offload/pass/offload_handle.rs) — it already has a
`fn main()` with inner structural-proof functions and imports `OffloadHandle`, `Offloadable`, serde. The
`SerdeOnly` type + `_check_serde_only` fn are appended into this file.

The mut_ref_param analog `.stderr` (single compact `error:` block, span at the parameter type) is the shape
the blessed `double_ref_param.stderr` should resemble — but the EXACT span/format depends on how the macro
re-emits the `syn::Error`, so it is generated, never hand-written (Pitfall 1, Open Question 2).

Macro-gate blindspot command sequence (project memory + RESEARCH §Macro Gate Blindspot):
```
cargo test -p ferro-macros --test offload_macro
cargo test -p ferro-macros
cargo test -p ferro-queue
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC#4 — extend the compile-PASS fixture with a serde-only success type</name>
  <read_first>
    - ferro-macros/tests/ui/offload/pass/offload_handle.rs (read the whole existing file — the `Report` type, the `fn main()` structural proofs)
    - .planning/phases/249.6-offload-macro-derivation-hardening/249.6-PATTERNS.md (§offload_handle.rs extend — exact snippets)
    - ferro-queue/src/offload.rs (confirm Plan 02's four hand-written `OffloadHandle<T>` impls are present — this fixture depends on them)
  </read_first>
  <files>ferro-macros/tests/ui/offload/pass/offload_handle.rs</files>
  <action>
Extend the EXISTING pass fixture (do not create a new file; the runner globs `pass/*.rs` and a sibling file
would need its own service scaffold). Two additions, per PATTERNS.md:

**(a)** Add a serde-only type at module level (deliberately missing `Clone`, `Debug`, `PartialEq`, `Eq`),
e.g. after the existing `Report` type:
```rust
// Serde-only: deliberately missing Clone, Debug, PartialEq, Eq.
#[derive(Serialize, Deserialize)]
pub struct SerdeOnly {
    pub x: i32,
}
```

**(b)** Add a structural-proof inner function that exercises `Clone`/`Debug`/`PartialEq` on
`OffloadHandle<SerdeOnly>` — either inside the existing `fn main()` (before its closing `}`) or as a free
`fn` after `main`, matching the file's existing inner-fn idiom (no `#[tokio::test]`, no `App`, no DB):
```rust
    // SC#4 regression: OffloadHandle<SerdeOnly> must support Clone, Debug, PartialEq
    // even though SerdeOnly does not implement those traits itself.
    fn _check_serde_only(h: OffloadHandle<SerdeOnly>) {
        let _ = h.clone();
        let _ = format!("{:?}", h);
        let h2 = h.clone();
        let _ = h == h2;
    }
```

This fixture compiles ONLY because Plan 02 removed the `T: Clone + Debug + Eq` bounds from `OffloadHandle<T>`
— it is the compile-PASS proof of SC#4. Do NOT modify `Report`, the `#[service]`/`#[offload]` trait, or the
existing structural proofs. The `.stderr`-free pass fixture needs no snapshot.
  </action>
  <verify>
    <automated>cargo test -p ferro-macros --test offload_macro 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub struct SerdeOnly" ferro-macros/tests/ui/offload/pass/offload_handle.rs` returns `1`
    - `grep -c "OffloadHandle<SerdeOnly>" ferro-macros/tests/ui/offload/pass/offload_handle.rs` returns at least `1`
    - `grep -c "derive(Serialize, Deserialize)" ferro-macros/tests/ui/offload/pass/offload_handle.rs` returns at least `1` (the SerdeOnly derive, no Clone/Debug/Eq)
    - the `pass/*` glob in `cargo test -p ferro-macros --test offload_macro` compiles `offload_handle.rs` cleanly (no pass-fixture failure)
  </acceptance_criteria>
  <done>`offload_handle.rs` contains a serde-only `SerdeOnly` type and a `_check_serde_only` proof over `OffloadHandle<SerdeOnly>`; it compiles as a pass fixture, proving the handle no longer injects `T` bounds.</done>
</task>

<task type="auto">
  <name>Task 2: SC#1 bless — generate the double_ref_param.stderr snapshot</name>
  <read_first>
    - ferro-macros/tests/ui/offload/fail/double_ref_param.rs (the Plan 01 fixture — confirm it exists)
    - ferro-macros/tests/ui/offload/fail/mut_ref_param.stderr (the analog snapshot shape)
    - .planning/phases/249.6-offload-macro-derivation-hardening/249.6-PATTERNS.md (§double_ref_param.stderr — Pitfall 1)
    - ferro-macros/src/offload.rs (confirm Plan 01's `owned_type` guard is present before blessing)
  </read_first>
  <files>ferro-macros/tests/ui/offload/fail/double_ref_param.stderr</files>
  <action>
The Plan 01 `owned_type` fix MUST be in place first (verify the `matches!(other, Type::Reference(_))` guard
exists in `ferro-macros/src/offload.rs`). Generate the snapshot — never hand-write it (Pitfall 1):

```
TRYBUILD=overwrite cargo test -p ferro-macros --test offload_macro
```

This writes `ferro-macros/tests/ui/offload/fail/double_ref_param.stderr`. Then INSPECT the generated file and
confirm the message contains the double-reference wording emitted by the fix (`must be fully owned` and
`double references (&&T)`). If the generated `.stderr` is empty or contains a DIFFERENT error (e.g. a
downstream serde bound error rather than the spanned `owned_type` message), the `owned_type` fix did not fire
at the expected span — STOP and report; do not commit a wrong snapshot.

After blessing, run the UI test WITHOUT the overwrite flag to confirm the committed snapshot matches:
```
cargo test -p ferro-macros --test offload_macro
```
Both the new `double_ref_param` fail fixture and the extended `offload_handle` pass fixture must be green.
  </action>
  <verify>
    <automated>cargo test -p ferro-macros --test offload_macro 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - File `ferro-macros/tests/ui/offload/fail/double_ref_param.stderr` exists
    - `grep -c "must be fully owned" ferro-macros/tests/ui/offload/fail/double_ref_param.stderr` returns at least `1`
    - `cargo test -p ferro-macros --test offload_macro` exits 0 (fail snapshot matches AND pass fixtures compile) with no `TRYBUILD=overwrite`
  </acceptance_criteria>
  <done>The blessed `double_ref_param.stderr` reflects the real spanned `owned_type` message; the `offload_macro_ui` trybuild test passes with the committed snapshot and the extended pass fixture.</done>
</task>

<task type="auto">
  <name>Task 3: SC#3 code-property confirmation + phase macro-gate + workspace gate (SC#5)</name>
  <read_first>
    - ferro-macros/src/offload.rs (confirm both handle sites use `.ok_or_else(...)?` and zero `.expect(#expect_msg)` remain — SC#3 code property)
    - ferro-queue/Cargo.toml (confirm `framework`/`ferro-rs` absent from dev-deps — the reason SC#3 has no ferro-queue runtime test)
    - .planning/phases/249.6-offload-macro-derivation-hardening/249.6-VALIDATION.md (§Sampling Rate, §Macro Gate Blindspot)
    - .planning/phases/249.6-offload-macro-derivation-hardening/249.6-RESEARCH.md (§WR-03 SC#3 semantic clarification — the `.expect` is on `App::make`, not serialization; the `?` operator is the guarantee)
  </read_first>
  <files>ferro-macros/src/offload.rs</files>
  <action>
**(a) SC#3 code-property confirmation (no source change; verification only).** Per RESEARCH: the SC#3 wording
"serialization failure" is a misnomer — the `.expect()` was on `App::make` (a missing container binding), and
Plan 01 converted both sites to `.ok_or_else(|| ...)?`. The `?` operator on a `Result` provably cannot panic
on the `None`/`Err` path, so SC#3 is satisfied as a compile-time code property. `ferro-queue/tests/` cannot
host a behavioral test (it lacks a `framework`/`ferro::App` dev-dep and adding one is circular — confirmed in
`ferro-queue/Cargo.toml`), and RESEARCH designates the `?` property as the primary acceptance criterion, so
NO new runtime test is added here. Confirm by grep that zero `.expect(#expect_msg)` remain and both sites use
the fallible form.

**(b) Phase macro-gate (blindspot) sequence.** Macro-emission changes require the trybuild UI gate PLUS the
full changed-crates test — per-crate runs miss broken emission because only the fixtures expand the attribute
(D-14 / project memory). Run, in order:
```
cargo test -p ferro-macros --test offload_macro
cargo test -p ferro-macros
cargo test -p ferro-queue
```
All three must be green.

**(c) Workspace gate.** Check disk first (`cargo test --all-features` is ENOSPC-prone — Pitfall 5); if `df -h`
shows the target volume near-full, `cargo clean` or free space before the full run. Then run the CI-exact gate:
```
cargo fmt --all -- --check
cargo clippy --all --all-targets -- -D warnings
cargo test --all-features
```
Note per feedback: CI also runs `cargo doc -Dwarnings`; run `cargo doc -p ferro-queue --no-deps` to confirm
the corrected doc comment introduces no doc warning. All gates must pass with zero warnings.
  </action>
  <verify>
    <automated>cargo test -p ferro-macros --test offload_macro && cargo test -p ferro-macros && cargo test -p ferro-queue 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ".expect(#expect_msg)" ferro-macros/src/offload.rs` returns `0`
    - `grep -c "ok_or_else(|| ::ferro::queue::Error::job_failed(#job_ident_str, #expect_msg))?" ferro-macros/src/offload.rs` returns `2`
    - `cargo test -p ferro-macros --test offload_macro` exits 0
    - `cargo test -p ferro-macros` exits 0 (full crate — macro-gate blindspot)
    - `cargo test -p ferro-queue` exits 0
    - `cargo fmt --all -- --check` exits 0; `cargo clippy --all --all-targets -- -D warnings` exits 0; `cargo test --all-features` exits 0
    - `cargo doc -p ferro-queue --no-deps 2>&1 | grep -ci "async_trait for convenience"` returns `0` in doc output (the stale label is gone from rendered docs)
  </acceptance_criteria>
  <done>Both handle sites are fallible (`?`, zero `.expect`); the macro-gate sequence (UI + full ferro-macros + ferro-queue) is green; the workspace gate (fmt + clippy `-D warnings` + `--all-features` test + `cargo doc`) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trybuild fixture source → compile-time macro expansion | Fixtures are developer-authored test inputs compiled by the harness; no runtime or untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-249.6-05 | Tampering | blessed `.stderr` snapshot | accept | The snapshot is generated by `TRYBUILD=overwrite`, then inspected for the expected wording before commit; a wrong snapshot is caught by the mandatory inspection step, not silently accepted. No runtime surface. |

**Assessment:** This plan is test-fixture and gate work only — no source behavior change beyond the Wave-1 fixes it verifies. No external input surface, no auth/network/untrusted-deserialization path. No high/medium threats are introduced; the phase net-reduces risk (panic→`Err`, compile-time rejection of malformed macro input).
</threat_model>

<verification>
This plan IS the phase verification gate. The macro-gate blindspot sequence
(`cargo test -p ferro-macros --test offload_macro` + `cargo test -p ferro-macros` + `cargo test -p ferro-queue`)
plus the CI-exact workspace gate (`fmt --check` + `clippy --all --all-targets -D warnings` + `test --all-features`)
plus `cargo doc -p ferro-queue --no-deps` must all be green. Check `df -h` before the `--all-features` run.
</verification>

<success_criteria>
- SC#1 (end-to-end): the `&&T` fixture emits its spanned error; the blessed `.stderr` matches; `offload_macro_ui` passes.
- SC#3: zero `.expect(#expect_msg)` remain; both handle sites use `.ok_or_else(...)?` (the `?` is the no-panic guarantee).
- SC#4: the serde-only pass fixture compiles, proving `OffloadHandle<T>` bounds nothing on `T`.
- SC#5: the full offload suite passes under the macro-gate sequence; the workspace gate is green; no stale `async_trait for convenience` label in rendered `ferro-queue` docs.
</success_criteria>

<output>
After completion, create `.planning/phases/249.6-offload-macro-derivation-hardening/249.6-03-SUMMARY.md`.
Record: the exact blessed `.stderr` message/span (for future toolchain-upgrade re-bless reference); the SC#3
decision (code-property only, no ferro-queue runtime test, with the dev-dep-cycle rationale); the full-gate
result (fmt/clippy/test/doc). Note any `df`/`target` cleanup performed before the `--all-features` run.
</output>
