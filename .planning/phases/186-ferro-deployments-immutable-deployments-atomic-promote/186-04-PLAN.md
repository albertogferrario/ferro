---
phase: 186-ferro-deployments-immutable-deployments-atomic-promote
plan: 04
type: execute
wave: 3
depends_on: [186-02, 186-03]
files_modified:
  - Cargo.toml
  - ferro-deployments/src/lib.rs
  - docs/src/SUMMARY.md
  - docs/src/features/deployments.md
autonomous: true
requirements: [DEPL-F-01, DEPL-F-02, DEPL-F-03]
must_haves:
  truths:
    - "A doc-test stores a non-HTML (JSON spec) artifact bundle through the same API end-to-end (criterion 5)"
    - "The crate contains zero HTML/gestiscilo-specific assumptions"
    - "docs/src/features/deployments.md documents the crate and is linked from SUMMARY.md"
    - "The workspace version is bumped to 0.2.45 so ferro-deployments ships at the bumped version"
    - "cargo publish -p ferro-deployments --dry-run packages cleanly"
  artifacts:
    - path: "docs/src/features/deployments.md"
      provides: "Feature documentation page (artifact-shape agnostic framing)"
      contains: "ferro-deployments"
    - path: "ferro-deployments/src/lib.rs"
      provides: "Crate-level doc-test storing a JSON artifact through the full lifecycle"
      contains: "```"
  key_links:
    - from: "docs/src/SUMMARY.md"
      to: "docs/src/features/deployments.md"
      via: "mdBook nav entry"
      pattern: "features/deployments.md"
    - from: "Cargo.toml [workspace.package]"
      to: "0.2.45"
      via: "version bump"
      pattern: "version = \"0.2.45\""
---

<objective>
Close the phase: prove artifact-shape agnosticism with a criterion-5 doc-test that stores a non-HTML (JSON spec) artifact through the full lifecycle API, write the feature docs page, bump the workspace version so the new crate ships at the bumped version, and gate on a `cargo publish --dry-run`.

Purpose: DEPL-F-01/02/03 integration proof (criterion 5) + new-crate workspace closeout (docs, version bump, publish dry-run) per CONTEXT D-15/D-16.
Output: a passing crate doc-test, `docs/src/features/deployments.md` + SUMMARY.md entry, version bump to 0.2.45, clean publish dry-run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/186-ferro-deployments-immutable-deployments-atomic-promote/186-CONTEXT.md
@.planning/phases/186-ferro-deployments-immutable-deployments-atomic-promote/186-RESEARCH.md
@.planning/phases/186-ferro-deployments-immutable-deployments-atomic-promote/186-01-SUMMARY.md
@.planning/phases/186-ferro-deployments-immutable-deployments-atomic-promote/186-02-SUMMARY.md
@.planning/phases/186-ferro-deployments-immutable-deployments-atomic-promote/186-03-SUMMARY.md

<interfaces>
<!-- Full public API assembled across Plans 01-03: -->
<!-- Deployments (handle), Deployment, DeploymentStatus, DeploymentConfig, -->
<!-- DeploymentStorage, StorageDeploymentStorage, preview_url, -->
<!-- CreateDeploymentsTable, CreateDeploymentPointersTable, Error. -->

Doc-test lifecycle (criterion 5 — non-HTML JSON artifact, no app identity):
  1. connect sqlite::memory + migrate both tables (TestMigrator inline in the doc-test, or a public helper)
  2. let deps = Deployments::new(conn);
  3. let d = deps.create("project:demo", Some("sha-abcdef")).await?;   // status Building
  4. build a Memory-driver StorageDeploymentStorage; store(d.id, "spec.json", br#"{"intent":"browse"}"#.into())
  5. deps.mark_ready(d.id, "deployments/{id}/", 42).await?;
  6. let prev = deps.promote("project:demo", d.id).await?;             // Some(None) on first promote
  7. retrieve(d.id, "spec.json") == the JSON bytes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criterion-5 doc-test (non-HTML JSON artifact through the full API)</name>
  <read_first>
    - ferro-deployments/src/lib.rs (current crate doc header + re-exports, from Plans 01-03)
    - ferro-deployments/src/deployment.rs (Deployments handle API, from Plan 02)
    - ferro-deployments/src/storage.rs (DeploymentStorage + Memory-driver construction, from Plan 03)
    - .planning/phases/186-ferro-deployments-immutable-deployments-atomic-promote/186-CONTEXT.md (D-15 — criterion 5 proof requirement)
  </read_first>
  <action>
    Add a runnable crate-level doc-test (` ```rust ` fenced block in the `//!` header of `ferro-deployments/src/lib.rs`) that exercises the full lifecycle storing a JSON spec bundle — explicitly NOT HTML, NOT gestiscilo-specific:
    - Use `#[tokio::main]`-style async wrapping appropriate for a doc-test (`# tokio_test::block_on(async { ... })` is unavailable unless tokio_test is a dev-dep; instead write the doc-test as ` ```rust ` with a `# async fn run() -> Result<(), ferro_deployments::Error> { ... }` hidden harness, OR mark it ` ```no_run ` if a runtime cannot be established cleanly). Prefer a runnable test: add `tokio = { features = ["macros","rt"] }` is already a dependency; use `#[doc = ...]` with an inline `tokio::runtime::Runtime::new().unwrap().block_on(async { ... })` wrapper inside the doc example so it actually runs under `cargo test --doc`.
    - Connect `sqlite::memory:`, run the two migrations (define a tiny `TestMigrator` inside the doc example or call a public migration-registration helper).
    - `create("project:demo", Some("sha-abcdef"))` → Building.
    - Build a Memory-driver `StorageDeploymentStorage`; `store(d.id, "spec.json", bytes_of(br#"{"intent":"browse","fields":[]}"#))`.
    - `mark_ready(d.id, &format!("deployments/{}/", d.id), 42)`.
    - `promote("project:demo", d.id)` → assert Ok.
    - `retrieve(d.id, "spec.json")` → assert equals the JSON bytes.
    - Hide setup lines with `#` so the rendered doc reads cleanly but the test runs.
    The whole example must contain zero HTML and zero app-identity strings — the artifact is a JSON spec. This is the executable proof of criterion 5.
  </action>
  <verify>
    <automated>cargo test -p ferro-deployments --doc 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - lib.rs contains a fenced ` ```rust ` doc example (`grep -q '```rust' ferro-deployments/src/lib.rs`)
    - The doc-test stores and retrieves a JSON artifact (`grep -q 'spec.json' ferro-deployments/src/lib.rs`)
    - No HTML in the doc example (`! grep -qi '<html\|<!doctype\|\.html"' ferro-deployments/src/lib.rs`)
    - No app-identity string in the crate (`! grep -rqi 'gestiscilo' ferro-deployments/src/`)
    - `cargo test -p ferro-deployments --doc` passes
  </acceptance_criteria>
  <done>A runnable crate doc-test stores a non-HTML JSON artifact through create→store→mark_ready→promote→retrieve and passes under `cargo test --doc`, with zero HTML/app-identity assumptions.</done>
</task>

<task type="auto">
  <name>Task 2: Feature docs page + SUMMARY.md entry</name>
  <read_first>
    - docs/src/SUMMARY.md (mdBook nav structure — find the features section)
    - docs/src/features/ (sibling feature pages — match heading structure and voice; e.g. an existing crate feature page)
    - .planning/phases/186-ferro-deployments-immutable-deployments-atomic-promote/186-CONTEXT.md (D-16 — docs page mandatory; neutral, artifact-shape-agnostic framing)
  </read_first>
  <action>
    Create `docs/src/features/deployments.md` documenting `ferro-deployments`. Cover, in neutral architectural voice (no marketing language, no "gestiscilo", no literal production domains):
    - What it is: immutable deployment rows + atomic promote/rollback + artifact storage abstraction; artifact shape is opaque (static sites, JSON-UI spec bundles, SSR manifests all fit).
    - Migration setup: registering `CreateDeploymentsTable` + `CreateDeploymentPointersTable` in the consumer's `Migrator` (mirror the rustdoc snippet from migration.rs).
    - The `Deployments` handle lifecycle API (create / mark_ready / mark_failed / get / list / active / promote / rollback) with a short example.
    - The atomic promote model: one UPDATE flips the active pointer and returns the previous id; rollback = promote-of-previous; non-ready and artifact-deleted promotes are rejected.
    - `DeploymentStorage` trait + S3-compatible default; `preview_url` reading `DEPLOYMENT_PREVIEW_DOMAIN`.
    - A one-line note that **preview URLs are addressable, not secret — they are not an access-control boundary; the consumer owns authorization** (security disposition T-186-08).
    Add a nav entry to `docs/src/SUMMARY.md` pointing to `features/deployments.md`, placed alongside sibling crate feature pages (match the surrounding list indentation).
  </action>
  <verify>
    <automated>test -f docs/src/features/deployments.md && grep -q 'features/deployments.md' docs/src/SUMMARY.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/src/features/deployments.md`
    - `grep -q 'ferro-deployments' docs/src/features/deployments.md`
    - `grep -q 'DeploymentStorage' docs/src/features/deployments.md && grep -q 'preview_url' docs/src/features/deployments.md`
    - `grep -q 'features/deployments.md' docs/src/SUMMARY.md`
    - No app-identity string in the page (`! grep -qi 'gestiscilo' docs/src/features/deployments.md`)
    - Page documents that preview URLs are not an access-control boundary (`grep -qi 'not.*access' docs/src/features/deployments.md || grep -qi 'addressable' docs/src/features/deployments.md`)
  </acceptance_criteria>
  <done>Feature docs page exists with neutral framing covering schema, lifecycle, atomic promote, storage trait, preview_url, and the preview-URL security note; linked from SUMMARY.md.</done>
</task>

<task type="auto">
  <name>Task 3: Workspace version bump + publish dry-run gate</name>
  <read_first>
    - Cargo.toml (workspace root — [workspace.package] version = "0.2.44" at line 34)
    - .planning/STATE.md (Workspace version line — confirm current 0.2.44, not yet pushed)
    - .planning/phases/186-ferro-deployments-immutable-deployments-atomic-promote/186-RESEARCH.md (first-publish note — CI token publish-update only)
  </read_first>
  <action>
    1. Bump `[workspace.package] version` in the root `Cargo.toml` from `0.2.44` to `0.2.45` (line 34) so `ferro-deployments` (which uses `version.workspace = true`) ships at 0.2.45.
    2. Run the full pre-commit gate to confirm the whole workspace still builds with the new crate and bumped version:
       `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features`
    3. Run `cargo publish -p ferro-deployments --dry-run` to verify the crate packages cleanly (manifest valid, all path deps resolvable for packaging). The dry-run does not upload.
    4. Record in the SUMMARY the one-time manual-publish reminder: because the CI token is `publish-update` only (not `publish-new`), `ferro-deployments` does NOT yet exist on crates.io and requires a one-time `cargo publish -p ferro-deployments` from a local terminal before the first CI push. This is a human-required step surfaced at phase closeout — note it in the SUMMARY; do not attempt the real publish here.
  </action>
  <verify>
    <automated>grep -q 'version = "0.2.45"' Cargo.toml && cargo publish -p ferro-deployments --dry-run 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'version = "0.2.45"' Cargo.toml` (workspace version bumped)
    - `cargo build --workspace` exits 0 with the bumped version
    - `cargo publish -p ferro-deployments --dry-run` completes without a packaging error
    - SUMMARY records the one-time manual `cargo publish -p ferro-deployments` reminder (CI token is publish-update only)
  </acceptance_criteria>
  <done>Workspace version is 0.2.45, the full fmt+clippy+test gate is green, the publish dry-run packages cleanly, and the manual first-publish reminder is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crate → crates.io | publish dry-run packages the manifest; no secrets in the package |
| docs → public readers | feature page is a public artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-186-11 | Information Disclosure | docs page / doc-test | accept | Docs use neutral voice, zero app identity, zero secrets; doc-test uses placeholder owner_key "project:demo" and a JSON spec. Severity: none. |
| T-186-08 (carried) | Information Disclosure | preview URL framing in docs | mitigate | Docs explicitly state preview URLs are addressable, not an access-control boundary — consumer owns authz. Severity: low, mitigated by documentation. |
</threat_model>

<verification>
- `cargo test -p ferro-deployments --doc` green (criterion-5 doc-test).
- `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` green (full workspace).
- `cargo publish -p ferro-deployments --dry-run` clean.
- `docs/src/features/deployments.md` exists and is linked from SUMMARY.md.
- Workspace version is 0.2.45.
</verification>

<success_criteria>
- Criterion 5 proven: a non-HTML JSON artifact bundle stores through the same API in a passing doc-test; crate contains zero HTML/app-specific assumptions.
- New-crate closeout complete: feature docs page + SUMMARY entry, version bump to 0.2.45, clean publish dry-run, manual first-publish reminder recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/186-ferro-deployments-immutable-deployments-atomic-promote/186-04-SUMMARY.md`
</output>
