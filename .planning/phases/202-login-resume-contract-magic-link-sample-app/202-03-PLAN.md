---
phase: 202-login-resume-contract-magic-link-sample-app
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - app/src/views/login.json
  - app/src/views/login_confirm.json
  - app/src/controllers/auth_controller.rs
autonomous: true
requirements: [SC-4]
must_haves:
  truths:
    - "The login view is an email-only 'send login link' form with no password field, posting to POST /auth/login"
    - "A confirmation view renders 'Check your email' and surfaces the dev link only when dev_mode is true"
    - "Both views are valid ferro-json-ui/v2 with layout:'auth' and render through ThemeMiddleware"
    - "The login_view_* test asserts the email-only contract (no password field)"
  artifacts:
    - path: "app/src/views/login.json"
      provides: "email-only request form"
      contains: "Send login link"
    - path: "app/src/views/login_confirm.json"
      provides: "confirmation state + conditional dev link"
      contains: "Check your email"
  key_links:
    - from: "app/src/views/login.json"
      to: "POST /auth/login"
      via: "Form action handler"
      pattern: "/auth/login"
    - from: "app/src/controllers/auth_controller.rs"
      to: "app/src/views/login_confirm.json"
      via: "JsonUi::render_file confirmation render"
      pattern: "login_confirm.json"
---

<objective>
Author the two JSON-UI auth views per the UI Design Contract: replace `login.json` with an email-only "send login link" form (no password), and add `login_confirm.json` for the "Check your email" confirmation state that conditionally surfaces the dev link. Update the existing view test to the magic-link contract.

Purpose: SC-4 — the login + magic-link views render through JSON-UI and are themed via ThemeMiddleware.
Output: rewritten `login.json`, new `login_confirm.json`, and an updated `login_view_*` test asserting the email-only contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/202-login-resume-contract-magic-link-sample-app/202-UI-SPEC.md
@.planning/phases/202-login-resume-contract-magic-link-sample-app/202-PATTERNS.md
@.planning/phases/202-login-resume-contract-magic-link-sample-app/202-RESEARCH.md

<interfaces>
<!-- The UI-SPEC View File Contract is authoritative. Author verbatim. -->

login.json (REPLACE — UI-SPEC View 1, verbatim):
```json
{
  "$schema": "ferro-json-ui/v2",
  "title": "Sign in",
  "layout": "auth",
  "root": "card",
  "elements": {
    "card": {
      "type": "Card",
      "props": { "title": "Sign in", "description": "Enter your email to receive a login link.", "variant": "elevated" },
      "children": ["form"]
    },
    "form": {
      "type": "Form",
      "props": { "action": { "handler": "/auth/login", "method": "POST" }, "method": "POST", "max_width": "narrow" },
      "children": ["email", "submit"]
    },
    "email": {
      "type": "Input",
      "props": { "field": "email", "label": "Email", "input_type": "email", "required": true, "data_path": "/email", "error": { "$data": "/error" } }
    },
    "submit": {
      "type": "Button",
      "props": { "label": "Send login link", "button_type": "submit", "variant": "default" }
    }
  }
}
```

login_confirm.json (NEW — UI-SPEC View 2, verbatim):
```json
{
  "$schema": "ferro-json-ui/v2",
  "title": "Check your email",
  "layout": "auth",
  "root": "card",
  "elements": {
    "card": {
      "type": "Card",
      "props": { "title": "Check your email", "description": "A login link has been sent. It expires in 15 minutes.", "variant": "elevated" },
      "children": ["body", "dev_link"]
    },
    "body": {
      "type": "Text",
      "props": { "content": "Click the link in the email to sign in. If you don't see it, check your spam folder." }
    },
    "dev_link": {
      "type": "Button",
      "props": { "label": { "$data": "/dev_link_label" }, "href": { "$data": "/dev_link" }, "variant": "outline", "visible": { "$data": "/dev_mode" } }
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author login.json (email-only) and login_confirm.json (confirmation + conditional dev link)</name>
  <files>app/src/views/login.json, app/src/views/login_confirm.json</files>
  <read_first>
    - app/src/views/login.json (current email+password structure — keep schema/layout/root/Card shape, remove password)
    - .planning/phases/202-login-resume-contract-magic-link-sample-app/202-UI-SPEC.md §"View File Contract" (View 1 + View 2 verbatim) and §"Copywriting Contract" (exact strings)
    - .planning/phases/202-login-resume-contract-magic-link-sample-app/202-RESEARCH.md Pitfall 1 (two-file approach), Pitfall 2 (CWD-relative view paths)
    - ferro-json-ui component support: confirm Button supports `href` and `visible` props (grep ferro-json-ui/src for the Button component schema)
  </read_first>
  <action>
    1. **Replace `app/src/views/login.json`** with the UI-SPEC View 1 JSON verbatim (from the interfaces block). Net change vs current file: `form.children` drops `"password"`, the `"password"` element is removed, submit label is `"Send login link"`, card gains the `description` "Enter your email to receive a login link."

    2. **Create `app/src/views/login_confirm.json`** with the UI-SPEC View 2 JSON verbatim.

    3. **Verify the `visible` / `href` Button props are supported** by ferro-json-ui (grep `ferro-json-ui/src` for `href` and `visible` in the Button component definition). The hard constraint (UI-SPEC): the raw verify URL must NOT appear in rendered HTML when `dev_mode` is false.
       - If Button supports `visible: { "$data": "/dev_mode" }` and hides the element (no anchor emitted when false): use the single `login_confirm.json` as written.
       - If Button does NOT support `visible` (the URL would leak in production HTML): apply the UI-SPEC fallback — create two files `login_confirm_dev.json` (with the dev_link Button) and `login_confirm.json` (without the dev_link element), and have the handler in Plan 02 pick the file by `is_development()`. If you take the fallback, you MUST update the Plan 02 request_link handler render call accordingly (this plan owns auth_controller.rs in Wave 3, so make the corresponding `JsonUi::render_file` change here and note it in the SUMMARY).

    All copy strings must be verbatim from the UI-SPEC Copywriting Contract — no paraphrasing.
  </action>
  <verify>
    <automated>python3 -c "import json; json.load(open('app/src/views/login.json')); json.load(open('app/src/views/login_confirm.json'))" && cargo test -p app login_view</automated>
  </verify>
  <acceptance_criteria>
    - Both files are valid JSON: `python3 -c "import json; json.load(open('app/src/views/login.json')); json.load(open('app/src/views/login_confirm.json'))"` exits 0
    - login.json has NO password field: `! grep -q '"password"' app/src/views/login.json && ! grep -q '"input_type": *"password"' app/src/views/login.json`
    - login.json posts to /auth/login: `grep -q '/auth/login' app/src/views/login.json`
    - Submit label correct: `grep -q 'Send login link' app/src/views/login.json`
    - Confirmation view present + titled: `grep -q 'Check your email' app/src/views/login_confirm.json`
    - Schema correct on both: `grep -q 'ferro-json-ui/v2' app/src/views/login.json && grep -q 'ferro-json-ui/v2' app/src/views/login_confirm.json`
  </acceptance_criteria>
  <done>login.json is the email-only form, login_confirm.json is the confirmation state with a dev-mode-gated link, both valid ferro-json-ui/v2, the production HTML never leaks the verify URL.</done>
</task>

<task type="auto">
  <name>Task 2: Update the login_view_* test to the email-only magic-link contract</name>
  <files>app/src/controllers/auth_controller.rs</files>
  <read_first>
    - app/src/controllers/auth_controller.rs lines 247-264 (the `login_view_is_valid_and_posts_to_login` test — currently asserts a `password` field + `input_type:"password"`)
    - app/src/views/login.json (the new structure from Task 1)
    - .planning/phases/202-login-resume-contract-magic-link-sample-app/202-PATTERNS.md §"auth_controller.rs" (test-to-update note)
  </read_first>
  <action>
    Update the existing `login_view_is_valid_and_posts_to_login` test in `auth_controller.rs`:
    - REMOVE `assert_eq!(v["elements"]["password"]["props"]["field"], "password")` and the `input_type:"password"` assertion.
    - ADD an assertion that the password element is gone: `assert!(v["elements"]["password"].is_null(), "login view must not contain a password field");`
    - KEEP / ensure: the email element exists with `input_type` `"email"`, the form posts to `/auth/login`, and the submit label is `"Send login link"`. Assert each:
      ```rust
      assert_eq!(v["elements"]["email"]["props"]["input_type"], "email");
      assert_eq!(v["elements"]["form"]["props"]["action"]["handler"], "/auth/login");
      assert_eq!(v["elements"]["submit"]["props"]["label"], "Send login link");
      ```
    - Keep the test's existing valid-schema check (it loads/parses `login.json`). Do NOT change the test name.

    Do not introduce a `render_file` request-time call in the test (CWD-relative — RESEARCH Pitfall 2); parse the JSON file directly as the existing test already does.
  </action>
  <verify>
    <automated>cargo test -p app login_view</automated>
  </verify>
  <acceptance_criteria>
    - No stale password assertion: `! grep -q 'input_type.*password' app/src/controllers/auth_controller.rs`
    - Asserts password is gone: `grep -q 'password.*is_null\|is_null.*password' app/src/controllers/auth_controller.rs`
    - Test name preserved: `grep -q 'login_view_is_valid_and_posts_to_login' app/src/controllers/auth_controller.rs`
    - `cargo test -p app login_view` exits 0
  </acceptance_criteria>
  <done>The view test asserts the email-only contract (no password, email input, posts to /auth/login, "Send login link" label) and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler data → rendered HTML | The dev link (a verify URL containing a live token) must reach the rendered page only in development. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-202-DEVLEAK | Information disclosure | `login_confirm.json` dev_link | mitigate | The dev link Button is gated by `visible: { "$data": "/dev_mode" }` (or, fallback, a separate production view file without the element). The raw verify URL must not appear in production-rendered HTML — verified by the dev_mode-false data contract (`dev_link: ""`, element hidden). |
| T-202-XSS | Tampering / injection | `error` / `dev_link` data binding | mitigate | All dynamic content flows through ferro-json-ui `$data` bindings (the renderer escapes output); no raw HTML interpolation in view files. |
</threat_model>

<verification>
- `cargo test -p app login_view` green
- Both view files parse as valid JSON; login.json has no password field; login_confirm.json gates the dev link on dev_mode
- Production render never contains the verify URL (data contract: dev_mode=false → dev_link="")
</verification>

<success_criteria>
- SC-4 satisfied: both auth views render through JSON-UI with layout:"auth", themed via the already-mounted ThemeMiddleware.
- The view test enforces the email-only magic-link contract.
</success_criteria>

<output>
After completion, create `.planning/phases/202-login-resume-contract-magic-link-sample-app/202-03-SUMMARY.md`
</output>
