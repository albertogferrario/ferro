---
phase: 202-login-resume-contract-magic-link-sample-app
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - .planning/phases/202-login-resume-contract-magic-link-sample-app/202-GATE.md
autonomous: true
requirements: [SC-5]
must_haves:
  truths:
    - "cargo fmt --all -- --check passes"
    - "cargo clippy --all --all-targets --all-features -- -D warnings passes (zero warnings)"
    - "cargo test --all-features passes (full workspace, including the new resume + magic-link tests)"
    - "RUSTDOCFLAGS=\"-D warnings\" cargo doc --no-deps --all-features passes (CI Docs gate is part of CI-green)"
    - "The app boots from a working directory other than app/ without a CWD-relative startup panic"
    - "No new from_path / CWD-sensitive startup code was introduced"
  artifacts:
    - path: ".planning/phases/202-login-resume-contract-magic-link-sample-app/202-GATE.md"
      provides: "recorded gate evidence (commands run + outcomes + CWD-boot result)"
      contains: "clippy"
  key_links: []
---

<objective>
Run the full Phase 202 quality gate and confirm CWD-independent boot. This is the SC-5 gate: `--all-features` clippy + tests green and the app boots from any working directory (no CWD-relative startup panic). Records evidence in a GATE.md.

Purpose: SC-5 — clippy/test green; app boots from any working directory.
Output: a gate-evidence record; any failures surfaced (this plan does NOT silently work around defects — it reports and, for trivial fmt/clippy fixes, fixes in place).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/202-login-resume-contract-magic-link-sample-app/202-RESEARCH.md

<interfaces>
<!-- Project memory constraints (MEMORY.md): -->
<!-- - cargo test --all-features can ENOSPC-fail (disk full) — check `df` and clean target/ first; that is NOT a real defect. -->
<!-- - Serialize CPU-heavy cargo runs (one at a time); do not parallelize clippy/test. -->
<!-- - CI also runs Docs build (cargo doc -Dwarnings) + cargo-deny; run cargo doc before declaring CI-green. -->
<!-- SC-5 CWD note (RESEARCH finding 6): JsonUi::render_file canonicalizes the view path at REQUEST time. -->
<!-- The ThemeMiddleware CWD-panic was already fixed (commit 10263291, Theme::default_theme embedded). -->
<!-- Confirm NO new from_path / CWD-relative startup code was introduced; boot is the assertion, not request-time view rendering. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run fmt + clippy(--all-features) + test(--all-features); fix trivial fmt/clippy issues; record evidence</name>
  <files>.planning/phases/202-login-resume-contract-magic-link-sample-app/202-GATE.md</files>
  <read_first>
    - CLAUDE.md "Testing & Linting" (exact gate commands; `--all-targets` required; CI enforces `-D warnings`)
    - .planning/phases/202-login-resume-contract-magic-link-sample-app/202-RESEARCH.md §"Validation Architecture" → Sampling Rate (phase gate command) and SC-5 row
    - .planning/phases/202-login-resume-contract-magic-link-sample-app/202-VALIDATION.md §"Test Infrastructure" (disk-full caveat; serialize CPU-heavy runs)
  </read_first>
  <action>
    Run the gate, one command at a time (serialize — do NOT parallelize CPU-heavy cargo runs per project memory). Before the full test run, check `df -h .` and `target/` size; if disk is tight, clean `target/` first (ENOSPC link/fingerprint errors are not real defects).

    1. `cargo fmt --all -- --check` — if it reports diffs, run `cargo fmt --all` to fix and re-check.
    2. `cargo clippy --all --all-targets --all-features -- -D warnings` — SC-5 uses `--all-features` on clippy. Fix any warning. For trivial mechanical warnings introduced by this phase (unused imports left after deleting the password path, needless clones, etc.) fix in place. If a warning indicates a real design discrepancy, surface it in the SUMMARY rather than papering over it (per project feedback: audit + report + fix, never silently work around).
    3. `cargo test --all-features` — full workspace; must include the new `resume`, `magic_link`, and `oauth_magic_link_resume_flow` tests plus the updated `login_view` test.
    4. `cargo doc --no-deps` (CI also runs Docs with `-Dwarnings`) — confirm the new resume rustdoc and the authentication.md changes do not introduce doc warnings.

    Record each command + its pass/fail + any fixes applied in `.planning/phases/202-login-resume-contract-magic-link-sample-app/202-GATE.md`.

    Note for the executor: trivial fmt/clippy fixes in `ferro-mcp-oauth` or `app` are permitted here even though those files belong to earlier plans — this is the consolidating gate. Record which files were touched in GATE.md.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --all --all-targets --all-features -- -D warnings && cargo test --all-features && RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy --all --all-targets --all-features -- -D warnings` exits 0 (zero warnings)
    - `cargo test --all-features` exits 0
    - `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --all-features` exits 0 (CI Docs gate — fmt/clippy/test alone is not CI-green per project memory)
    - GATE.md exists and records all four commands: `grep -q 'clippy' .planning/phases/202-login-resume-contract-magic-link-sample-app/202-GATE.md && grep -q 'test --all-features' .planning/phases/202-login-resume-contract-magic-link-sample-app/202-GATE.md && grep -q 'cargo doc' .planning/phases/202-login-resume-contract-magic-link-sample-app/202-GATE.md`
  </acceptance_criteria>
  <done>fmt, clippy --all-features, test --all-features, and RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --all-features all green; evidence recorded in GATE.md.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm CWD-independent boot and no new CWD-relative startup code</name>
  <files>.planning/phases/202-login-resume-contract-magic-link-sample-app/202-GATE.md</files>
  <read_first>
    - app/src/bootstrap.rs (ThemeMiddleware mount — confirm still `Theme::default_theme()`, the embedded CWD-independent form from commit 10263291)
    - .planning/phases/202-login-resume-contract-magic-link-sample-app/202-RESEARCH.md Finding 5 (ThemeMiddleware embedded), Finding 6 + Pitfall 2 (render_file is request-time CWD-relative, not a boot panic)
    - The Phase 202 diff surface: ferro-mcp-oauth/src/resume.rs, app/src/controllers/auth_controller.rs, app/src/views/*.json
  </read_first>
  <action>
    1. Static check: confirm no NEW `from_path` or CWD-relative file read was introduced at startup by this phase. Grep the phase's changed crates for `from_path(` and confirm `bootstrap.rs` still uses `Theme::default_theme()` (not a `from_path`). The magic-link views load via `JsonUi::render_file` which is request-time, not boot — that is expected and not an SC-5 boot failure.

    2. Boot check from a different working directory. Build the app binary, then run the boot from the repo root (a directory other than `app/`) and confirm it starts without a CWD-relative startup panic. Use a bounded run that exits cleanly — start the server from repo root with a short-lived health check or a `--check`/boot-only path if the app supports one; otherwise launch it backgrounded from repo root, confirm it reaches "listening" / does not panic on startup, then stop it. Do NOT exercise request-time view rendering for this check (the SC-5 assertion is *boot*, not request-time render — per RESEARCH finding 6 the test must not trigger CWD-relative view rendering from a changed CWD; run the live app from repo root only to prove the process boots).

       Concretely: from the repo root (NOT inside app/), run the app's boot. If the project has a binary like `target/debug/<appbin>`, invoke it from repo root with the env it needs (APP_ENV=local). Assert the process does not panic during startup (no "No such file or directory" / from_path panic in the first seconds of logs). Record the working directory used and the outcome in GATE.md.

    Append the boot-check result + the `from_path` grep result to `202-GATE.md`.
  </action>
  <verify>
    <automated>! grep -rn 'from_path(' app/src/bootstrap.rs && grep -q 'default_theme' app/src/bootstrap.rs</automated>
  </verify>
  <acceptance_criteria>
    - No from_path at boot: `! grep -q 'from_path(' app/src/bootstrap.rs`
    - Embedded theme retained: `grep -q 'default_theme' app/src/bootstrap.rs`
    - GATE.md records the CWD-boot outcome: `grep -qi 'boot\|working directory\|cwd' .planning/phases/202-login-resume-contract-magic-link-sample-app/202-GATE.md`
  </acceptance_criteria>
  <done>The app boots from the repo root (a non-app/ CWD) without a startup panic; no new from_path startup code; result recorded in GATE.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate → shipped artifact | The `--all-features` gate is the last barrier before the phase is considered done; a skipped warning ships a defect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-202-GATE | Repudiation / quality regression | clippy/test/doc gate | mitigate | Run the exact CI-matching gate (`--all --all-targets --all-features -D warnings` + `test --all-features` + `cargo doc`); record evidence in GATE.md; real warnings are fixed or surfaced, never suppressed. |
| T-202-BOOT | DoS / availability | app startup under non-app/ CWD | mitigate | Static grep confirms no new from_path; live boot from repo root confirms no startup panic (the ThemeMiddleware embedded-default fix is retained). |
</threat_model>

<verification>
- `cargo fmt --all -- --check && cargo clippy --all --all-targets --all-features -- -D warnings && cargo test --all-features` all green
- `cargo doc --no-deps` warning-free
- App boots from repo root without startup panic; no new from_path startup code
- 202-GATE.md records the evidence
</verification>

<success_criteria>
- SC-5 satisfied: clippy --all-features + test --all-features pass; app boots from any working directory with no CWD-relative startup panic.
</success_criteria>

<output>
After completion, create `.planning/phases/202-login-resume-contract-magic-link-sample-app/202-05-SUMMARY.md`
</output>
