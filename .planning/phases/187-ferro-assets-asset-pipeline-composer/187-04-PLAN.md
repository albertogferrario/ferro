---
phase: 187-ferro-assets-asset-pipeline-composer
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - ferro-assets/README.md
  - docs/src/features/ferro-assets.md
  - docs/src/SUMMARY.md
  - ferro-assets/src/lib.rs
  - ferro-assets/tests/passthrough_proof.rs
autonomous: false
requirements: [ASSET-F-01, ASSET-F-02, ASSET-F-03, ASSET-F-04]
user_setup:
  - service: crates.io
    why: "First publish of a brand-new crate (CI token has publish-update only, not publish-new)"
    dashboard_config:
      - task: "Run `cargo publish -p ferro-assets` once from a local terminal after this phase merges and before pushing master (CI cannot create a new crate). Same one-time chore as Phase 183/186."
        location: "local terminal at the ferro workspace root"
must_haves:
  truths:
    - "docs/src/features/ferro-assets.md exists and is linked from docs/src/SUMMARY.md"
    - "ferro-assets/README.md documents the passthrough guarantee, zero-C-deps, spawn_blocking contract, and the Asset.path/token-value caller-responsibility security notes"
    - "the crate-level rustdoc Quick Start compiles or is correctly ignored"
    - "a full-pipeline passthrough doc/integration proof runs the seven real transforms over a JSON file and asserts byte-identity (criterion 1, real-transform version)"
    - "the full CI-parity gate (fmt + clippy --all-targets + test --all-features) is green"
  artifacts:
    - path: "docs/src/features/ferro-assets.md"
      provides: "User-facing feature page (overview, quick start, transforms, custom transforms, error reference)"
    - path: "ferro-assets/README.md"
      provides: "crates.io front-page README with security caveats"
    - path: "docs/src/SUMMARY.md"
      provides: "Asset Pipeline entry under Features"
      contains: "features/ferro-assets.md"
  key_links:
    - from: "docs/src/SUMMARY.md"
      to: "docs/src/features/ferro-assets.md"
      via: "mdbook nav link"
      pattern: "features/ferro-assets.md"
---

<objective>
Finalize the crate for publication: write the `ferro-assets/README.md` (crates.io front page) and the
`docs/src/features/ferro-assets.md` feature page, link it from `docs/src/SUMMARY.md`, upgrade the SC-1
passthrough proof to run the SEVEN REAL transforms over a JSON file (criterion 1's real-transform
version), and run the full CI-parity gate. Surfaces the one-time manual `cargo publish` as a
non-autonomous follow-up (CI token cannot create a new crate).

Purpose: Closes the mandatory docs requirement (D-18), upgrades the criterion-1 proof from Plan 01's
mechanics-only test to a full real-pipeline byte-identity assertion, and confirms the whole crate
passes the exact CI command before verification.
Output: Published-ready crate with docs, README, full passthrough proof, and a recorded manual-publish step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/187-ferro-assets-asset-pipeline-composer/187-CONTEXT.md
@.planning/phases/187-ferro-assets-asset-pipeline-composer/187-RESEARCH.md
@.planning/phases/187-ferro-assets-asset-pipeline-composer/187-PATTERNS.md
@ferro-assets/src/lib.rs
@docs/src/features/deployments.md
@docs/src/SUMMARY.md
@ferro-bundle/README.md

<interfaces>
<!-- The complete public surface to document (Plans 01-03) -->
- `ferro_assets::{Asset, ContentType, infer_content_type, Pipeline, Transform, map_matching, Error}`
- `ferro_assets::transforms::{HtmlMinify, CssMinify, JsMinify, ImageTranscode, ResponsiveImages, InjectBeforeTag, ReplaceTokens}`
- Consumer composition order: html_minify → css_minify → js_minify → image_transcode → responsive_images → inject_before_tag → replace_tokens
- run() is synchronous; consumer wraps in tokio::task::spawn_blocking
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md + docs feature page + SUMMARY link + full real-transform passthrough proof + crate rustdoc</name>
  <files>ferro-assets/README.md, docs/src/features/ferro-assets.md, docs/src/SUMMARY.md, ferro-assets/src/lib.rs, ferro-assets/tests/passthrough_proof.rs</files>
  <read_first>
    - docs/src/features/deployments.md (exact docs-page structural model — Overview/Setup/API/Error Reference)
    - ferro-bundle/README.md and ferro-deployments/README.md (crates.io README shape)
    - docs/src/SUMMARY.md (line 52 Deployments entry — add Asset Pipeline after it)
    - .planning/phases/187-ferro-assets-asset-pipeline-composer/187-PATTERNS.md (§"docs/src/features/ferro-assets.md" section list; §"SUMMARY.md" edit; §"passthrough_proof.rs")
    - .planning/phases/187-ferro-assets-asset-pipeline-composer/187-CONTEXT.md (D-18 docs requirement; <specifics> zero-C-deps as a feature; passthrough proof criterion 1)
    - ferro-assets/src/lib.rs (current crate doc-header from Plan 01)
  </read_first>
  <action>
    1. Write `ferro-assets/README.md` (crates.io front page; mirror ferro-bundle/ferro-deployments shape):
       - one-line description + overview emphasizing the content-type-router passthrough guarantee
       - "Zero C system dependencies" stated as a feature (CONTEXT <specifics>; criterion 3 — every codec
         is pure Rust; libvips rejected)
       - Quick Start: Pipeline composition in the consumer order + the spawn_blocking note (run() is sync)
       - SECURITY NOTES section (from the threat models across Plans 01-03):
         * `Asset.path` is a LOGICAL key — ferro-assets writes no files; consumers MUST sanitize before
           using it as a real filesystem path (T-187-03)
         * `replace_tokens` does literal substitution with no eval/recursion — the CALLER owns and must
           sanitize token-map values (T-187-07)
         * image_transcode bounds concurrent encodes (default ≤2) to cap peak memory (T-187-09)
       - The README usage block must compile against the CURRENT public API (per STATE 160-05 lesson:
         crates.io front-page drift is a first-impression bug). NO app-identity strings (project-agnostic rule).
    2. Write `docs/src/features/ferro-assets.md` following the deployments.md model, sections (PATTERNS list):
       1) Overview (passthrough guarantee + zero-C-deps), 2) Quick Start (Pipeline + spawn_blocking),
       3) Asset & ContentType model, 4) Built-in Transforms (one subsection each: HtmlMinify with the
       opaque-script/style note, CssMinify, JsMinify, ImageTranscode with widths/AVIF/JPEG/bounded-concurrency,
       ResponsiveImages, InjectBeforeTag, ReplaceTokens with the caller-sanitization caveat),
       5) Writing Custom Transforms (the Transform trait + map_matching), 6) Error Reference table
       (Error::Transform, Error::Setup).
    3. Add `- [Asset Pipeline](features/ferro-assets.md)` to `docs/src/SUMMARY.md` immediately after the
       Deployments entry (line 52).
    4. Ensure the `ferro-assets/src/lib.rs` crate-rustdoc Quick Start example is `rust,ignore` (it uses
       tokio::spawn_blocking which ferro-assets does not depend on) so `cargo test --doc` does not fail.
    5. Upgrade `ferro-assets/tests/passthrough_proof.rs`: in addition to the Plan 01 mechanics test, add a
       test `json_file_unchanged_by_all_seven_real_transforms` that builds
       `Pipeline::new().add(HtmlMinify::new()).add(CssMinify::new()).add(JsMinify::new())
       .add(ImageTranscode::new()).add(ResponsiveImages::new()).add(InjectBeforeTag::new("</body>",
       "<script></script>")).add(ReplaceTokens::new(HashMap::new()))` and runs a JSON (ContentType::Other)
       asset through it, asserting the output bytes are byte-identical to the input (criterion 1's
       real-transform proof — this is the artifact-agnostic guarantee with the actual built-ins, replacing
       the local-stub version from Plan 01).
  </action>
  <verify>
    <automated>test -f docs/src/features/ferro-assets.md && grep -q 'features/ferro-assets.md' docs/src/SUMMARY.md && cargo test -p ferro-assets --test passthrough_proof 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `test -f ferro-assets/README.md` and `grep -qi 'zero C' ferro-assets/README.md`
    - `grep -qi 'spawn_blocking' ferro-assets/README.md`
    - `grep -qi 'Asset.path\|logical key' ferro-assets/README.md` (path-sanitization caveat)
    - `grep -qi 'sanitize' ferro-assets/README.md` (token-value caller responsibility)
    - `test -f docs/src/features/ferro-assets.md`
    - `grep -q 'features/ferro-assets.md' docs/src/SUMMARY.md`
    - `grep -q 'json_file_unchanged_by_all_seven_real_transforms' ferro-assets/tests/passthrough_proof.rs`
    - `cargo test -p ferro-assets --test passthrough_proof` exits 0 (full real-transform byte-identity passes)
  </acceptance_criteria>
  <done>README + docs feature page exist with security caveats and zero-C-deps framing; SUMMARY links the page; the full seven-real-transform passthrough proof is green; rustdoc doctest does not fail.</done>
</task>

<task type="auto">
  <name>Task 2: Full CI-parity gate (fmt + clippy --all-targets + test --all-features)</name>
  <files>ferro-assets/Cargo.toml</files>
  <read_first>
    - CLAUDE.md (Testing & Linting section — the exact CI-parity command; -D warnings is a build failure)
    - .planning/phases/187-ferro-assets-asset-pipeline-composer/187-VALIDATION.md (Sampling Rate: full suite before /gsd-verify-work)
    - MEMORY.md feedback_ci_clippy_command_match (match CI's exact clippy command)
  </read_first>
  <action>
    Run the full CI-parity gate exactly as CLAUDE.md specifies (one CPU op at a time — do NOT chain these
    into a single parallel invocation; run sequentially and reuse prior step evidence):
    1. `cargo fmt --all -- --check`
    2. `cargo clippy --all --all-targets -- -D warnings`
    3. `cargo test --all-features`
    Then run the heavy gated test once: `cargo test -p ferro-assets --features slow-tests`.
    Fix any formatting, clippy, or test failures introduced by ferro-assets. Do NOT bump the workspace
    version: per STATE.md the workspace is already at 0.2.45 and a new crate inherits the workspace version
    via `version.workspace = true` — confirm no separate bump is required (sibling new crates ferro-bundle/
    ferro-deployments did not bump for their own introduction). If `cargo test --all-features` reveals a
    cross-crate impact, fix it within ferro-assets scope only.
    DO NOT run `cargo publish` — the first publish is the non-autonomous manual follow-up below.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings 2>&1 | tail -5 && cargo test --all-features 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy --all --all-targets -- -D warnings` exits 0
    - `cargo test --all-features` exits 0
    - `cargo test -p ferro-assets --features slow-tests` exits 0 (heavy AVIF encode test passes once)
    - `grep -q 'version.workspace = true' ferro-assets/Cargo.toml` (inherits workspace version; no separate bump)
  </acceptance_criteria>
  <done>The full CI-parity command set is green across the workspace; the heavy transcode test passes under slow-tests; ferro-assets inherits the workspace version with no separate bump.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 (checkpoint): One-time manual first publish of ferro-assets to crates.io</name>
  <action>PAUSE for the human operator. The first publish of this new crate cannot be automated (CI token is publish-update only). The operator runs `cargo publish -p ferro-assets` once from a local terminal. See <how-to-verify> for the exact steps.</action>
  <what-built>ferro-assets is complete, documented, and CI-green. It is a brand-new crate that does not yet exist on crates.io.</what-built>
  <how-to-verify>
    The CI publish token has `publish-update` scope only — it cannot create a new crate (same constraint
    as Phase 183 ferro-bundle and Phase 186 ferro-deployments). Before pushing master so GH Actions can
    publish, run the one-time manual first publish from a local terminal at the ferro workspace root:

      cargo publish -p ferro-assets

    (Run `cargo publish -p ferro-assets --dry-run` first to confirm packaging.) After this one-time manual
    publish succeeds, all subsequent versions publish via CI automatically. This step is unavoidable and
    cannot be automated — it requires your authenticated local crates.io credentials with publish-new scope.
  </how-to-verify>
  <resume-signal>Type "published" once `cargo publish -p ferro-assets` has succeeded, or "defer" to leave the manual publish as a tracked pending todo for the milestone push.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/README → developer | Documentation conveys the security contract (path sanitization, token-value sanitization) to consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-13 | Information Disclosure / Tampering | Consumer misuses Asset.path or unsanitized token values for lack of documentation | mitigate | README + docs feature page explicitly document Asset.path-is-a-logical-key and replace_tokens-caller-sanitizes contracts (closes the documentation half of T-187-03 / T-187-07) |
| T-187-14 | Elevation of Privilege | Accidental CI publish of an unverified crate | accept | First publish is deliberately a manual local step (CI token lacks publish-new); the checkpoint enforces human gating before any push |
</threat_model>

<verification>
- docs/src/features/ferro-assets.md exists, linked from SUMMARY.md.
- README documents zero-C-deps, spawn_blocking, and both security caveats.
- Full real-transform passthrough proof (criterion 1) green.
- `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` all green.
- Heavy transcode test green under `--features slow-tests`.
- Manual first-publish surfaced as a blocking human-action checkpoint.
</verification>

<success_criteria>
All four requirements (ASSET-F-01..04) are documented and proven. The crate is publication-ready:
docs page + README (with security notes) + linked SUMMARY + CI-parity green + criterion-1 real-transform
passthrough proof. The one-time manual `cargo publish -p ferro-assets` is recorded as a non-autonomous
follow-up.
</success_criteria>

<output>
After completion, create `.planning/phases/187-ferro-assets-asset-pipeline-composer/187-04-SUMMARY.md`.
</output>
