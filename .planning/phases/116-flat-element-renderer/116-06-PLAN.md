---
phase: 116-flat-element-renderer
plan: 06
type: execute
wave: 4
depends_on:
  - "116-02"
  - "116-03"
  - "116-04"
  - "116-05"
files_modified:
  - framework/src/json_ui/mod.rs
autonomous: true
requirements:
  - RENDER-01
  - RENDER-02
  - RENDER-03
must_haves:
  truths:
    - "framework/src/json_ui/mod.rs tests assert real v2 HTML markers instead of the Phase 115 placeholder marker string"
    - "The Leaflet plugin integration test (test_plugin_component_renders_in_full_page) is un-ignored and passes against the real walker (leaflet.css, leaflet.js, data-ferro-map, DOMContentLoaded present in body)"
    - "Stale in-source comments that reference the 'placeholder renderer' are either deleted or rewritten to describe the real walker"
    - "The render_with_errors form integration test continues to pass — error strings from resolve_errors surface in the rendered Input HTML, not only in the data-view serialized spec"
    - "The full CI-parity gate (cargo fmt --all -- --check && cargo clippy --all --all-targets --all-features -- -D warnings && cargo test --all-features) exits 0"
    - "No grep hit for 'render_to_html(' in ferro-json-ui/src or framework/src (SC-6 holds after integration)"
    - "No grep hit for 'v2 render pipeline arrives in Phase 116' anywhere in the workspace (the placeholder marker is gone from source, tests, and docs)"
  artifacts:
    - path: "framework/src/json_ui/mod.rs"
      provides: "Integration tests aligned with the real v2 walker — no lingering references to the placeholder renderer"
      contains: "data-ferro-map"
    - path: ".planning/phases/116-flat-element-renderer/116-06-SUMMARY.md"
      provides: "Phase 116 completion summary — plans executed, tests added, grep-gates confirmed, workspace green"
      contains: "Phase 116 complete"
  key_links:
    - from: "framework/src/json_ui/mod.rs::test_plugin_component_renders_in_full_page"
      to: "ferro-json-ui::render::render_spec_to_html_with_plugins"
      via: "Map element type → plugin registry → Leaflet asset collection → head/body injection"
      pattern: "MapPlugin"
    - from: "framework/src/json_ui/mod.rs::render_with_errors_populates_form_fields"
      to: "ferro-json-ui::resolve::resolve_errors + render_input (Plan 05)"
      via: "resolve_errors populates props.errors; render_input emits the error strings into the Input HTML"
      pattern: "Name is required"
---

<objective>
Wave 4 integration work for Phase 116. The renderer is now real (Plans 02–05). This plan closes the loop at the framework layer:

1. Delete the placeholder-era comments and tests that no longer reflect reality.
2. Un-ignore the Leaflet plugin integration test and confirm it passes.
3. Confirm `render_with_errors_populates_form_fields` still asserts correct behavior against the real walker — update the assertion if the real `render_input` surfaces error strings differently from the serialized-spec fallback.
4. Run the full CI-parity gate across the workspace.
5. Write the Phase 116 completion summary.

This is the phase-closing plan — every earlier plan's output flows into here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-flat-element-renderer/116-CONTEXT.md
@.planning/phases/116-flat-element-renderer/116-RESEARCH.md
@.planning/phases/116-flat-element-renderer/116-VALIDATION.md
@framework/src/json_ui/mod.rs
@ferro-json-ui/src/render/mod.rs
@ferro-json-ui/src/render/atoms.rs
@ferro-json-ui/src/render/containers.rs
@ferro-json-ui/src/render/form.rs
@ferro-json-ui/src/render/data.rs
@ferro-json-ui/src/plugins/mod.rs
@ferro-json-ui/src/plugin.rs
@ferro-json-ui/src/spec.rs
@ferro-json-ui/src/resolve.rs
@ferro-json-ui/src/component.rs

<interfaces>
<!-- Integration surface. Framework-level tests exercise the real walker end-to-end. -->

From ferro-json-ui (after Plans 02–05):
```rust
pub fn render_spec_to_html(spec: &Spec, data: &Value) -> String;
pub fn render_spec_to_html_with_plugins(spec: &Spec, data: &Value) -> RenderResult;
pub struct RenderResult { pub html: String, pub css_head: String, pub scripts: String }
```

From framework/src/json_ui/mod.rs (current — tests exercise this entry point):
```rust
impl JsonUi {
    pub fn render(spec: &Spec, data: &Value) -> Response { ... }
    pub fn render_with_config(spec: &Spec, data: &Value, config: &JsonUiConfig) -> Response { ... }
    pub fn render_with_errors(spec: &Spec, data: &Value, errors: &HashMap<String, Vec<String>>) -> Response { ... }
    pub fn render_json(spec: &Spec, data: &Value) -> Response { ... }
}
```

No signature changes in this plan. The framework's `JsonUi` wrapper was already switched to `&Spec` in Phase 115-03; Plan 06 only updates tests and stale comments.
</interfaces>

<current_state>
Placeholder-era references currently in framework/src/json_ui/mod.rs:
- Line ~546–548: doc comment on `form_spec_with_inputs` helper refers to "the placeholder renderer does not walk the element graph, but the resolver still populates per-element props.errors so the serialized spec (in the data-view attribute and in the placeholder body) reflects them."
- Line ~607–608: inline comment in `render_with_errors_populates_form_fields` says "serialized spec (embedded in data-view and printed by the placeholder renderer) therefore contains the error strings."
- Line ~961–963: comment above the Leaflet plugin test explaining why it is ignored.
- Line ~966: `#[ignore = "TODO(Phase 116): placeholder renderer does not collect plugin assets"]` attribute.

These all need to be deleted or rewritten. The test bodies themselves do not need structural changes — their assertions (body contains "Name is required", "leaflet.css", "data-ferro-map", etc.) already describe the target behavior of the real walker. What changes is (a) the ignore is removed, (b) the comments describing why the tests work update to reflect the real walker path, (c) if any assertion fails after un-ignoring, investigate whether it's a plan-05 form renderer bug or a plan-02 walker bug before touching this plan.
</current_state>
</context>

<tasks>

<task id="1">
  <title>Delete placeholder-era comments and un-ignore the Leaflet plugin test</title>
  <wave>4</wave>
  <type>execute</type>
  <files>
    - framework/src/json_ui/mod.rs
  </files>
  <read_first>
    - framework/src/json_ui/mod.rs
    - .planning/phases/116-flat-element-renderer/116-CONTEXT.md
    - .planning/phases/116-flat-element-renderer/116-RESEARCH.md
    - ferro-json-ui/src/render/mod.rs (to confirm the real walker shape is landed)
  </read_first>
  <action>
**Step 1: Rewrite the `form_spec_with_inputs` helper doc comment.**

Locate the `/// A form spec with two Input children...` doc comment preceding `fn form_spec_with_inputs()` (currently ~line 546). Replace the three-line "The placeholder renderer does not walk the element graph, but the resolver still populates per-element `props.errors` so the serialized spec (in the `data-view` attribute and in the placeholder body) reflects them." passage with:

```
/// A form spec with two Input children identified by field name. The
/// v2 walker renders each Input with its `props.errors` array, which
/// `resolve_errors` populates per-field before render. Error strings
/// surface both in the rendered `<span class="... error ...">` markup
/// and (for JSON handlers) in the serialized spec under `data-view`.
```

**Step 2: Remove the inline placeholder-body comment inside `render_with_errors_populates_form_fields`.**

Locate the block (currently ~line 606–608):
```rust
// Each input's props.errors field is populated by resolve_errors; the
// serialized spec (embedded in data-view and printed by the placeholder
// renderer) therefore contains the error strings.
```

Replace with:
```rust
// resolve_errors populates each Input's props.errors array; the v2 walker
// renders each Input body so the error strings appear both in the HTML
// (render_input emits them alongside the field) and in the data-view
// serialized spec embedded in the page.
```

**Step 3: Remove the "placeholder renderer does not collect plugin assets" comment block.**

Locate (currently ~line 960–965):
```rust
// The placeholder renderer in Phase 115 does not walk the element graph,
// which means it does not collect plugin CSS/JS assets (no Leaflet link,
// no Leaflet script tag, no `data-ferro-map` container). Phase 116's real
// walker restores this behavior; until then the test below stays ignored.
```

Replace with a single line of explanatory context:
```rust
// Plugin integration: MapPlugin is auto-registered in the global plugin
// registry. The v2 walker dispatches unknown type_names ("Map") through
// the plugin registry and collects CSS/JS assets for head/body injection.
```

**Step 4: Un-ignore `test_plugin_component_renders_in_full_page`.**

Locate the `#[ignore = "TODO(Phase 116): placeholder renderer does not collect plugin assets"]` attribute on `fn test_plugin_component_renders_in_full_page()`. Delete the entire `#[ignore = "..."]` line. The `#[test]` attribute on the line above it remains.

**Step 5: Workspace-wide grep to catch any other lingering placeholder references.**

Run `grep -rn "placeholder renderer\|v2 render pipeline arrives in Phase 116" ferro-json-ui/src framework/src app/src`. If any hits remain in source files (tests, doc comments, or module-level docstrings), rewrite or delete them the same way — placeholder context is no longer accurate.

Expected hits after cleanup: zero.
  </action>
  <verify>
    <automated>! grep -rn "placeholder renderer\|v2 render pipeline arrives in Phase 116" ferro-json-ui/src framework/src app/src 2>/dev/null &amp;&amp; ! grep -n '#\[ignore = "TODO(Phase 116)' framework/src/json_ui/mod.rs &amp;&amp; cargo test -p ferro --lib json_ui::mod::tests::render_produces_valid_html &amp;&amp; cargo test -p ferro --lib json_ui::mod::tests::render_with_errors_populates_form_fields</automated>
  </verify>
  <done>
    - No `placeholder renderer` or `v2 render pipeline arrives in Phase 116` string remains anywhere under ferro-json-ui/src, framework/src, app/src.
    - `#[ignore = "TODO(Phase 116): placeholder renderer does not collect plugin assets"]` attribute deleted.
    - `render_produces_valid_html` and `render_with_errors_populates_form_fields` tests still pass.
    - Doc-comment rewrites describe the real walker behavior, not the placeholder.
  </done>
  <threat_model>
    No new attack surface — this task edits comments and test attributes only. The Leaflet plugin dispatch path was already present in v1; un-ignoring the test just exercises it against the new walker. XSS/injection posture is unchanged.
  </threat_model>
</task>

<task id="2">
  <title>Confirm the Leaflet plugin integration test passes end-to-end</title>
  <wave>4</wave>
  <type>execute</type>
  <files>
    - framework/src/json_ui/mod.rs
  </files>
  <read_first>
    - framework/src/json_ui/mod.rs (the just-un-ignored test)
    - ferro-json-ui/src/plugins/mod.rs (MapPlugin implementation)
    - ferro-json-ui/src/plugin.rs (registry + asset collection)
    - ferro-json-ui/src/render/mod.rs (walker's plugin fallback + collect_plugin_types)
    - ferro-json-ui/src/plugins/map.rs (if this exists — the MapPlugin's HTML emission template)
  </read_first>
  <action>
**Step 1: Run the un-ignored Leaflet test.**

```bash
cargo test -p ferro --lib json_ui::mod::tests::test_plugin_component_renders_in_full_page -- --nocapture
```

**Expected passing state:** the response body contains all four of:
- `leaflet.css`
- `leaflet.js`
- `data-ferro-map`
- `DOMContentLoaded`

**Step 2: If the test fails, diagnose by assertion.**

If the test fails on one of the four assertions, the failure points to a specific upstream plan's gap:

- **Fails on `leaflet.css` or `leaflet.js` missing:** asset collection is broken. Inspect `ferro-json-ui/src/render/mod.rs::collect_plugin_types` — it must walk `spec.elements.values()` and subtract `BUILTIN_TYPES`. Inspect `framework/src/json_ui/mod.rs` — it must call `render_spec_to_html_with_plugins` (not `render_spec_to_html`) and inject `result.css_head` into `<head>` and `result.scripts` before `</body>`. Report the gap and route the fix back to Plan 02.
- **Fails on `data-ferro-map` missing:** plugin dispatch is broken. Inspect the dispatch default arm in `render/mod.rs` — it must fall through to `with_plugin(type_name, |p| p.render(&el.props, data))`. Inspect MapPlugin's `render` method — it must emit a `<div data-ferro-map="..."` container. Route fix to Plan 02 (dispatch) or ferro-json-ui plugins module (MapPlugin body).
- **Fails on `DOMContentLoaded` missing:** plugin init scripts are not being injected. Inspect `collect_plugin_assets` in `plugin.rs` — it should collect `init_script()` returns. Inspect `render_js_tags` (or equivalent) in `render/mod.rs` — it must wrap init scripts in a `DOMContentLoaded` event listener. Route fix to Plan 02.

Do NOT modify the test assertions to make the test pass — the assertions are the contract. If an assertion seems wrong, surface it in the summary as a finding.

**Step 3: If the test passes, verify asset deduplication by spec with two Map elements.**

Add a secondary assertion inside the existing test (after the four current asserts):

```rust
// Assets are deduplicated — one `leaflet.css` link even if multiple Map
// elements reference the plugin.
let css_link_count = body.matches("leaflet.css").count();
assert_eq!(
    css_link_count, 1,
    "expected exactly 1 leaflet.css link in body, found {css_link_count}"
);
```

This guards CONTEXT D-18 (asset collection must deduplicate by URL).

**Step 4: Run the full ferro crate test suite to confirm no sibling regression.**

```bash
cargo test -p ferro --lib json_ui::mod::tests
```

All tests in the `json_ui::mod::tests` module should be green.
  </action>
  <verify>
    <automated>cargo test -p ferro --lib json_ui::mod::tests::test_plugin_component_renders_in_full_page &amp;&amp; cargo test -p ferro --lib json_ui::mod::tests</automated>
  </verify>
  <done>
    - `test_plugin_component_renders_in_full_page` passes with all 4 original assertions plus the deduplication guard.
    - Every test in `framework::src::json_ui::mod::tests` is green.
    - If any failure surfaces a gap in Plans 02/03/04/05, the gap is documented in the SUMMARY (do NOT silently patch here).
  </done>
  <threat_model>
    Plugin-path specific: the MapPlugin emits HTML containing user-supplied marker popup content. CONTEXT D-17 says plugins operate on raw `serde_json::Value` props — plugin authors are responsible for their own HTML escaping. The Leaflet test does not exercise escaping; not in scope for Phase 116. Flag "plugin-author XSS discipline" as a deferred item in the summary.
  </threat_model>
</task>

<task id="3">
  <title>Run the full CI-parity workspace gate</title>
  <wave>4</wave>
  <type>execute</type>
  <files>
    - (no files modified — verification task)
  </files>
  <read_first>
    - CLAUDE.md (confirms the command)
  </read_first>
  <action>
**Step 1: Run formatter check.**

```bash
cargo fmt --all -- --check
```

Expected: exits 0, no output. If there are diff reports, run `cargo fmt --all` to fix, then re-verify.

**Step 2: Run clippy gate across the entire workspace with all features and all targets.**

```bash
cargo clippy --all --all-targets --all-features -- -D warnings
```

Expected: exits 0. `-D warnings` promotes every warning to an error — a single clippy advisory from any crate fails this gate. Common failure modes in Phase 116:
- `#[allow(dead_code)]` left on `resolve_path`/`resolve_path_string` in `ferro-json-ui/src/data.rs` after Plan 05 dropped the attribute → clippy complains dead code. Fix: verify Plan 05's removal landed (grep for `#\[allow(dead_code)\]` in `data.rs` should return zero hits).
- Unused imports in `render/mod.rs`, `render/atoms.rs`, etc. — likely if a Plan 02 stub import was never consumed by Plan 03/04/05. Remove the unused import.
- Non-idiomatic patterns added during the port (e.g., `&format!(...)` instead of `&format!(...).as_str()`, unnecessary `.clone()` calls). Fix per clippy advisory.

**Step 3: Run the full test suite with all features.**

```bash
cargo test --all-features
```

Expected: exits 0. This covers:
- `ferro-json-ui` crate: spec, component, visibility, resolve, plugin, render::mod, render::atoms, render::containers, render::form, render::data (inline tests + integration under `tests/`).
- `framework` crate: `json_ui::mod::tests` including the un-ignored Leaflet test.
- Every other ferro crate — none should be affected by Phase 116, but the cross-cutting test run catches indirect breakage.

**Step 4: Grep confirmation that Phase 116 success criterion 6 holds.**

```bash
grep -rn "render_to_html\b" ferro-json-ui/src framework/src app/src 2>/dev/null
```

Expected: zero output. The v1 symbol `render_to_html` (no `_spec_` infix) must not appear in any source file. Phase 115-02 already removed it; Phase 116 verifies the removal survived.

**Step 5: Record the gate result.**

Capture the exit codes of steps 1, 2, 3 in the SUMMARY file (Task 4). If any step failed, stop and route the failure back to the appropriate plan — do NOT create a SUMMARY declaring success.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check &amp;&amp; cargo clippy --all --all-targets --all-features -- -D warnings &amp;&amp; cargo test --all-features &amp;&amp; ! grep -rn "render_to_html\b" ferro-json-ui/src framework/src app/src 2>/dev/null</automated>
  </verify>
  <done>
    - `cargo fmt --all -- --check` exits 0.
    - `cargo clippy --all --all-targets --all-features -- -D warnings` exits 0 across the workspace.
    - `cargo test --all-features` exits 0 with every ferro crate's tests green.
    - `grep -rn "render_to_html\b"` returns zero hits under ferro-json-ui/src, framework/src, app/src.
  </done>
  <threat_model>
    N/A — verification-only task.
  </threat_model>
</task>

<task id="4">
  <title>Write Phase 116 completion summary</title>
  <wave>4</wave>
  <type>execute</type>
  <files>
    - .planning/phases/116-flat-element-renderer/116-06-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/116-flat-element-renderer/116-CONTEXT.md
    - .planning/phases/116-flat-element-renderer/116-RESEARCH.md
    - .planning/phases/116-flat-element-renderer/116-VALIDATION.md
    - .planning/phases/116-flat-element-renderer/116-01-SUMMARY.md (if exists)
    - .planning/phases/116-flat-element-renderer/116-02-SUMMARY.md (if exists)
    - .planning/phases/116-flat-element-renderer/116-03-SUMMARY.md (if exists)
    - .planning/phases/116-flat-element-renderer/116-04-SUMMARY.md (if exists)
    - .planning/phases/116-flat-element-renderer/116-05-SUMMARY.md (if exists)
    - @$HOME/.claude/get-shit-done/templates/summary.md (format template)
  </read_first>
  <action>
Create `.planning/phases/116-flat-element-renderer/116-06-SUMMARY.md` following the template format. The summary closes the phase and must include:

**Section 1: Phase 116 Completion Status**

State whether Phase 116 succeeded. Reference the full-CI gate result from Task 3. Include the exact commit hash(es) if known.

**Section 2: Success Criteria Status**

Table of the 6 ROADMAP success criteria, with PASS/FAIL evidence for each:

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | render_spec_to_html renders all component types from flat element map | PASS | cargo test render::{atoms,containers,form,data} passes — N tests green (cite count) |
| 2 | Missing children graceful (skip + warn, no panic) | PASS | walker_missing_child test passes; diagnostic HTML comment `<!-- ferro-json-ui: element references missing id 'X' -->` present in output |
| 3 | Action resolution works on flat elements | PASS | walker_action_url_inlined + walker_action_url_unresolved tests pass; render_with_errors_populates_form_fields asserts on resolved URL |
| 4 | Visibility evaluation works on flat elements | PASS | walker_visible_hides_element + walker_visible_hides_children + walker_root_hidden tests pass; Visibility::evaluate handles all 11 VisibilityOperator variants |
| 5 | Plugin components render correctly in v2 specs | PASS | test_plugin_component_renders_in_full_page (un-ignored) passes with leaflet.css/js/data-ferro-map/DOMContentLoaded + dedup guard |
| 6 | Old render_to_html(view, data) function is deleted | PASS | grep -rn "render_to_html\b" returns zero hits in ferro-json-ui/src, framework/src, app/src |

**Section 3: Plans Executed**

List of Plans 01–06 with their net LOC delta and what landed. Reference each plan's SUMMARY.md.

**Section 4: Surfaces Touched**

- `ferro-json-ui/src/component.rs` — 5 slot fields re-added (CardProps.footer, ModalProps.footer, Tab.children, KanbanColumnProps.children, PageHeaderProps.actions).
- `ferro-json-ui/src/visibility.rs` — `Visibility::evaluate(&Value) -> bool` + `evaluate_condition` helper added (previously absent; v1 renderer ignored visibility).
- `ferro-json-ui/src/render.rs` — deleted (contents replaced by `render/` module).
- `ferro-json-ui/src/render/` — new directory: mod.rs (walker), atoms.rs, containers.rs, form.rs, data.rs.
- `ferro-json-ui/src/lib.rs` — re-exports unchanged; `COMPONENT_CATALOG` entries updated for the 5 slot-bearing components.
- `ferro-json-ui/src/data.rs` — `#[allow(dead_code)]` dropped on `resolve_path` and `resolve_path_string`.
- `framework/src/json_ui/mod.rs` — placeholder comments deleted; Leaflet plugin test un-ignored.

**Section 5: Test Coverage Delta**

- N inline tests ported from v1 render.rs (itemize by component file).
- M walker-specific tests added (missing-child, unknown-type, visibility-hides, plugin dispatch, action URL inlining, cycle tripwire, dedup).
- Framework-level test: `test_plugin_component_renders_in_full_page` activated.

**Section 6: Deferred Items (carried into later phases)**

- Catalog / JSON Schema assembly — Phase 117.
- Plugin schema registration API — Phase 117.
- `$data` / `$template` expression evaluation — Phase 118.
- Spec hot-reload / page loader — Phase 119.
- MCP v2 updates — Phase 120.
- Docs rewrite + gestiscilo field test — Phase 121.
- `tracing`/`log` dep for diagnostics (decision is "HTML comments for now; revisit if ops need more").
- Full slot-ID graph validation (Phase 117 catalog concern; Phase 116 accepts the gap).

**Section 7: Risks Realized / Mitigated**

Walk through the 4 RESEARCH risks (HIGH-1 COMPONENT_CATALOG drift, HIGH-2 Visibility::evaluate missing, MEDIUM framework test marker, MEDIUM sample app) and note whether each was fixed inline in Phase 116 or deferred with rationale.

**Section 8: Hand-off to Phase 117**

Phase 117 (Catalog & JSON Schema) now has:
- A real walker that dispatches by `type_name` — Catalog's job becomes "drive the walker with a schema".
- `schemars::JsonSchema` derives already live on every Props struct (Phase 115 shipped this).
- Per-component schemas can be assembled via `schema_for!(CardProps)` without touching the walker.
- `BUILTIN_TYPES` in `render/mod.rs` is the canonical type list Phase 117 can reflect on.

Call out the COMPONENT_CATALOG string as explicitly replaced by `catalog.prompt()` in Phase 117 — Phase 116's catalog fixes are a short-term consistency measure.

Commit the summary:

```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-116): plan 06 complete — integration + phase gate" --files ".planning/phases/116-flat-element-renderer/116-06-SUMMARY.md"
```
  </action>
  <verify>
    <automated>test -f .planning/phases/116-flat-element-renderer/116-06-SUMMARY.md &amp;&amp; grep -q "Phase 116 complete\|Phase 116 Completion" .planning/phases/116-flat-element-renderer/116-06-SUMMARY.md &amp;&amp; grep -q "Success Criteria" .planning/phases/116-flat-element-renderer/116-06-SUMMARY.md</automated>
  </verify>
  <done>
    - `116-06-SUMMARY.md` exists, follows the template format, and contains all 8 sections.
    - Every one of the 6 ROADMAP success criteria is listed with PASS/FAIL status and evidence.
    - Deferred items explicitly enumerated (not left implicit).
    - Hand-off section gives Phase 117 a clear starting surface.
    - Summary is committed via the gsd-tools commit helper.
  </done>
  <threat_model>
    N/A — documentation task.
  </threat_model>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Framework handler → JsonUi::render → ferro-json-ui walker → HTML response | All data flows one direction; Phase 116 does not introduce any new inbound channel. |
| Plugin registry lookup at walker default arm | `with_plugin` is a read-only registry lookup; no mutation, no I/O. |
| Leaflet CDN asset URLs | Loaded via `<link>` and `<script>` tags with SRI hashes if Asset::integrity is set. Asset URL hardcoded inside MapPlugin — not user-controlled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-P06-01 | Tampering | Placeholder comments in framework/src/json_ui/mod.rs mention code paths that no longer exist — misleading future readers | mitigate | Task 1 rewrites all placeholder-era doc comments to reflect the real walker. Task 4 summary records this cleanup. |
| T-116-P06-02 | Repudiation | Phase closes without a summary documenting what changed | mitigate | Task 4 produces `116-06-SUMMARY.md` with plans executed, tests added, risks realized. Committed via gsd-tools. |
| T-116-P06-03 | Information disclosure | Leaflet plugin integration test leaks no sensitive data — MapPlugin renders server-authored marker coordinates | accept | CONTEXT D-17 delegates plugin HTML escape discipline to the plugin author. Not a Phase 116 regression; flagged in summary as deferred. |
| T-116-P06-04 | Denial of service | Full CI-parity workspace test suite adds meaningful run time | accept | CI gate latency ~90s per CLAUDE.md; acceptable for phase-closing verification. |
| T-116-P06-05 | Elevation of privilege | N/A — no new privilege boundary introduced by Phase 116 | accept | Renderer is pure HTML string emission; no IPC, no FS writes, no network. |
</threat_model>

<verification>
- Phase 116 success criterion 1 (all component types render): confirmed via plan 03/04/05 unit tests + framework-level `render_produces_valid_html`.
- Phase 116 success criterion 2 (missing children graceful): confirmed via walker unit tests in Plan 02.
- Phase 116 success criterion 3 (action resolution on flat elements): confirmed via walker + form-control tests + `render_with_errors_populates_form_fields`.
- Phase 116 success criterion 4 (visibility evaluation): confirmed via walker visibility tests in Plan 02 + `Visibility::evaluate` tests in Plan 01.
- Phase 116 success criterion 5 (plugin components render in v2 specs): confirmed via `test_plugin_component_renders_in_full_page` un-ignore.
- Phase 116 success criterion 6 (old `render_to_html` deleted): confirmed via workspace grep returning zero hits.
- Workspace green: `cargo fmt --all -- --check && cargo clippy --all --all-targets --all-features -- -D warnings && cargo test --all-features` exits 0.
- No placeholder-era marker strings remain in source files.
- Phase summary committed.
</verification>

<success_criteria>
- 4 tasks complete (comment cleanup, Leaflet un-ignore, full CI gate, summary).
- The `#[ignore]` attribute on `test_plugin_component_renders_in_full_page` is removed.
- Every framework test in `json_ui::mod::tests` passes including the un-ignored Leaflet test with dedup guard.
- `cargo fmt --all -- --check` exits 0.
- `cargo clippy --all --all-targets --all-features -- -D warnings` exits 0.
- `cargo test --all-features` exits 0.
- `grep -rn "render_to_html\b" ferro-json-ui/src framework/src app/src` returns zero hits.
- `grep -rn "placeholder renderer\|v2 render pipeline arrives in Phase 116" ferro-json-ui/src framework/src app/src` returns zero hits.
- `.planning/phases/116-flat-element-renderer/116-06-SUMMARY.md` exists, is committed, and documents all 6 ROADMAP success criteria with PASS/FAIL evidence.
</success_criteria>

<output>
After completion, `.planning/phases/116-flat-element-renderer/116-06-SUMMARY.md` exists and is committed. Phase 116 is closed. State is ready for Phase 117 planning.
</output>
