---
phase: 181
plan: 07
type: execute
wave: 7
depends_on: ["06"]
files_modified:
  - .planning/phases/181-json-ui-input-error-prop-inline-render/181-07-AUDIT.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "A grep audit confirms no gestiscilo consumer reads the plural `errors` field off props from a manually constructed spec (D-08 clean break)"
    - "A representative sample of ~5 gestiscilo forms (one per controller area — cassa/products, calendario/bookings, settings, staff, documenti) demonstrates inline error <p> rendering after a forced ValidationError redirect-back"
    - "The has_validation_errors / toast_validation cross-field symptom (D-05) is verified: after Phase 181 ferro fixes are vendored into gestiscilo, the cross-field summary either works correctly OR the divergence is traced to a consumer-side helper (not a ferro bug per RESEARCH §Suspect 3)"
    - "The full pre-commit gate (cargo fmt + cargo clippy + cargo test --all-features) passes on the ferro workspace"
  artifacts:
    - path: .planning/phases/181-json-ui-input-error-prop-inline-render/181-07-AUDIT.md
      provides: "cross-repo audit results + manual UAT evidence + full-suite green confirmation"
      contains: "## D-08 Audit Results"
  key_links:
    - from: "ferro-json-ui (post-Plans 02-06)"
      to: "gestiscilo-it consumer (~30 .prop(\"error\", ...) sites)"
      via: "vendored path dependency or local cargo patch"
      pattern: "ferro = .* path"
---

<objective>
Wave 3 — Cross-repo audit (D-08) + full-suite gate. Confirm no gestiscilo consumer was depending on the pre-fix `errors` plural shape (per RESEARCH §Risk Surface §Fix B: probability is near-zero since the feature never worked end-to-end, but the audit is still mandatory per CONTEXT D-08). Verify the D-05 cross-field symptom resolution by sampling ~5 representative gestiscilo forms after Phase 181 fixes are vendored. Run the canonical ferro pre-commit gate once at this phase boundary (NOT after every Wave 2 plan — per `feedback_one_cpu_op_at_a_time.md`).

Purpose: Close D-08 (clean break + cross-repo verification) and D-05 (cross-field validation summary). Capture results in 181-07-AUDIT.md for traceability.

This plan is `autonomous: false` because the manual UAT step requires the user to open gestiscilo forms in a browser and confirm inline error rendering. The audit grep + ferro test gate are fully automated.

Output: 181-07-AUDIT.md capturing grep results, manual UAT screenshots/notes, and the canonical pre-commit gate green confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/181-json-ui-input-error-prop-inline-render/181-CONTEXT.md
@.planning/phases/181-json-ui-input-error-prop-inline-render/181-RESEARCH.md
@.planning/phases/181-json-ui-input-error-prop-inline-render/181-VALIDATION.md
@.planning/phases/181-json-ui-input-error-prop-inline-render/181-06-SUMMARY.md

<interfaces>
<!-- Audit greps and the full pre-commit gate. -->

D-08 audit commands (from RESEARCH §Risk Surface §Fix B and CONTEXT D-08):
```bash
# Search gestiscilo for any consumer reading the plural `errors` field from props.
rg '"errors"' ../gestiscilo-it/app/src/
rg '\.errors' ../gestiscilo-it/app/src/
```

Manual UAT representative sample (~30 sites total per CONTEXT spec):
```bash
rg '\.prop\("error"' ../gestiscilo-it/app/src/ | head -30
```

Per CLAUDE.md and memory `feedback_ci_clippy_command_match.md`, the canonical pre-commit gate is:
```bash
cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features
```
This is the ONLY full-suite invocation in the phase (per `feedback_one_cpu_op_at_a_time.md`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-08 grep audit + write 181-07-AUDIT.md</name>
  <files>.planning/phases/181-json-ui-input-error-prop-inline-render/181-07-AUDIT.md</files>
  <read_first>
    - .planning/phases/181-json-ui-input-error-prop-inline-render/181-CONTEXT.md (D-08 — clean break requirement)
    - .planning/phases/181-json-ui-input-error-prop-inline-render/181-RESEARCH.md (§Risk Surface §Fix B for audit rationale)
    - .planning/phases/181-json-ui-input-error-prop-inline-render/181-VALIDATION.md (lines 56-58 for the cross-repo audit expectations)
  </read_first>
  <action>
    Locate the sibling gestiscilo-it repository. By convention it lives at `../gestiscilo-it/` relative to the ferro repo root. If the path does not exist, document that in 181-07-AUDIT.md as "gestiscilo-it not present in workspace — audit deferred to consumer-side phase 176". Otherwise:

    **Step 1: Grep audit for plural `errors` reads.** Run from the ferro repo root:
    ```bash
    rg '"errors"' ../gestiscilo-it/app/src/ 2>&1 | tee /tmp/181-audit-errors-plural.txt
    rg '\.errors' ../gestiscilo-it/app/src/ 2>&1 | tee /tmp/181-audit-errors-dot.txt
    ```

    Inspect every hit. Classify each into one of three buckets:
    - **A. Non-issue:** the hit is reading the JSON-UI catalog `errors` field of a different system (e.g. `ValidationError::errors` on the ferro framework type, which is the legitimate Rust API and was never touched by this phase), or it's a comment / string literal unrelated to props.
    - **B. Pre-fix consumer of broken shape:** the hit is reading `props.errors` (plural) off a manually constructed spec the consumer built. THESE MUST BE FIXED IN GESTISCILO (per `feedback_audit_report_fix_discrepancies.md` — never silently work around). Log them with file:line + rationale.
    - **C. Migration candidate:** the hit is a consumer using the manual `$data` escape hatch that COULD switch to `JsonUi::render_validation_error` per CONTEXT D-04. Mark as a follow-up suggestion, not a blocker.

    Per RESEARCH expectation (§Risk Surface §Fix B): "Likely none, since the feature has never worked end-to-end." If bucket B is empty, D-08 is closed without gestiscilo changes. If bucket B has hits, surface them to the user in the SUMMARY — Phase 181's release loop must include the gestiscilo fix.

    **Step 2: Count the `.prop("error", ...)` sites:**
    ```bash
    rg '\.prop\("error"' ../gestiscilo-it/app/src/ 2>&1 | wc -l
    rg '\.prop\("error"' ../gestiscilo-it/app/src/ 2>&1 | tee /tmp/181-audit-error-bindings.txt
    ```
    Confirm the count matches the CONTEXT discovery note's ~30 sites (deviation > 50% in either direction warrants a re-read of the discovery context to confirm the consumer didn't refactor in the interim).

    **Step 3: Write 181-07-AUDIT.md** capturing:
    - Bucket A / B / C classification with file:line citations.
    - The `.prop("error", ...)` count.
    - Resolution status for D-08 (closed cleanly / requires gestiscilo PR / requires user decision).
    - A pointer to the representative-sample manual UAT (next task).

    Format:
    ```markdown
    # Phase 181 — D-08 Cross-Repo Audit

    ## Date
    [YYYY-MM-DD]

    ## Audit Commands Run
    - `rg '"errors"' ../gestiscilo-it/app/src/` — N hits
    - `rg '\.errors' ../gestiscilo-it/app/src/` — N hits
    - `rg '\.prop\("error"' ../gestiscilo-it/app/src/` — N hits

    ## Bucket A: Non-issue (other system / unrelated)
    [list with file:line + 1-line rationale]

    ## Bucket B: Pre-fix consumer of broken shape (BLOCKER if non-empty)
    [list with file:line + 1-line description of the broken read; empty if D-08 clean]

    ## Bucket C: Migration candidates (escape hatch → blessed path)
    [list of handlers that could switch to JsonUi::render_validation_error per D-04]

    ## Verdict
    - D-08 status: [closed cleanly | requires gestiscilo PR]
    - Action for the release loop: [none | open gestiscilo Phase X to migrate]
    ```

    Per `feedback_friction_loop_release_cadence.md`: if bucket B is non-empty, surface to the user but DO NOT publish a mid-loop ferro release. Phase 181's release happens ONCE at the end of the friction loop, after gestiscilo has consumed the fix.
  </action>
  <verify>
    <automated>ls -la .planning/phases/181-json-ui-input-error-prop-inline-render/181-07-AUDIT.md && grep -c '## Bucket' .planning/phases/181-json-ui-input-error-prop-inline-render/181-07-AUDIT.md</automated>
    EXPECTED: file exists; grep returns ≥ 3 (one heading per bucket).
  </verify>
  <acceptance_criteria>
    - `.planning/phases/181-json-ui-input-error-prop-inline-render/181-07-AUDIT.md` exists.
    - The file contains three `## Bucket A`, `## Bucket B`, `## Bucket C` sections.
    - The file documents a clear verdict (D-08 closed cleanly OR a gestiscilo PR is required).
    - If `../gestiscilo-it/app/src/` does not exist, the file documents that explicitly and the verdict is "audit deferred".
  </acceptance_criteria>
  <done>D-08 audit complete; results captured in 181-07-AUDIT.md; verdict documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT — representative sample of gestiscilo forms (5 controllers)</name>
  <files>.planning/phases/181-json-ui-input-error-prop-inline-render/181-07-AUDIT.md</files>
  <read_first>
    - .planning/phases/181-json-ui-input-error-prop-inline-render/181-CONTEXT.md (D-05 cross-field symptom; CONTEXT D-04 both paths)
    - .planning/phases/181-json-ui-input-error-prop-inline-render/181-VALIDATION.md (lines 76-82 for manual-only verifications)
    - .planning/phases/181-json-ui-input-error-prop-inline-render/181-RESEARCH.md (§Suspect 3 lines 207-239)
    - Memory note `project_v12_merge_task.md` (path-dep friction loop)
  </read_first>
  <action>
    Operator-driven manual UAT — agent supervises but cannot execute the browser-side verification. Procedure:

    1. Repoint gestiscilo's ferro dependency to the local ferro tree (`ferro = { path = "../ferro" }` in `../gestiscilo-it/Cargo.toml`) per `project_v12_merge_task.md` friction-loop convention.
    2. Start the gestiscilo dev server.
    3. Walk the 5 representative forms listed in `<how-to-verify>` below, in order.
    4. For each form, record PASS / FAIL with a 1-line note in `181-07-AUDIT.md` under a new `## Manual UAT — Representative Sample` heading.
    5. Take one screenshot (cassa/products is the recommended subject — it is the original discovery surface).
    6. Record the D-05 cross-field summary outcome: PASS at ferro layer if all 5 forms render the validation toast correctly; otherwise document the consumer-side handler that fails to add `toast_validation` to `root_children` (this is a consumer-side bug, not a ferro bug, per RESEARCH §Suspect 3).

    Per `feedback_macbook_thermal_pause.md`: if the gestiscilo build cycle stresses the system thermally, pause and report a clean resume point rather than chaining rebuilds.
  </action>
  <what-built>
    Phase 181 fixes (Plans 01-06) shipped:
    - Pipeline: JsonUi::render and JsonUi::render_validation_error now merge runtime data into spec.data clone before resolve (Fix A).
    - attach_errors writes singular `error: String` matching InputProps.error shape (Fix B).
    - All four form-control variants (Checkbox, CheckboxList, Switch, Input-file) emit destructive-tone ARIA + class chain when has_error.

    Together these unlock inline error rendering for the ~30 `.prop("error", ...)` sites in gestiscilo-it.
  </what-built>
  <how-to-verify>
    Operator must repoint gestiscilo's ferro dependency to the local ferro tree (e.g. `ferro = { path = "../ferro" }` in `../gestiscilo-it/Cargo.toml`) and start the gestiscilo dev server. Per memory `project_v12_merge_task.md`: gestiscilo consumes friction via path-dep during the friction loop.

    Then visit ONE representative form per area:

    1. **cassa/products (the discovery surface):** `/dashboard/cassa/prodotti/{id}/modifica`. Submit the form with `overage_threshold = 2` and `overage_price` empty (the discovery repro). Expected: an inline `<p>` below the "Soglia sovrapprezzo" input reading "Per il sovrapprezzo, compila sia la soglia che il prezzo".

    2. **calendario/bookings:** any booking new/edit form. Submit empty. Expected: inline error `<p>` below each required field.

    3. **settings:** staff or general settings form. Submit invalid. Expected: inline `<p>`.

    4. **staff:** staff member create/edit form. Submit with duplicate email. Expected: inline `<p>` below the email field.

    5. **documenti:** document upload form. Submit empty. Expected: inline `<p>` below the file input AND a destructive ring on the file input itself (Plan 06 verification).

    For EACH of the 5 forms, also confirm:
    - The top-of-page validation summary (toast) is rendered IF the form has a `toast_validation` branch tied to `req.has_validation_errors()`. Per RESEARCH §Suspect 3, both `has_validation_errors()` and `validation_error()` should now return consistently because they read the same key — the cross-field symptom (D-05) should auto-resolve at the ferro layer. If the toast still does NOT render despite per-field errors rendering, the consumer-side helper is the culprit (NOT a ferro bug); document the consumer-side handler that fails to add `toast_validation` to `root_children`.
    - aria-invalid="true" attribute is present in the rendered HTML for the offending input (inspect with browser devtools).
    - The form value pre-fills correctly via `req.old(...)` (D-09 flash round-trip pattern — should already work).

    Take a screenshot of one representative form (cassa/products is the recommended one — it's the discovery surface) and attach the path/URL in the SUMMARY.

    PASTE FINDINGS into 181-07-AUDIT.md under a new `## Manual UAT — Representative Sample` heading. Include:
    - For each of the 5 forms: PASS / FAIL + 1-line note.
    - For toast cross-field (D-05): PASS at ferro layer if all 5 forms render the toast correctly; otherwise document the consumer-side handler that needs fixing.
    - Screenshot path (if applicable).

    Per `feedback_macbook_thermal_pause.md` and `feedback_macbook_thermal_ferro_builds.md`: if the manual UAT requires multiple `cargo build` cycles on the gestiscilo side and you sense thermal stress, pause and report a clean resume point rather than chaining builds.
  </how-to-verify>
  <verify>
    <automated>grep -c '## Manual UAT' .planning/phases/181-json-ui-input-error-prop-inline-render/181-07-AUDIT.md</automated>
    EXPECTED: returns ≥ 1 after operator records UAT findings.
  </verify>
  <acceptance_criteria>
    - `181-07-AUDIT.md` contains a `## Manual UAT — Representative Sample` section.
    - All 5 forms documented PASS or FAIL with a 1-line note.
    - D-05 cross-field outcome documented (ferro PASS vs. consumer-side bug location).
  </acceptance_criteria>
  <done>Operator records UAT findings in 181-07-AUDIT.md and confirms resume-signal.</done>
  <resume-signal>Type "uat-passed" once all 5 forms render inline errors correctly; or describe failures with file:line citations. Update 181-07-AUDIT.md before resuming.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Full pre-commit gate (cargo fmt + clippy + test --all-features)</name>
  <files>.planning/phases/181-json-ui-input-error-prop-inline-render/181-07-AUDIT.md</files>
  <read_first>
    - CLAUDE.md (Testing & Linting section — `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features`)
    - Memory `feedback_ci_clippy_command_match.md` — match the CI command exactly.
    - Memory `feedback_one_cpu_op_at_a_time.md` — this is the SINGLE full-suite run in the phase.
  </read_first>
  <action>
    Run the canonical pre-commit gate exactly as CI runs it. Do NOT skip clippy. Do NOT use `--no-default-features`. Do NOT add `-- --test-threads=1` (CI does not pass that flag).

    Single command:
    ```bash
    cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features
    ```

    Per `feedback_one_cpu_op_at_a_time.md`: this is the ONLY full-suite invocation in the phase. Do NOT chain another `cargo test` after it. Reuse this evidence in 181-07-AUDIT.md as the phase gate proof.

    If `cargo fmt --check` fails: run `cargo fmt --all` then commit the formatting fixes in a separate commit before re-running the gate.

    If `cargo clippy ... -- -D warnings` fails: read the error, fix the root cause (do NOT add `#[allow(...)]` to suppress — per CLAUDE.md "delete old code completely" + "no migration code" philosophy). Re-run the gate. If a clippy warning was introduced by Plans 02-06 and is not material to correctness, fix it. If unsure, ask the user.

    If any test fails: do NOT bypass with `--ignore`. Read the failure, classify (ferro Phase 181 regression vs. pre-existing flakiness from another phase). For Phase 181 regressions: fix and re-run. For pre-existing flakiness: document in 181-07-AUDIT.md and surface to user.

    Capture the final `cargo test --all-features` summary output (passed/failed count) in 181-07-AUDIT.md under a new `## Phase Gate` heading.

    Per memory `feedback_macbook_thermal_ferro_builds.md`: this is a 3-5 minute cold cache run, 60-90s warm. If the system is thermally stressed, pause and resume rather than racing the gate.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features 2>&amp;1 | tail -50</automated>
    EXPECTED: exit 0. The test summary should show all tests passed; any new failures relative to master HEAD must be Phase 181 regressions and fixed before this plan closes.
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0.
    - `cargo clippy --all --all-targets -- -D warnings` exits 0.
    - `cargo test --all-features` exits 0.
    - 181-07-AUDIT.md contains the `## Phase Gate` section with passed/failed test count.
  </acceptance_criteria>
  <done>Full pre-commit gate green; result captured in 181-07-AUDIT.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan is verification-only (audit + manual UAT + pre-commit gate). No new runtime change, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-181-W3-A1 | (n/a) | audit + gate | accept | Verification-only; no runtime emission. |

No new attack surface — this plan validates the changes shipped in Plans 02-06 rather than introducing new code paths.
</threat_model>

<verification>
The full pre-commit gate IS the phase verification. After all three tasks complete:

```bash
# Re-confirm everything still green from this plan's terminal session
cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features 2>&1 | tail -20
ls -la .planning/phases/181-json-ui-input-error-prop-inline-render/181-07-AUDIT.md
grep -c '## ' .planning/phases/181-json-ui-input-error-prop-inline-render/181-07-AUDIT.md  # expect ≥ 4 sections
```
</verification>

<success_criteria>
- 181-07-AUDIT.md exists with D-08 audit results (buckets A/B/C), manual UAT findings, and phase-gate confirmation.
- D-08 verdict documented (clean break confirmed OR gestiscilo PR queued).
- D-05 cross-field symptom verified (ferro layer OK; consumer-side fix queued if needed).
- Full pre-commit gate (fmt + clippy + test --all-features) GREEN.
- No new clippy warnings introduced by Plans 02-06.
- Commit message: `docs(181): cross-repo D-08 audit + manual UAT evidence + phase gate green`
</success_criteria>

<output>
After completion, create `.planning/phases/181-json-ui-input-error-prop-inline-render/181-07-SUMMARY.md` that points to 181-07-AUDIT.md and confirms the phase gate. The audit file is the primary artifact.
</output>
