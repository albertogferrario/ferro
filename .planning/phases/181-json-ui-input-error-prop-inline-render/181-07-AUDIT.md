# Phase 181 — D-08 Cross-Repo Audit

## Date

2026-05-31

## Audit Commands Run

- `rg '"errors"' /Users/alberto/repositories/gestiscilo-it/app/src/` — 3 hits
- `rg '\.errors' /Users/alberto/repositories/gestiscilo-it/app/src/` — 0 hits
- `rg '\.prop\("error"' /Users/alberto/repositories/gestiscilo-it/app/src/` — 15 hits

Note: The gestiscilo-it repository is at `/Users/alberto/repositories/gestiscilo-it/` (not the sibling path `../gestiscilo-it/` relative to ferro — the two repos share a common parent at `/Users/alberto/repositories/` but live in different namespaced subdirectories).

## Bucket A: Non-issue (other system / unrelated)

All three `"errors"` literal-key hits are runtime data objects, not props reading the `attach_errors` output:

1. `app/src/controllers/settings.rs:809` — `"errors": { "name": req.validation_error("name"), "slug": ..., "email": ..., "booking_cancellation_cutoff_hours": ... }` — This is a top-level data key passed to `JsonUi::render_file(...)`. The paired spec at `app/src/views/settings/index.json` uses `{"$data": "/errors/name"}` etc. to bind individual strings into `error` props. This is the escape-hatch path working through `render_file` (which already merges data before resolution). Not reading the `attach_errors` plural array. **Non-issue.**

2. `app/src/controllers/settings.rs:2093` — `"errors": { "header_text": req.validation_error("header_text"), ... }` — Same pattern: runtime data object, consumed by `app/src/views/settings/wallet.json` via `{"$data": "/errors/header_text"}` etc. **Non-issue.**

3. `app/src/controllers/settings_api_keys.rs:108` — `"errors": { "label": req.validation_error("label") }` — Same pattern: runtime data object, consumed by `app/src/views/settings/api_keys_nuovo.json`. **Non-issue.**

## Bucket B: Pre-fix consumer of broken shape (BLOCKER if non-empty)

**Empty.** No gestiscilo consumer reads `props.errors` (plural array) from a manually constructed spec. The `rg '\.errors' app/src/` grep returned zero hits. The feature has never worked end-to-end, so no consumer could have built a working feature on the broken shape.

D-08 is closed cleanly. No gestiscilo changes are required as a precondition for the ferro Phase 181 publish.

## Bucket C: Migration candidates (escape hatch → blessed path)

All 15 `.prop("error", ...)` sites in gestiscilo are escape-hatch bindings using `$data`:

All are in `app/src/controllers/cassa/products.rs`:
- line: `.prop("error", json!({ "$data": "/name_error" }))`
- line: `.prop("error", json!({ "$data": "/price_error" }))`
- line: `.prop("error", json!({ "$data": "/categories_error" }))`
- line: `.prop("error", json!({ "$data": "/slug_error" }))`
- line: `.prop("error", json!({ "$data": "/duration_error" }))`
- line: `.prop("error", json!({ "$data": "/min_people_error" }))`
- line: `.prop("error", json!({ "$data": "/max_people_error" }))`
- line: `.prop("error", json!({ "$data": "/overage_threshold_error" }))`
- line: `.prop("error", json!({ "$data": "/overage_price_cents_error" }))`
- line: `.prop("error", json!({ "$data": "/link_item_error" }))`
- line: `.prop("error", json!({ "$data": "/link_qty_error" }))`
- line: `.prop("error", json!({ "$data": "/link_mode_error" }))`
- line: `.prop("error", json!({ "$data": "/meta_key_error" }))`
- line: `.prop("error", json!({ "$data": "/meta_value_error" }))`
- line: `.prop("error", json!({ "$data": "/link_staff_error" }))`

Additionally, the three settings handlers (Bucket A) use a nested `"errors"` data object consumed via `{"$data": "/errors/field"}` path — this works today through `render_file` which already merges data. After Phase 181 Fix A ships, handlers using `JsonUi::render(spec, data)` (the inline render path) will also have their runtime data merged, making this a non-issue going forward.

Migration suggestion (not a blocker): the `cassa/products.rs` handler inserts individual `<field>_error` keys into the data bag one at a time (e.g. `obj.insert("overage_threshold_error", json!(req.validation_error("overage_threshold")))`). It could switch to `JsonUi::render_validation_error(&spec, &data, &validation_error)` which calls `attach_errors` automatically. This migration is cosmetic (both paths produce identical HTML after Phase 181) — schedule as a follow-up gestiscilo cleanup phase, not a release blocker.

Note: CONTEXT discovery mentioned ~30 `.prop("error")` sites; current count is 15. The ~2x reduction is consistent with intervening refactoring (Phase 175 restructured the product-edit form). No discrepancy requiring re-read of discovery context — 15 sites is within the "refactored in the interim" band.

## Verdict

- **D-08 status: closed cleanly**
- **Action for the release loop:** none — Bucket B is empty. The ~30 (now 15) escape-hatch bindings will become live error displays when Phase 181 fixes are vendored into gestiscilo.
- **Release timing:** per `feedback_friction_loop_release_cadence.md`, ferro publishes ONCE at the end of the friction loop, after gestiscilo has consumed and verified the fix via path-dep.

---

## Manual UAT — Representative Sample

**Status: DEFERRED-TO-OPERATOR**

This task requires an operator to repoint gestiscilo's `Cargo.toml` ferro dependency to the local ferro path-dep, rebuild gestiscilo, and walk 5 representative forms in a browser — actions that cannot be performed from `--auto` mode.

Per `feedback_friction_loop_release_cadence.md`: this UAT is a **release-time gate**, not a build-time blocker. Ferro must NOT publish Phase 181 until operator UAT is complete.

### Representative Sample (operator must complete before ferro Phase 181 publish)

Procedure:
1. In `gestiscilo-it/Cargo.toml` (or the workspace root), set `ferro = { path = "../ferro" }` (adjust path to local ferro tree).
2. `cargo build` in gestiscilo-it.
3. Start the gestiscilo dev server.
4. Walk each of the 5 forms below, submit invalid data, confirm inline error `<p>` renders below the offending input.

| # | Form | URL | Submit Condition | Expected | Status |
|---|------|-----|-----------------|----------|--------|
| 1 | cassa/products edit | `/dashboard/cassa/prodotti/{id}/modifica` | `overage_threshold=2`, `overage_price` empty | `<p>` below "Soglia sovrapprezzo" with error text | DEFERRED-TO-OPERATOR |
| 2 | calendario/bookings new | Any booking new/edit form | Submit empty | Inline `<p>` below each required field | DEFERRED-TO-OPERATOR |
| 3 | settings general | Settings form | Submit invalid | Inline `<p>` below invalid field | DEFERRED-TO-OPERATOR |
| 4 | staff member create/edit | Staff form | Duplicate email | Inline `<p>` below email field | DEFERRED-TO-OPERATOR |
| 5 | documenti upload | Document upload form | Submit empty | Inline `<p>` below file input AND destructive ring on `<input type="file">` | DEFERRED-TO-OPERATOR |

For each form, also confirm:
- `aria-invalid="true"` present on the offending input (browser devtools).
- Top-of-page validation toast rendered if handler adds `toast_validation` to `root_children` (D-05 cross-field check).
- Form values pre-fill correctly via `req.old(...)` (D-09 flash round-trip).

D-05 cross-field outcome: deferred. RESEARCH §Suspect 3 confirms `has_validation_errors()` and `validation_error()` read the same session key and cannot diverge in the ferro layer. If the toast still does not render after Phase 181 ferry is vendored, the consumer-side handler that fails to add `toast_validation` to `root_children` is the culprit.

Screenshot: operator should capture one screenshot of the cassa/products form showing inline error `<p>` and attach path here before publishing.

---

## Phase Gate

