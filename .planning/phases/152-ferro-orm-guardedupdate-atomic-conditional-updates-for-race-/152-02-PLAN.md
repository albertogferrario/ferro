---
phase: 152
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Cargo.toml
  - .github/workflows/publish.yml
  - CLAUDE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Workspace `[workspace.members]` contains `\"ferro-orm\"` after the `\"ferro-wallet\"` entry (phase-introduction order preserved, not alphabetical)"
    - "`.github/workflows/publish.yml` Wave 1a string (`WAVE1A_CRATES` env var, line ~201) contains `ferro-orm` appended after `ferro-wallet`"
    - "CLAUDE.md Workspace Structure table contains a new row for `ferro-orm` describing its purpose"
    - "`cargo build --workspace` exits 0 with ferro-orm registered (the stub from plan 01 compiles as a workspace member)"
    - "`cargo metadata --no-deps` lists `ferro-orm` in the workspace member list"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace members list with ferro-orm registered"
      contains: "\"ferro-orm\""
    - path: ".github/workflows/publish.yml"
      provides: "Wave 1a publish slot reserved for ferro-orm"
      contains: "ferro-orm"
    - path: "CLAUDE.md"
      provides: "Workspace Structure table row for ferro-orm"
      contains: "ferro-orm"
  key_links:
    - from: "Cargo.toml [workspace.members]"
      to: "ferro-orm/Cargo.toml"
      via: "workspace member registration"
      pattern: "\"ferro-orm\""
    - from: ".github/workflows/publish.yml WAVE1A_CRATES"
      to: "crates.io ferro-orm publish slot (auto-publish for subsequent versions; first publish bootstrapped in plan 06)"
      via: "publish.yml `for crate in $WAVE1A_CRATES` loop"
      pattern: "WAVE1A_CRATES=.*ferro-orm"
---

<objective>
Register the new `ferro-orm` crate in the workspace's three discovery surfaces: the root `Cargo.toml` member list, the `.github/workflows/publish.yml` Wave 1a publish loop, and the `CLAUDE.md` Workspace Structure table. After this plan, `cargo build --workspace` includes ferro-orm and CI's publish workflow will attempt to publish it (the first attempt will fail because the crate doesn't exist on crates.io yet — that bootstrap happens in plan 06).

Purpose: registration is independent of the builder body (plan 03), the integration test (plan 04), and the docs (plan 05). Running it in parallel with plan 03 cuts the critical-path length.

Output: three modified files; `cargo build --workspace` includes `ferro-orm` as a member; `.github/workflows/publish.yml` Wave 1a string contains the new crate.
</objective>

<context>
@.planning/phases/152-ferro-orm-guardedupdate-atomic-conditional-updates-for-race-/152-CONTEXT.md
@.planning/phases/152-ferro-orm-guardedupdate-atomic-conditional-updates-for-race-/152-PATTERNS.md
@.planning/phases/152-ferro-orm-guardedupdate-atomic-conditional-updates-for-race-/152-RESEARCH.md
@CLAUDE.md
@Cargo.toml
@.github/workflows/publish.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ferro-orm to root Cargo.toml workspace members</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml (workspace root — observe `[workspace] members` array, the existing `"ferro-wallet"` is the most recent append at line 24)
    - .planning/phases/152-ferro-orm-guardedupdate-atomic-conditional-updates-for-race-/152-PATTERNS.md §"Edit 1: `Cargo.toml` workspace `[workspace] members` append" (exact insertion point)
    - ferro-orm/Cargo.toml (verify the crate scaffold from plan 01 exists)
  </read_first>
  <action>
    Edit `/Users/alberto/repositories/albertogferrario/ferro/Cargo.toml`.

    The current `[workspace.members]` array ends with `"ferro-wallet",` on line 24 (phase-introduction order, NOT alphabetical):

    ```toml
    members = [
        "framework",
        "app",
        "ferro-cli",
        ...
        "ferro-whatsapp",
        "ferro-wallet",
    ]
    ```

    Append `"ferro-orm",` on a new line immediately after `"ferro-wallet",` and before the closing `]`. Preserve the existing four-space indentation and trailing comma:

    ```toml
    members = [
        ...
        "ferro-whatsapp",
        "ferro-wallet",
        "ferro-orm",
    ]
    ```

    Do NOT re-order the existing entries (the order is significant for human readers — it traces the crate-introduction history of the project per Phase 151 PATTERNS.md precedent).

    Do NOT touch `[workspace.package] version`. The current value `"0.2.30"` is correct (per RESEARCH Open Question 1 — CI's `check-version` job auto-publishes whatever Cargo.toml says when the phase merges; STATE.md's stale `0.2.24` reading is a separate cleanup concern, not this plan's job).
  </action>
  <verify>
    <automated>grep -q '"ferro-orm",' Cargo.toml &amp;&amp; awk '/"ferro-wallet",/{f=1; next} f &amp;&amp; /"ferro-orm",/{print "ok"; exit}' Cargo.toml | grep -q ok &amp;&amp; cargo build --workspace 2>&amp;1</automated>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml` contains `"ferro-orm",` on a line by itself (within `[workspace] members`)
    - `"ferro-orm",` appears immediately after `"ferro-wallet",` (preserves phase-introduction order)
    - `[workspace.package] version` is unchanged (still `"0.2.30"` or whatever CI auto-bumped it to since)
    - `cargo build --workspace` exits 0
  </acceptance_criteria>
  <done>The workspace members array now contains `"ferro-orm",` directly after `"ferro-wallet",`. `cargo build --workspace` compiles successfully including the new ferro-orm member.</done>
</task>

<task type="auto">
  <name>Task 2: Add ferro-orm to publish.yml Wave 1a string</name>
  <files>.github/workflows/publish.yml</files>
  <read_first>
    - .github/workflows/publish.yml lines 195-225 (the Wave 1a `WAVE1A_CRATES` env var, current line ~201)
    - .planning/phases/152-ferro-orm-guardedupdate-atomic-conditional-updates-for-race-/152-PATTERNS.md §"Edit 2: `.github/workflows/publish.yml` Wave 1a append"
    - .planning/phases/152-ferro-orm-guardedupdate-atomic-conditional-updates-for-race-/152-RESEARCH.md §"Pitfall 5" (first-publish bootstrap — first CI run for ferro-orm WILL fail; that is expected; the operator runs `cargo publish -p ferro-orm` locally in plan 06)
  </read_first>
  <action>
    Edit `/Users/alberto/repositories/albertogferrario/ferro/.github/workflows/publish.yml`.

    Locate the line:

    ```yaml
              WAVE1A_CRATES="ferro-macros ferro-events ferro-queue ferro-broadcast ferro-storage ferro-cache ferro-lang ferro-theme ferro-json-ui ferro-inertia ferro-api-mcp ferro-wallet"
    ```

    Append ` ferro-orm` (note the leading space) to the string, immediately after `ferro-wallet` and before the closing double-quote. Resulting line:

    ```yaml
              WAVE1A_CRATES="ferro-macros ferro-events ferro-queue ferro-broadcast ferro-storage ferro-cache ferro-lang ferro-theme ferro-json-ui ferro-inertia ferro-api-mcp ferro-wallet ferro-orm"
    ```

    Rationale: `ferro-orm` has zero internal `ferro-*` dependencies (CONTEXT D-04 — only depends on `sea-orm` and `thiserror`), so it belongs in Wave 1a (pure leaf crates), not Wave 1b (crates with internal deps). This mirrors how Phase 151 placed `ferro-wallet` in Wave 1a.

    The first CI run after this change WILL fail to publish ferro-orm because the crate name does not yet exist on crates.io (CI's token has `publish-update` scope only, not `publish-new`). The publish.yml loop's "already exists / already uploaded" grep branch will NOT match the failure mode — the CI step will exit non-zero. **This is expected exactly once.** Plan 06 includes the manual `cargo publish -p ferro-orm` bootstrap from a local terminal. Do NOT pre-empt that here; do NOT add a special-case skip for ferro-orm in publish.yml.

    Do not modify any other line of publish.yml.
  </action>
  <verify>
    <automated>grep -q 'WAVE1A_CRATES=.*ferro-wallet ferro-orm' .github/workflows/publish.yml &amp;&amp; ! grep -q 'WAVE1A_CRATES=.*ferro-orm.*ferro-wallet' .github/workflows/publish.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/publish.yml` contains the exact substring `ferro-wallet ferro-orm` inside the `WAVE1A_CRATES` string (verifies order: ferro-orm appended AFTER ferro-wallet)
    - The order ferro-orm-then-ferro-wallet does NOT appear (the inverse order would mean a bad edit)
    - No other line of publish.yml was modified (run `git diff .github/workflows/publish.yml` to confirm only one line changed)
    - No special-case skip / conditional logic was added for `ferro-orm`
  </acceptance_criteria>
  <done>The `WAVE1A_CRATES` environment variable in publish.yml now includes `ferro-orm` at the end. The first CI publish attempt will fail with "no upload permission" — that is the expected pre-bootstrap state; plan 06 resolves it via local `cargo publish`.</done>
</task>

<task type="auto">
  <name>Task 3: Add ferro-orm row to CLAUDE.md Workspace Structure table</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md lines 35-60 (the Workspace Structure table — last `ferro-*` row is `ferro-whatsapp`; the `app` row marks the end of the framework listing)
    - .planning/phases/152-ferro-orm-guardedupdate-atomic-conditional-updates-for-race-/152-PATTERNS.md §"Edit 4: `CLAUDE.md` Workspace Structure table row"
  </read_first>
  <action>
    Edit `/Users/alberto/repositories/albertogferrario/ferro/CLAUDE.md`.

    The Workspace Structure table currently ends with these two rows:

    ```markdown
    | `ferro-whatsapp` | WhatsApp Business Cloud API integration | `src/lib.rs` |
    | `app` | Sample application | Reference implementation |
    ```

    Insert a new row for `ferro-orm` **between** the `ferro-whatsapp` row and the `app` row. The new row's columns:
    - Crate: `` `ferro-orm` ``
    - Purpose: `Atomic conditional updates and ORM primitives (`GuardedUpdate`)`
    - Key Files: `` `src/lib.rs` ``

    Resulting block:

    ```markdown
    | `ferro-whatsapp` | WhatsApp Business Cloud API integration | `src/lib.rs` |
    | `ferro-orm` | Atomic conditional updates and ORM primitives (`GuardedUpdate`) | `src/lib.rs` |
    | `app` | Sample application | Reference implementation |
    ```

    The table is NOT alphabetical — it preserves phase-introduction order. The new row goes at the END of the ferro-* block, just before `app`.

    Per PATTERNS.md flag: the `ferro-wallet` row is currently missing from this table (an oversight from Phase 151). Do NOT add it here — keep this plan focused on the ferro-orm registration. The missing ferro-wallet row is tracked separately (see additional_context in the planning brief: "decide based on scope discipline (probably defer to a separate cleanup phase)").

    Do not modify any other section of CLAUDE.md.
  </action>
  <verify>
    <automated>grep -q '| `ferro-orm` | Atomic conditional updates and ORM primitives' CLAUDE.md &amp;&amp; awk '/\| `ferro-whatsapp` \|/{f=1; next} f &amp;&amp; /\| `ferro-orm` \|/{print "ok-1"; f=0; g=1; next} g &amp;&amp; /\| `app` \|/{print "ok-2"; exit}' CLAUDE.md | grep -cq ok | grep -q 2 || awk '/\| `ferro-whatsapp` \|/{f=1; next} f &amp;&amp; /\| `ferro-orm` \|/{print "ok"; exit}' CLAUDE.md | grep -q ok</automated>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` contains a table row starting with `` | `ferro-orm` | ``
    - The new row's Purpose column reads exactly `Atomic conditional updates and ORM primitives (`GuardedUpdate`)`
    - The new row appears AFTER the `ferro-whatsapp` row and BEFORE the `app` row (phase-introduction order)
    - The `ferro-wallet` table row is NOT added here (deferred per PATTERNS.md scope-discipline flag)
    - No other section of CLAUDE.md is modified
  </acceptance_criteria>
  <done>CLAUDE.md's Workspace Structure table now lists `ferro-orm` between `ferro-whatsapp` and `app`. Downstream agents reading CLAUDE.md will see the crate immediately.</done>
</task>

</tasks>

<threat_model>
This plan modifies workspace registration and CI publish config. The publish.yml change is the only security-sensitive moment.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → CI runner | publish.yml is executed by GitHub Actions with `CARGO_REGISTRY_TOKEN` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-02-01 | E | `WAVE1A_CRATES` order in publish.yml | mitigate | Appending to the END preserves the existing publish order; no upstream crate's publish gets reordered. The new crate publishes last in Wave 1a (no downstream dependencies in Wave 1a anyway since these are pure leaves). |
| T-152-02-02 | I | CI token scope vs first-publish | accept | CI's `CARGO_REGISTRY_TOKEN` is `publish-update`-scoped. The first publish of ferro-orm WILL fail with "no upload permission" / "not found". Plan 06 includes the manual bootstrap via local-terminal `cargo publish` with a personal `publish-new`-scoped token. This is the same pattern Phase 151 used (`ferro-wallet` Phase 151 PLAN-09); the operational reality is documented and accepted. |
| T-152-02-03 | T | Workspace member injection | mitigate | The only mutation is a literal `"ferro-orm",` string in `[workspace.members]`; no path traversal, no glob, no derived names. The ferro-orm crate directory is at the documented location and was created in plan 01. |
</threat_model>

<verification>
- `git diff Cargo.toml` shows exactly one inserted line: `    "ferro-orm",`
- `git diff .github/workflows/publish.yml` shows exactly one modified line (the `WAVE1A_CRATES` string)
- `git diff CLAUDE.md` shows exactly one inserted line (the ferro-orm table row)
- `cargo build --workspace` exits 0 (workspace member is registered and compiles)
- `cargo metadata --no-deps --format-version 1 | jq -r '.workspace_members[]' | grep -c ferro-orm` returns 1
- `cargo clippy --workspace --all-targets -- -D warnings` exits 0 (verifies the existing workspace plus ferro-orm)
</verification>

<success_criteria>
The new ferro-orm crate is registered in all three workspace discovery surfaces: Cargo.toml member list, publish.yml Wave 1a publish loop, and CLAUDE.md documentation table. `cargo build --workspace` compiles the full workspace including ferro-orm. The crate is ready for plan 03 (builder body), plan 04 (integration test), plan 05 (docs), and plan 06 (release).
</success_criteria>

<output>
After completion, create `.planning/phases/152-ferro-orm-guardedupdate-atomic-conditional-updates-for-race-/152-02-SUMMARY.md` documenting:
- The three exact edits (one line per file)
- Confirmation that `cargo build --workspace` exits 0
- Confirmation that no other content was modified in the three files (i.e., the diffs are minimal)
- A note that the ferro-wallet table row is still missing from CLAUDE.md and is deferred to a future cleanup phase
</output>
