---
phase: 238-inertia-first-load-html-shell
plan: 04
type: execute
wave: 3
depends_on: ["238-01", "238-02", "238-03"]
files_modified:
  - docs/src/features/inertia.md
autonomous: true
requirements: [D-10, D-11]
tags: [inertia, docs, same-origin, vite-proxy]

must_haves:
  truths:
    - "The Manual Configuration code example compiles against the real InertiaConfig API (no struct literal with missing fields) (D-11)"
    - "Docs document the same-origin convention as the primary first-load story (D-10/SC-5)"
    - "Docs include a Vite server.proxy recipe showing the session cookie flowing across the split-port dev setup (D-10/SC-5)"
    - "The bootstrap example (from_env() + App::set_inertia_config) matches the now-shipped API (D-11)"
  artifacts:
    - path: "docs/src/features/inertia.md"
      provides: "corrected config example + first-load shell / same-origin / Vite proxy sections"
      contains: "server.proxy"
  key_links:
    - from: "docs/src/features/inertia.md Manual Configuration"
      to: "ferro_inertia::InertiaConfig builder API"
      via: "builder-chain example matching shipped fields"
      pattern: "InertiaConfig::from_env"
---

<objective>
Close the docs gap (SC-5) and fix the existing doc drift (D-11). This is the final
wave — it documents the API shipped by Plans 01-03.

Three doc changes in `docs/src/features/inertia.md`:
1. Fix the stale struct-literal at the "Manual Configuration" section (:48-60) — it
   uses a struct literal missing `app_name`, `manifest_path`, and (now) the three new
   fields, so it does not compile. Replace with a builder chain matching the real API.
2. Verify the "Bootstrap Setup" block (:34-46) is now ACCURATE — it already shows
   `InertiaConfig::from_env()` + `App::set_inertia_config(config)`, which Plans 01/03
   make real. Confirm signatures match; adjust only if they diverge.
3. ADD a "First-Load HTML Shell" section documenting (a) the same-origin story
   (primary — backend serves GET / → full HTML + asset tags, session cookies work with
   any SameSite) and (b) a Vite `server.proxy` recipe for the split-port dev flow,
   explicitly showing the session cookie flowing. Mark the proxy recipe MEDIUM-confidence
   (validate against current Vite docs at execution time).

DEPENDS ON Plans 01-03: the documented API (`from_env()`, new builders,
`App::set_inertia_config`) must exist so the examples are correct. Wave 3.

NOTE: `docs/book/...` is GENERATED — edit only `docs/src/features/inertia.md`; do not
hand-edit generated output.

NOTE (CLAUDE.md): docs are public artifacts — neutral architectural voice, no internal
strategy/marketing language. No hardcoded tenant identity; use generic "My App" /
"http://localhost:8080" sample values only.

Output: updated `docs/src/features/inertia.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/238-inertia-first-load-html-shell/238-CONTEXT.md
@.planning/phases/238-inertia-first-load-html-shell/238-RESEARCH.md
@.planning/phases/238-inertia-first-load-html-shell/238-PATTERNS.md

<interfaces>
Shipped InertiaConfig builder API (after Plans 01-02), all in ferro_inertia / re-exported as ferro::InertiaConfig:
  InertiaConfig::from_env() -> Self
  InertiaConfig::new() -> Self            // == default()
  .vite_dev_server(url) .entry_point(e) .version(v) .app_name(n) .manifest_path(p)
  .html_template(t) .production() .development()
  .title(t)            // NEW — overrides app_name in <title>
  .head_extras(html)   // NEW — raw HTML before </head>, IGNORED if html_template set
  .mount_id(id)        // NEW — mount node id, default "app"
Shipped framework API (after Plan 03):
  ferro::App::set_inertia_config(config: InertiaConfig)   // set once at bootstrap

Vite proxy recipe (RESEARCH §"Vite server.proxy recipe", MEDIUM confidence, cite
https://github.com/vitejs/vite/blob/main/docs/config/server-options.md):
  server.proxy with target backend, changeOrigin: false to preserve Origin for CSRF;
  SameSite=Lax cookies flow across the proxy; SameSite=Strict requires the same-origin
  story (backend serves the shell directly).

Existing doc anchors (docs/src/features/inertia.md):
  :34 ### Bootstrap Setup     (already shows from_env + set_inertia_config — verify)
  :48 ### Manual Configuration (stale struct literal :53-59 — FIX)
  :373 ### Vite Configuration  (existing vite.config section — add proxy recipe near here or in the new section)
  :700 ## Development vs Production
  :739 ## JSON API Fallback
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the Manual Configuration example and verify Bootstrap Setup</name>
  <files>docs/src/features/inertia.md</files>
  <read_first>
    - docs/src/features/inertia.md (:14-62 — Configuration, Bootstrap Setup, Manual Configuration)
    - ferro-inertia/src/config.rs (shipped builder API after Plan 01)
    - .planning/phases/238-inertia-first-load-html-shell/238-PATTERNS.md (section "MODIFY docs/src/features/inertia.md")
    - .planning/phases/238-inertia-first-load-html-shell/238-RESEARCH.md (§"Docs Gap — Detailed Diff")
  </read_first>
  <action>
1. Replace the stale struct literal in the "### Manual Configuration" block
   (docs/src/features/inertia.md:48-60). The current code fence is a struct literal
   `InertiaConfig { vite_dev_server: ..., entry_point: ..., version: ..., development: ...,
   html_template: None }` which omits `app_name` and `manifest_path` (and the new fields)
   and therefore does not compile. Replace the fenced block body with a builder chain:
```rust
use ferro::InertiaConfig;

let config = InertiaConfig::from_env()
    .title("My App")
    .head_extras(r#"<link rel="icon" href="/favicon.ico">"#)
    .mount_id("app");
```
   Keep the fence language tag as the file uses for compiled examples (the file uses
   plain ```rust for these — check the surrounding fences and match; if other config
   examples use ```rust,ignore, match that). Add one sentence noting `head_extras` is
   ignored when `html_template` is set (D-06).

2. Verify the "### Bootstrap Setup" block (:34-46). It already reads:
```rust
use ferro::{App, InertiaConfig};
pub async fn register() {
    let config = InertiaConfig::from_env();
    App::set_inertia_config(config);
}
```
   Confirm this matches the shipped API: `InertiaConfig::from_env()` (Plan 01) and
   `App::set_inertia_config(config)` (Plan 03). If `App::set_inertia_config` is an
   associated fn (it is — no &self), the call `App::set_inertia_config(config)` is correct.
   Leave it unchanged if accurate; fix only if signatures diverge from Plan 01/03 SUMMARYs.
  </action>
  <verify>
    <automated>grep -q "InertiaConfig::from_env" docs/src/features/inertia.md && grep -q "App::set_inertia_config" docs/src/features/inertia.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - the Manual Configuration block no longer contains a bare `InertiaConfig {` struct literal: `grep -n "InertiaConfig {" docs/src/features/inertia.md` returns 0 matches (or only inside the custom-html_template doc-comment example, not the config one)
    - `grep -n ".title(" docs/src/features/inertia.md` returns a match
    - `grep -n ".mount_id(" docs/src/features/inertia.md` returns a match
    - `grep -n "App::set_inertia_config" docs/src/features/inertia.md` returns a match
    - `grep -n "head_extras" docs/src/features/inertia.md` returns a match with the html_template caveat nearby
  </acceptance_criteria>
  <done>Manual Configuration uses a compiling builder chain with the new fields; Bootstrap Setup verified accurate against the shipped API.</done>
</task>

<task type="auto">
  <name>Task 2: Add the First-Load HTML Shell section with same-origin + Vite proxy recipe</name>
  <files>docs/src/features/inertia.md</files>
  <read_first>
    - docs/src/features/inertia.md (:330-396 Frontend Setup + Vite Configuration; :700-740 Dev vs Production; :739 JSON API Fallback)
    - .planning/phases/238-inertia-first-load-html-shell/238-RESEARCH.md (§"Vite server.proxy recipe for docs" + §"Same-origin story" — the full vite.config.ts snippet and cookie notes)
    - .planning/phases/238-inertia-first-load-html-shell/238-CONTEXT.md (D-10)
  </read_first>
  <action>
Add a new top-level section `## First-Load HTML Shell` (place it after the
"## How Inertia Works" section near the top, or before "## Development vs Production" —
choose the location that reads best given the surrounding flow; the same-origin story is
conceptual and belongs early, the proxy recipe is operational and can sit near Vite
Configuration). The section MUST cover:

A. **The first-load document** — one paragraph: a non-`X-Inertia` GET to an Inertia
   handler returns a full HTML document containing
   `<div id="app" data-page="{…json…}">` plus the resolved Vite asset tags; the same
   handler returns the JSON contract when the request carries `X-Inertia`. No hand-rolled
   root HTML or per-app manifest parsing is needed — the app supplies only page props.

B. **Same-origin convention (primary)** — backend serves `GET /` → full HTML + assets;
   browser and backend share one origin (e.g. `http://localhost:8080`). Session cookies
   work with any `SameSite` value. State this is the recommended pattern.

C. **Vite `server.proxy` recipe (split-port dev)** — for teams that want Vite HMR on a
   separate port. Provide the `vite.config.ts` snippet (MEDIUM confidence — add an HTML
   comment `<!-- Verify against current Vite docs: https://vitejs.dev/config/server-options -->`
   so the executor/reader validates it):
```typescript
// vite.config.ts — split-port dev: Vite on :5173, Ferro backend on :8080
import { defineConfig } from 'vite'

export default defineConfig({
  server: {
    proxy: {
      // Forward API (and other backend) requests to Ferro.
      // changeOrigin: false keeps the Origin header so CSRF/session validation passes.
      '/api': { target: 'http://localhost:8080', changeOrigin: false },
      // If the backend also serves the HTML shell, proxy the root:
      '/': { target: 'http://localhost:8080', changeOrigin: false },
    },
  },
})
```
   Then explain the cookie flow: from the browser's perspective every request goes to the
   Vite origin (:5173); Vite forwards to :8080; the `Set-Cookie` from Ferro is accepted.
   `SameSite=Lax` (or `None`) cookies flow across the proxy; `SameSite=Strict` /
   `HttpOnly` strict setups require the same-origin story (B). Cite the Vite docs URL.

D. **head_extras caveat** — reiterate that `head_extras` is ignored when a custom
   `html_template` is set (D-06), and that `head_extras` is developer-controlled config
   and must NOT be populated from request data (security note — matches the threat model).

Use only generic sample values (My App, localhost ports) — no tenant identity.
  </action>
  <verify>
    <automated>grep -q "server.proxy\|server:" docs/src/features/inertia.md && grep -q "changeOrigin" docs/src/features/inertia.md && grep -qi "same-origin" docs/src/features/inertia.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "First-Load HTML Shell" docs/src/features/inertia.md` returns a match
    - `grep -n "changeOrigin" docs/src/features/inertia.md` returns a match
    - `grep -ni "same-origin" docs/src/features/inertia.md` returns a match
    - `grep -n "data-page" docs/src/features/inertia.md` returns a match in the new section
    - `grep -ni "samesite" docs/src/features/inertia.md` returns a match (cookie-flow explanation)
    - `grep -n "Verify against current Vite docs" docs/src/features/inertia.md` returns a match (MEDIUM-confidence marker)
    - no hardcoded tenant identity: `grep -in "gestiscilo\|example.com" docs/src/features/inertia.md` returns 0 in the new section
  </acceptance_criteria>
  <done>First-Load HTML Shell section covers the document, same-origin (primary), Vite proxy recipe with cookie-flow explanation, and the head_extras caveat; proxy recipe carries a validate-against-Vite-docs marker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → developer reading them | docs guide how a downstream app configures head_extras and the dev proxy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-238-06 | Information disclosure (guidance) | docs could imply head_extras accepts user input | mitigate | The First-Load section explicitly states head_extras is developer-controlled config and must NOT be populated from request data, preventing a downstream XSS sink. |
| T-238-07 | Spoofing / session theft (guidance) | proxy recipe could recommend an insecure cookie/Origin setup | mitigate | The recipe documents changeOrigin:false to preserve Origin for CSRF validation and explains SameSite implications, steering away from session-leaking configs; marked MEDIUM-confidence to force validation against current Vite docs. |
</threat_model>

<verification>
- `cargo doc --no-deps` clean (no broken intra-doc links introduced)
- mdBook build of docs/ succeeds (if the toolchain is available; otherwise grep checks suffice)
- All grep acceptance criteria pass
- Manual: a developer can follow the new section to stand up the same-origin first-load
  flow and the split-port proxy flow (covered by VALIDATION.md Manual-Only Verifications)
</verification>

<success_criteria>
- Manual Configuration example compiles against the shipped API (D-11)
- Bootstrap Setup verified accurate (from_env + set_inertia_config) (D-11)
- Same-origin convention documented as primary (D-10/SC-5)
- Vite server.proxy recipe with session-cookie-flow explanation present (D-10/SC-5)
- head_extras security + html_template caveat documented (D-06)
- Neutral public voice; no tenant identity
</success_criteria>

<output>
After completion, create `.planning/phases/238-inertia-first-load-html-shell/238-04-SUMMARY.md`.
Note whether the Vite proxy recipe was validated against current Vite docs at execution
time or left as MEDIUM-confidence with the validate marker.
</output>
