---
status: complete
phase: 238-inertia-first-load-html-shell
source: [238-01-SUMMARY.md, 238-02-SUMMARY.md, 238-03-SUMMARY.md, 238-04-SUMMARY.md]
started: 2026-06-21
updated: 2026-06-21
---

## Current Test

[testing complete]

## Tests

### 1. Cold-start smoke test
expected: The sample `app` (which has GET / → Inertia::render("Home", ...)) boots via `cargo run` without errors and the route responds.
result: pass
note: `cargo build -p app` clean (34s — confirms the framework changes don't break a real consumer build); server booted "Ferro server running on http://127.0.0.1:8090" with no errors.

### 2. First-load HTML document (SC-1 + SC-3 dev)
expected: A plain GET / (no X-Inertia header) returns a full HTML document with `<div id="app" data-page="...">`, `<title>`, and dev-mode Vite tags.
result: pass
note: Verified via live same-origin fetch in Chrome. content-type text/html; `<!DOCTYPE html>` present; `<div id="app" data-page="...">` present; `<title>` = "Ferro MCP Dogfood" (rendered from APP_NAME config); `/@vite/client` + `@react-refresh` tags emitted against `http://localhost:5173` (configured dev-server URL); data-page component = "Home", keys component/props/url/version.

### 3. Content negotiation (SC-2)
expected: The same URL with X-Inertia header returns the JSON page contract (not HTML).
result: pass
note: Live fetch with `X-Inertia: true` returned content-type application/json + `X-Inertia: true` response header; body is the JSON page object (component "Home", keys component/props/url/version); not an HTML document.

### 4. data-page equals JSON contract (SC-1)
expected: The data-page payload from the HTML document, HTML-unescaped, equals the JSON-path page object.
result: pass
note: HTML data-page (after 5-char unescape) === JSON-path body, byte-identical — same component/props/url/version including the csrf prop.

### 5. Browser first-load hydration (end-to-end SC-1)
expected: With the Vite dev server serving this app's frontend, opening the app root cold renders the React "Home" component hydrated from data-page, no console errors.
result: pass
note: "Ran `npm install` in app/frontend (installed vite ^5.0.0), started this app's Vite on :5174, restarted the Ferro app with VITE_DEV_SERVER=http://localhost:5174, and reloaded http://127.0.0.1:8090/ in Chrome. The first-load document loaded the module from the configured dev server (vite_client_src = http://localhost:5174/@vite/client), React mounted into #app (children present), and the Home component hydrated from data-page: <h1>Welcome to Ferro!</h1>, User Info + Stats sections rendered, stats.visits shown as '1,234' (props/toLocaleString from the embedded page object). Zero console errors/warnings. Screenshot: 238-first-load-hydrated.png. End-to-end killer-demo confirmed — a Ferro+Inertia app opens cold in a browser and hydrates from the backend-rendered shell with zero hand-rolled HTML."

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — all 5 tests pass. The first-load HTML shell works end-to-end: backend document + content negotiation + dev asset tags + data-page integrity (tests 1-4) and live browser React hydration (test 5).]
