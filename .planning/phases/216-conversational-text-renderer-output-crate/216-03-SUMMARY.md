---
phase: 216-conversational-text-renderer-output-crate
plan: "03"
subsystem: ferro-text, framework
tags: [facade-wiring, ci-registration, projections-feature, publish-wave]
dependency_graph:
  requires: [216-02]
  provides: [ferro::TextRenderer, ferro::RenderHint, ferro::Verbosity, WAVE1B-ferro-text]
  affects: [framework/Cargo.toml, framework/src/lib.rs, .github/workflows/publish.yml, docs/protocol/schemas/]
tech_stack:
  added: []
  patterns: [optional-dep-feature-gate, facade-re-export-under-cfg-feature, wave1b-ci-registration]
key_files:
  created: []
  modified:
    - framework/Cargo.toml
    - framework/src/lib.rs
    - .github/workflows/publish.yml
    - ferro-text/src/lib.rs (fmt fix)
    - docs/protocol/schemas/field-def.json (render_hint in schema, and 18 other schema files)
decisions:
  - "ferro-text placed in WAVE1B after ferro-projections (required dep ordering — cannot be Wave 1a)"
  - "RenderHint and Verbosity added to ferro_projections facade re-export block (both already at ferro-projections crate root from Plan 01)"
  - "Schema JSON churn (docs/protocol/schemas/) committed as legitimate output of render_hint addition to FieldDef (Phase 94 export test regenerates on test run)"
metrics:
  duration: "1097s"
  completed: "2026-06-13"
  tasks_completed: 2
  files_modified: 23
requirements: [CHAN-04]
---

# Phase 216 Plan 03: Facade wiring + CI registration + full gate Summary

`ferro::TextRenderer` reachable behind the `projections` feature; `ferro-text` registered in `publish.yml` Wave 1b after `ferro-projections`; full workspace gate (fmt + clippy -Dwarnings + test --all-features + doc -Dwarnings) green.

## What Was Built

Wired `ferro-text` into the `ferro` facade and the publish pipeline:

**framework/Cargo.toml:**
- Added `ferro-text = { path = "../ferro-text", version = "0.2", optional = true }` as optional dep
- Extended `projections` feature to include `"dep:ferro-text"`

**framework/src/lib.rs:**
- Added `RenderHint` and `Verbosity` to the `ferro_projections` re-export block (both already available at ferro-projections crate root from Plan 01)
- Added `pub use ferro_text::TextRenderer` under `#[cfg(feature = "projections")]` immediately after the `ferro_json_ui` block

**.github/workflows/publish.yml:**
- Added `ferro-text` to `WAVE1B_CRATES` immediately after `ferro-projections`
- Added dependency comment `# ferro-text -> ferro-projections`

**Gate results:**
- `cargo fmt --all -- --check`: clean (after auto-fixing fmt drift in ferro-text/src/lib.rs from Plan 02)
- `cargo clippy --all --all-targets -- -D warnings`: clean
- `cargo test --all-features`: 21 test suites, all passed
- `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps`: clean

## Tasks Completed

| # | Name | Commit | Key Files |
|---|------|--------|-----------|
| 1 | Register ferro-text in workspace members, framework dep+feature, and facade re-exports | b58552d3 | framework/Cargo.toml, framework/src/lib.rs |
| 2 | Register ferro-text in publish.yml Wave 1b + run the full phase gate | 703bba89 | .github/workflows/publish.yml, ferro-text/src/lib.rs, docs/protocol/schemas/ |

## Verification

- `grep -n "ferro-text" Cargo.toml` — matches line 35 (workspace member, added Plan 02)
- `grep -n "dep:ferro-text" framework/Cargo.toml` — matches line 18 (projections feature)
- `grep -n "ferro-text = { path" framework/Cargo.toml` — matches line 45 (optional dep)
- `grep -n "ferro_text::TextRenderer" framework/src/lib.rs` — matches line 268 (SC-4)
- `grep -n "RenderHint" framework/src/lib.rs` — matches line 260 (projections re-export)
- `grep "ferro-projections ferro-text" .github/workflows/publish.yml` — matches line 247
- `cargo check -p ferro-rs --features projections` — exits 0
- `cargo check -p ferro-rs` (default, no projections) — exits 0 (feature gate works)
- SC-1: `grep -rn "impl Renderer" ferro-projections/src --include="*.rs" | grep -v "sketch|template"` — empty
- Full gate: all green (see above)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] rustfmt drift in ferro-text/src/lib.rs from Plan 02**
- **Found during:** Task 2 (cargo fmt --all -- --check)
- **Issue:** Six fmt violations in `ferro-text/src/lib.rs` (import line too long, function signature wrapping, iterator chain wrapping, assert_eq! multi-line) — formatting not applied before Plan 02 commit
- **Fix:** Ran `cargo fmt -p ferro-text`; all six violations resolved
- **Files modified:** ferro-text/src/lib.rs
- **Commit:** 703bba89

**2. [Rule 3 - Blocking] Schema JSON files regenerated by Phase 94 export test**
- **Found during:** Task 2 (post-test git status)
- **Issue:** `cargo test --all-features` regenerated 19 `docs/protocol/schemas/*.json` files reflecting `render_hint` addition to `FieldDef` (field-def.json gained `render_hint` + `RenderHint` $defs across all schema files)
- **Fix:** Committed the schema churn as legitimate output — these correctly document the Phase 216 schema extension. Not reverted (unlike pure churn that would be unrelated to the phase).
- **Files modified:** docs/protocol/schemas/ (19 files)
- **Commit:** 703bba89

## Known Stubs

None.

## Threat Flags

None — this plan is pure build/CI wiring. T-216-06 (wave ordering) and T-216-07 (feature gate) both verified by acceptance criteria.

## Self-Check: PASSED

- `framework/Cargo.toml` — FOUND (ferro-text dep + projections feature updated)
- `framework/src/lib.rs` — FOUND (ferro_text::TextRenderer at line 268, RenderHint at line 260)
- `.github/workflows/publish.yml` — FOUND (ferro-projections ferro-text ordering at line 247)
- Commit b58552d3 — FOUND
- Commit 703bba89 — FOUND
- `cargo check -p ferro-rs --features projections` — exits 0
- `cargo check -p ferro-rs` — exits 0
- Full gate: fmt clean, clippy clean, 21 test suites passed, doc clean
