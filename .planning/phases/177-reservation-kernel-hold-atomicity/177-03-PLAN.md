---
phase: 177
plan: 03
type: execute
wave: 3
depends_on:
  - 177-01
  - 177-02
files_modified:
  - docs/src/database/reservations.md
autonomous: true
requirements:
  - SC-6
must_haves:
  truths:
    - "`docs/src/database/reservations.md` no longer recommends a `tokio::Mutex` per resource key as the SQLite concurrency pattern. (SC-6)"
    - "`docs/src/database/reservations.md` Consistency Model section accurately describes the transaction-based fix and the `40001` -> `Insufficient` translation. (SC-6)"
    - "`docs/src/database/reservations.md` `hold` sequence step description no longer claims the capacity check and INSERT are non-atomic. (SC-6)"
    - "No claim remains anywhere in `docs/src/database/reservations.md` that Postgres correctness is a roadmap item — it ships in Phase 177. (SC-6)"
  artifacts:
    - path: "docs/src/database/reservations.md"
      provides: "Updated `hold` sequence note and Consistency Model section that match the Phase 177 transaction-based atomicity fix."
      contains: "SERIALIZABLE"
  key_links:
    - from: "docs/src/database/reservations.md `hold` sequence step"
      to: "kernel's serializable transaction"
      via: "the note after step 7 references the SERIALIZABLE transaction, NOT a tokio::Mutex"
      pattern: "(serializable|SERIALIZABLE)"
    - from: "docs/src/database/reservations.md Consistency Model section"
      to: "Postgres 40001 -> Insufficient translation"
      via: "explicit text mentioning SQLSTATE 40001 and the Insufficient error contract"
      pattern: "40001"
---

<objective>
Sweep `docs/src/database/reservations.md` for stale concurrency claims and replace them with accurate descriptions of the Phase 177 transaction-based atomicity fix.

Purpose: Before Phase 177, the docs told consumers to wrap `hold` calls in a `tokio::Mutex` per resource key on SQLite, and flagged Postgres correctness as "roadmap as a follow-up". Both claims become factually wrong once Plans 01 and 02 ship — the kernel now arbitrates concurrent holds itself via a SERIALIZABLE transaction, on both backends. This plan corrects two specific passages in the docs without touching unrelated sections, so the public-facing documentation matches the shipped behavior.

Output:
- `docs/src/database/reservations.md` — `hold` sequence note (current lines 145-148) rewritten to describe the transaction-based fix; Consistency Model section body (current lines 370-382) rewritten to cover the unified transaction-based behavior across SQLite and Postgres including the `40001` -> `Insufficient` translation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/177-reservation-kernel-hold-atomicity/177-CONTEXT.md
@.planning/phases/177-reservation-kernel-hold-atomicity/177-RESEARCH.md
@.planning/phases/177-reservation-kernel-hold-atomicity/177-PATTERNS.md
@.planning/phases/177-reservation-kernel-hold-atomicity/177-01-SUMMARY.md
@.planning/phases/177-reservation-kernel-hold-atomicity/177-02-SUMMARY.md

@docs/src/database/reservations.md
@ferro-reservation/src/kernel.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the `hold` sequence note (lines 145-148) and the Consistency Model body (lines 370-382) in docs/src/database/reservations.md</name>
  <files>docs/src/database/reservations.md</files>
  <read_first>
    - docs/src/database/reservations.md (full file — confirm current line numbers of the two stale passages; the offsets may shift slightly if the file has been edited since RESEARCH.md was written, so locate by string match)
    - .planning/phases/177-reservation-kernel-hold-atomicity/177-RESEARCH.md (Q8 documentation sweep table; verbatim replacement text)
    - .planning/phases/177-reservation-kernel-hold-atomicity/177-PATTERNS.md (docs/src/database/reservations.md DOC UPDATE section, lines 488-548 — exact before/after text)
    - ferro-reservation/src/kernel.rs (post-Plan-01 — confirm the actual implementation matches what we document)
  </read_first>
  <action>
    Make two surgical replacements in `docs/src/database/reservations.md`. Do NOT touch any other section. Locate each passage by exact string match (line numbers may drift; the string contents are stable).

    **Replacement 1 — `hold` sequence note (currently after step 7, around lines 145-148).**

    Find the existing paragraph that begins with `Note: the capacity check and the INSERT are two separate statements`. The full existing text is:

    ```
    Note: the capacity check and the INSERT are two separate statements, not a
    single atomic SQL operation. Under SQLite's serial-writer semantics concurrent
    tasks should serialize `hold` calls at the application layer (e.g., a
    `tokio::Mutex` per resource key). See the Consistency Model section.
    ```

    Replace it with the following exact text (preserves the same line position in the doc structure):

    ```
    The capacity check, the INSERT, and the audit write all execute inside a
    single `SERIALIZABLE` transaction. Concurrent callers on the same
    `(key, window)` are serialized at the database level — no application-layer
    mutex is required. The conflict-losing task receives
    `ReservationError::Insufficient`. See the Consistency Model section.
    ```

    **Replacement 2 — Consistency Model section body (currently around lines 370-382).**

    Find the two paragraphs that begin with `**`hold` on SQLite:**` and `**`hold` on Postgres:**`. The full existing text spans both paragraphs:

    ```
    **`hold` on SQLite:** SQLite WAL mode serializes writers at the file level, but
    the three-step hold sequence (capacity SELECT + held SELECT + INSERT) is not a
    single statement. Under concurrent tokio tasks connecting to the same SQLite
    database, the capacity check and the INSERT can interleave. Consumers running
    concurrent holds against SQLite should serialize `hold` calls at the application
    layer — a `tokio::sync::Mutex` per resource key is the idiomatic pattern.

    **`hold` on Postgres:** Under Postgres `READ COMMITTED`, the capacity check has
    a theoretical race window between the SELECT and the INSERT. The current crate is
    SQLite-validated; Postgres correctness for the capacity check is on the roadmap
    as a follow-up addition (`SELECT FOR UPDATE` or a counter column approach).
    `commit`, `release`, and `extend` via `GuardedUpdate` are race-free on both
    dialects.
    ```

    Replace BOTH paragraphs (and the blank line between them) with the following exact text:

    ```
    **`hold`:** The capacity check, INSERT, and audit write execute inside a
    `SERIALIZABLE` transaction (`sea_orm::IsolationLevel::Serializable`). On SQLite
    the transaction aligns with the WAL single-writer model; on Postgres it prevents
    phantom reads between the SELECT and INSERT. If two concurrent tasks race on the
    same `(key, window)`, the database serializes them — exactly one succeeds and the
    other receives `ReservationError::Insufficient`. No application-layer mutex is
    needed.

    A conflict-losing task on Postgres may receive SQLSTATE `40001` (serialization
    failure); the kernel translates this to `ReservationError::Insufficient` before
    returning to the caller. The error contract is uniform across backends.

    `commit`, `release`, and `extend` via `GuardedUpdate` are race-free on both
    dialects (single `UPDATE … WHERE` statement).
    ```

    **Do NOT modify:**
    - The `## The Anti-Pattern` section (lines 10-30 area) — still factually correct as a pre-kernel description.
    - The state diagram and `ReservationContext` sections.
    - The `## TTL and the Sweeper` section.
    - The `## Errors` table — `ReservationError::Insufficient` variant description is still accurate.
    - The `## Operational Footguns` section — still accurate.
    - The "race-free state machine" claim at line 7 — this becomes truthful after the fix, no change required.
    - Any other paragraph not listed in the two replacements above.

    **Verification of edits:**
    - The exact post-edit file MUST contain zero occurrences of `tokio::Mutex` and `tokio::sync::Mutex`. Both phrases existed only in the two passages being replaced.
    - The post-edit file MUST contain at least one occurrence of `SERIALIZABLE` (uppercase, in the replacement text).
    - The post-edit file MUST contain at least one occurrence of `40001` (in the second replacement paragraph).
    - The post-edit file MUST contain the phrase `no application-layer mutex` (lowercase variant in replacement 2) AND `no application-layer mutex is required` (replacement 1).
    - The phrase `on the roadmap as a follow-up` and `SQLite-validated` MUST be absent — both were in the deleted Postgres paragraph.

    If the docs use mdbook, no rebuild is required for verification (the file is read directly). If a contributor wants to preview the changes locally, `mdbook serve docs` from the repo root is the convention — not part of this task's automated verify.
  </action>
  <verify>
    <automated>test "$(grep -c -E 'tokio::(sync::)?Mutex' docs/src/database/reservations.md)" -eq 0 &amp;&amp; test "$(grep -c 'SERIALIZABLE' docs/src/database/reservations.md)" -ge 1 &amp;&amp; test "$(grep -c '40001' docs/src/database/reservations.md)" -ge 1 &amp;&amp; test "$(grep -c 'on the roadmap as a follow-up' docs/src/database/reservations.md)" -eq 0 &amp;&amp; test "$(grep -c 'SQLite-validated' docs/src/database/reservations.md)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -E 'tokio::(sync::)?Mutex' docs/src/database/reservations.md` returns 0 (both `tokio::Mutex` and `tokio::sync::Mutex` references removed).
    - `grep -c 'SERIALIZABLE' docs/src/database/reservations.md` returns ≥1 (introduced by both replacement passages).
    - `grep -c '40001' docs/src/database/reservations.md` returns ≥1 (introduced by replacement 2).
    - `grep -c 'no application-layer mutex' docs/src/database/reservations.md` returns ≥2 (appears in both replacement 1 and replacement 2).
    - `grep -c 'on the roadmap as a follow-up' docs/src/database/reservations.md` returns 0 (stale Postgres "roadmap" claim removed).
    - `grep -c 'SQLite-validated' docs/src/database/reservations.md` returns 0 (stale "only validated against SQLite" claim removed).
    - `grep -c 'sea_orm::IsolationLevel::Serializable' docs/src/database/reservations.md` returns ≥1 (replacement 2 names the exact SeaORM enum).
    - `grep -c 'ReservationError::Insufficient' docs/src/database/reservations.md` returns ≥2 (appears in both replacements as the documented error contract).
    - The `## The Anti-Pattern` heading is still present: `grep -c '^## The Anti-Pattern' docs/src/database/reservations.md` returns exactly 1.
    - The `## Consistency Model` heading is still present: `grep -c '^## Consistency Model' docs/src/database/reservations.md` returns exactly 1.
    - The `## Operational Footguns` heading is still present: `grep -c '^## Operational Footguns' docs/src/database/reservations.md` returns exactly 1.
    - `git diff --stat docs/src/database/reservations.md` shows changes ONLY to this file (no other docs modified) and the line delta is small (replacement 1 swaps 4 lines for 5; replacement 2 swaps 13 lines for 12 — net delta is approximately ±2 lines, not a full-file rewrite).
  </acceptance_criteria>
  <done>
    The two stale passages in `docs/src/database/reservations.md` have been replaced with accurate descriptions of the Phase 177 transaction-based fix. Zero references to `tokio::Mutex` remain anywhere in the file. The new text names the `SERIALIZABLE` isolation level, the `sea_orm::IsolationLevel::Serializable` SeaORM enum, the `40001` SQLSTATE behavior, and the unified `ReservationError::Insufficient` error contract across both backends. All other sections of the file are byte-identical.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/src -> public documentation | This file ships as part of the framework's mdbook user guide. Inaccurate concurrency advice misleads consumers and can produce production bugs (e.g., a consumer wraps `hold` in a useless application-level mutex, or skips Postgres on the false assumption that it is unsafe). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-DOC-DRIFT | Repudiation / misinformation | `docs/src/database/reservations.md` Consistency Model section | mitigate | Replace stale `tokio::Mutex` recommendation and "Postgres is roadmap" claim with accurate text describing the SERIALIZABLE transaction and the `40001` -> `Insufficient` translation. Verified by grep-based acceptance criteria above (zero remaining stale phrases). |
| T-177-DOC-OVER-EDIT | Tampering (unintended scope creep) | The rest of `reservations.md` | mitigate | The action explicitly lists which sections MUST NOT be modified. `git diff --stat` is part of acceptance criteria to confirm the line delta is minimal. The two replacements are anchored by exact string matches, not blind line-number edits — so file drift between RESEARCH and execution does not produce wrong-section edits. |
</threat_model>

<verification>
After the task completes, run the documentation-correctness checks:

```bash
# Stale-phrase removal
grep -c -E 'tokio::(sync::)?Mutex' docs/src/database/reservations.md  # must return 0
grep -c 'on the roadmap as a follow-up' docs/src/database/reservations.md  # must return 0
grep -c 'SQLite-validated' docs/src/database/reservations.md  # must return 0

# New-content presence
grep -c 'SERIALIZABLE' docs/src/database/reservations.md  # must return >=1
grep -c '40001' docs/src/database/reservations.md  # must return >=1
grep -c 'sea_orm::IsolationLevel::Serializable' docs/src/database/reservations.md  # must return >=1
grep -c 'ReservationError::Insufficient' docs/src/database/reservations.md  # must return >=2

# Structural integrity
grep -c '^## Consistency Model' docs/src/database/reservations.md  # must return 1
grep -c '^## The Anti-Pattern' docs/src/database/reservations.md  # must return 1
grep -c '^## Operational Footguns' docs/src/database/reservations.md  # must return 1
```

All ten checks must satisfy their stated conditions.
</verification>

<success_criteria>
- All ten verification checks above satisfy their stated conditions.
- `git diff docs/src/database/reservations.md` shows a focused diff (two replacements only, no other section touched).
- The factual correctness check: the new text in `reservations.md` matches the actual code shipped in `ferro-reservation/src/kernel.rs` (Plan 01) — specifically, the `IsolationLevel::Serializable` + audit-inside-txn + `40001` -> `Insufficient` translation triad.
</success_criteria>

<output>
After completion, create `.planning/phases/177-reservation-kernel-hold-atomicity/177-03-SUMMARY.md` documenting:
- The exact line ranges in `reservations.md` that changed (use `git diff docs/src/database/reservations.md`).
- Confirmation that all ten verification grep checks satisfy their conditions.
- Whether any drift between RESEARCH.md line offsets and the actual current line offsets was encountered — if so, note how the string-anchor replacement strategy handled it.
</output>
