---
phase: 13-rebrand-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/13-rebrand-audit/AUDIT.md, .planning/phases/13-rebrand-audit/RENAME-MAPPING.md]
autonomous: true
---

<objective>
Generate comprehensive audit of all "cancer" occurrences and create rename mapping document.

Purpose: Establish complete inventory of what needs to change for the cancer → ferro rebrand, enabling subsequent phases to execute systematically.
Output: AUDIT.md (categorized inventory) and RENAME-MAPPING.md (transformation rules)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STRUCTURE.md
@Cargo.toml
@framework/Cargo.toml
@cancer-cli/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate comprehensive audit document</name>
  <files>.planning/phases/13-rebrand-audit/AUDIT.md</files>
  <action>
  Create AUDIT.md with categorized inventory of all "cancer" occurrences:

  **1. Crate Directories (9 total):**
  Run: `find . -type d -name "cancer*" | grep -v target | grep -v node_modules`
  Document each directory that needs renaming.

  **2. Cargo.toml Package Names:**
  Run: `grep -r "name = \"cancer" --include="Cargo.toml" .`
  Document each package name declaration.

  **3. Cargo.toml Dependencies:**
  Run: `grep -r "cancer" --include="Cargo.toml" . | grep -v "^#"`
  Document all dependency references (path dependencies, features, etc.)

  **4. Rust Code - Imports and Uses:**
  Run: `grep -r "use cancer" --include="*.rs" . | grep -v target`
  Run: `grep -r "extern crate cancer" --include="*.rs" . | grep -v target`
  Document import patterns.

  **5. Rust Code - Derive Macros:**
  Run: `grep -r "CancerModel" --include="*.rs" . | grep -v target`
  Document derive macro usage.

  **6. Documentation Files:**
  Run: `grep -r "cancer" --include="*.md" . | grep -v target | grep -v .planning`
  Categorize by: README files, docs/, inline code blocks, prose text.

  **7. CLI Binary Name:**
  Check: `cancer-cli/Cargo.toml` for `[[bin]]` section
  Document the binary name declaration.

  **8. MCP Server:**
  Run: `grep -r "cancer" cancer-mcp/src/ --include="*.rs"`
  Document tool names, descriptions, and references.

  **9. GitHub/Repository References:**
  Run: `grep -r "github.com/albertogferrario/cancer" .`
  Document all repository URL references.

  **10. Special Patterns:**
  Search for: `cancer-rs`, `cancer_`, `CANCER`, variations that might be missed.

  Structure the output with counts per category and example occurrences.
  </action>
  <verify>File exists and contains all 10 categories with counts</verify>
  <done>AUDIT.md created with comprehensive categorized inventory including exact counts and representative examples for each category</done>
</task>

<task type="auto">
  <name>Task 2: Create rename mapping document</name>
  <files>.planning/phases/13-rebrand-audit/RENAME-MAPPING.md</files>
  <action>
  Create RENAME-MAPPING.md with transformation rules for each category:

  **Structure:**
  ```markdown
  # Rename Mapping: cancer → ferro

  ## Quick Reference
  | Old | New | Notes |
  |-----|-----|-------|
  | cancer-* directories | ferro-* | All 9 crate directories |
  | cancer-rs | ferro | Main framework package |
  | CancerModel | FerroModel | Derive macro |
  | cancer (CLI binary) | ferro | CLI tool name |
  | etc. | | |

  ## Directory Renames
  [Each directory with exact old → new path]

  ## Cargo.toml Changes
  [Package names, dependency paths, metadata]

  ## Rust Code Changes
  [Imports, derives, trait references]

  ## Documentation Changes
  [Which files, what patterns]

  ## CLI Changes
  [Binary name, command references, help text]

  ## MCP Server Changes
  [Tool names, descriptions, server name]

  ## Repository/CI Changes
  [GitHub URLs, badges, CI config paths]

  ## Execution Order
  Recommended sequence for subsequent phases:
  1. Core framework (cancer-rs → ferro)
  2. Supporting crates (cancer-* → ferro-*)
  3. CLI (cancer-cli → ferro-cli, binary name)
  4. MCP server
  5. Documentation
  6. Sample app
  7. Templates
  8. Repository/CI
  9. Publishing
  ```

  Include notes on:
  - Which changes are mechanical (find/replace)
  - Which need careful handling (re-exports, public API)
  - Potential breaking changes for existing users
  - Order dependencies between rename steps
  </action>
  <verify>File exists and contains mapping table plus execution order</verify>
  <done>RENAME-MAPPING.md created with complete transformation rules, execution order, and special handling notes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] AUDIT.md exists with all 10 categories documented
- [ ] RENAME-MAPPING.md exists with transformation rules
- [ ] Both files are well-structured and actionable
- [ ] Execution order in RENAME-MAPPING aligns with ROADMAP phases 14-22
</verification>

<success_criteria>

- AUDIT.md contains comprehensive inventory of all "cancer" occurrences
- RENAME-MAPPING.md provides clear transformation rules
- Mapping document includes execution order matching roadmap structure
- Both documents are detailed enough to enable systematic execution in subsequent phases
</success_criteria>

<output>
After completion, create `.planning/phases/13-rebrand-audit/13-01-SUMMARY.md`
</output>
