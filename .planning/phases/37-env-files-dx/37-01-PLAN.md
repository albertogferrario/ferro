---
phase: 37-env-files-dx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [ferro-cli/src/templates/files/root/env.tpl, ferro-cli/src/templates/files/root/env.example.tpl]
autonomous: true
---

<objective>
Optimize .env templates for developer experience: minimal .env for quick start, complete .env.example as documentation.

Purpose: New projects should "just work" with minimal configuration while providing comprehensive documentation for advanced setup.
Output: Updated env.tpl (minimal) and env.example.tpl (complete with documentation).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@ferro-cli/src/templates/files/root/env.tpl
@ferro-cli/src/templates/files/root/env.example.tpl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Minimize env.tpl for quick start</name>
  <files>ferro-cli/src/templates/files/root/env.tpl</files>
  <action>
Reduce env.tpl to only essential config for immediate development:

```
APP_NAME="{project_name}"
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost:8080

SERVER_HOST=127.0.0.1
SERVER_PORT=8080

VITE_PORT=5173

# Database (SQLite works out of the box)
DATABASE_URL=sqlite://./database.db
```

Remove: DB pool settings, session config, Redis, cache, queue, storage, AWS, broadcasting, mail, AI keys.

Rationale: SQLite + memory cache + sync queue work with zero setup. Users can copy from .env.example when they need more.
  </action>
  <verify>File contains only ~12 lines. DATABASE_URL present. No REDIS, MAIL, AWS, or PUSHER keys.</verify>
  <done>env.tpl is minimal - only app basics + database URL</done>
</task>

<task type="auto">
  <name>Task 2: Enhance env.example.tpl as documentation</name>
  <files>ferro-cli/src/templates/files/root/env.example.tpl</files>
  <action>
Update env.example.tpl to serve as comprehensive documentation:

1. Keep ALL configuration options (current content is already comprehensive)
2. Add section headers with clear explanations
3. Add comments explaining when/why to change defaults
4. Group related settings with blank line separators

Structure:
```
# ==============================================================================
# APPLICATION
# ==============================================================================
# Core application settings

APP_NAME="Ferro Application"
APP_ENV=local          # local, staging, production
APP_DEBUG=true         # Set false in production
APP_URL=http://localhost:8080

# ==============================================================================
# SERVER
# ==============================================================================
# Development server binding

SERVER_HOST=127.0.0.1
SERVER_PORT=8080
VITE_PORT=5173

# ==============================================================================
# BUILD CLEANUP
# ==============================================================================
# Auto-remove build artifacts on `ferro serve` (requires cargo-sweep)

CARGO_SWEEP_DAYS=7     # Set 0 to disable

# ==============================================================================
# DATABASE
# ==============================================================================
# SQLite works out of the box. For PostgreSQL:
# DATABASE_URL=postgres://user:pass@localhost:5432/dbname

DATABASE_URL=sqlite://./database.db
DB_MAX_CONNECTIONS=10
DB_MIN_CONNECTIONS=1
DB_CONNECT_TIMEOUT=30
DB_LOGGING=false       # Set true to see SQL queries

# ==============================================================================
# SESSION
# ==============================================================================
# Session configuration for authentication

SESSION_LIFETIME=120   # Minutes
SESSION_COOKIE=ferro_session
SESSION_SECURE=false   # Set true when using HTTPS
SESSION_PATH=/
SESSION_SAME_SITE=Lax  # Strict, Lax, or None

# ==============================================================================
# REDIS
# ==============================================================================
# Required for: redis cache driver, redis queue driver, redis broadcasting

REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DATABASE=0

# ==============================================================================
# CACHE
# ==============================================================================
# Drivers: memory (default, no setup), file, redis

CACHE_DRIVER=memory
CACHE_PREFIX=ferro_cache_

# ==============================================================================
# QUEUE
# ==============================================================================
# Drivers: sync (default, inline), redis (background workers)

QUEUE_CONNECTION=sync
QUEUE_DEFAULT=default
QUEUE_RETRY_AFTER=90

# ==============================================================================
# STORAGE
# ==============================================================================
# Disks: local (default), s3

FILESYSTEM_DISK=local
STORAGE_PATH=storage/app

# ==============================================================================
# AWS S3
# ==============================================================================
# Required when FILESYSTEM_DISK=s3

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_ENDPOINT=          # For S3-compatible services (MinIO, DigitalOcean Spaces)

# ==============================================================================
# BROADCASTING
# ==============================================================================
# Drivers: log (default, for debugging), redis, pusher

BROADCAST_DRIVER=log
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_CLUSTER=mt1

# ==============================================================================
# MAIL
# ==============================================================================
# Drivers: smtp, log (for development)

MAIL_DRIVER=smtp
MAIL_HOST=localhost
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM_ADDRESS=hello@example.com
MAIL_FROM_NAME="Ferro App"

# ==============================================================================
# AI
# ==============================================================================
# For AI-powered features

ANTHROPIC_API_KEY=
```
  </action>
  <verify>File has section headers with `===`. Each section has explanatory comments. All config options present.</verify>
  <done>env.example.tpl is comprehensive documentation with clear sections and explanations</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] env.tpl is minimal (~12 lines, just app + db basics)
- [ ] env.example.tpl has clear section headers
- [ ] env.example.tpl has inline comments explaining options
- [ ] Template test passes: `cargo test -p ferro-cli test_env_not_empty`
- [ ] Template test passes: `cargo test -p ferro-cli test_env_example_not_empty`
</verification>

<success_criteria>

- env.tpl reduced to essential quick-start config
- env.example.tpl serves as comprehensive documentation
- All template tests pass
- No runtime configuration broken (defaults still work)
</success_criteria>

<output>
After completion, create `.planning/phases/37-env-files-dx/37-01-SUMMARY.md`
</output>
