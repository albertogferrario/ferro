---
phase: 166-structured-outputs-tool-calling-servicedef-aware-schema-normalizer
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - .github/workflows/publish.yml
autonomous: true
requirements: [AISDK-02, AISDK-03]
must_haves:
  truths:
    - "ferro-projections is published BEFORE ferro-ai in the WAVE1B loop (ferro-ai now depends on it)"
    - "cargo test --all-features passes; existing Classifier<T> tests are green (SC#7)"
    - "cargo fmt --check and cargo clippy --all --all-targets -D warnings are clean"
  artifacts:
    - path: ".github/workflows/publish.yml"
      provides: "WAVE1B order with ferro-projections before ferro-ai"
      contains: "ferro-projections ferro-ai"
  key_links:
    - from: ".github/workflows/publish.yml WAVE1B_CRATES"
      to: "ferro-projections before ferro-ai"
      via: "publish loop order"
      pattern: "ferro-projections ferro-ai"
---

<objective>
Close Phase 166: fix the publish-wave ordering so `ferro-projections` is published before `ferro-ai` (now that `ferro-ai` depends on it), and run the full phase gate proving SC#7 — `cargo test --all-features` green with existing `Classifier<T>` tests intact, plus a clean fmt + clippy pass.

Purpose: A new cross-crate dependency must respect publish waves (workspace convention from memory). Both crates sit in WAVE1B and are published in list order by a `for` loop; the current order publishes `ferro-ai` first, which would fail on crates.io because its `ferro-projections` dependency isn't published yet. This plan also is the phase-wide quality gate.
Output: publish.yml reordered; full suite + fmt + clippy green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/166-structured-outputs-tool-calling-servicedef-aware-schema-normalizer/166-CONTEXT.md
@.planning/phases/166-structured-outputs-tool-calling-servicedef-aware-schema-normalizer/166-PATTERNS.md
@.planning/phases/166-structured-outputs-tool-calling-servicedef-aware-schema-normalizer/166-VALIDATION.md

<interfaces>
<!-- Current WAVE1B loop (.github/workflows/publish.yml ~line 246) publishes crates in LIST ORDER: -->
WAVE1B_CRATES="ferro-ai ferro-projections ferro-stripe ferro-whatsapp ferro-notifications ferro-reservation ferro-projection ferro-deployments"
<!-- The for-loop iterates this string left-to-right; `ferro-ai` is published BEFORE `ferro-projections`.
     After Phase 166, ferro-ai depends on ferro-projections, so this order breaks first-publish. -->
<!-- The dependency comment block above the var (~lines 240-245) also needs the `ferro-ai -> ferro-projections`
     edge documented. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder WAVE1B so ferro-projections precedes ferro-ai</name>
  <files>.github/workflows/publish.yml</files>
  <read_first>
    - .github/workflows/publish.yml (the file being modified — WAVE1B_CRATES var ~line 246 and the dep-comment block ~lines 240-245)
    - 166-PATTERNS.md §"ferro-ai/Cargo.toml" Publish wave note (lines 572-573)
    - 166-CONTEXT.md §"Integration Points" (publish wave update is a workspace convention)
  </read_first>
  <action>
    In `.github/workflows/publish.yml`, edit the `WAVE1B_CRATES` string so `ferro-projections` appears BEFORE `ferro-ai`:
    ```
    WAVE1B_CRATES="ferro-projections ferro-ai ferro-stripe ferro-whatsapp ferro-notifications ferro-reservation ferro-projection ferro-deployments"
    ```
    Update the dependency comment block directly above the variable to add the new edge:
    ```
    # ferro-ai            -> ferro-events, ferro-projections   (Phase 166)
    ```
    Do NOT move either crate out of WAVE1B (both still belong there — ferro-projections depends only on ferro-theme which is WAVE1A; ferro-ai depends on ferro-events (WAVE1A) + ferro-projections (now earlier in WAVE1B)). Do NOT touch other waves.
  </action>
  <verify>
    <automated>grep -n "WAVE1B_CRATES=" .github/workflows/publish.yml | grep -q "ferro-projections ferro-ai" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/publish.yml` `WAVE1B_CRATES` lists `ferro-projections` immediately before `ferro-ai`
    - the dependency comment block documents `ferro-ai -> ... ferro-projections`
    - no other WAVE definitions changed: `grep -c "WAVE1A_CRATES=\|WAVE2_CRATES=\|WAVE3_CRATES=" .github/workflows/publish.yml` returns 3 (unchanged)
    - YAML still parses: `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/publish.yml'))"` exits 0
  </acceptance_criteria>
  <done>ferro-projections publishes before ferro-ai in WAVE1B; the new dependency edge is documented; no other waves touched.</done>
</task>

<task type="auto">
  <name>Task 2: Full phase gate — fmt + clippy + cargo test --all-features (SC#7)</name>
  <files>(no source files — verification gate only)</files>
  <read_first>
    - 166-VALIDATION.md §"Sampling Rate" (phase gate commands) and the Per-Task Verification Map (SC#7 row)
    - 166-ROADMAP §"Phase 166" Success Criterion 7 (existing Classifier<T> tests green)
    - CLAUDE.md §"Testing & Linting" (exact CI commands)
  </read_first>
  <action>
    Run the full phase gate (serialize CPU-heavy steps — one at a time per workspace convention; do NOT parallelize cargo runs):
    1. `cargo fmt --all -- --check` — if it fails, run `cargo fmt --all` and re-check.
    2. `cargo clippy --all --all-targets -- -D warnings` — fix any warnings surfaced in ferro-ai (new code); do not suppress with `#[allow]` unless justified in a comment.
    3. `cargo test --all-features` — full workspace suite. Must be green, INCLUDING the existing `Classifier<T>` tests (SC#7) and every Phase 166 test added in Plans 01-04 (schema_probe, schema::, projection_schema, complete, tool*).
    If any ferro-ai test added earlier is flaky or red, fix it here (this is the consolidation gate). Do NOT modify other crates' source to make the suite pass unless Phase 166's new dependency genuinely broke them (e.g. a ferro-projections re-export change) — if so, document the fix in the SUMMARY.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings 2>&1 | tail -3 && cargo test --all-features 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy --all --all-targets -- -D warnings` exits 0
    - `cargo test --all-features` exits 0
    - `cargo test -p ferro-ai classifier` exits 0 (SC#7 — existing Classifier<T> tests green)
    - all Phase 166 tests present and green: `cargo test -p ferro-ai 2>&1 | grep -E "schema_probe|servicedef_schema|complete_returns_typed_result|tool_registry_enforces_max_iterations"` shows passing tests
  </acceptance_criteria>
  <done>Full workspace suite, fmt, and clippy are green; existing Classifier<T> tests pass alongside all Phase 166 additions (SC#7).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI publish pipeline | crate publish order must satisfy the dependency DAG or first-publish fails |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-PUB-01 | DoS (broken release pipeline) | WAVE1B publish order | mitigate | Reorder so ferro-projections precedes ferro-ai; the dependency DAG is satisfied within WAVE1B. Verified by grep + YAML parse. |
| T-166-GATE-01 | Tampering (regression slips through) | full suite gate | mitigate | `cargo test --all-features` + clippy `-D warnings` is the phase-wide regression gate; SC#7 specifically asserts existing Classifier<T> tests stay green. |
</threat_model>

<verification>
- publish.yml: ferro-projections before ferro-ai in WAVE1B; YAML valid.
- `cargo fmt --all -- --check` clean.
- `cargo clippy --all --all-targets -- -D warnings` clean.
- `cargo test --all-features` green (SC#7).
</verification>

<success_criteria>
SC#7 satisfied: full suite green with existing Classifier<T> tests intact; publish wave order corrected for the new ferro-ai → ferro-projections dependency; the entire phase passes fmt + clippy + test.
</success_criteria>

<output>
After completion, create `.planning/phases/166-structured-outputs-tool-calling-servicedef-aware-schema-normalizer/166-05-SUMMARY.md`
</output>
