---
status: complete
phase: 166-structured-outputs-tool-calling-servicedef-aware-schema-normalizer
source: [166-01-SUMMARY.md, 166-02-SUMMARY.md, 166-03-SUMMARY.md, 166-04-SUMMARY.md, 166-05-SUMMARY.md]
started: 2026-06-08T11:12:58Z
updated: 2026-06-08T11:15:54Z
---

## Current Test

[testing complete]

## Tests

### 1. Typed Structured Output — complete::<T>()
expected: As a developer, `ferro_ai::complete::<T>(client, prompt)` returns a deserialized `T` from the LLM JSON; malformed output surfaces as `Error::Deserialization`, not a panic. (`complete_returns_typed_result`, `complete_propagates_deserialization_error` green)
result: pass

### 2. Generic Schema Normalizer — for_structured_output()
expected: `ferro_ai::for_structured_output(schema)` resolves `$ref`/`$defs` inline (cycle-guarded), strips Anthropic-rejected keywords, and adds `additionalProperties: false` to every object-with-properties — while always preserving `enum`. Running it twice is a no-op. (`schema_normalizer_*` tests green)
result: pass

### 3. ServiceDef Vocabulary Lock (killer feature)
expected: The normalized ServiceDef schema structurally locks the LLM to valid projection vocabulary. An invalid `FieldMeaning` ("totally_bogus") or `Intent` ("bogus") fails `jsonschema::draft202012` validation; all 18 FieldMeaning and 7 Intent variants pass; no `$ref` survives normalization. (`servicedef_schema_rejects_invalid_*`, `servicedef_schema_accepts_all_known_*`, `normalized_schema_has_no_surviving_refs` green)
result: pass

### 4. Tool Calling Layer — ToolRegistry + dispatch loop
expected: `ToolRegistry` requires `max_iterations` at construction (no Default, no zero-arg ctor), enforces a hard cap (`Error::ToolIterationLimit` at the cap, no override path), and surfaces tool failures to the model as model-legible `ToolError` strings — never raw panics/stack traces/DB strings. `complete_with_tools` is opt-in; existing `complete()` callers are unaffected. (`tool_*`, `dispatch_*` — green)
result: pass

### 5. Full Workspace Gate + Publish-wave Ordering (SC#7)
expected: `cargo fmt --all -- --check`, `cargo clippy --all --all-targets -- -D warnings`, and `cargo test --all-features` all pass clean. The existing `Classifier<T>` suite is intact. `publish.yml` WAVE1B lists `ferro-projections` before `ferro-ai` so the new dependency edge releases in DAG order.
result: pass

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]
