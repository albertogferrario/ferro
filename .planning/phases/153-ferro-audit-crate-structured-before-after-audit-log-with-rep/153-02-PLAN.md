---
phase: 153
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - .github/workflows/publish.yml
  - CLAUDE.md
  - README.md
autonomous: true
requirements: [D-04, D-38, D-39]
requirements_addressed: [D-04, D-38, D-39]
must_haves:
  truths:
    - "Workspace root Cargo.toml has 'ferro-audit' in [workspace.members] (alphabetically or appended after 'ferro-orm')"
    - "Workspace root Cargo.toml [workspace.package] version bumped from 0.2.30 to 0.2.31 (per RESEARCH F-07 / version-drift correction — NOT D-38's stale 0.2.25 → 0.2.26)"
    - ".github/workflows/publish.yml WAVE1A_CRATES string contains 'ferro-audit'"
    - "CLAUDE.md Workspace Structure table contains a row for 'ferro-audit' with purpose 'Append-only structured before/after audit log with replay'"
    - "Workspace root README.md crates table / 'What's included' list contains a 'ferro-audit' bullet"
    - "cargo metadata --no-deps succeeds (manifest still well-formed after edit)"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace manifest with ferro-audit registered + version bumped"
      contains: "ferro-audit"
    - path: ".github/workflows/publish.yml"
      provides: "Auto-publish Wave 1a slot for ferro-audit"
      contains: "ferro-audit"
    - path: "CLAUDE.md"
      provides: "Workspace Structure row for ferro-audit"
      contains: "ferro-audit"
    - path: "README.md"
      provides: "Workspace README crates list entry"
      contains: "ferro-audit"
  key_links:
    - from: "Cargo.toml [workspace.members]"
      to: "ferro-audit/Cargo.toml (created by plan 153-01)"
      via: "workspace.members entry"
      pattern: '"ferro-audit"'
    - from: ".github/workflows/publish.yml WAVE1A_CRATES"
      to: "crates.io ferro-audit auto-publish (post-bootstrap)"
      via: "Wave 1a publish loop"
      pattern: "ferro-audit"
---

<objective>
Register `ferro-audit` in the workspace and bump the workspace version. This plan modifies only workspace-root files — `Cargo.toml`, `.github/workflows/publish.yml`, `CLAUDE.md`, `README.md` — and has zero file overlap with plan 153-01 (which modifies only `ferro-audit/**`). The two plans run in parallel in Wave 1.

Purpose: make `ferro-audit` a recognized workspace member so `cargo build --workspace` picks it up; reserve its auto-publish slot in CI; surface the crate in the developer-facing project documents (`CLAUDE.md` Workspace Structure table; workspace `README.md` crates list).

**Critical version-drift correction (per RESEARCH F-07):** The workspace `[workspace.package] version` reads `"0.2.30"` post-Phase 152 (verified at gather time in CONTEXT). CONTEXT D-38 wrote `0.2.25 → 0.2.26` which was stale at gather time. The actual bump for Phase 153 is `0.2.30 → 0.2.31`. If `cargo metadata` shows a value different from `0.2.30` at execution time (because CI auto-bumped after Phase 152's publish), bump to one minor-patch above the current value. Read Cargo.toml at execution time, do not rely on D-38.

Output: workspace registration committed; `cargo metadata --no-deps` still succeeds; `cargo build --workspace` succeeds once plan 01's `ferro-audit/Cargo.toml` exists.
</objective>

<context>
@.planning/phases/153-ferro-audit-crate-structured-before-after-audit-log-with-rep/153-CONTEXT.md
@.planning/phases/153-ferro-audit-crate-structured-before-after-audit-log-with-rep/153-RESEARCH.md
@.planning/phases/153-ferro-audit-crate-structured-before-after-audit-log-with-rep/153-PATTERNS.md
@Cargo.toml
@.github/workflows/publish.yml
@CLAUDE.md
@README.md

<interfaces>
<!-- Current state of files this plan modifies. Excerpts extracted from codebase. -->

From Cargo.toml [workspace.members] (current):
```toml
members = [
    "framework",
    "app",
    "ferro-cli",
    "ferro-macros",
    "ferro-events",
    "ferro-queue",
    "ferro-notifications",
    "ferro-broadcast",
    "ferro-storage",
    "ferro-cache",
    "ferro-mcp",
    "ferro-inertia",
    "ferro-json-ui",
    "ferro-lang",
    "ferro-api-mcp",
    "ferro-projections",
    "ferro-stripe",
    "ferro-theme",
    "ferro-ai",
    "ferro-whatsapp",
    "ferro-wallet",
    "ferro-orm",
]
```

From Cargo.toml [workspace.package] (current):
```toml
[workspace.package]
version = "0.2.30"
```

From .github/workflows/publish.yml line 201:
```
WAVE1A_CRATES="ferro-macros ferro-events ferro-queue ferro-broadcast ferro-storage ferro-cache ferro-lang ferro-theme ferro-json-ui ferro-inertia ferro-api-mcp ferro-wallet ferro-orm"
```

From CLAUDE.md Workspace Structure table (relevant rows, near the end):
```markdown
| `ferro-orm` | Atomic conditional updates and ORM primitives (`GuardedUpdate`) | `src/lib.rs` |
| `app` | Sample application | Reference implementation |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register ferro-audit in workspace Cargo.toml and bump version 0.2.30 → 0.2.31</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml lines 1-30 (current `[workspace] members` list + `[workspace.package] version`)
    - .planning/phases/153-ferro-audit-crate-structured-before-after-audit-log-with-rep/153-RESEARCH.md §"F-07: Version is 0.2.30, not 0.2.25 as D-38 states" (version-drift correction is load-bearing)
    - .planning/phases/153-ferro-audit-crate-structured-before-after-audit-log-with-rep/153-PATTERNS.md §"Cargo.toml (workspace root, members + version)" (full target content)
  </read_first>
  <behavior>
    - Workspace `[workspace.members]` now contains `"ferro-audit"` (added at the end of the list, after `"ferro-orm"`)
    - `[workspace.package] version` bumped from `"0.2.30"` to `"0.2.31"` (NOT `0.2.26` — D-38's value was stale; see RESEARCH F-07)
    - `cargo metadata --no-deps` succeeds (manifest still well-formed)
    - No other workspace member entries are removed or reordered
  </behavior>
  <action>
    Edit `/Users/alberto/repositories/albertogferrario/ferro/Cargo.toml`.

    **Step 1 — Verify current version at execution time.** Run:
    ```bash
    grep -E '^version = ' Cargo.toml | head -1
    ```
    Expected current value: `version = "0.2.30"`. If the version is already higher than `0.2.30` (CI auto-bumped after Phase 152 publish), bump to the next patch (e.g. `0.2.30 → 0.2.31`, or `0.2.32 → 0.2.33`). The principle is: bump by one patch from whatever is currently recorded. Do NOT use D-38's stale `0.2.25 → 0.2.26`.

    **Step 2 — Add `"ferro-audit"` to `[workspace.members]`.** Insert at the END of the list (after the existing `"ferro-orm"` line). The resulting block:

    ```toml
    members = [
        "framework",
        "app",
        "ferro-cli",
        "ferro-macros",
        "ferro-events",
        "ferro-queue",
        "ferro-notifications",
        "ferro-broadcast",
        "ferro-storage",
        "ferro-cache",
        "ferro-mcp",
        "ferro-inertia",
        "ferro-json-ui",
        "ferro-lang",
        "ferro-api-mcp",
        "ferro-projections",
        "ferro-stripe",
        "ferro-theme",
        "ferro-ai",
        "ferro-whatsapp",
        "ferro-wallet",
        "ferro-orm",
        "ferro-audit",
    ]
    ```

    **Step 3 — Bump `[workspace.package] version`** from `"0.2.30"` to `"0.2.31"` (use Step 1's result if the current value differs from `0.2.30`).

    Final `[workspace.package]` block:
    ```toml
    [workspace.package]
    version = "0.2.31"
    edition = "2021"
    rust-version = "1.88.0"
    license = "MIT"
    ```
    (Do not touch `edition`, `rust-version`, or `license`.)

    **Step 4 — Verify the manifest still parses.** Run:
    ```bash
    cargo metadata --no-deps --format-version 1 > /dev/null
    ```
    This must exit 0. If it fails with `package not found at 'ferro-audit'` and plan 01 has NOT yet completed (parallel execution race), this is expected — plan 01 creates the `ferro-audit/Cargo.toml` that this entry points to. Retry after plan 01's Task 1 completes.
  </action>
  <verify>
    <automated>grep -q '"ferro-audit"' Cargo.toml &amp;&amp; grep -E '^version = "0\.2\.3[1-9]"' Cargo.toml &amp;&amp; ! grep -q '^version = "0.2.30"' Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml` contains `"ferro-audit"` in `[workspace.members]`
    - `Cargo.toml` `[workspace.package] version` is `"0.2.31"` or higher (NEVER still `"0.2.30"` after this task)
    - The version is NEVER `"0.2.26"` (D-38's stale value would be incorrect)
    - All previous workspace members are still present
    - `cargo metadata --no-deps` exits 0 (after plan 01's Cargo.toml exists)
  </acceptance_criteria>
  <done>Workspace Cargo.toml registers ferro-audit and bumps version. Manifest still parses.</done>
</task>

<task type="auto">
  <name>Task 2: Add ferro-audit to .github/workflows/publish.yml Wave 1a</name>
  <files>.github/workflows/publish.yml</files>
  <read_first>
    - .github/workflows/publish.yml line 201 (current `WAVE1A_CRATES` string)
    - .planning/phases/153-ferro-audit-crate-structured-before-after-audit-log-with-rep/153-RESEARCH.md §"F-08: Wave 1a WAVE1A_CRATES string to update in publish.yml" (exact target string)
    - .planning/phases/153-ferro-audit-crate-structured-before-after-audit-log-with-rep/153-PATTERNS.md §".github/workflows/publish.yml"
  </read_first>
  <behavior>
    - `.github/workflows/publish.yml` line 201 (or the line containing `WAVE1A_CRATES=`) ends with `ferro-orm ferro-audit"`
    - No change to `WAVE2_CRATES` (ferro-audit is Wave 1a, not Wave 2 — it has no internal ferro-* deps per D-03)
    - No change to the loop body that consumes `$WAVE1A_CRATES`
    - The file is still valid YAML
  </behavior>
  <action>
    Edit `/Users/alberto/repositories/albertogferrario/ferro/.github/workflows/publish.yml`.

    Locate the line:
    ```
              WAVE1A_CRATES="ferro-macros ferro-events ferro-queue ferro-broadcast ferro-storage ferro-cache ferro-lang ferro-theme ferro-json-ui ferro-inertia ferro-api-mcp ferro-wallet ferro-orm"
    ```

    Replace it with:
    ```
              WAVE1A_CRATES="ferro-macros ferro-events ferro-queue ferro-broadcast ferro-storage ferro-cache ferro-lang ferro-theme ferro-json-ui ferro-inertia ferro-api-mcp ferro-wallet ferro-orm ferro-audit"
    ```

    Append `ferro-audit` at the very end (after `ferro-orm`). Wave 1a order does not matter for the publish loop (these crates have no internal deps between themselves per D-03 / D-04); appending at the end is the lowest-risk edit.

    Do NOT modify `WAVE2_CRATES="ferro-rs ferro-mcp"` (line 264) — ferro-audit is a leaf crate (D-03 / D-04: no internal ferro-* deps), so it belongs in Wave 1a, not Wave 2.

    Validate YAML still parses:
    ```bash
    python3 -c "import yaml; yaml.safe_load(open('.github/workflows/publish.yml'))" 2>&1 || \
      ruby -ryaml -e "YAML.load_file('.github/workflows/publish.yml')" 2>&1
    ```
    (Use whichever interpreter is available; both validate YAML well-formedness.)
  </action>
  <verify>
    <automated>grep -q 'WAVE1A_CRATES=".*ferro-audit"' .github/workflows/publish.yml &amp;&amp; ! grep -q 'WAVE2_CRATES=".*ferro-audit' .github/workflows/publish.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/publish.yml` contains `WAVE1A_CRATES=` line that includes `ferro-audit` at the end
    - `.github/workflows/publish.yml` `WAVE2_CRATES=` line does NOT contain `ferro-audit`
    - All prior Wave 1a crates remain in the string
    - File is still valid YAML (can be parsed without error)
  </acceptance_criteria>
  <done>publish.yml registers ferro-audit in Wave 1a. CI will auto-publish ferro-audit subsequent versions; the first publish is bootstrapped manually in plan 153-06.</done>
</task>

<task type="auto">
  <name>Task 3: Add ferro-audit row to CLAUDE.md Workspace Structure table</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md lines 50-65 (Workspace Structure table — verify exact column layout and current `ferro-orm` row position)
    - .planning/phases/153-ferro-audit-crate-structured-before-after-audit-log-with-rep/153-PATTERNS.md §"CLAUDE.md Workspace Structure table"
    - .planning/phases/153-ferro-audit-crate-structured-before-after-audit-log-with-rep/153-RESEARCH.md §"F-11"
  </read_first>
  <behavior>
    - CLAUDE.md `## Workspace Structure` (or similarly titled) table contains a row for `ferro-audit`
    - The new row sits between the `ferro-orm` row and the `app` row
    - Row format matches the existing table: `| `ferro-audit` | Append-only structured before/after audit log with replay | `src/lib.rs` |`
  </behavior>
  <action>
    Edit `/Users/alberto/repositories/albertogferrario/ferro/CLAUDE.md`.

    Locate the Workspace Structure table. The current adjacent rows (near the end of the table) are:
    ```markdown
    | `ferro-orm` | Atomic conditional updates and ORM primitives (`GuardedUpdate`) | `src/lib.rs` |
    | `app` | Sample application | Reference implementation |
    ```

    Insert a new row IMMEDIATELY AFTER the `ferro-orm` row and IMMEDIATELY BEFORE the `app` row:
    ```markdown
    | `ferro-audit` | Append-only structured before/after audit log with replay | `src/lib.rs` |
    ```

    Resulting block:
    ```markdown
    | `ferro-orm` | Atomic conditional updates and ORM primitives (`GuardedUpdate`) | `src/lib.rs` |
    | `ferro-audit` | Append-only structured before/after audit log with replay | `src/lib.rs` |
    | `app` | Sample application | Reference implementation |
    ```

    Do not modify any other table rows or any other section of CLAUDE.md.
  </action>
  <verify>
    <automated>grep -q '| `ferro-audit` | Append-only structured before/after audit log with replay | `src/lib.rs` |' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains the exact row: `| `ferro-audit` | Append-only structured before/after audit log with replay | `src/lib.rs` |`
    - The row sits between `ferro-orm` and `app` rows in the table
    - No other CLAUDE.md sections are modified
  </acceptance_criteria>
  <done>CLAUDE.md Workspace Structure table includes a row for ferro-audit at the right position.</done>
</task>

<task type="auto">
  <name>Task 4: Add ferro-audit bullet to workspace README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (workspace root — find the "What's included" / crates list section that includes `ferro-orm` and `ferro-wallet`)
    - .planning/phases/153-ferro-audit-crate-structured-before-after-audit-log-with-rep/153-PATTERNS.md §"README.md (workspace root, 'What's included' section)"
  </read_first>
  <behavior>
    - Workspace root `README.md` includes a `ferro-audit` reference in its crates list
    - The new entry sits at the same indentation / format as the existing `ferro-orm` entry
    - No other README content is modified
  </behavior>
  <action>
    Edit `/Users/alberto/repositories/albertogferrario/ferro/README.md`.

    First, run:
    ```bash
    grep -n 'ferro-orm\|ferro-wallet' README.md | head -10
    ```
    to find the existing crates-list entries. The format follows the "What's included" list pattern. PATTERNS.md notes the analog line:
    ```markdown
    - **Atomic conditional updates** — race-free counter mutations and state transitions (`ferro-orm`)
    ```

    Insert a new bullet IMMEDIATELY AFTER the `ferro-orm` bullet:
    ```markdown
    - **Structured audit log** — append-only before/after history with replay (`ferro-audit`)
    ```

    If the README.md format differs from PATTERNS.md (different list style, different section heading), match the actual format you find in the file. The constraint is: the new entry must appear in the same list as `ferro-orm`, with parallel phrasing, and reference the crate as `(`ferro-audit`)`. No marketing language — neutral, scientific tone (CLAUDE.md §"Repository documents must read as neutral").

    Do NOT mention "gestiscilo", "killer feature", "the bet", or any tenant-specific identifier. The bullet describes WHAT the crate provides, nothing more.
  </action>
  <verify>
    <automated>grep -q 'ferro-audit' README.md &amp;&amp; ! grep -qi 'killer feature\|the bet\|load-bearing\|gestiscilo\|forcing function' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains a reference to `ferro-audit`
    - The new entry is positioned adjacent to the existing `ferro-orm` entry (same list)
    - Tone is neutral — no forbidden trigger phrases
    - No tenant identifiers are introduced
  </acceptance_criteria>
  <done>Workspace README.md surfaces ferro-audit in its crates list at the same prominence as ferro-orm.</done>
</task>

</tasks>

<threat_model>
This plan modifies workspace-root configuration. The threat surface is limited to mis-registration (file syntax errors that break the workspace) and the supply-chain implications of the publish.yml change.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → CI runner | publish.yml is executed by GH Actions with `CARGO_REGISTRY_TOKEN`; misconfiguration could cause CI to attempt a publish before the crate is ready |
| repo → cargo workspace | Cargo.toml `[workspace.members]` controls what `cargo build --workspace` compiles; a typo would silently exclude the crate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-02-W1 | T | Cargo.toml [workspace.members] | mitigate | `cargo metadata --no-deps` is part of the verify command; a typo in the member name would surface immediately when plan 01's `ferro-audit/Cargo.toml` is parsed and the path-to-name match fails. |
| T-153-02-W2 | E | publish.yml WAVE1A_CRATES | mitigate | The first publish is bootstrapped MANUALLY in plan 153-06 (CI's `publish-update` token cannot create a new crate per RESEARCH §Environment Availability + Phase 151/152 pattern); CI will only auto-publish after the manual bootstrap succeeds. Risk window: between the master push of plan 02's edit and plan 06's manual bootstrap. Mitigated by plan 06's `if ! cargo search ferro-orm | head -5` style check before the manual publish (mirrored from Phase 151/152). |
| T-153-02-W3 | I | CLAUDE.md / README.md content | mitigate | Plan tasks include a `! grep -qi 'killer feature\|the bet\|...'` guard ensuring no internal-voice phrases land in the public-facing docs (CLAUDE.md §"Repository documents must read as neutral"). |
</threat_model>

<verification>
- `grep -q '"ferro-audit"' Cargo.toml` succeeds
- `grep -E '^version = "0\.2\.3[1-9]"' Cargo.toml` succeeds (version bumped to 0.2.31 or higher)
- `grep -q 'WAVE1A_CRATES=".*ferro-audit"' .github/workflows/publish.yml` succeeds
- `grep -q '| `ferro-audit` |' CLAUDE.md` succeeds
- `grep -q 'ferro-audit' README.md` succeeds
- After plan 153-01 completes: `cargo build --workspace` exits 0 (workspace builds with the new member registered)
</verification>

<success_criteria>
`ferro-audit` is a recognized workspace member. CI's Wave 1a publish loop will pick it up for auto-publish (after the manual first-publish bootstrap in plan 153-06). Developer-facing project documents (`CLAUDE.md` Workspace Structure table, workspace `README.md` crates list) surface the new crate at the same prominence as `ferro-orm`. The workspace version is bumped to `0.2.31` (per RESEARCH F-07, NOT D-38's stale `0.2.26`).
</success_criteria>

<output>
After completion, create `.planning/phases/153-ferro-audit-crate-structured-before-after-audit-log-with-rep/153-02-SUMMARY.md` documenting:
- The four files modified (Cargo.toml, .github/workflows/publish.yml, CLAUDE.md, README.md)
- The exact version bump applied (e.g. `0.2.30 → 0.2.31`, or whatever the actual values were at execution time)
- The position chosen for the `ferro-audit` entry in each file
- Confirmation that `cargo metadata --no-deps` exits 0
- Confirmation that publish.yml is still valid YAML
- Any deviations from PATTERNS.md and why
</output>
</content>
</invoke>