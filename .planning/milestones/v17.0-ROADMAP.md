# Milestone v17.0: Live Projection Surface

**Status:** ✅ SHIPPED — phases complete 2026-07-26; single operator-gated publish per Phase 262
**Phases:** 259–262
**Total Plans:** 14 (3 + 4 + 4 + 3)

## Overview

Join the two halves of a live-rendering story ferro already carries — `ferro-projection`'s
per-key snapshot + delta broadcast and `ferro-json-ui`'s server-side rendering — into a
declarative `LiveFragment` element that binds a rendered fragment to a projection key and
re-renders in place on delta, server-authoritatively, with no WASM. Two supporting ergonomics
land alongside: request-scoped fan-out-deduplicated memoization wired into the projection
render pass, and a one-line `asset!()` declaration over the existing bundle/pipeline crates.
The live fragment makes the singular projection runtime a first-class, agent-composable,
MCP-introspectable JSON-UI rendering target.

**Anchor spec:** `docs/superpowers/specs/2026-07-21-live-projection-surface-design.md`.

**Architectural constraints:** no new crates (memoization in `framework` + `ferro-macros`;
live fragment in `ferro-json-ui` + `ferro-projection`; asset macro in `ferro-macros` over
`ferro-assets`/`ferro-bundle`); rendering stays server-authoritative (client runtime only opens
a socket and swaps HTML — no WASM, no client state); one per-key snapshot per fragment (list
reconciliation deferred); single crates.io publish at Phase 262.

## Phases

### Phase 259: Request-scoped memoization

**Goal:** Give the render path a request-scoped memo store so an async fn / `#[service]` method
marked `#[memoize]` runs at most once per `(callsite, args)` per request, coalescing concurrent
callers onto one shared computation — the fan-out dedup a multi-intent projection render needs.
**Requirements:** LIVE-01. **Plans:** 3/3.

- [x] 259-01: `MemoStore` + `MEMO_STORE` task-local + futures dep + server.rs scope entry + re-export
- [x] 259-02: `#[memoize]` proc-macro in ferro-macros + registration + macro-level proofs
- [x] 259-03: render-path integration harness (N intents / one key → one fetch) + rustdoc audit + CI gate

### Phase 260: Live reactive fragment  *(killer feature)*

**Goal:** Add a `LiveFragment` JSON-UI element that binds a child template to a projection key,
renders the current snapshot on first paint, and re-renders in place on each delta —
server-authoritatively, with a client runtime that only opens the `ferro-broadcast` socket and
swaps HTML. **Requirements:** LIVE-02. **Plans:** 4/4.

- [x] 260-01: `ferro-projection` `fragment_hook` seam + `with_fragment_renderer()` + `apply_event` fire + integration test
- [x] 260-02: `LiveFragmentProps` + `render_live_fragment` first-paint / absent-snapshot renderer
- [x] 260-03: no-WASM `setupLiveFragments` client runtime + runtime assembly + enumerating tests
- [x] 260-04: BUILTIN lockstep (count 52→53) + end-to-end render proof

**Verification:** passed. LiveFragment live DOM swap verified first-hand via Chrome MCP
(2026-07-28): booted app with `ProjectionRuntime` + fragment renderer, `POST /live-test/trigger`
→ `[data-live-fragment]` container `innerHTML` swapped in place (placeholder → `<span>2</span>`,
no reload), screenshot captured, `projection_snapshots` row persisted.

### Phase 261: `asset!()` ergonomics

**Goal:** Collapse the boot-time bundle builder to a one-line `asset!("path")` at the use site,
and give an opt-in author-time fetch for Iconify sets and Fontsource families, all flowing
through the existing content-hashed pipeline with no new infrastructure. **Requirements:**
LIVE-03. **Plans:** 4/4.

- [x] 261-01: decouple `ferro-bundle` to a leaf crate + `mime_from_ext` + publish-wave move
- [x] 261-02: `ferro::bundle` re-export + `Bundle::serve` Request adapter
- [x] 261-03: `ferro assets fetch` CLI (Iconify/Fontsource, validated, rustls-tls)
- [x] 261-04: `asset!()` proc-macro + `ferro::asset!` re-export + trybuild pass fixture

### Phase 262: MCP + catalog + docs + publish

**Goal:** Close the single-source loop — surface `LiveFragment` through the same catalog and
`generation_context` an agent reads, document all three capabilities, and ship in one
operator-gated publish. **Requirements:** LIVE-04. **Plans:** 3/3.

- [x] 262-01: `LiveProjectionGuidance` in generation_context (LiveFragment / `#[memoize]` / `asset!()`) + drift guard + catalog evidence
- [x] 262-02: `docs/src` sections for all three capabilities + mdBook build gate
- [x] 262-03: full CI-exact gate + operator-gated single crates.io publish

## Requirement → Phase Mapping

| Requirement | Phase |
|-------------|-------|
| LIVE-01 (request-scoped `#[memoize]` + render-path fetch dedup) | 259 |
| LIVE-02 (`LiveFragment` element + projection render hook + client runtime) | 260 |
| LIVE-03 (`asset!()` macro + Iconify/Fontsource fetch) | 261 |
| LIVE-04 (`ferro-mcp` catalog + generation_context + docs + publish) | 262 |

4/4 requirements mapped, no orphans, no duplicates.

---

## Milestone Summary

**Key Decisions:**

- **`LiveFragment` is server-authoritative** — the client runtime only opens the
  `ferro-broadcast` socket and swaps inner HTML; no WASM, no client-side state, no signal system.
- One per-key snapshot bound to one fragment; **list/collection reconciliation deferred** (spec
  non-goal) — a documented, honest limitation.
- `#[memoize]` is request-scoped only (cross-request caching stays `ferro-cache`); it complements
  `eager_loading`/`BatchLoad`, not replaces it.
- No new crates; the milestone is authoring primitives over already-shipped substrate
  (ferro-projection delta broadcast, ferro-bundle content-hashing, Request extensions type-map).

**Technical Debt / Deferred:** `LiveFragment` list reconciliation (deferred direction in the
spec).

---

_For current project status, see .planning/ROADMAP.md_
