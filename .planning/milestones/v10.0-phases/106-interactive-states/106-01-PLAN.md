---
phase: 106-interactive-states
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - ferro-json-ui/src/render.rs
  - ferro-json-ui/src/layout.rs
autonomous: true
requirements: [INT-01, INT-02, INT-03, INT-04, INT-05, INT-06, INT-07]

must_haves:
  truths:
    - "Tabbing through a JSON-UI page shows a visible 2px focus ring on every button"
    - "Tabbing shows a visible focus ring on tab buttons and tab links"
    - "Tabbing shows a visible focus ring on pagination prev/next/page links"
    - "Tabbing shows a visible focus ring on breadcrumb links"
    - "Tabbing shows a visible focus ring on sidebar nav items (both standalone and layout)"
    - "Table body rows highlight on hover with bg-surface"
    - "All interactive elements animate color transitions over 150ms with reduced-motion suppression"
    - "Focus rings use focus-visible: (not focus:) so mouse clicks do not trigger the ring"
  artifacts:
    - path: "ferro-json-ui/src/render.rs"
      provides: "Focus rings on buttons, tabs, pagination, breadcrumbs, sidebar nav items; hover on table rows; transitions on all"
      contains: "focus-visible:ring-primary"
    - path: "ferro-json-ui/src/layout.rs"
      provides: "Focus rings and transitions on DashboardLayout sidebar nav items"
      contains: "focus-visible:ring-primary"
  key_links:
    - from: "ferro-json-ui/src/render.rs"
      to: "Tailwind v4 CDN"
      via: "class strings using focus-visible:* and motion-reduce:* utilities"
      pattern: "focus-visible:ring-2"
    - from: "ferro-json-ui/src/layout.rs"
      to: "ferro-json-ui/src/render.rs"
      via: "identical sidebar nav item class pattern"
      pattern: "focus-visible:ring-primary"
---

<objective>
Add keyboard focus rings (focus-visible:ring-2) and hover states to all interactive elements in the JSON-UI renderer: buttons, tab buttons/links, pagination links, breadcrumb links, sidebar nav items (both standalone and DashboardLayout), and table row hover. Complete the transition-colors/duration-150/motion-reduce triple on all interactive elements.

Purpose: Every interactive element must be keyboard-navigable with a visible 2px focus ring indicator, completing the accessibility story started in Phase 105 for form elements.
Output: Updated render.rs and layout.rs with focus rings, transitions, and table row hover; 7+ new structural tests.
</objective>

<execution_context>
@/Users/alberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-interactive-states/106-RESEARCH.md
@.planning/phases/105-form-polish/105-01-SUMMARY.md

<interfaces>
<!-- Key types, helpers, and patterns the executor needs -->

From ferro-json-ui/src/render.rs (test helpers, line 5223):
```rust
fn has_class(html: &str, class: &str) -> bool {
    html.contains(&format!("class=\"{class}\""))
        || html.contains(&format!("class=\"{class} "))
        || html.contains(&format!(" {class}\""))
        || html.contains(&format!(" {class} "))
}
fn assert_element(html: &str, tag: &str, content: &str) { ... }
```

From ferro-json-ui/src/render.rs (test node constructors, line 1839):
```rust
fn button_node(key: &str, label: &str, variant: ButtonVariant, size: Size) -> ComponentNode
```

Canonical focus ring class string (from Phase 105, render.rs line 696):
```
focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2
```

Canonical transition class string (from Phase 105):
```
transition-colors duration-150 motion-reduce:transition-none
```

Structural test module location: render.rs line 5251 `mod structural_tests { ... }` ends at line 5873.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED -- Write failing tests for all 7 INT requirements</name>
  <files>ferro-json-ui/src/render.rs</files>
  <behavior>
    - INT-01: Button renders with `focus-visible:ring-primary` and `duration-150`
    - INT-02: Tab button renders with `focus-visible:ring-primary` and `duration-150`
    - INT-03: Pagination link renders with `focus-visible:ring-primary` and `duration-150`
    - INT-04: Breadcrumb link renders with `focus-visible:ring-primary` and `duration-150`
    - INT-05: Sidebar nav item renders with `focus-visible:ring-primary` and `duration-150`
    - INT-06: Table body row renders with `hover:bg-surface`
    - INT-07: Covered by INT-01 through INT-05 testing `duration-150` and `motion-reduce:transition-none`
  </behavior>
  <action>
Add 7 new test functions inside `mod structural_tests` (before the closing `}` at line 5873 of render.rs). Use the `has_class()` helper and existing test node constructors. Each test creates a minimal component, renders it, and asserts on the presence of specific classes.

Test 1 -- `button_focus_ring`: Create a default button via `button_node`. Assert `has_class(&html, "focus-visible:ring-primary")`, `has_class(&html, "duration-150")`, and `html.contains("motion-reduce:transition-none")`.

Test 2 -- `tabs_focus_ring`: Create a TabsProps with 2 tabs (to avoid the single-tab auto-hide path). Assert rendered HTML contains `focus-visible:ring-primary` and `duration-150`.

Test 3 -- `pagination_focus_ring`: Create a PaginationProps with total=30, per_page=10, current_page=2. Assert the rendered `<a` tags contain `focus-visible:ring-primary` and `duration-150`.

Test 4 -- `breadcrumb_focus_ring`: Create a BreadcrumbProps with 2 items (first with URL, second without). Assert the `<a` link contains `focus-visible:ring-primary` and `duration-150`.

Test 5 -- `sidebar_nav_focus_ring`: Create a SidebarProps with one nav item. Assert the `<a` tag contains `focus-visible:ring-primary` and `duration-150`.

Test 6 -- `table_row_hover`: Create a TableProps with columns and data. Assert the rendered `<tr` body row contains `hover:bg-surface`.

Test 7 -- `layout_sidebar_nav_focus_ring`: Import `layout_sidebar_nav_item` from `super::super` (or call it via the layout module). This test may need to be placed in the layout.rs test module instead if import is not feasible. Assert focus-visible ring on the `<a` tag.

For test 7, check if layout.rs has an existing test module. If so, add the test there. If not, create a minimal `#[cfg(test)] mod tests` in layout.rs with the assertion. The research confirms layout.rs has test code around line 1198.

Run `cargo test -p ferro-json-ui` -- all 7 new tests MUST FAIL (RED phase). Commit with message `test(106-01): add failing tests for interactive states (INT-01 through INT-07)`.
  </action>
  <verify>
    <automated>cargo test -p ferro-json-ui 2>&1 | grep -E "FAILED|test result"</automated>
  </verify>
  <done>7 new tests exist and all fail because focus-visible/hover classes are not yet present in the render output</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN -- Implement focus rings, transitions, and table row hover</name>
  <files>ferro-json-ui/src/render.rs, ferro-json-ui/src/layout.rs</files>
  <action>
Apply the canonical focus ring and transition class strings to each interactive element. All changes are class string modifications -- no structural HTML changes needed.

**render.rs changes:**

1. **render_button** (line 1095): Replace `"inline-flex items-center justify-center rounded-md font-medium transition-colors"` with `"inline-flex items-center justify-center rounded-md font-medium transition-colors duration-150 motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"` (INT-01, INT-07).

2. **render_tabs** (lines 489-498 and 501-510): Both the `<button>` (client-side trigger) and `<a>` (server-driven tab) class strings need `transition-colors duration-150 motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2` appended after `cursor-pointer` / `font-medium` respectively (INT-02, INT-07).

3. **render_pagination** (lines 1306, 1325, 1337): All three `<a>` class strings (prev link, page number link, next link) need `transition-colors duration-150 motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2` appended (INT-03, INT-07).

4. **render_breadcrumb** (line 1273): The `<a>` class string `"hover:text-text"` becomes `"hover:text-text transition-colors duration-150 motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"` (INT-04, INT-07).

5. **render_sidebar_nav_item** (line 1727-1731): Both active and inactive class strings need focus ring and transition appended. Active: append `transition-colors duration-150 motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2`. Inactive: append the same (it already has `hover:text-text hover:bg-surface`) (INT-05, INT-07).

6. **render_table** (line 627): Change `html.push_str("<tr>")` to `html.push_str("<tr class=\"hover:bg-surface\">")` for body rows (INT-06).

7. **Checkbox cleanup** (line 926): Opportunistically update `focus:ring-primary` to `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2` to match the established standard. This is not an INT requirement but is adjacent and wrong by current convention.

**layout.rs changes:**

8. **layout_sidebar_nav_item** (lines 145-148): Both active and inactive class strings need the same focus ring and transition classes appended, identical to render.rs step 5 (INT-05, INT-07).

After all changes, run `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features`. All 7 new tests MUST PASS (GREEN phase). Commit with message `feat(106-01): add focus rings, transitions, and hover states to all interactive elements`.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test -p ferro-json-ui 2>&1 | tail -5</automated>
  </verify>
  <done>All 7 INT requirements implemented: buttons, tabs, pagination, breadcrumbs, sidebar nav items have focus-visible ring and transition triple; table rows have hover:bg-surface; all tests pass; clippy clean</done>
</task>

</tasks>

<verification>
1. `cargo test -p ferro-json-ui` -- all tests pass (413 existing + 7 new)
2. `cargo fmt --all -- --check` -- no formatting issues
3. `cargo clippy --all --all-targets -- -D warnings` -- no warnings
4. `cargo test --all-features` -- full workspace green
5. Grep verification: `grep -c "focus-visible:ring-primary" ferro-json-ui/src/render.rs` should return >= 8 (6 from Phase 105 form elements + 6+ from Phase 106 interactive elements)
6. Grep verification: `grep -c "focus-visible:ring-primary" ferro-json-ui/src/layout.rs` should return >= 2 (active + inactive sidebar nav item)
7. Grep verification: `grep "hover:bg-surface" ferro-json-ui/src/render.rs | grep "<tr"` should return 1 match
</verification>

<success_criteria>
- Every button rendered by render_button contains focus-visible:ring-primary and duration-150
- Tab buttons and tab links contain focus-visible ring and transition classes
- All pagination `<a>` links contain focus-visible ring and transition classes
- Breadcrumb `<a>` links contain focus-visible ring and transition classes
- Sidebar nav items in both render.rs and layout.rs contain focus-visible ring and transition classes
- Table body rows render with hover:bg-surface class
- All interactive elements have the full transition-colors duration-150 motion-reduce:transition-none triple
- 7+ new structural tests pass
- Full workspace cargo fmt + clippy + test green
</success_criteria>

<output>
After completion, create `.planning/phases/106-interactive-states/106-01-SUMMARY.md`
</output>
