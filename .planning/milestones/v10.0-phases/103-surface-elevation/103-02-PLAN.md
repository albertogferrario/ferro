---
phase: 103-surface-elevation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-json-ui/src/runtime.rs
autonomous: true
requirements: [SRF-07]

must_haves:
  truths:
    - "Toast VARIANT_CLASSES uses semantic tokens (bg-primary, bg-success, bg-warning, bg-destructive)"
    - "Tab switcher active state uses border-primary text-primary (not border-blue-600)"
    - "Tab switcher inactive state uses text-text-muted (not text-gray-500)"
    - "Toast element uses text-primary-foreground pattern instead of text-white"
    - "Toast close button uses text-current (not text-white)"
  artifacts:
    - path: "ferro-json-ui/src/runtime.rs"
      provides: "Semantic token classes in all JS runtime class manipulation"
      contains: "bg-primary"
  key_links:
    - from: "ferro-json-ui/src/runtime.rs"
      to: "ferro-theme/assets/default.css"
      via: "VARIANT_CLASSES references semantic token classes that resolve to CSS custom properties"
      pattern: "bg-primary|bg-success|bg-warning|bg-destructive"
---

<objective>
Replace all hardcoded Tailwind palette classes in the runtime JS with semantic token classes.

Purpose: The runtime JS (toast display, tab switching) uses hardcoded color classes like bg-blue-500, text-gray-500, border-blue-600 that bypass the theme system. Replacing them with semantic tokens (bg-primary, text-text-muted, border-primary) ensures dynamic behaviors match the active theme.

Output: runtime.rs with all semantic token classes. New test module verifying no hardcoded palette classes remain.
</objective>

<execution_context>
@/Users/alberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-surface-elevation/103-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

Static toast pattern from render.rs (the JS toast should mirror this approach):
```rust
// render_toast uses light background + semantic text color:
ToastVariant::Info => "bg-primary/10 border-primary text-primary",
ToastVariant::Success => "bg-success/10 border-success text-success",
```

JS toast uses solid background (different visual style, but should use semantic tokens):
- Current: `bg-blue-500` with `text-white`
- Target: `bg-primary text-primary-foreground` (solid colored background, appropriate foreground text)

Research open question resolved: Use `bg-primary text-primary-foreground` pattern in VARIANT_CLASSES since JS toasts are solid-colored notifications (different from the light-tinted static toasts). The primary-foreground token ensures correct text color regardless of theme.

Tab switcher static HTML already uses semantic tokens:
```rust
// Active: "border-primary" + "text-primary"
// Inactive: "border-transparent" + "text-text-muted hover:text-text"
```
The JS must match these exact class strings.

runtime.rs exports only:
```rust
pub(crate) const FERRO_RUNTIME_JS: &str = r#"..."#;
```
No existing test module in runtime.rs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runtime JS semantic token tests (Wave 0)</name>
  <files>ferro-json-ui/src/runtime.rs</files>
  <behavior>
    - Test 1: FERRO_RUNTIME_JS contains "bg-primary" (not "bg-blue-500")
    - Test 2: FERRO_RUNTIME_JS contains "bg-success" (not "bg-green-500")
    - Test 3: FERRO_RUNTIME_JS contains "bg-warning" (not "bg-yellow-500")
    - Test 4: FERRO_RUNTIME_JS contains "bg-destructive" (not "bg-red-500")
    - Test 5: FERRO_RUNTIME_JS contains "border-primary" (not "border-blue-600")
    - Test 6: FERRO_RUNTIME_JS contains "text-primary" in tab section (not "text-blue-600")
    - Test 7: FERRO_RUNTIME_JS contains "text-text-muted" (not "text-gray-500")
    - Test 8: FERRO_RUNTIME_JS does NOT contain "text-white" (removed from toast)
    - Test 9: FERRO_RUNTIME_JS contains "text-primary-foreground" (toast text color)
  </behavior>
  <action>
Add a `#[cfg(test)] mod tests` block at the end of runtime.rs with tests that assert FERRO_RUNTIME_JS contains the expected semantic token classes and does NOT contain the hardcoded palette classes.

These tests must FAIL initially (RED phase) because runtime.rs still has hardcoded classes. Then proceed to Task 2 to make them pass.

Test structure:
```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn variant_classes_use_semantic_tokens() {
        assert!(FERRO_RUNTIME_JS.contains("'bg-primary'"), "info variant should use bg-primary");
        assert!(FERRO_RUNTIME_JS.contains("'bg-success'"), "success variant should use bg-success");
        assert!(FERRO_RUNTIME_JS.contains("'bg-warning'"), "warning variant should use bg-warning");
        assert!(FERRO_RUNTIME_JS.contains("'bg-destructive'"), "error variant should use bg-destructive");
        assert!(!FERRO_RUNTIME_JS.contains("bg-blue-500"), "should not contain hardcoded bg-blue-500");
        assert!(!FERRO_RUNTIME_JS.contains("bg-green-500"), "should not contain hardcoded bg-green-500");
        assert!(!FERRO_RUNTIME_JS.contains("bg-yellow-500"), "should not contain hardcoded bg-yellow-500");
        assert!(!FERRO_RUNTIME_JS.contains("bg-red-500"), "should not contain hardcoded bg-red-500");
    }

    #[test]
    fn tab_switcher_uses_semantic_tokens() {
        assert!(FERRO_RUNTIME_JS.contains("border-primary"), "active tab should use border-primary");
        assert!(FERRO_RUNTIME_JS.contains("text-primary"), "active tab should use text-primary");
        assert!(FERRO_RUNTIME_JS.contains("text-text-muted"), "inactive tab should use text-text-muted");
        assert!(!FERRO_RUNTIME_JS.contains("border-blue-600"), "should not contain hardcoded border-blue-600");
        assert!(!FERRO_RUNTIME_JS.contains("text-blue-600"), "should not contain hardcoded text-blue-600");
        assert!(!FERRO_RUNTIME_JS.contains("text-gray-500"), "should not contain hardcoded text-gray-500");
    }

    #[test]
    fn toast_uses_semantic_text_color() {
        assert!(FERRO_RUNTIME_JS.contains("text-primary-foreground"), "toast should use text-primary-foreground");
        assert!(!FERRO_RUNTIME_JS.contains("text-white"), "toast should not use hardcoded text-white");
    }
}
```
  </action>
  <verify>
    <automated>cd /Users/alberto/repositories/albertogferrario/ferro && cargo test -p ferro-json-ui runtime -- 2>&1 | tail -10</automated>
  </verify>
  <done>Test module exists in runtime.rs. Tests compile but FAIL (RED phase -- hardcoded classes still present).</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded palette classes with semantic tokens in runtime JS</name>
  <files>ferro-json-ui/src/runtime.rs</files>
  <action>
Make all tests from Task 1 pass by replacing hardcoded classes in FERRO_RUNTIME_JS:

1. **VARIANT_CLASSES object** (~line 84-89):
   - `info: 'bg-blue-500'` -> `info: 'bg-primary text-primary-foreground'`
   - `success: 'bg-green-500'` -> `success: 'bg-success text-primary-foreground'`
   - `warning: 'bg-yellow-500'` -> `warning: 'bg-warning text-primary-foreground'`
   - `error: 'bg-red-500'` -> `error: 'bg-destructive text-primary-foreground'`

2. **Toast element class string** (~line 101):
   - Remove `text-white` from the className string
   - The text color now comes from VARIANT_CLASSES (text-primary-foreground)

3. **Toast close button** (~line 105):
   - Change `class="text-white opacity-70 hover:opacity-100 text-lg leading-none"` to `class="text-current opacity-70 hover:opacity-100 text-lg leading-none"`
   - `text-current` inherits the foreground color from the parent, which now uses text-primary-foreground

4. **Tab switcher active state** (~line 251-252):
   - `t.classList.remove('border-transparent', 'text-gray-500', 'hover:text-gray-700');` -> `t.classList.remove('border-transparent', 'text-text-muted', 'hover:text-text');`
   - `t.classList.add('border-blue-600', 'text-blue-600');` -> `t.classList.add('border-primary', 'text-primary');`

5. **Tab switcher inactive state** (~line 255-256):
   - `t.classList.remove('border-blue-600', 'text-blue-600');` -> `t.classList.remove('border-primary', 'text-primary');`
   - `t.classList.add('border-transparent', 'text-gray-500', 'hover:text-gray-700');` -> `t.classList.add('border-transparent', 'text-text-muted', 'hover:text-text');`

Verify the tab switcher JS now matches the static tab HTML in render.rs (render_tabs function):
- Active: border-primary, text-primary
- Inactive: border-transparent, text-text-muted hover:text-text
  </action>
  <verify>
    <automated>cd /Users/alberto/repositories/albertogferrario/ferro && cargo test -p ferro-json-ui runtime -- 2>&1 | tail -10</automated>
  </verify>
  <done>All 3 runtime tests pass (GREEN phase). FERRO_RUNTIME_JS contains only semantic token classes. No hardcoded palette classes remain (no bg-blue-500, no text-gray-500, no border-blue-600, no text-white).</done>
</task>

</tasks>

<verification>
- `cargo test -p ferro-json-ui runtime` passes all 3 new tests
- `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` passes
- FERRO_RUNTIME_JS does not contain: bg-blue-500, bg-green-500, bg-yellow-500, bg-red-500, border-blue-600, text-blue-600, text-gray-500, hover:text-gray-700, text-white
- FERRO_RUNTIME_JS does contain: bg-primary, bg-success, bg-warning, bg-destructive, border-primary, text-primary, text-text-muted, hover:text-text, text-primary-foreground, text-current
</verification>

<success_criteria>
- VARIANT_CLASSES uses bg-primary/bg-success/bg-warning/bg-destructive with text-primary-foreground
- Tab switcher uses border-primary/text-primary (active) and text-text-muted (inactive)
- Toast element no longer uses text-white; close button uses text-current
- 3 new tests verify semantic token presence and hardcoded class absence
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/103-surface-elevation/103-02-SUMMARY.md`
</output>
