---
phase: 104-typography-scale
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-json-ui/src/render.rs
  - ferro-json-ui/src/layout.rs
autonomous: true
requirements: [TYP-01, TYP-02, TYP-03, TYP-04, TYP-05]

must_haves:
  truths:
    - "H1 and H2 headings render with leading-tight tracking-tight classes"
    - "H3 headings render with leading-snug class"
    - "Body text (P, Div, Section) renders with leading-relaxed class"
    - "Span elements do NOT receive leading classes (inline elements)"
    - "Sidebar group labels use text-text-muted consistently across layout.rs and render.rs"
  artifacts:
    - path: "ferro-json-ui/src/render.rs"
      provides: "Typography scale classes on all text element variants and inline headings"
      contains: "leading-tight tracking-tight"
    - path: "ferro-json-ui/src/layout.rs"
      provides: "Consistent muted text color on sidebar group labels"
      contains: "text-text-muted"
  key_links:
    - from: "render_text (TextElement::H1/H2)"
      to: "Tailwind leading-tight tracking-tight"
      via: "class string in format! macro"
      pattern: "leading-tight tracking-tight"
    - from: "render_text (TextElement::H3)"
      to: "Tailwind leading-snug"
      via: "class string in format! macro"
      pattern: "leading-snug"
    - from: "render_text (TextElement::P/Div/Section)"
      to: "Tailwind leading-relaxed"
      via: "class string in format! macro"
      pattern: "leading-relaxed"
    - from: "layout_sidebar_group"
      to: "text-text-muted token"
      via: "class string in format! macro"
      pattern: "text-text-muted"
---

<objective>
Add line-height and letter-spacing classes to all heading and body text elements in ferro-json-ui, and fix the sidebar group label muted text inconsistency.

Purpose: Headings currently use browser default line-height (~1.5) and no letter-spacing. Inter Variable font renders better with tighter heading rhythm (leading-tight, tracking-tight) and more generous body spacing (leading-relaxed). The sidebar group label in layout.rs uses text-text instead of text-text-muted, creating a visual inconsistency.

Output: Updated render.rs and layout.rs with typography scale classes, all cosmetic tests updated to match new class strings.
</objective>

<execution_context>
@/Users/alberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-typography-scale/104-RESEARCH.md

@.planning/phases/102-foundation/102-02-SUMMARY.md (has_class test infrastructure)

<interfaces>
<!-- Key types and rendering patterns the executor needs -->

From ferro-json-ui/src/render.rs — render_text function (line 1054):
```rust
// Current TextElement match arms:
TextElement::P       => format!("<p class=\"text-base text-text\">{content}</p>"),
TextElement::H1      => format!("<h1 class=\"text-3xl font-bold text-text\">{content}</h1>"),
TextElement::H2      => format!("<h2 class=\"text-2xl font-semibold text-text\">{content}</h2>"),
TextElement::H3      => format!("<h3 class=\"text-xl font-semibold text-text\">{content}</h3>"),
TextElement::Span    => format!("<span class=\"text-base text-text\">{content}</span>"),
TextElement::Div     => format!("<div class=\"text-base text-text\">{content}</div>"),
TextElement::Section => format!("<section class=\"text-base text-text\">{content}</section>"),
```

From ferro-json-ui/src/render.rs — inline headings (not routed through render_text):
- render_page_header (line 350): `"<h2 class=\"text-2xl font-semibold text-text\">{}</h2>"`
- render_card (line 384): `"<h3 class=\"text-lg font-semibold text-text\">{}</h3>"`
- render_modal (line 424): `"<h3 class=\"text-lg font-semibold text-text\">{}</h3>"`
- render_checklist (line 1501): `"<h3 class=\"text-sm font-semibold text-text\">{}</h3>"`

From ferro-json-ui/src/layout.rs — sidebar group label (line 172):
```rust
"<p class=\"px-2 py-1 text-xs font-semibold text-text\">{}</p>"
// Should be: text-text-muted (matches render.rs line 1682)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typography scale classes to headings and body text in render.rs</name>
  <files>ferro-json-ui/src/render.rs</files>
  <action>
Update the render_text function (line 1054) to add typography classes:

1. H1: Add `leading-tight tracking-tight` after `font-bold`:
   `"<h1 class=\"text-3xl font-bold leading-tight tracking-tight text-text\">{content}</h1>"`

2. H2: Add `leading-tight tracking-tight` after `font-semibold`:
   `"<h2 class=\"text-2xl font-semibold leading-tight tracking-tight text-text\">{content}</h2>"`

3. H3: Add `leading-snug` after `font-semibold`:
   `"<h3 class=\"text-xl font-semibold leading-snug text-text\">{content}</h3>"`

4. P: Add `leading-relaxed` after `text-base`:
   `"<p class=\"text-base leading-relaxed text-text\">{content}</p>"`

5. Div: Add `leading-relaxed` after `text-base`:
   `"<div class=\"text-base leading-relaxed text-text\">{content}</div>"`

6. Section: Add `leading-relaxed` after `text-base`:
   `"<section class=\"text-base leading-relaxed text-text\">{content}</section>"`

7. Span: DO NOT CHANGE. Spans are inline elements and inherit line-height from their block parent.

Update inline headings (not routed through render_text):

8. render_page_header (line 350): Add `leading-tight tracking-tight` to h2:
   `"<h2 class=\"text-2xl font-semibold leading-tight tracking-tight text-text\">{}</h2>"`

9. render_card (line 384): Add `leading-snug` to h3:
   `"<h3 class=\"text-lg font-semibold leading-snug text-text\">{}</h3>"`

10. render_modal (line 424): Add `leading-snug` to h3:
    `"<h3 class=\"text-lg font-semibold leading-snug text-text\">{}</h3>"`

11. render_checklist (line 1501): Add `leading-snug` to h3:
    `"<h3 class=\"text-sm font-semibold leading-snug text-text\">{}</h3>"`

Update all 8 cosmetic test assertions that assert the old class strings:

- `render_view_with_component_wraps_in_div` (~line 1873): Update P assertion to include `leading-relaxed`
- `text_p_variant` (~line 1882): Update to `"<p class=\"text-base leading-relaxed text-text\">Paragraph</p>"`
- `text_h1_variant` (~line 1889): Update to include `leading-tight tracking-tight`
- `text_h2_variant` (~line 1896): Update to include `leading-tight tracking-tight`
- `text_h3_variant` (~line 1903): Update to include `leading-snug`
- `card_renders_title_and_description` (~line 2872): Update h3 to include `leading-snug`
- `modal_renders_details_summary` (~line 2944): Update h3 to include `leading-snug`
- `test_render_page_header_title_only` (~line 4925): Update h2 to include `leading-tight tracking-tight`

After updating, grep render.rs for all remaining `<h2` and `<h3` strings to confirm nothing was missed. The `<h4>` in render_alert and the `<h3>` in layout.rs section titles (which uses text-text-muted uppercase tracking-wider — a different pattern) are intentionally excluded.

DO NOT add leading-relaxed to Span — inline elements inherit line-height.
DO NOT change StatCard value paragraph (text-2xl font-bold) — numeric display, not body text.
  </action>
  <verify>
    <automated>cargo test -p ferro-json-ui</automated>
  </verify>
  <done>
    - H1 renders with leading-tight tracking-tight (TYP-01)
    - H2 renders with leading-tight tracking-tight in both render_text and render_page_header (TYP-02)
    - H3 renders with leading-snug in render_text, render_card, render_modal, and render_checklist (TYP-03)
    - P, Div, Section render with leading-relaxed; Span unchanged (TYP-04)
    - All 8 cosmetic tests updated and passing
    - All structural tests pass without modification
    - cargo test -p ferro-json-ui green
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix sidebar group label muted text consistency in layout.rs</name>
  <files>ferro-json-ui/src/layout.rs</files>
  <action>
Fix the sidebar group label inconsistency (TYP-05):

1. In layout_sidebar_group (line 172), change `text-text` to `text-text-muted`:
   BEFORE: `"<p class=\"px-2 py-1 text-xs font-semibold text-text\">{}</p>"`
   AFTER:  `"<p class=\"px-2 py-1 text-xs font-semibold text-text-muted\">{}</p>"`

This aligns layout.rs with render.rs line 1682 which already correctly uses text-text-muted for sidebar group labels in standalone Sidebar components.

2. Check layout tests for any assertion that matches the old `text-text` class on the group label paragraph. The tests around lines 930 and 1220 may assert sidebar group label HTML. Update any failing assertion to use `text-text-muted`.

Run `cargo test -p ferro-json-ui` to verify. Then run the full lint+test suite:
`cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features`
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features</automated>
  </verify>
  <done>
    - Sidebar group label in layout.rs uses text-text-muted (TYP-05)
    - Both code paths (layout.rs and render.rs) emit text-text-muted for sidebar group labels
    - All layout tests pass with updated assertion
    - Full workspace lint + clippy + test suite green
  </done>
</task>

</tasks>

<verification>
Phase-level checks after both tasks complete:

1. `cargo test -p ferro-json-ui` — all tests green (structural tests unchanged, cosmetic tests updated)
2. `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` — full workspace green
3. Grep verification:
   - `grep -n "leading-tight" ferro-json-ui/src/render.rs` — should show H1, H2, and page_header locations
   - `grep -n "leading-snug" ferro-json-ui/src/render.rs` — should show H3, card, modal, checklist locations
   - `grep -n "leading-relaxed" ferro-json-ui/src/render.rs` — should show P, Div, Section locations
   - `grep -n "text-text-muted" ferro-json-ui/src/layout.rs` — should include sidebar group label at line 172
</verification>

<success_criteria>
- All 5 requirements (TYP-01 through TYP-05) implemented
- No new files created — purely class string additions in existing render.rs and layout.rs
- Full workspace test suite passes (fmt + clippy + test)
- Span element unchanged (no leading class)
- StatCard value paragraph unchanged (numeric display, not body text)
</success_criteria>

<output>
After completion, create `.planning/phases/104-typography-scale/104-01-SUMMARY.md`
</output>
