---
phase: 102-foundation
plan: 01
subsystem: ui
tags: [tailwind-v4, css-tokens, typography, inter, bunny-fonts, json-ui]

# Dependency graph
requires: []
provides:
  - Tailwind v4 --font-sans token in default.css with Inter at front of stack
  - TOKEN_FONT_SANS / TOKEN_FONT_MONO constants in ferro-theme/v1 vocabulary
  - Bunny Fonts CDN preconnect + Inter stylesheet in every JSON-UI page head
  - font-sans body class on all JSON-UI pages via default body_class
affects:
  - phase-103 (surface elevation — safe to test font rendering alongside color tokens)
  - phase-104 (typography — font-sans is the base; heading scales build on it)
  - phase-107 (component details — all component text now renders in Inter)

# Tech tracking
tech-stack:
  added: [Bunny Fonts CDN (bunny.net/inter), Inter Variable (300-700 weights)]
  patterns:
    - Tailwind v4 font tokens use --font-sans / --font-mono namespace (not --font-family-*)
    - Font CDN link is unconditional — loads regardless of tailwind_cdn flag
    - Body class carries font-sans so inheritance cascades to all components

key-files:
  created: []
  modified:
    - ferro-theme/assets/default.css
    - ferro-theme/src/token.rs
    - ferro-cli/src/commands/make_theme.rs
    - framework/src/json_ui/mod.rs
    - ferro-json-ui/src/config.rs

key-decisions:
  - "Font CDN link is unconditional (not gated behind tailwind_cdn) because Inter should load regardless of CDN setting"
  - "Bunny Fonts used instead of Google Fonts — privacy-respecting CDN with same performance"
  - "300-700 weight range loaded to cover all UI needs: 300 muted, 400 body, 500 medium, 600 semibold, 700 bold"

patterns-established:
  - "Tailwind v4 font tokens: --font-sans / --font-mono (v3 --font-family-* namespace is wrong)"
  - "New theme templates generated by make_theme CLI use correct v4 token names"

requirements-completed: [FND-01, FND-02, FND-03]

# Metrics
duration: 15min
completed: 2026-03-25
---

# Phase 102 Plan 01: Font Token Namespace Fix Summary

**Fixed Tailwind v4 font token namespace bug and wired Inter Variable via Bunny Fonts CDN into all JSON-UI pages**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-03-25T00:00:00Z
- **Completed:** 2026-03-25T00:15:00Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Renamed `--font-family-sans/mono` (Tailwind v3 convention ignored by v4) to `--font-sans/mono` across default.css, token.rs, and make_theme CLI template
- Added Bunny Fonts preconnect + Inter Variable stylesheet link unconditionally in every JSON-UI page head
- Updated default body_class to include `font-sans` so Inter cascades to all components automatically

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix font token namespace across all three sources** - `7e9dd4a` (fix)
2. **Task 2: Add Bunny Fonts link and font-sans body class** - `f779634` (feat)

## Files Created/Modified

- `ferro-theme/assets/default.css` - Renamed --font-family-sans/mono to --font-sans/mono, added "Inter" to sans stack
- `ferro-theme/src/token.rs` - Renamed TOKEN_FONT_FAMILY_SANS/MONO to TOKEN_FONT_SANS/MONO with correct values
- `ferro-cli/src/commands/make_theme.rs` - Updated CLI template and test assertions to use --font-sans/mono
- `framework/src/json_ui/mod.rs` - Added Bunny Fonts preconnect + stylesheet before Tailwind CDN, added bunny_fonts_link_in_head test
- `ferro-json-ui/src/config.rs` - Default body_class updated to "bg-background text-text font-sans"

## Decisions Made

- Font CDN link is unconditional — not gated behind `tailwind_cdn` flag because Inter should load even when Tailwind CDN is disabled
- Bunny Fonts instead of Google Fonts — privacy-respecting CDN, same delivery performance
- 300-700 weight range: covers all UI weight needs without over-fetching

## Deviations from Plan

None - plan executed exactly as written. Task 1 was already committed (7e9dd4a) before this execution; Task 2 changes were in the working tree and committed during this run.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Font foundation complete: Inter renders in all JSON-UI pages via `font-sans` utility class
- Phase 103 (Surface Elevation) can now proceed — background hierarchy and dark mode tokens
- Phase 104 (Typography) can proceed — heading scales, body text sizing, and weight utilities build on Inter

---
*Phase: 102-foundation*
*Completed: 2026-03-25*
