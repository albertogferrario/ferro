---
phase: 105-form-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ferro-json-ui/src/render.rs
autonomous: true
requirements: [FRM-01, FRM-02, FRM-03, FRM-04, FRM-05, FRM-06, FRM-07]

must_haves:
  truths:
    - "Select elements display a visible SVG chevron arrow without JavaScript"
    - "Input, Select, and Textarea in error state show a destructive (red) focus ring, not the primary ring"
    - "All form elements animate color transitions over 150ms and suppress animation for prefers-reduced-motion"
    - "Disabled form elements render at reduced opacity with not-allowed cursor"
    - "Form field DOM order is label then input then description then error message"
  artifacts:
    - path: "ferro-json-ui/src/render.rs"
      provides: "Form polish: SVG chevron, error focus rings, transitions, disabled states, DOM reorder"
      contains: "focus-visible:ring-destructive"
  key_links:
    - from: "ferro-json-ui/src/render.rs (render_select)"
      to: "ferro-theme/assets/default.css"
      via: "ring-destructive semantic class referencing --color-destructive token"
      pattern: "ring-destructive"
    - from: "ferro-json-ui/src/render.rs (render_input)"
      to: "ferro-theme/assets/default.css"
      via: "ring-primary semantic class referencing --color-primary token"
      pattern: "ring-primary"
---

<objective>
Apply six categories of visual polish to all form elements in the JSON-UI renderer: custom SVG chevron on select, error-state destructive focus rings on input/select/textarea, transition animations with reduced-motion support, disabled state styling, and corrected label-input-description-error DOM ordering.

Purpose: Form elements currently lack visual refinement — no custom select arrow (appearance-none hides native arrow), no error-specific focus ring color, no transitions, no disabled visual state, and description text renders before the input instead of after it.

Output: Updated `render_input()` and `render_select()` functions in `ferro-json-ui/src/render.rs` with all 7 FRM requirements implemented, plus 6 new tests and 2 updated test assertions.
</objective>

<execution_context>
@/Users/alberto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alberto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-form-polish/105-RESEARCH.md

# Prior phase context — test infrastructure patterns
@.planning/phases/102-foundation/102-02-SUMMARY.md

<interfaces>
<!-- Key types and rendering functions the executor needs from render.rs -->

From ferro-json-ui/src/render.rs (current form functions):

render_input(props: &InputProps, data: &Value) -> String
  - Line 679: handles InputType::Hidden, InputType::Textarea, and all other input types
  - Line 696: opens wrapper `<div class="space-y-1">`
  - Line 697-701: renders label
  - Line 703-708: renders description (CURRENTLY before input — must move AFTER)
  - Line 710-790: renders input/textarea based on InputType
  - Line 792-797: renders error message
  - Line 723: Textarea class string — currently `focus:border-primary focus:ring-1 focus:ring-primary`
  - Line 753: Input class string — currently `focus:border-primary focus:ring-1 focus:ring-primary`

render_select(props: &SelectProps, data: &Value) -> String
  - Line 802: function start
  - Line 819: opens wrapper `<div class="space-y-1">`
  - Line 820-824: renders label
  - Line 826-831: renders description (CURRENTLY before select — must move AFTER)
  - Line 833-845: renders select element — currently `focus:border-primary focus:ring-1 focus:ring-primary`
  - Line 834: class includes `appearance-none bg-background` (already present)
  - Line 865: `</select>` close
  - Line 867-872: renders error

render_checkbox(props: &CheckboxProps, data: &Value) -> String
  - Line 877: function start
  - Line 905: checkbox input class — `h-4 w-4 rounded-sm border-border text-primary focus:ring-primary`
  - Checkbox order is already correct (input+label → description → error) — no FRM-07 change needed

Test helpers (from Phase 102):
  has_class(html: &str, class: &str) -> bool  (line 5194)
  assert_element(html: &str, tag: &str, content: &str)  (follows has_class)

Key token: --color-destructive exists in ferro-theme/assets/default.css (line 18)
  ring-destructive and focus-visible:ring-destructive are valid semantic Tailwind v4 classes
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement form polish on render_input and render_select</name>
  <files>ferro-json-ui/src/render.rs</files>
  <behavior>
    - FRM-02: Input with error renders class containing `focus-visible:ring-destructive` (not `ring-primary`)
    - FRM-02: Input without error renders class containing `focus-visible:ring-primary`
    - FRM-06: Textarea with error renders class containing `focus-visible:ring-destructive`
    - FRM-05: Select with error renders class containing `focus-visible:ring-destructive`
    - FRM-03: Input class contains `transition-colors duration-150 motion-reduce:transition-none`
    - FRM-03: Select class contains `transition-colors duration-150 motion-reduce:transition-none`
    - FRM-03: Textarea class contains `transition-colors duration-150 motion-reduce:transition-none`
    - FRM-04: Input class contains `disabled:opacity-50 disabled:cursor-not-allowed`
    - FRM-04: Select class contains `disabled:opacity-50 disabled:cursor-not-allowed`
    - FRM-04: Textarea class contains `disabled:opacity-50 disabled:cursor-not-allowed`
    - FRM-01: Select wrapped in `<div class="relative">` with inline SVG chevron after `</select>`
    - FRM-01: SVG span has `pointer-events-none absolute inset-y-0 right-3 flex items-center` and `aria-hidden="true"`
    - FRM-01: Select class has `pr-10` to prevent text overlap with chevron
    - FRM-07: In render_input, description `<p>` appears AFTER the input/textarea element, BEFORE error `<p>`
    - FRM-07: In render_select, description `<p>` appears AFTER the `</div>` (select wrapper close), BEFORE error `<p>`
    - FRM-07: Checkbox ordering is NOT changed (already correct)
  </behavior>
  <action>
    Modify two functions in `ferro-json-ui/src/render.rs`:

    **1. render_input() (line 679):**

    a) Add a `focus_ring_class` variable after the `border_class` assignment (line 694):
    ```rust
    let focus_ring_class = if has_error {
        "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-destructive focus-visible:ring-offset-2"
    } else {
        "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"
    };
    ```

    b) Reorder DOM: Move the description block (lines 703-708) to AFTER the `match props.input_type { ... }` block (after line 790) and BEFORE the error block (line 792). The order becomes: label → input/textarea → description → error.

    c) Update Textarea class string (line 723): Replace `focus:border-primary focus:ring-1 focus:ring-primary` with `transition-colors duration-150 motion-reduce:transition-none disabled:opacity-50 disabled:cursor-not-allowed {}` using `focus_ring_class` format interpolation.

    d) Update regular Input class string (line 753): Same replacement — remove `focus:border-primary focus:ring-1 focus:ring-primary`, add `transition-colors duration-150 motion-reduce:transition-none disabled:opacity-50 disabled:cursor-not-allowed {focus_ring_class}`.

    **2. render_select() (line 802):**

    a) Add `focus_ring_class` variable after `border_class` (line 817), same pattern as render_input.

    b) Wrap the `<select>` element in `<div class="relative">`:
       - Before the `<select>` format string (line 833): push `<div class="relative">`
       - Add `pr-10` to the select class string (to prevent text overlapping the chevron)
       - Replace `focus:border-primary focus:ring-1 focus:ring-primary` with `transition-colors duration-150 motion-reduce:transition-none disabled:opacity-50 disabled:cursor-not-allowed {focus_ring_class}`
       - After `html.push_str("</select>")` (line 865): push the inline SVG chevron span using `concat!`:
         ```rust
         html.push_str(concat!(
             "<span class=\"pointer-events-none absolute inset-y-0 right-3 flex items-center\" aria-hidden=\"true\">",
             "<svg class=\"h-4 w-4 text-text-muted\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 0 20 20\" fill=\"currentColor\">",
             "<path fill-rule=\"evenodd\" d=\"M5.23 7.21a.75.75 0 011.06.02L10 11.168l3.71-3.938a.75.75 0 111.08 1.04l-4.25 4.5a.75.75 0 01-1.08 0l-4.25-4.5a.75.75 0 01.02-1.06z\" clip-rule=\"evenodd\"/>",
             "</svg></span>"
         ));
         html.push_str("</div>");
         ```
       - Close the wrapper: `</div>` after the SVG span

    c) Move the description block (lines 826-831) to AFTER the wrapper div close and BEFORE the error block. Order becomes: label → relative-div(select + SVG) → description → error.

    **Checkbox/Switch:** Also add `transition-colors duration-150 motion-reduce:transition-none disabled:opacity-50 disabled:cursor-not-allowed` to the checkbox class string (line 905) for FRM-03/FRM-04 consistency ("All form elements"). The Switch uses `sr-only peer` input so its visual state is handled by the toggle div — no class change needed on the switch input.

    **What NOT to do:**
    - Do NOT use SVG data URI as background-image (`bg-[url('data:...')]`) — fails in CDN mode
    - Do NOT keep `focus:border-primary` — focus feedback is entirely via ring-2
    - Do NOT reorder checkbox DOM — checkbox order (input+label → description → error) is already correct
    - Do NOT forget `pr-10` on select — text will overlap chevron without it
    - Do NOT forget `pointer-events-none` on SVG span — clicks must pass through to select
  </action>
  <verify>
    <automated>cargo test -p ferro-json-ui 2>&1 | tail -5</automated>
  </verify>
  <done>
    - render_input: textarea and input use conditional `focus_ring_class` (destructive for error, primary for normal)
    - render_input: textarea and input have `transition-colors duration-150 motion-reduce:transition-none disabled:opacity-50 disabled:cursor-not-allowed`
    - render_input: description appears after input/textarea, before error
    - render_select: select wrapped in `<div class="relative">` with inline SVG chevron
    - render_select: select has `pr-10` and conditional focus ring
    - render_select: description appears after wrapper div close, before error
    - render_checkbox: has transition and disabled classes
    - All existing tests pass (may need cosmetic assertion updates)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add and update form polish tests</name>
  <files>ferro-json-ui/src/render.rs</files>
  <action>
    Add 6 new tests and update 2 existing test assertions in `ferro-json-ui/src/render.rs` to cover all FRM requirements. Place new tests after the existing form-related tests (after line ~3454) and before the checkbox tests.

    **New tests (follow existing test patterns — create InputProps/SelectProps, render, assert with `has_class` and `html.contains`):**

    1. `select_renders_chevron_wrapper` (FRM-01):
       - Create a Select component, render it
       - Assert `html.contains("<div class=\"relative\">")` — wrapper div exists
       - Assert `html.contains("aria-hidden=\"true\"")` — SVG span has aria-hidden
       - Assert `html.contains("<svg")` — inline SVG exists
       - Assert `html.contains("pointer-events-none")` — span is non-interactive
       - Assert `has_class(&html, "pr-10")` — select has right padding

    2. `input_renders_transition_classes` (FRM-03):
       - Create an Input component (InputType::Text), render it
       - Assert `has_class(&html, "transition-colors")` — transition present
       - Assert `has_class(&html, "duration-150")` — duration present
       - Assert `html.contains("motion-reduce:transition-none")` — reduced motion

    3. `input_disabled_renders_disabled_classes` (FRM-04):
       - Create an Input component with `disabled: Some(true)`, render it
       - Assert `html.contains("disabled:opacity-50")` — opacity class
       - Assert `html.contains("disabled:cursor-not-allowed")` — cursor class
       - Assert `html.contains(" disabled")` — HTML disabled attribute still present

    4. `textarea_renders_error_focus_ring` (FRM-06):
       - Create an Input component with `input_type: InputType::Textarea` and `error: Some("Required".into())`
       - Assert `html.contains("ring-destructive")` — error focus ring
       - Assert `!html.contains("ring-primary")` or `html.contains("ring-destructive")` — not primary

    5. `input_description_order` (FRM-07):
       - Create an Input component with `description: Some("Help text".into())`
       - Render it, find positions: `html.find("<input")` and `html.find("Help text")`
       - Assert the input position < description position (input comes first in DOM)

    6. `select_description_order` (FRM-07):
       - Create a Select component with `description: Some("Pick one".into())`
       - Render it, find positions: `html.find("</select>")` and `html.find("Pick one")`
       - Assert the select close position < description position

    **Updated existing tests:**

    7. Extend `input_renders_error_with_red_border` (line 3156) (FRM-02):
       - Add: `assert!(html.contains("ring-destructive"), "error input should have destructive ring");`

    8. Extend `select_renders_error` (line 3433) (FRM-05):
       - Add: `assert!(html.contains("ring-destructive"), "error select should have destructive ring");`

    Run `cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features` to validate everything passes.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --all --all-targets -- -D warnings && cargo test --all-features 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 6 new tests exist and pass: select_renders_chevron_wrapper, input_renders_transition_classes, input_disabled_renders_disabled_classes, textarea_renders_error_focus_ring, input_description_order, select_description_order
    - 2 existing tests updated with ring-destructive assertions and pass
    - Full workspace lint + clippy + test suite green
  </done>
</task>

</tasks>

<verification>
1. `cargo test -p ferro-json-ui` — all tests pass including 6 new + 2 updated assertions
2. `cargo fmt --all -- --check` — formatting clean
3. `cargo clippy --all --all-targets -- -D warnings` — no warnings
4. `cargo test --all-features` — full workspace green

Spot-check rendered output:
- Select HTML contains `<div class="relative">`, `<svg`, `pointer-events-none`, `pr-10`
- Error input HTML contains `ring-destructive`, NOT `ring-primary`
- Normal input HTML contains `ring-primary`, NOT `ring-destructive`
- Input HTML contains `transition-colors duration-150 motion-reduce:transition-none`
- Input HTML contains `disabled:opacity-50 disabled:cursor-not-allowed`
- Description `<p>` appears after `<input` / `</select>` in rendered HTML
</verification>

<success_criteria>
- All 7 FRM requirements implemented in render.rs
- FRM-01: Select renders inline SVG chevron in relative wrapper div
- FRM-02: Input error state uses focus-visible:ring-destructive
- FRM-03: Input/Select/Textarea have transition-colors with motion-reduce
- FRM-04: Input/Select/Textarea have disabled: variant classes
- FRM-05: Select error state uses focus-visible:ring-destructive
- FRM-06: Textarea error state uses focus-visible:ring-destructive
- FRM-07: DOM order is label → input → description → error
- 8 test assertions cover all requirements (6 new + 2 extended)
- Full workspace cargo fmt + clippy + test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/105-form-polish/105-01-SUMMARY.md`
</output>
