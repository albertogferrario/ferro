---
milestone: v12.7
milestone_name: Passwordless MCP Auth
audited: 2026-06-12
status: tech_debt
scores:
  requirements: n/a (Success-Criteria-driven; no REQ-IDs mapped)
  phases: 3/3 verified passed
  integration: 8/8 cross-phase points connected
  flows: 2/2 happy-path complete (1 pre-existing edge-case interruption)
  security: 3/3 phases secured (0 threats open)
gaps: []   # no critical blockers; no unsatisfied requirements
tech_debt:
  - phase: 202-login-resume-contract-magic-link-sample-app
    items:
      - "WR-02 (edge case): GET /auth/verify sits under GuestMiddleware — an already-authenticated browser tab clicking a magic-link during an in-flight device grant is redirected to / instead of resuming; happy path (new/unauthenticated session) unaffected."
      - "WR-03 (edge case): POST /auth/register does not call oauth_resume_redirect — a new-account registration during an in-flight OAuth/device flow abandons the resume."
  - phase: 202/203/204-validation
    items:
      - "VALIDATION.md nyquist_compliant: false on all three phases — the draft flag was never flipped to true by a /gsd-validate-phase run, though every phase's tests passed in VERIFICATION (status: passed). Process/flag debt, not a coverage gap."
nyquist:
  compliant_phases: 0
  partial_phases: 3   # 202, 203, 204 — VALIDATION.md exists, nyquist_compliant: false, tests green
  missing_phases: 0
  overall: partial
---

# Milestone v12.7 — Passwordless MCP Auth — Audit

**Scope:** Phases 202, 203 (the declared v12.7 pair) + 204 (appended ferro-storage CDN refactor).
**Result:** No critical blockers. All phases verified, secured, and **shipped to crates.io at 0.2.53** (2026-06-12). Light tech debt (pre-existing Phase-202 edge cases + Nyquist draft flags).

## Phase status

| Phase | Verification | Security (open threats) | Plans | Nyquist |
|-------|--------------|--------------------------|-------|---------|
| 202 Login-resume contract + magic-link sample app | passed | 0 | 5/5 | partial (flag false, tests green) |
| 203 OAuth Device Authorization Grant (RFC 8628) | passed | 0 | 5/5 | partial (flag false, tests green) |
| 204 ferro-storage provider-agnostic CDN config | passed | 0 | 3/3 | partial (flag false, tests green) |

## Requirements coverage

No formal REQ-IDs are mapped to these phases — they are **Success-Criteria-driven** (202: SC-1…5; 203: SC-1…5; 204: SC-1…7). Every SC was verified in the per-phase VERIFICATION.md (all `passed`). No orphaned requirements (none assigned), so no `unsatisfied` gate triggered.

## Cross-phase integration (the milestone's load-bearing claim)

Conceptual-coherence invariant — **one token issuer, one consent surface, one login path** — verified in code (gsd-integration-checker, 8/8 connected):

| Invariant | Wiring | Status |
|-----------|--------|--------|
| One login path | `device_verification_get` → `store_oauth_return_to` → `/auth/login` → `verify_magic_link` → `oauth_resume_redirect` → `/device` | WIRED |
| One consent surface | device approval reuses `get_csrf_token` + `ct_eq` + `Auth::id()` + `current_tenant()` from `consent.rs` | WIRED |
| One token issuer | device-code arm calls `build_claims` + `mint_token` identically to the auth-code arm (asserted by `device_grant_token_claims_identical_to_auth_code`) | WIRED |
| Discovery advertises device grant | `device_authorization_endpoint` + device-code URN in `grant_types_supported` | WIRED |
| Route-group parity with `/authorize` | `/device` under `SessionUserTenantResolver` + `TenantFailureMode::Allow` | WIRED |
| 204 independence | no shared imports/routes/session/cache namespaces with 202/203 | ISOLATED |

## E2E flows

- **Authorization-code via magic-link (same-device):** complete.
- **Device grant (cross-device):** complete — traced device_authorization → verification page → login-resume → consent/approve → token poll → tenant-scoped JWT.
- **Edge-case interruption:** an already-authenticated tab clicking a magic-link mid-device-grant is dropped by `GuestMiddleware` (WR-02, Phase 202). Non-blocking; primary flow unaffected.

## Tech debt (non-blocking)

1. **WR-02 / WR-03 (Phase 202):** `GET /auth/verify` under `GuestMiddleware` and `POST /auth/register` not calling `oauth_resume_redirect` — both edge-case resume interruptions documented in 202's verification, unchanged in 203. Candidates for a small hardening pass if passwordless/cross-device adoption surfaces them.
2. **Nyquist flags:** flip `nyquist_compliant` to true via `/gsd-validate-phase 202|203|204` if formal Nyquist sign-off is wanted (tests already green).

## Verdict

**tech_debt** — milestone goal achieved and shipped; the only open items are two pre-existing Phase-202 edge cases and the unflipped Nyquist flags. Safe to complete and archive (accept debt to backlog) or run a small hardening pass first.
