# Milestone v8.1: API DX Polish

**Status:** SHIPPED 2026-02-28
**Phases:** 83
**Total Plans:** 5

## Overview

Close the DX gaps between `ferro make:api` scaffold and a working MCP integration. Add API key CLI command, post-scaffold guidance, model/field selection, and x-mcp route-level customization.

## Phases

### Phase 83: API DX Polish

**Goal**: Fix five DX gaps discovered during v8.0 validation: (1) CLI command for API key generation, (2) model/field selection in make:api, (3) route-level x-mcp customization API, (4) post-scaffold next-steps guidance with MCP config snippet, (5) local API verification before MCP wiring.
**Depends on**: Phase 82
**Plans**: 5 plans in 2 waves

Plans:

- [x] 83-01: API Key CLI Command — `ferro make:api-key` generates keys with SHA-256 hashing, SQL/Rust snippets (8 tests)
- [x] 83-02: Route-Level x-MCP Customization — .mcp_tool_name(), .mcp_description(), .mcp_hint(), .mcp_hidden() builder methods with group defaults (5 tests)
- [x] 83-03: make:api Field Selection — --exclude, --include-all flags, auto-excludes 8 sensitive field patterns (8 tests)
- [x] 83-04: Local API Verification — `ferro api:check` validates connectivity, spec, auth in 4 sequential checks (7 tests)
- [x] 83-05: Post-scaffold Guidance & Docs — Enhanced output with setup steps and MCP config snippets, complete documentation

---

## Milestone Summary

**Key Decisions:**
- Key generation logic replicated (~20 lines) to keep ferro-cli independent from framework crate
- McpDefaults internal struct propagates group MCP settings to children; child overrides take precedence
- Exact match only for sensitive field patterns (no substring matching)
- validate_openapi_json extracted as pub fn for testability without HTTP mocking
- App name read from ./Cargo.toml with fallback to 'my-app' for personalized MCP config snippets

**Issues Resolved:**
- Parallel plan compilation conflicts resolved via stub files and #[cfg(test)] wrapper functions
- No issues deferred

**Technical Debt Incurred:**
- None

---

_For current project status, see .planning/ROADMAP.md_
