# cargo-audit configuration
# https://github.com/rustsec/rustsec/tree/main/cargo-audit

[advisories]
ignore = [
    # rsa: Marvin Attack timing sidechannel. No fix available.
    # Only pulled in via sqlx-mysql which Ferro does not use (Postgres/SQLite only).
    "RUSTSEC-2023-0071",

    # lru: IterMut Stacked Borrows unsoundness. Only used by aws-sdk-s3,
    # never calling IterMut in our code path.
    "RUSTSEC-2026-0002",
]
