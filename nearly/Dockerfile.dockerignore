# Build context ignore for `docker build -f nearly/Dockerfile .` (BuildKit reads
# this Dockerfile-scoped ignore, so other repo docker builds are unaffected).
.git/
**/target/
target/
**/node_modules/
nearly/public/
app/public/
**/*.db
**/*.db-journal
**/*.db-wal
